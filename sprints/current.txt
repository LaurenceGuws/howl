# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point coders and reviewers only at live active artifacts.

Sprint:

- `sprints/2026-06-13-pty-vt-folder-owner-structure-sprint.md`

Active research:

- `research/2026-06-13-pty-vt-folder-owner-structure-plan.md`

Active loops:

- `loops/pty-vt-folder-owner-structure-live-loop.txt`

Active restart note:

- The host folder/file owner structure sprint is closed and archived as done; its artifacts are historical receipts only.
- The active combined PTY+VT folder/file owner planning package is reviewer-accepted; implementation is authorized only through explicit seeded slices.
- Slice 1, `PTY proof-root relocation out of src`, is accepted in `howl-pty` `13b7fbd` and root `2718455`.
- Slice 2, `VT proof and non-product roots out of src`, is accepted in `howl-vt` `556ab69` and root `921f630`.
- Slice 3, `VT terminal core root establishment`, is accepted in `howl-vt` `20fb714` and root `4a4fec4`.
- Slice 4 and Slice 5 remaining plan is researcher-repaired and reviewer-accepted under the explicit final no-`terminal/` override; plan repair commit receipt root `27bcb75`.
- Slice 4, `VT protocol bucket removal toward final no-wrapper layout`, is accepted in `howl-vt` `538a96b` and root `3a3ea9d`.
- Slice 5, `Delete temporary VT terminal wrapper and finalize shallow src layout`, is accepted in `howl-vt` `4e76640`; root receipt pending.
- User direction: PTY is small, so PTY and VT can be cleaned up together in one sprint.
- Explicit user override receipt: user said, "ok slice 5 will be deleting terminal anmd moving all its content into src/ terminal makes the fodlers all 1 deeper for no reason. you can delegate until all that is done". This overrides the Ghostty-shaped `src/terminal/` subdomain pressure for the final Howl VT tree because the user rejected the extra folder depth as unjustified for Howl. Orchestrator session id: `orch-2026-06-13-pty-vt-folder-structure-01`.
- Reference pressure: Ghostty terminal and termio structure is primary for VT and PTY owner/folder shape; Alacritty terminal/event_loop/tty pressure is secondary where it sharpens PTY/runtime folder boundaries.
- Current PTY and VT trees still show likely folder/location discipline debt: mixed top-level files and child folders, likely weak test-root placement, and unresolved owner pressure across VT parser/control/host-facing folders plus PTY session/transport/test surfaces.
- Orchestrator session id: `orch-2026-06-13-pty-vt-folder-structure-01`.
- Researcher session id: `research-2026-06-13-pty-vt-folder-structure-01`.
- Reviewer session id: `review-2026-06-13-pty-vt-folder-structure-01`.
- Planning status: reviewer-accepted after repair.
- Planning commit-hash receipt: root `efbd9ad`.

Execution rule:

- No further coding is authorized until Slice 5 root receipt is committed and the sprint is closed.
- Only the active sprint, active loop, and active research artifact listed above are authoritative for the current step.
- Historical host/render artifacts are navigation only until fresh current-source and reference proof re-promotes a fact into the active research artifact.

Rule:

- If a sprint or loop is not active, it must not be treated as authoritative.
- If no accepted reviewer-gated slice exists, implementation is not authorized.
