# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point coders and reviewers only at live active artifacts.

Sprint:

- `sprints/2026-06-13-all-src-shallow-owner-structure-sprint.md`

Active research:

- `research/2026-06-13-all-src-shallow-owner-structure-plan.md`

Active loops:

- `loops/all-src-shallow-owner-structure-live-loop.txt`

Active restart note:

- The PTY+VT folder owner structure sprint is closed and archived as done; its artifacts are historical receipts only.
- The active step is Slice 2 execution under the all-`src/` shallow directory sprint.
- User direction: restart the sprint; make dirs in all `src/` dirs as shallow as possible; delete fake abstractions; treat the just-deleted `terminal/` folder as the model.
- Scope pressure: inspect every package `src/` tree in the workspace, identify fake abstraction directories and needless depth, then plan owner-true shallow cuts with exact slices.
- Orchestrator session id: `orch-2026-06-13-all-src-shallow-structure-01`.
- Researcher session id: `research-2026-06-13-all-src-shallow-structure-01`.
- Reviewer session id: `review-2026-06-13-all-src-shallow-structure-01`.
- Planning status: reviewer-accepted after repair.
- Planning seed commit-hash receipt: root `16a877d`.
- Planning package commit-hash receipt: root `c69e039`.
- Slice 1, `PTY src/pty/ Depth Deletion`, is accepted in `howl-pty` `714d41c` and root `3e0b50e`.
- Slice 2, `VT Fake Folder Deletion`, is accepted in `howl-vt` `da25b16` and root `861d909`.
- Slice 3, `Render Root Geometry And Text Classification Flattening`, is accepted in `howl-render` `d4445e3`; root receipt pending.

- Slice 4, `Host One-Extra-Depth Deletion`, is active and uncommitted.

Execution rule:

- Coding is authorized only from the condensed Slice 4 execution contract in `loops/all-src-shallow-owner-structure-live-loop.txt`.
- Only the active sprint, active loop, and active research artifact listed above are authoritative for the current step.
- Historical host/render/PTY/VT artifacts are navigation only until fresh current-source and reference proof re-promotes a fact into the active research artifact.

Rule:

- If a sprint or loop is not active, it must not be treated as authoritative.
- If no accepted reviewer-gated slice exists, implementation is not authorized.
