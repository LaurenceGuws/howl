# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/howl-linux-host-surface-layout-redesign.md`.
- Active loop artifact: `loops/howl-linux-host-surface-layout-loop.txt`.

Active work:

- Host terminal surface layout redesign for the tab-bar/viewport geometry bug.
- Orchestrator session id: `orch-2026-06-19-host-surface-layout-01`.
- Slice 1, snapped terminal surface geometry, is accepted and verified. It is receipt context only.
- Slice 2, display-owned terminal placement, is accepted and verified. It is receipt context only.
- Current executable slice: Slice 3, terminal input uses placed snapped terminal rect.

Current Slice 3 Problem:

- Display now owns snapped terminal placement for presentation.
- Input still uses raw content logical and pixel extents.
- Raw leftover pixels below/right of the snapped terminal grid can still be treated as terminal input and clamp to the last row/column.

Required Reference Pressure:

- Alacritty `SizeInfo.contains_point(...)` excludes padding/non-grid area from terminal grid input.
- Kitty uses explicit window geometry regions before rendering/input consequences.
- TigerBeetle/Howl law requires owner-true names, assertions, and proof.

Execution Gate:

- Reviewer must gate the Slice 3 section of `research/howl-linux-host-surface-layout-redesign.md` before coder execution.
- If reviewer accepts, coder implements only Slice 3 and records evidence in `loops/howl-linux-host-surface-layout-loop.txt`.
- If reviewer rejects, reviewer writes exact findings in the loop and the plan is repaired before coding.

Required Slice 3 Shape:

- Add `DisplayLayout.terminalLogicalSize(content_logical: Size, content_px: Size, terminal_px: Size) Size`.
- `terminalLogicalSize(...)` scales snapped terminal pixels into logical input extents, clamps to at least `1`, clamps to content logical extents, and asserts positive/fitting dimensions.
- Change `event.forwardTerminalInput(...)` to forward terminal logical width/height derived from snapped terminal texture size, not raw content logical size.
- Change `terminal/input.zig` mouse pixel mapping to use snapped `self.term.render.surface_layout.render_px`, not raw `self.geometry.content_px_w/content_px_h`.
- Keep terminal placement top-left inside content region.

Required Slice 3 Gates:

- `DisplayLayout.terminalLogicalSize(...)` test: content `960x570`, terminal `960x560`, logical `960x570` returns logical height `560`.
- Terminal input test proves a mouse event in the raw leftover strip below snapped terminal height is rejected rather than mapped to the last row.
- `zig build check` in `howl-linux-host`.
- `zig build test:unit` in `howl-linux-host`.
- Root `zig build check`.

Stop Conditions:

- Reviewer gate is missing.
- Coder needs files outside the Slice 3 allowed list in `research/howl-linux-host-surface-layout-redesign.md`.
- Pointer events outside the snapped terminal rect still map to terminal row/column.
- Fix requires VT, PTY, render C ABI, headers, or SDL event collection changes.

Artifact Rule:

- `sprints/current.txt` carries Slice 3 only as live execution scope.
- Earlier slices and deferred artifacts are navigation or receipt context only until explicitly re-promoted.
