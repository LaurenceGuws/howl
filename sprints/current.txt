# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/howl-product-event-semantics.md`.
- Active loop artifact: `loops/howl-product-event-semantics-loop.txt`.

Active execution:

- Product-wide event/interface vocabulary cleanup across all repos.
- Orchestrator session id: `orch-2026-06-19-product-event-semantics-01`.
- Researcher session id: `research-2026-06-19-product-event-semantics-01`.
- Reviewer session id: `review-2026-06-19-product-event-semantics-03`.
- Slice 1, host retained render admission vocabulary, is accepted and committed as `howl-linux-host d2bde33` and root `4120269`. It is receipt context only.
- Slice 2, host test-injection ops vocabulary, is accepted and committed as `howl-linux-host b53d381` and root `0675b44`. It is receipt context only.
- Slice 3, stale host render session mirror deletion, is accepted and committed as `howl-linux-host af7bf44` and root `74bc609`. It is receipt context only.
- Slice 4, documentation event vocabulary alignment, is accepted and committed as `howl-linux-host 3076c56`, `howl-render 1eae264`, and root `984af4c`. It is receipt context only.
- Current executable slice: Slice 5, render benchmark API case vocabulary correction.

Current Slice 5 Problem:

- The previous plan wrongly left render benchmark API/output vocabulary as `workload`.
- `howl-render/src/benchmark_main.zig` is an active render measurement surface and its CLI text/NDJSON output fields are product-facing enough to use exact benchmark-case vocabulary.

Required Reference Pressure:

- Alacritty governs host/runtime/display/presentation/render organization.
- Ghostty governs VT and embedding seam pressure.
- TigerBeetle requires exact names, bounded event loops/queues, assertions, and proof.
- Current ABI/interface shape is not assumed correct and is not protected from correction.

Required Slice 5 Shape:

- Rename render benchmark data shapes from `Workload*` to `Benchmark*`/`BenchmarkCase*` names.
- Rename builder and runner functions from `*Workload*` to `*BenchmarkCase*`/`*Case` names.
- Rename local variables from `workload(s)` to `benchmark_case(s)`.
- Rename text output `workload=` to `benchmark_case=`.
- Rename NDJSON field `workload` to `benchmark_case`.
- Leave VT benchmark/simulation workload terms for a separate pass unless explicitly promoted.

Allowed Files:

- `howl-render/src/benchmark_main.zig`.
- `loops/howl-product-event-semantics-loop.txt` for coder/reviewer evidence.

Required Gates:

- `cd howl-render && zig build check`.
- `cd howl-render && zig build test:unit`.
- `cd howl-render && zig build test:abi`.
- `cd howl-render && zig build benchmark:render:build`.
- Root `zig build check`.
- Render source/header grep for `work`/`workload` terms returns no matches.

Non-Goals:

- No renderer behavior changes.
- No render ABI/header changes beyond benchmark output labels.
- No VT benchmark/simulation workload term changes in this slice.
- No Apache/license term changes.

Stop Conditions:

- Render source/header grep still finds `work`/`workload` terms after the slice.
- Coder needs files outside the allowed list.

Artifact Rule:

- `sprints/current.txt` carries Slice 5 only as live execution scope.
- Later slices in `research/howl-product-event-semantics.md` are queued but not live until promoted here.
