# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/vt-render-state-maturity-plan.md`.
- Active loop artifact: `loops/vt-render-state-maturity-loop.txt`.

Active sprint note:

- Active sprint: VT render-state maturity subsprint.
- Orchestrator session id: `orch-2026-06-16-vt-render-state-planning-01`.
- Researcher session id: `researcher-2026-06-16-vt-render-state-plan-correction-02`.
- Reviewer session id: `reviewer-2026-06-16-vt-render-state-plan-rereview-02`.
- Current loop phase: planning accepted, execution not started.
- Coder is not authorized yet because orchestrator has not seeded Slice 1 for execution.
- Current goal: hold a worker-ready, reviewer-accepted slice queue for a Ghostty-derived VT render-state boundary before any host/render cleanup.
- Planning receipt: accepted by reviewer; commit-hash receipt is pending because user requested orchestration only and no commit was requested.

Start Gates:

- No renderer `VtSurface` cleanup before the VT render-state boundary exists and is tested.
- No host hover/highlight mutation cleanup before the VT render-state boundary exposes hover/highlight/selection facts honestly and is tested.
- No render input reshaping before host/render consumers can consume the new VT render-state boundary.
- No compatibility aliases, stale bridge endpoint, or optional `if` language. Boundary maturity is a start condition.

Authorized next step:

- Orchestrator may seed Slice 1 for a coder only after user authorizes execution.
- Slice 1 source is `research/vt-render-state-maturity-plan.md`, section `Slice 1: VT Render-State Owner And ABI Skeleton`.
- Reviewer for execution must remain `reviewer-2026-06-16-vt-render-state-plan-rereview-02` unless explicitly reassigned.

Artifact rule:

- Active `loops/`, `research/`, and `sprints/` contents must stay current-only.
- `research/` carries evidence and the explicit worker-ready slice plan.
- `loops/` carries the live team scratchpad, role notes, review verdicts, blockers, and receipts.
- `sprints/current.txt` carries the active planning contract only.
