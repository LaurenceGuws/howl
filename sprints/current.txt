# Current Scope Index

Purpose:

- Record whether a strict sprint is active.
- Point the agent at the live scoping artifacts when no strict sprint is active.

Current mode:

- No strict sprint is active.
- We are in a user <-> single-agent scoping loop for host I/O machinery.
- This is not a worker-ready slice queue.
- The goal is to define the full elephant slowly and correctly, then eat it byte by byte with the user.

Live artifacts:

- Active scoping loop: `loops/howl-host-io-machinery-loop.txt`.
- Active scoped index/research artifact: `research/howl-host-io-machinery.md`.

Current git state:

- Root has accountability updates for the multiplexing shape and host pointer.
- `howl-linux-host` is clean on `main` at `eca3b25 Extract VT cached buffers`.

Immediate accountability:

- Continue the host I/O machinery scoping from the source references.
- Build the event/consequence scoped index with the user before broad bucket dissection.
