# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/howl-render-surface-layout-present.md`.
- Active loop artifact: `loops/howl-render-surface-layout-present-loop.txt`.

Active execution:

- Render surface/layout/present ownership and naming across `howl-render`, `howl-linux-host`, root docs/accountability, and any required C ABI/header surfaces.
- Orchestrator session id: `orch-2026-06-19-render-surface-layout-present-01`.
- Researcher session id: `research-2026-06-19-render-surface-layout-present-01`.
- Reviewer session id: `review-2026-06-19-render-surface-layout-present-02`.
- Coder session id: `coder-2026-06-19-render-surface-layout-present-01`.
- Planning/root commit receipt: open; child render commit receipt: `howl-render` `5383bf6 Rename render layout and draw owners`.

Problem:

- Current Howl render/display vocabulary overlaps across `surface`, `scene`, `geometry`, and `grid`.
- Current APIs and cross-repo contracts are not frozen; wrong names and wrong boundaries must be changed explicitly.
- First host scope is Linux Wayland under KDE Wayland-only and Hyprland.
- SDL remains the pragmatic window/input/text-input/clipboard/event harness unless source-backed research proves it blocks required GPU-present control.
- We need Alacritty-style Wayland EGL present-damage control, and SDL's normal `SDL_GL_SwapWindow` hides that damage hook.

Required Reference Pressure:

- Alacritty governs host display, renderer organization, damage tracking, frame drawing, and Wayland EGL present damage.
- SDL official docs/source govern what SDL exposes and hides.
- Kitty/GLFW is secondary evidence for Linux/Wayland/EGL framework pressure and the historical Howl GLFW seam.
- Ghostty governs VT and embedding seam pressure only where Alacritty lacks the shape.
- TigerBeetle requires exact names, owner truth, assertions, bounded proof, and no bucket owners.

Current Live Slice:

- Execution Slice 1: rename render layout and prepared draw owners.

Execution Slice 1 Required Shape:

- Move `howl-render/src/geometry.zig` to `howl-render/src/layout.zig`.
- Move `howl-render/src/scene.zig` to `howl-render/src/text/draw_list.zig`.
- Move `howl-render/src/grid/scene.zig` to `howl-render/src/text/draw_primitives.zig`.
- Move `howl-render/src/grid/damage.zig` to `howl-render/src/text/damage.zig`.
- Move `howl-render/src/grid/rects.zig` to `howl-render/src/text/rect_primitives.zig`.
- Rename `Geometry` to `RenderLayout`.
- Rename `GeometryLayout` to `RenderLayoutInput`.
- Rename `GeometryResponse` to `RenderLayoutResponse`.
- Rename `PrepareLayout` to `PreparedLayout`.
- Rename `SurfacePixels` to `DrawablePixels`.
- Rename `SurfaceLayout` to `CellGridLayout`.
- Rename `SurfaceGeometryError` to `RenderLayoutError`.
- Rename `deriveGridForSurface` to `deriveCellGridForLayout`.
- Rename `deriveGridSize` to `deriveCellGridSize`.
- Rename `OwnedTextScene` to `OwnedTextDrawList`.
- Rename `BorrowedTextScene` to `BorrowedTextDrawList`.
- Rename `RetainedScratch` to `RetainedDrawScratch`.
- Rename `BuildOptions` to `DrawListOptions`.
- Rename `SceneAssembly` to `DrawListAssembly`.
- Rename `TextScene` to `TextDrawList`.
- Rename `GridMetrics` to `CellGridMetrics`.
- Do not change C ABI/header names in this slice.
- Do not preserve compatibility aliases named `TextScene` or the old layout names.

Allowed Files For Execution Slice 1:

- `howl-render/src/geometry.zig` -> `howl-render/src/layout.zig`.
- `howl-render/src/scene.zig` -> `howl-render/src/text/draw_list.zig`.
- `howl-render/src/grid/scene.zig` -> `howl-render/src/text/draw_primitives.zig`.
- `howl-render/src/grid/damage.zig` -> `howl-render/src/text/damage.zig`.
- `howl-render/src/grid/rects.zig` -> `howl-render/src/text/rect_primitives.zig`.
- `howl-render/src/libhowl_render.zig`.
- `howl-render/src/text/direct_normal.zig`.
- `howl-render/src/surface/surface_preparer.zig`.
- `howl-render/src/text/surface.zig`.
- `howl-render/src/benchmark_main.zig`.
- Existing owner-local tests embedded in moved files.
- Direct import updates in files that fail solely because of these moves.

Required Gates:

- Run the existing `howl-render` package test command.
- Grep gate: no `@import("grid/scene.zig")`.
- Grep gate: no `@import("grid/damage.zig")`.
- Grep gate: no `@import("grid/rects.zig")`.
- Grep gate: no public `TextScene`.
- Grep gate: no `GeometryLayout`.
- Grep gate: no `GeometryResponse`.
- Grep gate: no `SurfaceGeometryError` outside historical docs.
- Existing scene, damage, and rect tests must move with their owner and remain reachable from one curated root.

Non-Goals:

- No C ABI/header rename in this slice.
- No SDL/EGL code.
- No behavior change to draw primitives.
- No X11 implementation slice.
- No resurrection of the old GLFW backend unless source-backed research proves SDL blocks the required path and the user approves that direction explicitly.
- No preservation of stale API/ABI names by default.

Stop Conditions:

- Moving files requires changing C ABI/header names.
- Tests require duplicate roots.
- A required replacement name proves impossible without a new owner concept.
- References conflict on an owner boundary and no explicit user override receipt exists.
- Any old-name compatibility alias seems necessary.

Artifact Rule:

- `sprints/current.txt` carries Execution Slice 1 only as live execution scope.
- Coder must report child `howl-render` repo status and root repo status.
- Reviewer must identify a child commit hash or explicit uncommitted handoff.
- Orchestrator must close both root and child receipts before accepting the slice.

Execution status:

- Coder `coder-2026-06-19-render-surface-layout-present-01` implemented Slice 1 in `howl-render`.
- Verification passed: `zig build test:unit`, `zig build check`, `zig build test:abi`, and `zig build benchmark:render:build` in `howl-render`.
- Grep gates passed for stale grid/damage/rect imports and old public layout/draw owner names under `howl-render/src`.
- Review accepted by `review-2026-06-19-render-surface-layout-present-03` with no findings.
- Child render commit receipt: `5383bf6 Rename render layout and draw owners`.
- Root commit receipt: open until this artifact and the child pointer are committed.
