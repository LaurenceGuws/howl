# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/howl-render-cleanup-plan.md`.
- Active loop artifact: `loops/howl-render-cleanup-loop.txt`.

Active work:

- Renderer cleanup accountability sprint after VT render-state maturity closure.
- Orchestrator session id: `orch-2026-06-18-render-cleanup-accountability-01`.
- Prior VT render-state maturity sprint is closed by root `fb7b0f6 Track VT surface ABI deletion`.
- Accepted Recovery Slice 5 product receipts: `howl-vt` `39c5f7d`, `howl-render` `0f9d339`, `howl-linux-host` `876c21e`, root `a83bb52`.
- Accepted Recovery Slice 6 product receipts: `howl-vt` `f786252`, root `fb7b0f6`.

Current problem:

- `howl-render/src/vt_surface` survived as residual namespace debt after renderer consumption moved to VT render-state.
- The folder is not a valid endpoint. Live cursor-presentation facts must live under a true renderer/text owner, not under the rejected old VT-surface namespace.

Authorized cleanup:

- Move remaining live cursor presentation data/constants to a text-owned file.
- Repoint text scene/contract imports to the text-owned file.
- Delete `howl-render/src/vt_surface` entirely.
- Update active accountability notes so they no longer say Recovery Slice 6 is pending.

Required verification:

- `rg "vt_surface|VtSurface|vtSurface|HowlVtSurface|invalidateForVtSurface" howl-render/src howl-render/include`
- `zig build test:unit` in `howl-render`
- `zig build check` in `howl-render`
- `zig build check` at the workspace root after render package verification passes.

Stop conditions:

- Any old surface payload, compatibility shim, bridge, alias, or renderer-retained VT mirror is reintroduced.
- The cleanup requires host or VT ABI changes.
- The cleanup changes render behavior instead of moving/deleting stale ownership names.

Next planning target:

- Inventory `howl-render` for dead code, bucket nouns, fake lifecycle nouns, stale reference names, and breached folder ownership.
- Researcher must produce one exact deletion/naming slice before coder implementation.
- Reviewer must reject partial cleanup that preserves stale names, folders, comments, aggregate roots, or compatibility aliases.

Artifact rule:

- `sprints/current.txt` carries this live cleanup contract only.
- Historical VT render-state maturity details are archived in `loops/done/` and `research/done/`; they are not current execution instructions.
