# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/howl-render-cleanup-plan.md`.
- Active loop artifact: `loops/howl-render-cleanup-loop.txt`.

Active work:

- Renderer cleanup accountability sprint after VT render-state maturity closure.
- Orchestrator session id: `orch-2026-06-18-render-cleanup-accountability-01`.
- Prior VT render-state maturity sprint is closed by root `fb7b0f6 Track VT surface ABI deletion`.
- Accepted render cleanup slice receipts: `howl-render` `864ea8b Use C cursor cadence ABI directly`, root `f84e1c8 Track cursor cadence ABI cleanup`; `howl-render` `7af878b Delete dead render shapes`, root `6d0e393`; `howl-render` `d7f93b7 Delete cursor trail config bucket`, root `398da7f`.

Current problem:

- The cursor-trail `Config` cleanup slice is accepted, committed, and pushed.
- Current deterministic lanes in `research/howl-render-cleanup-plan.md` prove package-wide bucket metrics are zero, but cursor trail capacity is still duplicated as Zig literals in `howl-render/src/text/cursor_presentation.zig` and `howl-render/src/render_session.zig` while the C ABI already owns `HOWL_RENDER_CURSOR_TRAIL_RECTS_MAX`.
- The next executable slice must delete those duplicated Zig limit literals and route render-session copying through the text contract limit before broader live owner renames or folder moves.

Authorized cleanup:

- In `howl-render/src/text/cursor_presentation.zig`, replace `pub const max_cursor_trail_rects = 16;` with `pub const max_cursor_trail_rects = @import("howl_render_c").HOWL_RENDER_CURSOR_TRAIL_RECTS_MAX;`.
- In `howl-render/src/render_session.zig`, delete private `const max_cursor_trail_rects = 16;`.
- In `howl-render/src/render_session.zig`, change the `trail_rects` array repeat count and copy bound in `readCursorPresentation` to use `contract.max_cursor_trail_rects`.
- Do not touch C ABI files, shipped headers, `src/c/text_session.zig`, root `cursor_presentation.zig`, `text/contract.zig`, `text/scene.zig`, `text/scene_rects.zig`, `text/ft_hb/support.zig`, build files, README files, or benchmark files.

Required verification:

- `rg -n '^const max_cursor_trail_rects = 16;' howl-render/src/render_session.zig`
- `rg -n '^pub const max_cursor_trail_rects = 16;' howl-render/src/text/cursor_presentation.zig`
- `rg -n '\bmax_cursor_trail_rects\b' howl-render/src/render_session.zig howl-render/src/text/cursor_presentation.zig howl-render/src/text/contract.zig howl-render/src/text/scene.zig howl-render/src/text/scene_rects.zig`
- `rg -n 'HOWL_RENDER_CURSOR_TRAIL_RECTS_MAX' howl-render/include/howl_render.h howl-render/src/c/text_session.zig howl-render/src/cursor_presentation.zig howl-render/src/text/cursor_presentation.zig howl-render/src/render_session.zig`
- Before/after `python3 style.py --by-file --format json --sort prod howl-render`.
- Before/after `python3 style.py --by-file --format json --sort path howl-render/src/text/cursor_presentation.zig howl-render/src/render_session.zig`.
- Before/after forbidden-token content/path scans from `research/howl-render-cleanup-plan.md`.
- `zig build test:unit` in `howl-render`
- `zig build test:abi` in `howl-render`
- `zig build check` in `howl-render`
- `zig build check` at the workspace root after render package verification passes.

Stop conditions:

- Any private cursor trail limit literal remains in `render_session.zig` or `text/cursor_presentation.zig` after the change.
- `render_session.zig` imports `howl_render_c` only to reach cursor trail capacity instead of consuming `contract.max_cursor_trail_rects`.
- Worker needs files outside the allowed list in `research/howl-render-cleanup-plan.md`.
- The cleanup requires host, VT, or render C ABI/header changes.
- The cleanup changes cursor trail math, opacity behavior, animation behavior, scene rendering, or cadence validation behavior instead of deleting only duplicate limit ownership.
- Whole-package `style.py` `bucket_named_structs` and `bucket_struct_lines` stop being zero.

Next planning target:

- After this cursor trail limit authority cleanup slice is accepted, choose the next strongest deterministic target from `render_session.zig`, `text/ft_hb/support.zig`, scene contract pressure, duplicate mutex wrappers, special raster long functions, or current metric-dominant files.
- Reviewer must reject partial cleanup that preserves stale names, folders, comments, aggregate roots, dead tests, duplicate limits, or compatibility aliases.

Artifact rule:

- `sprints/current.txt` carries this live cleanup contract only.
- Historical VT render-state maturity details are archived in `loops/done/` and `research/done/`; they are not current execution instructions.
