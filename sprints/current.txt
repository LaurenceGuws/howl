# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point coders and reviewers only at live active artifacts.

Sprint:

- No accepted execution sprint seeded. Planning is active for runtime debug-noise cleanup.

Active research:

- `research/2026-06-14-runtime-debug-noise-cleanup-plan.md`

Active loops:

- `loops/runtime-debug-noise-cleanup-live-loop.txt`

Active restart note:

- Active planning scope: runtime debug-noise cleanup.
- Orchestrator session id: `orch-2026-06-14-runtime-debug-noise-01`.
- Researcher session id: `research-2026-06-14-runtime-debug-noise-01`.
- Reviewer session id: `review-2026-06-14-runtime-debug-noise-01`.
- No implementation is authorized until planning is accepted and execution slices are seeded.
- Previous latest closed sprint: host render-surface ownership.
- Latest closed sprint planning receipt: `455402d` `Accept host render-surface planning`.
- Latest closed sprint accepted slices:
  - Slice 1 `5c7a2a2` `Extract host render-surface resources`
  - Slice 2 `2977c3b` `Extract host render-surface commands`
  - Slice 3 `618d0c5` `Curate host render-surface root`
- Latest closed sprint root receipts:
  - `d1d8654` `Accept host render-surface Slice 1`
  - `afe558e` `Accept host render-surface Slice 2`
  - `bb28278` `Accept host render-surface Slice 3`
  - `cef2b37` `Archive host render-surface sprint`

Execution rule:

- No coding is authorized until the active planning package is reviewer-accepted and a slice execution contract is seeded.
- The host render-surface sprint execution is complete and archived.
- Only the active sprint, active loop, and active research artifact listed above are authoritative for the current step.
- Historical host/render/PTY/VT/all-src artifacts are navigation only until fresh current-source and reference proof re-promotes a fact into the active research artifact.

Rule:

- If a sprint or loop is not active, it must not be treated as authoritative.
- If no seeded active slice exists, implementation is not authorized.
