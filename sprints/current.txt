# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/howl-product-event-semantics.md`.
- Active loop artifact: `loops/howl-product-event-semantics-loop.txt`.

Active execution:

- Product-wide event/interface vocabulary cleanup across all repos.
- Orchestrator session id: `orch-2026-06-19-product-event-semantics-01`.
- Researcher session id: `research-2026-06-19-product-event-semantics-01`.
- Reviewer session id: `review-2026-06-19-product-event-semantics-03`.
- Slice 1, host retained render admission vocabulary, is accepted and committed as `howl-linux-host d2bde33` and root `4120269`. It is receipt context only.
- Current executable slice: Slice 2, host test-injection ops vocabulary.

Current Slice 2 Problem:

- Host test-injection seams use `RealOps`, which hides the concrete platform seam under a generic "real" namespace.
- PTY/runtime test names still use generic "work" wording where they prove progress turns, transport slices, runtime obligations, or locked feed byte caps.

Required Reference Pressure:

- Alacritty governs host/runtime/display/presentation/render organization.
- Ghostty governs VT and embedding seam pressure.
- TigerBeetle requires exact names, bounded event loops/queues, assertions, and proof.
- Current ABI/interface shape is not assumed correct and is not protected from correction.

Required Slice 2 Shape:

- Rename `RealOps` in `howl-linux-host/src/terminal/pty_pump.zig` to `TerminalProgressOps`.
- Rename `RealOps` in `howl-linux-host/src/terminal/pty_wait_thread.zig` to `ProgressThreadOps`.
- Rename `RealOps` in `howl-linux-host/src/terminal/vt_surface.zig` to `RenderStateCaptureOps`.
- Rename `RealOps` in `howl-linux-host/src/terminal/fonts.zig` to `FontSizeOps`.
- Keep `RealAckOps` unless implementation proves reviewer needs the exact `RenderStateAckOps` rename; accepted research says it is already role-specific enough.
- Rename PTY/runtime test names:
  - `progress drive keeps work bounded after saturated transport slice` -> `progress drive requests another turn after saturated transport slice`.
  - `progress drive requests redraw and next turn for runtime work` -> `progress drive requests redraw and next turn for runtime obligation`.
  - `transport pump caps locked feed work` -> `transport pump caps locked feed bytes`.
  - `progress thread drains kept work before waiting again` -> `progress thread drains kept turns before waiting again`.

Allowed Files:

- `howl-linux-host/src/terminal/pty_pump.zig`.
- `howl-linux-host/src/terminal/pty_wait_thread.zig`.
- `howl-linux-host/src/terminal/vt_surface.zig`.
- `howl-linux-host/src/terminal/fonts.zig`.
- `loops/howl-product-event-semantics-loop.txt` for coder/reviewer evidence.

Required Gates:

- `cd howl-linux-host && zig build test:unit -- "progress drive requests another turn after saturated transport slice"`.
- `cd howl-linux-host && zig build test:unit -- "progress drive requests redraw and next turn for runtime obligation"`.
- `cd howl-linux-host && zig build test:unit -- "transport pump caps locked feed bytes"`.
- `cd howl-linux-host && zig build test:unit -- "progress thread drains kept turns before waiting again"`.
- `cd howl-linux-host && zig build test:unit`.
- `cd howl-linux-host && zig build check`.
- Root `zig build check`.

Non-Goals:

- No PTY/VT behavior changes.
- No queue size or timing changes.
- No renderer ABI changes in this slice.
- No new test harnesses unless an existing harness needs a rename.

Stop Conditions:

- A rename exposes shared fake ops across unrelated seams; split the test-injection type locally rather than preserving a broad abstraction.
- Coder needs files outside the allowed list.

Artifact Rule:

- `sprints/current.txt` carries Slice 2 only as live execution scope.
- Later slices in `research/howl-product-event-semantics.md` are queued but not live until promoted here.
