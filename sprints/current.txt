# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point coder and reviewer only at live active artifacts.

Sprint:

- Active planning artifact: `research/2026-06-15-cursor-kitty-full-sprint-plan.md`.

Active loop:

- Active sprint loop artifact: `loops/cursor-kitty-full-sprint.txt`.

Active sprint note:

- Active sprint: rip out existing Howl cursor code and port Kitty's cursor code to Zig.
- Orchestrator session id: `orch-2026-06-15-cursor-restart-01`.
- Planning researcher session id: `ses_135074a6bffeuLKDxy30qK2cAL`.
- Planning reviewer session id: `ses_13508b5e7ffe0XZUyUcJd4EPEs`.
- Active sprint researcher session id: `ses_134441825ffenA2u9ugDoVS07f`.
- Active sprint reviewer session id: `ses_1344417eaffeA2DD2YSHiMCisr`.
- Active sprint coder session id: `ses_1344417c5ffehYHsYH3lQQog67`.
- User execution-team decision: start this sprint with one fresh teammate per role and reuse those task ids for the live sprint.
- User slice-acceptance decision: when a planned slice comes due, preexisting tree state for that slice may be reviewed for acceptance or required changes instead of being discarded automatically for predating this loop.
- Cursor-scoped rule: Kitty parity is primary; Ghostty may only be consulted if full Kitty cursor behavior cannot be modeled without ownership blurring.
- Last accepted slice: slice 1 accepted with reviewer `ses_1344417eaffeA2DD2YSHiMCisr`, coder `ses_1344417c5ffehYHsYH3lQQog67`, and `howl-vt` commit `76c179d`.
- Current stage: slice 2 is the active slice.

Active slice:

- Slice name: `cursor-kitty-slice-2-vt-abi-and-render-publication`.
- Slice class: execution slice 2 of 4.
- Slice source: `loops/cursor-kitty-full-sprint.txt` queue plus `research/2026-06-15-cursor-kitty-full-sprint-plan.md` evidence.
- Authorized next step: coder may implement only this active slice.

Exact goal:

- Carry explicit Kitty no-shape through the shipped VT C ABI and across all active render publication owners without reinterpretation.
- Preserve cursor-only damage and preparation semantics through the active render publication path.

Exact files allowed:

- `howl-vt/src/ffi/lifecycle.zig`
- `howl-vt/src/ffi/surface.zig`
- `howl-vt/include/howl_vt.h`
- `howl-vt/test/abi.zig`
- `howl-render/src/vt_publication/abi.zig`
- `howl-render/src/vt_publication/publication.zig`
- `howl-render/src/vt_publication/source_slot.zig`
- `howl-render/src/vt_publication/cursor.zig`
- `howl-render/src/vt_publication/text_input.zig`
- `howl-render/src/vt_publication/damage.zig`
- `howl-render/src/vt_publication/prepare_queue.zig`

Exact required shape:

- Widen the shipped VT cursor ABI to represent Kitty no-shape exactly.
- Widen the render publication ABI and all direct publication consumers to preserve no-shape without reinterpretation.
- Keep render publication consumer-only: no VT policy invention in render.
- Preserve cursor-only damage and preparation semantics through the publication path.

Slice invariants:

- No-shape is explicit shape truth, not collapsed hidden visibility.
- ABI widening must include both Zig translation and `howl-vt/include/howl_vt.h`.
- Render publication remains consumer-only and preserves VT truth unchanged.
- Cursor-only damage handling must remain proved, not silently widened into full-scene fallback.

Exact proof roots:

- `howl-vt/test/abi.zig`
- `howl-vt/src/ffi/lifecycle.zig` inline tests
- `howl-vt/src/ffi/surface.zig` inline tests
- `howl-render/src/vt_publication/abi.zig` inline tests
- `howl-render/src/vt_publication/publication.zig` inline tests
- `howl-render/src/vt_publication/source_slot.zig` inline tests
- `howl-render/src/vt_publication/cursor.zig` inline tests
- `howl-render/src/vt_publication/text_input.zig` inline tests
- `howl-render/src/vt_publication/damage.zig` inline tests
- `howl-render/src/vt_publication/prepare_queue.zig` inline tests

Verification command:

- `zig build test:abi` in `howl-vt`
- `zig build test` in `howl-render`

Exact non-goals:

- Rewriting VT savepoint ownership again.
- Host cadence consumption changes.
- Render draw primitive changes.
- Multiple cursors.
- Pointer cursor shape.

Exact stop conditions:

- Stop if no-shape is collapsed into hidden visibility or inferred block style.
- Stop if ABI widening is done in Zig only and not in `howl-vt/include/howl_vt.h`.
- Stop if render publication adds policy instead of preserving VT truth.
- Stop if cursor-only damage handling regresses into full-scene fallback without proof.

Artifact rule:

- Active `loops/`, `research/`, and `sprints/` contents must stay current-only.
- `research/` carries evidence and planning reasoning.
- `loops/` carries the live whole-sprint scratchpad and slice queue.
- `sprints/current.txt` carries the active slice contract only.
