# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point coders and reviewers only at live active artifacts.

Sprint:

- `sprints/2026-06-12-whole-workspace-minification-sprint.md`

Active research:

- `research/2026-06-12-whole-workspace-minification-plan.md`

Active loops:

- `loops/whole-workspace-minification-live-loop.txt`

Active restart note:

- Prior render API pragmatic shape artifacts are archived as done and are navigation only.
- The active whole-workspace minification planning package is reviewer-accepted.
- Slice 1, `render-emitter-duplicate-pass-collapse`, is accepted and committed in `howl-render` `49dfaf9` and root `33fecd5`.
- Slice 2, `render-generated-special-routing-parity-proof`, is accepted and committed in `howl-render` `c91ac5c` and root `2ffd424`.
- The active step is execution of Slice 3, `render-generated-special-raster-owner-deletion`.
- Coding is authorized only from the condensed execution contract in `loops/whole-workspace-minification-live-loop.txt`.

Execution rule:

- Only the active sprint, active loop, and active research artifact listed above are authoritative for the current step.
- The accepted research artifact is authoritative for the ordered minification slice plan.
- Deferred or done planning artifacts are navigation only until fresh current-source proof re-promotes a fact into the active research artifact.

Rule:

- If a sprint or loop is not active, it must not be treated as authoritative.
- If no sprint is active, execution is not authorized.
