# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/howl-render-surface-layout-present.md`.
- Active loop artifact: `loops/howl-render-surface-layout-present-loop.txt`.

Active execution:

- Render surface/layout/present ownership and naming across `howl-render`, `howl-linux-host`, root docs/accountability, and any required C ABI/header surfaces.
- Orchestrator session id: `orch-2026-06-19-render-surface-layout-present-01`.
- Researcher session id: `research-2026-06-19-render-surface-layout-present-01`.
- Reviewer session id: `review-2026-06-19-render-surface-layout-present-02`.
- Coder session id: open.
- Slice 1 child render commit receipt: `howl-render` `5383bf6 Rename render layout and draw owners`.
- Slice 1 root commit receipt: `71b72c7 Track render layout draw owner rename`.
- Slice 2 child render commit receipt: `howl-render` `6c08bf8 Rename render surface frame ABI`.
- Slice 2 child linux-host commit receipt: `howl-linux-host` `968d496 Rename host render texture ABI`.

Problem:

- Current Howl render/display vocabulary overlaps across `surface`, `scene`, `geometry`, and `grid`.
- Current APIs and cross-repo contracts are not frozen; wrong names and wrong boundaries must be changed explicitly.
- First host scope is Linux Wayland under KDE Wayland-only and Hyprland.
- SDL remains the pragmatic window/input/text-input/clipboard/event harness unless source-backed research proves it blocks required GPU-present control.
- We need Alacritty-style Wayland EGL present-damage control, and SDL's normal `SDL_GL_SwapWindow` hides that damage hook.

Required Reference Pressure:

- Alacritty governs host display, renderer organization, damage tracking, frame drawing, and Wayland EGL present damage.
- SDL official docs/source govern what SDL exposes and hides.
- Kitty/GLFW is secondary evidence for Linux/Wayland/EGL framework pressure and the historical Howl GLFW seam.
- Ghostty governs VT and embedding seam pressure only where Alacritty lacks the shape.
- TigerBeetle requires exact names, owner truth, assertions, bounded proof, and no bucket owners.

Current Live Slice:

- Execution Slice 2: C ABI packet and host texture rename. Completed and awaiting this root receipt commit.
- Next executable slice is not promoted in this artifact.

Execution Slice 2 Required Shape:

- Rename `HowlRenderSurface` to `HowlRenderSurfaceFrame`.
- Rename `HowlRenderHostSurface` to `HowlRenderHostTexture`.
- Rename `HowlRenderSurfaceToken` to `HowlRenderSurfaceFrameToken`.
- Keep `HowlRenderSurfaceRect` unchanged because it is a render-surface coordinate rect used by frame damage and commands.
- Rename `HowlRenderSurfaceDamageItem` to `HowlRenderSurfaceFrameDamageItem`.
- Rename `HowlRenderSurfaceDamageSpan` to `HowlRenderSurfaceFrameDamageSpan`.
- Rename `HowlRenderSurfaceCommand` to `HowlRenderSurfaceFrameCommand`.
- Rename `HowlRenderSurfaceCommandSpan` to `HowlRenderSurfaceFrameCommandSpan`.
- Rename constants with `HOWL_RENDER_SURFACE_*` only when they describe frame packet limits or commands to `HOWL_RENDER_SURFACE_FRAME_*`.
- Rename C ABI `HowlRenderGridSize` to `HowlRenderCellGrid`.
- Update Zig imports/usages directly.
- Do not add compatibility typedefs or aliases.

Allowed Files For Execution Slice 2:

- `howl-render/include/howl_render.h`.
- `howl-render/src/libhowl_render.zig`.
- `howl-render/src/surface/emitter.zig`.
- `howl-render/src/surface/emitter_test.zig`.
- `howl-render/src/surface/realizer.zig`.
- `howl-render/src/surface/realizer_test.zig`.
- `howl-render/src/surface/realizer_resource_store.zig`.
- `howl-render/src/surface/resource_store.zig`.
- `howl-render/src/text/surface.zig`.
- `howl-linux-host/src/howl_render_c.h`.
- `howl-linux-host/build.zig` only if translate-C include behavior breaks.
- `howl-linux-host/src/display/render_surface.zig`.
- `howl-linux-host/src/display/render_surface_commands.zig`.
- `howl-linux-host/src/display/render_surface_resources.zig`.
- `howl-linux-host/src/display/render_surface_test.zig`.
- `howl-linux-host/src/terminal/render_retained.zig`.
- `howl-linux-host/src/terminal/surface.zig`.
- `howl-linux-host/src/host_test_root.zig` only if test root imports require renaming.

Required Gates:

- Render and linux-host tests/builds pass.
- Grep gate for old C ABI names: `HowlRenderSurface`, `HowlRenderHostSurface`, `HowlRenderGridSize`, `HowlRenderSurfaceToken`, `HowlRenderSurfaceDamageItem`, `HowlRenderSurfaceDamageSpan`, `HowlRenderSurfaceCommand`, `HowlRenderSurfaceCommandSpan` except explicit historical notes inside `research/howl-render-surface-layout-present.md`.
- Compile gate for both nested repos.
- ABI assertions still prove packet bounds and span counts.

Non-Goals:

- No EGL damaged present implementation.
- No X11 implementation slice.
- No resurrection of the old GLFW backend unless source-backed research proves SDL blocks the required path and the user approves that direction explicitly.
- No preservation of old C ABI names by typedef or alias.

Stop Conditions:

- Translate-C generated bindings cannot be refreshed by normal build/test.
- Another repo outside allowed files depends on old names.
- Any old-name alias seems necessary.
- References conflict on an owner boundary and no explicit user override receipt exists.

Artifact Rule:

- `sprints/current.txt` carries Execution Slice 2 only as live execution scope.
- Coder must report child `howl-render`, child `howl-linux-host`, and root repo status.
- Reviewer must identify child commit hashes or explicit uncommitted handoffs.
- Orchestrator must close render, linux-host, and root receipts before accepting the slice.

Execution status:

- Slice 1 accepted and committed: `howl-render` `5383bf6`; root `71b72c7`.
- Slice 2 accepted after reviewer pass `review-2026-06-19-render-surface-layout-present-04` and committed in child repos: `howl-render` `6c08bf8`; `howl-linux-host` `968d496`.
- Slice 2 verification passed: `howl-render` `zig build test:unit`, `zig build check`, `zig build test:abi`, `zig build benchmark:render:build`; `howl-linux-host` `zig build test:unit`, `zig build check`; stale C ABI and host-surface vocabulary grep gates clean.
