# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point coder and reviewer only at live active artifacts.

Sprint:

- Active planning artifacts:
  - `research/2026-06-15-cursor-kitty-line-map.md`
  - `research/2026-06-15-cursor-kitty-amended-slices.md`

Active loop:

- Active sprint loop artifact: `loops/cursor-kitty-amended-reopen.txt`.

Active sprint note:

- Active sprint: rip out the code touching the cursor and port Kitty's cursor code.
- Orchestrator session id: `orch-2026-06-15-cursor-restart-01`.
- Researcher session id: `ses_134441825ffenA2u9ugDoVS07f`.
- Reviewer session id: `ses_1344417eaffeA2DD2YSHiMCisr`.
- Coder session id: `ses_1344417c5ffehYHsYH3lQQog67`.
- Accepted amended slice 1 receipt: `howl-vt` `72e5253`.
- Accepted amended slice 2 receipts: `howl-render` `1760cbd`, `howl-linux-host` `81152bb`.
- Accepted amended slice 3 receipt: `howl-linux-host` `6b4f3e7`.
- Accepted amended slice 4 receipt: `howl-vt` `02bdc3b`.
- Accepted frame-resync receipt: `howl-linux-host` `08fe547`.
- Current stage: narrow host wake/present frame-resync fix is the active slice.
- Slice-5 status: blocked pending later planning repair for Kitty multiple-cursor owner coverage.

Active slice:

- Slice name: `host-wake-present-starvation-frame-resync`.
- Slice class: narrow host regression fix inside current cursor sprint.
- Slice source: `research/2026-06-15-host-wake-present-starvation-frame-resync.md`.
- Authorized next step: coder may implement only this active slice.

Exact goal:

- Fix the host wake/present frame-resync regression where the real repro shows a first matched terminal frame, then an unmatched `host_damage`, then continued PTY wakes, and terminal frames still do not advance autonomously.
- Repair the centralized frame-pacing and loop-admission seam without reintroducing deleted cursor-event ownership.

Exact files:

- `howl-linux-host/src/display/frame_timer.zig`
- `howl-linux-host/src/event.zig`
- `howl-linux-host/src/display/present.zig`
- `howl-linux-host/src/terminal/surface_test.zig`

Exact required shape:

- Make `frame_timer.zig` return a finite follow-up wait through the existing `wait_ms` seam whenever terminal/present progression still requires another loop turn.
- Keep the owner boundary centered on `frame_timer.zig`, `event.zig`, and `present.zig`.
- Do not restore cursor-driven redraw/wait state to the event processor.
- Do not change `surface.zig`.
- Do not change `event_loop.zig`.

Exact proof roots:

- `howl-linux-host/src/display/frame_timer.zig`
- `howl-linux-host/src/display/present.zig`
- `howl-linux-host/src/event.zig`
- `howl-linux-host/src/terminal/surface_test.zig`

Verification command:

- `timeout 300s zig build test:unit` in `howl-linux-host`

Exact stop conditions:

- Stop if the fix masks the issue through cursor-only redraw ownership.
- Stop if the host loop can still stall terminal frame progression until user input.
- Stop if the real repro is still only being addressed by test rewriting.
- Stop if the diff changes `surface.zig`.
- Stop if the diff changes `event_loop.zig`.

Artifact rule:

- Active `loops/`, `research/`, and `sprints/` contents must stay current-only.
- `research/` carries evidence and planning reasoning.
- `loops/` carries the live whole-sprint scratchpad and slice queue.
- `sprints/current.txt` carries the active slice contract only.
