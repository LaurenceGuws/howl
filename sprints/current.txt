# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/howl-product-event-semantics.md`.
- Active loop artifact: `loops/howl-product-event-semantics-loop.txt`.

Active execution:

- Product-wide event/interface vocabulary cleanup across all repos.
- Orchestrator session id: `orch-2026-06-19-product-event-semantics-01`.
- Researcher session id: `research-2026-06-19-product-event-semantics-01`.
- Reviewer session id: `review-2026-06-19-product-event-semantics-03`.
- Slice 1, host retained render admission vocabulary, is accepted and committed as `howl-linux-host d2bde33` and root `4120269`. It is receipt context only.
- Slice 2, host test-injection ops vocabulary, is accepted and committed as `howl-linux-host b53d381` and root `0675b44`. It is receipt context only.
- Current executable slice: Slice 3, delete stale host render session mirror.

Current Slice 3 Problem:

- `howl-linux-host/src/render_session.zig` is unreferenced by host source/build and carries stale renderer-shaped ownership, including `SessionWorkState` and `workState(...)`.
- Renaming dead `SessionWorkState` would preserve false ownership. The correct Slice 3 action is deletion.
- Current source also shows dormant `howl-render` files importing missing `../render_session.zig`; that is a separate renderer structural cleanup, not this host stale-mirror deletion.

Required Reference Pressure:

- Alacritty governs host/runtime/display/presentation/render organization.
- Ghostty governs VT and embedding seam pressure.
- TigerBeetle requires exact names, bounded event loops/queues, assertions, and proof.
- Current ABI/interface shape is not assumed correct and is not protected from correction.

Required Slice 3 Shape:

- Delete `howl-linux-host/src/render_session.zig`.
- Do not add compatibility aliases, stubs, or replacement host-local session surfaces.
- Prove no host import or build root references `render_session.zig`, `SessionWorkState`, `TextSessionOwner`, or host `.workState(...)` after deletion.

Allowed Files:

- `howl-linux-host/src/render_session.zig`.
- `loops/howl-product-event-semantics-loop.txt` for coder/reviewer evidence.

Required Gates:

- `rg "render_session|SessionWorkState|TextSessionOwner|\.workState\(" howl-linux-host/src` returns no matches.
- `cd howl-linux-host && zig build test:unit`.
- `cd howl-linux-host && zig build check`.
- Root `zig build check`.

Non-Goals:

- No renderer file changes in this slice.
- No renderer ABI changes in this slice.
- No host replacement abstraction.
- No docs changes; docs are Slice 4.

Stop Conditions:

- Any host source or build root still imports `render_session.zig`.
- Deletion requires renderer source changes to keep host green.
- Coder needs files outside the allowed list.

Artifact Rule:

- `sprints/current.txt` carries Slice 3 only as live execution scope.
- Later slices in `research/howl-product-event-semantics.md` are queued but not live until promoted here.
