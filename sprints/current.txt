# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/vt-render-state-maturity-plan.md`.
- Active loop artifact: `loops/vt-render-state-maturity-loop.txt`.

Active sprint note:

- Active sprint: VT render-state maturity subsprint.
- Orchestrator session id: `orch-2026-06-16-vt-render-state-planning-01`.
- Researcher session id: `researcher-2026-06-17-render-state-cell-abi-repair-correction-12`.
- Reviewer session id: `reviewer-2026-06-17-render-state-cell-abi-repair-rereview-03`.
- Current loop phase: Recovery Slice 4B accepted; Recovery Slice 5 not seeded yet.
- Coder is blocked until orchestrator seeds Recovery Slice 5.
- Current goal: insert a VT-owned `HowlVtRenderStateCell` ABI maturity slice before Recovery Slice 5 so renderer consumption has no old `HowlVtSurfaceCell` dependency, clone, alias, or shim.
- Planning receipt: corrected recovery amendment committed in root `87f37fc Recover VT render state sprint plan`; Recovery Slice 5 scope repair committed in root `e114b80 Repair VT recovery slice scope`; Recovery Slice 5 hyperlink repair committed in root `b964014 Repair VT recovery hyperlink scope`; Recovery Slice 5 text-owner repair committed in root `4bd92d1 Repair VT recovery text owner scope`; Recovery Slice 4B render-state cell ABI repair committed in root `64bfb03 Plan VT render state cell ABI`.

Start Gates:

- No renderer `VtSurface` cleanup before the VT render-state boundary exists and is tested.
- No host hover/highlight mutation cleanup before the VT render-state boundary exposes public hover/highlight update and read facts honestly and is tested.
- No render input reshaping before host/render consumers can consume the new VT render-state boundary.
- No compatibility aliases, stale bridge endpoint, or optional `if` language. Boundary maturity is a start condition.

Authorized next step:

- Orchestrator may seed Recovery Slice 5 after recording Slice 4B root receipt.
- Slice 1 accepted receipt: `howl-vt` `aed729e Add VT render state ABI skeleton`.
- Slice 2 accepted receipt: `howl-vt` `a015e1e Populate VT render state from terminal`.
- Slice 3 accepted receipt: `howl-vt` `f6d2f8d Complete VT render state row reads`.
- Amended Slice 4 accepted receipt: `howl-vt` `cf6b80c Expose VT render state hover highlight update`.
- Recovery Slice 4B accepted receipt: `howl-vt` `eb2d944 Add VT render state cell ABI`.

Recovery planning amendment:

- Active recovery section: `Recovery Amendment: No Compatibility Payload Boundary` in `research/vt-render-state-maturity-plan.md`.
- Corrected next implementation slice if accepted: `Recovery Slice 4B: VT Render-State Cell ABI Maturity`.
- Recovery premise: Alacritty window/control pace requests full display rendering; CPU/GPU render work is not spun outside the presentation cadence. In Howl, the existing host `driveRenderLocked` prepare branch remains the pacing owner while renderer consumption changes from old `HowlVtSurfaceResult` to VT render-state C ABI in the same boundary slice.
- Recovery plan accepted by reviewer `reviewer-2026-06-17-vt-render-state-recovery-plan-rereview-02` and committed in root `87f37fc Recover VT render state sprint plan`; scope repair accepted and committed in root `e114b80 Repair VT recovery slice scope`; hyperlink repair accepted and committed in root `b964014 Repair VT recovery hyperlink scope`; text-owner repair accepted and committed in root `4bd92d1 Repair VT recovery text owner scope`.
- Recovery Slice 4B is accepted and committed.
- Active slice after orchestrator seeding: `Recovery Slice 5: Window-Paced Host And Renderer Consume VT Render State As One Boundary`.
- Exact Recovery Slice 4B allowed `howl-vt` files: `howl-vt/include/howl_vt.h`, `howl-vt/src/render_state.zig`, `howl-vt/src/ffi/render_state.zig`, `howl-vt/src/ffi/main.zig`, `howl-vt/test_ffi.zig`, `howl-vt/test/abi.zig`, `howl-vt/test_abi.zig`, `howl-vt/test_unit.zig`.
- Exact Recovery Slice 4B goal: choose Option B and keep the current unsized fixed-output row-cells getter contract: `howl_vt_render_state_row_cells_get(HowlVtRenderStateRowCellsHandle cells, HowlVtRenderStateRowCellsData data, void *out)` and `howl_vt_render_state_row_cells_get_multi(HowlVtRenderStateRowCellsHandle cells, size_t count, const HowlVtRenderStateRowCellsData *keys, void **values, size_t *out_written)`. Add fixed-size VT-owned public cell ABI declarations with no size field and no getter signature change. `HowlVtRenderStateCellFlags` fields are exactly `uint8_t continuation`, `uint8_t reserved0`, `uint8_t reserved1`, `uint8_t reserved2`. `HowlVtRenderStateCellAttrs` fields are exactly `uint8_t bold`, `uint8_t dim`, `uint8_t italic`, `uint8_t underline`, `uint8_t underline_color_set`, `uint8_t blink`, `uint8_t inverse`, `uint8_t invisible`, `uint8_t strikethrough`, `uint8_t reserved0`. `HowlVtRenderStateCell` fields are exactly `uint32_t codepoint`, `uint8_t combining_len`, `uint8_t reserved0`, `uint8_t reserved1`, `uint8_t reserved2`, `uint32_t combining[3]`, `HowlVtRenderStateCellFlags flags`, `HowlVtColor fg_color`, `HowlVtColor bg_color`, `HowlVtColor underline_color`, `uint8_t underline_style`, `uint8_t reserved3`, `uint8_t reserved4`, `uint8_t reserved5`, `HowlVtRenderStateCellAttrs attrs`, `uint16_t reserved6`, `uint32_t link_id`. There is no width field because current internal `RenderState.Cell` has no width owner.
- Exact Recovery Slice 4B color/RGB owner decision: public `HowlVtColor` and `HowlVtRgb8` are reusable leaf ABI values, not old surface payloads. `howl-vt/src/ffi/render_state.zig` must not import `ffi/surface.zig`; it must own local `FfiRenderStateColor { kind: u8, value: u32 }` and `FfiRenderStateRgb8 { r: u8, g: u8, b: u8 }` extern structs for render-state cell/colors FFI.
- Exact Recovery Slice 4B selection/highlight decision: `HowlVtRenderStateCell` must not include selected/highlighted bits; selection and hover styling remain separate row/cell reads through existing render-state selection/highlight APIs.
- Exact Recovery Slice 4B verification: `zig build test:abi -- render_state`, `zig build test:unit -- render_state`, `zig build test:abi -- surface`, and `zig build check` in `howl-vt`.
- Exact Recovery Slice 4B search gate: `rg "@import\\(\"surface\\.zig\"\\)|surface\\.|FfiSurfaceCell|HowlVtSurfaceCell" howl-vt/src/ffi/render_state.zig howl-vt/test/abi.zig howl-vt/test_ffi.zig` must show no render-state FFI/test dependency on old surface FFI or old surface cells.
- Exact Recovery Slice 4B stop conditions: render-state cell ABI still returns or tests `surface.FfiSurfaceCell`, `render_state.zig` imports `ffi/surface.zig` for any render-state FFI type including color/RGB, `HowlVtRenderStateCell` includes selected/highlighted bits or a width field, Slice 4B changes `howl_vt_render_state_row_cells_get` or `howl_vt_render_state_row_cells_get_multi` signatures instead of preserving the current unsized fixed-output contract, renderer-owned cell clones are introduced, old monolithic surface ABI is deleted early, ABI-root layout coverage is missing, null output and get-multi behavior are not tested, or the no-old-surface render-state search gate fails.
- Exact allowed `howl-vt` files for hyperlink repair only: `howl-vt/include/howl_vt.h`, `howl-vt/src/terminal.zig`, `howl-vt/src/ffi/hyperlink.zig`, `howl-vt/src/ffi/main.zig`, `howl-vt/src/libhowl_vt.zig`, `howl-vt/test_ffi.zig`, `howl-vt/test/abi.zig`, `howl-vt/test_abi.zig`, `howl-vt/test_unit.zig`.
- Exact allowed `howl-linux-host` files: `howl-linux-host/src/terminal/term.zig`, `howl-linux-host/src/terminal/vt_surface.zig`, `howl-linux-host/src/terminal/surface.zig`, `howl-linux-host/src/terminal/render_retained.zig`, `howl-linux-host/src/terminal/links.zig`, `howl-linux-host/src/terminal/surface_test.zig`.
- Exact allowed `howl-render` files: `howl-render/build.zig`, `howl-render/build.zig.zon`, `howl-render/include/howl_render.h`, `howl-render/src/c/prepare_request.zig`, `howl-render/src/c/prepare_request_test.zig`, `howl-render/src/c/test_support.zig`, `howl-render/src/libhowl_render.zig`, `howl-render/src/render_session.zig`, `howl-render/src/cursor_presentation.zig`, `howl-render/src/benchmark_main.zig`, `howl-render/src/vt_surface/surface.zig`, `howl-render/src/vt_surface/text_input.zig`, `howl-render/src/vt_surface/damage.zig`, `howl-render/src/vt_surface/cursor.zig`, `howl-render/src/vt_surface/theme.zig`, `howl-render/src/text/surface_preparer.zig`, `howl-render/src/text/direct_normal.zig`, `howl-render/src/text/shape/cluster.zig`, `howl-render/src/test_unit.zig`, `howl-render/src/test_abi.zig`.
- Exact goal: replace the live window-paced host-to-renderer prepare path with VT render-state C ABI consumption in one slice, deleting host copied-surface visible ownership, renderer `VtSurface` ownership, and old `HowlVtSurfaceResult` renderer prepare input together.
- Exact hyperlink decision: existing `howl_vt_terminal_copy_surface_hyperlink` is not acceptable as the live host path because source proves it is declared beside old surface APIs, implemented in `howl-vt/src/ffi/surface.zig`, and depends on host-provided old visible metadata/snapshot. Recovery Slice 5 must add `howl_vt_terminal_copy_visible_hyperlink(HowlVtHandle handle, uint64_t scrollback_offset, uint16_t row, uint16_t col, uint8_t *ptr, size_t cap)` in a non-surface VT FFI owner and move `links.zig` to that API without forcing render-state update or render prepare from mouse movement.
- Exact benchmark decision: `howl-render/src/benchmark_main.zig` is an explicit benchmark surface, not runtime product code, but `howl-render/build.zig` compiles it during `zig build check`; Recovery Slice 5 must include it and remove or convert old `vt_surface` benchmark workloads before the no-old-surface search gate.
- Exact text-owner decision: `howl-render/src/text/surface_preparer.zig` cannot be repaired without `howl-render/src/text/direct_normal.zig` and `howl-render/src/text/shape/cluster.zig` because current compiled source routes old `vt_surface`/`VtSurface`/`HowlVtSurfaceCell` paths through those owners. No separate sub-slice is allowed unless it builds and passes the no-old-surface gate without retaining old surface compatibility; current source does not, so these files remain in Recovery Slice 5.
- Exact verification: `zig build test:abi -- render_state`, `zig build test:unit -- render_state`, and `zig build check` in `howl-vt`; `zig build test:abi`, `zig build test:unit`, `zig build benchmark:render:build`, and `zig build check` in `howl-render`; `zig build test:unit -- terminal`, `zig build test:integration`, and `zig build check` in `howl-linux-host`; `zig build test:abi`, `zig build test:unit`, and `zig build check` at workspace root.
- Exact search gate: `rg "HowlVtSurfaceResult|HowlVtSurfaceCell|howl_vt_terminal_query_visible_meta|howl_vt_terminal_copy_surface|howl_vt_terminal_copy_surface_hyperlink|howl_vt_terminal_ack_surface|\bVtSurface\b|VtSurfaceCandidate|sourceRenderableTextFromVtSurface|buildSparseVtSurfaceCellsWithDamageScratch|countVtSurfaceCodepoints|vtSurfaceFromResult|applyHyperlinkHover|applyHostCursorCadenceToVtSurface" howl-linux-host howl-render` must show no host/render product-code, render text-owner, or compiled benchmark references; `rg "vt_surface" howl-render/src/benchmark_main.zig howl-render/src/cursor_presentation.zig howl-render/src/text/direct_normal.zig howl-render/src/text/shape/cluster.zig howl-render/src/text/surface_preparer.zig howl-linux-host/src/terminal/links.zig` must show no old-surface benchmark/input/presentation/text/link references; `rg "howl_vt_terminal_copy_visible_hyperlink" howl-linux-host howl-vt` must show the new host link use plus VT declaration/route/export/tests.
- Exact stop conditions: any compatibility payload, bridge, shim, temporary `HowlVtSurfaceResult`, old `HowlVtSurfaceResult` inside a new host visible owner, renderer `VtSurface`, renderer-retained VT cells/dirty arrays/colors/cursor/selection/highlights, `cursor_presentation.zig` importing or mutating `VtSurface`, `benchmark_main.zig` keeping old `vt_surface` workloads/imports or old `HowlVtSurfaceCell` benchmark construction, `surface_preparer.zig`, `direct_normal.zig`, or `shape/cluster.zig` retaining `vt_surface`, `VtSurface`, `VtSurfaceCandidate`, `HowlVtSurfaceCell`, `sourceRenderableTextFromVtSurface`, `buildSparseVtSurfaceCellsWithDamageScratch`, or `countVtSurfaceCodepoints`, a separate text-owner cleanup requiring old surface compatibility, `links.zig` importing `vt_surface.zig`, `links.zig` calling old visible metadata, `links.zig` calling `howl_vt_terminal_copy_surface_hyperlink`, new hyperlink ABI implemented in `ffi/surface.zig`, new hyperlink ABI requiring `snapshot_seq` from host, host copied-cell hover mutation, test-only public substitute, hidden render work outside host/window pace control, link hover/open causing render-state update or render prepare from mouse movement, render prepare from outside `.prepare_needed`, render ABI still accepting `HowlVtSurfaceResult`, host/render product code still calling `howl_vt_terminal_copy_surface` or `howl_vt_terminal_ack_surface`, render check requiring a post-slice benchmark cleanup to pass, or renderer reading `howl-vt` Zig internals.
- Blocker repaired in planning only: `direct_normal.zig` and `shape/cluster.zig` old text-owner paths are now in Recovery Slice 5 scope; accepted and committed in root `4bd92d1`.
- Blocker repaired in planning only: `links.zig` old hyperlink-copy path and old visible-surface metadata dependency are now in Recovery Slice 5 scope; accepted and committed in root `b964014`.
- Blocker repaired in planning only: coder found old renderer surface references in `howl-render/src/cursor_presentation.zig` and compiled benchmark references in `howl-render/src/benchmark_main.zig`, both outside the prior accepted allowed file list. They are now included in the repaired Recovery Slice 5 scope; accepted and committed in root `e114b80`.
- Compatibility payloads, bridges, old `HowlVtSurfaceResult` in a new visible owner, renderer `VtSurface`, host cell mutation, test-only public substitute, and render work outside host/window pace control are stop conditions.
- Old Slice 5, old Slice 6, and old Slice 7 from the prior plan are invalid and not executable from this sprint index.

Artifact rule:

- Active `loops/`, `research/`, and `sprints/` contents must stay current-only.
- `research/` carries evidence and the explicit worker-ready slice plan.
- `loops/` carries the live team scratchpad, role notes, review verdicts, blockers, and receipts.
- `sprints/current.txt` carries the active execution contract only.
