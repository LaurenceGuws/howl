# Active Sprint Index

Purpose:

- Track the current live sprint guidance only.
- Point researcher, reviewer, coder, and orchestrator only at live active artifacts.

Sprint:

- Active planning artifact: `research/howl-render-cleanup-plan.md`.
- Active loop artifact: `loops/howl-render-cleanup-loop.txt`.

Active work:

- Renderer cleanup accountability sprint after VT render-state maturity closure.
- Orchestrator session id: `orch-2026-06-18-render-cleanup-accountability-01`.
- Prior VT render-state maturity sprint is closed by root `fb7b0f6 Track VT surface ABI deletion`.
- Accepted render cleanup slice receipts: `howl-render` `864ea8b Use C cursor cadence ABI directly`, root `f84e1c8 Track cursor cadence ABI cleanup`; `howl-render` `7af878b Delete dead render shapes`, root `6d0e393`.

Current problem:

- The first two cleanup slices are accepted, committed, and pushed.
- Current deterministic lanes in `research/howl-render-cleanup-plan.md` prove exactly one package-wide `style.py` bucket offender: `howl-render/src/text/cursor_trail.zig` `Config`.
- The next executable slice must delete that bucket by passing exact cursor-trail decay fields directly before broader live owner renames or folder moves.

Authorized cleanup:

- Delete `pub const Config` from `howl-render/src/text/cursor_trail.zig`.
- Change `CursorTrail.update`, `updateCorners`, and `updateOpacity` to take exact `decay_fast_s` and/or `decay_slow_s` parameters instead of a generic `Config` bucket.
- Change the one product call in `howl-render/src/cursor_presentation.zig` to pass `self.cursor_trail_decay_fast_s` and `self.cursor_trail_decay_slow_s` directly.
- Update only the existing owner-local `cursor_trail.zig` tests that call `trail.update`.
- Do not touch C ABI files, shipped headers, `render_session.zig`, `text/ft_hb/support.zig`, scene contract files, scene rect files, build files, README files, or benchmark files.

Required verification:

- `rg -n '\bpub const Config\b|\bConfig\b' howl-render/src/text/cursor_trail.zig`
- `rg -n 'cursor_trail\.update\(\.\{' howl-render/src`
- `rg -n '\bcursor_trail\.Config\b|\btext_cursor_trail\.Config\b' howl-render/src howl-render/include`
- `rg -n '\bdecay_fast_s\b|\bdecay_slow_s\b' howl-render/src/text/cursor_trail.zig howl-render/src/cursor_presentation.zig`
- Before/after `python3 style.py --by-file --format json --sort prod howl-render`.
- Before/after `python3 style.py --by-file --format json --sort path howl-render/src/text/cursor_trail.zig howl-render/src/cursor_presentation.zig`.
- Before/after forbidden-token content/path scans from `research/howl-render-cleanup-plan.md`.
- `zig build test:unit` in `howl-render`
- `zig build test:abi` in `howl-render`
- `zig build check` in `howl-render`
- `zig build check` at the workspace root after render package verification passes.

Stop conditions:

- Any `Config` type or `cursor_trail.update(.{ ... })` anonymous bucket call remains after deletion.
- Worker finds product reachability for `cursor_trail.Config` as a named type outside the current source evidence.
- Worker needs files outside the allowed list in `research/howl-render-cleanup-plan.md`.
- The cleanup requires host, VT, or render C ABI changes.
- The cleanup changes cursor trail math, opacity behavior, animation behavior, or cadence validation behavior instead of deleting only the bucket shape.
- Whole-package `style.py` `bucket_named_structs` and `bucket_struct_lines` do not reach zero.

Next planning target:

- After this bucket deletion slice is accepted, choose the next strongest deterministic target from cursor duplicate/limit cleanup, `render_session.zig`, `text/ft_hb/support.zig`, scene contract pressure, duplicate mutex wrappers, or current metric-dominant files.
- Reviewer must reject partial cleanup that preserves stale names, folders, comments, aggregate roots, dead tests, or compatibility aliases.

Artifact rule:

- `sprints/current.txt` carries this live cleanup contract only.
- Historical VT render-state maturity details are archived in `loops/done/` and `research/done/`; they are not current execution instructions.
