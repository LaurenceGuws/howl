# Runtime Debug Noise Cleanup Live Loop

Authority:

- Live planning scratchpad for the runtime debug-noise cleanup sprint.
- Orchestrator session id: `orch-2026-06-14-runtime-debug-noise-01`.
- User handoff supplied 2026-06-14.
- No implementation is authorized until research planning is accepted by reviewer and exact slices are seeded.

Problem statement:

- Remove debugging-shaped noise from active runtime paths while preserving runtime truth, assertions, bounds, ABI behavior, and proof.
- Debugging-shaped noise includes timing structs, env-var-gated profiling branches, measurement counters, diagnostic result wrappers, and helper surfaces whose only job is observation rather than runtime truth.
- Prefer deletion over relocation. If measurement is benchmark-needed, benchmark/proof roots must own it outside runtime ownership.

Primary target order:

1. `howl-render/src/surface/emitter.zig`
2. `howl-render/src/render_session.zig`
3. `howl-linux-host/src/terminal/surface.zig`
4. `howl-render/src/surface/realizer.zig`

Planning contract:

- Researcher session id: `research-2026-06-14-runtime-debug-noise-01`.
- Reviewer session id: `review-2026-06-14-runtime-debug-noise-01`.
- Active research output path: `research/2026-06-14-runtime-debug-noise-cleanup-plan.md`.
- Research must produce exact block-level inventory for the four target files.
- Each candidate block must be classified as runtime truth, proof-only scaffolding, benchmark-only scaffolding, or stale migration residue.
- Each candidate block must receive an action: delete, move to proof/benchmark root, or retain as runtime truth.
- Planning must identify exact proof roots for each touched owner.
- Planning must produce ordered execution slices with exact allowed files, required shape, tests, non-goals, stop conditions, session ids, and receipt fields.

Hard rules:

- Keep assertions.
- Keep owner-true state.
- Keep proof roots passing.
- Do not invent `debug.zig`, `metrics.zig`, `instrumentation.zig`, `helpers.zig`, `types.zig`, or bucket files.
- Do not broaden into architecture redesign unless research proves debug posture hides a deeper false owner.
- Do not touch ABI headers unless explicitly required and re-approved.
- Do not move debug code sideways into another runtime owner.

Orchestrator note:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: seed a clean planning-only contract from the user handoff.
- Outcome: live loop now captures problem, target order, hard rules, research output, and planning gate.
- Handoff: researcher must read required workflow docs, `sprints/current.txt`, this loop, `reference-index.md`, TigerBeetle readings, current target files, and current proof roots before writing the active research package.

Researcher note:

- Hello from `research-2026-06-14-runtime-debug-noise-01`.
- Intent: produce the runtime debug-noise cleanup research package with exact target inventory, proof roots, and ordered execution slices.
- Outcome: wrote `research/2026-06-14-runtime-debug-noise-cleanup-plan.md`; inventory found heavy stale timing/debug residue in `emitter.zig`, `render_session.zig`, and host `terminal/surface.zig`, while `surface/realizer.zig` is currently runtime validation/drawing truth with no debug-shaped candidate blocks.
- Handoff: reviewer `review-2026-06-14-runtime-debug-noise-01` should review the plan hard, especially the required adjacent allowed files for `surface_preparer.zig`, benchmark timing, and host upload stats; blocker if planning is constrained to only the four primary files because that would leave renamed/sideways runtime noise.

Researcher repair note:

- Hello from `research-2026-06-14-runtime-debug-noise-01`.
- Intent: repair the reviewer rejection by adding the missed upstream direct-normal timing producer, tightening Slice 2 file/test scope, and making Slice 4 no-op only.
- Outcome: updated `research/2026-06-14-runtime-debug-noise-cleanup-plan.md` with `howl-render/src/text/direct_normal.zig` block inventory, explicit deletion of `direct_normal.Timings` from runtime rather than sideways relocation, exact Slice 2 allowed files, direct-normal proof roots, and no-edit realizer verification.
- Handoff: reviewer `review-2026-06-14-runtime-debug-noise-01` should re-review repaired Slice 2 and Slice 4; blocker remains if benchmark detailed phase timings are required without allowing a benchmark-owned redesign that does not preserve runtime timing payloads.

Reviewer acceptance note:

- Hello from `review-2026-06-14-runtime-debug-noise-01`.
- Intent: re-review repaired runtime debug-noise cleanup planning package.
- Outcome: accepted planning with no blocking findings. The repaired plan inventories `direct_normal.zig`, deletes runtime timing payloads instead of relocating them, tightens Slice 2 exact-file authority, adds direct-normal proof roots, and makes Slice 4 read-only/no-op verification.
- Handoff: orchestrator may record the accepted planning receipt and promote the plan into an execution sprint contract. No implementation is authorized until that execution contract is seeded with exact slice/coder receipt fields.

Orchestrator promotion note:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: promote accepted planning into a single active execution sprint contract.
- Outcome: accepted planning receipt `1ccaddd` is recorded; `research/2026-06-14-runtime-debug-noise-cleanup-plan.md` was archived to `research/done/2026-06-14-runtime-debug-noise-cleanup-plan.md`; active execution contract is `sprints/2026-06-14-runtime-debug-noise-cleanup-sprint.md`.
- Handoff: sprint seed receipt is `d52f6d2` `Seed runtime debug-noise sprint`. Slice 1 is ready to seed to coder `coder-2026-06-14-runtime-debug-noise-slice-01`. Slice 1 authority is `howl-render/src/surface/emitter.zig` plus `emitter_test.zig` only if compile/test expectations require access changes.

Slice 1 execution seed:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: seed Slice 1 exactly from the accepted sprint contract.
- Outcome: Slice 1 is authorized for coder `coder-2026-06-14-runtime-debug-noise-slice-01` only.
- Allowed files: `howl-render/src/surface/emitter.zig`, `howl-render/src/surface/emitter_test.zig` only if compile/test expectations require access changes.
- Required shape: no `monotonicNs`, no `DebugEmitPreparedTiming`, no env-var gate, no timing totals, no timing result wrappers, no debug global, no timing parameters. `emitPrepared`, `emitPreparedFresh`, `appendPreparedPass`, `appendPreparedSprites`, and `publishSurface` must read as direct runtime transformations with existing assertions preserved.
- Required tests from `howl-render`: `zig build test:unit -- surface/emitter`, `zig build test:unit`, and `zig build test:abi` if public compile shape changes indirectly affect ABI tests.
- Stop conditions: if removing timing requires changing render-surface ABI structs, stop; if a test only proves deleted timing, delete/rewrite that test in the allowed test file rather than preserving timing; if unrelated formatter churn touches other render files, stop and ask orchestrator.
- Required handoff fields: changed files, summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.

Coder Slice 1 note:

- Hello from `coder-2026-06-14-runtime-debug-noise-slice-01`.
- Intent: remove emitter runtime timing/debug noise while preserving direct emission transformations and assertions.
- Outcome: `howl-render/src/surface/emitter.zig` no longer contains `monotonicNs`, `DebugEmitPreparedTiming`, env-var timing gates, timing totals, debug globals, timing parameters, or timing result wrappers; targeted and full `howl-render` unit tests passed.
- Handoff: ready for reviewer `review-2026-06-14-runtime-debug-noise-01`; no blocker, no ABI test run because no public ABI/header shape changed.

Reviewer Slice 1 acceptance note:

- Hello from `review-2026-06-14-runtime-debug-noise-01`.
- Intent: review Slice 1 emitter cleanup against accepted sprint contract.
- Outcome: accepted with no blocking findings. Scope was clean, no residual emitter timing/debug surfaces remained, assertions and bounds stayed in publish/admission paths, and reported tests were sufficient.
- Handoff: orchestrator should verify `zig build test:unit -- surface/emitter` and `zig build test:unit`, then record nested commit and root acceptance receipts.

Orchestrator Slice 1 acceptance note:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: verify and accept Slice 1 after reviewer acceptance.
- Outcome: `zig build test:unit -- surface/emitter` passed from `howl-render`; `zig build test:unit` passed from `howl-render`; `zig build test:abi` was not required because no public ABI/header shape changed. Nested render commit is `932bd29` `Remove emitter debug timing noise`.
- Handoff: root acceptance receipt is `cd00a7e` `Accept runtime debug-noise Slice 1`; Slice 2 is next.

Slice 2 execution seed:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: seed Slice 2 exactly from the accepted sprint contract after Slice 1 root acceptance.
- Outcome: Slice 2 is authorized for coder `coder-2026-06-14-runtime-debug-noise-slice-02` only.
- Allowed files: `howl-render/src/render_session.zig`, `howl-render/src/text/surface_preparer.zig`, `howl-render/src/text/direct_normal.zig`, `howl-render/src/benchmark_main.zig`.
- Required shape: delete `DebugPrepareTiming`, `monotonicNs`, debug global, `prepareHandle` timing, `prepareSurface` timing, timing mutations, `surface_preparer.PrepareTimings`, and `direct_normal.Timings` runtime payloads. Benchmark may retain only benchmark-owned outer duration, allocation, fill/glyph/upload counts, and throughput.
- Required tests from `howl-render`: `zig build test:unit -- render session`, `zig build test:unit -- direct normal`, `zig build test:unit`, `zig build benchmark:render:build`, and `zig build check`.
- Stop conditions: if any file outside the four allowed files needs content changes, stop and reseed exact scope; if benchmark requirements require runtime timing payloads, stop for reviewer/user decision; if prepared ownership/mutex assertions are at risk, stop.
- Required handoff fields: changed files, benchmark timing ownership decision, summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.

Coder Slice 2 note:

- Hello from `coder-2026-06-14-runtime-debug-noise-slice-02`.
- Intent: remove render-session and text runtime prepare timing payloads while preserving prepared ownership, mutex discipline, counters, lane reports, and benchmark-owned observations.
- Outcome: deleted scoped runtime timing structs, timing fields, timing clocks, timing mutations, and benchmark detailed phase output; required render unit, benchmark build, and check commands passed.
- Handoff: ready for reviewer `review-2026-06-14-runtime-debug-noise-01`; no blocker, no commit touched by coder.

Reviewer Slice 2 rejection note:

- Hello from `review-2026-06-14-runtime-debug-noise-01`.
- Intent: review Slice 2 prepare timing cleanup against the accepted sprint contract and stop conditions.
- Outcome: rejected. `howl-render/src/surface/handle.zig:10-60` and `handle.zig:73-94` still contain adjacent prepare-handle runtime timing/debug noise called from `render_session.zig:474`, outside the Slice 2 allowed files. The Slice 2 stop condition required reseeding exact file scope.
- Handoff: orchestrator must stop Slice 2 acceptance and reseed exact scope for `surface/handle.zig`, or record an explicit reviewed deferral with receipt fields.

Orchestrator Slice 2 correction seed:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: restart from the earliest broken stage for the missed `surface/handle.zig` timing owner.
- Outcome: active correction research artifact is `research/2026-06-14-runtime-debug-noise-slice2-handle-correction.md`; Slice 2 remains unaccepted and the existing implementation diff remains uncommitted.
- Handoff: researcher must classify the exact handle timing blocks, choose delete/retain, name exact allowed files/tests/non-goals/stop conditions/receipt fields, and update the sprint/loop only after reviewer acceptance.

Researcher Slice 2 correction note:

- Hello from `research-2026-06-14-runtime-debug-noise-01`.
- Intent: classify the missed `surface/handle.zig` prepare-handle timing blocks and produce a precise reseed package for the rejected Slice 2.
- Outcome: wrote `research/2026-06-14-runtime-debug-noise-slice2-handle-correction.md`; it classifies `monotonicNs`, `DebugPreparedHandleCreateTiming`, `HOWL_RENDER_DEBUG_TIMING`, debug timing global, timing aggregates, timing locals, and timing print/record calls in `handle.zig` as stale migration residue to delete while retaining handle allocation, prepared-surface ownership transfer, registration, payload emission, emission-failure mapping, and lifecycle cleanup.
- Handoff: reviewer `review-2026-06-14-runtime-debug-noise-01` should review the correction. Slice 2 remains blocked until reviewer acceptance and orchestrator reseeding add `howl-render/src/surface/handle.zig` to the exact allowed files.

Reviewer Slice 2 correction acceptance note:

- Hello from `review-2026-06-14-runtime-debug-noise-01`.
- Intent: review the focused Slice 2 handle correction package.
- Outcome: accepted with no findings. The correction adds `howl-render/src/surface/handle.zig` to Slice 2, classifies the missed timing blocks exactly, preserves prepared-handle lifecycle/runtime truth, and names exact tests and stop conditions.
- Handoff: orchestrator may promote the correction into the active sprint contract and reseed Slice 2 repair.

Orchestrator Slice 2 repair seed:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: promote correction acceptance `e94864b` into the active sprint contract and reseed Slice 2 repair.
- Outcome: Slice 2 allowed files now include `howl-render/src/surface/handle.zig`; correction research moved to `research/done/2026-06-14-runtime-debug-noise-slice2-handle-correction.md`.
- Allowed files: `howl-render/src/render_session.zig`, `howl-render/src/text/surface_preparer.zig`, `howl-render/src/text/direct_normal.zig`, `howl-render/src/benchmark_main.zig`, `howl-render/src/surface/handle.zig`.
- Required additional shape: `handle.zig` must have no `monotonicNs`, no `DebugPreparedHandleCreateTiming`, no `HOWL_RENDER_DEBUG_TIMING`, no debug timing global, no aggregate timing fields, no timing locals in `PreparedHandle.create`, and no timing `std.debug.print`. Preserve handle allocation, prepared-surface ownership transfer, registration, payload emission, emission-failure mapping, lifecycle cleanup, and assertions.
- Required tests from `howl-render`: `zig build test:unit -- render session`, `zig build test:unit -- direct normal`, `zig build test:unit -- prepared handle`, `zig build test:unit`, `zig build benchmark:render:build`, and `zig build check`.
- Handoff: coder `coder-2026-06-14-runtime-debug-noise-slice-02` may repair the existing unaccepted Slice 2 diff only inside the five allowed files and then return normal handoff fields.

Coder Slice 2 repair note:

- Hello from `coder-2026-06-14-runtime-debug-noise-slice-02`.
- Intent: repair rejected Slice 2 by deleting the missed `surface/handle.zig` prepared-handle creation timing noise inside the corrected five-file scope.
- Outcome: `handle.zig` no longer has `monotonicNs`, `DebugPreparedHandleCreateTiming`, `HOWL_RENDER_DEBUG_TIMING`, debug timing globals, aggregate timing fields, timing locals, or timing prints; corrected render unit, prepared-handle, benchmark build, and check commands passed.
- Handoff: ready for reviewer `review-2026-06-14-runtime-debug-noise-01`; no blocker, no commit touched by coder.

Reviewer Slice 2 acceptance note:

- Hello from `review-2026-06-14-runtime-debug-noise-01`.
- Intent: review corrected Slice 2 prepare timing cleanup with `surface/handle.zig` included.
- Outcome: accepted with no findings. Scope was clean, forbidden timing/debug symbols were absent from corrected Slice 2 runtime owners, runtime ownership paths and benchmark-owned observations were preserved, and required loop handoff was present.
- Handoff: orchestrator should verify the corrected Slice 2 test set and record nested/root acceptance receipts.

Orchestrator Slice 2 acceptance note:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: verify and accept corrected Slice 2 after reviewer acceptance.
- Outcome: `zig build test:unit -- render session`, `zig build test:unit -- direct normal`, `zig build test:unit -- prepared handle`, `zig build test:unit`, `zig build benchmark:render:build`, and `zig build check` all passed from `howl-render`. Nested render commit is `3f54a58` `Remove prepare debug timing noise`.
- Handoff: root acceptance receipt is `69eba22` `Accept runtime debug-noise Slice 2`; Slice 3 is next.

Slice 3 execution seed:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: seed Slice 3 exactly from the accepted sprint contract after Slice 2 root acceptance.
- Outcome: Slice 3 is authorized for coder `coder-2026-06-14-runtime-debug-noise-slice-03` only.
- Allowed files: `howl-linux-host/src/terminal/surface.zig`, `howl-linux-host/src/terminal/surface_test.zig`, `howl-linux-host/src/display/render_surface.zig`, `howl-linux-host/src/display/render_surface_commands.zig`, `howl-linux-host/src/display/render_surface_resources.zig`, `howl-linux-host/src/display/render_surface_test.zig` only if compile/test expectations require stats signature changes.
- Required shape: `TurnResult`, `DriveResult`, and `SubmitPreparedResult` carry runtime decision facts only: work before/after, prepared flag, step, present snapshot, submit result, snapshot sequence. Delete upload timing, upload counts/bytes, dispatch/draw timings, retained-submit timing, `UploadStats`, optional stats sinks, and clocks. Preserve render action, present blocking, stale-handle checks, GL upload behavior, resource metadata commits, and assertions.
- Required tests from `howl-linux-host`: `zig build test:unit -- terminal surface`, `zig build test:unit -- render surface`, `zig build test:unit`, `zig build test:integration`, and `zig build check`.
- Stop conditions: if any non-test runtime caller requires upload timing/count fields for behavior, stop and classify that caller before proceeding; if GL upload correctness starts depending on observation structs, stop; if deletion would weaken present/submitted ordering assertions, stop.
- Required handoff fields: changed files, summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.

Coder Slice 3 note:

- Hello from `coder-2026-06-14-runtime-debug-noise-slice-03`.
- Intent: remove host terminal/render-surface upload timing and stats plumbing while preserving runtime render decisions, stale-handle checks, GL upload behavior, metadata commits, present blocking, and assertions.
- Outcome: deleted `UploadStats`, optional stats sinks, clocks, upload/dispatch/draw/retained-submit timing fields, and telemetry-only tests; required host unit, integration, and check commands passed.
- Handoff: ready for reviewer `review-2026-06-14-runtime-debug-noise-01`; no blocker, no commit touched by coder.

Reviewer Slice 3 acceptance note:

- Hello from `review-2026-06-14-runtime-debug-noise-01`.
- Intent: review Slice 3 host terminal/render-surface upload stats cleanup.
- Outcome: accepted with no findings. Scope was clean, forbidden telemetry/debug plumbing was absent, runtime decision facts and GL/resource/present invariants were preserved, and tests were rewritten around runtime facts instead of telemetry.
- Handoff: orchestrator should verify the Slice 3 host test set and record nested/root acceptance receipts.

Orchestrator Slice 3 acceptance note:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: verify and accept Slice 3 after reviewer acceptance.
- Outcome: `zig build test:unit -- terminal surface`, `zig build test:unit -- render surface`, `zig build test:unit`, `zig build test:integration`, and `zig build check` all passed from `howl-linux-host`. Nested host commit is `8a2f1d7` `Remove host upload stats noise`.
- Handoff: root acceptance receipt is `9eddb77` `Accept runtime debug-noise Slice 3`; Slice 4 read-only realizer verification is next.

Slice 4 execution seed:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: seed Slice 4 exactly from the accepted sprint contract after Slice 3 root acceptance.
- Outcome: Slice 4 is authorized for coder `coder-2026-06-14-runtime-debug-noise-slice-04` only.
- Allowed files: none for editing. Read-only inspection of `howl-render/src/surface/realizer.zig` is allowed. Required loop note is allowed.
- Required shape: no-op verification only. Confirm no timing structs, env-var profiling, measurement counters, diagnostic wrappers, or observation-only helpers remain. Assertions, validation, retained resource checks, and drawing helpers remain untouched.
- Required tests from `howl-render`: `zig build test:unit -- realizer`; prior `zig build test:unit` remains supporting proof.
- Stop conditions: if coder or reviewer finds a real debug-only block missed by research, stop with no edits and reseed exact block action before any `realizer.zig` change; if any proposed deletion is an invariant or ABI validation, stop.
- Required handoff fields: inspection result, verification commands/results, blockers/deviations, coder session id, explicit no-code-change handoff status.

Coder Slice 4 note:

- Hello from `coder-2026-06-14-runtime-debug-noise-slice-04`.
- Intent: perform the read-only `realizer.zig` verification and prove no debug-noise cleanup edit is needed.
- Outcome: inspected `howl-render/src/surface/realizer.zig`; no timing structs, env-var profiling, measurement counters, diagnostic wrappers, or observation-only helpers remain. `zig build test:unit -- realizer` passed from `howl-render`.
- Handoff: no implementation files changed; ready for reviewer/orchestrator no-code-change handoff.

Reviewer Slice 4 acceptance note:

- Hello from `review-2026-06-14-runtime-debug-noise-01`.
- Intent: review Slice 4 read-only realizer verification.
- Outcome: accepted with no findings. No implementation files changed; `realizer.zig` is runtime realization, validation, drawing, bounds, resource, upload, geometry, and pixel helper logic with no debug-shaped timing or observation scaffolding found.
- Handoff: orchestrator should rerun `zig build test:unit -- realizer` and record no-code-change acceptance.

Orchestrator Slice 4 acceptance note:

- Hello from `orch-2026-06-14-runtime-debug-noise-01`.
- Intent: verify and accept Slice 4 after reviewer acceptance.
- Outcome: `zig build test:unit -- realizer` passed from `howl-render`. No implementation files changed for Slice 4.
- Handoff: root no-code-change acceptance receipt is pending this commit; sprint execution is complete after receipt closure.
