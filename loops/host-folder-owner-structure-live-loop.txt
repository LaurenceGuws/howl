Host folder owner structure live loop scratchpad

Date: 2026-06-13.

Role owner: orchestrator.

Status: active planning loop.

Orchestrator session id: `orch-2026-06-13-host-folder-structure-01`.

Researcher session id: `research-2026-06-13-host-folder-structure-01`.

Reviewer session id: `review-2026-06-13-host-folder-structure-01`.

Loop commit-hash receipt: planning package closed in root commit `ff58fa8`.

Shared scratchpad rule:

- This file is the live team scratchpad for the current step.
- Every non-trivial orchestrator, researcher, reviewer, or coder pass must append a short note with role/session id, hello, intent, one outcome, and next handoff or blocker.

Controlling sprint:

- `/home/home/personal/projects/howl/sprints/2026-06-13-host-folder-owner-structure-sprint.md`

Active research target:

- `/home/home/personal/projects/howl/research/2026-06-13-host-folder-owner-structure-plan.md`

Current step:

- The host folder/file owner structure planning package is accepted and committed in root `ff58fa8`.
- Slice 1, `Control-Spine Root Cleanup`, is seeded for execution.
- Coder may execute only the active condensed execution contract below.

Exact user direction:

- The next offender for the same cleanup is the host.
- Clean up host file and folder locations and naming with discipline and intent.
- Keep only curated owner units in `src`.
- Use shallow child folders only for true subdomains or per-owner definitions.
- Take primary pressure from Alacritty host/runtime/display/window/input structure.

Exact non-goals:

- No implementation during planning.
- No sprint narrowing, v1 language, or partial cleanup unless the user explicitly narrows scope.
- No Howl-only folder inventions while Alacritty, Ghostty, TigerBeetle, Kitty, or official docs provide source-backed pressure.
- No fake compatibility wrappers or preserving dead directories for comfort.

Exact stop conditions:

- Stop if the active research cannot name exact allowed files, required shapes, tests, non-goals, stop conditions, and receipt fields for each slice.
- Stop if current source and references conflict in a way that needs explicit user override.
- Stop if active `loops/`, `research/`, or `sprints/` contain stale non-archive artifacts.
- Stop if the plan collapses into rename theater instead of accountable owner/folder boundary work.

Team scratchpad

1. Role: orchestrator
- Session id: `orch-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: seed the new host folder/file owner structure sprint with reviewer-gated planning and no implementation authority.
- How one task went: confirmed the render folder sprint is closed; current `howl-linux-host/src` top-level tree looks broader and less intentional than Alacritty's host tree, with multiple likely weak roots and top-level C headers; the next step is a source-backed host folder/file structure plan, not ad hoc movement.
- Next handoff or blocker: researcher must produce the full source-backed host folder/file structure plan; reviewer gates it before any coding.

2. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: gate the host folder/file owner structure planning package.
- How one task went: after several narrow planning corrections for exact import-rewire allowlists, internal target-tree consistency, and exact later-slice file lists, the reviewer accepted the full planning package. The slices are now fully explicit, the `gl_c.h` target path is singular, the target tree is internally consistent, and the work is real host owner/folder-boundary restructuring rather than rename theater.
- Outcome: accept.
- First executable slice expectations: orchestrator may seed only Slice 1 from `research/2026-06-13-host-folder-owner-structure-plan.md:267-276` after closing the planning commit-hash receipt; Slice 1 must keep the coder inside that exact allowlist, require `zig build test:unit` and `zig build test:integration`, preserve the cited event-loop and control-spine assertions, and demand coder session id plus commit-hash receipt closure.
- Next handoff or blocker: orchestrator should record reviewer acceptance and stop before execution until the planning commit-hash receipt is closed.

3. Role: orchestrator
- Session id: `orch-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: record reviewer acceptance and stop before execution until the planning commit-hash receipt is closed.
- How one task went: active sprint, research, and current index now mark the host folder/file owner structure plan as reviewer-accepted with planning commit-hash receipt pending; no coder is authorized because the receipt is not closed.
- Next handoff or blocker: commit the accepted planning package, then seed only Slice 1.

Condensed execution contract for coder session `coder-2026-06-13-host-folder-structure-01`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-host-folder-owner-structure-plan.md`
- Implement only Slice 1, `Control-Spine Root Cleanup`.
- Allowed files:
  - `howl-linux-host/src/main.zig`
  - `howl-linux-host/src/app/processor.zig`
  - `howl-linux-host/src/cli/args.zig`
  - `howl-linux-host/src/event_loop.zig`
  - `howl-linux-host/src/polling/window_wake.zig`
  - `howl-linux-host/src/terminal/context.zig`
  - `howl-linux-host/src/terminal/input.zig`
  - `howl-linux-host/src/terminal/scrollbar.zig`
  - `howl-linux-host/src/terminal/pty/wait_thread.zig`
  - `howl-linux-host/src/terminal/render/surface_layout.zig`
  - `howl-linux-host/src/host_test_root.zig`
  - `howl-linux-host/src/integration_test_root.zig`
  - `howl-linux-host/build.zig`
- Required shape:
  - move `src/app/processor.zig` to `src/event.zig`.
  - move `src/cli/args.zig` to `src/cli.zig`.
  - move `src/event_loop.zig` to `src/polling/event_loop.zig`.
  - rewire every direct import site that currently names `event_loop.zig`, specifically `src/main.zig`, `src/terminal/context.zig`, `src/terminal/input.zig`, `src/terminal/scrollbar.zig`, `src/terminal/pty/wait_thread.zig`, `src/terminal/render/surface_layout.zig`, and `src/host_test_root.zig`.
  - update build references so `app/` and `cli/` become removable and `polling/` becomes a real subdomain with more than one owner.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:integration`
- Required assertions:
  - preserve the wake-event assertions and event-turn bound from current `event_loop.zig:31-37`, `event_loop.zig:56-64`, and `event_loop.zig:101-105`.
  - preserve the host control-spine tab bounds and quit-path assertions from `app/processor.zig:134-154`, `app/processor.zig:216-249`, and all existing inline tests that move with the files.
- Exact non-goals:
  - no tab-model redesign.
  - no new runtime abstractions.
  - no behavior changes to event processing.
  - no new public host API.
- Exact stop conditions:
  - stop if the move cannot be completed without inventing a second event owner.
  - stop if `polling/` still ends the slice as a one-file folder.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-host-folder-structure-01`
  - commit-hash handoff status for orchestrator receipt closure

4. Role: orchestrator
- Session id: `orch-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: close the accepted planning receipt and seed Slice 1 for execution.
- How one task went: the accepted planning package was committed as root `ff58fa8`, the active sprint/research/current index now record that receipt, and Slice 1 has an exact condensed execution contract with allowed files, required shape, tests, non-goals, stop conditions, and receipt demand.
- Next handoff or blocker: coder may execute only `coder-2026-06-13-host-folder-structure-01`; no broader slice or product-code work is authorized.

2. Role: researcher
- Session id: `research-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: produce the active host folder/file owner structure research artifact with source-backed slices, exact allowed files, required shapes, tests, non-goals, stop conditions, and receipt demands.
- How one task went: re-proved the current host offenders from `main.zig`, `build.zig`, the current test roots, and the host subtrees; the strongest plan is to collapse weak roots toward Alacritty-style `event`, `display`, `input`, `config`, and `polling` owners, use Ghostty pressure to rename `terminal/context.zig` to a surface owner, and finish by moving translated C headers and curated test roots out of `src` top level.
- Next handoff or blocker: reviewer should gate `research/2026-06-13-host-folder-owner-structure-plan.md`; no implementation is authorized unless the reviewer accepts the full slice plan.

3. Role: researcher
- Session id: `research-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: correct the rejected plan by expanding Slice 1 and Slice 2 to include all direct import rewires and by resolving `gl_c.h` to one exact final path and one exact slice.
- How one task went: re-read the cited host files and confirmed the missing rewires at `terminal/context.zig`, `terminal/input.zig`, `terminal/scrollbar.zig`, `terminal/context_test.zig`, `terminal/links.zig`, and `host_test_root.zig`; corrected the slice allowlists and required-shape text so those import sites are explicitly in-scope, and locked `gl_c.h` to final path `src/c/gl_c.h` moved only in Slice 4.
- Next handoff or blocker: reviewer should re-gate the corrected active research artifact; no implementation is authorized.

4. Role: researcher
- Session id: `research-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: correct the second rejection by making the final target tree internally consistent and resolving `gl_c.h` ownership to one truth everywhere in the plan.
- How one task went: removed the duplicate second `src/c/` block from the target tree and corrected the owner-role prose so `display/` no longer claims the GL helper header; `gl_c.h` now belongs only to the translated-C seam at final path `src/c/gl_c.h`, consistent with the slice contracts.
- Next handoff or blocker: reviewer should re-gate the corrected active research artifact; no implementation is authorized.

5. Role: researcher
- Session id: `research-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: correct the third rejection by replacing the remaining wildcard allowlist language in Slice 3 and Slice 4 with exact file lists.
- How one task went: re-read the active slice text plus the current `build.zig`, `display/display.zig`, and the known `terminal/context` import sites; rewrote Slice 3 to enumerate the exact terminal owner files and direct rewire sites, and rewrote Slice 4 to enumerate the exact old/new C-header files, old/new test-root files, build file, and direct `gl_c` import sites without any wildcard wording.
- Next handoff or blocker: reviewer should re-gate the corrected active research artifact; no implementation is authorized.

6. Role: researcher
- Session id: `research-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: correct the execution-exposed Slice 1 defect by expanding its allowlist to cover every file needed for the `event_loop.zig` move and proof closure.
- How one task went: re-read `terminal/pty/wait_thread.zig` and `terminal/render/surface_layout.zig`, then re-checked current `event_loop.zig` import users across `src/`; the missing stale users were exactly those two files, so Slice 1 now explicitly allows them and names them in the required event-loop rewire set alongside `main.zig`, `terminal/context.zig`, `terminal/input.zig`, `terminal/scrollbar.zig`, and `host_test_root.zig`.
- Next handoff or blocker: reviewer should re-gate the corrected active planning artifact; no implementation is authorized.

3. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: review the active host folder/file owner structure plan against the planning contract and reject any slice that still leaves coder invention on file moves, imports, tests, or stop conditions.
- How one task went: rejected the plan. Finding 1: Slice 1 is under-scoped on exact allowed files. `research/2026-06-13-host-folder-owner-structure-plan.md:269-275` requires moving `src/event_loop.zig` to `src/polling/event_loop.zig`, but current import rewiring also has to touch `howl-linux-host/src/terminal/context.zig:2`, `howl-linux-host/src/terminal/input.zig:2`, and `howl-linux-host/src/terminal/scrollbar.zig:2`, which are outside the Slice 1 allowlist. Finding 2: Slice 2 is also under-scoped on exact allowed files. `research/2026-06-13-host-folder-owner-structure-plan.md:280-286` moves `window_chrome/window.zig`, `display/renderer/*`, and `tab_bar/slots.zig`, but required import rewiring also reaches `howl-linux-host/src/terminal/context.zig:3,5`, `howl-linux-host/src/terminal/context_test.zig:7`, `howl-linux-host/src/terminal/links.zig:6`, and `howl-linux-host/src/host_test_root.zig:7,12-13`, which are not all authorized there. Finding 3: the GL header path contract is not exact enough for execution. The proposed final tree puts `gl_c.h` under `src/c/` at `research/2026-06-13-host-folder-owner-structure-plan.md:183-188`, but Slice 2 still moves it under `display/` at `:280-281` and Slice 4 allows `src/display/gl_c.h or the equivalent moved file from Slice 2` at `:302-303`, leaving the worker without one exact path contract.
- Next handoff or blocker: researcher must correct the slice contracts so every required import site and moved path is explicitly authorized, with one exact `gl_c.h` relocation story across slices, then return for reviewer re-gate.

4. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: re-review the corrected plan against the prior allowlist and `gl_c.h` findings and re-check the full planning gate.
- How one task went: rejected the corrected plan. Finding 1: the prior slice-contract defects are fixed. Slice 1 now explicitly authorizes the real `event_loop.zig` import rewires at `research/2026-06-13-host-folder-owner-structure-plan.md:276-277`, and Slice 2 now explicitly authorizes the real window/render-surface rewires at `:287-288`, while `gl_c.h` is locked to Slice 4 at `:309-310` and `:341`. Finding 2: the active target tree is still internally inconsistent, so the plan is not yet an exact execution contract. `research/2026-06-13-host-folder-owner-structure-plan.md:179-247` lists `src/c/` twice, with a second duplicate `c/` block at `:241-246`. That leaves the authoritative target shape wrong on its face. Finding 3: owner truth for `gl_c.h` is still contradictory inside the same plan. `research/2026-06-13-host-folder-owner-structure-plan.md:168` says `display/` owns the GL helper header, but `:174`, `:184-189`, `:309-310`, and `:341` say `c/` is the single translated-C seam and `gl_c.h` ends at `src/c/gl_c.h`. The researcher must resolve that owner statement so the final owner/package boundary is singular.
- Next handoff or blocker: researcher must correct the active research artifact so the required target tree appears exactly once and the owner-role prose matches the one exact `gl_c.h` final home at `src/c/gl_c.h`, then return for reviewer re-gate.

5. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: re-review the corrected plan for final internal consistency and re-check the full planning gate.
- How one task went: rejected the plan. Finding 1: the remaining internal consistency blockers are fixed. The target tree now has one `src/c/` block at `research/2026-06-13-host-folder-owner-structure-plan.md:177-241`, and `gl_c.h` ownership/location is now singular at `:168-175`, `:303-305`, and `:335`. Finding 2: the full planning gate is still not met because Slice 3 does not name exact allowed files. `research/2026-06-13-host-folder-owner-structure-plan.md:292` allows `every file in howl-linux-host/src/terminal/`, which violates the workflow requirement for exact allowed files per slice from `loop/flow.md:24-31`. Finding 3: Slice 4 also fails the exact-allowlist requirement. `research/2026-06-13-host-folder-owner-structure-plan.md:303` ends with `and any direct import sites`, which leaves file scope open-ended and forces coder reconstruction instead of an exact execution contract.
- Next handoff or blocker: researcher must replace the wildcard Slice 3 terminal allowlist and the Slice 4 `any direct import sites` allowance with exact file lists, then return for reviewer re-gate.

6. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: re-review the corrected Slice 3 and Slice 4 allowlists and re-check the full planning gate one more time.
- How one task went: accepted the plan. Finding 1: Slice 3 now has a fully explicit allowlist at `research/2026-06-13-host-folder-owner-structure-plan.md:289-298`, replacing the prior wildcard terminal scope with exact files. Finding 2: Slice 4 now has a fully explicit allowlist at `research/2026-06-13-host-folder-owner-structure-plan.md:300-309`, replacing the prior open-ended import-site wording with exact files. Finding 3: the full planning gate is now met: the plan is sequential and each slice names exact allowed files, required shape, required tests, non-goals, stop conditions, session ids, and commit-hash receipt demand at `research/2026-06-13-host-folder-owner-structure-plan.md:267-309`, with sprint-wide assertions, tests, risks, proof gaps, and readiness recorded at `:311-342`.
- Next handoff or blocker: orchestrator may seed Slice 1 only. The execution contract should preserve the Slice 1 file boundary from `research/2026-06-13-host-folder-owner-structure-plan.md:270-276`, require `zig build test:unit` and `zig build test:integration`, preserve the cited event-loop and control-spine assertions, and demand coder session id plus commit-hash receipt closure.

7. Role: coder
- Session id: `coder-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: execute only Slice 1 by moving `app/processor.zig` to `event.zig`, `cli/args.zig` to `cli.zig`, `event_loop.zig` to `polling/event_loop.zig`, and rewiring the exact authorized import and build paths.
- How one task went: completed the authorized file moves and rewires in `src/main.zig`, `src/event.zig`, `src/cli.zig`, `src/polling/event_loop.zig`, `src/terminal/context.zig`, `src/terminal/input.zig`, `src/terminal/scrollbar.zig`, `src/host_test_root.zig`, and `build.zig`; the preserved event-loop assertions and moved inline tests remained in place. Verification then failed at an out-of-scope import still pointing to the removed top-level path: `howl-linux-host/src/terminal/pty/wait_thread.zig:3` imports `../../event_loop.zig`, so `zig build test:unit` and `zig build test:integration` both stop with `src/event_loop.zig:1:1: error: unable to load 'event_loop.zig': FileNotFound`.
- Next handoff or blocker: blocked on execution-contract scope. Fixing the failing import requires touching `howl-linux-host/src/terminal/pty/wait_thread.zig`, which is outside Slice 1's allowed files. Commit-hash handoff status: none; git not touched.

8. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: review the Slice 1 planning correction after execution exposed missing `event_loop.zig` import users and confirm whether Slice 1 may be reseeded.
- How one task went: accepted the planning correction. Finding 1: Slice 1 now explicitly allows the additional stale import users `howl-linux-host/src/terminal/pty/wait_thread.zig` and `howl-linux-host/src/terminal/render/surface_layout.zig` at `research/2026-06-13-host-folder-owner-structure-plan.md:270`, and the required shape names both files in the exact `event_loop.zig` rewire set at `:271`. Finding 2: current source proof matches that correction: the remaining stale `event_loop.zig` import users are exactly `howl-linux-host/src/terminal/pty/wait_thread.zig:3` and `howl-linux-host/src/terminal/render/surface_layout.zig:2`, while the other known users already point at `polling/event_loop.zig`. Finding 3: no new planning vagueness or scope drift was introduced. The correction is still Slice 1 only, keeps the exact file list and exact rewire set, and does not authorize broader implementation beyond reseeding Slice 1.
- Next handoff or blocker: orchestrator may reseed Slice 1 and reuse the current worktree state, with execution limited to the corrected Slice 1 contract and the missing rewires plus re-verification only.

9. Role: orchestrator
- Session id: `orch-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: reseed Slice 1 after the accepted planning correction for missing `event_loop.zig` import users.
- How one task went: the live Slice 1 contract now matches the accepted research allowlist and required rewires for `terminal/pty/wait_thread.zig` and `terminal/render/surface_layout.zig`, so execution can resume without forcing out-of-scope edits.
- Next handoff or blocker: commit the accepted planning correction, then rerun Slice 1 under the refreshed contract.

10. Role: coder
- Session id: `coder-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: execute Slice 1, `Control-Spine Root Cleanup`, under the refreshed allowlist.
- How one task went: moved `app/processor.zig` to `event.zig`, `cli/args.zig` to `cli.zig`, and `event_loop.zig` to `polling/event_loop.zig`; rewired every authorized direct `event_loop.zig` import site including `terminal/pty/wait_thread.zig` and `terminal/render/surface_layout.zig`; updated `build.zig` so `cli/` is no longer build-owned and `polling/` now has more than one owner.
- Next handoff or blocker: reviewer/orchestrator can verify the Slice 1 diff under the accepted contract.

11. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: review execution Slice 1, `Control-Spine Root Cleanup`, against allowed-file scope, owner moves, import rewires, preserved assertions, and proof receipts.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must inspect `git status` and the exact `howl-linux-host` diff to confirm only the accepted Slice 1 file set is included; confirm the recorded `zig build test:unit` and `zig build test:integration` receipts match the final worktree; record the Slice 1 commit hash in the loop/sprint receipts before seeding Slice 2.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested host Slice 1 change, record the root receipt, and seed exactly Slice 2.

12. Role: orchestrator
- Session id: `orch-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: close Slice 1 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:integration`; `polling/` now contains `event_loop.zig` and `window_wake.zig`; accepted implementation is committed in `howl-linux-host` as `6e77a10` (`Clean host control spine roots`).
- Commit-hash receipt: `slice-1-control-spine-root-cleanup=6e77a10`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root Slice 1 receipt and seed Slice 2 from the accepted research plan.

13. Role: coder
- Session id: `coder-2026-06-13-host-folder-structure-02`
- Hello: hi
- Intended task: implement Slice 2, `Display, Window, And Tab-Visual Owner Cleanup`, inside the allowed files only.
- How one task went: moved the contracted visual owners into `display/` and `terminal/`, rewired the authorized direct import sites in `terminal/context.zig`, `terminal/context_test.zig`, `terminal/links.zig`, and `host_test_root.zig`, updated owning runtime/build imports, and removed `window_chrome/` and `tab_bar/`. The deferred `gl_c.h` seam was left alone under `display/renderer/` per the explicit non-goal.
- Next handoff or blocker: reviewer/orchestrator can verify the Slice 2 diff under the accepted contract.

14. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: review execution Slice 2, `Display, Window, And Tab-Visual Owner Cleanup`, against allowed-file scope, owner moves, rewires, preserved assertions/tests, and proof receipts.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must inspect `git status` and the exact `howl-linux-host` diff to confirm only the accepted Slice 2 file set is included; confirm the recorded `zig build test:unit` and `zig build test:integration` receipts match the final Slice 2 worktree; record the Slice 2 commit hash in the loop/sprint receipts before seeding Slice 3.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested host Slice 2 change, record the root receipt, and seed exactly Slice 3.

15. Role: orchestrator
- Session id: `orch-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: close Slice 2 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:integration`; the authorized moves landed, `window_chrome/` and `tab_bar/` are gone, and the only remaining `display/renderer/` presence is the deferred `gl_c.h` seam; accepted implementation is committed in `howl-linux-host` as `5998100` (`Clean host display roots`).
- Commit-hash receipt: `slice-2-display-window-tab-owners=5998100`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root Slice 2 receipt and seed Slice 3 from the accepted research plan.

16. Role: coder
- Session id: `coder-2026-06-13-host-folder-structure-03`
- Hello: hi
- Intended task: implement Slice 3, `Terminal Surface Naming Cleanup`, inside the allowed files only.
- How one task went: renamed `terminal/context.zig` to `terminal/surface.zig`, renamed `terminal/context_test.zig` to `terminal/surface_test.zig`, updated the authorized imports and test references, and preserved the terminal-surface initialization, deinit, geometry, and tab-slot assertion/test surfaces.
- Next handoff or blocker: reviewer/orchestrator can verify the final Slice 3 diff under the accepted contract.

17. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: review execution Slice 3, `Terminal Surface Naming Cleanup`, against allowed-file scope, owner rename, preserved child folders, assertions/tests, and proof receipts.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must inspect `git status` and the exact `howl-linux-host` diff to confirm only the accepted Slice 3 file set is included; confirm the recorded `zig build test:unit` and `zig build test:integration` receipts match the final Slice 3 worktree; record the Slice 3 commit hash in the loop/sprint receipts before recording the final nested host commit and sprint-close receipt.
- Next handoff or blocker: orchestrator may close the host sprint receipt.

18. Role: orchestrator
- Session id: `orch-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: close Slice 3 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:integration`; the terminal owner now reads as `surface.zig`/`surface_test.zig` with `terminal/pty/`, `terminal/render/`, and `terminal/vt/` preserved; accepted implementation is committed in `howl-linux-host` as `e053085` (`Name host terminal surface`).
- Commit-hash receipt: `slice-3-terminal-surface-naming=e053085`.
- Final sprint proof hash: nested `howl-linux-host` `e053085`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root final sprint receipt closure.

16. Role: coder
- Session id: `coder-2026-06-13-host-folder-structure-03`
- Hello: hi
- Intended task: execute only Slice 3 by renaming the embedded terminal owner from `terminal/context.zig` to `terminal/surface.zig`, renaming its test file to `terminal/surface_test.zig`, rewiring the authorized imports/test references, and rerunning the required host tests.
- How one task went: completed the owner rename and rewires in the authorized files. `src/terminal/context.zig` moved to `src/terminal/surface.zig`, `src/terminal/context_test.zig` moved to `src/terminal/surface_test.zig`, the exported owner now reads as `Surface`, `src/event.zig`, `src/display/present.zig`, `src/terminal/tab_slots.zig`, `src/host_test_root.zig`, and `src/integration_test_root.zig` now use `TerminalSurface`/`surface` references, and `build.zig` now names the surface test artifact accordingly. The terminal-surface init/deinit/geometry assertions and existing surface tests were preserved, and the tab-slot bounds/lifecycle assertions remained intact after the rename. Exact verification results: `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:unit` succeeded; `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:integration` succeeded.
- Next handoff or blocker: reviewer/orchestrator can review Slice 3 against the accepted contract. Commit-hash handoff status: none; git not touched.

Condensed execution contract for coder session `coder-2026-06-13-host-folder-structure-03`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-host-folder-owner-structure-plan.md`
- Implement only Slice 3, `Terminal Surface Naming Cleanup`.
- Allowed files:
  - `howl-linux-host/src/terminal/context.zig`
  - `howl-linux-host/src/terminal/context_test.zig`
  - `howl-linux-host/src/terminal/cursor_blink.zig`
  - `howl-linux-host/src/terminal/input.zig`
  - `howl-linux-host/src/terminal/links.zig`
  - `howl-linux-host/src/terminal/scrollbar.zig`
  - `howl-linux-host/src/terminal/selection.zig`
  - `howl-linux-host/src/terminal/tab_slots.zig`
  - `howl-linux-host/src/terminal/term.zig`
  - `howl-linux-host/src/terminal/pty/pump.zig`
  - `howl-linux-host/src/terminal/pty/session.zig`
  - `howl-linux-host/src/terminal/pty/wait_thread.zig`
  - `howl-linux-host/src/terminal/render/font_size.zig`
  - `howl-linux-host/src/terminal/render/fonts_linux.zig`
  - `howl-linux-host/src/terminal/render/retained.zig`
  - `howl-linux-host/src/terminal/render/surface_layout.zig`
  - `howl-linux-host/src/terminal/vt/input.zig`
  - `howl-linux-host/src/terminal/vt/retained.zig`
  - `howl-linux-host/src/terminal/vt/surface.zig`
  - `howl-linux-host/src/main.zig`
  - `howl-linux-host/src/event.zig`
  - `howl-linux-host/src/display/present.zig`
  - `howl-linux-host/src/display/tab_bar.zig`
  - `howl-linux-host/src/display/window.zig`
  - `howl-linux-host/src/input/input.zig`
  - `howl-linux-host/src/host_test_root.zig`
  - `howl-linux-host/src/integration_test_root.zig`
  - `howl-linux-host/build.zig`
- Required shape:
  - rename `terminal/context.zig` to `terminal/surface.zig`.
  - rename `terminal/context_test.zig` to `terminal/surface_test.zig`.
  - update all imports and test references.
  - keep the current `terminal/pty/`, `terminal/render/`, and `terminal/vt/` child folders.
  - ensure the renamed owner reads as the embedded terminal surface owner under Ghostty pressure rather than a generic context bucket.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:integration`
- Required assertions:
  - preserve terminal-surface initialization, deinit, and geometry assertions from `terminal/context.zig:137-219`.
  - preserve existing surface tests.
  - preserve any bounds or lifecycle assertions in `terminal/tab_slots.zig` after the rename.
- Exact non-goals:
  - no PTY, VT, or render behavior changes.
  - no surface lifecycle redesign beyond owner naming and import rewiring.
- Exact stop conditions:
  - stop if the rename reveals that `surface.zig` is not the real owner and would need a broader runtime redesign not already scoped here.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-host-folder-structure-03`
  - commit-hash handoff status for orchestrator receipt closure

Condensed execution contract for coder session `coder-2026-06-13-host-folder-structure-02`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-host-folder-owner-structure-plan.md`
- Implement only Slice 2, `Display, Window, And Tab-Visual Owner Cleanup`.
- Allowed files:
  - `howl-linux-host/src/display/display.zig`
  - `howl-linux-host/src/display/frame_timer.zig`
  - `howl-linux-host/src/display/layout.zig`
  - `howl-linux-host/src/display/renderer/render_surface.zig`
  - `howl-linux-host/src/display/renderer/render_surface_test.zig`
  - `howl-linux-host/src/display/renderer/rects.zig`
  - `howl-linux-host/src/window_chrome/window.zig`
  - `howl-linux-host/src/window_chrome/icon.zig`
  - `howl-linux-host/src/window_chrome/stb_image.c`
  - `howl-linux-host/src/tab_bar/tab_bar.zig`
  - `howl-linux-host/src/tab_bar/slots.zig`
  - `howl-linux-host/src/app/present.zig`
  - `howl-linux-host/src/main.zig`
  - `howl-linux-host/src/event.zig`
  - `howl-linux-host/src/terminal/context.zig`
  - `howl-linux-host/src/terminal/context_test.zig`
  - `howl-linux-host/src/terminal/links.zig`
  - `howl-linux-host/src/host_test_root.zig`
  - `howl-linux-host/src/integration_test_root.zig`
  - `howl-linux-host/build.zig`
- Required shape:
  - move `app/present.zig` to `display/present.zig`.
  - move `window_chrome/window.zig` to `display/window.zig`.
  - move `window_chrome/icon.zig` and `window_chrome/stb_image.c` under `display/`.
  - move `display/renderer/render_surface.zig`, `display/renderer/render_surface_test.zig`, and `display/renderer/rects.zig` directly under `display/`.
  - move `tab_bar/tab_bar.zig` to `display/tab_bar.zig`.
  - move `tab_bar/slots.zig` into the terminal package as `terminal/tab_slots.zig`.
  - rewire every direct import site that currently names `window_chrome/window.zig` or `display/renderer/render_surface.zig`, specifically `src/terminal/context.zig`, `src/terminal/context_test.zig`, `src/terminal/links.zig`, and `src/host_test_root.zig`.
  - remove the weak `window_chrome/`, `tab_bar/`, and `display/renderer/` folders.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:integration`
- Required assertions:
  - preserve display present token assertions from `display/display.zig:122-164`.
  - preserve present-reason and pending-terminal assertions from `app/present.zig:30-77`.
  - preserve tab-slot bounds assertions from `tab_bar/slots.zig:47-98`.
  - preserve tab-bar snapshot tests from `tab_bar/tab_bar.zig:37-58`.
- Exact non-goals:
  - no renderer-architecture redesign.
  - no new top-level `renderer/` root.
  - no tab feature changes.
  - no host/render ABI changes.
  - no `gl_c.h` move in this slice.
- Exact stop conditions:
  - stop if the move would create another bucket folder under `display/`.
  - stop if `tab_slots` cannot be placed without clear terminal-surface ownership.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-host-folder-structure-02`
  - commit-hash handoff status for orchestrator receipt closure

10. Role: coder
- Session id: `coder-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: resume Slice 1 on the existing worktree by finishing the newly authorized `event_loop.zig` rewires in `terminal/pty/wait_thread.zig` and `terminal/render/surface_layout.zig`, then rerun the exact Slice 1 tests.
- How one task went: updated both newly authorized import sites to `polling/event_loop.zig`, re-checked `src/` for any remaining direct `event_loop.zig` imports, and verified the moved control-spine state now builds and tests cleanly. Exact verification results: `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:unit` succeeded; `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:integration` succeeded.
- Next handoff or blocker: reviewer can now review Slice 1 against the refreshed contract. Commit-hash handoff status: none; git not touched.

11. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: review execution Slice 1 against the refreshed condensed contract and accepted planning artifact.
- How one task went: accepted Slice 1. Finding 1: the worktree stays inside the authorized Slice 1 file set plus the exact required move targets. In `howl-linux-host`, the only product-code changes are `build.zig`, deletions of `src/app/processor.zig`, `src/cli/args.zig`, and `src/event_loop.zig`, modifications to `src/main.zig`, `src/host_test_root.zig`, `src/terminal/context.zig`, `src/terminal/input.zig`, `src/terminal/scrollbar.zig`, `src/terminal/pty/wait_thread.zig`, and `src/terminal/render/surface_layout.zig`, and creation of the required move targets `src/event.zig`, `src/cli.zig`, and `src/polling/event_loop.zig`. Finding 2: the required path moves and rewires are complete: `app/processor.zig` moved to `event.zig`, `cli/args.zig` moved to `cli.zig`, `event_loop.zig` moved to `polling/event_loop.zig`, and the authorized direct import rewires now point to the polling path in `src/main.zig`, `src/terminal/context.zig`, `src/terminal/input.zig`, `src/terminal/scrollbar.zig`, `src/terminal/pty/wait_thread.zig`, `src/terminal/render/surface_layout.zig`, and `src/host_test_root.zig`. Finding 3: `build.zig` now points CLI tests at `src/cli.zig`, `src/cli/` is empty, and `src/polling/` now contains both `event_loop.zig` and `window_wake.zig`, so the slice leaves `polling/` as a real subdomain and does not require `app/` or `cli/` for the moved owners. Finding 4: the key assertions are preserved in the moved files: `src/polling/event_loop.zig` still carries the wake-event assertions and bounded drain assertions from the accepted contract, and `src/event.zig` preserves the control-spine assertions and inline tests. Finding 5: the coder recorded exact verification receipts in the live loop, and the claimed results match the refreshed slice contract: `zig build test:unit` and `zig build test:integration` succeeded. No evidence of tab-model redesign, new runtime abstractions, event-processing behavior changes, or new public host API surfaced in the Slice 1 diff.
- Next handoff or blocker: orchestrator should verify the worktree against this accepted Slice 1 receipt, inspect the exact host repo diff/status, and only then create the Slice 1 commit with commit-hash receipt closure before seeding Slice 2.

13. Role: coder
- Session id: `coder-2026-06-13-host-folder-structure-02`
- Hello: hi
- Intended task: execute only Slice 2 by moving the display/window/tab-visual owners into `display/` and `terminal/`, rewiring the exact authorized imports, and rerunning the required host tests.
- How one task went: moved `src/app/present.zig` to `src/display/present.zig`, `src/window_chrome/window.zig` to `src/display/window.zig`, `src/window_chrome/icon.zig` to `src/display/icon.zig`, `src/window_chrome/stb_image.c` to `src/display/stb_image.c`, `src/display/renderer/render_surface.zig` to `src/display/render_surface.zig`, `src/display/renderer/render_surface_test.zig` to `src/display/render_surface_test.zig`, `src/display/renderer/rects.zig` to `src/display/rects.zig`, `src/tab_bar/tab_bar.zig` to `src/display/tab_bar.zig`, and `src/tab_bar/slots.zig` to `src/terminal/tab_slots.zig`; rewired the authorized direct import sites in `src/terminal/context.zig`, `src/terminal/context_test.zig`, `src/terminal/links.zig`, and `src/host_test_root.zig`, plus the owning runtime/build imports in `src/main.zig`, `src/event.zig`, `src/display/display.zig`, `src/display/layout.zig`, `src/display/rects.zig`, and `build.zig`; preserved the present-token assertions, present-reason/pending-terminal assertions, tab-slot bounds assertions, and tab-bar snapshot tests. Exact verification results: `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:unit` succeeded; `cd /home/home/personal/projects/howl/howl-linux-host && zig build test:integration` succeeded. `src/window_chrome/` and `src/tab_bar/` were removed once empty.
- Next handoff or blocker: reviewer/orchestrator can review Slice 2. Deviation: `src/display/renderer/` still contains `gl_c.h`, so that folder cannot be removed in this slice without violating the explicit no-`gl_c.h`-move rule; commit-hash handoff status: none; git not touched.

15. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: review execution Slice 3, `Terminal Surface Naming Cleanup`, against the active condensed contract and accepted planning artifact.
- How one task went: accepted Slice 3. Finding 1: the worktree stays inside the authorized Slice 3 file set plus the exact required rename targets. In `howl-linux-host`, the product-code changes are limited to `build.zig`, `src/display/present.zig`, `src/event.zig`, `src/host_test_root.zig`, `src/integration_test_root.zig`, `src/main.zig`, `src/terminal/tab_slots.zig`, deletion of `src/terminal/context.zig` and `src/terminal/context_test.zig`, and creation of the required targets `src/terminal/surface.zig` and `src/terminal/surface_test.zig`. Finding 2: the required renames and import/test rewires are complete. `terminal/context.zig` was renamed to `terminal/surface.zig`, `terminal/context_test.zig` was renamed to `terminal/surface_test.zig`, and the authorized import/test references now point at `terminal/surface.zig` or `terminal/surface_test.zig` in `src/event.zig`, `src/display/present.zig`, `src/host_test_root.zig`, `src/integration_test_root.zig`, `src/terminal/tab_slots.zig`, and the renamed test file itself. No lingering `context.zig` or `context_test.zig` references remain in the host tree. Finding 3: the current `terminal/pty/`, `terminal/render/`, and `terminal/vt/` child folders are preserved unchanged as folders, matching the slice contract. Finding 4: the renamed owner now reads as an embedded terminal surface owner rather than a generic context bucket. `src/terminal/surface.zig` exposes `pub const Surface = struct` and the host/runtime call sites now use `TerminalSurface`, while `src/terminal/tab_slots.zig` owns `surfaces` and `*TerminalSurface` references, which sharpens the owner noun without introducing broader lifecycle redesign. Finding 5: the required assertions and tests are preserved. `src/terminal/surface.zig:137-219` still carries the init/deinit/geometry assertions from the former context owner, `src/terminal/surface_test.zig` preserves the prior owner-local proof surface under the new name, and `src/terminal/tab_slots.zig` keeps its bounds and lifecycle assertions after the rename. Finding 6: no PTY, VT, or render behavior changes, and no broader surface lifecycle redesign, surfaced in the Slice 3 diff beyond owner naming and import rewiring. Finding 7: exact verification receipts and loop hygiene are present. The coder recorded `zig build test:unit` and `zig build test:integration` success in the live loop, and the worktree state matches that receipt.
- Next handoff or blocker: orchestrator should verify the accepted Slice 3 worktree in `howl-linux-host`, confirm the recorded unit/integration test receipts against the final diff, and record the Slice 3 commit hash before the final nested host commit and sprint-close receipt are recorded.

14. Role: reviewer
- Session id: `review-2026-06-13-host-folder-structure-01`
- Hello: hi
- Intended task: review execution Slice 2, `Display, Window, And Tab-Visual Owner Cleanup`, against the active condensed contract and accepted planning artifact.
- How one task went: accepted Slice 2. Finding 1: the worktree stays inside the authorized Slice 2 file set plus the exact required move targets. In `howl-linux-host`, the product-code changes are limited to the allowed sources, their deletions, and the required targets `src/display/present.zig`, `src/display/window.zig`, `src/display/icon.zig`, `src/display/stb_image.c`, `src/display/render_surface.zig`, `src/display/render_surface_test.zig`, `src/display/rects.zig`, `src/display/tab_bar.zig`, and `src/terminal/tab_slots.zig`. Finding 2: the contracted owner moves and rewires are complete. `app/present.zig`, `window_chrome/window.zig`, `window_chrome/icon.zig`, `window_chrome/stb_image.c`, `display/renderer/render_surface.zig`, `display/renderer/render_surface_test.zig`, `display/renderer/rects.zig`, `tab_bar/tab_bar.zig`, and `tab_bar/slots.zig` were moved to the required `display/` and `terminal/` homes, and the authorized direct import rewires now point at the new paths in `src/terminal/context.zig`, `src/terminal/context_test.zig`, `src/terminal/links.zig`, and `src/host_test_root.zig`; the owning runtime/build rewires in `src/main.zig`, `src/event.zig`, `src/display/display.zig`, `src/display/layout.zig`, and `build.zig` also match the new owners. Finding 3: the cited assertion/test carriers are preserved at their moved homes. `src/display/display.zig:122-164` still carries the present-token assertions, `src/display/present.zig:30-77` still carries the present-reason and pending-terminal assertions, `src/terminal/tab_slots.zig:47-98` still carries the tab-slot bounds assertions, and `src/display/tab_bar.zig:37-58` still carries the tab-bar snapshot tests. Finding 4: no evidence of renderer-architecture redesign, a new top-level `renderer/` root, tab feature changes, host/render ABI changes, or a `gl_c.h` move surfaced in the Slice 2 diff. Leaving `src/display/renderer/` present solely for deferred `gl_c.h` is acceptable in this slice and is not a blocker, because the active contract explicitly forbids moving `gl_c.h` in Slice 2 and no remaining Zig import sites point into `display/renderer/`; the folder is therefore a temporary receipt of the deferred Slice 4 C-translation cleanup rather than unauthorized redesign. Finding 5: exact verification receipts and loop hygiene are present. The coder recorded `zig build test:unit` and `zig build test:integration` success in the live loop, and the worktree state matches that receipt.
- Next handoff or blocker: orchestrator should verify the accepted Slice 2 worktree in `howl-linux-host`, confirm the recorded unit/integration test receipts against the final diff, and record the Slice 2 commit hash before seeding Slice 3.
