Host render surface ownership live loop scratchpad

Date: 2026-06-13.

Role owner: orchestrator.

Status: Slice 3 accepted; sprint execution complete.

Orchestrator session id: `orch-2026-06-13-host-render-surface-01`.

Researcher session id: `research-2026-06-13-host-render-surface-01`.

Reviewer session id: `review-2026-06-13-host-render-surface-01`.

Planning acceptance commit-hash receipt: `455402d` `Accept host render-surface planning`.

Slice 1 acceptance receipt: `5c7a2a2` `Extract host render-surface resources`.

Slice 2 acceptance receipt: `2977c3b` `Extract host render-surface commands`.

Slice 3 acceptance receipt: `618d0c5` `Curate host render-surface root`.

Execution seed commit-hash receipt: `afe558e` `Accept host render-surface Slice 2`.

Coder session id: `coder-2026-06-13-host-render-surface-slice-03`.

Shared scratchpad rule:

- This file is the live team scratchpad for the current step.
- Every non-trivial orchestrator, researcher, reviewer, or coder pass must append a short note with role/session id, hello, intent, outcome, and handoff or blocker.

Controlling sprint:

- `/home/home/personal/projects/howl/sprints/2026-06-13-host-render-surface-ownership-sprint.md`

Active research target:

- `/home/home/personal/projects/howl/research/2026-06-13-host-render-surface-ownership-plan.md`

Current step:

- Planning is accepted.
- Slice 3 is accepted.
- Sprint execution is complete.
- No further implementation is authorized under this sprint.

Slice 3 execution contract:

- Name: root curation and proof-root cleanup after both owner extractions.
- Allowed files:
  - `howl-linux-host/src/display/render_surface.zig`
  - `howl-linux-host/src/display/render_surface_resources.zig`
  - `howl-linux-host/src/display/render_surface_commands.zig`
- Exact shape:
  - Reduce `render_surface.zig` to the public root and upload orchestration spine only.
  - Keep source order explicit: imports, public re-exports, orchestration entrypoints, then root-local helpers only if they remain owner-true.
  - Remove stale wrapper duplicates or stale test shims introduced by Slice 1 and Slice 2.
  - Keep `render_surface.testing` proving the new owner boundaries rather than accidental old names.
- Exact tests:
  - `zig build test:unit -- test-filter test-render-surface`
  - `zig build test:unit -- test-filter test-terminal-surface`
- Exact non-goals:
  - No new owner extraction beyond the two child owners.
  - No new test roots.
  - No `host_test_root.zig` edits.
  - No `render_surface_test.zig` edits.
  - No changes to `display/display.zig`, `event.zig`, `present.zig`, `terminal/surface.zig`, render-side files, or ABI headers.
- Exact stop conditions:
  - Stop if cleanup would broaden into display/runtime redesign.
  - Stop if test coverage can only be preserved by inventing extra helper exports rather than routing through the two real child owners.
- Receipt fields required on completion:
  - orchestrator `orch-2026-06-13-host-render-surface-01`
  - researcher `research-2026-06-13-host-render-surface-01`
  - reviewer `review-2026-06-13-host-render-surface-01`
  - coder `coder-2026-06-13-host-render-surface-slice-03`
  - commit hash
  - required verification results

Exact user direction:

- Do not stay out of host code.
- Make everything pristine.
- Push the next sprint into host ownership pressure rather than avoiding it.

Exact non-goals:

- No implementation during planning.
- No ABI/header churn.
- No benchmark theater.
- No generalized host-runtime redesign.

Exact stop conditions:

- Stop if planning cannot name exact slice boundaries, allowed files, tests, non-goals, and stop conditions.
- Stop if the references point to a conflicting shape that needs explicit user review.
- Stop if active accountability files drift from the real live state.

Team scratchpad

1. Role: orchestrator
- Session id: `orch-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: seed the next planning sprint around host render-surface ownership pressure.
- How one task went: promoted a new host-focused planning sprint around `howl-linux-host/src/display/render_surface.zig` so the active surface stays live after the raster sprint close.
- Next handoff or blocker: researcher must write the full planning package and either prove `render_surface.zig` is the first cut or prove one ranked fallback seam must preempt it.

2. Role: researcher
- Session id: `research-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: prove whether `howl-linux-host/src/display/render_surface.zig` is the first true host ownership cut and write the full slice plan.
- How one task went: current source and Alacritty/TigerBeetle pressure both say `render_surface.zig` goes first; the true first split is its retained host resource owner, not a fallback seam and not helper shattering.
- Next handoff or blocker: reviewer should gate the planning package in `research/2026-06-13-host-render-surface-ownership-plan.md`; no reference override is needed.

3. Role: reviewer
- Session id: `review-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: review the active host render-surface ownership planning package for source backing and executability.
- How one task went: rejected the research artifact because the first-cut ranking against the sprint's fallback files was under-proved and the slice queue still left helper/symbol ownership to coder judgment.
- Next handoff or blocker: researcher must add direct fallback-file comparison proof, replace category-level helper language with exact symbol-to-file slice instructions, and tighten allowed-file authority before planning can be accepted.

4. Role: researcher
- Session id: `research-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: repair the rejected planning package without implementation.
- How one task went: read the ranked fallback files directly, rewrote the plan with explicit fallback comparison, exact symbol moves for Slice 1 and Slice 2, exact allowed-file consequences, and removed the under-proved claims.
- Next handoff or blocker: reviewer should re-gate the repaired artifact in `research/2026-06-13-host-render-surface-ownership-plan.md`; no implementation is authorized.

5. Role: reviewer
- Session id: `review-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: re-gate the repaired host render-surface ownership planning package.
- How one task went: rejected the repaired artifact because Slice 1 still left shared helper ownership unresolved; the extracted resource owner needs `spanSlice`, `sameResource`, `bytesPerPixel`, and `rectFitsResource` before Slice 2 moves them.
- Next handoff or blocker: researcher must repair Slice 1 with exact interim symbol ownership/visibility for every helper the moved resource owner calls, then re-submit for review.

6. Role: researcher
- Session id: `research-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: repair the remaining Slice 1 shared-helper ownership blocker.
- How one task went: assigned `spanSlice`, `sameResource`, `bytesPerPixel`, and `rectFitsResource` to `render_surface_resources.zig` in Slice 1 and allowed only temporary root-local aliases until command extraction.
- Next handoff or blocker: reviewer should re-gate the repaired planning package; no implementation is authorized.

7. Role: reviewer
- Session id: `review-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: re-gate the twice-repaired host render-surface ownership planning package.
- How one task went: rejected the artifact because Slice 1 still did not explicitly authorize the visibility/facade route for moved `RenderResourceTextures.Slot`, `commitUploadMetadata`, and `validateSurface`, and the active loop scratchpad contained a stale duplicated researcher note.
- Next handoff or blocker: researcher must make Slice 1 executable without coder invention by naming the exact resource-owner visibility/testing-facade route, and orchestrator/researcher must clean the stale duplicate loop note before re-review.

8. Role: researcher
- Session id: `research-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: repair the Slice 1 resource testing-facade route and clean stale loop state.
- How one task went: named `render_surface_resources.testing` as the resource-owned facade for `TextureSlot`, `commitUploadMetadata`, and `validateSurface`, kept `render_surface.testing` as the only test import facade, and removed the stale duplicate loop note.
- Next handoff or blocker: reviewer should re-gate the repaired planning package; no implementation is authorized.

9. Role: reviewer
- Session id: `review-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: re-gate the latest repaired host render-surface ownership planning package.
- How one task went: rejected the package because the main prior symbol/facade repairs are present, but the allowed-file lists still conditionally authorize files without exact expected changes.
- Next handoff or blocker: researcher must remove or make exact the conditional `terminal/surface_test.zig` authorizations and the conditional Slice 2 `render_surface_resources.zig` visibility authorization before acceptance.

10. Role: researcher
- Session id: `research-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: repair conditional allowed-file authority after the latest reviewer rejection.
- How one task went: removed `terminal/surface_test.zig` from all editable slice file lists and removed conditional Slice 2 authority for `render_surface_resources.zig`; the tests remain required verification only.
- Next handoff or blocker: reviewer should re-gate the repaired planning package; no implementation is authorized.

11. Role: reviewer
- Session id: `review-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: re-gate the latest allowed-file repair for the host render-surface ownership planning package.
- How one task went: rejected the package because the latest repair fixed the named Slice 1/Slice 2 symbol and facade defects, removed the stale duplicate loop note, and removed the prior conditional `terminal/surface_test.zig` and Slice 2 `render_surface_resources.zig` authority, but Slice 3 still conditionally authorized `render_surface_test.zig` edits.
- Next handoff or blocker: researcher must make Slice 3 allowed files exact by removing the conditional test-file edit or replacing it with one named expected change, then update the readiness claim before re-review.

12. Role: researcher
- Session id: `research-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: repair the remaining Slice 3 conditional allowed-file authority.
- How one task went: removed `render_surface_test.zig` from Slice 3 allowed files and added `No render_surface_test.zig edits` as a Slice 3 non-goal.
- Next handoff or blocker: reviewer should re-gate the repaired planning package; no implementation is authorized.

13. Role: reviewer
- Session id: `review-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: re-gate the Slice 3 allowed-file repair for the host render-surface ownership planning package.
- How one task went: accepted the repaired package; fallback ranking, exact Slice 1/Slice 2 symbol moves, Slice 1 helper ownership, resource testing facade routing, allowed-file authority, readiness claim, and active loop state now satisfy the planning gate.
- Next handoff or blocker: orchestrator may promote the accepted planning package into execution seeding; no implementation is authorized until the next accepted execution slice is seeded.

14. Role: orchestrator
- Session id: `orch-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: close the accepted planning receipt and seed Slice 1 as the active execution contract.
- How one task went: recorded planning acceptance receipt `455402d` and promoted Slice 1 into the controlling execution contract with exact files, symbols, tests, non-goals, stop conditions, and coder session id `coder-2026-06-13-host-render-surface-slice-01`.
- Next handoff or blocker: coder may execute Slice 1 only; reviewer remains `review-2026-06-13-host-render-surface-01` for execution gating.

15. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-01`
- Hello: hi
- Intended task: execute Slice 1 by extracting the retained host resource owner into `render_surface_resources.zig` while preserving the root import and test facade.
- How one task went: moved `RenderResourceTextures` and its resource validation/upload helpers into the new resource owner file, kept temporary root helper aliases for command code, and routed the root testing facade through `render_surface_resources.testing`.
- Next handoff or blocker: run the required host unit tests and then hand the diff to reviewer `review-2026-06-13-host-render-surface-01`.

16. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-01`
- Hello: hi
- Intended task: verify Slice 1 against the required host proof roots.
- How one task went: both proof roots passed with the current build step `zig build test:unit -- test-filter test-render-surface` and `zig build test:unit -- test-filter test-terminal-surface`; the seeded contract spelled the old step name `test-unit`, so the verification command name needs orchestrator correction but the proof roots themselves passed.
- Next handoff or blocker: hand the Slice 1 diff and verification results to reviewer `review-2026-06-13-host-render-surface-01`.

17. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-01`
- Hello: hi
- Intended task: repair the reviewer-rejected Slice 1 drift without broadening scope.
- How one task went: removed the unauthorized duplicate `panicGlBroken` owner from `render_surface_resources.zig` and kept the resource owner within the approved moved symbol set.
- Next handoff or blocker: rerun the two proof roots and re-submit the Slice 1 diff to reviewer `review-2026-06-13-host-render-surface-01`.

18. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-01`
- Hello: hi
- Intended task: record post-repair verification on the repaired Slice 1 diff.
- How one task went: reran `zig build test:unit -- test-filter test-render-surface` and `zig build test:unit -- test-filter test-terminal-surface`; both passed on the repaired diff.
- Next handoff or blocker: reviewer `review-2026-06-13-host-render-surface-01` should re-review the repaired Slice 1 diff against the corrected live contract.

19. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-01`
- Hello: hi
- Intended task: resolve the stale verification-command blocker note in the live scratchpad.
- How one task went: confirmed the active Slice 1 contract already uses `zig build test:unit -- test-filter test-render-surface` and `zig build test:unit -- test-filter test-terminal-surface`, confirmed both passed on the repaired diff, and closed note 16's earlier command-correction statement as stale rather than active.
- Next handoff or blocker: reviewer `review-2026-06-13-host-render-surface-01` should re-review Slice 1; no implementation changes remain for this repair.

20. Role: reviewer
- Session id: `review-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: re-review Slice 1 after the accountability-only closure note.
- How one task went: accepted; the live loop now carries non-contradictory verification truth because note 19 closes note 16's stale command-correction statement, and the current `render_surface.zig` / `render_surface_resources.zig` / `render_surface_test.zig` surface shows no new implementation drift beyond the already repaired Slice 1 scope.
- Next handoff or blocker: Slice 1 review gate is clear; orchestrator may record acceptance or advance per loop flow.

21. Role: orchestrator
- Session id: `orch-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: record Slice 1 acceptance and seed Slice 2 as the next active execution contract.
- How one task went: recorded Slice 1 host implementation receipt `5c7a2a2`, kept its reviewer acceptance in the live loop, and promoted Slice 2 into the controlling execution contract with coder session id `coder-2026-06-13-host-render-surface-slice-02`.
- Next handoff or blocker: coder may execute Slice 2 only; reviewer remains `review-2026-06-13-host-render-surface-01` for execution gating.

22. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-02`
- Hello: hi
- Intended task: execute Slice 2 by extracting the render-surface command grammar and draw owner into `render_surface_commands.zig` while keeping the root as the upload spine.
- How one task went: moved the command classification, validation, draw, and test-hook surface into `render_surface_commands.zig`, and rewrote `render_surface.zig` as the public root with command-owner delegation plus the existing resource/test facade routing.
- Next handoff or blocker: format the Slice 2 files, run the two required proof roots, and then send the diff to reviewer `review-2026-06-13-host-render-surface-01`.

23. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-02`
- Hello: hi
- Intended task: verify Slice 2 against the required host proof roots.
- How one task went: `zig build test:unit -- test-filter test-render-surface` and `zig build test:unit -- test-filter test-terminal-surface` both passed after the Slice 2 extraction.
- Next handoff or blocker: reviewer `review-2026-06-13-host-render-surface-01` should review the Slice 2 diff.

24. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-02`
- Hello: hi
- Intended task: re-verify Slice 2 after rewriting the root and command owner split in one clean pass.
- How one task went: reformatted `render_surface.zig` and `render_surface_commands.zig`, then reran `zig build test:unit -- test-filter test-render-surface` and `zig build test:unit -- test-filter test-terminal-surface`; both passed again.
- Next handoff or blocker: reviewer `review-2026-06-13-host-render-surface-01` should review the final Slice 2 diff.

25. Role: reviewer
- Session id: `review-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: review the actual Slice 2 execution diff against the accepted command-owner extraction contract.
- How one task went: accepted; `render_surface.zig` remains the public upload spine, command grammar/draw ownership now lives in `render_surface_commands.zig`, tests still enter only through `render_surface.testing`, and the Slice 2 loop notes match the current code and passed proof roots.
- Next handoff or blocker: Slice 2 review gate is clear; orchestrator may record acceptance and advance per loop flow.

26. Role: orchestrator
- Session id: `orch-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: record Slice 2 acceptance and seed Slice 3 as the next active execution contract.
- How one task went: recorded Slice 2 host implementation receipt `2977c3b`, kept its reviewer acceptance in the live loop, and promoted Slice 3 into the controlling execution contract with coder session id `coder-2026-06-13-host-render-surface-slice-03`.
- Next handoff or blocker: coder may execute Slice 3 only; reviewer remains `review-2026-06-13-host-render-surface-01` for execution gating.

27. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-03`
- Hello: hi
- Intended task: execute Slice 3 root curation and proof-root cleanup after both owner extractions.
- How one task went: reduced `render_surface.zig` to the upload spine plus public aliases to the real command owner, kept `render_surface.testing` routed through the real owners, and left the resource and command owner files as the only behavior owners.
- Next handoff or blocker: rerun the required proof roots and then hand the final Slice 3 diff to reviewer `review-2026-06-13-host-render-surface-01`.

28. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-03`
- Hello: hi
- Intended task: verify Slice 3 after the root curation cleanup.
- How one task went: `zig build test:unit -- test-filter test-render-surface` and `zig build test:unit -- test-filter test-terminal-surface` both passed after the Slice 3 cleanup pass.
- Next handoff or blocker: reviewer `review-2026-06-13-host-render-surface-01` should re-review the final Slice 3 diff.

29. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-03`
- Hello: hi
- Intended task: repair the remaining Slice 3 root source-order rejection.
- How one task went: reordered `render_surface.zig` so the file now reads imports, public re-exports, orchestration entrypoint, then root-local helpers and testing facade.
- Next handoff or blocker: rerun the two required proof roots and return the cleaned Slice 3 surface to reviewer `review-2026-06-13-host-render-surface-01`.

30. Role: coder
- Session id: `coder-2026-06-13-host-render-surface-slice-03`
- Hello: hi
- Intended task: close the final Slice 3 note-truth mismatch.
- How one task went: moved `UploadStats` into the public type section ahead of `uploadRenderSurface`, and the accurate owner statement is now: `render_surface_resources.zig` owns retained resources, `render_surface_commands.zig` owns command grammar and draw behavior, and `render_surface.zig` still owns the public upload spine plus root-local host-surface helpers.
- Next handoff or blocker: rerun the two required proof roots and return the corrected Slice 3 surface to reviewer `review-2026-06-13-host-render-surface-01`.

31. Role: reviewer
- Session id: `review-2026-06-13-host-render-surface-01`
- Hello: hi
- Intended task: final re-review Slice 3 after the UploadStats reorder and loop note-truth repair.
- How one task went: accepted; the prior note-truth mismatch is fixed, the root source order now matches the seeded Slice 3 contract, and `render_surface.zig` remains the public upload spine with only owner-true host-surface helpers plus the centralized testing facade.
- Next handoff or blocker: Slice 3 review gate is clear; orchestrator may record acceptance.
