Host Layout Multiplexer Rewrite Loop

Orchestrator session id: `orch-2026-06-21-layout-mux-01`.
Reviewer session id: `review-2026-06-21-layout-mux-01`.

Scoping statement:

- The active sprint is to rewrite `howl-linux-host/src/layout/` into the host structure layer for tabs, panes, splits, scrollbar, and scroll chip.
- `window/` owns SDL window creation and OS-window lifecycle.
- `layout/` owns the interior structure of the window created by `window/`.
- `layout/` is the HTML/CSS layer for host composition.
- `layout/` must not use VT nouns such as `viewport` or `screen` for host structure.
- `layout/` must follow terminal multiplexer pressure first for this slice: tmux `session/window/pane/layout` and zellij `tab/pane/layout`.
- Ghostty `Surface` remains a reference for the terminal widget concept, but it is not the top naming pressure for this slice.
- The rewrite must make panes loosely coupled from placement so panes can move across splits, tabs, and windows, and can become tiled, floating, hidden, or shown while retaining accurate placement facts.
- Tabs must be able to move between windows.
- Floating panes must be able to hide/show while preserving correct geometry and identity.

Current mode:

- Single-agent direct collaboration.
- This is a strict sprint, not a casual rename pass.
- Execution proceeds only through the accepted staged contract below.

Non-negotiables:

- `window/` owns SDL windowing only.
- `layout/` owns host structure only.
- No fake `viewport` owner survives sprint exit.
- No VT `screen` ownership leaks into host structure.
- No bucket owners, `Context`, `Info`, `Request`, `Snapshot`, or compatibility glue.
- Pane identity must be placement-independent.
- Split structure must reference pane ids/handles rather than embedding pane runtime state.
- End-of-sprint state must be owner-true, testable, grep-clean for stale layout lies, and fit future split/tab/window transfer work.

Required reference pressure:

- tmux for `session/window/pane/layout` vocabulary and split action semantics.
- zellij for `tab/pane/layout` vocabulary and modern split/floating UX pressure.
- WezTerm as supporting mux `window/tab/pane/split` pressure.
- Ghostty for terminal widget `Surface` pressure and `SplitTree` implementation lessons where needed.
- Alacritty for host window/display/runtime organization.
- TigerBeetle for directness, exact names, bounds, assertions, and tests.

Strict sprint stages:

1. make/move the planned files/folders
2. define symbols, signatures and tests
3. define signatures' bodies correctly to make correct valuable tests pass

Loop notes:

- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: promote host layout rewrite to the active strict sprint after user rejected `viewport` and set multiplexer pressure above Ghostty for this slice. Outcome: active sprint now targets `layout/` as host structure layer with pane/tab/split nouns, explicit `window/` vs `layout/` boundary, and a three-stage execution contract. Handoff: research artifact must define the exact file moves, symbol signatures, and proof gates before product-code body work broadens.
- Teammate `teammate-2026-06-21-layout-mux-stage1-01`: hello. Intent: re-prove the current host window/layout/event coupling and compress Stage 1 into an executable structural contract only. Outcome: `research/layout-host-multiplexer-rewrite.md` now narrows Stage 1 to moving the current `Viewport` API into `layout/window.zig`, adds placeholder owner files only, updates the exact caller/test files, and explicitly defers `splits`, pane ids, and behavior splits. Handoff: coder can execute Stage 1 only if the work stays inside the named files and keeps `zig build check`, `zig build test:unit`, and the stale-viewport grep gates green; otherwise stop and escalate.
- Reviewer `review-2026-06-21-layout-mux-01`: hello. Intent: review the condensed Stage 1 contract for executability and scope truth. Outcome: accepted `research/layout-host-multiplexer-rewrite.md:121-173` as the controlling Stage 1 execution contract; narrowing out `splits`, `PaneId`, and behavior splitting is correct for current source. Handoff: one coder pass may now execute Stage 1 only inside the named files and stop on any boundary spill.
- Coder `coder-2026-06-21-layout-mux-stage1-01`: hello. Intent: execute accepted Stage 1 only by moving the current viewport geometry owner into `layout/window.zig`, retargeting caller/export imports, and adding placeholder layout owner files. Outcome: `layout/viewport.zig` is gone, `layout/window.zig` now owns the moved geometry API and tests, `events/event.zig` and `host_test_root.zig` now point at the new owner path, placeholder `layout/tab.zig`, `layout/pane.zig`, `layout/tab_bar.zig`, and `layout/overlay.zig` exist with unit docs only, and required formatting/build/test/grep gates passed. Handoff: ready for reviewer verification and receipt closure; no blocker.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: close accepted Stage 1 with reviewer acceptance and commit-hash receipt. Outcome: reviewer accepted the Stage 1 structural move and the accepted host tree is now committed in `howl-linux-host` as `807f311 Move viewport owner to layout window`. Handoff: sprint advances to Stage 2 planning for exact symbols, signatures, and tests.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: stop Stage 2 launch after user rescope on host layering nouns. Outcome: `overlay` is now rejected as a host structure noun; future planning must use `scrollbar` and `scroll_chip` with explicit z-index/layer modeling instead of a generic overlay owner. Handoff: revise the Stage 2 planning contract before any further coder pass.
- Teammate `teammate-2026-06-21-layout-mux-stage2-scroll-layer-01`: hello. Intent: produce revised Stage 2 research/coder-prep contract after user rejected `overlay`. Outcome: ready contract returned for deleting `layout/overlay.zig`, adding `layout/z_index.zig`, `layout/scrollbar.zig`, and `layout/scroll_chip.zig`, splitting `Layout.ScrollbarLayout` into separate scrollbar and scroll-chip placements, and proving z-index order with tests. Handoff: orchestrator review required before coding.
- Reviewer `review-2026-06-21-layout-mux-stage2-scroll-layer-01`: hello. Verdict: accept with clarification. Evidence: teammate contract is source-backed by tmux `z_index`/`zentry` pressure and zellij floating pane `z_indices`/layer stack pressure; it directly addresses current stale symbols `OverlaySnapshot`, `overlaySnapshot`, and local `overlay`. Clarification: `layout/z_index.zig` may exist only as the explicit host composition ordering contract and must not become a generic constants bucket, layer manager, draw-policy owner, or hidden overlay replacement. Handoff: launch coder for accepted Stage 2 slice only.
- Coder `coder-2026-06-21-layout-mux-stage2-scroll-layer-01`: hello. Intent: execute accepted Stage 2 only by deleting the rejected placeholder, adding scrollbar/scroll-chip placement owners with explicit z-index ordering, and splitting frame presentation into separate track and chip placements. Outcome: host source now routes scrollbar and scroll-chip placements through `layout/scrollbar.zig`, `layout/scroll_chip.zig`, and `layout/z_index.zig`; stale rejected product symbols are gone from host source; formatting, check, unit tests, diff check, and stale-symbol search passed. Handoff: ready for reviewer verification; no blocker.
- Reviewer `review-2026-06-21-layout-mux-stage2-scroll-layer-02`: hello. Verdict: accept. Evidence: reviewed diff scope and new owner files, confirmed `layout/overlay.zig`, `OverlaySnapshot`, `overlaySnapshot`, and local `overlay` were removed from host source, confirmed `z_index.zig` remains a narrow composition-order contract, and reran `zig fmt build.zig src`, `zig build check`, `zig build test:unit`, `git diff --check`, plus stale-symbol search successfully. Handoff: accepted but not committed because no explicit commit request was made.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: commit accepted Stage 2 after user clarified accepted work must be committed. Outcome: `howl-linux-host` now records accepted Stage 2 as `41b7964 Split scrollbar layer placement`. Handoff: commit root pointer and accountability artifacts, then continue to the next sprint slice.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: close root accountability for accepted Stage 2 and remove forward-looking stale `overlay` wording from sprint artifacts. Outcome: root recorded the host pointer and sprint artifacts as `2d96c2c Track layout multiplexer sprint`; current sprint text now names scrollbar and scroll chip instead of overlays. Handoff: launch the next teammate planning pass for pane/tab/window structure symbols.
- Teammate `teammate-2026-06-21-layout-mux-pane-tab-plan-01`: hello. Intent: define the next smallest source-backed pane/tab/window structure slice after scrollbar-layer cleanup. Outcome: ready contract returned for routing current single-tab/single-pane geometry through `layout/window.zig`, `layout/tab_bar.zig`, `layout/tab.zig`, and `layout/pane.zig`, replacing temporary `Regions`/`Terminal`/`viewport` names while deferring splits/floating/transfer behavior. Handoff: orchestrator review required before coding.
- Reviewer `review-2026-06-21-layout-mux-pane-tab-plan-01`: hello. Verdict: accept with clarifications. Evidence: contract is source-backed by tmux pane/layout identity pressure, zellij tab/pane/PaneId pressure, and WezTerm mux window/tab/pane pressure; it removes current Stage 1 temporary names without adding splits. Clarifications: `Pane.TerminalPlacement` is host geometry only and must not imply pane ownership of render ABI resources; `PaneId.first` is a temporary single-pane identity seam only. Handoff: launch coder for this accepted slice only.
- Coder `coder-2026-06-21-layout-mux-pane-tab-01`: hello. Intent: execute accepted pane/tab/window structure slice only. Outcome: current single-tab/single-pane geometry now routes through `layout/window.zig`, `layout/tab_bar.zig`, `layout/tab.zig`, and `layout/pane.zig`; temporary `Regions`/`Terminal`/`regions`/`terminal` names and targeted host-structure `viewport` locals are removed; no splits, floating, transfer, id allocation, ABI, or render-resource ownership was added. Handoff: ready for reviewer verification.
- Reviewer `review-2026-06-21-layout-mux-pane-tab-02`: hello. Verdict: accept. Evidence: reviewed diff scope, confirmed new symbols remain owner-true geometry/identity seams, and reran `zig fmt build.zig src`, `zig build check`, `zig build test:unit`, `git diff --check`, plus stale-symbol searches successfully. Handoff: commit accepted host slice, then commit root pointer/accountability artifacts.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: commit accepted pane/tab/window structure slice. Outcome: `howl-linux-host` now records the accepted slice as `a72b97c Route layout through tab and pane owners`. Handoff: commit root pointer and accountability artifacts, then plan the next source-backed slice.
- Teammate `teammate-2026-06-21-layout-mux-splits-plan-01`: hello. Intent: define the next smallest source-backed split structure slice. Outcome: ready contract returned for `layout/splits.zig` as a non-allocating split layout shape owner over `Pane.PaneId`, supporting one leaf and two-pane left-right/top-bottom placement inside `Tab.Body`. Handoff: orchestrator review required before coding.
- Reviewer `review-2026-06-21-layout-mux-splits-plan-01`: hello. Verdict: accept with clarification. Evidence: contract is source-backed by tmux layout cell tree and left-right/top-bottom naming, zellij split direction and pane-id grid pressure, and WezTerm pane-id split operations; it keeps terminal/runtime/render state out of split layout. Clarification: stale-name gates must target host layout structure symbols and files, not official VT scroll viewport protocol names or GL viewport API spellings. Handoff: launch coder for accepted split structure slice only.
- Coder `coder-2026-06-21-layout-mux-splits-01`: hello. Intent: execute accepted Stage 4 split structure slice only by adding `layout/splits.zig` as a pure split layout shape and placement owner over `Pane.PaneId`. Outcome: implementation and verification complete; final report records changed files, commands, and no blockers. Handoff: ready for reviewer verification.
- Reviewer `review-2026-06-21-layout-mux-splits-02`: hello. Verdict: accept. Evidence: reviewed `layout/splits.zig`, `layout.zig`, and `host_test_root.zig`; confirmed the slice is a pure non-allocating split shape/placement owner over `Pane.PaneId` with no event routing, floating, transfer, serialization, terminal/runtime/render mutation, or ABI change. Reran `zig fmt build.zig src`, `zig build check`, `zig build test:unit`, host/root `git diff --check`, and targeted stale-symbol scans successfully. Handoff: commit accepted host slice, then commit root pointer/accountability artifacts.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: commit accepted split structure slice. Outcome: `howl-linux-host` now records accepted Stage 4 as `972c0e4 Add split layout placements`. Handoff: commit root pointer and accountability artifacts, then continue to next source-backed layout slice.
- Teammate `teammate-2026-06-21-layout-mux-tab-splits-plan-01`: hello. Intent: plan tab owner integration with split placement. Outcome: proposed `layout/tab.zig` importing `layout/splits.zig` and delegating `placePanes` to `Splits.place`. Handoff: rejected by reviewer because current `layout/splits.zig` already imports `layout/tab.zig`, so the contract knowingly risks an import cycle instead of resolving ownership first.
- Reviewer `review-2026-06-21-layout-mux-tab-splits-plan-01`: hello. Verdict: reject. Evidence: accepted Stage 4 currently has `splits.zig` importing `tab.zig` for `Tab.Body`; the proposed next slice adds the reverse import from `tab.zig` to `splits.zig`. That violates directness and would leave the coder to patch architecture under compile pressure. Handoff: re-plan the tab/splits seam with an explicit no-cycle signature before any coding.
- Teammate `teammate-2026-06-21-layout-mux-tab-splits-plan-02`: hello. Intent: re-plan tab/splits seam without an import cycle. Outcome: ready contract returned to remove `splits.zig`'s import of `tab.zig`, make `Splits.place` take concrete geometry directly, and add `Tab.placePanes` as the tab-owned body-to-split-placement seam. Handoff: orchestrator review required before coding.
- Reviewer `review-2026-06-21-layout-mux-tab-splits-plan-02`: hello. Verdict: accept. Evidence: dependency direction becomes `tab.zig -> splits.zig -> pane.zig`, `splits.zig` stops depending on `tab.zig`, and the concrete geometry signature avoids `anytype` and bucket structs while keeping runtime/storage/ABI behavior deferred. Handoff: launch coder for the accepted no-cycle tab/splits seam only.
- Coder `coder-2026-06-21-layout-mux-tab-splits-01`: hello. Intent: execute accepted no-cycle tab/splits seam only. Outcome: `layout/splits.zig` no longer imports `layout/tab.zig`; `Splits.place` takes concrete geometry directly; `layout/tab.zig` now owns `placePanes` and routes `singlePane` through `Splits.leaf`; no event/runtime/storage/ABI/render behavior was added. Handoff: ready for reviewer verification.
- Reviewer `review-2026-06-21-layout-mux-tab-splits-01`: hello. Verdict: accept. Evidence: reviewed diff, confirmed dependency direction `tab.zig -> splits.zig -> pane.zig`, confirmed no `@import("tab.zig")` in `splits.zig`, and reran `zig fmt build.zig src`, `zig build check`, `zig build test:unit`, host/root `git diff --check`, and targeted stale-symbol scans successfully. Handoff: commit accepted host slice, then commit root pointer/accountability artifacts.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: commit accepted no-cycle tab/splits seam. Outcome: `howl-linux-host` now records accepted Stage 5 as `42d888d Route tab placement through splits`. Handoff: commit root pointer and accountability artifacts, then evaluate whether the sprint needs the user before moving toward runtime split behavior.
- Teammate `teammate-2026-06-21-layout-mux-runtime-split-assess-01`: hello. Intent: assess whether next work can continue structurally or needs a user decision for runtime split behavior. Outcome: ready recommendation to add a runtime tab owner first while preserving one-terminal-per-tab behavior; actual split creation remains blocked afterward on UX/allocation policy. Handoff: plan exact runtime tab owner slice.
- Teammate `teammate-2026-06-21-layout-mux-runtime-tab-plan-01`: hello. Intent: plan exact runtime tab owner structural slice. Outcome: ready contract returned for adding `src/tab.zig`, changing `tab_bar/tab_slots.zig` to store runtime tabs instead of raw terminal surfaces, and routing `events/event.zig` through `RuntimeTab` while preserving behavior. Handoff: orchestrator review required before coding.
- Reviewer `review-2026-06-21-layout-mux-runtime-tab-plan-01`: hello. Verdict: accept with clarification. Evidence: contract is source-backed by tmux window/pane ownership and zellij/wezterm tab-pane object pressure; it creates the owner required for future pane collections without adding runtime split behavior. Clarification: forwarded associated type constants in `tab.zig` are current-behavior delegation seams only, not compatibility shims or a new public host integration API. Handoff: launch coder for accepted runtime tab owner slice only.
- Coder `coder-2026-06-21-layout-mux-runtime-tab-01`: hello. Intent: execute accepted runtime tab owner structural slice only. Outcome: added `src/tab.zig` as a one-pane runtime tab owner, changed `tab_bar/tab_slots.zig` to store runtime tabs instead of raw terminal surfaces, and updated `events/event.zig` to use `RuntimeTab` while preserving behavior. No split runtime, allocation, keybinding, focus movement, transfer, ABI, render ABI, or bucket cleanup changes were made. Handoff: ready for reviewer verification.
- Reviewer `review-2026-06-21-layout-mux-runtime-tab-01`: hello. Verdict: accept. Evidence: reviewed diff and confirmed `Tab` immediately owns current single-pane lifecycle/delegation, `events/event.zig` no longer imports raw terminal surface, `tab_slots` stores runtime tabs, and verification passed: `zig fmt build.zig src`, `zig build check`, `zig build test:unit`, host/root `git diff --check`, targeted stale-symbol scan. Handoff: commit accepted host slice, then commit root pointer/accountability artifacts.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: commit accepted runtime tab owner slice. Outcome: `howl-linux-host` now records accepted Stage 6 as `f315a88 Add runtime tab owner`. Handoff: commit root pointer and accountability artifacts; actual split creation is now blocked on user-visible split UX/allocation policy.
- User decision: copy Zellij split UX while learning. Start runtime split behavior with both split directions using the same machinery except placement math. First split creation should spawn a new terminal in the current tab using default command/cwd. Defer cwd inheritance, custom commands, sizing UI, moving panes, floating, hidden panes, transfer, and advanced focus until explicitly planned.
- Teammate `teammate-2026-06-21-layout-mux-zellij-splits-plan-01`: hello. Intent: plan minimum Zellij-style runtime split action set after user chose both directions with default command/cwd. Outcome: returned a broad ready plan spanning actions, runtime allocation, active pane routing, and multi-pane drawing. Handoff: rejected by orchestrator as too broad/imprecise for execution and lacking exact enough citation/accountability.
- Teammate `teammate-2026-06-21-layout-mux-zellij-splits-plan-02`: hello. Intent: refine runtime split support into auditable phases. Outcome: ready phased contract returned: Phase 1 pluralizes host presentation frame with no user-visible split action; Phase 2 adds bounded two-pane runtime ownership; Phase 3 wires split-right/split-down actions and config; later phases add richer pane behavior. Handoff: orchestrator review required before coding.
- Reviewer `review-2026-06-21-layout-mux-zellij-splits-plan-02`: hello. Verdict: accept Phase 1. Evidence: current `Layout.Frame` and `texture/frame.zig` are single-terminal-texture shaped, so user-visible split actions would be fake until multi-pane presentation exists. The accepted first slice changes only host-internal frame presentation and preserves one-pane runtime behavior. Handoff: launch coder for Phase 1 plural presentation only.
- Coder `coder-2026-06-21-layout-mux-frame-panes-01`: hello. Intent: execute Stage 7 Phase 1 only by pluralizing host presentation frames without adding split actions or runtime pane allocation. Outcome: `Layout.Frame` now carries `FramePane` slices and explicit tab-bar height; event presentation emits one current pane; texture presentation loops over pane textures and scroll layers. Reviewer found and coder fixed a lifetime bug by making `presentFrame` take `*const RenderFrame`. Handoff: ready for reviewer verification.
- Reviewer `review-2026-06-21-layout-mux-frame-panes-01`: hello. Verdict: accept. Evidence: reviewed diff and fixed slice lifetime; verified no user-visible split action/config symbols, no C/render ABI changes, and no forbidden owner files. Reran `zig fmt build.zig src`, `zig build check`, `zig build test:unit`, host/root `git diff --check`, and targeted gates successfully. Handoff: commit accepted host slice, then commit root pointer/accountability artifacts.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: commit accepted Stage 7 Phase 1 plural frame presentation. Outcome: `howl-linux-host` now records accepted Phase 1 as `c11ffee Pluralize frame pane presentation`. Handoff: commit root pointer and accountability artifacts, then continue to Phase 2 bounded two-pane runtime ownership.
- Teammate `teammate-2026-06-21-layout-mux-two-pane-runtime-plan-01`: hello. Intent: plan Stage 7 Phase 2 bounded two-pane runtime ownership after plural presentation. Outcome: ready contract returned to convert `src/tab.zig` to two-pane ownership, aggregate runtime/render state, and update `events/event.zig` to consume tab-owned frame panes while keeping split actions/config/default bindings deferred. Handoff: orchestrator review required before coding.
- Reviewer `review-2026-06-21-layout-mux-two-pane-runtime-plan-01`: hello. Verdict: accept with clarification. Evidence: contract follows Zellij tiled new-pane pressure, tmux pane collection/spawn ownership, and WezTerm default split behavior while preserving Phase 2 as internal runtime machinery only. Clarification: `Tab.splitRight` and `Tab.splitDown` are internal methods only; no user-visible input actions, config fields, or default keybindings until Phase 3. Handoff: launch coder for Phase 2 only.
- Coder `coder-2026-06-21-layout-mux-two-pane-runtime-01`: hello. Intent: execute Stage 7 Phase 2 bounded two-pane runtime ownership only. Outcome: `src/tab.zig` now owns up to two terminal panes, active pane identity, split tree, internal splitRight/splitDown methods, aggregate runtime/render/present behavior, and frame pane production; `events/event.zig` consumes tab-owned active placement and frame panes. No input actions, config fields, default config, C/render ABI, or extra files changed. Handoff: ready for reviewer verification.
- Reviewer `review-2026-06-21-layout-mux-two-pane-runtime-01`: hello. Verdict: accept. Evidence: reviewed `tab.zig` and `events/event.zig`, checked aggregate runtime/render/present behavior against terminal surface methods, confirmed inactive panes do not admit published input, and reran `zig fmt build.zig src`, `zig build check`, `zig build test:unit`, host/root `git diff --check`, no-action/config gates, changed-file gates, and forbidden-owner scans successfully. Handoff: commit accepted host slice, then commit root pointer/accountability artifacts.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: commit accepted Stage 7 Phase 2 bounded two-pane runtime ownership. Outcome: `howl-linux-host` now records accepted Phase 2 as `ddaadd7 Add bounded two-pane tab runtime`. Handoff: commit root pointer/accountability artifacts, then plan Phase 3 user-visible split action/config binding.
- Teammate `teammate-2026-06-21-layout-mux-split-bindings-plan-01`: hello. Intent: plan Stage 7 Phase 3 user-visible split-right/split-down binding. Outcome: ready contract returned for `terminal_split_right`/`terminal_split_down`, terminal config fields `split_right`/`split_down`, default parser-supported key labels, and event dispatch to active tab split methods. Handoff: orchestrator review required before coding.
- Reviewer `review-2026-06-21-layout-mux-split-bindings-plan-01`: hello. Verdict: accept. Evidence: contract follows Zellij right/down new-pane pressure and WezTerm default split key pressure, uses current Howl action/config binding owners, and keeps all advanced pane behavior deferred. Handoff: launch coder for Phase 3 only.
- Coder `coder-2026-06-21-layout-mux-split-bindings-01`: hello. Intent: execute Stage 7 Phase 3 split action/config/default binding only. Outcome: added `terminal_split_right` and `terminal_split_down` actions, terminal config fields `split_right`/`split_down`, default bindings `ctrl+shift+alt+five` and `ctrl+shift+alt+apostrophe`, and event dispatch to active tab split methods. No capacity, cwd/custom command, floating/hidden pane behavior, move/transfer, resize UI, C/render ABI, file add, or file delete changes were made. Handoff: ready for reviewer verification.
- Reviewer `review-2026-06-21-layout-mux-split-bindings-01`: hello. Verdict: accept. Evidence: reviewed diff, confirmed changed files are exactly input keys, terminal config, default config, and event dispatch; verified split failures no-op without redraw, success requests redraw/config/title sync, and all advanced pane behavior remains deferred. Reran `zig fmt build.zig src`, `zig build check`, `zig build test:unit`, host/root `git diff --check`, changed-file gate, and ABI/deferred-feature scans. Handoff: commit accepted host slice, then commit root pointer/accountability artifacts.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: commit accepted Stage 7 Phase 3 split bindings. Outcome: `howl-linux-host` now records accepted Phase 3 as `02eaa45 Add split pane bindings`. Handoff: commit root pointer/accountability artifacts; initial Zellij-style split-right/split-down behavior is now user-visible with default command/cwd and bounded two-pane capacity.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: record remaining split runtime order after user observed current behavior. Outcome: research artifact now records current accepted state and the recommended remaining order: two-pane focus movement, pane close, pane resize, more than two panes, cwd/command semantics, pane movement/transfer, floating/hidden panes, and tab transfer across windows. Handoff: next recommended slice is two-pane focus movement only.
- Coder `coder-2026-06-21-layout-mux-tab-splits-01`: hello. Intent: execute accepted Stage 5 tab/splits seam only. Outcome: implementation and verification complete; `splits.zig` no longer imports `tab.zig`, `tab.zig` owns `placePanes`, and `singlePane` routes through a split leaf with no runtime/ABI broadening. Handoff: ready for reviewer verification.
- Coder `coder-2026-06-21-layout-mux-runtime-tab-01`: hello. Intent: execute accepted Stage 6 runtime tab owner structural slice only. Outcome: implementation and verification complete; `src/tab.zig` now owns one `PaneId.first` terminal runtime, tab slots store runtime tabs, and event orchestration routes through `RuntimeTab` without split/runtime/ABI broadening. Handoff: ready for reviewer verification.
- Teammate `ses_116502da4ffeGfmz3gkBalJn1T`: hello. Intent: plan Stage 8 Phase 1 two-pane focus movement from zellij/tmux/wezterm pressure without bucket cleanup. Outcome: ready contract returned for pane-direction focus movement bounded to two panes, using `layout.pane.Direction`, `Tab.focusPane`, layout-owned bindings, and event dispatch that redraws only on focus change. Handoff: accepted for implementation with user decision that Lua/config mappings can be practical and shuffled later.
- Coder `ses_116502da4ffeGfmz3gkBalJn1T`: hello. Intent: execute accepted Stage 8 Phase 1 only. Outcome: added two-pane focus movement, added layout-owned split/tab/pane bindings under `layout.bindings`, moved tab bindings out of `tab_bar`, kept terminal bindings terminal-local, and verified `zig fmt build.zig src`, `zig build check`, `zig build test:unit`, and `git diff --check`. Handoff: ready for commit.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: commit accepted Stage 8 Phase 1 focus movement and layout binding ownership. Outcome: `howl-linux-host` now records accepted work as `a49966c Add two pane focus movement`. Handoff: commit root pointer and accountability artifacts, then discuss the next layout/bucket pressure slice.
- Teammate `ses_11638bd08ffeFeIg9JUtmmmhFi`: hello. Intent: execute Stage 8 Phase 2A by reworking the dirty visibility policy toward Zellij's tab/pane info model while preserving the bucket-face and explicit-scrollbar cuts. Outcome: added `layout.pane.Kind`, `layout.pane.Visibility`, `TabInfo`, `PaneInfo`, selected/unselected tab info, tiled/visible/selectable current facts, and tests for counts, visibility, focus-only movement, input admission, and visible-unfocused redraw. Handoff: orchestrator review required before acceptance.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: review and tighten Stage 8 Phase 2A before any commit. Outcome: fixed the tautological pane kind test, asserted pane id initialization through `paneConst`, routed runtime facts/progress/frame pane policy through `PaneInfo`, preserved no bucket mux `active` argument, recorded Zellij citations in research, and verified `zig fmt --check build.zig src`, `zig build check`, `zig build test:unit`, and `git diff --check`. Handoff: host and root changes are accepted pending explicit commit request; no commit was made.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: execute the next smallest bucket-dissection cut after committing Phase 2A. Outcome: deleted bucket text/pointer input drain forwarding methods, made the terminal input adapter callable, routed `Tab.drainTextInputFastPath` and `Tab.drainPointerInput` directly through `input/processor.zig`, preserved active-pane-only input behavior, and verified `zig fmt --check build.zig src`, `zig build check`, `zig build test:unit`, and `git diff --check`. Handoff: commit host slice and root accountability, then continue with the next bucket face.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: continue bucket input-face deletion with the adjacent scroll wrapper. Outcome: deleted `Surface.handleScrollInput`, routed `Tab.handleScrollInput` directly to `terminal_scrollbar.handlePages` for the active pane, preserved active-pane scroll behavior, and verified `zig fmt --check build.zig src`, `zig build check`, `zig build test:unit`, and `git diff --check`. Handoff: commit host slice and root accountability, then continue with the next bucket face.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: continue active-pane policy deletion with hover input policy wrappers. Outcome: deleted `Surface.wantsLinkHover` and `Surface.wantsTerminalHoverReporting`, routed `Tab` directly to the active pane config/liveness and VT input reporting fact, preserved active-pane input policy behavior, and verified `zig fmt --check build.zig src`, `zig build check`, `zig build test:unit`, and `git diff --check`. Handoff: commit host slice and root accountability, then continue with the next bucket face.
- Orchestrator `orch-2026-06-21-layout-mux-01`: hello. Intent: continue active-pane bucket face deletion with paste. Outcome: deleted `Surface.paste`, routed `Tab.paste` directly to `term_input.publishPaste` and active pane cursor activity reset, preserved active-pane paste behavior, and verified `zig fmt --check build.zig src`, `zig build check`, `zig build test:unit`, and `git diff --check`. Handoff: commit host slice and root accountability, then continue with the next bucket face.
