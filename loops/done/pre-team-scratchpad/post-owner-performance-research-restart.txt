ASCII rain post-owner performance research restart

Date: 2026-06-09.
Role owner: orchestrator.
Status: active.
Orchestrator session id: `orch-2026-06-08-ascii-rain-performance-01`.
Researcher session id: `research-2026-06-09-post-owner-performance-01`.
Reviewer session id: `rev-2026-06-09-post-owner-performance-01`.
Coder session id: none authorized.
Scope: restart from the researcher stage after the rejected `emitter-alpha-reuse-fast-path` probe.

Problem statement:

- The broad sprint goal remains beating Alacritty on the agreed ASCII rain benchmark.
- The post-owner baseline is accepted and remains the current truth.
- The first emitter alpha-reuse probe was rejected at verification.
- The failed probe did not expose a new bucket owner, but it did invalidate the planned fix shape for the next slice.

Accepted baseline receipts:

- clean benchmark:
  - `/home/home/personal/projects/howl/artifacts/stress/20260609-115340-ascii/summary.json`
  - Howl `79.42 fps`
  - Alacritty `1039.12 fps`
- direct host accounting:
  - `/home/home/personal/projects/howl/artifacts/stress/20260609-095340-ascii-direct-post-owner/howl-direct.accounting.log`
  - direct host run `109.56 fps`
- detailed timing:
  - `/home/home/personal/projects/howl/artifacts/stress/20260609-095743-ascii-direct-post-owner-timing/howl-term.stderr.log`

Rejected execution receipt lineage:

- reviewer session id: `rev-2026-06-09-post-owner-performance-01`
- coder session id: `coder-2026-06-09-emitter-alpha-reuse-fast-path-01`
- rejection reason:
  - the probe removed `stage_upload_avg_us`
  - but replaced it with a worse per-query prepared-sprite byte hash
  - real benchmark regressed, so the slice is dropped

Current authorized work:

- research correction only
- no code edits are authorized from this loop

Required research output:

- decide whether the next slice stays in:
  - `howl-render/src/prepared/render_surface_emitter.zig`
  - `howl-render/src/prepared/sprite_resource_store.zig`
  or switches to:
  - `howl-render/src/text/direct_normal.zig`
- prove the next slice against:
  - current source
  - accepted post-owner baseline receipts
  - the failed-probe findings
- return exact allowed files, required shape, tests, non-goals, stop conditions, and receipts for the next coder slice

Hard stops:

- stop if new proof exposes another bucket or false owner
- stop if the only remaining candidate requires broadening into session, FFI, or host GL ownership
- stop if the next slice cannot beat or at least honestly target the accepted post-owner baseline

Acceptance gate for leaving research restart:

- same reviewer accepts the corrected next-slice plan
- `sprints/current.txt`, the active loop, and the active research file all point at the corrected plan
- only after that may a new coder session be seeded
