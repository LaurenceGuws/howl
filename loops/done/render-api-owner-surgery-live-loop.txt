Historical authority: active loop during the 2026-06-13 render API owner surgery sprint.

Why superseded or done: sprint closed with final root receipts `8aa8327` and `57bba4b`.

Must not be used for: current sprint authority, new render folder/file owner planning, or execution authorization.

Render API owner surgery live loop scratchpad

Date: 2026-06-13.

Role owner: orchestrator.

Status: active planning loop.

Orchestrator session id: `orch-2026-06-13-render-api-owner-surgery-01`.

Researcher session id: `research-2026-06-13-render-api-owner-surgery-01`.

Reviewer session id: `review-2026-06-13-render-api-owner-surgery-01`.

Loop commit-hash receipt: planning package closed in root commit `e5f7e4b`.

Shared scratchpad rule:

- This file is the live team scratchpad for the current step.
- Every non-trivial orchestrator, researcher, reviewer, or coder pass must append a short note with role/session id, hello, intent, one outcome, and next handoff or blocker.

Controlling sprint:

- `/home/home/personal/projects/howl/sprints/2026-06-13-render-api-owner-surgery-sprint.md`

Active research target:

- `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md`

Current step:

- The render API ownership planning package is accepted and committed in root `e5f7e4b`.
- Slice 1, `VT Publication Boundary And Storage`, is seeded for execution.
- Coder may execute only the active condensed execution contract below.

Exact user direction:

- New sprint focus: cut render into real idiomatic owners.
- `howl-render/src/tv_surface` is fake.
- `howl-render/src/tv_surface/vt.zig` is using renderer C imports and redefining VT structs.
- `howl-render/src/session` is fake and may need to move toward cache or store naming after checking Alacritty.
- `howl-render/src/prepared` is a big fake bucket.
- File names are unacceptable.
- `text`, effects, geometry, and related render API structure are mixed together and must be recut.
- Nothing about the current render API structure is accepted as authority without reference proof.

Exact non-goals:

- No implementation during planning.
- No sprint narrowing, v1 language, or partial cleanup unless the user explicitly narrows scope.
- No Howl-only owner inventions while Alacritty, Ghostty, TigerBeetle, Kitty, or official docs provide source-backed pressure.
- No compatibility aliases or wrapper preservation without a concrete ABI/downstream need.
- No host/VT Zig-shaped convenience imports across the C ABI boundary.

Exact stop conditions:

- Stop if the active research cannot name exact allowed files, required shapes, tests, non-goals, stop conditions, and receipt fields for each slice.
- Stop if current source and references conflict in a way that needs explicit user override.
- Stop if proposed changes would bypass the C ABI boundary between `howl-render`, `howl-vt`, and hosts.
- Stop if active `loops/`, `research/`, or `sprints/` contain stale non-archive artifacts.

Team scratchpad

1. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: seed the new render API owner surgery sprint with reviewer-gated planning and no implementation authority.
- How one task went: closed minification artifacts were archived as done with historical-authority headers; `sprints/current.txt` now points only at the render owner surgery sprint, loop, and research target; the sprint contract records the user-specified fake owner seams and requires researcher output plus reviewer acceptance before coding.
- Next handoff or blocker: researcher must produce the full source-backed render owner plan; reviewer gates it before any execution slice.

2. Role: researcher
- Session id: `research-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: produce the full source-backed render API owner surgery sprint plan with exact slices and no implementation.
- How one task went: wrote `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md` with current-source facts, Alacritty/Ghostty/TigerBeetle anchors, proposed owner shape, seven ordered execution slices, tests, assertions, stop conditions, and commit-hash receipt demands.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` must gate the plan before orchestrator seeds any coder slice; planning commit-hash receipt is still pending.

3. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review the render API owner surgery planning package only, gate acceptance, and append the required loop note without implementing product code.
- How one task went: reviewed the workflow, role docs, active sprint, active loop, active research plan, active artifact hygiene, and current source spot checks; active top-level `loops/`, `research/`, and `sprints/` contain only the expected live artifacts, but the plan is not executable without coder invention.
- Outcome: reject. Findings: `research/2026-06-13-render-api-owner-surgery-plan.md:87-99`, `:117`, `:141`, and `:153` conflict or leave conditional owner choices (`vt_publication/storage` versus `src/storage`, and `render_session.zig` versus `text/renderer.zig`) instead of one required shape; `:116`, `:128`, `:140`, `:152`, `:164`, `:176`, and `:188` do not provide exact enough allowed-file coverage for current live import users such as `howl-render/src/prepare_request.zig:5`, `src/benchmark_main.zig:3-5`, `src/submission.zig:3-4`, `src/handle.zig:2-3`, `src/work_state.zig:3`, `src/text/shape/cluster.zig:5-6`, `src/text/surface_preparer.zig:19-20`, `src/text/direct_normal.zig:14-15`, and `src/test_support.zig:10-17`; `build.zig:124-150` makes the benchmark part of `check`, so deleting `tv_surface` without an exact benchmark migration slice will break required proof; `:188` uses the non-exact bucket `any render source file touched by slices 1-6` and loose documentation files, which violates the exact-files gate.
- Next handoff or blocker: researcher must revise the planning package before implementation is authorized; the corrected plan must name one owner shape per slice, reconcile storage ownership, enumerate all live source/test/benchmark/FFI import users in exact allowed files, and keep commit-hash receipt demands intact for reviewer re-gate.

4. Role: researcher
- Session id: `research-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: correct the rejected render API owner surgery research plan without implementing product code.
- How one task went: revised `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md` to fix one exact owner shape (`storage/publication_storage.zig`, `render_session.zig`, existing `text/raster/cache.zig`), enumerate live import users and benchmark/check migration files by exact path, require `check` where benchmark imports are affected, and remove the non-exact final-sweep file bucket.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should re-review the corrected plan; planning commit-hash receipt remains pending.

5. Role: researcher
- Session id: `research-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: correct the second reviewer rejection by resolving cache/store/session proof, text/effects/geometry recut coverage, and geometry/resource ownership conflict.
- How one task went: reread `text/contract.zig`, `text/ft_hb/cache.zig`, `text/raster/cache.zig`, `text/surface_preparer.zig`, `text/direct_normal.zig`, `geometry/render_surface_realizer.zig`, and resource/caller tests; updated the plan with exact text owner files, final FT/HB cache and raster atlas ownership, and separate emit-time versus realizer resource stores under `surface`.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should re-review; no implementation is authorized and planning commit-hash receipt remains pending.

5. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: re-review the corrected render API owner surgery planning package against the prior findings and full planning gate, without implementing product code.
- How one task went: verified active artifact hygiene is clean and the prior mechanical defects are mostly corrected: storage now has one owner, the long-lived owner is fixed to `render_session.zig`, the prior live import users and benchmark/check migration paths are enumerated, benchmark-triggered `check` proof is required, and the former non-exact final-sweep `any render source file touched by slices 1-6` language is gone.
- Outcome: reject. Findings: `research/2026-06-13-render-api-owner-surgery-plan.md:93` and `:225` keep `text/raster/cache.zig` as the cache owner while admitting all `text/` cache/raster files were not fully inspected and deferring glyph-cache naming to a later sprint; that is weak evidence and narrows the user-directed session/cache/store naming pressure in `sprints/2026-06-13-render-api-owner-surgery-sprint.md:22`, `:25`, and `loops/render-api-owner-surgery-live-loop.txt:42`, `:45`. The corrected slice plan still does not explicitly scope the full text/effects/geometry recut: current `howl-render/src/text/contract.zig:10-27`, `:67-90`, and `:92-112` mix semantic colors/effects, decoration/cursor geometry, grid/cell metrics, and cell input in one text contract, but no slice gives an exact required shape, tests, or stop condition for splitting those owners. Current `howl-render/src/geometry/render_surface_realizer.zig:17-34` owns a C render-surface `ResourceStore` inside geometry while the plan creates `surface/resource_store.zig` in `research/2026-06-13-render-api-owner-surgery-plan.md:98` and slice 5 without listing or resolving `geometry/render_surface_realizer.zig`; that leaves a geometry/resource owner conflict outside the slice queue. Because planning must cover the full user problem and cannot defer a source-backed owner decision to a later sprint without explicit user narrowing, implementation remains unauthorized.
- Next handoff or blocker: researcher must revise the plan again to either source-prove that `text/contract.zig`, `text/raster/cache.zig`, and `geometry/render_surface_realizer.zig` are already acceptable owners or add exact ordered slices with allowed files, required shapes, tests, non-goals, stop conditions, session ids, and commit-hash receipt demands for those owner cuts.

6. Role: researcher
- Session id: `research-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: correct the second reviewer rejection after rereading the cited cache, text contract, and realizer resource sources.
- How one task went: updated the active plan so cache ownership is final (`text/ft_hb/cache.zig` and `text/raster/atlas.zig`), added an exact text color/effects/metrics/cell-input/scene-contract recut slice, and resolved the geometry/resource conflict by moving CPU realization and realized resource state to `surface/realizer.zig` and `surface/realizer_resource_store.zig` while keeping emit-time allocation in `surface/resource_store.zig`.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should re-review; no implementation is authorized and planning commit-hash receipt remains pending.

7. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: re-review the corrected active research plan against the prior remaining blockers and full planning gate, without implementing product code.
- Findings: no blocking findings. The plan now fixes cache/store/session ownership with final owners `text/ft_hb/cache.zig`, `text/raster/atlas.zig`, and `render_session.zig`; adds an exact text color/effects/metrics/cell-input/scene-contract recut slice with exact allowed files, tests, non-goals, stop conditions, session id, and commit receipt; and resolves the `geometry/render_surface_realizer.zig` resource conflict by moving CPU realization to `surface/realizer.zig`, realized C-resource state to `surface/realizer_resource_store.zig`, and emit-time allocation to `surface/resource_store.zig`. Active artifact hygiene remains clean: only the expected active loop, research plan, sprint, and current index are present. The plan keeps implementation unauthorized until reviewer acceptance and orchestrator planning receipt closure.
- Outcome: accept.
- First executable slice expectations: orchestrator may seed Slice 1 only after recording/closing the planning commit-hash receipt. Slice 1 worker id is `coder-2026-06-13-render-api-owner-surgery-01`; allowed files are exactly those in `research/2026-06-13-render-api-owner-surgery-plan.md:133`; required shape is `vt_publication/publication.zig`, `vt_publication/abi.zig`, and `storage/publication_storage.zig` with no `vt_publication/storage.zig`; required proof is `zig build -Dskip-host=true test:unit` and `zig build -Dskip-host=true check` from repo root; commit receipt demand is `slice-1-vt-publication-boundary=<hash>`.
- Next handoff or blocker: orchestrator should record planning acceptance/commit receipt, then seed exactly Slice 1; no broader implementation is authorized.

8. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: record reviewer acceptance and stop before execution until the planning commit-hash receipt is closed.
- How one task went: active sprint, research, and current index now mark the render API owner surgery plan as reviewer-accepted with planning commit-hash receipt pending; no coder is authorized because the receipt is not closed.
- Next handoff or blocker: planning receipt is now closed; orchestrator may seed Slice 1 and start autonomous execution under the accepted sprint.

9. Role: researcher
- Session id: `research-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: correct the accepted planning package after Slice 1 execution exposed invalid render proof commands.
- How one task went: rechecked the active render plan against `howl-render/build.zig:85-90` and `:121-150`, removed the invalid `-Dskip-host=true` flag from every render proof command, and made the required cwd exact: `/home/home/personal/projects/howl/howl-render` for `zig build test:unit`, `zig build test:abi`, and `zig build check`.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should re-gate the planning correction before further execution continues.

10. Role: researcher
- Session id: `research-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: correct the Slice 3 planning defect after execution exposed remaining `tv_surface` import users outside the seeded allowed-file list.
- How one task went: reread the remaining import users in `storage/publication_storage.zig`, `renderable_content/content.zig`, `renderable_content/color.zig`, `renderable_content/cursor.zig`, `vt_publication/abi.zig`, `vt_publication/publication.zig`, and `session/text.zig`; updated Slice 3 so its exact allowed files cover every remaining live `tv_surface` import that must change before the deletion stop condition can pass.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should re-gate the planning correction before Slice 3 execution continues.

Condensed execution contract for coder session `coder-2026-06-13-render-api-owner-surgery-01`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md`
- Implement only Slice 1, `VT Publication Boundary And Storage`.
- Allowed files:
  - `howl-render/src/tv_surface/vt.zig`
  - `howl-render/src/tv_surface/cell.zig`
  - `howl-render/src/tv_surface/slot.zig`
  - `howl-render/src/tv_surface/damage.zig`
  - `howl-render/src/prepare_request.zig`
  - `howl-render/src/benchmark_main.zig`
  - `howl-render/src/session/text.zig`
  - `howl-render/src/session/text_test.zig`
  - `howl-render/src/test_support.zig`
  - `howl-render/src/test_unit.zig`
  - `howl-render/src/vt_publication/abi.zig`
  - `howl-render/src/vt_publication/publication.zig`
  - `howl-render/src/storage/publication_storage.zig`
- Required shape:
  - create `vt_publication/publication.zig` for `PublicationSource` and `VtSnapshot`.
  - create `vt_publication/abi.zig` for aliases to shipped `HowlVt*` structs and C result validation.
  - create `storage/publication_storage.zig` for retained publication storage currently in `slot.zig`.
  - `cell.zig` local duplicate structs remain temporary compatibility input only until Slice 2 moves them to `renderable_content/content.zig`; they must not become storage or VT ABI truth.
  - no `vt_publication/storage.zig` may be created.
- Required assertions:
  - cols/rows nonzero before allocation.
  - cell count checked with overflow handling.
  - dirty spans equal rows.
  - copied cell count equals `cols * rows`.
  - `combining_len <= combining.len`.
  - color kind is one of shipped VT kinds.
  - underline style is in range.
  - retained storage sources are never freed as owned duplicates.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-render && zig build check`
  - keep or move existing prepare-request and publication validation tests through the existing unit root.
  - add or keep owner-local tests for invalid color kind, invalid combining length, dirty span length mismatch, retained source deinit not freeing slot storage, and copy-in preserving snapshot/dirty metadata.
- Non-goals:
  - no text scene mapping.
  - no prepare admission changes beyond imports.
  - no C ABI header changes.
  - no host changes.
- Stop conditions:
  - stop if any render code imports VT Zig internals.
  - stop if a non-ABI duplicate of `HowlVtSurfaceCell` remains as render truth.
  - stop if C header changes seem required.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-render-api-owner-surgery-01`
  - commit-hash handoff status for orchestrator receipt closure

9. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: close the accepted planning receipt and seed Slice 1 for execution.
- How one task went: the accepted planning package was committed as root `e5f7e4b`, the active sprint/research/current index now record that receipt, and Slice 1 has an exact condensed execution contract with allowed files, assertions, tests, non-goals, stop conditions, and receipt demand.
- Next handoff or blocker: coder may execute only `coder-2026-06-13-render-api-owner-surgery-01`; no broader slice or product-code work is authorized.

10. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: reseed Slice 1 after accepted planning correction for stale render proof commands.
- How one task went: the accepted research correction proved `howl-render/build.zig` exposes `zig build test:unit`, `zig build test:abi`, and `zig build check` directly, so the live Slice 1 contract now uses exact current commands and exact cwd instead of the invalid `-Dskip-host=true` flags.
- Next handoff or blocker: commit the accepted planning correction, then rerun Slice 1 proof and send the existing working-tree diff to reviewer under the refreshed contract.

10. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: implement Slice 1 VT publication boundary/storage owner cuts inside the allowed files only, then run the exact unit and check proofs.
- How one task went: created `vt_publication/abi.zig`, `vt_publication/publication.zig`, and `storage/publication_storage.zig`; turned `tv_surface/vt.zig` and `tv_surface/slot.zig` into compatibility re-export seams; updated allowed import users to point at the new publication/storage owners where this slice permits imports only.
- Next handoff or blocker: run `zig build -Dskip-host=true test:unit` and `zig build -Dskip-host=true check`, then hand results to reviewer/orchestrator with the slice 1 receipt status.

11. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: finish Slice 1 proof by running the exact required verification commands and record the accountable handoff.
- How one task went: `zig fmt` succeeded for all edited Zig files, but both required proof commands failed before any build work because this workspace no longer accepts `-Dskip-host`: `zig build -Dskip-host=true test:unit` -> `error: invalid option: -Dskip-host`; `zig build -Dskip-host=true check` -> `error: invalid option: -Dskip-host`. Per the sprint contract I did not substitute different proof commands after that exact command failure.
- Next handoff or blocker: reviewer/orchestrator need to decide whether the active slice contract must be refreshed with the current build option spelling before proof can continue; commit-hash handoff status is pending reviewer acceptance and no git was touched.

12. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review the planning-only correction after Slice 1 exposed invalid render proof commands, without implementing product code.
- Findings: the active research plan is corrected to current render build entrypoints from `howl-render/build.zig:85-90` and `:121-150`: it now requires `zig build test:unit`, `zig build test:abi`, and `zig build check` in exact cwd `/home/home/personal/projects/howl/howl-render` at `research/2026-06-13-render-api-owner-surgery-plan.md:124`, with the same exact command/cwd language carried into slice proofs at `:136`, `:148`, `:160`, `:172`, `:184`, `:196`, `:208`, and `:220`. No new planning vagueness, wildcard file scope, or user-scope narrowing was introduced in the research artifact. This is a planning correction only and does not authorize broader implementation beyond reseeding the active Slice 1 execution contract. The live loop still contains the stale condensed Slice 1 proof commands with `-Dskip-host=true` at `loops/render-api-owner-surgery-live-loop.txt:173-174`, so orchestrator must refresh that condensed execution contract before execution resumes.
- Outcome: accept.
- Next handoff or blocker: orchestrator may reseed Slice 1 with the corrected proof commands and exact cwd, then the current uncommitted Slice 1 diff may be reused for normal slice review after rerunning proof under the refreshed contract; no broader slice is authorized.

13. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: fix the Slice 1 compile failures found after the proof commands were corrected and rerun the exact proof.
- How one task went: fixed the non-mutated local binding, made the storage test fixture use `tv_surface.cell.CursorInfo`, converted const test arrays to mutable slices for owned publication input, zero-initialized reserved-slot cells before validation, and corrected the retained-storage dirty-span expectation; then `zig build test:unit` and `zig build check` both passed in `/home/home/personal/projects/howl/howl-render`.
- Next handoff or blocker: reviewer/orchestrator can treat the existing working-tree diff as normal Slice 1 execution and verify it against the accepted contract.

14. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi

19. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-03`
- Hello: hi
- Intended task: implement Slice 3, `Prepare Queue Cutover And tv_surface Deletion`, inside the allowed files only.
- How one task went: stopped before product-code edits because the seeded Slice 3 contract is not executable as written. Current live imports to `tv_surface/` still exist in non-allowed files: `howl-render/src/storage/publication_storage.zig:2-3`, `howl-render/src/renderable_content/content.zig:2`, `howl-render/src/renderable_content/color.zig:2`, `howl-render/src/renderable_content/cursor.zig:2`, `howl-render/src/vt_publication/abi.zig:3`, `howl-render/src/vt_publication/publication.zig:249`, and `howl-render/src/session/text.zig:10,13`. Deleting the seven `tv_surface` files would violate the slice stop condition because imports would still contain `tv_surface/`, but correcting them would require edits outside the allowed Slice 3 file list.
- Next handoff or blocker: orchestrator/reviewer need to reseed Slice 3 with an exact allowed-file list that includes every remaining `tv_surface` import user, or else adjust the stop condition; no git was touched and no product-code files were changed.
- Intended task: review execution Slice 1, `VT Publication Boundary And Storage`, against allowed-file scope, owner shape, temporary compatibility boundaries, assertions/tests, and claimed proof.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must verify the passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`; verify only Slice 1 allowed render files are staged; do not stage unrelated workspace files such as root `devtools_options.yaml`; record the commit hash as `slice-1-vt-publication-boundary=<hash>` before seeding Slice 2.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested render Slice 1 change, record the root receipt, and seed exactly Slice 2.

15. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: close Slice 1 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`; nested `howl-render` status matched the Slice 1 allowed files; accepted implementation committed as `howl-render` `3d9ff6b` (`Cut VT publication boundary`).
- Commit-hash receipt: `slice-1-vt-publication-boundary=3d9ff6b`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root Slice 1 receipt and seed Slice 2 from the accepted research plan.

Condensed execution contract for coder session `coder-2026-06-13-render-api-owner-surgery-02`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md`
- Implement only Slice 2, `Renderable Content And Damage Owners`.
- Allowed files:
  - `howl-render/src/tv_surface/text_input.zig`
  - `howl-render/src/tv_surface/publication_cell_map.zig`
  - `howl-render/src/tv_surface/damage.zig`
  - `howl-render/src/tv_surface/cell.zig`
  - `howl-render/src/benchmark_main.zig`
  - `howl-render/src/session/text.zig`
  - `howl-render/src/session/text_test.zig`
  - `howl-render/src/text/contract.zig`
  - `howl-render/src/text/direct_normal.zig`
  - `howl-render/src/text/surface_preparer.zig`
  - `howl-render/src/text/shape/cluster.zig`
  - `howl-render/src/test_support.zig`
  - `howl-render/src/renderable_content/content.zig`
  - `howl-render/src/renderable_content/color.zig`
  - `howl-render/src/renderable_content/cursor.zig`
  - `howl-render/src/damage/publication_damage.zig`
- Required shape:
  - move VT-publication-to-text input mapping into `renderable_content/content.zig`.
  - move theme/palette and color mapping into `renderable_content/color.zig`.
  - move cursor mapping into `renderable_content/cursor.zig`.
  - move dirty metadata validation/canonicalization and snapshot classification into `damage/publication_damage.zig`.
  - names are fixed for this sprint; no `damage/tracker.zig`, broad `map.zig`, or alternate renderable-content file names are allowed.
- Required assertions:
  - semantic empty classification asserts default fg/bg when empty.
  - inverse/selection assert resulting opaque colors.
  - dirty rows assert row bounds and column start/end order.
  - cursor row/col asserts within publication grid when visible.
  - full damage asserts all rows/cols are covered or explicitly classified full.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-render && zig build check`
  - move existing publication cell map tests.
  - add tests for default background opacity, inverse style, selected style, invisible/continuation not falsely empty, cursor blink hidden/visible mapping, partial dirty row canonicalization, and full damage classification when cursor/color/geometry/scroll changes.
- Non-goals:
  - no text shaping/raster changes.
  - no resource store changes.
  - no C ABI header changes.
- Stop conditions:
  - stop if `renderable_content` starts owning VT storage or prepare queue state.
  - stop if damage code imports text raster/font owners.
  - stop if the old `tv_surface` folder still contains live owner code after this slice except temporary compatibility imports explicitly scheduled for deletion in Slice 3.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-render-api-owner-surgery-02`
  - commit-hash handoff status for orchestrator receipt closure

16. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-02`
- Hello: hi
- Intended task: implement Slice 2, `Renderable Content And Damage Owners`, inside the allowed files only.
- How one task went: moved publication-to-text input mapping into `renderable_content/content.zig`, theme/palette and semantic color styling into `renderable_content/color.zig`, cursor mapping into `renderable_content/cursor.zig`, dirty metadata validation/canonicalization into `damage/publication_damage.zig`, and reduced the old `tv_surface` files to temporary compatibility seams only.
- Next handoff or blocker: reviewer/orchestrator can verify the Slice 2 diff under the accepted contract.

17. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 2, `Renderable Content And Damage Owners`, against allowed-file scope, owner shape, temporary compatibility seams, assertions/tests, and claimed proof.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must verify passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`; verify nested `howl-render` status still stages only the Slice 2 allowed files; record the commit hash as `slice-2-renderable-content-damage=<hash>` before seeding Slice 3.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested render Slice 2 change, record the root receipt, and seed exactly Slice 3.

18. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review the planning-only correction after Slice 3 exposed missing allowed-file coverage for remaining `tv_surface` import users, without implementing product code.
- Findings: the active research plan corrects Slice 3 exactly. `research/2026-06-13-render-api-owner-surgery-plan.md:157-158` now includes the remaining live `tv_surface` import users needed to satisfy the deletion stop condition: `storage/publication_storage.zig`, `renderable_content/content.zig`, `renderable_content/color.zig`, `renderable_content/cursor.zig`, `vt_publication/abi.zig`, `vt_publication/publication.zig`, and `session/text.zig`, matching the current source grep hits in those files plus the remaining benchmark `tv_surface/cell.zig` import. No new wildcard file language, scope drift, or user-scope narrowing was introduced in the research artifact. This is a planning correction only and does not authorize broader implementation beyond reseeding Slice 3 with a refreshed condensed execution contract.
- Outcome: accept.
- Next handoff or blocker: orchestrator may reseed Slice 3 and reuse the current no-product-change worktree state, but must first refresh the live condensed Slice 3 contract in this loop to match the corrected research allowlist before coder execution resumes.

18. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: close Slice 2 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`; nested `howl-render` status matched the Slice 2 allowed files; accepted implementation committed as `howl-render` `d1bfa57` (`Cut renderable content owners`).
- Commit-hash receipt: `slice-2-renderable-content-damage=d1bfa57`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root Slice 2 receipt and seed Slice 3 from the accepted research plan.

Condensed execution contract for coder session `coder-2026-06-13-render-api-owner-surgery-03`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md`
- Implement only Slice 3, `Prepare Queue Cutover And tv_surface Deletion`.
- Allowed files:
  - `howl-render/src/tv_surface/prepare_request.zig`
  - `howl-render/src/tv_surface/vt.zig`
  - `howl-render/src/tv_surface/cell.zig`
  - `howl-render/src/tv_surface/slot.zig`
  - `howl-render/src/tv_surface/publication_cell_map.zig`
  - `howl-render/src/tv_surface/damage.zig`
  - `howl-render/src/tv_surface/text_input.zig`
  - `howl-render/src/prepare/queue.zig`
  - `howl-render/src/prepare_request.zig`
  - `howl-render/src/benchmark_main.zig`
  - `howl-render/src/storage/publication_storage.zig`
  - `howl-render/src/renderable_content/content.zig`
  - `howl-render/src/renderable_content/color.zig`
  - `howl-render/src/renderable_content/cursor.zig`
  - `howl-render/src/vt_publication/abi.zig`
  - `howl-render/src/vt_publication/publication.zig`
  - `howl-render/src/session/text.zig`
  - `howl-render/src/session/text_test.zig`
  - `howl-render/src/text/direct_normal.zig`
  - `howl-render/src/text/surface_preparer.zig`
  - `howl-render/src/text/shape/cluster.zig`
  - `howl-render/src/test_support.zig`
  - `howl-render/src/prepare_request_test.zig`
  - `howl-render/src/test_unit.zig`
- Required shape:
  - create `prepare/queue.zig` for active prepare admission/take/consume/retry/retire and blink refresh.
  - prepare queue may depend on `vt_publication`, `storage/publication_storage.zig`, `damage/publication_damage.zig`, `geometry/tokens.zig`, and `geometry/geometry_contract.zig` only.
  - migrate every remaining live import away from all seven `tv_surface` files, including the remaining users in `storage/publication_storage.zig`, `renderable_content/content.zig`, `renderable_content/color.zig`, `renderable_content/cursor.zig`, `vt_publication/abi.zig`, `vt_publication/publication.zig`, and `session/text.zig`, and then delete `tv_surface/prepare_request.zig`, `tv_surface/vt.zig`, `tv_surface/cell.zig`, `tv_surface/slot.zig`, `tv_surface/publication_cell_map.zig`, `tv_surface/damage.zig`, and `tv_surface/text_input.zig`.
- Required assertions:
  - active source is present before consuming.
  - token equality is asserted before returning prepare consume.
  - retry only flips taken state for the same token.
  - geometry epoch changes force full damage.
  - retained source clone/deinit ownership is explicit.
  - blink refresh cannot fabricate a source.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-render && zig build check`
  - preserve and relocate all prepare request tests.
  - add tests for no-op duplicate source admission, geometry change forcing full, stale base forcing full for partial damage, blink refresh after taken prepare, retry taken prepare token mismatch, and retire at or before submitted token.
- Non-goals:
  - no text session rename yet.
  - no prepared/surface changes.
  - no host changes.
- Stop conditions:
  - stop if any import path still contains `tv_surface/` after this slice.
  - stop if a live concept remains in `tv_surface` without an owner named above.
  - stop if prepare queue needs text or font imports.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-render-api-owner-surgery-03`
  - commit-hash handoff status for orchestrator receipt closure

19. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-03`
- Hello: hi
- Intended task: implement Slice 3, `Prepare Queue Cutover And tv_surface Deletion`, inside the allowed files only.
- How one task went: stopped before product-code edits because the seeded Slice 3 contract was not executable as written. Current live imports to `tv_surface/` still existed in non-allowed files: `howl-render/src/storage/publication_storage.zig:2-3`, `howl-render/src/renderable_content/content.zig:2`, `howl-render/src/renderable_content/color.zig:2`, `howl-render/src/renderable_content/cursor.zig:2`, `howl-render/src/vt_publication/abi.zig:3`, `howl-render/src/vt_publication/publication.zig:249`, and `howl-render/src/session/text.zig:10,13`. Deleting the seven `tv_surface` files would have violated the slice stop condition because imports would still contain `tv_surface/`, but correcting them required edits outside the seeded allowlist.
- Next handoff or blocker: orchestrator/reviewer must refresh Slice 3 with an exact allowed-file list that includes every remaining `tv_surface` import user before coding resumes; no git was touched and no product code changed.

20. Role: researcher
- Session id: `research-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: correct the Slice 3 planning defect exposed by the blocked coder pass.
- How one task went: re-read the cited remaining import users and expanded Slice 3's exact allowlist to include `storage/publication_storage.zig`, `renderable_content/content.zig`, `renderable_content/color.zig`, `renderable_content/cursor.zig`, `vt_publication/abi.zig`, `vt_publication/publication.zig`, and `session/text.zig`, with the required shape now calling those remaining users out explicitly.
- Next handoff or blocker: reviewer must re-gate the Slice 3 planning correction before execution resumes.

21. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review the Slice 3 planning correction after the blocked coder pass, without implementing product code.
- Findings: the active research plan now fixes Slice 3 allowed-file coverage exactly by including the missing remaining `tv_surface` import users `storage/publication_storage.zig`, `renderable_content/content.zig`, `renderable_content/color.zig`, `renderable_content/cursor.zig`, `vt_publication/abi.zig`, `vt_publication/publication.zig`, and `session/text.zig`, matching the current remaining `tv_surface/` import users in source. No new planning vagueness, wildcard scope, or user-scope narrowing was introduced. This is a planning correction only and does not authorize broader implementation. The live loop's condensed Slice 3 contract must be refreshed to match the corrected research allowlist before coding resumes.
- Outcome: accept.
- Next handoff or blocker: orchestrator may reseed Slice 3 and reuse the current no-product-change worktree state, but must first refresh the live condensed Slice 3 contract to match the corrected research allowlist.

22. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: reseed Slice 3 after the accepted planning correction for missing `tv_surface` import users.
- How one task went: the live Slice 3 contract now matches the accepted research allowlist and required shape, so execution can resume without forcing out-of-scope edits.
- Next handoff or blocker: commit the accepted planning correction, then rerun Slice 3 under the refreshed contract.

23. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-03`
- Hello: hi
- Intended task: implement Slice 3, `Prepare Queue Cutover And tv_surface Deletion`, under the refreshed allowlist.
- How one task went: created `src/prepare/queue.zig` as the active prepare owner, moved `session/text.zig` off `tv_surface/prepare_request.zig`, removed every remaining `tv_surface/` import from the refreshed allowlist by pointing storage, renderable-content, VT publication, and benchmark code at real owners, replaced the last `tv_surface/cell.zig` cursor compatibility dependency with VT publication ABI cursor truth, and deleted all seven `tv_surface` files after cutover.
- Next handoff or blocker: reviewer/orchestrator can verify the Slice 3 diff under the accepted contract.

24. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 3, `Prepare Queue Cutover And tv_surface Deletion`, against the refreshed contract, owner boundaries, deletions, assertions/tests, and proof receipts.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must verify passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`; verify final `tv_surface/` grep under `howl-render/src` returned no matches; verify `howl-render/src/tv_surface` glob returned no files; verify nested `howl-render` status stages only the Slice 3 allowed files; record the commit hash as `slice-3-prepare-storage-cutover=<hash>` before seeding Slice 4.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested render Slice 3 change, record the root receipt, and seed exactly Slice 4.

25. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: close Slice 3 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, `cd /home/home/personal/projects/howl/howl-render && zig build check`, `rg "tv_surface/" src`, and `src/tv_surface` glob; nested `howl-render` status matched the Slice 3 allowed files; accepted implementation committed as `howl-render` `0f4fdb1` (`Delete tv surface bucket`).
- Commit-hash receipt: `slice-3-prepare-storage-cutover=0f4fdb1`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root Slice 3 receipt and seed Slice 4 from the accepted research plan.

26. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-04`
- Hello: hi
- Intended task: implement Slice 4, `Text Effects Geometry Contract Recut`, inside the allowed files only.
- How one task went: split `text/contract.zig` into exact owner files `color.zig`, `effects.zig`, `metrics.zig`, `cell_input.zig`, and `scene_contract.zig`; reduced `text/contract.zig` to a curated re-export root plus smoke test; moved the live raster atlas owner into `text/raster/atlas.zig`; updated live imports; deleted `text/raster/cache.zig`; and kept `text/ft_hb/cache.zig` as the FT/HB cache owner.
- Next handoff or blocker: reviewer/orchestrator can verify the Slice 4 diff under the accepted contract.

27. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 4, `Text Effects Geometry Contract Recut`, against allowed-file scope, owner splits, atlas/cache ownership, assertions/tests, and proof receipts.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must verify passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`; confirm nested `howl-render` status stages only the Slice 4 allowed files; verify `howl-render/src/text/raster/cache.zig` is absent; verify `howl-render/src/text/raster/atlas.zig` is the live atlas file; record the commit hash as `slice-4-text-effects-geometry-contract-recut=<hash>` before seeding Slice 5.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested render Slice 4 change, record the root receipt, and seed exactly Slice 5.

28. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: close Slice 4 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`; `text/raster/cache.zig` is absent; `text/raster/atlas.zig` is the live atlas file; nested `howl-render` status matched the Slice 4 allowed files; accepted implementation committed as `howl-render` `3ce570f` (`Split text contract owners`).
- Commit-hash receipt: `slice-4-text-effects-geometry-contract-recut=3ce570f`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root Slice 4 receipt and seed Slice 5 from the accepted research plan.

29. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-05`
- Hello: hi
- Intended task: implement Slice 5, `Render Session Owner`, inside the allowed files only.
- How one task went: replaced the long-lived `session/text.zig` owner with `render_session.zig`, moved submitted-token ownership to `submitted_surface.zig`, rewired the allowed import users off the old `session/*` paths, preserved existing C export names, and migrated the owner-local session tests into the new owner files plus the existing unit root.
- Next handoff or blocker: reviewer/orchestrator can verify the Slice 5 diff under the accepted contract.

30. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 5, `Render Session Owner`, against allowed-file scope, owner replacement, session-file deletion, assertions/tests, and proof receipts.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must verify passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`, and `cd /home/home/personal/projects/howl/howl-render && zig build check`; verify final grep for `session/text.zig|session/submitted.zig` under `howl-render/src` returned no matches; verify final globs for `howl-render/src/session/text.zig` and `howl-render/src/session/submitted.zig` returned no files; confirm nested `howl-render` status stages only the Slice 5 allowed files; record the commit hash as `slice-5-render-session-owner=<hash>` before seeding Slice 6.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested render Slice 5 change, record the root receipt, and seed exactly Slice 6.

31. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: close Slice 5 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`, `cd /home/home/personal/projects/howl/howl-render && zig build check`, `rg "session/text.zig|session/submitted.zig" src`, and globs for the deleted session files; nested `howl-render` status matched the Slice 5 allowed files; accepted implementation committed as `howl-render` `81bc7ca` (`Name render session owner`).
- Commit-hash receipt: `slice-5-render-session-owner=81bc7ca`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root Slice 5 receipt and seed Slice 6 from the accepted research plan.

32. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-06`
- Hello: hi
- Intended task: implement Slice 6, `Prepared Surface And Surface Resource Split`, inside the allowed files only.
- How one task went: moved prepared/render-surface owners into `src/surface/`, split retained realizer resource state into `surface/realizer_resource_store.zig`, rewired the allowed roots/tests off `prepared/` and `geometry/render_surface_realizer.zig`, added the required nonzero/base-seq/pixel-length/state-transition assertions plus owner-local handle/compositor tests, and removed the live `src/prepared` directory.
- Next handoff or blocker: reviewer/orchestrator can verify the Slice 6 diff under the accepted contract.

33. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 6, `Prepared Surface And Surface Resource Split`, against allowed-file scope, owner moves, deletions, separate resource stores, assertions/tests, and proof receipts.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must verify passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build check`, `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, and `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`; verify `prepared/` grep under `howl-render/src` returned no matches; verify `howl-render/src/prepared/**` glob returned no files; verify `howl-render/src/geometry/render_surface_realizer.zig` glob returned no files; confirm nested `howl-render` status stages only the Slice 6 allowed files; record the commit hash as `slice-6-prepared-surface-resource-split=<hash>` before seeding Slice 7.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested render Slice 6 change, record the root receipt, and seed exactly Slice 7.

34. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: close Slice 6 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-render && zig build check`, `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`, `rg "prepared/" src`, and globs for the deleted `prepared/` directory and `geometry/render_surface_realizer.zig`; nested `howl-render` status matched the Slice 6 allowed files; accepted implementation committed as `howl-render` `09c6770` (`Split surface resource owners`).
- Commit-hash receipt: `slice-6-prepared-surface-resource-split=09c6770`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root Slice 6 receipt and seed Slice 7 from the accepted research plan.

35. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-07`
- Hello: hi
- Intended task: implement Slice 7, `FFI And ABI Owner Sweep`, inside the allowed files only.
- How one task went: kept `libhowl_render.zig` translator-only and left exported C names and the render header unchanged; tightened `submission.zig` submit-output status handling for missing-handle vs invalid-argument cases while preserving shipped submit return codes; and added ABI coverage for exported entrypoints, enum values, struct sizes, prepare failure zeroing, prepared-surface status mapping, and submit status translation.
- Next handoff or blocker: reviewer/orchestrator can verify the Slice 7 diff under the accepted contract.

36. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 7, `FFI And ABI Owner Sweep`, against allowed-file scope, translator-only FFI posture, ABI stability, assertions/tests, and proof receipts.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must verify passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build check`, `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, and `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`; confirm nested `howl-render` status stages only the Slice 7 allowed files; verify `src/libhowl_render.zig` remains unchanged; verify `include/howl_render.h` remains unchanged; record the commit hash as `slice-7-ffi-abi-sweep=<hash>` before seeding Slice 8.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested render Slice 7 change, record the root receipt, and seed exactly Slice 8.

37. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: close Slice 7 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-render && zig build check`, `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, and `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`; `src/libhowl_render.zig` and `include/howl_render.h` stayed unchanged; nested `howl-render` status matched the Slice 7 allowed files; accepted implementation committed as `howl-render` `c0675c8` (`Tighten render ABI proofs`).
- Commit-hash receipt: `slice-7-ffi-abi-sweep=c0675c8`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root Slice 7 receipt and seed Slice 8 from the accepted research plan.

38. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-08`
- Hello: hi
- Intended task: implement Slice 8, `Final Source Order, Deletion, And Proof Sweep`, inside the allowed files only.
- How one task went: removed the remaining `vt_publication/publication.zig` aggregation seam, rewired ABI-type consumers to import `vt_publication/abi.zig` directly, collapsed the duplicate publication cell-input mapping path in `renderable_content/content.zig`, deleted dead top-level handle helpers, and added explicit boundary assertions around prepare/prepared/submission/work-state C ABI copy-out helpers.
- Next handoff or blocker: reviewer/orchestrator can verify the Slice 8 diff under the accepted contract.

39. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 8, `Final Source Order, Deletion, And Proof Sweep`, against allowed-file scope, final cleanup, boundary assertions, and proof receipts.
- Findings: none.
- Outcome: accept.
- Acceptance notes: orchestrator must verify passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build check`, `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, and `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`; confirm nested `howl-render` status stages only the Slice 8 allowed files; record the commit hash as `slice-8-final-proof-sweep=<hash>` and record the final sprint proof hash before closing the nested render sprint receipts.
- Next handoff or blocker: orchestrator may verify, commit the accepted nested render Slice 8 change, and close the root sprint receipt.

40. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: close Slice 8 after reviewer acceptance and final verification.
- How one task went: final verification passed for `cd /home/home/personal/projects/howl/howl-render && zig build check`, `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, and `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`; nested `howl-render` status matched the Slice 8 allowed files; accepted implementation committed as `howl-render` `82a3e72` (`Finish render owner cleanup`).
- Commit-hash receipt: `slice-8-final-proof-sweep=82a3e72`.
- Final sprint proof hash: nested `howl-render` `82a3e72`.
- Blockers or deviations: unrelated untracked root `devtools_options.yaml` remains unstaged and untouched.
- Next handoff or blocker: commit the root final sprint receipt closure.

41. Role: orchestrator
- Session id: `orch-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: record the final root close hash after the sprint close commit landed.
- How one task went: root close receipt committed as `8aa8327` (`Close render owner sprint`); active sprint artifacts now record Slice 8 root `8aa8327` and the sprint is closed with no further coding authorized from this sprint.
- Next handoff or blocker: await the next user-directed sprint or archive this active surface when the next live step is promoted.

Condensed execution contract for coder session `coder-2026-06-13-render-api-owner-surgery-08`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md`
- Implement only Slice 8, `Final Source Order, Deletion, And Proof Sweep`.
- Allowed files:
  - `howl-render/build.zig`
  - `howl-render/src/test_unit.zig`
  - `howl-render/src/test_abi.zig`
  - `howl-render/src/test/unit/root.zig`
  - `howl-render/src/benchmark_main.zig`
  - `howl-render/src/libhowl_render.zig`
  - `howl-render/src/text_session.zig`
  - `howl-render/src/prepare_request.zig`
  - `howl-render/src/prepared_surface.zig`
  - `howl-render/src/submission.zig`
  - `howl-render/src/work_state.zig`
  - `howl-render/src/handle.zig`
  - `howl-render/src/render_session.zig`
  - `howl-render/src/submitted_surface.zig`
  - `howl-render/src/vt_publication/abi.zig`
  - `howl-render/src/vt_publication/publication.zig`
  - `howl-render/src/storage/publication_storage.zig`
  - `howl-render/src/renderable_content/content.zig`
  - `howl-render/src/renderable_content/color.zig`
  - `howl-render/src/renderable_content/cursor.zig`
  - `howl-render/src/damage/publication_damage.zig`
  - `howl-render/src/prepare/queue.zig`
  - `howl-render/src/surface/prepared_surface.zig`
  - `howl-render/src/surface/compositor.zig`
  - `howl-render/src/surface/handle.zig`
  - `howl-render/src/surface/emitter.zig`
  - `howl-render/src/surface/resource_store.zig`
  - `howl-render/src/surface/realizer.zig`
  - `howl-render/src/surface/realizer_resource_store.zig`
  - `howl-render/src/surface/realizer_test.zig`
  - `howl-render/src/text/color.zig`
  - `howl-render/src/text/effects.zig`
  - `howl-render/src/text/metrics.zig`
  - `howl-render/src/text/cell_input.zig`
  - `howl-render/src/text/scene_contract.zig`
  - `howl-render/src/text/contract.zig`
  - `howl-render/src/text/raster/atlas.zig`
  - `howl-render/src/text/direct_normal.zig`
  - `howl-render/src/text/surface_preparer.zig`
  - `howl-render/src/text/shape/cluster.zig`
  - `howl-render/src/text/ft_hb/support.zig`
  - `howl-render/src/text/ft_hb/glyph_raster.zig`
  - `howl-render/src/text/ft_hb/support_test.zig`
  - `howl-render/src/test_support.zig`
  - `howl-render/src/text_session_test.zig`
  - `howl-render/src/prepare_request_test.zig`
  - `howl-render/src/prepared_surface_test.zig`
  - `howl-render/src/submission_test.zig`
  - `howl-render/src/surface/handle_test.zig`
  - `howl-render/src/surface/emitter_test.zig`
- Required shape:
  - delete empty compatibility wrappers, stale imports, and false directories.
  - enforce source order inside owners.
  - collapse formatter-resistant signatures only if under the house limit.
  - ensure public roots curate exports only.
  - ensure namespace wrappers aggregate owners only.
- Required assertions:
  - audit each new owner for positive and negative-space assertions at boundaries, count/capacity assertions before slice or indexing, and pair assertions around C ABI copy-in/copy-out.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-render && zig build check`
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`
- Non-goals:
  - no new features.
  - no renderer backend architecture expansion.
  - no UX changes.
- Stop conditions:
  - stop if any new bucket or generic owner exists.
  - stop if tests are weakened or duplicate roots are added.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-render-api-owner-surgery-08`
  - commit-hash handoff status for orchestrator receipt closure

Condensed execution contract for coder session `coder-2026-06-13-render-api-owner-surgery-07`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md`
- Implement only Slice 7, `FFI And ABI Owner Sweep`.
- Allowed files:
  - `howl-render/src/libhowl_render.zig`
  - `howl-render/include/howl_render.h`
  - `howl-render/src/test_abi.zig`
  - `howl-render/src/text_session.zig`
  - `howl-render/src/prepare_request.zig`
  - `howl-render/src/prepared_surface.zig`
  - `howl-render/src/submission.zig`
  - `howl-render/src/work_state.zig`
  - `howl-render/src/handle.zig`
  - `howl-render/src/render_session.zig`
  - `howl-render/src/surface/handle.zig`
  - `howl-render/src/surface/prepared_surface.zig`
  - `howl-render/src/surface/emitter.zig`
  - `howl-render/src/text_session_test.zig`
  - `howl-render/src/prepare_request_test.zig`
  - `howl-render/src/prepared_surface_test.zig`
  - `howl-render/src/submission_test.zig`
  - `howl-render/src/test_support.zig`
  - `howl-linux-host/src/terminal/term.zig`
  - `howl-linux-host/src/terminal/vt/surface.zig`
- Required shape:
  - keep `libhowl_render.zig` as FFI/export translator only.
  - exported C names remain stable in this sprint.
  - if a C ABI rename is still necessary after owner surgery, stop for explicit user approval before changing header, translated usage, ABI tests, or host call sites.
- Required assertions:
  - every C handle argument checked before use.
  - every pointer/span validity checked before slicing.
  - status mapping covers all internal errors.
  - public enum values remain stable or are deliberately changed with receipt.
  - output structs are fully initialized before returning OK.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-render && zig build check`
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`
- Non-goals:
  - no new Zig-shaped host convenience API.
  - no umbrella runtime.
  - no behavior changes outside ABI translation.
- Stop conditions:
  - stop if an ABI-breaking change is proposed without explicit user approval.
  - stop if host needs a Zig import into render internals.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-render-api-owner-surgery-07`
  - commit-hash handoff status for orchestrator receipt closure

Condensed execution contract for coder session `coder-2026-06-13-render-api-owner-surgery-06`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md`
- Implement only Slice 6, `Prepared Surface And Surface Resource Split`.
- Allowed files:
  - `howl-render/src/prepared/surface.zig`
  - `howl-render/src/prepared/buffer.zig`
  - `howl-render/src/prepared/handle.zig`
  - `howl-render/src/prepared/render_surface_emitter.zig`
  - `howl-render/src/prepared/sprite_resource_store.zig`
  - `howl-render/src/prepared/handle_test.zig`
  - `howl-render/src/prepared/render_surface_emitter_test.zig`
  - `howl-render/src/prepared_surface.zig`
  - `howl-render/src/submission.zig`
  - `howl-render/src/handle.zig`
  - `howl-render/src/render_session.zig`
  - `howl-render/src/test_support.zig`
  - `howl-render/src/prepared_surface_test.zig`
  - `howl-render/src/submission_test.zig`
  - `howl-render/src/test/unit/root.zig`
  - `howl-render/src/geometry/render_surface_realizer.zig`
  - `howl-render/src/geometry/render_surface_realizer_test.zig`
  - `howl-render/src/surface/prepared_surface.zig`
  - `howl-render/src/surface/compositor.zig`
  - `howl-render/src/surface/handle.zig`
  - `howl-render/src/surface/emitter.zig`
  - `howl-render/src/surface/resource_store.zig`
  - `howl-render/src/surface/realizer.zig`
  - `howl-render/src/surface/realizer_resource_store.zig`
  - `howl-render/src/surface/handle_test.zig`
  - `howl-render/src/surface/emitter_test.zig`
  - `howl-render/src/surface/realizer_test.zig`
- Required shape:
  - move prepared identity to `surface/prepared_surface.zig`.
  - move CPU composition to `surface/compositor.zig`.
  - move C handle lifetime to `surface/handle.zig`.
  - move C surface emission to `surface/emitter.zig`.
  - move emit-time sprite/glyph resource allocation from `prepared/sprite_resource_store.zig` to `surface/resource_store.zig`.
  - move CPU realization from `geometry/render_surface_realizer.zig` to `surface/realizer.zig`.
  - move the nested realizer `ResourceStore` to `surface/realizer_resource_store.zig`.
  - delete `prepared/` after imports are cut over and leave geometry owning only geometry.
- Required assertions:
  - prepared token required base seq rules.
  - surface dimensions nonzero.
  - CPU pixel buffer length equals `width * height * 4` with overflow checks.
  - base pixels length matches output length for partial composition.
  - handle state transitions are valid.
  - `render_surface_payload` is null before emission and null after free.
  - resource and atlas counts stay within C ABI maxima.
  - rollback restore exactly matches saved state.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-render && zig build check`
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`
- Non-goals:
  - no VT or renderable-content changes.
  - no text shaping changes.
  - no C ABI header changes.
- Stop conditions:
  - stop if any import path still contains `prepared/` after this slice.
  - stop if `prepared` remains as a live directory after this slice.
  - stop if `geometry/render_surface_realizer.zig` remains live.
  - stop if emitter imports `render_session.zig` for anything except owner-approved cache or resource lookup.
  - stop if emit-time resource store and realizer resource store are merged.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-render-api-owner-surgery-06`
  - commit-hash handoff status for orchestrator receipt closure

Condensed execution contract for coder session `coder-2026-06-13-render-api-owner-surgery-05`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md`
- Implement only Slice 5, `Render Session Owner`.
- Allowed files:
  - `howl-render/src/session/text.zig`
  - `howl-render/src/session/submitted.zig`
  - `howl-render/src/session/text_test.zig`
  - `howl-render/src/render_session.zig`
  - `howl-render/src/submitted_surface.zig`
  - `howl-render/src/text_session.zig`
  - `howl-render/src/handle.zig`
  - `howl-render/src/work_state.zig`
  - `howl-render/src/submission.zig`
  - `howl-render/src/prepared_surface.zig`
  - `howl-render/src/prepare_request.zig`
  - `howl-render/src/libhowl_render.zig`
  - `howl-render/src/text/ft_hb/support.zig`
  - `howl-render/src/text/ft_hb/glyph_raster.zig`
  - `howl-render/src/text/ft_hb/support_test.zig`
  - `howl-render/src/prepared/buffer.zig`
  - `howl-render/src/prepared/handle.zig`
  - `howl-render/src/prepared/render_surface_emitter.zig`
  - `howl-render/src/prepared/handle_test.zig`
  - `howl-render/src/prepared/render_surface_emitter_test.zig`
  - `howl-render/src/test_support.zig`
  - `howl-render/src/text_session_test.zig`
  - `howl-render/src/submission_test.zig`
  - `howl-render/src/surface_geometry.zig`
  - `howl-render/src/test/unit/root.zig`
- Required shape:
  - replace generic `session/text.zig` with `render_session.zig` as the single long-lived C ABI render session owner.
  - move submitted-token tracking from `session/submitted.zig` to `submitted_surface.zig`.
  - keep text shaping/font/raster owners under existing `text/` files; use `text/ft_hb/cache.zig` for FT/HB face/shape/glyph-cell caches and `text/raster/atlas.zig` for raster atlas slot/cache ownership.
  - do not create `text/renderer.zig` or `text/glyph_cache.zig` in this sprint.
  - delete `session/text.zig`, `session/submitted.zig`, and `session/text_test.zig` after all imports are migrated.
- Required assertions:
  - mutex-protected state mutations enter through the long-lived owner.
  - font fallback count is bounded by existing constants.
  - scratch cell input length matches publication cell count.
  - derived grid and cell sizes are nonzero.
  - prepared surface creation cannot hold lock across handle registration unless explicitly asserted safe.
  - submitted token monotonicity is asserted.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`
  - `cd /home/home/personal/projects/howl/howl-render && zig build check`
- Non-goals:
  - no C render surface emission behavior changes.
  - no resource store ownership move.
  - no ABI header changes.
- Stop conditions:
  - stop if renaming exported C ABI symbols is required.
  - stop if `render_session.zig` still directly owns VT mapping, damage classification, CPU composition, C surface emission, and resource store logic after imports are corrected.
  - stop if any import path still contains `session/text.zig` after this slice.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-render-api-owner-surgery-05`
  - commit-hash handoff status for orchestrator receipt closure

Condensed execution contract for coder session `coder-2026-06-13-render-api-owner-surgery-04`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-render-api-owner-surgery-plan.md`
- Implement only Slice 4, `Text Effects Geometry Contract Recut`.
- Allowed files:
  - `howl-render/src/text/contract.zig`
  - `howl-render/src/text/color.zig`
  - `howl-render/src/text/effects.zig`
  - `howl-render/src/text/metrics.zig`
  - `howl-render/src/text/cell_input.zig`
  - `howl-render/src/text/scene_contract.zig`
  - `howl-render/src/text/raster/cache.zig`
  - `howl-render/src/text/raster/atlas.zig`
  - `howl-render/src/text/raster/key.zig`
  - `howl-render/src/text/raster/operation.zig`
  - `howl-render/src/text/raster/rasterizer.zig`
  - `howl-render/src/text/raster/special.zig`
  - `howl-render/src/text/raster/special_test.zig`
  - `howl-render/src/text/ft_hb/cache.zig`
  - `howl-render/src/text/ft_hb/support.zig`
  - `howl-render/src/text/ft_hb/support_test.zig`
  - `howl-render/src/text/ft_hb/glyph_raster.zig`
  - `howl-render/src/text/ft_hb/provider.zig`
  - `howl-render/src/text/session.zig`
  - `howl-render/src/text/provider.zig`
  - `howl-render/src/text/resolver.zig`
  - `howl-render/src/text/resolve.zig`
  - `howl-render/src/text/scene.zig`
  - `howl-render/src/text/direct_scene.zig`
  - `howl-render/src/text/direct_normal.zig`
  - `howl-render/src/text/surface_preparer.zig`
  - `howl-render/src/text/shape/cluster.zig`
  - `howl-render/src/text/shape/grouping.zig`
  - `howl-render/src/text/shape/run.zig`
  - `howl-render/src/text/classify/lane.zig`
  - `howl-render/src/text/classify/symbol_map.zig`
  - `howl-render/src/benchmark_main.zig`
  - `howl-render/src/test_support.zig`
  - `howl-render/src/session/text.zig`
  - `howl-render/src/session/text_test.zig`
  - `howl-render/src/prepared/buffer.zig`
  - `howl-render/src/prepared/handle_test.zig`
  - `howl-render/src/prepared/render_surface_emitter.zig`
  - `howl-render/src/prepared/render_surface_emitter_test.zig`
- Required shape:
  - split `text/contract.zig` into exact owners: `text/color.zig`, `text/effects.zig`, `text/metrics.zig`, `text/cell_input.zig`, and `text/scene_contract.zig`.
  - keep `text/contract.zig` as a curated re-export root only.
  - rename `text/raster/cache.zig` to `text/raster/atlas.zig` and update all imports.
  - `text/ft_hb/cache.zig` remains the FT/HB cache owner.
- Required assertions:
  - preserve existing text contract deterministic defaults in owner-local tests.
  - add owner-local tests proving color defaults, effect enum stability, metrics nonzero fixtures, `CellInput.combining_len <= combining.len`, text scene draw spans retain first-cell/cell-span facts, raster atlas reserve/store bounds, and FT/HB cache capacity errors.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`
  - `cd /home/home/personal/projects/howl/howl-render && zig build check`
- Non-goals:
  - no VT publication changes.
  - no render session move.
  - no prepared surface split.
  - no C ABI header changes.
  - no feature additions.
- Stop conditions:
  - stop if `text/contract.zig` still owns structs or mutation after this slice.
  - stop if `text/raster/cache.zig` remains as a live owner file.
  - stop if FT/HB caches are merged with raster atlas cache.
  - stop if geometry owner files receive text metrics or render effects.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-render-api-owner-surgery-04`
  - commit-hash handoff status for orchestrator receipt closure

23. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-03`
- Hello: hi
- Intended task: implement Slice 3 `Prepare Queue Cutover And tv_surface Deletion` inside the refreshed allowlist, run the exact proof commands, and leave the accountable handoff.
- How one task went: created `howl-render/src/prepare/queue.zig` as the active prepare owner for admission, take/consume/retry/retire, blink refresh, and retained-source refresh; moved every remaining live import off `tv_surface` in the refreshed allowlist; replaced the old temporary VT cursor compatibility import with `vt_publication/abi.zig` cursor truth; and deleted `howl-render/src/tv_surface/prepare_request.zig`, `tv_surface/vt.zig`, `tv_surface/cell.zig`, `tv_surface/slot.zig`, `tv_surface/publication_cell_map.zig`, `tv_surface/damage.zig`, and `tv_surface/text_input.zig`.
- Files changed: `howl-render/src/prepare/queue.zig`, `howl-render/src/session/text.zig`, `howl-render/src/storage/publication_storage.zig`, `howl-render/src/renderable_content/content.zig`, `howl-render/src/renderable_content/color.zig`, `howl-render/src/renderable_content/cursor.zig`, `howl-render/src/vt_publication/abi.zig`, `howl-render/src/vt_publication/publication.zig`, `howl-render/src/benchmark_main.zig`, and this active loop file; deleted `howl-render/src/tv_surface/prepare_request.zig`, `howl-render/src/tv_surface/vt.zig`, `howl-render/src/tv_surface/cell.zig`, `howl-render/src/tv_surface/slot.zig`, `howl-render/src/tv_surface/publication_cell_map.zig`, `howl-render/src/tv_surface/damage.zig`, `howl-render/src/tv_surface/text_input.zig`.
- Concise implementation summary: Slice 3 now has a real `prepare/queue.zig` owner with the required assertions and queue-lifecycle tests, all remaining render imports point at `prepare`, `vt_publication`, `storage`, `damage`, and `renderable_content` owners, retained-source clone/deinit ownership is explicit in the queue cutover, geometry-epoch changes force full damage, blink refresh cannot fabricate a source, and the old `tv_surface` seam is fully deleted from `howl-render/src`.
- Exact verification results: `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` passed; `cd /home/home/personal/projects/howl/howl-render && zig build check` passed; final grep for `tv_surface/` under `/home/home/personal/projects/howl/howl-render/src` returned no matches; final glob under `/home/home/personal/projects/howl/howl-render/src/tv_surface` returned no files.
- Blockers or deviations: no blockers; one initial queue compile failure (`var` not mutated) and one stale-base queue test fixture mistake were fixed during proof; no out-of-scope files were touched and no git commands were used.
- Commit-hash handoff status: pending reviewer acceptance and orchestrator receipt closure; no git was touched.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should review the current Slice 3 diff against the refreshed contract and proof receipts.

13. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: fix the orchestrator-seeded Slice 1 compile failures in the existing working-tree diff and rerun the exact refreshed proof commands.
- How one task went: fixed the three seeded failures plus the two owner-local test fixture issues they exposed in `vt_publication/publication.zig` and `storage/publication_storage.zig`; `zig build test:unit` now passes and `zig build check` now passes in `/home/home/personal/projects/howl/howl-render`.
- Next handoff or blocker: ready for reviewer on the current Slice 1 diff; commit-hash handoff status remains pending reviewer acceptance and orchestrator receipt closure, and no git was touched.

14. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 1 `VT Publication Boundary And Storage` against the active condensed contract and accepted planning artifact, without implementing product code.
- Findings: no blocking findings. Changed render files stay within the Slice 1 allowed set: `src/benchmark_main.zig`, `src/prepare_request.zig`, `src/session/text.zig`, `src/session/text_test.zig`, `src/test_support.zig`, `src/tv_surface/cell.zig`, `src/tv_surface/damage.zig`, `src/tv_surface/slot.zig`, `src/tv_surface/vt.zig`, plus new `src/vt_publication/abi.zig`, `src/vt_publication/publication.zig`, and `src/storage/publication_storage.zig`; no `vt_publication/storage.zig` exists. `src/vt_publication/abi.zig` owns shipped VT C aliases and publication validation; `src/vt_publication/publication.zig` owns `PublicationSource`, `VtSnapshot`, copy-in, clone/deinit, and owner-local validation tests; `src/storage/publication_storage.zig` owns retained publication slot/storage truth. `src/tv_surface/vt.zig:1-24` and `src/tv_surface/slot.zig:1-7` are thin temporary import seams only, and `src/tv_surface/cell.zig:1-93` remains an explicitly temporary compatibility input, not VT ABI or storage truth. No render file imports VT Zig internals. Slice 1 assertions and tests are present and meaningful, including invalid color kind, invalid combining length, dirty span length mismatch, retained-storage deinit safety, and copy-in metadata preservation in `src/vt_publication/publication.zig:294-384`, plus retained-slot ownership/reuse tests in `src/storage/publication_storage.zig:260-438`. The slice does not change text-scene mapping, prepare queue design beyond imports, the C ABI header, or host code.
- Outcome: accept.
- Next handoff or blocker: orchestrator should verify the passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`, confirm the slice still stages only the allowed render files and not unrelated workspace files such as root `devtools_options.yaml`, then record the commit hash as `slice-1-vt-publication-boundary=<hash>` before seeding Slice 2.

16. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-02`
- Hello: hi
- Intended task: implement Slice 2 `Renderable Content And Damage Owners` only inside the allowed files, move the owner logic out of `tv_surface`, run the exact unit/check proofs, and leave the required receipt note.
- How one task went: moved VT publication cell-input mapping into `src/renderable_content/content.zig`, moved palette/theme and semantic color mapping plus inverse/selection/empty truth into `src/renderable_content/color.zig`, moved cursor visibility/shape mapping into `src/renderable_content/cursor.zig`, moved dirty metadata validation/canonicalization and snapshot damage classification into `src/damage/publication_damage.zig`, updated the allowed import users to point at the new owners, and reduced `src/tv_surface/text_input.zig`, `src/tv_surface/publication_cell_map.zig`, and `src/tv_surface/damage.zig` to temporary compatibility seams only.
- Files changed: `howl-render/src/tv_surface/text_input.zig`, `howl-render/src/tv_surface/publication_cell_map.zig`, `howl-render/src/tv_surface/damage.zig`, `howl-render/src/benchmark_main.zig`, `howl-render/src/session/text.zig`, `howl-render/src/text/direct_normal.zig`, `howl-render/src/text/surface_preparer.zig`, `howl-render/src/text/shape/cluster.zig`, `howl-render/src/renderable_content/content.zig`, `howl-render/src/renderable_content/color.zig`, `howl-render/src/renderable_content/cursor.zig`, `howl-render/src/damage/publication_damage.zig`.
- Concise implementation summary: the slice now has real renderable-content owners for publication-to-text mapping, theme/color truth, and cursor mapping, plus a dedicated damage owner for dirty metadata and full/partial classification; the old `tv_surface` files no longer own live mapping or damage behavior.
- Exact verification results: `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` passed; `cd /home/home/personal/projects/howl/howl-render && zig build check` passed.
- Blockers or deviations: no blockers; one owner-local test initially expected selection colors from the static fallback theme instead of the active VT theme, and one semantic-empty gap for invisible cells was fixed during proof.
- Commit-hash handoff status: pending reviewer acceptance and orchestrator receipt closure; no git was touched.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should review the current Slice 2 diff against the accepted contract and exact proof receipts.

17. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 2 `Renderable Content And Damage Owners` against the active condensed contract and accepted planning artifact, without implementing product code.
- Findings: no blocking findings. Changed render files stay within the Slice 2 allowlist: `src/tv_surface/text_input.zig`, `src/tv_surface/publication_cell_map.zig`, `src/tv_surface/damage.zig`, `src/benchmark_main.zig`, `src/session/text.zig`, `src/text/direct_normal.zig`, `src/text/surface_preparer.zig`, `src/text/shape/cluster.zig`, plus new `src/renderable_content/content.zig`, `src/renderable_content/color.zig`, `src/renderable_content/cursor.zig`, and `src/damage/publication_damage.zig`. `src/renderable_content/content.zig` owns VT/publication-to-text input mapping and borrowed/owned scene-input construction; `src/renderable_content/color.zig` owns theme, palette, semantic color truth, inverse/selection consequences, and empty classification; `src/renderable_content/cursor.zig` owns cursor visibility/shape mapping; `src/damage/publication_damage.zig` owns dirty metadata validation/canonicalization and full/partial damage classification. The old `tv_surface/text_input.zig:1-20`, `tv_surface/publication_cell_map.zig:1-15`, and `tv_surface/damage.zig:1-10` are temporary compatibility seams only. `renderable_content` does not own VT storage or prepare queue state, and `damage/publication_damage.zig` imports no text raster or font owners. Required assertions and tests are present and meaningful, including default background opacity, inverse style, selected style, continuation/invisible-not-empty truth, cursor blink hidden/visible mapping, partial dirty-row canonicalization, and full-vs-partial damage classification across `src/renderable_content/content.zig:260-482`, `src/renderable_content/color.zig:213-270`, `src/renderable_content/cursor.zig:51-95`, and `src/damage/publication_damage.zig:134-175`. The consumer changes in `benchmark_main.zig`, `session/text.zig`, `text/direct_normal.zig`, `text/surface_preparer.zig`, and `text/shape/cluster.zig` are import rewires only; no text shaping/raster redesign, resource-store changes, or C ABI header changes are in this slice.
- Outcome: accept.
- Next handoff or blocker: orchestrator should verify the passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`, confirm the nested `howl-render` status still stages only the Slice 2 allowed files, then record the commit hash as `slice-2-renderable-content-damage=<hash>` before seeding Slice 3.

24. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 3 `Prepare Queue Cutover And tv_surface Deletion` against the refreshed condensed contract and accepted planning artifact, without implementing product code.
- Findings: no blocking findings. The nested render worktree stays within the refreshed Slice 3 allowlist: modified `src/prepare/queue.zig`, `src/session/text.zig`, `src/storage/publication_storage.zig`, `src/renderable_content/content.zig`, `src/renderable_content/color.zig`, `src/renderable_content/cursor.zig`, `src/vt_publication/abi.zig`, `src/vt_publication/publication.zig`, `src/benchmark_main.zig`, and deleted `src/tv_surface/prepare_request.zig`, `src/tv_surface/vt.zig`, `src/tv_surface/cell.zig`, `src/tv_surface/slot.zig`, `src/tv_surface/publication_cell_map.zig`, `src/tv_surface/damage.zig`, and `src/tv_surface/text_input.zig`. `src/prepare/queue.zig` is the exact prepare owner for admission, take/consume/retry/retire, blink refresh, and retained-source refresh, and its imports are limited to `vt_publication/publication.zig`, `storage/publication_storage.zig`, `damage/publication_damage.zig`, `geometry/tokens.zig`, and `geometry/geometry_contract.zig` only. Every remaining live `tv_surface` import user in the refreshed allowlist was cut over, the final `tv_surface/` grep under `howl-render/src` returned no matches, and the final glob under `howl-render/src/tv_surface` returned no files, so all seven `tv_surface` files are deleted. Required Slice 3 assertions and tests are present and meaningful in `src/prepare/queue.zig`, including no-op duplicate admission, geometry change forcing full damage, stale-base partial forcing full damage, blink refresh after taken prepare, retry token mismatch, and retire-at-or-before ordering. The slice does not rename the text session, does not touch prepared/surface owners, and does not touch host code.
- Outcome: accept.
- Next handoff or blocker: orchestrator should verify the passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`, verify the final `tv_surface/` grep under `howl-render/src` returned no matches and the `howl-render/src/tv_surface` glob returned no files, confirm nested `howl-render` status stages only the Slice 3 allowed files, then record the commit hash as `slice-3-prepare-storage-cutover=<hash>` before seeding Slice 4.

25. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-04`
- Hello: hi
- Intended task: implement Slice 4 `Text Effects Geometry Contract Recut` inside the allowed files only.
- How one task went: split `src/text/contract.zig` into owner files `src/text/color.zig`, `src/text/effects.zig`, `src/text/metrics.zig`, `src/text/cell_input.zig`, and `src/text/scene_contract.zig`; kept `src/text/contract.zig` as a curated re-export root only; moved the raster atlas owner to `src/text/raster/atlas.zig`, deleted `src/text/raster/cache.zig`, updated the live atlas imports, and added owner-local tests for color defaults, effect enum stability, metrics fixtures, cell-input combining bounds, scene draw spans, atlas bounds, and FT/HB cache capacity errors.
- Exact verification results: `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` passed; `cd /home/home/personal/projects/howl/howl-render && zig build check` passed.
- Blockers or deviations: no blockers; no out-of-scope files were touched and no git commands were used.
- Commit-hash handoff status: pending reviewer acceptance and orchestrator receipt closure; no git was touched.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` can review the current Slice 4 diff against the accepted contract and proof receipts.

26. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 4 `Text Effects Geometry Contract Recut` against the active condensed contract and accepted planning artifact, without implementing product code.
- Findings: no blocking findings. The nested render worktree stays within the Slice 4 allowlist: modified `src/session/text.zig`, `src/text/contract.zig`, `src/text/direct_normal.zig`, `src/text/ft_hb/cache.zig`, `src/text/raster/atlas.zig`, `src/text/scene.zig`, `src/text/surface_preparer.zig`, added `src/text/color.zig`, `src/text/effects.zig`, `src/text/metrics.zig`, `src/text/cell_input.zig`, `src/text/scene_contract.zig`, and deleted `src/text/raster/cache.zig`. The new owner files match the required shape: `src/text/color.zig` owns `Rgba8`, `SemanticColorKind`, and `SemanticColor`; `src/text/effects.zig` owns underline/style/presentation/effect enums plus backend caps; `src/text/metrics.zig` owns font/face/decoration/cursor/cell/grid metrics; `src/text/cell_input.zig` owns `CellInput` and its combining-bounds assertion; `src/text/scene_contract.zig` owns text scene, draw, sprite, cluster, and missing-glyph contract facts. `src/text/contract.zig` is reduced to a curated re-export root plus a root re-export test only, with no owner state or mutation. `src/text/raster/cache.zig` is deleted and `src/text/raster/atlas.zig` is the live atlas owner, while `src/text/ft_hb/cache.zig` remains the FT/HB cache owner with capacity-error tests intact. Required assertions and tests are present and meaningful, including color defaults, effect enum stability, nonzero metrics fixtures, `CellInput.combining_len <= combining.len`, scene draw span retention, atlas reserve/store bounds, and FT/HB cache capacity errors. No VT publication files changed, no render session move occurred, no prepared surface split occurred, no C ABI header changed, no features were added, and no geometry owner file received text metrics or render effects.
- Outcome: accept.
- Next handoff or blocker: orchestrator should verify the passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` and `cd /home/home/personal/projects/howl/howl-render && zig build check`, confirm nested `howl-render` status stages only the Slice 4 allowed files, verify `howl-render/src/text/raster/cache.zig` is absent and `howl-render/src/text/raster/atlas.zig` is the live atlas file, then record the commit hash as `slice-4-text-effects-geometry-contract-recut=<hash>` before seeding Slice 5.

27. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-05`
- Hello: hi
- Intended task: implement Slice 5 `Render Session Owner` by replacing `session/text.zig` with `render_session.zig`, moving submitted-token tracking to `submitted_surface.zig`, migrating imports/tests, and running the exact unit/abi/check proofs.
- How one task went: moved the long-lived owner into `src/render_session.zig`, moved submitted-token state/tests into `src/submitted_surface.zig`, deleted `src/session/text.zig`, `src/session/submitted.zig`, and `src/session/text_test.zig`, rewired every allowed import user to the new owner paths, added owner-local assertions for nonzero derived layout, bounded fallback count, exact scratch slice length, unlocked handle registration, and monotonic submitted tokens, and moved the old session-owner tests into the new owner files plus the existing unit root.
- Files changed: `howl-render/src/render_session.zig`, `howl-render/src/submitted_surface.zig`, `howl-render/src/text_session.zig`, `howl-render/src/handle.zig`, `howl-render/src/work_state.zig`, `howl-render/src/submission.zig`, `howl-render/src/text/ft_hb/support.zig`, `howl-render/src/text/ft_hb/glyph_raster.zig`, `howl-render/src/text/ft_hb/support_test.zig`, `howl-render/src/prepared/buffer.zig`, `howl-render/src/prepared/handle.zig`, `howl-render/src/prepared/render_surface_emitter.zig`, `howl-render/src/prepared/handle_test.zig`, `howl-render/src/prepared/render_surface_emitter_test.zig`, `howl-render/src/test_support.zig`, `howl-render/src/text_session_test.zig`, `howl-render/src/test/unit/root.zig`, and this active loop file; deleted `howl-render/src/session/text.zig`, `howl-render/src/session/submitted.zig`, `howl-render/src/session/text_test.zig`.
- Concise implementation summary: Slice 5 now has `render_session.zig` as the single long-lived render session owner, `submitted_surface.zig` as the submitted-token owner, stable C export names through the existing FFI entry files, migrated unit coverage for layout/font/scratch/submitted behavior, and no remaining `session/text.zig` or `session/submitted.zig` imports under `howl-render/src`.
- Exact verification results: `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` passed; `cd /home/home/personal/projects/howl/howl-render && zig build test:abi` passed; `cd /home/home/personal/projects/howl/howl-render && zig build check` passed; final grep for `session/text.zig|session/submitted.zig` under `/home/home/personal/projects/howl/howl-render/src` returned no matches; final globs for `/home/home/personal/projects/howl/howl-render/src/session/text.zig` and `/home/home/personal/projects/howl/howl-render/src/session/submitted.zig` returned no files.
- Blockers or deviations: no blockers; one initial root-relative import break after moving `session/text.zig` to `src/render_session.zig` and one incorrect new font-path test expectation were fixed during proof; no out-of-scope files were touched and no git commands were used.
- Commit-hash handoff status: pending reviewer acceptance and orchestrator receipt closure; no git was touched.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should review the current Slice 5 diff against the accepted contract and proof receipts.

28. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 5 `Render Session Owner` against the active condensed contract and accepted planning artifact, without implementing product code.
- Findings: no blocking findings. The nested render worktree stays within the Slice 5 allowlist: modified `src/text_session.zig`, `src/handle.zig`, `src/work_state.zig`, `src/submission.zig`, `src/text/ft_hb/support.zig`, `src/text/ft_hb/glyph_raster.zig`, `src/text/ft_hb/support_test.zig`, `src/prepared/buffer.zig`, `src/prepared/handle.zig`, `src/prepared/render_surface_emitter.zig`, `src/prepared/handle_test.zig`, `src/prepared/render_surface_emitter_test.zig`, `src/test_support.zig`, `src/text_session_test.zig`, `src/test/unit/root.zig`, added `src/render_session.zig` and `src/submitted_surface.zig`, and deleted `src/session/text.zig`, `src/session/submitted.zig`, and `src/session/text_test.zig`. `src/render_session.zig` is the single long-lived render session owner and `src/submitted_surface.zig` is the submitted-token owner, with required assertions and tests for nonzero derived layout, bounded fallback count, exact scratch length reuse, unlocked handle registration, stale/needs-prepare behavior, and submitted-token monotonicity. Existing text/raster owners are preserved: imports point at `text/ft_hb/cache.zig` and `text/raster/atlas.zig`, and no `text/renderer.zig` or `text/glyph_cache.zig` exists. The final grep for `session/text.zig|session/submitted.zig` under `howl-render/src` returned no matches and the final globs for `src/session/text.zig` and `src/session/submitted.zig` returned no files. No C render-surface emission behavior change, no resource-store ownership move, and no ABI header change are in this slice; exported C ABI symbol rename was not required because the FFI export entry files stayed stable.
- Outcome: accept.
- Next handoff or blocker: orchestrator should verify the passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`, and `cd /home/home/personal/projects/howl/howl-render && zig build check`, verify the final grep for `session/text.zig|session/submitted.zig` under `howl-render/src` returned no matches and the final globs for `howl-render/src/session/text.zig` and `howl-render/src/session/submitted.zig` returned no files, confirm nested `howl-render` status stages only the Slice 5 allowed files, then record the commit hash as `slice-5-render-session-owner=<hash>` before seeding Slice 6.

29. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-06`
- Hello: hi
- Intended task: implement Slice 6 `Prepared Surface And Surface Resource Split` inside the allowed files only, cut every live import to `surface/*`, split realizer resource state into its own owner, and run the exact proof plus stop-condition checks.
- How one task went: moved prepared identity to `src/surface/prepared_surface.zig`, CPU composition to `src/surface/compositor.zig`, handle lifetime to `src/surface/handle.zig`, C emission to `src/surface/emitter.zig`, emit-time resource allocation to `src/surface/resource_store.zig`, CPU realization to `src/surface/realizer.zig`, and realized retained-resource state to new `src/surface/realizer_resource_store.zig`; rewired `prepared_surface.zig`, `submission.zig`, `handle.zig`, `render_session.zig`, `test_support.zig`, and the unit root to the new owners; added nonzero/base-seq/pixel-length/state-transition assertions and owner-local tests for handle idempotence, session ownership, and compositor retained-base length truth; removed the live `src/prepared` directory after cutover.
- Files changed: `howl-render/src/prepared_surface.zig`, `howl-render/src/submission.zig`, `howl-render/src/handle.zig`, `howl-render/src/render_session.zig`, `howl-render/src/test_support.zig`, `howl-render/src/prepared_surface_test.zig`, `howl-render/src/submission_test.zig`, `howl-render/src/test/unit/root.zig`, added `howl-render/src/surface/prepared_surface.zig`, `howl-render/src/surface/compositor.zig`, `howl-render/src/surface/handle.zig`, `howl-render/src/surface/emitter.zig`, `howl-render/src/surface/resource_store.zig`, `howl-render/src/surface/realizer.zig`, `howl-render/src/surface/realizer_resource_store.zig`, `howl-render/src/surface/handle_test.zig`, `howl-render/src/surface/emitter_test.zig`, `howl-render/src/surface/realizer_test.zig`; deleted the former `howl-render/src/prepared/*.zig` files and `howl-render/src/geometry/render_surface_realizer.zig`, `howl-render/src/geometry/render_surface_realizer_test.zig` by moving them to `surface/*` owners.
- Concise implementation summary: Slice 6 now makes `surface/` the sole owner namespace for prepared-surface identity, composition, emission, handle lifetime, emit-time resource allocation, and CPU realization; the retained realizer resource store is separated from emit-time sprite allocation; `geometry/` no longer owns render-surface realization; and no render import path under `src` still points at `prepared/`.
- Exact verification results: `cd /home/home/personal/projects/howl/howl-render && zig build check` passed; `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` passed; `cd /home/home/personal/projects/howl/howl-render && zig build test:abi` passed; grep for `prepared/` under `/home/home/personal/projects/howl/howl-render/src` returned no matches; glob for `/home/home/personal/projects/howl/howl-render/src/prepared/**` returned no files; glob for `/home/home/personal/projects/howl/howl-render/src/geometry/render_surface_realizer.zig` returned no files.
- Blockers or deviations: no blockers; `src/surface/emitter.zig` still imports `../render_session.zig` only for `TextSession` atlas lookup via `session.atlasRaster`, which matches the slice stop-condition allowance for owner-approved cache/resource lookup; no out-of-scope files were touched and no git commands were used.
- Commit-hash handoff status: pending reviewer acceptance and orchestrator receipt closure; no git was touched.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should review the current Slice 6 diff against the accepted contract, exact proof receipts, and the prepared/realizer deletion stop conditions.

30. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 6 `Prepared Surface And Surface Resource Split` against the active condensed contract and accepted planning artifact, without implementing product code.
- Findings: no blocking findings. The nested render worktree stays within the Slice 6 allowlist: modified `src/prepared_surface.zig`, `src/submission.zig`, `src/handle.zig`, `src/render_session.zig`, `src/test_support.zig`, `src/test/unit/root.zig`, deleted `src/prepared/surface.zig`, `src/prepared/buffer.zig`, `src/prepared/handle.zig`, `src/prepared/render_surface_emitter.zig`, `src/prepared/sprite_resource_store.zig`, `src/prepared/handle_test.zig`, `src/prepared/render_surface_emitter_test.zig`, `src/geometry/render_surface_realizer.zig`, `src/geometry/render_surface_realizer_test.zig`, and added `src/surface/prepared_surface.zig`, `src/surface/compositor.zig`, `src/surface/handle.zig`, `src/surface/emitter.zig`, `src/surface/resource_store.zig`, `src/surface/realizer.zig`, `src/surface/realizer_resource_store.zig`, `src/surface/handle_test.zig`, `src/surface/emitter_test.zig`, and `src/surface/realizer_test.zig`. The new owner shape matches the slice exactly: `surface/prepared_surface.zig` owns prepared identity/info/buffer truth, `surface/compositor.zig` owns CPU composition and base-length assertions, `surface/handle.zig` owns prepared-handle lifetime/state transitions, `surface/emitter.zig` owns C surface emission, `surface/resource_store.zig` owns emit-time sprite/glyph resource allocation and atlas rollback, `surface/realizer.zig` owns CPU realization, and `surface/realizer_resource_store.zig` owns retained realized-resource transition state. `prepared/` is fully deleted, `geometry/` no longer owns realizer code, and the emit-time resource store stays separate from the realizer resource store. Required assertions and tests are present and meaningful, including prepared-token/base-seq truth, compositor pixel/base length checks, handle release/consume/session ownership, emitter failure mapping and bounds, resource-store rollback behavior, and realizer create/upload/retire validation. No VT/renderable-content files changed, no text shaping owners changed, and no C ABI header changed. The `surface/emitter.zig` import of `render_session.zig` is limited to owner-approved atlas cache lookup via `session.atlasRaster`; no import path under `howl-render/src` still contains `prepared/`, no live `prepared` directory remains, and `geometry/render_surface_realizer.zig` is absent.
- Outcome: accept.
- Next handoff or blocker: orchestrator should verify the passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build check`, `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, and `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`, verify the `prepared/` grep under `howl-render/src` returned no matches, the `howl-render/src/prepared/**` glob returned no files, and the `howl-render/src/geometry/render_surface_realizer.zig` glob returned no files, confirm nested `howl-render` status stages only the Slice 6 allowed files, then record the commit hash as `slice-6-prepared-surface-resource-split=<hash>` before seeding Slice 7.

31. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-07`
- Hello: hi
- Intended task: implement Slice 7 `FFI And ABI Owner Sweep` inside the allowed ABI boundary files only, keep exported C names stable, tighten FFI status translation, and run the exact render proofs.
- How one task went: tightened `src/submission.zig` so submit failure outputs now preserve precise ABI status for missing-handle versus invalid-argument failures while keeping the exported submit status codes unchanged; added explicit ABI coverage in `src/test_abi.zig` for exported entrypoints, enum values, and stable struct sizes; and added missing-handle/invalid-argument/render-surface-status boundary tests in the allowed FFI test files.
- Files changed: `howl-render/src/submission.zig`, `howl-render/src/text_session_test.zig`, `howl-render/src/prepare_request_test.zig`, `howl-render/src/prepared_surface_test.zig`, `howl-render/src/submission_test.zig`, `howl-render/src/test_abi.zig`.
- Concise implementation summary: Slice 7 keeps `libhowl_render.zig` as translator-only and leaves the C header and exported names unchanged; the FFI submit seam now initializes failure outputs with argument-accurate `HowlRenderCallStatus` values; ABI tests now prove header translation, export presence, enum stability, struct layout expectations, prepare failure zeroing, prepared-surface status mapping, and submit status translation.
- Exact verification results: `cd /home/home/personal/projects/howl/howl-render && zig build check` passed; `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` passed; `cd /home/home/personal/projects/howl/howl-render && zig build test:abi` passed.
- Blockers or deviations: no blockers; no ABI-breaking rename was proposed, so `howl-render/include/howl_render.h` and host call sites were left unchanged; the allowed host files did not need edits, so no host-side test command was required by the slice contract; no git commands were used.
- Commit-hash handoff status: pending reviewer acceptance and orchestrator receipt closure; no git was touched.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should review the current Slice 7 diff against the accepted contract, with focus on FFI status translation coverage, exported-name stability, and the passed `check`, `test:unit`, and `test:abi` receipts.

32. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 7 `FFI And ABI Owner Sweep` against the active condensed contract and accepted planning artifact, without implementing product code.
- Findings: no blocking findings. The nested render worktree stays within the Slice 7 allowlist: modified `src/submission.zig`, `src/text_session_test.zig`, `src/prepare_request_test.zig`, `src/prepared_surface_test.zig`, `src/submission_test.zig`, and `src/test_abi.zig`. `src/libhowl_render.zig` remains FFI/export translator only and is unchanged; `include/howl_render.h` is unchanged; exported C names remain stable with the same `@export` names and translated header declarations. The Slice 7 code changes stay inside ABI translation behavior: `src/submission.zig` now preserves precise `HowlRenderCallStatus` values in failure outputs for missing-handle versus invalid-argument cases while leaving the outer submit status returns unchanged. Required ABI/FFI assertions and tests are present and meaningful in the changed test files, covering exported entrypoint presence, enum stability, stable struct sizes, missing-handle and invalid-argument paths, prepared render-surface status mapping, prepare failure zeroing, and submit status translation. No new Zig-shaped host convenience API was added, no umbrella runtime was introduced, no ABI-breaking rename was proposed, and no host file needed a Zig import into render internals.
- Outcome: accept.
- Next handoff or blocker: orchestrator should verify the passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build check`, `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, and `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`, confirm nested `howl-render` status stages only the Slice 7 allowed files, verify `src/libhowl_render.zig` and `include/howl_render.h` remain unchanged, and then record the commit hash as `slice-7-ffi-abi-sweep=<hash>` before seeding Slice 8.

33. Role: coder
- Session id: `coder-2026-06-13-render-api-owner-surgery-08`
- Hello: hi
- Intended task: implement Slice 8 `Final Source Order, Deletion, And Proof Sweep` inside the allowed files only, remove remaining compatibility aggregation seams, tighten owner-boundary assertions, and rerun the exact render proofs.
- How one task went: removed the remaining `vt_publication/publication.zig` ABI re-export aggregation seam, rewired publication-type consumers to import `vt_publication/abi.zig` directly where they only need shipped VT structs, collapsed the duplicate publication cell-input mapper in `renderable_content/content.zig` to one owner-local implementation, deleted dead top-level handle conversion helpers, and added explicit debug assertions around ABI copy-out helpers for prepare, prepared-surface info, submission tokens/results, and session work-state output.
- Files changed: `howl-render/src/vt_publication/publication.zig`, `howl-render/src/benchmark_main.zig`, `howl-render/src/renderable_content/content.zig`, `howl-render/src/renderable_content/color.zig`, `howl-render/src/renderable_content/cursor.zig`, `howl-render/src/text/direct_normal.zig`, `howl-render/src/text/surface_preparer.zig`, `howl-render/src/text/shape/cluster.zig`, `howl-render/src/handle.zig`, `howl-render/src/prepare_request.zig`, `howl-render/src/prepared_surface.zig`, `howl-render/src/submission.zig`, `howl-render/src/work_state.zig`, and this active loop file.
- Concise implementation summary: Slice 8 removes the last false publication namespace aggregation, keeps ABI structs owned by `vt_publication/abi.zig`, keeps `vt_publication/publication.zig` focused on publication ownership only, deduplicates one stale renderable-content compatibility path, and tightens owner-boundary proof with pair assertions on the remaining C ABI copy-out helpers.
- Exact verification results: `cd /home/home/personal/projects/howl/howl-render && zig build check` passed; `cd /home/home/personal/projects/howl/howl-render && zig build test:unit` passed; `cd /home/home/personal/projects/howl/howl-render && zig build test:abi` passed.
- Blockers or deviations: no blockers; the first proof pass failed only because `src/prepared_surface.zig`, `src/submission.zig`, and `src/work_state.zig` needed `std` imports for the new assertions, and that was fixed in-slice before rerunning proof; no out-of-scope files were touched and no git commands were used.
- Commit-hash handoff status: pending reviewer acceptance and orchestrator receipt closure; no git was touched.
- Next handoff or blocker: reviewer `review-2026-06-13-render-api-owner-surgery-01` should review the current Slice 8 diff against the accepted contract and the passed `check`, `test:unit`, and `test:abi` receipts.

34. Role: reviewer
- Session id: `review-2026-06-13-render-api-owner-surgery-01`
- Hello: hi
- Intended task: review execution Slice 8 `Final Source Order, Deletion, And Proof Sweep` against the active condensed contract and accepted planning artifact, without implementing product code.
- Findings: no blocking findings. The nested render worktree stays within the Slice 8 allowlist: modified `build.zig`, `src/benchmark_main.zig`, `src/handle.zig`, `src/prepare_request.zig`, `src/prepared_surface.zig`, `src/renderable_content/color.zig`, `src/renderable_content/content.zig`, `src/renderable_content/cursor.zig`, `src/submission.zig`, `src/text/direct_normal.zig`, `src/text/shape/cluster.zig`, `src/text/surface_preparer.zig`, `src/vt_publication/publication.zig`, and `src/work_state.zig`. The slice removes remaining false aggregation and stale compatibility seams where allowed: `vt_publication/publication.zig` now stays publication-owner true, shipped VT ABI structs remain owned by `vt_publication/abi.zig`, duplicate publication cell-input mapping was collapsed back to one owner-local implementation, and dead top-level handle conversion helpers were deleted. Public roots still curate exports only and namespace wrappers still aggregate owners only. The added boundary assertions in `prepare_request.zig`, `prepared_surface.zig`, `submission.zig`, and `work_state.zig` are meaningful pair assertions around C ABI copy-in/copy-out helpers. No new bucket or generic owner was introduced, tests were not weakened, and no duplicate test roots were added. No new feature, renderer backend architecture expansion, or UX change appears in this slice.
- Outcome: accept.
- Next handoff or blocker: orchestrator should verify the passed receipts for `cd /home/home/personal/projects/howl/howl-render && zig build check`, `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`, and `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`, confirm nested `howl-render` status stages only the Slice 8 allowed files, then record the commit hash as `slice-8-final-proof-sweep=<hash>` and the final sprint proof hash before closing the nested render sprint receipts.
