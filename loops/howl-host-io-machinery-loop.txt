Howl Host I/O Machinery Loop

Orchestrator session id: `orch-2026-06-20-host-io-machinery-01`.
Researcher session id: `research-2026-06-20-host-io-machinery-01`.
Reviewer session id: `review-2026-06-20-host-io-machinery-01`.

Scoping statement:

- Howl's Linux host is a terminal PTY windowing shell. The repo currently hides that truth behind bucket files, vague `input` machinery, and duplicated event/consequence paths.
- The goal is to eliminate bad I/O machinery shape completely, not rename buckets into nicer buckets.
- The host must have one consistent event/consequence ideology across key/text, mouse/pointer, window/focus/resize, wake, clipboard, PTY child bytes/status, VT damage/metadata/replies, render frames, and present actions.
- PTY owns byte transport to/from the child process. PTY must not own key/mouse/window semantics.
- VT owns terminal protocol encoding/decoding consequences. Events/host runtime owns ordering and action dispatch. Window/display/present owners produce actions only below GL/EGL present.

Current mode:

- Single-agent user <-> agent collaboration is the default.
- The user explicitly requested a researcher <-> reviewer planning loop for this elephant definition before more code cuts.
- Research and review are planning-only inputs for the user <-> agent scoping loop.
- This loop does not produce worker-ready slices by default.
- The user and agent will build the scoped index together slowly, then choose execution cuts from that index.

Current dirty state:

- `howl-linux-host` has an uncommitted FairMutex extraction:
  - `src/sync/fair_mutex.zig`
  - `src/buckets that must die/bucket4.zig`
  - `src/pty/pump.zig`
  - `src/buckets that must die/bucekt2_test.zig`
- Verification already passed for that extraction:
  - `zig build check`
  - `zig build test:unit`
  - `git diff --check`
- Root has uncommitted accountability updates:
  - `loop/flow.md`
  - this active loop replacement
  - `sprints/current.txt`
  - `research/howl-host-io-machinery.md`

Non-negotiables:

- No compatibility aliases, old paths, shims, or stability theater.
- No final sprint shape may keep bucket owners, fake terminal surface abstractions, vague input buckets, or render/PTY/VT owners consuming host event meaning.
- Breakage during a structural slice is acceptable; broken sprint-end shape is failure.
- Slow source-backed scoping is required before cutting the full I/O machinery elephant.

Loop notes:

- Orchestrator `orch-2026-06-20-host-io-machinery-01`: hello. Intent: reconcile the accountability surface after the user requested researcher/reviewer planning for the I/O machinery elephant. Outcome: identified stale render/present active artifacts, dirty FairMutex extraction in `howl-linux-host`, and root `loop/flow.md` doctrine changes. Handoff: user and agent are in a scoping loop, not a strict sprint; use `research/howl-host-io-machinery.md` as the scoped index and do not turn it into worker-ready slices unless the user asks.
