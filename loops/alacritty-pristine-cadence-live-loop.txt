# Alacritty Pristine Cadence Live Loop

Authority:

- Live loop for the active benchmark sprint.
- Orchestrator session id: `orch-2026-06-14-ascii-rain-performance-01`.
- User handoff supplied 2026-06-14.
- The previous live sprint contract was rejected and moved to history.

Problem statement:

- Beat Alacritty on the benchmark.
- Do it by making Howl pristine, pragmatic, and idiomatic.
- The current live section is host/runtime present cadence and cursor truth.
- The defect to prove is whether Howl replays stale intermediate content/cursor states instead of presenting the latest state under redraw pressure.

Current sprint law:

- Micro-optimization is banned until the codebase is much more pragmatic and idiomatic than Alacritty.
- Fake narrow cuts are banned.
- The old ASCII-rain `direct_normal` proof chain is history only.
- The current planning target is one real owner boundary across wake, runtime drive, retained submit, present completion, and cursor truth.

Current handoff:

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello.
- Intent: reset the rejected live sprint surface, preserve the benchmark goal, and promote host/runtime present cadence and cursor truth as the active section.
- Outcome: previous active sprint, loop, and research artifacts were demoted to history; a fresh active sprint surface now points at the current section and bans micro-optimization and fake narrow cuts.
- Handoff: researcher must plan the next 5-10 big cuts for this section; reviewer must gate that plan before coder work begins.

- Researcher `research-2026-06-14-ascii-rain-performance-01`: hello.
- Intent: plan the host/runtime present-cadence and cursor-truth section into worker-ready big cuts on the real owner boundary.
- Outcome: completed a 7-cut plan covering present lifecycle, frame cadence, runtime admission/drive separation, retained state machine truth, cursor cadence ownership, unified wait admission, and end-to-end latest-state tests.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should gate the active research artifact and either accept the section plan or reject it with exact planning defects.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello.
- Intent: gate the refocused active sprint surface and the new 7-cut section plan.
- Outcome: accepted. The benchmark sprint remains intact, the host/runtime present-cadence and cursor-truth section is now live, micro-optimization and fake narrow cuts are banned, the old `direct_normal` path is demoted to history, and the active research artifact is worker-ready.
- Handoff: orchestrator should record acceptance, close the planning receipt, and seed Cut 1 for coder execution.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close the accepted refocus/planning loop and seed the first implementation section.
- Outcome: acceptance recorded after root commit `9b2b4b0`. Cut 1, present lifecycle explicit single-owner cleanup, is now the active coder section.
- Handoff: coder must implement only Cut 1 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-gpt-5.4-cut1-01`: hello.
- Intent: implement only Cut 1, make present lifecycle explicit and single-owned.
- Outcome: completed the allowed-file implementation in `display/present.zig`, `display/frame_timer.zig`, `event.zig`, and `display/display.zig`; verification passed with `zig build test:unit` in `howl-linux-host`; no blockers or deviations.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should gate Cut 1 for acceptance or rejection.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: review Cut 1 against the accepted section contract.
- Outcome: accepted. Present lifecycle is now explicit and single-owned, SDL synchronous completion truth is named honestly, event-loop present-token branching was removed, and the required tests are present and green.
- Handoff: orchestrator should commit the accepted host change and seed Cut 2.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 1 and promote the next section.
- Outcome: Cut 1 committed in `howl-linux-host` as `c925170` `Own synchronous present lifecycle`. Cut 2, frame cadence explicit single-owner cleanup, is now the active coder section.
- Handoff: coder must implement only Cut 2 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-gpt-5.4-cut2-01`: hello.
- Intent: implement only Cut 2, rebuild frame cadence around one explicit frame owner.
- Outcome: initial Cut 2 implementation passed tests but was rejected because `event.zig` still carried post-complete pacing policy; the same slice was corrected, re-verified with `zig build test:unit`, and returned with no blockers.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should gate the corrected Cut 2 state.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: re-review the corrected Cut 2 implementation against the accepted contract.
- Outcome: accepted. `FrameTimer` now owns wait, render permission, present admission, post-submit, and post-complete semantics, and `event.zig` no longer carries the rejected post-complete pacing branch.
- Handoff: orchestrator should commit the accepted host change and seed Cut 3.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 2 and promote the next section.
- Outcome: Cut 2 committed in `howl-linux-host` as `93e9224` `Own frame cadence policy`. Cut 3, runtime admission/drive separation, is now the active coder section.
- Handoff: coder must implement only Cut 3 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-gpt-5.4-cut3-01`: hello.
- Intent: implement only Cut 3, separate runtime admission from runtime drive for terminal surfaces.
- Outcome: completed the allowed-file implementation in `event.zig` and `terminal/surface.zig`; the exact required Cut 3 tests passed via filtered unit runs, and `zig build test:unit:build` succeeded; the full `zig build test:unit` timed out but did not block the slice contract.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should gate Cut 3 for acceptance or rejection.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: review Cut 3 against the accepted section contract.
- Outcome: accepted. `surface.zig` now exposes explicit runtime facts, separates bounded drive from post-drive consequences, and `event.zig` consumes explicit surface facts instead of re-deriving mixed admission booleans.
- Handoff: orchestrator should commit the accepted host change and seed Cut 4.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 3 and promote the next section.
- Outcome: Cut 3 committed in `howl-linux-host` as `3aa9621` `Separate runtime admission from drive`. Cut 4, retained render/present state-machine cleanup, is now the active coder section.
- Handoff: coder must implement only Cut 4 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-gpt-5.4-cut4-01`: hello.
- Intent: implement only Cut 4, make the retained render/present state machine explicit.
- Outcome: the first pass exposed a real terminal-surface blocker and an untruthful `failed` state path; the narrowed follow-up fixed retained owner truth and cleaned the isolated blocker path; the bounded closure gate `timeout 300s zig build test:unit` then passed cleanly.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should perform final Cut 4 acceptance.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 4 after the bounded full-suite gate.
- Outcome: accepted. The retained state machine is explicit and truthful, the render/present transition reads from one owner path, the required tests are present, and the bounded full unit suite passes.
- Handoff: orchestrator should commit the accepted host change and seed Cut 5.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 4 and promote the next section.
- Outcome: Cut 4 committed in `howl-linux-host` as `667fb1a` `Name retained render state machine`. Cut 5, cursor-truth cadence cleanup, is now the active coder section.
- Handoff: coder must implement only Cut 5 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-gpt-5.4-cut5-01`: hello.
- Intent: implement only Cut 5, restore cursor truth as a cadence concern.
- Outcome: the first pass was rejected because the surface-level activity-reset proof was too weak and the loop still split cursor cadence across separate calls; the follow-up fixed both defects and the bounded full suite `timeout 300s zig build test:unit` passed.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should perform final Cut 5 acceptance.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 5 after the rejection fix and bounded full-suite gate.
- Outcome: accepted. The loop now consumes one coherent cursor owner seam, surface-level cursor truth is explicit and tested, and the bounded full unit suite passes.
- Handoff: orchestrator should commit the accepted host change and seed Cut 6.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 5 and promote the next section.
- Outcome: Cut 5 committed in `howl-linux-host` as `16dc836` `Own cursor cadence facts`. Cut 6, loop wait-admission unification, is now the active coder section.
- Handoff: coder must implement only Cut 6 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-gpt-5.4-cut6-01`: hello.
- Intent: implement only Cut 6, unify loop wait admission over explicit owner facts.
- Outcome: completed the allowed-file implementation in `event.zig` and `display/frame_timer.zig`; the loop wait policy now reads from one explicit admission struct and one function family; `timeout 300s zig build test:unit` passed.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should perform final Cut 6 acceptance.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 6 after the bounded full-suite gate.
- Outcome: accepted. Loop wait admission is now unified through one owner-shaped struct and one function family, the required tests are present, and the bounded full unit suite passes.
- Handoff: orchestrator should commit the accepted host change and seed Cut 7.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 6 and promote the next section.
- Outcome: Cut 6 committed in `howl-linux-host` as `5af42d9` `Unify loop wait admission`. Cut 7, end-to-end latest-snapshot proof cleanup, is now the active coder section.
- Handoff: coder must implement only Cut 7 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-gpt-5.4-cut7-01`: hello.
- Intent: implement only Cut 7, end-to-end latest-snapshot proof cleanup.
- Outcome: added latest-snapshot and cursor-visibility end-to-end proofs at the existing owner seams in `display/present.zig` and `terminal/surface_test.zig`; `timeout 300s zig build test:unit` passed.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should perform final Cut 7 acceptance.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 7 after the bounded full-suite gate.
- Outcome: accepted. Latest-snapshot truth is proved, stale tokens do not become current truth, cursor visibility change is included in the proof surface, and the bounded full unit suite passes.
- Handoff: orchestrator should commit the accepted host change and stop implementation until benchmark/live-behavior reassessment names the next section.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close Cut 7 and complete the planned section.
- Outcome: Cut 7 committed in `howl-linux-host` as `3e15ab4` `Prove latest snapshot truth`. The planned 7-cut host/runtime present-cadence and cursor-truth section is complete.
- Handoff: benchmark and live-behavior reassessment must decide the next section before more coder work is seeded.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: gate the next section after reassessment.
- Outcome: accepted the next section as RCA-first reproduction and fix of autonomous unsolicited `terminal_frame` production. The first slice must pin the defect in one failing deterministic test before any behavior fix is seeded.
- Handoff: orchestrator should seed the failing-test reproduction slice with an exact owner seam and no fix work yet.

- Coder `coder-2026-06-14-gpt-5.4-rca-01`: hello.
- Intent: pin unsolicited `terminal_frame` production in one deterministic failing test before any fix.
- Outcome: the first helper-only reproduction was rejected; the replacement failing test now enters through the real `event.zig` present-planning seam and proves that autonomous cursor-only redraw is currently planned as `.terminal_frame` just like keydown-driven runtime redraw.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should gate the corrected failing-test RCA slice.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: re-review the failing-test RCA slice after the rejection fix.
- Outcome: accepted. The failing test now pins one real owner gap: present planning lacks a redraw-source discriminator separating runtime/key-input terminal progress from autonomous cursor-only redraw.
- Handoff: orchestrator should seed the fix slice on that exact failing test seam.

- Coder `coder-2026-06-14-gpt-5.4-rca-01`: hello again.
- Intent: fix the accepted owner gap at the unsolicited `terminal_frame` seam and make the failing RCA test pass.
- Outcome: completed the fix in `event.zig` and `display/present.zig`, the accepted failing test now passes, `timeout 300s zig build test:unit` passes, and the temporary user-requested host timing logs remain available in `event_loop.zig` and `display/present.zig` for reassessment.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should perform final acceptance on the unsolicited `terminal_frame` fix slice.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: review the unsolicited `terminal_frame` fix slice.
- Outcome: accepted. Cursor-only autonomous redraw no longer claims `terminal_frame` ownership, the accepted failing seam test passes, and the bounded full unit suite passes.
- Handoff: orchestrator should commit the accepted host change and stop implementation until reassessment decides whether another autonomous frame source remains.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close the unsolicited `terminal_frame` RCA section and pause for reassessment.
- Outcome: the accepted fix committed in `howl-linux-host` as `05a3324` `Classify autonomous cursor redraws`. Further implementation is paused pending live-behavior reassessment.
- Handoff: reassessment must determine whether any autonomous `terminal_frame` submits remain before another section is seeded.

- Researcher `research-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: replace reassessment-only guidance with a worker-ready sliced sprint plan for the user-directed Kitty cursor parity rewrite.
- Outcome: wrote the active research artifact as a full worker-ready sprint document covering VT, render, host, config, ABI, tests, and multiple cursors under the explicit Kitty override; updated the active sprint surface so reviewer gating is now the authorized next step.
- Handoff: reviewer `review-2026-06-14-ascii-rain-performance-01` should gate the Kitty cursor parity sprint document and either accept it or reject it with exact planning defects.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: gate the worker-ready Kitty cursor parity sprint document against full parity, no deferrals, exact worker slices, and no coder-invented architecture.
- Outcome: rejected twice for deferrals, wildcard allowlists, ambiguous coverage ownership, incomplete ABI/data-shape/protocol contracts, and under-specified algorithms. The researcher corrected the artifact after each rejection. Final verdict accepted: the active research artifact now contains worker-ready slices for full Kitty cursor parity.
- Handoff: orchestrator should commit the accepted planning artifact and seed Slice 1.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close the accepted Kitty cursor parity planning loop and seed the first implementation section.
- Outcome: Slice 1, rebuild VT semantic cursor truth and protocol vocabulary, is now the active coder section.
- Handoff: coder must implement only Slice 1 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: implement Slice 1 as originally seeded.
- Outcome: stopped correctly. The accepted Slice 1 allowlist omitted current VT cursor mutation/read owner files, so removing duplicate `Screen` cursor fields would necessarily break files outside the allowlist or preserve duplicate cursor truth.
- Handoff: orchestrator corrected Slice 1's allowlist to include the existing VT cursor helpers and `ffi/surface.zig` for compile-preserving reads only. No ABI widening or render/host work is authorized in Slice 1.

- Coder `coder-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: implement corrected Slice 1.
- Outcome: production cursor-owner move is contained to the corrected VT allowlist, but verification exposed exact existing VT tests/support still pinned to removed duplicate cursor fields.
- Handoff: orchestrator corrected Slice 1 again with exact affected test/support files only. Coder should finish proof updates and rerun bounded VT verification without changing render, host, or ABI-widening scope.

- Coder `coder-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: finish Slice 1 proof and address reviewer rejection on semantic cursor truth.
- Outcome: completed the VT semantic cursor owner move, restored correct DECSCUSR `0/1` semantics, split client cursor movement from structural cursor restoration for `position_changed_by_client_at`, moved cursor-color routing into first-class semantic event flow, and reran `zig build test:unit:build`, `timeout 300s zig build test:unit`, and `zig build test:abi` successfully.
- Handoff: reviewer should gate the corrected Slice 1 VT diff against the accepted Slice 1 contract.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: re-review corrected Slice 1 VT semantic cursor truth.
- Outcome: accepted. No findings remain. Reviewer confirmed corrected DECSCUSR behavior, client-only `position_changed_by_client_at`, first-class cursor-color semantic routing, one semantic cursor owner, report reads from that owner, and no VT/render ABI widening in this slice.
- Handoff: orchestrator should commit the accepted `howl-vt` Slice 1 diff and seed Slice 2.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close accepted Slice 1 and advance the Kitty cursor parity sprint.
- Outcome: committed accepted Slice 1 in `howl-vt` as `15caf66` `Rebuild VT semantic cursor truth`. Slice 2 is now the active implementation section.
- Handoff: coder must implement only Slice 2 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: implement Slice 2 as originally seeded.
- Outcome: stopped correctly. The Slice 2 contract required extra-cursor export and cursor-trail publication facts whose real owners still live in later slices outside the Slice 2 allowlist.
- Handoff: orchestrator must correct Slice 2 so this slice owns only widened ABI/publication seams and empty aggregate preservation, while Slice 5 remains the trail source owner and Slice 7 remains the extra-cursor state/export owner.

- Coder `coder-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: implement corrected Slice 2 seam widening.
- Outcome: widened the allowed ABI/publication files but stopped correctly because render and host verification compile against the shipped `howl-vt/include/howl_vt.h`, which still had the old cursor ABI shape and was not in the Slice 2 allowlist.
- Handoff: orchestrator must add the shipped VT C header to the Slice 2 allowlist, then coder should finish the widened ABI seam and remaining allowed-file compile fixes.

- Coder `coder-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: finish corrected Slice 2 widened ABI/publication seam.
- Outcome: completed the widened primary cursor ABI, shipped C header, zeroed extra-cursor aggregate preservation, zeroed trail aggregate preservation, validation/source-slot/publication/damage seam updates, and the inactive-tail decode fix; reran `zig build test:abi` in `howl-vt`, `zig build test` in `howl-render`, `zig build test:unit:build` in `howl-linux-host`, and `timeout 300s zig build test:unit` in `howl-linux-host` successfully.
- Handoff: reviewer should gate the corrected Slice 2 seam against the accepted contract.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: re-review corrected Slice 2 widened ABI/publication seam.
- Outcome: accepted. No findings remain. Reviewer confirmed exact widened shapes/constants, active-prefix extra-cursor decoding, zeroed extra-cursor aggregate until Slice 7, zeroed trail aggregate until Slice 5, and no invented later-slice owners.
- Handoff: orchestrator should commit the accepted Slice 2 diffs and seed Slice 3.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close accepted Slice 2 and advance the Kitty cursor parity sprint.
- Outcome: committed accepted Slice 2 in `howl-vt` as `82addbf` `Widen VT cursor ABI`, in `howl-render` as `ec49cb2` `Widen cursor publication seam`, and in `howl-linux-host` as `7805b98` `Preserve widened cursor publication`. Slice 3 is now the active implementation section.
- Handoff: coder must implement only Slice 3 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: fix the reviewer rejection on Slice 3.
- Outcome: stopped correctly. Removing the old public `CursorInput` seam from `text/scene.zig` requires exact mechanical updates in render consumer files outside the original Slice 3 allowlist.
- Handoff: orchestrator must add only those exact render consumer files to Slice 3 for mechanical `CursorPresentation` consumption, without pulling Slice 4 primitive policy into this slice.

- Coder `coder-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: finish corrected Slice 3 render-owned cursor presentation data.
- Outcome: removed the old public `CursorInput` seam from the render boundary, carried `CursorPresentation` through the contract/scene/preparer/render-consumer seams, kept `CursorColor` unresolved, and reran `zig build test` in `howl-render` successfully.
- Handoff: reviewer should gate the corrected Slice 3 render seam against the accepted contract.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: re-review corrected Slice 3 render-owned cursor presentation data.
- Outcome: accepted. No findings remain. Reviewer confirmed the old public `CursorInput` seam is gone, `CursorPresentation` is the render-owned boundary truth, and no scene-side cursor color policy or second cursor owner was introduced.
- Handoff: orchestrator should commit the accepted `howl-render` Slice 3 diff and seed Slice 4.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close accepted Slice 3 and advance the Kitty cursor parity sprint.
- Outcome: committed accepted Slice 3 in `howl-render` as `9f48e1d` `Introduce cursor presentation owner`. Slice 4 is now the active implementation section.
- Handoff: coder must implement only Slice 4 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.

- Coder `coder-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: implement and then correct Slice 4 cursor primitive emission.
- Outcome: rebuilt cursor primitive emission around explicit scene-owned cursor fill, text recolor, and trail primitives; fixed reviewer findings on grid-width `first_cell` ownership, multicell `cell_span` truth, `u32` first-cell overflow/truncation, and hidden-main-cursor suppression while preserving independent trail emission; reran `zig build test` in `howl-render` successfully after each correction.
- Handoff: reviewer should gate the corrected Slice 4 render diff against the accepted contract.

- Reviewer `review-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: re-review corrected Slice 4 cursor primitive emission.
- Outcome: accepted. No findings remain. Reviewer confirmed correct primitive ownership metadata, multicell coverage, hidden-main-cursor suppression across all draw entrypoints, and independent trail emission.
- Handoff: orchestrator should commit the accepted `howl-render` Slice 4 diff and seed Slice 5.

- Orchestrator `orch-2026-06-14-ascii-rain-performance-01`: hello again.
- Intent: close accepted Slice 4 and advance the Kitty cursor parity sprint.
- Outcome: committed accepted Slice 4 in `howl-render` as `b04de62` `Rebuild cursor primitive emission`. Slice 5 is now the active implementation section.
- Handoff: coder must implement only Slice 5 from `research/2026-06-14-alacritty-pristine-cadence-plan.md` and return exact verification receipts for reviewer pressure.
