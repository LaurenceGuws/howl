ASCII rain live performance loop scratchpad

Date: 2026-06-11.
Role owner: orchestrator.
Status: active live loop.
Orchestrator session id: `orch-2026-06-11-ascii-rain-honest-performance-02`.
Accepted control-run worker session id: `worker-2026-06-11-ascii-rain-control-run-01`.
Accepted bottleneck-proof worker session id: `worker-2026-06-12-ascii-rain-bottleneck-proof-01`.
Accepted direct-normal worker session id: `worker-2026-06-12-direct-normal-single-pass-01`.
Accepted owner-create worker session id: `worker-2026-06-12-owner-create-cache-first-01`.
Accepted fresh owner-create worker session id: `worker-2026-06-12-owner-create-fresh-rollback-01`.
Accepted payload initialization worker session id: `worker-2026-06-12-payload-in-place-init-01`.
Current required worker session id: pending for the next bottleneck-proof pass after payload initialization.
Next required worker session id: pending after the next reviewer-accepted bottleneck plan.
Researcher session id: `research-2026-06-12-render-surface-payload-init-01`.
Reviewer session id: `review-2026-06-12-render-surface-payload-init-01`.
Loop commit-hash receipt: pending until archival.

Shared scratchpad rule:

- This file is the live team scratchpad for the current step.
- Every non-trivial orchestrator, coder, researcher, or reviewer pass must append a short note with:
  - role and session id
  - a brief hello
  - what the teammate wanted to achieve in the pass
  - how at least one task went
  - next handoff or blocker

Controlling sprint:

- `/home/home/personal/projects/howl/sprints/2026-06-11-ascii-rain-honest-performance-sprint.md`

Problem statement:

- The active sprint already settles the restart rule: no optimization before fresh proof on the corrected current tree.
- Old benchmark receipts and old bottleneck conclusions are navigation only until the current tree re-proves the benchmark surface and current hot-owner order.
- The orchestrator must keep one controlling live loop that the whole team can read and write without reconstructing the current step from archive prose.
- The immediate target is not a performance fix. It is the first two accountable receipts in the user-directed order: control run first, temporary bottleneck proof second.
- The first live sequence is user-directed: control run first, worker bottleneck proof second, researcher and reviewer planning third.

Loop goal:

- Land the first live receipt for the agreed Howl vs Alacritty control run on the current tree.
- Then seed the next worker slice to identify the current biggest performance bottleneck with temporary logs only.
- Only after that evidence exists, hand the bottleneck package to researcher and reviewer for planning.
- No optimization or broad redesign slice is authorized yet.

Accepted slice: `control-run-receipt`

- Purpose:
  - record one fresh base-number control run against the agreed utils FPS telemetry harness
- Allowed files:
  - no file edits authorized
- Required commands:
  - `/home/home/personal/projects/howl/howl-linux-host`: `zig build install -Doptimize=ReleaseFast`
  - `/home/home/personal/projects/howl/utils/tools/rain-bench`: `zig build --release=fast stress:rain:build`
  - `/home/home/personal/projects/howl`: `python3 utils/tools/rain-bench/benchmark_terminals.py --duration 10 --mode ascii --terminals howl alacritty`
- Required output:
  - exact run directory path under `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/<timestamp>-ascii/`
  - exact `summary.json` receipt path
  - exact final Howl FPS and Alacritty FPS from that receipt
- Required verification:
  - the benchmark run completes for both terminals with a final metrics receipt
- Non-goals:
  - no source edits
  - no temporary logs yet
  - no redesign
  - no researcher planning yet
- Stop conditions:
  - stop if the agreed control-run harness no longer works on the current tree
  - stop if the control run exposes a correctness failure instead of a pure performance gap
- Acceptance gate:
  - worker session id
  - exact commands run
  - exact receipt paths
  - final Howl vs Alacritty FPS values

Accepted receipt:

- run directory:
  - `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-000332-ascii/`
- summary:
  - `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-000332-ascii/summary.json`
- final Howl FPS:
  - `11.35`
- final Alacritty FPS:
  - `970.58`
- metrics completion:
  - both terminals completed with `schema=1` and `final=true`

Current slice: `temporary-bottleneck-logging`

- Purpose:
  - use temporary logs to pin down the current biggest performance bottleneck
- Allowed files:
  - no source edits are required for the first proof pass
  - if current built-in timing still proves insufficient, temporary debug-log edits are authorized only in:
    - `/home/home/personal/projects/howl/howl-render/src/session/text.zig`
    - `/home/home/personal/projects/howl/howl-render/src/prepared/handle.zig`
    - `/home/home/personal/projects/howl/howl-render/src/prepared/render_surface_emitter.zig`
- Required current proof path:
  - use the accepted decoupled rain-bench harness
  - set `HOWL_RENDER_DEBUG_TIMING=1`
  - capture Howl stderr to a proof log file while running a Howl-only benchmark through the decoupled wrapper
- Required commands:
  - `/home/home/personal/projects/howl/howl-linux-host`: `zig build install -Doptimize=ReleaseFast`
  - `/home/home/personal/projects/howl/utils/tools/rain-bench`: `zig build --release=fast stress:rain:build`
  - `/home/home/personal/projects/howl`: `env HOWL_RENDER_DEBUG_TIMING=1 python3 utils/tools/rain-bench/benchmark_terminals.py --duration 3 --mode ascii --terminals howl 2> /tmp/opencode/howl-render-debug-control.log`
- Required output:
  - exact benchmark run directory path
  - exact `summary.json` path for the Howl-only proof run
  - exact stderr proof-log path
  - exact timing lines showing the strongest current bottleneck class
  - explicit conclusion naming the current biggest bottleneck owner seam
- Required verification:
  - the Howl-only benchmark completes with final metrics receipt
  - the stderr proof log contains `howl-render-debug` timing lines
- Non-goals:
  - no product fix yet
  - no broad host runtime logging resurrection
  - no PTY, VT, or benchmark-wrapper redesign
  - no research planning yet
- Stop conditions:
  - stop if the built-in timing seam does not emit enough signal to rank the biggest bottleneck honestly
  - stop if the next truthful step would require temporary logging outside the three allowed owner files
  - stop if the proof exposes a big correctness issue or vague bucket
- Acceptance gate:
  - worker session id
  - exact commands run
  - exact receipt paths
  - exact stderr proof-log path
  - quoted timing lines supporting the bottleneck ranking
  - explicit biggest-bottleneck conclusion

Orchestrator proof before worker handoff:

- decoupling receipt:
  - commit `5b138d9` `decouple rain bench from workspace root`
- stable control-run proof:
  - `utils/tools/build.zig` is intentionally empty; rain-bench owns the live build surface
  - `/home/home/personal/projects/howl/utils/tools/rain-bench/README.md` documents:
    - `zig build --release=fast stress:rain:build`
    - `python3 utils/tools/rain-bench/benchmark_terminals.py ...`
- built-in temporary timing seam proof:
  - `/home/home/personal/projects/howl/howl-render/src/session/text.zig`: `HOWL_RENDER_DEBUG_TIMING` emits `direct_normal_*` and `owner_create_*`
  - `/home/home/personal/projects/howl/howl-render/src/prepared/handle.zig`: `HOWL_RENDER_DEBUG_TIMING` emits `prepared_handle_create`
  - `/home/home/personal/projects/howl/howl-render/src/prepared/render_surface_emitter.zig`: `HOWL_RENDER_DEBUG_TIMING` emits `emit_prepared`
- orchestrator sample proof receipt:
  - benchmark run dir:
    - `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-000456-ascii/`
  - summary:
    - `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-000456-ascii/summary.json`
  - stderr timing log:
    - `/tmp/opencode/howl-render-debug-control.log`
  - current strongest timing signal from the sample proof:
    - `direct_normal_scan_avg_us=2197-2229`
    - `owner_create_avg_us=1277-1416`
    - `sprites_avg_us=456-561`
  - current working conclusion:
    - the biggest current bottleneck class is direct-normal scan work in the text prepare path

Research handoff after worker evidence: `bottleneck-evidence-review-plan`

- Researcher and reviewer start only after the bottleneck evidence exists.
- If the bottleneck hunt exposes a big correctness issue or a vague bucket, stop and bring it to the user for a subsprint instead of forcing planning through it.

Active research target:

- `/home/home/personal/projects/howl/research/2026-06-12-render-surface-payload-init-plan.md`

Research goal:

- turn the accepted current bottleneck proof into a reviewer-accepted next worker slice that attacks the true owner seam in pristine shape
- keep the scope inside the proven bottleneck owner seam unless current source proves another owner is entangled tightly enough that the slice would be dishonest without it
- use Alacritty and TigerBeetle pressure to reject fake quick wins, vague bucket moves, or host-side distractions

Research package must include:

- sources read in order
- exact file and line references
- current-code facts
- reference facts
- compact anchor map for the bottleneck decision
- exact proof receipts being relied on
- owner roles and proposed shape
- explicit ordered worker slice plan
- exact allowed files
- exact required shape
- exact tests
- exact non-goals
- exact stop conditions
- required assertions
- risks
- proof gaps
- readiness judgment

Reviewer acceptance gate for this handoff:

- reject if the plan does not center the current `owner_create` proof seam explicitly
- reject if the plan jumps to host GL, PTY, VT, or benchmark-wrapper redesign without current-source proof that the bottleneck ranking is wrong
- reject if the next worker slice lacks exact allowed files, required shape, tests, non-goals, stop conditions, or receipt fields
- reject if the plan recommends optimization before addressing owner truth or bottleneck-owner debt inside the true seam

Required current-source read pack:

- `/home/home/personal/projects/howl/reference-index.md`
- `/home/home/personal/projects/howl/utils/tools/rain-bench/README.md`
- `/home/home/personal/projects/howl/utils/tools/rain-bench/benchmark_terminals.py`
- `/home/home/personal/projects/howl/utils/tools/rain-bench/ascii_rain_stress.zig`
- `/home/home/personal/projects/howl/utils/tools/rain-bench/visual_rain_stress.zig` if the current harness points there
- `/home/home/personal/projects/howl/utils/tools/rain-bench/build.zig`
- `/home/home/personal/projects/howl/howl-linux-host/build.zig`
- `/home/home/personal/projects/howl/howl-linux-host/src/main.zig`
- `/home/home/personal/projects/howl/howl-linux-host/src/cli/args.zig`
- `/home/home/personal/projects/howl/howl-linux-host/src/app/processor.zig`
- `/home/home/personal/projects/howl/howl-render/src/session/text.zig`
- `/home/home/personal/projects/howl/howl-render/src/prepared/handle.zig`
- `/home/home/personal/projects/howl/howl-render/src/prepared/render_surface_emitter.zig`
- Navigation only:
  - `/home/home/personal/projects/howl/research/done/cache-2026-06-08-ascii-rain-benchmark-surface.md`
  - `/home/home/personal/projects/howl/loops/done/pre-team-scratchpad/ascii-rain-baseline-bottleneck.txt`

Stop conditions:

- stop if the current benchmark harness and the agreed user workflow materially conflict
- stop if the current tree exposes a correctness blocker or vague bucket that would make temporary bottleneck logging dishonest
- stop if stable references and current source directly disagree in a way that needs user override

Completion gate:

- `sprints/current.txt` continues to point only at the live sprint, active loop, and active research artifact for this step
- one accepted control-run receipt exists
- one accepted bottleneck-proof receipt exists
- one accepted direct-normal execution receipt exists
- reviewer accepts or rejects the active owner-create bottleneck plan before the next worker slice is seeded

Accepted execution slice: `direct-normal-single-pass-publication`

- Coder session id:
  - `worker-2026-06-12-direct-normal-single-pass-01`
- Files changed:
  - `/home/home/personal/projects/howl/howl-render/src/text/direct_normal.zig`
  - `/home/home/personal/projects/howl/howl-render/src/text/frame_preparer.zig`
- Tests run:
  - `/home/home/personal/projects/howl/howl-render`: `zig build test:unit`
  - `/home/home/personal/projects/howl/howl-linux-host`: `zig build install -Doptimize=ReleaseFast`
  - `/home/home/personal/projects/howl/utils/tools/rain-bench`: `zig build --release=fast stress:rain:build`
  - `/home/home/personal/projects/howl`: `env HOWL_RENDER_DEBUG_TIMING=1 python3 utils/tools/rain-bench/benchmark_terminals.py --duration 3 --mode ascii --terminals howl 2> /tmp/opencode/howl-render-debug-control.log`
  - `/home/home/personal/projects/howl`: `python3 utils/tools/rain-bench/benchmark_terminals.py --duration 10 --mode ascii --terminals howl alacritty`
- Updated receipt paths:
  - 3-second summary: `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-002652-ascii/summary.json`
  - 10-second summary: `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-002709-ascii/summary.json`
  - timing log: `/tmp/opencode/howl-render-debug-control.log`
- Before timing proof:
  - `direct_normal_scan_avg_us=1832 owner_create_avg_us=1315 sprites_avg_us=569`
- After timing proof:
  - `howl-render-debug prepare_handle count=640 prepare_surface_avg_us=973 prepare_surface_max_us=18814 input_avg_us=0 session_preparer_avg_us=0 session_prepare_cells_avg_us=0 direct_normal_avg_us=971 direct_normal_scan_avg_us=857 direct_normal_backgrounds_avg_us=31 direct_normal_clears_avg_us=0 direct_normal_decorations_avg_us=46 direct_normal_cursor_avg_us=0 direct_normal_raster_avg_us=34 owner_create_avg_us=1151 owner_create_max_us=4940`
- Result:
  - Howl improved from `11.35 fps` to `33.85 fps`
  - Alacritty rerun is `1012.33 fps`
  - `direct_normal_scan` is no longer the top bucket; `owner_create` is now higher
- Commit-hash handoff status:
  - pending orchestrator receipt closure after review and commit

Current slice: `owner-create-cache-first-emission`

- Coder session id:
  - `worker-2026-06-12-owner-create-cache-first-01`
- Purpose:
  - remove repeated upload-byte staging from the hot prepared-surface emission path when the sprite/atlas cache already proves reuse
- Allowed files:
  - `/home/home/personal/projects/howl/howl-render/src/prepared/render_surface_emitter.zig`
  - `/home/home/personal/projects/howl/howl-render/src/prepared/sprite_resource_store.zig`
  - `/home/home/personal/projects/howl/howl-render/src/prepared/render_surface_emitter_test.zig`
  - `/home/home/personal/projects/howl/howl-render/src/prepared/owner_test.zig`
- Not allowed:
  - any other file
- Required tests and verification:
  - `/home/home/personal/projects/howl/howl-render`: `zig build test:unit`
  - `/home/home/personal/projects/howl/howl-linux-host`: `zig build install -Doptimize=ReleaseFast`
  - `/home/home/personal/projects/howl/utils/tools/rain-bench`: `zig build --release=fast stress:rain:build`
  - `/home/home/personal/projects/howl`: `env HOWL_RENDER_DEBUG_TIMING=1 python3 utils/tools/rain-bench/benchmark_terminals.py --duration 3 --mode ascii --terminals howl 2> /tmp/opencode/howl-render-debug-control.log`
  - `/home/home/personal/projects/howl`: `python3 utils/tools/rain-bench/benchmark_terminals.py --duration 10 --mode ascii --terminals howl alacritty`
- Required existing proof roots to keep green:
  - `render surface surface emitter persists prepared sprite resource across surfaces`
  - `render surface surface emitter failure preserves accepted persistent resource state`
  - `render-surface sprite resource store reuses last atlas and resource lookups`
  - `create reports missing-sprite diagnostic without double free`
- Required new owner-local tests:
  - reused alpha atlas sprite emits zero uploads and leaves `upload_bytes_count` unchanged on the second emission
  - reused persistent color sprite emits zero uploads and leaves `upload_bytes_count` unchanged on the second emission
  - cache admission reports reuse before emitter-side byte staging for both atlas and color sprite paths
- Exact stop conditions:
  - stop if truthful cache-first admission requires widening beyond the four allowed files
  - stop if current tests expose a correctness bug in sprite/resource identity rather than a pure performance debt
  - stop if the worker cannot preserve existing failure mapping and persistent/transient semantics
  - stop if the 3-second timing rerun does not lower `owner_create_avg_us` below `1151`
  - stop if the benchmark rerun changes semantics or breaks existing proof roots even when `owner_create_avg_us` improves
- Receipt fields required from the worker:
  - coder session id
  - files changed
  - exact tests run
  - exact updated receipt paths
  - quoted timing lines from `/tmp/opencode/howl-render-debug-control.log`
  - exact before/after `owner_create_avg_us`, `prepared_handle_create emit_avg_us`, `stage_upload_avg_us`, and `sprites_avg_us`
  - commit-hash handoff status pending orchestrator closure

Execution receipt candidate for current review:

- Files changed:
  - `/home/home/personal/projects/howl/howl-render/src/prepared/render_surface_emitter.zig`
  - `/home/home/personal/projects/howl/howl-render/src/prepared/sprite_resource_store.zig`
  - `/home/home/personal/projects/howl/howl-render/src/prepared/render_surface_emitter_test.zig`
- Exact tests run:
  - `/home/home/personal/projects/howl/howl-render`: `zig build test:unit`
  - `/home/home/personal/projects/howl/howl-linux-host`: `zig build install -Doptimize=ReleaseFast`
  - `/home/home/personal/projects/howl/utils/tools/rain-bench`: `zig build --release=fast stress:rain:build`
  - `/home/home/personal/projects/howl`: `env HOWL_RENDER_DEBUG_TIMING=1 python3 utils/tools/rain-bench/benchmark_terminals.py --duration 3 --mode ascii --terminals howl 2> /tmp/opencode/howl-render-debug-control.log`
  - `/home/home/personal/projects/howl`: `python3 utils/tools/rain-bench/benchmark_terminals.py --duration 10 --mode ascii --terminals howl alacritty`
- Exact receipt paths counted for acceptance:
  - 3-second summary: `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-005326-ascii/summary.json`
  - 10-second summary: `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-005338-ascii/summary.json`
  - timing log: `/tmp/opencode/howl-render-debug-control.log`
- Same-turn probe receipts that must not be used for acceptance:
  - `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-005056-ascii/`
  - `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-005103-ascii/`
- Before timing proof:
  - `owner_create_avg_us=1151`
  - `prepared_handle_create emit_avg_us=1149`
  - `stage_upload_avg_us=100`
  - `sprites_avg_us=441`
- After timing proof:
  - `owner_create_avg_us=1094`
  - `prepared_handle_create emit_avg_us=1093`
  - `stage_upload_avg_us=0`
  - `sprites_avg_us=345`
- Result:
  - 3-second Howl rerun: `34.1 fps`
  - 10-second Howl rerun: `35.67 fps`
  - 10-second Alacritty rerun: `1008.17 fps`
  - `owner_create` improved below the `1151` gate, but Howl still trails Alacritty badly
- Commit-hash handoff status:
  - pending orchestrator receipt closure after review and commit

Team scratchpad

1. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: seed one live loop that keeps the team on the same current step and clears out old loop clutter.
- How one task went: I converted the active loop into the controlling shared scratchpad and prepared the old loop archive to move under one subdirectory.
- Next handoff or blocker: the control-run worker can now use this file as the shared live handoff surface for the first receipt.

2. Role: coder/worker
- Session id: `worker-2026-06-11-ascii-rain-control-run-01`
- Hello: hello
- Intended task: run the seeded control benchmark commands and record the fresh Howl vs Alacritty receipt.
- How one task went: the host ReleaseFast install command ran, but the seeded `utils/tools` stress build command failed immediately because the current `utils/tools` build entrypoint rejected `-Doptimize=ReleaseFast`.
- Next handoff or blocker: blocker for orchestrator; the agreed control-run harness commands are stale on the current tree, so I stopped before forcing the benchmark step.

3. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: re-prove the decoupled rain-bench control path myself, stabilize the control receipt, and seed the next worker with exact timing-proof usage.
- How one task went: the decoupling proof is current, the control run completed at Howl `11.35 fps` vs Alacritty `970.58 fps`, and the built-in `HOWL_RENDER_DEBUG_TIMING` seam already isolated `direct_normal_scan` as the strongest current bottleneck signal.
- Next handoff or blocker: the next worker can run the seeded `temporary-bottleneck-logging` slice exactly as written; temporary debug-log edits are authorized only if the built-in seam proves insufficient.

4. Role: coder/worker
- Session id: `worker-2026-06-12-ascii-rain-bottleneck-proof-01`
- Hello: hello
- Intended task: run the seeded Howl-only timing proof and identify the biggest current bottleneck owner seam without product edits.
- How one task went: the built-in `HOWL_RENDER_DEBUG_TIMING` seam was sufficient on the first pass, and the stderr proof log kept `direct_normal_scan_avg_us` well above the other logged classes.
- Next handoff or blocker: hand off the fresh receipt and timing proof to the orchestrator, researcher, and reviewer for the next planning step; no blocker in this slice.

5. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: seed the researcher and reviewer handoff from the accepted bottleneck proof so the next worker slice is source-backed instead of improvised.
- How one task went: the evidence is strong enough to hand off directly, with `direct_normal_scan` dominating the built-in timing proof and no correctness blocker exposed yet.
- Next handoff or blocker: researcher owns the next pass in `/home/home/personal/projects/howl/research/2026-06-12-direct-normal-scan-bottleneck-plan.md`, then reviewer gates the resulting plan.

6. Role: researcher
- Session id: `research-2026-06-12-direct-normal-scan-plan-01`

7. Role: coder/worker
- Session id: `worker-2026-06-12-owner-create-cache-first-01`
- Hello: hello
- Intended task: remove repeated prepared-sprite upload staging from the owner-create emission path when the cache already proves reuse.
- How one task went: the first cache-first pass removed staging but missed the timing gate, and a same-owner contiguous fast path in the sprite resource store dropped `owner_create_avg_us` to `1094` on the required rerun.
- Next handoff or blocker: hand off the verified slice receipts and timing proof to the reviewer and orchestrator for acceptance, review, and commit-hash closure.
- Hello: hello
- Intended task: turn the accepted `direct_normal_scan` proof into a reviewer-acceptable worker slice centered on the true render owner seam.
- How one task went: current source and the proof log lined up cleanly around `direct_normal`'s duplicated normal-lane scan, and I also found that `benchmark_main.zig` currently bypasses the exact publication hot path.
- Next handoff or blocker: reviewer should gate the active research artifact; the main proof gap is that the owner-local render benchmark is not yet truthful for publication-path acceptance.

7. Role: reviewer
- Session id: `review-2026-06-12-direct-normal-scan-plan-01`
- Hello: hello
- Intended task: gate the active bottleneck plan and reject any vague slice boundary, weak proof target, or host-side drift.
- How one task went: the first pass rejected real scope ambiguity around `benchmark_main.zig` and vague proof/test requirements, and the corrected plan now passes with one exact worker slice, one exact file boundary, and one exact timing target.
- Next handoff or blocker: the next worker slice can now be seeded from `/home/home/personal/projects/howl/research/2026-06-12-direct-normal-scan-bottleneck-plan.md`.

8. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: close the researcher/reviewer planning step with an accepted next worker slice.
- How one task went: the plan is now reviewer-accepted and centered on collapsing the duplicated `direct_normal` publication scan inside the true hot owner seam.
- Next handoff or blocker: seed the `direct-normal-single-pass-publication` worker slice.

9. Role: coder/worker
- Session id: `worker-2026-06-12-direct-normal-single-pass-01`
- Hello: hello
- Intended task: remove the duplicated publication direct-normal scan without changing render consequences.
- How one task went: the direct-normal owner now rolls back partial fast-path scratch on the first complex publication cell and reuses that same pass to carry the exact complex-cell proof into the shared fallback.
- Next handoff or blocker: hand off the verified slice receipts to the orchestrator; the rerun moved `direct_normal_scan` below `owner_create`, so I stopped at the slice boundary instead of broadening further.

10. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: close the accepted direct-normal slice honestly and restart the loop from the new post-slice bottleneck instead of coding by momentum.
- How one task went: the slice passed its proof gate, but the rerun still trails Alacritty and the top bucket flipped from `direct_normal_scan` to `owner_create`, so the loop must restart at the new owner ranking.
- Next handoff or blocker: researcher owns the new owner-create planning pass in `/home/home/personal/projects/howl/research/2026-06-12-owner-create-bottleneck-plan.md`.

11. Role: researcher
- Session id: `research-2026-06-12-owner-create-plan-01`
- Hello: hello
- Intended task: turn the accepted `owner_create` proof into a reviewer-acceptable next worker slice centered on the true render owner seam.
- How one task went: the current source and timing receipts lined up around `PreparedHandle.create` being hot only because it immediately runs render-surface emission, and the strongest owner-local debt is redundant sprite upload-byte staging before cache reuse admission.
- Next handoff or blocker: reviewer should gate `/home/home/personal/projects/howl/research/2026-06-12-owner-create-bottleneck-plan.md`; no blocker yet, but the next worker must stay inside `render_surface_emitter` and `sprite_resource_store`.

12. Role: reviewer
- Session id: `review-2026-06-12-owner-create-plan-01`
- Hello: hello
- Intended task: gate the active owner-create bottleneck plan and reject vague proof placement, invented mechanisms, or weak stop conditions.
- How one task went: the first pass rejected loose proof placement and an over-invented cache mechanism, and the corrected plan now passes with exact test files, exact assertions, and an exact `owner_create_avg_us < 1151` stop gate.
- Next handoff or blocker: the next worker slice can now be seeded from `/home/home/personal/projects/howl/research/2026-06-12-owner-create-bottleneck-plan.md`.

13. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: close the accepted owner-create planning step before handing new implementation work to the team.
- How one task went: the plan is now reviewer-accepted and tightly centered on cache-first reuse admission inside `render_surface_emitter` plus `sprite_resource_store`.
- Next handoff or blocker: commit and push this accepted planning state, then seed the `owner-create-cache-first-emission` worker slice.

14. Role: coder/worker
- Session id: `worker-2026-06-12-owner-create-cache-first-01`
- Hello: hello
- Intended task: remove repeated upload-byte staging when the prepared sprite cache already proves reuse.
- How one task went: the cache-first emission pass cleared the timing gate after one in-slice refinement, with `stage_upload_avg_us` dropping to `0` and `owner_create_avg_us` dropping to `1094`.
- Next handoff or blocker: hand off the verified slice receipts to the orchestrator and reviewer; Howl improved again, but the sprint must loop because we are still slower than Alacritty.

15. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: close the accepted owner-create slice and immediately restart the sprint loop from the new baseline instead of stalling on a finished step.
- How one task went: the owner-create slice is accepted and pushed, the new 10-second control rerun is `35.67 fps` vs Alacritty `1008.17 fps`, and `owner_create` remains the top bucket.
- Next handoff or blocker: researcher owns the next post-cache-first owner-create planning pass in `/home/home/personal/projects/howl/research/2026-06-12-owner-create-post-cache-first-plan.md`.

16. Role: researcher
- Session id: `research-2026-06-12-owner-create-post-cache-first-01`
- Hello: hello
- Intended task: turn the fresh post-cache-first `owner_create` proof into the next reviewer-acceptable worker slice without drifting out of the current seam.
- How one task went: current source and the fresh timing receipts lined up around `emitPreparedFresh` still copying the whole retained sprite-resource store even after cache-first reuse removed upload staging.
- Next handoff or blocker: reviewer should gate `/home/home/personal/projects/howl/research/2026-06-12-owner-create-post-cache-first-plan.md`; no blocker yet, but the next worker must stay inside fresh emitter/resource-store rollback plus fresh-path tests.

17. Role: reviewer
- Session id: `review-2026-06-12-owner-create-post-cache-first-01`
- Hello: hello
- Intended task: gate the post-cache-first owner-create plan before worker execution.
- How one task went: the first pass rejected an incomplete rollback contract and unproved absolute timing gate; the corrected plan now explicitly restores `atlas_resource` and uses the accepted `1093`/`1094` timing baselines.
- Next handoff or blocker: seed the `owner-create-fresh-resource-rollback` worker slice from `/home/home/personal/projects/howl/research/2026-06-12-owner-create-post-cache-first-plan.md`; no blocker.

18. Role: coder/worker
- Session id: `worker-2026-06-12-owner-create-fresh-rollback-01`
- Hello: hello
- Intended task: remove the fresh-path whole `SpriteResourceStore` copy while preserving exact retained admission rollback on emission failure.
- How one task went: the fresh emitter now mutates the retained store directly behind an owner-local admission rollback, and the timing rerun moved `prepared_handle_create emit_avg_us` to `1052` and `owner_create_avg_us` to `1053`.
- Next handoff or blocker: hand off the verified slice receipts to the orchestrator and reviewer; no stop condition triggered.

19. Role: reviewer
- Session id: `review-2026-06-12-owner-create-fresh-rollback-01`
- Hello: hello
- Intended task: gate the implemented fresh owner-create rollback slice against the accepted worker contract.
- How one task went: the slice was accepted with no findings; `emitPreparedFresh` no longer copies the whole store, rollback covers `atlas_resource` and retained metadata, and fresh-path reuse/failure tests are present.
- Next handoff or blocker: orchestrator should commit the accepted render slice and root receipts, then restart the loop because Howl is still slower than Alacritty.

20. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: close the accepted fresh rollback slice and restart from the new measured baseline.
- How one task went: render commit `8bd867b` landed the accepted slice; accepted receipts are `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-011550-ascii/summary.json` and `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-011602-ascii/summary.json`, with Howl `36.85 fps` vs Alacritty `1009.95 fps`.
- Next handoff or blocker: seed the next bottleneck-proof worker pass from the fresh rollback baseline; no correctness or vague-bucket blocker is known.

21. Role: coder/worker
- Session id: `worker-2026-06-12-post-fresh-bottleneck-proof-01`
- Hello: hello
- Intended task: prove the current biggest bottleneck after the accepted fresh rollback slice without implementing another performance fix.
- How one task went: the existing `HOWL_RENDER_DEBUG_TIMING` control log was sufficient; `owner_create_avg_us=1053` remains above `direct_normal_avg_us=952`, and `prepared_handle_create emit_avg_us=1052` accounts for nearly all of the owner-create bucket.
- Next handoff or blocker: researcher planning is ready to target the current prepared-handle fresh emission gap; no temporary logs were added and no correctness blocker was exposed.

22. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: promote the next active research target from the post-fresh bottleneck proof.
- How one task went: the current control proof keeps `owner_create` on top, but the visible emitter sub-buckets do not fully account for `prepared_handle_create emit_avg_us=1052`, so the active research target is now the prepared-handle fresh emission gap.
- Next handoff or blocker: researcher owns `/home/home/personal/projects/howl/research/2026-06-12-prepared-handle-fresh-emission-gap-plan.md`; no correctness or vague-bucket blocker is known.

23. Role: researcher
- Session id: `research-2026-06-12-prepared-handle-fresh-emission-gap-01`
- Hello: hello
- Intended task: source-pin the post-fresh prepared-handle emission gap and produce one reviewer-gated next worker slice.
- How one task went: current source and receipts proved a real residual between `prepared_handle_create emit_avg_us=1052` and visible `emit_prepared` sub-buckets, so the proposed next slice is proof-only temporary owner-local timing rather than an optimization guess.
- Next handoff or blocker: reviewer should gate `/home/home/personal/projects/howl/research/2026-06-12-prepared-handle-fresh-emission-gap-plan.md`; no correctness blocker is known, but optimization remains blocked until the residual is line-owned.

24. Role: reviewer
- Session id: `review-2026-06-12-prepared-handle-fresh-emission-gap-01`
- Hello: hello
- Intended task: gate the prepared-handle fresh emission gap plan as a proof-only worker slice before any optimization.
- How one task went: the plan is correctly centered on `handle.zig` and `render_surface_emitter.zig`, but the temporary instrumentation cleanup contract is not explicit enough for acceptance.
- Next handoff or blocker: researcher/orchestrator should correct the cleanup and receipt requirements before seeding a worker.

25. Role: reviewer
- Session id: `review-2026-06-12-prepared-handle-fresh-emission-gap-01`
- Hello: hello
- Intended task: re-review the corrected prepared-handle fresh emission gap plan after the cleanup-contract rejection.
- How one task went: the updated plan now requires temporary instrumentation cleanup before handoff, explicit `git diff`/`git status` confirmation for the touched files, and an orchestrator receipt if instrumentation is intentionally kept.
- Next handoff or blocker: the proof-only worker slice can be seeded from the active research artifact.

26. Role: coder/worker
- Session id: `worker-2026-06-12-prepared-handle-fresh-emission-proof-01`
- Hello: hello
- Intended task: run the accepted proof-only residual timing slice and remove all temporary source instrumentation before handoff.
- How one task went: the proof run source-pinned the post-fresh prepared-handle gap to `payload.* = .{}` with `payload_init_avg_us=740`, while `payload_emit_residual_avg_us=0`, `fresh_residual_avg_us=0`, and `fresh_record_avg_us=0` at the 640-frame receipt.
- Next handoff or blocker: hand off to researcher planning for the payload initialization owner; no blocker and no temporary source instrumentation remains.

27. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: promote the next active optimization-planning target from the proof-only residual receipt.
- How one task went: the proof-only worker removed temporary instrumentation cleanly and line-owned the largest child bucket to `payload.* = .{}` with `payload_init_avg_us=740`, so the next research target is render-surface payload initialization.
- Next handoff or blocker: researcher owns `/home/home/personal/projects/howl/research/2026-06-12-render-surface-payload-init-plan.md`; no correctness or vague-bucket blocker is known.

28. Role: researcher
- Session id: `research-2026-06-12-render-surface-payload-init-01`
- Hello: hello
- Intended task: turn the line-owned payload initialization proof into one reviewer-acceptable optimization slice.
- How one task went: the plan centers on removing `payload.* = .{}` from `PreparedHandle.emitRenderSurfacePayload` by making fresh emission initialize undefined payload storage before publication.
- Next handoff or blocker: reviewer should gate `/home/home/personal/projects/howl/research/2026-06-12-render-surface-payload-init-plan.md`; no blocker.

29. Role: reviewer
- Session id: `review-2026-06-12-render-surface-payload-init-01`
- Hello: hello
- Intended task: gate the render-surface payload initialization optimization plan before worker execution.
- How one task went: the plan was accepted with no blocking findings because it stays inside `PreparedHandle` allocation/publication plus `Emitter` fresh initialization and requires undefined-storage and pointer-stability proof.
- Next handoff or blocker: seed the `prepared-handle-fresh-payload-in-place-init` worker slice; no blocker.

28. Role: researcher
- Session id: `research-2026-06-12-render-surface-payload-init-01`
- Hello: hello
- Intended task: turn the payload-init proof into one reviewer-acceptable optimization slice without drifting out of the prepared render-surface owner seam.
- How one task went: current source proved `publishSurface` derives all host-visible spans from counts after `resetPrepared`, so the worker slice can remove the full payload aggregate assignment only if undefined fresh-emitter initialization is tested directly.
- Next handoff or blocker: reviewer should gate `/home/home/personal/projects/howl/research/2026-06-12-render-surface-payload-init-plan.md`; proof gap is that `payload_init_avg_us` was temporary instrumentation and may not be re-emitted after cleanup.

29. Role: coder/worker
- Session id: `worker-2026-06-12-payload-in-place-init-01`
- Hello: hello
- Intended task: implement the accepted `prepared-handle-fresh-payload-in-place-init` slice by removing full payload aggregate initialization and proving fresh emission initializes undefined storage safely.
- How one task went: the fresh payload path now publishes only after `emitPreparedFresh` resets and publishes initialized control fields, and the required rerun moved `prepared_handle_create emit_avg_us` from `1052` to `360`.
- Next handoff or blocker: hand off the verified receipts to reviewer and orchestrator; no stop condition triggered and commit-hash closure remains pending.

30. Role: reviewer
- Session id: `review-2026-06-12-payload-in-place-init-01`
- Hello: hello
- Intended task: gate the implemented payload in-place initialization slice against undefined-storage and publication-safety requirements.
- How one task went: the slice was accepted with no blocking findings; `payload.* = .{}` is removed, fresh emission initializes before publication, and unit tests passed independently in `howl-render`.
- Next handoff or blocker: orchestrator should commit the accepted render slice and root receipts, then restart the loop because Howl remains slower than Alacritty.

31. Role: orchestrator
- Session id: `orch-2026-06-11-ascii-rain-honest-performance-02`
- Hello: hi
- Intended task: close the accepted payload initialization slice and restart from the new measured baseline.
- How one task went: render commit `01e8bec` landed the accepted slice; receipts are `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-014446-ascii/summary.json` and `/home/home/personal/projects/howl/utils/tools/rain-bench/artifacts/stress/20260612-014500-ascii/summary.json`, with Howl `55.54 fps` vs Alacritty `987.81 fps`.
- Next handoff or blocker: seed the next bottleneck-proof worker pass from the payload-init baseline; no correctness or vague-bucket blocker is known.
