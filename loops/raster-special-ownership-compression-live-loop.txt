Raster special ownership compression live loop scratchpad

Date: 2026-06-13.

Role owner: orchestrator.

Status: Slice 5 seeded for execution; reviewer-gated implementation pending.

Orchestrator session id: `orch-2026-06-13-raster-special-01`.

Researcher session id: `research-2026-06-13-raster-special-01`.

Reviewer session id: `review-2026-06-13-raster-special-01`.

Loop commit-hash receipt: root `0e53a8e`.

Shared scratchpad rule:

- This file is the live team scratchpad for the current step.
- Every non-trivial orchestrator, researcher, reviewer, or coder pass must append a short note with role/session id, hello, intent, outcome, and handoff or blocker.

Controlling sprint:

- `/home/home/personal/projects/howl/sprints/2026-06-13-raster-special-ownership-compression-sprint.md`

Active research target:

- `/home/home/personal/projects/howl/research/2026-06-13-raster-special-ownership-compression-plan.md`

Current step:

- The planning package is accepted at root `0e53a8e`.
- Slice 1, `Extract undercurl owner`, is accepted in `howl-render` `42b68b8`; root `a8c3e50`.
- Slice 2, `Create generated-special root and extract box owner`, is accepted in `howl-render` `a542d8a`; root `c52f69d`.
- Slice 3, `Extract powerline owner`, is accepted in `howl-render` `24286d6`; root `0a73776`.
- Slice 4, `Extract the current block-plus-braille dispatch owner`, is accepted in `howl-render` `1986d89`.
- Root receipt for Slice 4 is pending orchestrator commit.
- Slice 5, `Extract the current legacy-computing owner`, is the active execution slice.
- Only Slice 5 is authorized.
- Coder session id: `coder-2026-06-13-raster-special-05`.

Exact user direction:

- Seed the next sprint now because the active surface is clean but empty.
- Prefer `howl-render/src/text/raster/special.zig` as the next battlefield.
- Pressure the target with reference guidance before execution planning is accepted.

Exact non-goals:

- No route-table expansion.
- No ABI/header change.
- No host/display edit.
- No render-wide redesign.

Exact stop conditions:

- Stop if the move requires changing the `.legacy_computing` route proved by `howl-render/src/text/symbol_map.zig:10,31-49` and `howl-render/src/text/resolver.zig:307-327,330-341`.
- Stop if the move requires retaining any legacy-only helper from `howl-render/src/text/raster/special.zig:317-318,784-866` inside `generated_special.zig`.
- Stop if the move requires introducing any new generic helper bucket outside `generated_special.zig`.

Slice 5 execution contract:

- Allowed files only:
  - `/home/home/personal/projects/howl/howl-render/src/text/raster/generated_special.zig`
  - `/home/home/personal/projects/howl/howl-render/src/text/raster/special_legacy_computing.zig`
- Required shape:
  - Move the current legacy-computing raster owner from `howl-render/src/text/raster/special.zig:186-239,261-866,1255-1367` out of `generated_special.zig` and into `howl-render/src/text/raster/special_legacy_computing.zig`.
  - `special_legacy_computing.zig` owns exactly:
    - `rasterizeGeneratedEightBarAlpha`
    - `rasterizeGeneratedSextantAlpha`
    - `rasterizeEightBarAlpha`
    - `rasterizeGeneratedOctantAlpha`
    - `generatedOctantPattern`
    - `rasterizeGeneratedSmoothMosaicAlpha`
    - `smoothMosaicPoints`
    - `drawSmoothMosaic`
    - `SpriteEdge`
    - `AlphaCorner`
    - `rasterizeGeneratedHalfTriangleAlpha`
    - `drawHalfTriangle`
    - `rasterizeGeneratedEightBarCompositeAlpha`
    - `SpriteShade`
    - `rasterizeGeneratedShadeCornerCrossAlpha`
    - `drawCheckerShade`
    - `drawCrossShade`
    - `applyCornerMask`
    - `rasterizeGeneratedMidLineAlpha`
    - `drawMidLine`
    - `rasterizeGeneratedBranchAlpha`
    - `BranchNode`
    - `BranchEdge`
    - `drawBranchNode`
    - `drawBranchLine`
    - `drawBranchArc`
    - `fillTriangleAlpha`
    - `pointInTriangle`
    - `triangleEdge`
    - `drawSegmentStrokeAlpha`
    - `distanceToSegment`
    - `fillCircleAlpha`
    - `drawCircleArcAlpha`
    - `rasterizeOctantAlpha`
    - `fillOctantSegment`
    - `fourthRange`
    - `octantMask`
    - `rasterizeSextantAlpha`
    - `drawSextantRow`
    - `fillSextantCell`
  - `generated_special.zig` keeps only the helpers that are still truly shared after slices 1 through 4:
    - public generated-special entrypoints and family dispatch
    - `PointF`
    - `lineY`
    - `drawLineAlpha`
    - `Range`
    - `eighthPartitionRange`
    - `supersampledCoverage`
    - `fillRectAlpha`
    - `saturatingSubU16`
    - `pixelRowOffset`
    - `pixelOffset`
    - `pixelCount`
    - `count32`
  - Legacy-only helpers from `howl-render/src/text/raster/special.zig:317-318,784-866` must not remain in `generated_special.zig`.
- Required proof roots and runs:
  - `howl-render/src/text/raster/special_test.zig:60-111,302-310,312-378,628-679`
  - `howl-render/src/text/ft_hb/glyph_raster.zig:239-261`
  - `howl-render/src/text/symbol_map.zig:31-41,44-51`
  - `howl-render/src/text/resolver.zig:294-342`
  - `howl-render/src/text/lane.zig:449-465`
  - `howl-render/src/text/shape/grouping.zig:259-275`
  - package-local run: `cd howl-render && zig build test:unit`
- Verification handoff requirements:
  - coder must report files changed, concise implementation summary, verification run/results, blockers/deviations, coder session id, and commit-hash handoff status
  - reviewer verdict plus exact findings is required before acceptance

Team scratchpad

1. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: seed the next planning sprint around the render raster concentration with `special.zig` as the first pressure target.
- How one task went: converted the current post-scene-compression pressure ranking into live sprint, research, and loop artifacts; kept execution unauthorized and required reference-first pressure on `howl-render/src/text/raster/special.zig` before any slice plan can be accepted.
- Next handoff or blocker: researcher must write the full planning package and either prove `special.zig` is the first cut or prove one ranked fallback seam must preempt it.

2. Role: researcher
- Session id: `research-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: re-prove whether `howl-render/src/text/raster/special.zig` is the first true owner-compression cut and seed exact execution slices.
- How one task went: current source and Alacritty/TigerBeetle pressure agree that the raster concentration is local to `special.zig`; the first cut is undercurl extraction, then generated-special family owners by coarse raster domains, not host/display fallbacks and not helper shattering.
- Next handoff or blocker: reviewer should gate the slice package; no reference override is needed from current evidence.

3. Role: reviewer
- Session id: `review-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: review the raster special ownership compression planning package for source-backed ranking, exact slice contracts, and accountability.
- How one task went: the package correctly identifies `special.zig` as the current raster concentration, but I rejected it because fallback ranking evidence is incomplete and slices 4-5 still leave owner/helper decisions and proof obligations to the coder.
- Next handoff or blocker: researcher must tighten fallback citations, replace invented coarse owners with source-backed boundaries, and rewrite per-slice files/tests/stop conditions so execution requires zero coder invention.

4. Role: researcher
- Session id: `research-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: repair the raster special planning artifact against the reviewer rejection.
- How one task went: added exact fallback seam citations for all ranked alternatives, replaced the invented slice-4 and slice-5 owners with current-source boundaries (`rasterizeGeneratedBlockAlpha` for block+braille and `.legacy_computing` for sextant-through-branch), and rewrote every slice with exact proof roots plus explicit include/exclude status for `symbol_map.zig`, `resolver.zig`, `lane.zig`, `grouping.zig`, `glyph_raster.zig`, and `scene.zig`.
- Next handoff or blocker: reviewer should re-gate the repaired package; from current evidence the plan is `accept-ready`.

5. Role: reviewer
- Session id: `review-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: re-gate the repaired raster special ownership compression planning package.
- How one task went: fallback ranking and slice 4-5 naming are improved, but slice 5 still retains legacy-only helpers inside `generated_special.zig`, and the research artifact still misses required workflow/output-contract elements.
- Next handoff or blocker: researcher must repair slice 5 retained-owner rules and bring the artifact into exact `loop/researcher.md` contract compliance before re-review.

6. Role: researcher
- Session id: `research-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: final repair pass on the raster special planning artifact against the remaining reviewer findings.
- How one task went: fixed the sources-read contract to match `loop/researcher.md`, added accountable compact-anchor-map/sprint-scratchpad/required-tests sections, and tightened slice 5 so legacy-only helpers at `special.zig:317-318,784-866` move into `special_legacy_computing.zig` while `generated_special.zig` retains only truly shared helpers.
- Next handoff or blocker: reviewer should re-gate the repaired package; from current source and reference evidence it is `accept-ready`.

7. Role: reviewer
- Session id: `review-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: final re-gate the repaired raster special ownership compression planning artifact after the read-order fix.
- How one task went: accepted; the repaired research artifact now follows `reference-index.md` reference order, includes the full `loop/researcher.md` output contract, and resolves the prior slice-5 retained-helper ambiguity.
- Next handoff or blocker: orchestrator may treat the planning package as accepted and proceed with the loop workflow; no blocker from review.

8. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: close the accepted planning receipt and seed Slice 1 for execution.
- How one task went: committed the accepted planning package as root `0e53a8e` and condensed Slice 1 into this live-loop execution contract with coder session id `coder-2026-06-13-raster-special-01`.
- Next handoff or blocker: coder should implement Slice 1 only and return verification plus commit-hash handoff status for reviewer gating.

9. Role: coder
- Session id: `coder-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: implement Slice 1 only inside the allowed raster files.
- How one task went: created `undercurl.zig` as the undercurl owner, moved `requestForUndercurl`, `rasterizeUndercurlAlpha`, and `addAlpha` into it, kept `special.zig` as the curated re-export root, and passed package-local `zig build test:unit` with the undercurl proof roots still covered.
- Next handoff or blocker: reviewer should gate the Slice 1 diff; no commit hash exists yet because git was intentionally untouched.

10. Role: reviewer
- Session id: `review-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: gate the executed Slice 1 diff for undercurl owner extraction against the accepted execution contract.
- How one task went: reviewed the live contract, accepted planning artifact, active scratchpad receipts, and the current `special.zig` and `undercurl.zig` source; the slice keeps `special.zig` as the curated re-export root, moves only the undercurl owner surface into `undercurl.zig`, preserves required assertions, leaves `rasterizer.zig:133-136` unchanged, and the required proof roots remain intact.
- Next handoff or blocker: verdict is `accept`; orchestrator may verify receipts and commit Slice 1 for commit-hash closure.

11. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: commit the accepted Slice 1 render work and close the root receipt.
- How one task went: committed the nested `howl-render` Slice 1 diff as `42b68b8` `Extract undercurl owner` after reviewer acceptance.
- Next handoff or blocker: commit the root receipt for Slice 1, then seed Slice 2 if continuing.

12. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: close the Slice 1 root receipt and seed Slice 2 for execution.
- How one task went: recorded Slice 1 root receipt `a8c3e50` and condensed Slice 2 into this live-loop execution contract with coder session id `coder-2026-06-13-raster-special-02`.
- Next handoff or blocker: coder should implement Slice 2 only and return verification plus commit-hash handoff status for reviewer gating.

13. Role: coder
- Session id: `coder-2026-06-13-raster-special-02`
- Hello: hi
- Intended task: implement Slice 2 only inside the allowed raster files.
- How one task went: reduced `special.zig` to the curated public root, moved generated-special public entrypoints and family dispatch into `generated_special.zig`, extracted the full box owner surface into `special_box.zig`, and passed `cd howl-render && zig build test:unit`.
- Next handoff or blocker: reviewer should gate the Slice 2 diff; no commit hash exists yet because git was intentionally untouched.

14. Role: reviewer
- Session id: `review-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: gate the executed Slice 2 diff for generated-special root extraction and box owner extraction against the accepted execution contract.
- How one task went: reviewed the live contract, accepted planning artifact, proof seams, and the current `special.zig`, `generated_special.zig`, and `special_box.zig` sources; the slice keeps `special.zig` as the curated root, moves generated-special entrypoints and dispatch into `generated_special.zig`, moves the full box owner surface into `special_box.zig`, preserves provider and route behavior, and passes `zig fmt --check` plus `cd howl-render && zig build test:unit`.
- Next handoff or blocker: verdict is `accept`; orchestrator may verify receipts and commit Slice 2 for commit-hash closure.

15. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: commit the accepted Slice 2 render work and close the root receipt.
- How one task went: committed the nested `howl-render` Slice 2 diff as `a542d8a` `Extract special box owner` after reviewer acceptance.
- Next handoff or blocker: commit the root receipt for Slice 2, then seed Slice 3 if continuing.

16. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: close the Slice 2 root receipt and seed Slice 3 for execution.
- How one task went: recorded Slice 2 root receipt `c52f69d` and condensed Slice 3 into this live-loop execution contract with coder session id `coder-2026-06-13-raster-special-03`.
- Next handoff or blocker: coder should implement Slice 3 only and return verification plus commit-hash handoff status for reviewer gating.

17. Role: coder
- Session id: `coder-2026-06-13-raster-special-03`
- Hello: hi
- Intended task: implement Slice 3 only inside the allowed raster files.
- How one task went: extracted the full powerline raster owner surface into `special_powerline.zig`, rewired `generated_special.zig` to dispatch powerline families there, kept the shared helper surface in `generated_special.zig`, and passed `zig fmt` plus `cd howl-render && zig build test:unit`.
- Next handoff or blocker: reviewer should gate the Slice 3 diff; no commit hash exists yet because git was intentionally untouched.

18. Role: reviewer
- Session id: `review-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: gate the executed Slice 3 diff for powerline owner extraction against the accepted execution contract.
- How one task went: reviewed the live contract, accepted plan, coder receipt, and the current `generated_special.zig` and `special_powerline.zig` sources; the powerline owner move is mostly in place, but `generated_special.zig` does not retain the contract-required shared helper `pixelCount`.
- Next handoff or blocker: verdict is `reject`; coder must restore the exact retained helper surface in `generated_special.zig` and return the slice for re-review before orchestrator can commit.

19. Role: coder
- Session id: `coder-2026-06-13-raster-special-03`
- Hello: hi
- Intended task: repair Slice 3 by restoring the missing shared helper `pixelCount` in `generated_special.zig`.
- How one task went: added back `pixelCount(width: u16, height: u16) u32` to `generated_special.zig` so the retained helper surface now matches the accepted Slice 3 contract exactly, and passed `zig fmt --check` plus `zig build test:unit` again.
- Next handoff or blocker: reviewer should re-gate the repaired Slice 3 diff; no commit hash exists yet because git was intentionally untouched.

20. Role: reviewer
- Session id: `review-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: re-gate the repaired Slice 3 powerline owner extraction after the `pixelCount` repair.
- How one task went: accepted; `generated_special.zig` restores contract-required `pixelCount`, the retained shared helper surface and extracted `special_powerline.zig` owner match the Slice 3 contract, and verification passed with `zig fmt --check` plus `cd howl-render && zig build test:unit`.
- Next handoff or blocker: orchestrator may commit Slice 3; no blocker from review.

21. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: commit the accepted Slice 3 render work and close the root receipt.
- How one task went: committed the nested `howl-render` Slice 3 diff as `24286d6` `Extract special powerline owner` after reviewer acceptance.
- Next handoff or blocker: commit the root receipt for Slice 3, then seed Slice 4 if continuing.

22. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: close the Slice 3 root receipt and seed Slice 4 for execution.
- How one task went: recorded Slice 3 root receipt `0a73776` and condensed Slice 4 into this live-loop execution contract with coder session id `coder-2026-06-13-raster-special-04`.
- Next handoff or blocker: coder should implement Slice 4 only and return verification plus commit-hash handoff status for reviewer gating.

23. Role: coder
- Session id: `coder-2026-06-13-raster-special-04`
- Hello: hi
- Intended task: implement Slice 4 only inside the allowed raster files.
- How one task went: moved the block-plus-braille dispatch owner into `special_block_braille.zig`, retained the contract-required shared helper surface in `generated_special.zig`, and passed `zig fmt` plus `cd howl-render && zig build test:unit`.
- Next handoff or blocker: reviewer should gate the Slice 4 diff; no commit hash exists yet because git was intentionally untouched.

24. Role: reviewer
- Session id: `review-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: gate the executed Slice 4 diff for block-plus-braille owner extraction against the accepted execution contract.
- How one task went: accepted; `special_block_braille.zig` now owns the exact block-plus-braille dispatch surface, `generated_special.zig` retains the contract-required shared helper surface, and the recorded verification receipt shows `zig fmt` plus `cd howl-render && zig build test:unit` passed.
- Next handoff or blocker: orchestrator may verify receipts and commit Slice 4 for commit-hash closure.

25. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: commit the accepted Slice 4 render work and close the root receipt.
- How one task went: committed the nested `howl-render` Slice 4 diff as `1986d89` `Extract special block braille owner` after reviewer acceptance.
- Next handoff or blocker: commit the root receipt for Slice 4, then seed Slice 5 if continuing.

26. Role: orchestrator
- Session id: `orch-2026-06-13-raster-special-01`
- Hello: hi
- Intended task: close the Slice 4 root receipt and seed Slice 5 for execution.
- How one task went: recorded Slice 4 root receipt pending in this active state and condensed Slice 5 into this live-loop execution contract with coder session id `coder-2026-06-13-raster-special-05`.
- Next handoff or blocker: coder should implement Slice 5 only and return verification plus commit-hash handoff status for reviewer gating.
