# Six-Target Pragmatic Shape Live Loop

Authority:

- Live planning scratchpad for the six-target pragmatic low-level code-shape sprint.
- Orchestrator session id: `orch-2026-06-14-six-target-pragmatic-shape-01`.
- User handoff supplied 2026-06-14.
- No implementation is authorized until research planning is accepted by reviewer and exact slices are seeded.

Problem statement:

- Close the remaining gap between Howl and Alacritty on pragmatic low-level code shape.
- This sprint is not about size reduction by itself.
- Remove the remaining forms of ugliness:
  - support buckets
  - ownership-probing generics
  - excess local protocol/result nouns
  - transitional names
  - narrated runtime code where direct owner code would be clearer
- The target is to make the code read more like a settled runtime and less like a system still explaining its own structure.

Target set:

1. `projects/howl/howl-linux-host/src/terminal/surface.zig`
2. `projects/howl/howl-render/src/text/ft_hb/support.zig`
3. `projects/howl/howl-render/src/text/shape/cluster.zig`
4. `projects/howl/howl-render/src/surface/emitter.zig`
5. `projects/howl/howl-render/src/text/raster/special_legacy_computing.zig`
6. `projects/howl/howl-vt/src/parser.zig`

Research contract:

- Researcher session id: `research-2026-06-14-six-target-pragmatic-shape-01`.
- Reviewer session id: `review-2026-06-14-six-target-pragmatic-shape-01`.
- Active research output path: `research/2026-06-14-six-target-pragmatic-shape-plan.md`.
- For each target, research must answer:
  1. Is this file large because it is the true owner?
  2. Which local concepts are real domain concepts, and which are scaffolding?
  3. Which named result/request/assembly/helper types can be deleted or inlined without making control flow worse?
  4. Which `anytype` helpers are hiding unsettled ownership?
  5. Which names still sound transitional or over-explanatory?
  6. If the file stays large, how do we make it read like one thing faster?
- Required output per target:
  - exact file and line references
  - exact reference comparison
  - keep-large or cut verdict
  - if cut: exact child owner and exact symbols to move/delete/inline/rename
  - if keep-large: exact ugly/fake concepts to remove without splitting the owner
  - required assertions
  - required tests
  - non-goals
  - stop conditions

Reference order and pressure:

- Use Alacritty first for host runtime owner shape, render/text owner directness, pragmatic naming, and tolerated concept surface.
- Use TigerBeetle for anti-bucket pressure, directness, rejection of ambient genericity, and compression after understanding.
- Do not use current Howl shape as authority.

Initial ranking bias only, not authorization:

1. `howl-render/src/text/ft_hb/support.zig`
2. `howl-linux-host/src/terminal/surface.zig`
3. `howl-render/src/text/shape/cluster.zig`
4. `howl-render/src/surface/emitter.zig`
5. `howl-render/src/text/raster/special_legacy_computing.zig`
6. `howl-vt/src/parser.zig`

Hard non-goals:

- No split-because-big reasoning.
- No generic helper buckets.
- No new `types.zig`, `helpers.zig`, `manager`, `engine`, or `controller`.
- No benchmark theater.
- No ABI churn unless explicitly forced.
- No parser fragmentation unless reference-backed and unavoidable.

Hard stop conditions:

- Stop if a file is ugly only by metric but owner-true in source.
- Stop if a proposed split just moves narration sideways.
- Stop if a proposed helper uses `anytype` to dodge ownership clarity.
- Stop if a rename is prettier but less direct.
- Stop if parser work threatens the control spine.
- Stop if research cannot prove a better reference-backed shape.

Orchestrator note:

- Hello from `orch-2026-06-14-six-target-pragmatic-shape-01`.
- Intent: seed a planning-only sprint around the six remaining pragmatic low-level shape pressure points.
- Outcome: live loop now captures the target set, ugliness criteria, research questions, output contract, ranking bias, and hard non-goals/stops.
- Handoff: researcher must read required workflow docs, `sprints/current.txt`, this loop, `reference-index.md`, TigerBeetle readings, the six target files, and the governing reference seams before writing the active research package.

- Researcher `research-2026-06-14-six-target-pragmatic-shape-01`: hello.
- Intent: evaluate the six remaining pragmatic-shape targets against Alacritty directness and TigerBeetle anti-bucket pressure, then write a fully sliceable research package.
- Outcome: completed the planning package with keep-large or cut judgments for all six targets, a full six-slice execution queue, and an initial Slice 1 focused on `howl-render/src/text/ft_hb/support.zig` directness cleanup.
- Handoff: reviewer should pressure exactness, keep-large proof quality, and whether any slice still leaves coder guesswork.

- Researcher `research-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: repair the rejected planning package by adding the missing researcher sections, full slice contracts, stronger emitter proof, and explicit keep-large cleanup answers.
- Outcome: repaired the package with explicit current/reference facts, owner roles, sprint scratchpad, six full slice contracts, and stronger emitter/no-op proof.
- Handoff: reviewer should re-check slice exactness and keep-large proof quality, especially for later slices.

- Researcher `research-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: repair the remaining exactness defects in later slices.
- Outcome: Slice 4 now names exact files and exact raster proof tests, Slice 5 is an exact verification-only or micro-cleanup path, and the package now uses exact test names consistently across slices.
- Handoff: reviewer should perform final exactness re-check. No additional planning content is expected beyond acceptance or a new defect.

- Reviewer `review-2026-06-14-six-target-pragmatic-shape-01`: hello.
- Intent: perform final planning review after the last exactness repair.
- Outcome: accepted with no findings. The package is now execution-ready, slices 1-6 meet the exactness bar, and no further planning changes are required before seeding execution.
- Handoff: orchestrator may record accepted planning receipt and promote the package into the active sprint contract.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: promote accepted planning into the active execution sprint contract without starting coder work yet.
- Outcome: accepted planning receipt `9f20f26` is recorded; `research/2026-06-14-six-target-pragmatic-shape-plan.md` moved to `research/done/2026-06-14-six-target-pragmatic-shape-plan.md`; active sprint contract is `sprints/2026-06-14-six-target-pragmatic-shape-sprint.md`.
- Handoff: sprint seed receipt is `1f2c1cc` `Seed six-target pragmatic-shape sprint`. Slice 1 is next to seed for coder `coder-2026-06-14-six-target-pragmatic-shape-slice-01` when execution is authorized.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: seed Slice 1 exactly from the accepted sprint contract.
- Outcome: Slice 1 is authorized for coder `coder-2026-06-14-six-target-pragmatic-shape-slice-01` only.
- Allowed files: `howl-render/src/text/ft_hb/support.zig`, `howl-render/src/render_session.zig`, `howl-render/src/text/ft_hb/support_test.zig`.
- Required shape: keep `FtHbSupport` as the sole state owner in `support.zig`; move context extraction to the render-session boundary; delete ownership-probing helpers `textState`, `configView`, `lockFt`, and `unlockFt`; change support internals to take explicit state/config inputs instead of `anytype`; do not add a vague replacement bucket; keep external behavior and fallback order unchanged.
- Required tests from `howl-render`: `zig build test:unit`.
- Required exact proof names in `howl-render/src/text/ft_hb/support_test.zig`: `provider loads fallback face for symbol glyph with primary present`, `ft hb state configures explicit retained cache and input capacities`, `shape run input assembly reuses retained bounded buffers`.
- Stop conditions: stop if generic removal requires inventing a weaker ownership bucket; stop if the change spreads into non-target render files outside the allowed list; stop if fallback behavior changes.
- Required handoff fields: changed files, concise implementation summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.

- Researcher note from `research-2026-06-14-six-target-pragmatic-shape-01`.
- Hello. Intent: prove keep-large vs cut for all six targets against Alacritty, Ghostty, and TigerBeetle pressure, then seed an exact first execution slice.
- Outcome: all six targets are now judged in `research/2026-06-14-six-target-pragmatic-shape-plan.md`; five stay large with specific ugliness removals, and `support.zig` is ranked first because its ownership-probing `anytype` seam is the clearest directness violation with existing proof.
- Handoff: reviewer should pressure the per-target stop conditions, especially the no-split verdicts for `surface.zig`, `emitter.zig`, and `parser.zig`, and the new-test requirement for `special_legacy_computing.zig`.

- Researcher repair note from `research-2026-06-14-six-target-pragmatic-shape-01`.
- Hello. Intent: repair the rejected planning package by adding the missing research sections, full slice contracts for all six slices, and tighter keep-large proof for `emitter.zig` plus naming/readability answers for every target.
- Outcome: `research/2026-06-14-six-target-pragmatic-shape-plan.md` now includes explicit current-code facts, reference facts, owner roles and proposed shape, a sprint scratchpad, full execution contracts for Slices 1-6, tighter `emitter.zig` proof with current tests, and explicit transitional-name/readability answers for every keep-large target.
- Handoff: reviewer should now check completion of the six-slice execution package and decide whether the `special_legacy_computing.zig` test-root wording is acceptable as the single remaining proof-risk note.

- Researcher repair note from `research-2026-06-14-six-target-pragmatic-shape-01`.
- Hello. Intent: close the remaining exactness defects by fixing Slice 4 allowed/test paths, tightening Slice 5 to exact no-op verification tests, and rechecking Slice 2, Slice 3, and Slice 6 test contracts to the same bar.
- Outcome: the active research plan now names exact file paths for Slice 4, exact preserved/added raster test names, exact emitter no-op verification tests for Slice 5, and exact test-name lists for the remaining slices that were previously broader.
- Handoff: reviewer should re-check only the execution-contract exactness bar and confirm that Slice 4's added test name is acceptable as the explicit proof obligation for the degenerate-size gap.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-01`: hello.
- Intent: remove the ownership-probing support helpers, move session context extraction to `render_session.zig`, and keep `FtHbSupport` as the direct state owner without changing fallback behavior.
- Outcome: `support.zig` now routes real work through explicit `FtHbSupport` plus `TextSessionConfig` functions, `render_session.zig` extracts that state/config at the boundary before calling into support, the probing helpers are deleted, and `zig build test:unit` passed in `howl-render`.
- Handoff: ready for reviewer inspection and orchestrator receipt closure once the slice diff is reviewed; no blocker from implementation.

- Reviewer `review-2026-06-14-six-target-pragmatic-shape-01`: hello.
- Intent: review Slice 1 support direct-owner cleanup against the accepted sprint contract.
- Outcome: rejected. `support.zig` still keeps ownership-probing `anytype` and context-extracting public wrappers, so render-session is not yet the sole explicit extraction boundary and the direct-owner cleanup is incomplete.
- Handoff: orchestrator must restart Slice 1 at correction/reseed stage and prove the exact additional caller scope needed to delete the remaining wrappers instead of preserving compatibility shims.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: restart Slice 1 from the earliest broken stage after reviewer rejection.
- Outcome: active correction research artifact is `research/2026-06-14-pragmatic-shape-slice1-correction.md`; Slice 1 remains unaccepted and the current render diff stays uncommitted.
- Handoff: researcher must inventory the remaining `support.zig` wrappers, identify exact caller files still depending on them, and reseed corrected file scope with exact tests/non-goals/stop conditions/receipt fields.

- Researcher `research-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: identify the exact remaining caller scope needed to finish Slice 1 without compatibility shims.
- Outcome: the only remaining caller outside current Slice 1 scope is `howl-render/src/text/ft_hb/glyph_raster.zig`; the correction keeps the slice in place and expands scope just enough to delete the remaining probing wrappers cleanly.
- Handoff: reviewer should verify the corrected Slice 1 contract and exact file expansion before reseed.

- Reviewer `review-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: review the focused Slice 1 correction package.
- Outcome: accepted with no findings. The correction inventories the remaining wrappers exactly, proves the minimal `glyph_raster.zig` expansion, and supplies a corrected execution contract with exact files, tests, non-goals, stop conditions, and receipt fields.
- Handoff: orchestrator may promote the correction into the active sprint contract and reseed Slice 1 repair.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: promote the accepted Slice 1 correction into the active sprint contract and reseed Slice 1 repair.
- Outcome: corrected Slice 1 scope now includes `howl-render/src/text/ft_hb/glyph_raster.zig`; correction research moved to `research/done/2026-06-14-pragmatic-shape-slice1-correction.md`.
- Handoff: coder `coder-2026-06-14-six-target-pragmatic-shape-slice-01` may repair the existing unaccepted Slice 1 diff only inside the corrected file scope and then return standard handoff fields.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-01`: hello again.
- Intent: repair Slice 1 by deleting the remaining probing wrappers and routing `glyph_raster.zig` through explicit support state/config seams.
- Outcome: `support.zig` now exposes only explicit support-state entry points, `glyph_raster.zig` no longer depends on support-side context recovery, `render_session.zig` remains the sole context-extraction boundary, and `zig build test:unit` passed in `howl-render`.
- Handoff: ready for reviewer inspection and orchestrator receipt closure once the repaired diff is reviewed.

- Reviewer `review-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: review the repaired Slice 1 direct-owner cleanup after scope correction.
- Outcome: accepted with no findings. The remaining public probing wrappers are gone, `glyph_raster.zig` uses explicit support state/config, render-session is the only extraction boundary, and the required proof tests remain present.
- Handoff: orchestrator should verify `zig build test:unit` in `howl-render`, then record nested and root Slice 1 acceptance receipts.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: verify and accept corrected Slice 1 after reviewer acceptance.
- Outcome: `zig build test:unit` passed from `howl-render`. Nested render commit is `34cad2c` `Remove support probing wrappers`.
- Handoff: root acceptance receipt is `378a186` `Accept six-target pragmatic-shape Slice 1`; Slice 2 is next.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: seed Slice 2 exactly from the accepted sprint contract.
- Outcome: Slice 2 is authorized for coder `coder-2026-06-14-six-target-pragmatic-shape-slice-02` only.
- Allowed files: `howl-linux-host/src/terminal/surface.zig`, `howl-linux-host/src/terminal/surface_test.zig`.
- Required shape: keep `Surface` as the one host-surface owner; remove internal `anytype`/`Ops` narration where the real owner is already `Surface` or `HowlTerm`; prefer direct owner code over `ContextDriveOps`, `ContextSubmitBackend`, and local generic present/clipboard helpers; inline or delete local request/result buckets that survive only as scaffolding after helper removal; preserve the centralized render/progress and submit/present spine.
- Required tests from `howl-linux-host`: `zig build test:unit`.
- Required exact proof names in `howl-linux-host/src/terminal/surface_test.zig`: `pending VT clipboard write follows OSC 52 policy`, `drive progress keeps per-terminal continuation admission until a later non-keep turn`, `inactive tab continuation re-enters from per-terminal continuation admission`, `present pending blocks submit path until host present ack`, `submit path runs once no host present is in flight`, `submit backend upload observes terminal mutex unlocked`, `render submit runs under terminal mutex after backend upload`, `host upload failure returns failed submit without render submit`, `prepared handle mutation after upload does not submit`, `resize success path submits full surface and acks matching present token`, `resize upload failure zeros host dimensions and retry submits same full frame`, `resize while present pending waits for matching ack before resized submit`, `complete present acks matching host-owned token once and clears`, `mismatched complete present does not ack or clear`.
- Stop conditions: stop if direct helper removal forces a weaker test seam than the current proof; stop if mutex or submit/present control flow becomes less centralized; stop if the change spreads outside the allowed files.
- Required handoff fields: changed files, concise implementation summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.

- Researcher `research-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: map the remaining public probing wrappers in `support.zig`, prove their exact caller graph, and decide whether Slice 1 needs a narrow reseed or a stop.
- Outcome: `research/2026-06-14-pragmatic-shape-slice1-correction.md` now shows that only `glyph_raster.zig` still depends on the remaining support wrappers; all other remaining wrapper exports are dead shim surface, so Slice 1 should be repaired in place with exact scope expansion to `howl-render/src/text/ft_hb/glyph_raster.zig` and its existing inline tests.
- Handoff: reviewer should check the exact wrapper inventory and the corrected Slice 1 allowed-file expansion; orchestrator can reseed if accepted.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-01`: hello again.
- Intent: finish the Slice 1 repair by deleting the remaining public support wrappers and routing glyph-raster callers through explicit support state/config seams.
- Outcome: `support.zig` now exposes only explicit `WithConfig` support entry points for this slice, `render_session.zig` remains the only session/context extraction boundary, `glyph_raster.zig` now calls support with explicit `FtHbSupport` plus `TextSessionConfig`, and `zig build test:unit` passed in `howl-render`.
- Handoff: ready for reviewer inspection; no blocker from implementation.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-02`: hello.
- Intent: remove `surface.zig` owner-probing `Ops` scaffolding while keeping `Surface` as the centralized submit/present owner and preserving the current proof seams.
- Outcome: `Surface` now drives progress, clipboard application, present completion, and submit directly in production code; remaining generic seams are confined to test-only helpers in `surface.zig`, and `surface_test.zig` was updated to call those explicit test helpers.
- Handoff: run `zig build test:unit` in `howl-linux-host`, then hand the diff to reviewer if the proof set stays green.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-02`: hello again.
- Intent: finish Slice 2 by removing the remaining duplicated runtime spine from the host tests.
- Outcome: drive-progress proofs now call `Surface.driveProgress` directly, submit-path proofs now call the real submit path through narrow owner-true hooks, and the duplicated helper/control-path reconstruction is gone from `surface_test.zig`.
- Handoff: ready for reviewer inspection and orchestrator receipt closure once the repaired host diff is reviewed.

- Reviewer `review-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: review the repaired Slice 2 host direct-owner cleanup after the proof-seam fixes.
- Outcome: accepted with no findings. Drive-progress proofs call `Surface.driveProgress` directly, submit-path proofs call the real submit path, and the remaining hooks in `surface.zig` are narrow owner-true setup/observation seams rather than alternate control paths.
- Handoff: orchestrator should verify `zig build test:unit` in `howl-linux-host`, then record nested and root Slice 2 acceptance receipts.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: verify and accept repaired Slice 2 after reviewer acceptance.
- Outcome: `zig build test:unit` passed from `howl-linux-host`. Nested host commit is `9f12d7e` `Simplify terminal surface owner`.
- Handoff: root acceptance receipt is `76f9bb0` `Accept six-target pragmatic-shape Slice 2`; Slice 3 is next.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: seed Slice 3 exactly from the accepted sprint contract.
- Outcome: Slice 3 is authorized for coder `coder-2026-06-14-six-target-pragmatic-shape-slice-03` only.
- Allowed files: `howl-render/src/text/shape/cluster.zig`.
- Required shape: keep `cluster.zig` as one cluster/text-cache assembly owner; delete `InputRenderableAssembly` and `CellLineTextCacheAssembly`; keep `RetainedScratch` as the explicit bounded retained owner; rewrite direct build paths through scratch/local slices without moving ownership into a new helper file.
- Required tests from `howl-render`: `zig build test:unit`.
- Required exact proof names in `howl-render/src/text/shape/cluster.zig`: `cell inputs build text cache renderable cells and clusters`, `cell inputs retain combining sequences in text cache`, `cell inputs preserve style and presentation into renderables and clusters`, `partial damage filters clean clusters before shaping`, `sparse cells keep only damaged base cells`, `sparse cells intern repeated codepoints`, `sparse cells keep empty background witnesses for scene ownership`, `rich cell text interning deduplicates codepoint sequences`, `rich cell text renderables resolve exact interned text ids`, `retained scratch bounds sparse cell assembly`, `retained scratch bounds rich input codepoint assembly`.
- Stop conditions: stop if a replacement helper just moves the same narration sideways; stop if `RetainedScratch` is targeted without new reference proof; stop if proof requires weakening current inline tests.
- Required handoff fields: changed files, concise implementation summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-02`: hello again.
- Intent: repair Slice 2 by deleting the remaining fake proof seams from `surface.zig` and keeping the production direct-owner cleanup intact.
- Outcome: the rejected `surface.zig` test seams are gone; `surface_test.zig` now carries only concrete local proof setup for the remaining host-surface behaviors without any `surface.zig` generic protocol layer.
- Handoff: rerun `zig build test:unit` in `howl-linux-host`, then return the repaired diff for reviewer inspection.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-02`: hello again.
- Intent: remove the last duplicated control-path helpers from `surface_test.zig` without disturbing the current production cleanup in `surface.zig`.
- Outcome: `driveProgressForTest` and `submitPreparedLockedForTest` are deleted; the affected proofs now perform direct local setup in each test and keep only narrow setup/observation helpers such as execution construction and present completion.
- Handoff: rerun `zig build test:unit` in `howl-linux-host`, then hand the repair back for reviewer inspection.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-02`: hello again.
- Intent: repair the remaining Slice 2 rejection by moving the drive-progress and submit-path proofs onto the real `Surface` owner code.
- Outcome: `surface.zig` now exposes only narrow test hooks for injected environment facts and submit/upload observation, while `surface_test.zig` exercises `Surface.driveProgress` and the real submit path directly instead of reconstructing the runtime spine.
- Handoff: `zig build test:unit` passed in `howl-linux-host`; ready for reviewer inspection.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-03`: hello.
- Intent: remove `InputRenderableAssembly` and `CellLineTextCacheAssembly` from `cluster.zig` while keeping the file as the direct cluster/text-cache assembly owner and preserving retained-scratch bounds/tests.
- Outcome: `cluster.zig` now builds cell text caches and renderable arrays directly through local allocation plus existing scratch slices, with both deleted assembly structs removed and no ownership moved into a new helper file.
- Handoff: run `zig build test:unit` in `howl-render`, then hand the slice to reviewer if the inline proof set stays green.

- Reviewer `review-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: review Slice 3 cluster assembly cleanup.
- Outcome: accepted with no findings. The bucket structs are deleted rather than moved sideways, `RetainedScratch` remains the explicit bounded owner, and required proof coverage remains intact inside `cluster.zig`.
- Handoff: orchestrator should verify `zig build test:unit` in `howl-render`, then record nested and root Slice 3 acceptance receipts.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: verify and accept Slice 3 after reviewer acceptance.
- Outcome: `zig build test:unit` passed from `howl-render`. Nested render commit is `06b16f1` `Simplify cluster assembly owner`.
- Handoff: root acceptance receipt is `d531a48` `Accept six-target pragmatic-shape Slice 3`; Slice 4 is next.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: seed Slice 4 exactly from the accepted sprint contract.
- Outcome: Slice 4 is authorized for coder `coder-2026-06-14-six-target-pragmatic-shape-slice-04` only.
- Allowed files: `howl-render/src/text/raster/special_legacy_computing.zig`, `howl-render/src/text/raster/special_test.zig`.
- Required shape: keep `special_legacy_computing.zig` as one special-raster owner; remove `SpriteShade` and `BranchNode` option buckets; replace them with exact draw verbs or exact argument lists; add explicit degenerate-size assertions where width/height math assumes positive sizes.
- Required tests from `howl-render`: `zig build test:unit`.
- Required exact proof names in `howl-render/src/text/raster/special_test.zig`: preserved `generated shade preserves fallback intensity levels`, preserved `generated branch nodes preserve filled and unfilled variants`, and added `generated legacy computing raster guards degenerate size bounds`.
- Stop conditions: stop if cleanup cannot be proved with bounded tests; stop if the replacement requires a new generic options bucket elsewhere; stop if the required test root cannot stay owner-true and single-purpose.
- Required handoff fields: changed files, concise implementation summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.

- Coder `coder-2026-06-14-six-target-pragmatic-shape-slice-04`: hello.
- Intent: remove the legacy computing shade and branch option buckets, replace them with direct draw arguments, and add bounded proof for degenerate size guards in the owner-true raster tests.
- Outcome: `special_legacy_computing.zig` now draws shade and branch variants through explicit arguments, positive-size assertions now guard the `width - 1` and `height - 1` geometry paths, and `special_test.zig` adds the required degenerate-size bounds proof.
- Handoff: run `zig build test:unit` in `howl-render`, then hand the slice to reviewer if the proof set stays green.

- Reviewer `review-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: review Slice 4 special legacy raster bucket cleanup.
- Outcome: accepted with no findings. The option buckets are gone, the degenerate-size proof is present, and the corrected loop chronology now keeps the Slice 4 pass at the live end.
- Handoff: orchestrator should verify `zig build test:unit` in `howl-render`, then record nested and root Slice 4 acceptance receipts.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: verify and accept Slice 4 after reviewer acceptance.
- Outcome: `zig build test:unit` passed from `howl-render`. Nested render commit is `788ca2e` `Simplify legacy computing raster owner`.
- Handoff: root acceptance receipt is `fdeb7f2` `Accept six-target pragmatic-shape Slice 4`; Slice 5 is next.

- Orchestrator `orch-2026-06-14-six-target-pragmatic-shape-01`: hello again.
- Intent: seed Slice 5 exactly from the accepted sprint contract.
- Outcome: Slice 5 is authorized for coder `coder-2026-06-14-six-target-pragmatic-shape-slice-05` only.
- Allowed files: `howl-render/src/surface/emitter.zig`, `howl-render/src/surface/emitter_test.zig`, `howl-render/src/surface/handle_test.zig`.
- Required shape: re-verify during execution that `emitter.zig` is still owner-true and direct; if no clearer reference-backed cleanup survives, record an explicit no-op acceptance for this slice; if a cleanup is still justified, limit it to collapsing the four identical fill-pass wrappers or removing tiny testing pass-through wrappers; keep `Emitter(comptime limits)` and all explicit bounded arrays/counters.
- Required tests from `howl-render`: `zig build test:unit`.
- Required exact proof names in `howl-render/src/surface/emitter_test.zig` and `howl-render/src/surface/handle_test.zig`: `render surface surface emitter coalesces adjacent prepared fill commands`, `render surface surface emitter does not coalesce distinct prepared fills`, `render surface surface emitter rejects command bound overflow`, `render surface surface emitter rejects upload bound overflow`, `render surface surface emitter rejects retire bound overflow`, `render surface surface emitter rejects upload byte total overflow`, `render surface surface emitter emits transient sprite beyond persistent budget`, `render surface surface emitter reports exact transient retire bound`, `prepared handle reports missing surface when render_surface emission overflows`.
- Stop conditions: stop and convert to explicit no-op if cleanup is metric-only; stop if a replacement abstraction is less direct than current explicit arrays/counters; stop if tests would need widening just to justify a cosmetic rewrite.
- Required handoff fields: changed files, concise implementation summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.
