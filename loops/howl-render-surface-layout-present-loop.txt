Howl Render Surface Layout Present Loop

Orchestrator session id: `orch-2026-06-19-render-surface-layout-present-01`.
Researcher session id: `research-2026-06-19-render-surface-layout-present-01`.
Reviewer session id: `review-2026-06-19-render-surface-layout-present-02`.

Problem statement:

- Howl currently uses overlapping render nouns: `surface`, `scene`, `geometry`, and `grid` across `howl-render`, `howl-linux-host`, and the render C ABI.
- Those nouns are not presumed correct. Existing APIs and cross-repo contracts are not frozen.
- The sprint must produce the reference-backed owner model first, then execute owner-true code/API slices without preserving stale names for compatibility comfort.
- Linux Wayland is the first host scope, with KDE Wayland-only and Hyprland as the practical target environments. X11 is not an implementation target for this sprint.
- SDL remains the pragmatic window/input/text-input/clipboard/event harness unless research proves it blocks the required GPU-present control.
- SDL's normal `SDL_GL_SwapWindow` hides the EGL swap-damage hook; SDL still exposes EGL display, EGL window surface, and EGL proc lookup APIs that may allow a small direct damaged-present path.

Loop phase:

- Active execution loop for Slice 4 SDL EGL damaged-present feasibility proof after completed render surface/layout/present ownership and damage slices.
- Active research plan is `research/howl-render-surface-layout-present.md`.
- Planning is accepted through Slice 4 proof. Slice 1, Slice 2, and Slice 3 are committed with root receipts. Slice 4 code proof is promoted; Slice 5 production damaged present is not promoted until Slice 4 is accepted.

Non-negotiables:

- Alacritty governs host display, damage, renderer organization, and Wayland EGL present-damage shape unless the user explicitly overrides a specific case.
- Ghostty contributes VT and embedding seam pressure, not broad renderer/display architecture when Alacritty has a direct shape.
- Kitty contributes GLFW/Wayland/EGL and UX pressure where Alacritty or SDL facts leave a gap, but Kitty does not override Alacritty for the display/render owner split.
- TigerBeetle style is a hard gate for exact nouns, owner truth, assertions, bounded arrays/queues, and proof.
- API and ABI names are in scope; no cross-repo API is frozen.
- Howl must not add an umbrella runtime layer, generic backend manager, or bucket owner to hide the overlap.
- Do not implement X11 fallback during this sprint.

Initial reference facts already established in this session:

- Alacritty tracks terminal `LineDamageBounds` and `TermDamage`, imports that into display `DamageTracker`, shapes damaged lines into pixel `Rect`s, overdamages for nearby/wide cells, merges overlapping rects, draws normally, and on Wayland EGL calls `swap_buffers_with_damage` at present time.
- Alacritty's partial GPU update is a present-damage hint, not a retained partial render command stream.
- SDL3 docs expose `SDL_EGL_GetCurrentDisplay`, `SDL_EGL_GetWindowSurface`, and `SDL_EGL_GetProcAddress`.
- SDL3 Wayland `SDL_GL_SwapWindow` calls plain `eglSwapBuffers` in `src/video/wayland/SDL_waylandopengles.c`; it has no public damage-rect parameter.
- Early `howl-linux-host` had a `-Dwindow-backend=sdl|glfw` compile-time backend seam; Kitty vendors a modified GLFW, but its normal GLFW EGL swap path also uses plain `eglSwapBuffers`.

Loop notes:

- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: start the render surface/layout/present sprint after user clarified Wayland scope, SDL retention, damaged-present priority, and unfrozen API/cross-repo authority. Outcome: opened a new active loop, archived the completed event-semantics loop, and seeded the non-negotiables and already established reference facts. Handoff: researcher must write the full source-backed plan in `research/howl-render-surface-layout-present.md`, covering all `surface`/`scene`/`geometry`/`grid` owners and the SDL/EGL damaged-present path before any coder slice is promoted.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: seed the first research scan without turning it into implementation authority. Outcome: scanned current `howl-render`, current `howl-linux-host`, Alacritty, SDL docs/source, and Kitty/GLFW for the suspicious owner nouns and damaged-present anchors; recorded only compact seed facts in the research artifact. Handoff: researcher still must complete the full inventory, line-cited reference map, and ordered slice plan; no coding is authorized.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: incorporate the user's TigerBeetle-agent style scan as sprint bias without changing the accepted scope. Outcome: recorded that the next maturity pressure is generic-ish debt, especially `howl-render/src/grid/rects.zig`, `howl-render/src/text/direct_normal.zig`, and `howl-render/src/scene.zig`; this reinforces the current render surface/layout/scene/grid audit instead of creating a separate hygiene sprint. Handoff: researcher must account for `usize`/`anytype` leakage when classifying render/grid/scene owners and must not equate file size with debt.
- Researcher `research-2026-06-19-render-surface-layout-present-01`: hello. Intent: produce the full source-backed planning package without implementing product code. Outcome: read the required live artifacts and reference stack, inventoried current render/display owners, expanded Alacritty/SDL/Kitty/TigerBeetle line-cited anchors, classified `surface`/`scene`/`geometry`/`grid`, and wrote the ordered sprint slice plan in `research/howl-render-surface-layout-present.md`. Handoff: reviewer should review the package for source support, owner truth, slice completeness, ABI rename readiness, SDL/EGL damaged-present risk handling, and generic-ish debt treatment before any coder slice is promoted.
- Reviewer `review-2026-06-19-render-surface-layout-present-01`: hello. Intent: review the planning package against the active sprint, loop contract, current source, and reference pressure without implementing product code. Outcome: rejected the package because the ABI packet name remains an unresolved blocker, several slices do not provide exact executable allowed files/names/tests, and SDL/EGL damaged-present risk is left to execution-time proof instead of being settled enough for a coder contract. Handoff: orchestrator should send the package back for research correction before any coding slice is promoted.
- Researcher `research-2026-06-19-render-surface-layout-present-01`: hello. Intent: correct the rejected planning package without implementing product code. Outcome: reread the active sprint, research package, loop rejection at line 46, current render ABI/header/export paths, VT dirty-column storage/ABI gap, linux-host translate-C binding path, and SDL swap seam; updated `research/howl-render-surface-layout-present.md` with reviewer correction notes, fixed ABI names (`HowlRenderSurfaceFrame`, `HowlRenderHostTexture`, `HowlRenderCellGrid`), exact Slice 1 owner rename outcomes, exact allowed files/tests/non-goals/stop conditions/receipt rules, VT dirty-column ABI work in Slice 3, and proof-first SDL/EGL Slice 4 before implementation Slice 5. Handoff: reviewer should re-review the corrected package; no coder slice is authorized until reviewer acceptance and orchestrator promotion.
- Reviewer `review-2026-06-19-render-surface-layout-present-02`: hello. Intent: re-review the corrected planning package against the active sprint, workflow gate, TigerBeetle owner/naming law, and prior rejection. Outcome: accepted the corrected package because it fixes the unresolved ABI packet name as `HowlRenderSurfaceFrame`, fixes host resource naming as `HowlRenderHostTexture`, assigns `HowlRenderCellGrid`, gives exact ordered slice contracts with tests/non-goals/stop conditions, moves VT dirty-column exposure into Slice 3, and makes SDL/EGL damaged present proof-only before any implementation. Handoff: orchestrator may promote Slice 1, but planning archival and final acceptance remain blocked until the root commit receipt is closed.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: promote the first executable slice after reviewer acceptance without widening scope. Outcome: updated `sprints/current.txt` to authorize only Slice 1 render layout/prepared draw owner renames and recorded the open planning commit receipt. Handoff: coder may execute only the promoted Slice 1 contract; do not start ABI, VT dirty-column, or SDL/EGL work in this slice.
- Coder `coder-2026-06-19-render-surface-layout-present-01`: hello. Intent: execute only Slice 1 render layout and prepared draw owner renames in `howl-render`. Outcome: moved `geometry.zig` to `layout.zig`, moved prepared text draw owners from `scene.zig` and `grid/{scene,damage,rects}.zig` into `text/{draw_list,draw_primitives,damage,rect_primitives}.zig`, renamed the required layout/draw owner symbols, updated direct imports/usages caused by the moves, and preserved behavior. Verification passed in `howl-render`: `zig build test:unit`, `zig build check`, `zig build test:abi`, `zig build benchmark:render:build`, plus grep gates for stale moved imports and old public names. Handoff: reviewer should review the uncommitted child diff; child/root commit receipts remain open until the user explicitly requests commits.
- Reviewer `review-2026-06-19-render-surface-layout-present-03`: hello. Intent: review the final Slice 1 implementation after stale draw-list/layout aliases were corrected. Outcome: accepted with no findings; verified the uncommitted handoff, required owner moves/names, no C ABI/header rename, clean stale-name grep gates, and passing render verification commands. Handoff: orchestrator may accept Slice 1 only as an uncommitted reviewed handoff; child/root commit receipts remain open until the user explicitly requests commits.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: close the child repo receipt after the user explicitly requested commits before continuing. Outcome: committed the accepted `howl-render` Slice 1 diff as `5383bf6 Rename render layout and draw owners`. Handoff: root accountability commit remains open until the root artifact updates and child pointer are committed.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: close the Slice 1 root receipt and promote the next accepted slice. Outcome: root committed Slice 1 accountability and child pointer as `71b72c7 Track render layout draw owner rename`; updated `sprints/current.txt` to authorize only Slice 2 C ABI packet and host texture renames across `howl-render` and `howl-linux-host`. Handoff: coder may execute only Slice 2; no EGL damaged-present work is authorized.
- Coder `coder-2026-06-19-render-surface-layout-present-02`: hello. Intent: execute only Slice 2 C ABI packet and host texture renames. Outcome: renamed the public render C ABI packet/resource vocabulary to `HowlRenderSurfaceFrame`, `HowlRenderHostTexture`, `HowlRenderCellGrid`, frame damage/command/token/span names, frame constants, and frame member names; updated `howl-render` producers/validators/tests and `howl-linux-host` translate-C consumers, GL texture upload owners, retained submit path, and tests; no compatibility typedefs or aliases were added. Verification passed in `howl-render`: `zig build test:unit`, `zig build check`, `zig build test:abi`, `zig build benchmark:render:build`; verification passed in `howl-linux-host`: `zig build test:unit`, `zig build check`; stale C ABI and host-surface vocabulary grep gates were clean. Handoff: reviewer should inspect the uncommitted child diffs before orchestrator acceptance.
- Reviewer `review-2026-06-19-render-surface-layout-present-04`: hello. Intent: review Slice 2 implementation against ABI naming, host texture ownership, bounds, and accidental out-of-scope edits. Outcome: rejected the first pass for stale `host_surface`/`host_surface_id` vocabulary in tracked dormant render handle files; after correction, old ABI and host-surface vocabulary grep gates were clean and verification was rerun. Handoff: orchestrator may accept and commit the corrected Slice 2 child repos.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: close Slice 2 child receipts and root accountability. Outcome: committed `howl-render` as `6c08bf8 Rename render surface frame ABI` and `howl-linux-host` as `968d496 Rename host render texture ABI`; updated `sprints/current.txt` with Slice 2 receipts and verification. Handoff: root pointer/accountability commit is open; Slice 3 is not promoted until the next execution update.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: promote Slice 3 after root Slice 2 receipt `326cf52` closed. Outcome: updated `sprints/current.txt` to authorize only VT dirty column ABI exposure and render packet damage rect shaping across `howl-vt` and `howl-render`. Handoff: coder may execute only Slice 3; no host EGL damaged-present work is authorized.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: correct Slice 3 allowed files after inspecting current VT owner paths. Outcome: added `howl-vt/src/render_state.zig` because `screen_set.copyDirtyRows` already copies dirty column bounds, but `RenderState.Row` discards them before FFI row-data access. Handoff: worker may add row-local dirty column fields and assignments only; broader VT ownership/lifecycle changes remain a stop condition.
- Coder `coder-2026-06-19-render-surface-layout-present-03`: hello. Intent: execute Slice 3 dirty-column ABI and render damage packet shaping. Outcome: extended VT row-data ABI with dirty column start/end fields, retained copied dirty bounds on `RenderState.Row`, fixed hover-highlight dirty bound union and clear-to-no-link/out-of-range global dirty truth, added ABI tests for single-cell, full-screen, scroll-exposed, hover, moved-hover, and cleared-hover dirty bounds, added render prepared-surface damage rect shaping, damage rect validation, and full-damage fallback for unsafe public C text prepare, command-bearing partial frames, zero-area partial spans, and damage bound overflow. Verification passed in `howl-vt`: `zig build test:abi`, `zig build test:unit`, `zig build check`; verification passed in `howl-render`: `zig build test:unit`, `zig build check`, `zig build test:abi`, `zig build benchmark:render:build`. Handoff: reviewers identified and fixes closed missing retained-base safety, damage-overflow fallback, hover dirty union, hover clear global dirty, and command-bounded partial damage risks.
- Reviewer `review-2026-06-19-render-surface-layout-present-07`: hello. Intent: final review after retained-base, overflow, hover, and command-bounds corrections. Outcome: no remaining blocker after the final out-of-range hover clear fix was applied and verification rerun. Handoff: orchestrator may commit child repos and close the root receipt.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: close Slice 3 child receipts and root accountability. Outcome: committed `howl-vt` as `25c42c5 Expose VT dirty column bounds` and `howl-render` as `0acbf36 Shape render frame damage rects`; updated `sprints/current.txt` with Slice 3 receipts, verification, and the safety note that public C text prepare emits full damage until a submitted-base ABI exists. Handoff: root pointer/accountability commit is open; Slice 4 is not promoted until the next execution update.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: continue after Slice 3 root receipt and harden Slice 4 planning per user direction. Outcome: confirmed SDL Wayland `Wayland_GLES_SwapWindow` owns hidden-window skip, frame-callback wait, plain `eglSwapBuffers`, and display flush; confirmed SDL docs expose main-thread EGL display/window-surface/proc lookup but `SDL_GL_SwapWindow` has no damage parameter; confirmed Alacritty shapes damage in display state and calls Wayland EGL `swap_buffers_with_damage`; confirmed Kitty/GLFW is fallback pressure only and normally swaps with plain `eglSwapBuffers`. Updated `research/howl-render-surface-layout-present.md` and `sprints/current.txt` so Slice 4 must choose among explicit options with tradeoffs instead of stopping at a vague blocked state. Handoff: reviewer should review the planning-only root diff before any Slice 4 code proof is promoted.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: sharpen Slice 4 after user clarified that vendor limitations must not shape Howl internals. Outcome: incorporated official SDL/Khronos doc pressure: SDL exposes EGL display/window-surface/proc lookup, Khronos defines damaged swap as an alternative to `eglSwapBuffers` with bottom-left damage rectangles and full-surface behavior for `n_rects == 0`. Updated the plan so Slice 4 must try the SDL-retained damaged-present proof honestly, keep plain SDL swap only as a capability fallback, and escalate to Howl-owned Wayland/EGL present or a more drastic host seam if SDL blocks correct damaged present. Handoff: reviewer should treat any permanent plain-swap conclusion as a rejection unless the user explicitly changes product direction.
- Orchestrator `orch-2026-06-19-render-surface-layout-present-01`: hello. Intent: promote Slice 4 code proof after the user said to start. Outcome: updated `sprints/current.txt` to authorize only a `howl-linux-host` SDL/EGL proof seam, fake-C tests, and binding/header changes if required; production damaged present, custom Wayland event loop, X11, GLFW/backend seam, and Slice 5 behavior remain non-goals. Handoff: coder `coder-2026-06-19-render-surface-layout-present-04` may execute only the promoted proof contract and must stop if SDL EGL binding or SDL Wayland semantics cannot be proved honestly.
- Coder `coder-2026-06-19-render-surface-layout-present-04`: hello. Intent: execute Slice 4 SDL EGL damaged-present feasibility proof only. Outcome: added display-owned `egl_present.zig`, exposed SDL EGL headers through `sdl_c.h`, imported the proof tests in `host_test_root.zig`, and left production `displaySubmitPresentSync` on `SDL_GL_SwapWindow`. The proof resolves KHR before EXT, falls back to plain swap only when KHR/EXT are unavailable, maps full/resize damage to `n_rects == 0`, converts damage rects to EGL bottom-left coordinates, validates main thread/current window/current context/EGL handles/bounds with overflow-safe checks, and explicitly blocks the current SDL-retained public path because hidden-window skip and Wayland display flush are not preserved. Verification passed in `howl-linux-host`: `zig build test:unit`, `zig build check`. Handoff: committed child repo as `132b810 Prove SDL EGL damaged present seam`; next implementation direction is Option C/D host-present ownership change unless the user changes product direction.
