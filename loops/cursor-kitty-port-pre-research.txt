# Cursor Kitty Port Pre-Research

Status:

- Active pre-research loop artifact.
- Purpose: block stale cursor planning before researcher work starts.

Receipt Header:

- Orchestrator session id: `orch-2026-06-15-cursor-restart-01`.
- Reviewer session id: `ses_13508b5e7ffe0XZUyUcJd4EPEs`.
- Active-status note: pre-research loop is active and `research/2026-06-15-cursor-kitty-restart-plan.md` now exists as the current planning artifact under review.

Live User Instruction And Non-Negotiables:

- Rip out existing Howl cursor code.
- Port Kitty cursor code to Zig.
- Kitty parity is primary for cursor work.
- Ghostty may only be consulted as fallback ownership advice if full Kitty cursor behavior cannot otherwise stay clean.
- C ABI boundary remains non-negotiable.

Problem Statement:

- This is full cursor replacement and remaining parity work, not partial cleanup, compatibility patching, or diff-minimizing repair.

Cursor-Scoped Reference Receipt Rule:

- User direction is primary.
- Kitty is primary for cursor behavior.
- Ghostty may only enter if full Kitty cursor behavior cannot be modeled without ownership blurring.

Reference Order And Stop Rule:

- User direction.
- Kitty cursor sources.
- Alacritty host/render organization.
- TigerBeetle ownership, assertions, source order, and tests.
- Ghostty fallback only for ownership cleanliness if Kitty parity is otherwise impossible to model cleanly.
- Official docs for protocol facts.
- Stop on any unresolved conflict or ownership gap before sprint planning.

Required Current-State Owner Map Scope:

- Current Howl owners, files, symbols, ABI seams, and tests for cursor state, cursor parsing, cursor style/blink/visibility, save/restore, alt-screen cursor consequences, DECSCUSR, DECRQSS `" q"`, render publication, and host cursor cadence.

Required Kitty Source Map Scope:

- Exact Kitty source files, symbols, and behaviors that define the cursor system being ported, separated from adjacent terminal features.

Pre-Research Scoping Questions:

- What exact current Howl owners, files, symbols, ABI seams, and tests define cursor state, cursor parsing, cursor style/blink/visibility, save/restore, alt-screen cursor consequences, DECSCUSR, DECRQSS `" q"`, render publication, and host cursor cadence?
- What exact Kitty source files, symbols, and behaviors constitute the cursor system to port, and which are required for full parity rather than adjacent behavior?
- What exact Kitty cursor behaviors are still absent in current Howl after the latest accepted worktree state, with file references on both the Howl side and Kitty side?
- Which cursor consequences belong to `howl-vt`, which belong to `howl-render`, which belong to host code, and which belong only to ABI translation, under a Kitty-first parity rule?
- Which existing Howl cursor owners and paths must be deleted outright, which must be replaced, which must remain consumer-only, and which must remain untouched?
- What exact owner/file split should exist after the remaining Kitty cursor parity work, even if current Howl cursor paths or names must be broken?
- What exact protocol surface remains in scope for full Kitty cursor parity: `ESC 7/8`, `CSI s/u`, `CSI Ps SP q`, `?25`, `47`, `1047`, `1049`, DECRQSS `" q"`, style mapping, no-shape, visibility, blink, savepoint payload, restore-without-save, alt-screen entry/exit, reset behavior, render/publication consequences, host cadence consequences?
- What exact ABI changes, if any, are still required for remaining parity, and where must they be proved?
- What exact assertions and tests must prove the remaining parity work, and what curated proof roots own them?
- Are there any places where full Kitty cursor parity still appears to conflict with clean ownership; if so, what exact Ghostty fallback question must be asked without weakening Kitty parity?
- What is the full real next slice boundary so the team does not split coupled cursor work into fake incremental cuts or preserve stale paths for comfort?

Anti-Stale Constraints:

- Existing Howl cursor files, names, enums, and owner seams are not authority to preserve.
- Deleted loop, research, and sprint artifacts are navigation only.
- Research may not preserve stale cursor save/restore, alt-screen, publication, or host paths for diff comfort.

Research Entry Gate:

- No sprint, no slices, and no worker authorization until every scoping question above is closed with current-source and reference proof.

Must Explicitly Prevent:

- Treating existing Howl cursor files, names, enums, or owner seams as authority to preserve.
- Reusing deleted loop, research, or sprint artifacts as live planning authority.
- Writing a sprint plan before re-proving the current Howl owner map and the Kitty source map from the current tree.
- Narrowing "port Kitty cursor code to Zig" into a partial patch, shim, compatibility layer, or follow-up-heavy v1.
- Letting Ghostty shrink, reinterpret, or weaken required Kitty cursor parity.
- Preserving stale cursor save/restore, alt-screen, publication, or host paths for minimal diff comfort instead of moving behavior to the true owner.
- Planning VT, render, ABI, and host cursor consequences as separate convenience tracks when the parity surface spans them.
- Accepting research without exact file and line references, delete/replace accounting, required tests, and explicit proof roots.
- Hiding unresolved ownership gaps or reference conflicts behind future-cleanup language.
- Allowing worker invention through vague phrases like `as needed`, `if necessary`, or `likely unchanged` in any cursor owner, ABI, or proof surface.

Loop Notes Log:

- Reviewer re-scoped this artifact from the blank restart state before new research begins.
- Researcher `ses_135074a6bffeuLKDxy30qK2cAL`: hello again.
- Intent: answer the pre-research scoping questions from the blank restart state and author the replacement planning artifact on disk.
- Outcome: created `research/2026-06-15-cursor-kitty-restart-plan.md` and then updated it through multiple reviewer-directed planning corrections until the remaining defects were limited to loop-accountability only.
- Handoff: reviewer should gate the live research artifact and this loop receipt together as the current planning authority.
