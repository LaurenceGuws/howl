Howl Linux Host Surface Layout Loop

Orchestrator session id: `orch-2026-06-19-host-surface-layout-01`.

Problem statement:

- The host terminal surface layout path currently permits contradictory geometry: visible rows multiplied by cell height can disagree with the render/grid pixel height.
- `deriveHostLayout(...)` owns that contradiction and must be deleted.
- Reference pressure says display/tab regions, terminal grid dimensions, and render viewport/surface geometry must be explicit owner truths, not one vague derivation.

Loop phase:

- Active planning seed for reviewer gate.
- Coder must not implement until reviewer accepts or repairs the execution contract.
- Coder and reviewer should resolve execution defects inside this loop without re-promoting the deferred render cleanup sprint.

Active artifacts:

- Sprint index: `sprints/current.txt`.
- Research/plan: `research/howl-linux-host-surface-layout-redesign.md`.
- Loop scratchpad: `loops/howl-linux-host-surface-layout-loop.txt`.

Non-negotiables:

- Delete `deriveHostLayout(...)`; do not preserve it as a wrapper.
- Keep the C ABI boundary intact.
- Keep VT and PTY behavior changes limited to corrected layout consequences.
- Assert snapped grid/render invariants where layout is committed.
- Prove with tests and `zig build check`.

Loop notes:

- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: stop the stale render cleanup active surface, defer it with historical notes, and seed a clean host surface layout sprint from the Alacritty/Kitty-backed bug finding. Outcome: moved prior active render cleanup and stale host event-loop research into `defered/`, wrote `research/howl-linux-host-surface-layout-redesign.md`, and rewrote `sprints/current.txt` to point only at this sprint. Handoff: reviewer should gate the plan; if accepted, coder should delete `deriveHostLayout(...)` and implement snapped terminal surface geometry inside the allowed files.

- Reviewer `review-2026-06-19-host-surface-layout-plan-01`: hello. Intent: gate the host surface layout redesign plan for coder readiness against exact files, owner truth, tests, references, non-goals, and stop conditions. Outcome: reject. Findings: `research/howl-linux-host-surface-layout-redesign.md:93-103` omits required test file `howl-linux-host/src/terminal/surface_test.zig`, but current tests call `surface_layout.deriveHostLayout(...)` at `howl-linux-host/src/terminal/surface_test.zig:242` and `howl-linux-host/src/terminal/surface_test.zig:322`, while the plan requires deleting that symbol at `research/howl-linux-host-surface-layout-redesign.md:114`; coder cannot both delete the symbol and keep to the allowed file list. Finding: `research/howl-linux-host-surface-layout-redesign.md:115` says to delete or rename `SurfaceLayoutRequest` fields without giving the exact replacement owner name and field names, so coder must invent API shape even though `loop/flow.md:32-38` and `loop/coder.md:31-35` require exact executable contracts. Finding: `research/howl-linux-host-surface-layout-redesign.md:104-108` permits editing `research/howl-linux-host-surface-layout-redesign.md` and `sprints/current.txt` during the executable slice even though execution evidence is supposed to go in this loop at `sprints/current.txt:35-37`; that broadens coder authority over active planning artifacts after review. Handoff: repair the plan before coding by naming the exact replacement request shape, exact test files, exact tests to update/add, and restricting coder accountability edits to this loop unless the orchestrator explicitly needs a separate receipt update.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: repair the rejected execution contract without product-code changes. Outcome: added `howl-linux-host/src/terminal/surface_test.zig` to allowed files, restricted coder accountability edits to this loop only, replaced the vague request-shape instruction with exact `SurfaceLayoutRequest { content_px }`, named the replacement derivation `snapSurfaceLayout(...)`, and required content-owned `State` field names. Handoff: same reviewer should re-gate the repaired plan before coder execution.

- Reviewer `review-2026-06-19-host-surface-layout-plan-01`: hello. Intent: re-gate the repaired host surface layout plan and specifically check the prior rejection findings. Outcome: reject. The prior findings are repaired: `howl-linux-host/src/terminal/surface_test.zig` is allowed at `research/howl-linux-host-surface-layout-redesign.md:95-103`, the replacement request/function shape is exact at `research/howl-linux-host-surface-layout-redesign.md:113-126`, and coder accountability edits are restricted to this loop at `research/howl-linux-host-surface-layout-redesign.md:105-107`. Remaining finding: the plan still requires present to use a snapped terminal texture rect at `research/howl-linux-host-surface-layout-redesign.md:139` and test it at `research/howl-linux-host-surface-layout-redesign.md:157`, but current `event.renderSnapshot` builds the texture rect directly from `DisplayLayout.contentRect(...)` at `howl-linux-host/src/event.zig:438-446`, and the plan does not name the exact replacement owner method, return shape, or call site that will convert display-owned content rect plus terminal snapped layout into the present rect. Coder would have to invent whether `TerminalSurface`, `DisplayLayout`, or `event.zig` owns that API. Handoff: repair the plan by naming the exact present-rect owner function/method and required test location/name so coder can execute without designing that seam.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: repair the remaining present-rect seam without product-code changes. Outcome: assigned the seam to `TerminalSurface.textureRect(content_rect: Layout.Rect) Layout.Rect`, specified exact x/y and snapped width/height behavior, required fit assertions, required `event.renderSnapshot(...)` to call it, and added a `surface_test.zig` proof for raw content height `570` producing snapped texture height `560`. Handoff: same reviewer should re-gate the repaired plan before coder execution.

- Reviewer `review-2026-06-19-host-surface-layout-plan-01`: hello. Intent: re-gate the repaired present seam and decide coder readiness. Outcome: accept. Findings: no blocking findings remain. The present seam is exact at `research/howl-linux-host-surface-layout-redesign.md:139-147`: `TerminalSurface.textureRect(content_rect: Layout.Rect) Layout.Rect` owns converting display content rect plus retained snapped render layout into the present rect, preserves x/y, uses snapped render width/height, asserts positive fit, and `event.renderSnapshot(...)` must call it before overlay/present snapshot construction. The proof obligation is exact enough at `research/howl-linux-host-surface-layout-redesign.md:163-166`, including the 570 raw content height to 560 snapped texture height case in `howl-linux-host/src/terminal/surface_test.zig`. Handoff: coder may execute only the accepted slice, may edit only the allowed product files plus this loop for evidence, must not preserve `deriveHostLayout(...)`, and must run/record the required host and root verification gates.

- Coder `coder-2026-06-19-host-surface-layout-01`: hello. Intent: implement the reviewer-accepted host terminal surface layout redesign without broadening scope. Outcome: blocked before product edits. Evidence: current `howl-linux-host/src/terminal/input.zig:132` directly reads `self.geometry.render_px_w` and `self.geometry.render_px_h`, while the accepted contract requires replacing those `State` fields with `content_px_w` and `content_px_h`; `howl-linux-host/src/terminal/input.zig` is not in the allowed product file list. Verification: not run because product edits would either break the build or require an out-of-scope file. Blocker/deviation status: blocker, no product files changed, no deliberate deviation. Handoff: orchestrator/reviewer must either add `howl-linux-host/src/terminal/input.zig` to the allowed files or repair the contract with an explicit source-backed alternative. Commit-hash handoff status: open; no git touched.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: repair the coder-discovered allowed-file blocker without product-code changes. Outcome: added `howl-linux-host/src/terminal/input.zig` to the allowed product files and required its current geometry access to use `content_px_w` and `content_px_h` after the field rename. Handoff: reviewer should gate this scope repair, then the same coder can continue the accepted implementation.

- Reviewer `review-2026-06-19-host-surface-layout-plan-01`: hello. Intent: gate only the coder-blocker scope repair for `terminal/input.zig`. Outcome: accept. Findings: no blocking findings. The added file is a direct consequence of the already accepted `State` field rename: current `howl-linux-host/src/terminal/input.zig:132` reads `self.geometry.render_px_w` and `self.geometry.render_px_h`, while the plan now requires `content_px_w` and `content_px_h` at `research/howl-linux-host-surface-layout-redesign.md:129-134`. The allowed file list includes `howl-linux-host/src/terminal/input.zig` at `research/howl-linux-host-surface-layout-redesign.md:95-104`, and the sprint records the same limited consequence at `sprints/current.txt:53`. This does not broaden beyond the accepted slice because it only keeps content-relative mouse scaling compiling against the renamed content-owned geometry fields. Handoff: same coder may continue the accepted implementation with `terminal/input.zig` limited to the named geometry field rename consequence.

- Coder `coder-2026-06-19-host-surface-layout-01`: hello. Intent: implement the accepted snapped host terminal surface layout redesign after the `terminal/input.zig` scope repair. Outcome: completed. Files changed: `howl-linux-host/src/terminal/render_surface_layout.zig`, `howl-linux-host/src/terminal/surface.zig`, `howl-linux-host/src/terminal/input.zig`, `howl-linux-host/src/event.zig`, `howl-linux-host/src/terminal/surface_test.zig`, and this loop note. Evidence: deleted `deriveHostLayout(...)`; `SurfaceLayoutRequest` is content-only; `snapSurfaceLayout(...)` derives cols/rows from content pixels and snaps grid/render pixels; committed layout asserts positive snapped invariants; `Surface.textureRect(...)` preserves content rect x/y and uses snapped retained render size; `event.renderSnapshot(...)` passes the snapped texture rect to overlay/present; `terminal/input.zig` now uses `content_px_w` and `content_px_h` for the renamed geometry fields. Verification: `zig build check` in `howl-linux-host` passed; host build target discovery found `test:unit`; `zig build test:unit` in `howl-linux-host` passed after updating the stale one-cell snap expectation; root `zig build check` passed. Blocker/deviation status: no blockers, no deliberate deviations, no git touched. Commit-hash handoff status: open for orchestrator; no commit made by coder.

- Reviewer `review-2026-06-19-host-surface-layout-plan-01`: hello. Intent: review the coder implementation diff and verification against the accepted host surface layout contract. Outcome: accept. Findings: no blocking findings. Host product changes are limited to the accepted files: `src/event.zig`, `src/terminal/input.zig`, `src/terminal/render_surface_layout.zig`, `src/terminal/surface.zig`, and `src/terminal/surface_test.zig`. `deriveHostLayout(...)` is deleted with no wrapper, `SurfaceLayoutRequest` is content-only, `snapSurfaceLayout(...)` snaps `960x570` at `8x16` cells to `120x35` and `960x560`, layout commit asserts snapped render/grid invariants, geometry fields were renamed to content-owned names, and `event.renderSnapshot(...)` now presents `tab.textureRect(content_rect)`. Tests prove the required snapped layout and texture rect `570 -> 560` cases in `src/terminal/surface_test.zig`. Verification independently rerun by reviewer: `zig build check` in `howl-linux-host` passed; `zig build test:unit` in `howl-linux-host` passed; root `zig build check` passed. Handoff: orchestrator can verify receipts and close/commit the accepted slice; commit-hash receipt remains open until orchestrator action.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: independently verify the reviewer-accepted host surface layout redesign and record acceptance state. Outcome: searched host source for deleted layout owner symbols; `deriveHostLayout(...)` and deleted geometry state fields are gone, with only local input helper parameter names still using `render_px_w` and `render_px_h`. Reran `zig build check` and `zig build test:unit` in `howl-linux-host`, then root `zig build check`; all passed. Handoff: implementation is accepted by loop review and orchestrator verification; commit-hash receipt remains open because no commit was requested in this turn.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: continue cutting after user reported zero visual progress by promoting the next reference-backed viewport owner seam. Outcome: updated `research/howl-linux-host-surface-layout-redesign.md` with Slice 2, moving terminal placement from `TerminalSurface.textureRect(...)` to display-owned `DisplayLayout.terminalRect(...)` while terminal surface exposes only snapped `textureSize(...)`. Handoff: reviewer should gate Slice 2 before coder execution.

- Reviewer `review-2026-06-19-host-surface-layout-placement-01`: hello. Intent: gate Slice 2, Display-Owned Terminal Placement, for coder readiness without product-code changes or git. Outcome: reject. Findings: `sprints/current.txt:41-57` still presents the accepted Slice 1 implementation shape as live required implementation, including `Surface.textureRect(content_rect: Layout.Rect) Layout.Rect` at `sprints/current.txt:53` and `event.renderSnapshot(...)` calling `tab.textureRect(DisplayLayout.contentRect(...))` at `sprints/current.txt:54`, while Slice 2 requires replacing `Surface.textureRect(...)` with `Surface.textureSize(...)` and moving placement to `DisplayLayout.terminalRect(...)` at `research/howl-linux-host-surface-layout-redesign.md:200-219`; coder would receive contradictory live contracts from the required read set. Finding: Slice 2's source-level shape is otherwise executable and does not conflict with the accepted Slice 1 code direction: `howl-linux-host/src/display/layout.zig` already owns `Rect`/`Size` and content region functions, `howl-linux-host/src/terminal/surface.zig:321-335` currently owns the placement method to delete/replace, `howl-linux-host/src/event.zig:438-441` is the exact call site, and `howl-linux-host/src/terminal/surface_test.zig:382-393` is the stale Slice 1 proof to move/replace. Handoff: repair `sprints/current.txt` so the live sprint contract names Slice 2's exact functions, files, tests, non-goals, and stop conditions instead of stale Slice 1 requirements; then re-gate before coder execution.

- Reviewer `review-2026-06-19-host-surface-layout-placement-01`: hello. Intent: re-gate Slice 2 after the sprint-index repair and decide coder readiness. Outcome: accept. Findings: no blocking findings remain. The prior live-contract conflict is repaired: `sprints/current.txt:17-18` marks Slice 1 as receipt context only and `sprints/current.txt:38-63` now carries Slice 2's display-owned placement shape, tests, and stop conditions without requiring `Surface.textureRect(...)`. Acceptance notes: Slice 2 is executable without coder invention because `research/howl-linux-host-surface-layout-redesign.md:202-219` and `sprints/current.txt:40-47` name the exact replacement APIs and call flow: add `DisplayLayout.terminalRect(content_rect: Rect, texture_size: Size) Rect`, replace `Surface.textureRect(...)` with `Surface.textureSize() Layout.Size`, and change `event.renderSnapshot(...)` to place the texture through `DisplayLayout.terminalRect(...)` before `overlaySnapshot(...)`. Current source seams match the allowed file list: `howl-linux-host/src/display/layout.zig:3-13` already owns `Rect`/`Size`, `howl-linux-host/src/terminal/surface.zig:321-335` is the exact terminal-owned placement method to delete/replace, `howl-linux-host/src/event.zig:438-441` is the exact call site, and `howl-linux-host/src/terminal/surface_test.zig:382-393` is the stale Slice 1 proof to move/replace. Handoff: coder may execute only Slice 2, may edit only the allowed product files plus this loop for evidence, must leave terminal placement display-owned, must run `zig build check` and `zig build test:unit` in `howl-linux-host`, then root `zig build check`, and must not touch git.

- Coder `coder-2026-06-19-host-surface-layout-placement-01`: hello. Intent: implement reviewer-accepted Slice 2, Display-Owned Terminal Placement, without broadening scope or touching git. Outcome: completed. Files changed: `howl-linux-host/src/display/layout.zig`, `howl-linux-host/src/terminal/surface.zig`, `howl-linux-host/src/terminal/surface_test.zig`, `howl-linux-host/src/event.zig`, and this loop note. Evidence: added `DisplayLayout.terminalRect(content_rect, texture_size)` with positive and fit assertions; replaced terminal-owned `Surface.textureRect(...)` with `Surface.textureSize()` returning asserted snapped retained render dimensions; changed `event.renderSnapshot(...)` to compute content rect, texture size, display-owned terminal rect, then pass the placed rect to `overlaySnapshot(...)`; updated tests for display terminal rect placement and surface texture size. Verification: `zig build check` in `howl-linux-host` passed; `zig build test:unit` in `howl-linux-host` passed; root `zig build check` passed. Blocker/deviation status: no blockers, no deliberate deviations, no git touched. Commit-hash handoff status: open for orchestrator; no commit made by coder.

- Reviewer `review-2026-06-19-host-surface-layout-placement-01`: hello. Intent: review the Slice 2 implementation diff and verification against display-owned terminal placement. Outcome: accept. Findings: no blocking findings. Product changes visible in the allowed files match the accepted contract: `DisplayLayout.terminalRect(...)` at `howl-linux-host/src/display/layout.zig:61-72` preserves `content_rect.x/y`, returns `texture_size.width/height`, and asserts positive texture dimensions plus fit inside the content rect; `Surface.textureRect(...)` is gone from terminal surface and replaced by `Surface.textureSize()` at `howl-linux-host/src/terminal/surface.zig:321-331`, returning asserted snapped retained render dimensions; `event.renderSnapshot(...)` at `howl-linux-host/src/event.zig:438-442` now computes content rect, terminal texture size, display-owned terminal rect, and passes the placed rect to `overlaySnapshot(...)`. Tests prove the required cases in `howl-linux-host/src/terminal/surface_test.zig:383-400`: display terminal rect maps raw content height `570` plus texture size `960x560` to rect height `560` while preserving `x/y`, and surface texture size returns snapped retained render size. Verification independently rerun by reviewer: `zig build check` in `howl-linux-host` passed; `zig build test:unit` in `howl-linux-host` passed; root `zig build check` passed. Handoff: orchestrator can verify receipts and close/commit Slice 2; commit-hash receipt remains open until orchestrator action.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: independently verify reviewer-accepted Slice 2 and record acceptance. Outcome: searched host source for terminal placement calls; `event.zig` now calls `tab.textureSize()` and `DisplayLayout.terminalRect(...)`, terminal surface owns only `textureSize()`, and display layout owns `terminalRect(...)`. Reran `zig build check` and `zig build test:unit` in `howl-linux-host`, then root `zig build check`; all passed. Handoff: Slice 2 is accepted by loop review and orchestrator verification; commit-hash receipt remains open because no commit was requested.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: promote the next viewport seam after Slice 2 by making terminal input consume the placed snapped terminal rect instead of raw content extents. Outcome: updated `research/howl-linux-host-surface-layout-redesign.md` with Slice 3 and rewrote `sprints/current.txt` so Slice 3 is the only live execution scope. Handoff: reviewer should gate Slice 3 before coder execution.
- Reviewer `review-2026-06-19-host-surface-layout-input-01`: hello. Intent: gate Slice 3, Terminal Input Uses Placed Terminal Rect, for coder readiness without product-code changes or git. Outcome: accept. Findings: no blocking findings. Slice 3 aligns input extents with the snapped terminal rect by requiring `DisplayLayout.terminalLogicalSize(...)` at `research/howl-linux-host-surface-layout-redesign.md:267-281` and `sprints/current.txt:41-45`, then passing that logical size into `event.forwardTerminalInput(...)` while keeping the top-left origin under the tab bar. The current seams match the allowed files: `howl-linux-host/src/display/layout.zig:37-106` owns content/logical scaling helpers and can add the logical terminal-size helper; `howl-linux-host/src/event.zig:324-335` is the exact forwarding call path; `howl-linux-host/src/terminal/input.zig:123-183` is the exact mouse-localization path currently using raw content pixels; `howl-linux-host/src/terminal/surface_test.zig:383-400` already proves Slice 2 placement/texture size and `howl-linux-host/src/terminal/surface_test.zig:560-824` has owner-local input harnesses for the leftover-strip rejection proof. Acceptance notes: coder may execute only Slice 3, may edit only `howl-linux-host/src/display/layout.zig`, `howl-linux-host/src/event.zig`, `howl-linux-host/src/terminal/input.zig`, `howl-linux-host/src/terminal/surface_test.zig`, plus this loop for evidence; must prove `960x570` content with `960x560` terminal pixels maps to terminal logical height `560`; must prove pointer events outside the snapped terminal rect no longer publish/map to a terminal row/column; must run `zig build check` and `zig build test:unit` in `howl-linux-host`, then root `zig build check`; must stop if SDL input collection, VT, PTY, render ABI, headers, or files outside the allowed list are needed.

- Coder `coder-2026-06-19-host-surface-layout-input-01`: hello. Intent: implement reviewer-accepted Slice 3, Terminal Input Uses Placed Terminal Rect, without broadening scope or touching git. Outcome: completed. Files changed: `howl-linux-host/src/display/layout.zig`, `howl-linux-host/src/event.zig`, `howl-linux-host/src/terminal/input.zig`, `howl-linux-host/src/terminal/surface_test.zig`, and this loop note. Evidence: added `DisplayLayout.terminalLogicalSize(...)` with positive/fitting assertions and snapped-pixel-to-logical scaling; `event.forwardTerminalInput(...)` now derives content pixels, content logical size, active tab snapped texture size, and terminal logical size before forwarding input; `terminal/input.zig` now maps mouse coordinates through `self.term.render.surface_layout.render_px` instead of raw content pixels; tests prove `960x570` content plus `960x560` terminal pixels returns logical height `560` and rejects a mouse event in the raw leftover strip below snapped terminal height. Verification: `zig build check` in `howl-linux-host` passed; `zig build test:unit` in `howl-linux-host` passed; root `zig build check` passed. Blocker/deviation status: no blockers, no deliberate deviations, no git touched. Commit-hash handoff status: open for orchestrator; no commit made by coder.
- Reviewer `review-2026-06-19-host-surface-layout-input-01`: hello. Intent: review the Slice 3 implementation and verification against terminal input using the placed snapped terminal rect. Outcome: accept. Findings: no blocking findings. Read-only diff checks for the allowed Slice 3 files showed no unstaged or staged diff, so review was against current source plus the coder loop evidence. Current source matches the accepted contract: `DisplayLayout.terminalLogicalSize(...)` at `howl-linux-host/src/display/layout.zig:74-91` asserts positive content/terminal dimensions, asserts terminal pixels fit inside content pixels, scales terminal pixels to logical size with clamping to `[1, content_logical]`; `event.forwardTerminalInput(...)` at `howl-linux-host/src/event.zig:324-331` derives `content_px`, `content_logical`, `terminal_px = tab.textureSize()`, and forwards `terminal_logical.width/height`; `terminal/input.zig:132-134` maps mouse input through `self.term.render.surface_layout.render_px` instead of raw content geometry. Tests cover the required proofs: `howl-linux-host/src/terminal/surface_test.zig:392-397` proves `960x570` content/logical with `960x560` terminal pixels returns logical height `560`, and `howl-linux-host/src/terminal/surface_test.zig:843-905` proves a mouse event in the raw leftover strip at `y=565` is rejected with zero terminal mouse publications. Verification independently rerun by reviewer: `zig build check` in `howl-linux-host` passed; `zig build test:unit` in `howl-linux-host` passed; root `zig build check` passed. Handoff: orchestrator can verify receipts and close/commit Slice 3; commit-hash receipt remains open until orchestrator action.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: independently verify reviewer-accepted Slice 3 and record acceptance before committing. Outcome: searched host source for input and placement seams; `event.forwardTerminalInput(...)` derives terminal logical input dimensions from `tab.textureSize()` and `DisplayLayout.terminalLogicalSize(...)`, `terminal/input.zig` maps mouse coordinates using snapped `self.term.render.surface_layout.render_px`, and display layout owns both `terminalRect(...)` and `terminalLogicalSize(...)`. Reran `zig build check` and `zig build test:unit` in `howl-linux-host`, then root `zig build check`; all passed. Handoff: accepted slices 1-3 are ready for host and root commits.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: close the accepted host product commit receipt. Outcome: committed accepted `howl-linux-host` slices 1-3 as `f95391f Align terminal surface viewport geometry`. Handoff: root pointer/accountability commit remains open.
- Orchestrator `orch-2026-06-19-host-surface-layout-01`: hello. Intent: independently verify reviewer-accepted Slice 3 and record acceptance. Outcome: searched host source for input and placement seams; `event.forwardTerminalInput(...)` derives terminal logical input dimensions from `tab.textureSize()` and `DisplayLayout.terminalLogicalSize(...)`, `terminal/input.zig` maps mouse coordinates using snapped `self.term.render.surface_layout.render_px`, and display layout owns both `terminalRect(...)` and `terminalLogicalSize(...)`. Reran `zig build check` and `zig build test:unit` in `howl-linux-host`, then root `zig build check`; all passed. Handoff: Slice 3 is accepted by loop review and orchestrator verification; commit-hash receipt remains open because no commit was requested.
