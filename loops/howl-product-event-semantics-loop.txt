Howl Product Event Semantics Loop

Orchestrator session id: `orch-2026-06-19-product-event-semantics-01`.
Researcher session id: open.

Problem statement:

- Howl is one product split across multiple repositories. Interfaces and ABI-facing contracts are not sacred; they must be made true.
- Generic `work`/`workload`/`WorkState`/`SessionWorkState`/`RealOps` terms are suspect across all repos when a more exact event, queue, drain, admission, prepare, submit, present, completion, or benchmark-fixture owner exists.

Loop phase:

- Active research loop for product-wide event/interface vocabulary cleanup.
- The active research plan is `research/howl-product-event-semantics.md`.
- No coder execution is live until product-wide research and review settle exact slices.

Non-negotiables:

- Alacritty host event-loop/presentation/render vocabulary pressure wins unless the user explicitly overrides it.
- Ghostty VT and embedding seam pressure applies to VT/ABI-facing terminal contracts.
- TigerBeetle naming, bounded queue, and assertion pressure is a hard gate.
- ABI-facing names and structs are in scope when they are wrong.
- Every implementation slice must carry repo-specific tests plus root pointer receipts where nested repos move.

Loop notes:

- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: correct the wrong host-only/no-ABI-change event-semantics plan after explicit user correction. Outcome: replaced the active plan with a product-wide research contract, made all repos and ABI-facing contracts in scope, and stopped implementation. Handoff: researcher must inventory and classify event/work vocabulary across all repos before any coder slice is promoted.
- Researcher `research-2026-06-19-product-event-semantics-01`: hello. Intent: replace the incomplete active research contract with a source-backed product-wide event/interface vocabulary plan. Outcome: inventoried active source/test/build/header hits across root, host, render, VT, PTY, and benchmark tools; separated benchmark workload terms from product event/admission names; wrote the replacement report in `research/howl-product-event-semantics.md`. Handoff: ready for reviewer; coder remains blocked until reviewer/orchestrator acceptance and slice promotion.
- Reviewer `review-2026-06-19-product-event-semantics-01`: hello. Intent: review the active product-wide event semantics research plan for execution readiness without implementing or using git. Outcome: reject. Findings: Slice 1 misses live `frame_work_pending` product fields in `howl-linux-host/src/event.zig:75`, `81`, `212`, `233`, `665`, `679`, `693`, `707` while only planning `render_work_pending` and test-title renames; this leaves suspicious `work` vocabulary inside the same host event-loop admission owner. The inventory also misses active workload hits outside benchmark output classification, including root `build.zig:67` simulation workload wording and `howl-vt/simulation/main.zig:6`; benchmark/load-case terms may stay, but every active hit must be classified. Slice 1 file scope is inconsistent because `howl-linux-host/design.md` is allowed there while docs are deferred to Slice 4, creating room for coder invention. Slice 1 targeted tests omit the known impacted `surface_test.zig:496` retained-render wake test even though the vocabulary map requires renaming it; full unit coverage is not an exact targeted proof. Required repairs: classify and map `frame_work_pending` exactly, classify all active workload hits, either remove docs from Slice 1 or make the doc change exact, and add the missing targeted retained-render wake test command or explicitly justify why full unit is sufficient. Handoff: researcher correction required before coder promotion.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: repair the reviewer findings without product-code changes. Outcome: updated the active research plan to classify `frame_work_pending` as `visual_present_pending`, classify root and VT simulation workload wording as simulation load-case/build UX terms, remove `howl-linux-host/design.md` from Slice 1, and add the retained-render wake targeted test. Handoff: reviewer should re-gate before any coder slice is promoted.
- Reviewer `review-2026-06-19-product-event-semantics-02`: hello. Intent: re-review the corrected product-wide event semantics research plan without implementation or git. Outcome: reject. Findings: prior repairs are mostly present, but the active inventory still misses `howl-vt/simulation/scrollback.zig:4` (`Workloads use deterministic`), while the plan claims VT simulation workload hits are classified at `howl-vt/simulation/main.zig:5-6` and root `build.zig:67` only. Because all repos and all active product/test/build/header suspicious vocabulary hits are in scope, Slice 1 cannot be promoted from an incomplete product-wide inventory. Slice 1 itself is otherwise executable enough on the checked points: `frame_work_pending` is mapped to `visual_present_pending`, docs are removed from Slice 1, and the retained-render wake test is targeted. Handoff: researcher/orchestrator must classify the missing VT simulation helper workload hit, update the sources read/search inventory/vocabulary map if needed, then request re-review before coder promotion.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: repair the second reviewer finding without product-code changes. Outcome: classified `howl-vt/simulation/scrollback.zig:4-5` as a simulation load-case term in the research plan and recorded the second rejection in the correction notes. Handoff: reviewer should re-gate before any coder slice is promoted.
- Reviewer `review-2026-06-19-product-event-semantics-03`: hello. Intent: re-review the active product-wide event semantics research plan after the second correction without implementation or git. Outcome: accept. Findings: no blocking findings. Prior findings are repaired: `frame_work_pending` maps to `visual_present_pending`, root `build.zig`, VT `simulation/main.zig`, and VT `simulation/scrollback.zig` workload hits are classified, Slice 1 excludes docs, and the retained-render wake targeted test is included. Handoff: orchestrator may promote Slice 1; coder can execute only the accepted retained-render admission vocabulary slice.
- Coder `coder-2026-06-19-product-event-semantics-01`: hello. Intent: implement accepted Slice 1 retained-render admission vocabulary without broadening scope. Outcome: completed. Files changed: `howl-linux-host/src/terminal/render_retained.zig`, `howl-linux-host/src/terminal/surface.zig`, `howl-linux-host/src/terminal/surface_test.zig`, `howl-linux-host/src/event.zig`, `howl-linux-host/src/display/present.zig`, and active accountability files. Evidence: renamed `WorkState` to `RenderTurnAdmission`, `workState(...)` to `admitRenderTurn(...)`, `needsRenderSurface()` to `needsRenderTurn()`, host render facts to `render_turn_pending`, frame wait facts/local present eligibility to `visual_present_pending`, and test names to turn/present wording. Verification: all six targeted Slice 1 `zig build test:unit -- <filter>` commands passed; full `zig build test:unit` and `zig build check` in `howl-linux-host` passed; root `zig build check` passed. Blocker/deviation status: no blockers, no deliberate deviations, no git touched by coder. Commit-hash handoff status: open for orchestrator.
- Reviewer `review-2026-06-19-product-event-semantics-slice1-01`: hello. Intent: review the Slice 1 implementation diff and verification against the accepted retained-render admission vocabulary contract. Outcome: accept. Findings: no blocking findings. Product diff is limited to the five accepted Slice 1 files, required renames are complete in accepted files, test names match required target filters, and no behavior changes to retained states, prepare/submit/present ordering, frame wait, or present submission were visible. Remaining `SessionWorkState`/`workState` hits are only in `src/render_session.zig`, outside Slice 1. Handoff: orchestrator may close the host commit and root pointer/accountability receipts.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: close the accepted Slice 1 host product commit receipt. Outcome: committed and pushed `howl-linux-host` as `d2bde33 Name host render turn admission`. Handoff: root pointer/accountability commit remains open.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: close the Slice 1 root pointer/accountability receipt and promote Slice 2. Outcome: committed and pushed root as `4120269 Track product event semantics slice`; updated `sprints/current.txt` so Slice 2, host test-injection ops vocabulary, is the only live executable scope. Handoff: coder may execute only Slice 2.
- Coder `coder-2026-06-19-product-event-semantics-02`: hello. Intent: implement accepted Slice 2 host test-injection ops vocabulary without broadening scope. Outcome: completed. Files changed: `howl-linux-host/src/terminal/pty_pump.zig`, `howl-linux-host/src/terminal/pty_wait_thread.zig`, `howl-linux-host/src/terminal/vt_surface.zig`, `howl-linux-host/src/terminal/fonts.zig`, and this loop note. Evidence: renamed `RealOps` to `TerminalProgressOps`, `ProgressThreadOps`, `RenderStateCaptureOps`, and `FontSizeOps`; kept `RealAckOps` unchanged per accepted plan; renamed PTY/runtime tests from generic work wording to progress turn, runtime obligation, and locked feed byte wording. Verification: targeted `zig build test:unit -- <filter>` commands for all four required tests passed; full `zig build test:unit` and `zig build check` in `howl-linux-host` passed; root `zig build check` passed. Blocker/deviation status: no blockers, no deliberate deviations, no git touched by coder. Commit-hash handoff status: open for orchestrator.
- Reviewer `review-2026-06-19-product-event-semantics-slice2-01`: hello. Intent: review the Slice 2 implementation diff and verification against the accepted host test-injection ops vocabulary contract. Outcome: accept. Findings: no blocking findings. Product diff is limited to accepted Slice 2 files; `RealOps` renames are exact; `RealAckOps` remains only in the VT ack path and is role-specific enough per plan; required test names match the Slice 2 filters; no behavior changes to PTY/VT progress, wake coalescing, transport bounds, queues, timing, or render capture were visible. Handoff: orchestrator may close the host commit and root pointer/accountability receipts.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: close the accepted Slice 2 host product commit receipt. Outcome: committed and pushed `howl-linux-host` as `b53d381 Name host platform op seams`. Handoff: root pointer/accountability commit remains open.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: close the Slice 2 root pointer/accountability receipt and resolve the Slice 3 render-session decision. Outcome: committed and pushed root as `0675b44 Track host platform op seams`; proved `howl-linux-host/src/render_session.zig` is unreferenced by host source/build and contains stale `SessionWorkState`/`workState(...)`; promoted Slice 3 as deletion of that stale host mirror, not a false rename. Handoff: coder may execute only Slice 3 deletion and required greps/builds.
- Coder `coder-2026-06-19-product-event-semantics-03`: hello. Intent: execute Slice 3 by deleting the stale unreferenced host render-session mirror instead of preserving dead `SessionWorkState` vocabulary. Outcome: completed. Files changed: deleted `howl-linux-host/src/render_session.zig` and updated active accountability files. Evidence: required grep for `render_session|SessionWorkState|TextSessionOwner|\.workState\(` under `howl-linux-host/src` returned no matches. Verification: `zig build test:unit` and `zig build check` in `howl-linux-host` passed; root `zig build check` passed. Blocker/deviation status: no blockers, no deliberate deviations, no git touched by coder. Commit-hash handoff status: open for orchestrator.
- Reviewer `review-2026-06-19-product-event-semantics-slice3-01`: hello. Intent: review the Slice 3 stale host render-session mirror deletion. Outcome: accept. Findings: no blocking findings. Host product diff is deletion-only for `src/render_session.zig`; no remaining host references to `render_session`, `SessionWorkState`, `TextSessionOwner`, or `.workState(`; no out-of-scope host product files changed; required grep and build gates passed. Handoff: orchestrator may close the host commit and root pointer/accountability receipts.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: close the accepted Slice 3 host product commit receipt. Outcome: committed and pushed `howl-linux-host` as `af7bf44 Delete stale host render session mirror`. Handoff: root pointer/accountability commit remains open.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: close the Slice 3 root pointer/accountability receipt and promote/apply Slice 4 docs vocabulary alignment. Outcome: committed and pushed root as `74bc609 Track stale host render session deletion`; updated active docs to use render-turn admission, prepare/submit/upload turns, presented render output, resource ack, and visible draw/update wording instead of generic work/work-state language. Handoff: verify root check and review docs-only Slice 4 before committing.
- Coder `coder-2026-06-19-product-event-semantics-04`: hello. Intent: execute Slice 4 documentation vocabulary alignment without product-code changes. Outcome: completed. Files changed: `howl-linux-host/design.md`, `howl-render/design.md`, `docs/render-surface.md`, `sprints/current.txt`, and this loop note. Evidence: active docs now use render-turn admission, render prepare/submit/upload turns, presented render output, host-side commands/backend submissions, and visible draw/update wording instead of generic work/work-state language; benchmark/simulation workload terms were not changed. Verification: root `zig build check` passed; greps for `\bwork\b|\bwork state\b|\bWorkState\b` in the three touched docs returned no matches. Blocker/deviation status: no blockers, no deliberate deviations, no product code changed. Commit-hash handoff status: open for orchestrator.
- Reviewer `review-2026-06-19-product-event-semantics-slice4-01`: hello. Intent: review Slice 4 docs vocabulary alignment. Outcome: accept. Findings: no blocking findings. No product code changed; nested repos show only `design.md` edits; touched docs use accepted vocabulary and do not change benchmark/simulation workload terms; docs do not describe unaccepted implementation decisions; root check and stale-doc greps passed. Handoff: orchestrator may close nested doc commits and root pointer/accountability receipts.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: close accepted Slice 4 nested doc commit receipts. Outcome: committed and pushed `howl-linux-host` as `3076c56 Align host render turn docs` and `howl-render` as `1eae264 Align render preparation docs`. Handoff: root pointer/docs/accountability commit remains open.
- Reviewer `review-2026-06-19-product-event-semantics-03`: hello. Intent: re-review the second-corrected product-wide event semantics research plan without implementation or git. Outcome: accept. Findings: none blocking. Prior findings are repaired: `frame_work_pending` is mapped exactly to `visual_present_pending`; root `build.zig:67`, `howl-vt/simulation/main.zig:5-6`, and `howl-vt/simulation/scrollback.zig:4-5` workload hits are classified as simulation load-case/build UX terms; docs are excluded from Slice 1 and deferred to exact Slice 4 docs work; the retained-render wake target `pty wake observes retained render turn prepared by pty thread` is included; Slice 1 has exact allowed files, required renames, assertions, targeted tests, non-goals, and stop conditions. Handoff: orchestrator may promote Slice 1 with worker/reviewer receipts and nested repo/root pointer receipt requirements; coder remains blocked until that promotion.
- Reviewer `review-2026-06-19-product-event-semantics-03`: hello. Intent: review the actual Slice 1 implementation diff in `howl-linux-host` without implementing or using git commit. Outcome: accept. Findings: none blocking. Product diff is limited to accepted files `src/terminal/render_retained.zig`, `src/terminal/surface.zig`, `src/terminal/surface_test.zig`, `src/event.zig`, and `src/display/present.zig`; `WorkState`/`workState`/`needsRenderSurface` are renamed to `RenderTurnAdmission`/`admitRenderTurn`/`needsRenderTurn`; `render_work_pending` is renamed to `render_turn_pending`; `frame_work_pending` and local `render_pending` are renamed to `visual_present_pending`; required test names match the Slice 1 filters; retained states, prepare/submit/present ordering, frame wait, and present submission behavior are unchanged in the reviewed diff. Residual `SessionWorkState`/`workState` hits are only in `src/render_session.zig`, which is outside Slice 1 and queued separately. Verification recorded by coder is adequate: six targeted Slice 1 filters, full host unit, host check, and root check all passed. Handoff: orchestrator may proceed to independent verification and receipt closure; no product-code changes were made by reviewer.
- Reviewer `review-2026-06-19-product-event-semantics-03`: hello. Intent: review the actual Slice 2 implementation diff in `howl-linux-host` without implementing or using git commit. Outcome: accept. Findings: none blocking. Product diff is limited to accepted files `src/terminal/pty_pump.zig`, `src/terminal/pty_wait_thread.zig`, `src/terminal/vt_surface.zig`, and `src/terminal/fonts.zig`; `RealOps` is renamed exactly to `TerminalProgressOps`, `ProgressThreadOps`, `RenderStateCaptureOps`, and `FontSizeOps`; `RealAckOps` remains only in the VT ack path and is role-specific enough per the accepted plan; required test names match all four Slice 2 filters; PTY/VT progress, wake coalescing, transport bounds, queues, timing, and render-state capture behavior are unchanged in the reviewed diff. Verification recorded by coder is adequate: all four targeted Slice 2 filters, full host unit, host check, and root check all passed. Handoff: orchestrator may proceed to independent verification and receipt closure; no product-code changes were made by reviewer.
- Reviewer `review-2026-06-19-product-event-semantics-03`: hello. Intent: review the actual Slice 3 deletion diff in `howl-linux-host` without implementing or using git commit. Outcome: accept. Findings: none blocking. Product diff is limited to deletion of accepted file `src/render_session.zig`; required host reference grep for `render_session|SessionWorkState|TextSessionOwner|\.workState\(` is clean; no host source/build root references remain; no replacement alias, stub, host-local session surface, renderer file change, or other host product file change is present. Verification recorded by coder is adequate and reviewer re-ran the gates: required grep, host `zig build test:unit`, host `zig build check`, and root `zig build check` all passed. Handoff: orchestrator may proceed to independent verification and receipt closure; no product-code changes were made by reviewer.
- Reviewer `review-2026-06-19-product-event-semantics-slice4-01`: hello. Intent: review the Slice 4 documentation vocabulary alignment without implementing or committing. Outcome: accept. Findings: none blocking. Diff is limited to documentation/accountability files in scope: `howl-linux-host/design.md`, `howl-render/design.md`, `docs/render-surface.md`, `sprints/current.txt`, and this loop note; nested repos show only `design.md` changes and no product code changes. Generic `work`/`work state` wording in the three touched docs is replaced with render-turn admission, render prepare/submit/upload turns, presented render output, host-side commands/backend submissions, and visible draw/update wording; no benchmark/simulation workload terms were changed; docs do not describe unaccepted implementation decisions. Verification recorded by coder is adequate and reviewer re-ran the gates: root `zig build check` passed, and stale-doc greps for `\bwork\b|\bwork state\b|\bWorkState\b` in the three touched docs returned no matches. Handoff: orchestrator may close the Slice 4 documentation/accountability receipt; no product-code changes were made by reviewer.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: correct the missed render benchmark API vocabulary after user review. Outcome: acknowledged the previous classification was wrong for `howl-render/src/benchmark_main.zig`; promoted Slice 5 for render benchmark-case vocabulary and updated the research correction notes. Handoff: implementation changes only `howl-render/src/benchmark_main.zig` and must prove render source/header grep has no work/workload symbols.
- Coder `coder-2026-06-19-product-event-semantics-05`: hello. Intent: execute Slice 5 by removing `workload` vocabulary from the render benchmark API/output surface. Outcome: completed. Files changed: `howl-render/src/benchmark_main.zig` and active accountability files. Evidence: renamed `WorkloadInput`, `WorkloadResult`, `WorkloadDamage`, `Workload`, and `WorkloadPrepareContext` to benchmark input/result/damage/case/prepare-context names; renamed build/run/prepare helpers to benchmark-case names; renamed local `workload(s)` to `benchmark_case(s)`; changed text output `workload=` to `benchmark_case=` and NDJSON field `workload` to `benchmark_case`. Verification: `zig build check`, `zig build test:unit`, `zig build test:abi`, and `zig build benchmark:render:build` in `howl-render` passed; root `zig build check` passed; render source/header grep for work/workload terms returned no matches. Blocker/deviation status: no blockers, no deliberate deviations. Commit-hash handoff status: open for orchestrator.
- Orchestrator `orch-2026-06-19-product-event-semantics-01`: hello. Intent: close the Slice 5 render benchmark vocabulary product commit. Outcome: committed and pushed `howl-render` as `dd176cf Name render benchmark cases`. Handoff: root pointer/accountability commit remains open.
