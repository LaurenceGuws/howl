Render folder owner structure live loop scratchpad

Date: 2026-06-13.

Role owner: orchestrator.

Status: active planning loop.

Orchestrator session id: `orch-2026-06-13-render-folder-structure-01`.

Researcher session id: `research-2026-06-13-render-folder-structure-01`.

Reviewer session id: `review-2026-06-13-render-folder-structure-01`.

Loop commit-hash receipt: planning package closed in root commit `ca6f1c4`.

Shared scratchpad rule:

- This file is the live team scratchpad for the current step.
- Every non-trivial orchestrator, researcher, reviewer, or coder pass must append a short note with role/session id, hello, intent, one outcome, and next handoff or blocker.

Controlling sprint:

- `/home/home/personal/projects/howl/sprints/2026-06-13-render-folder-owner-structure-sprint.md`

Active research target:

- `/home/home/personal/projects/howl/research/2026-06-13-render-folder-owner-structure-plan.md`

Current step:

- The render folder/file owner structure planning package is accepted and committed in root `ca6f1c4`.
- Slice 1, `C boundary quarantine`, is seeded for execution.
- Coder may execute only the active condensed execution contract below.

Exact user direction:

- Clean up file and folder locations and naming.
- Keep only curated owner units in `src`.
- Use shallow child folders only for per-owner definitions or true subdomains.
- Take pressure from `utils/dev_references/terminals/ghostty/src/terminal` and its child folders for intentional structure.
- Current render API still looks undisciplined compared to a project with real discipline and intent.

Exact non-goals:

- No implementation during planning.
- No sprint narrowing, v1 language, or partial cleanup unless the user explicitly narrows scope.
- No Howl-only folder inventions while Ghostty, Alacritty, TigerBeetle, Kitty, or official docs provide source-backed pressure.
- No fake compatibility wrappers or preserving dead directories for comfort.

Exact stop conditions:

- Stop if the active research cannot name exact allowed files, required shapes, tests, non-goals, stop conditions, and receipt fields for each slice.
- Stop if current source and references conflict in a way that needs explicit user override.
- Stop if active `loops/`, `research/`, or `sprints/` contain stale non-archive artifacts.
- Stop if the plan collapses into rename theater instead of accountable owner/folder boundary work.

Team scratchpad

1. Role: orchestrator
- Session id: `orch-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: seed the new render folder/file owner structure sprint with reviewer-gated planning and no implementation authority.
- How one task went: confirmed the prior render owner sprint is closed; Ghostty terminal structure shows a deliberate top-level owner list with only a few narrow child folders like `c/`, `kitty/`, `osc/`, and `search/`; current `howl-render/src` still carries structure debt such as an empty `session/` directory and top-level/file placement that is not yet disciplined enough.
- Next handoff or blocker: researcher must produce the full source-backed folder/file structure plan; reviewer gates it before any coding.

2. Role: reviewer
- Session id: `review-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: gate the render folder/file owner structure planning package.
- How one task went: after two narrow planning corrections for exact allowlists and final top-level closure, the reviewer accepted the full planning package. Slice 2 and Slice 3 now have fully explicit allowlists, the final `src/` top-level truth is explicit, the work is real owner/folder-boundary restructuring rather than rename theater, and active artifacts remain current-only.
- Outcome: accept.
- First executable slice expectations: orchestrator may seed only Slice 1 from `research/2026-06-13-render-folder-owner-structure-plan.md:267-325` after closing the planning commit-hash receipt; Slice 1 must keep the coder inside the exact allowlist, require `zig build test:abi` and `zig build test:unit`, preserve ABI exports byte-for-byte, and close a commit-hash receipt before Slice 2 starts.
- Next handoff or blocker: orchestrator should record reviewer acceptance and stop before execution until the planning commit-hash receipt is closed.

3. Role: orchestrator
- Session id: `orch-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: record reviewer acceptance and stop before execution until the planning commit-hash receipt is closed.
- How one task went: active sprint, research, and current index now mark the render folder/file owner structure plan as reviewer-accepted with planning commit-hash receipt pending; no coder is authorized because the receipt is not closed.
- Next handoff or blocker: commit the accepted planning package, then seed only Slice 1.

Condensed execution contract for coder session `coder-2026-06-13-render-folder-structure-01`:

- Accepted planning input:
  - `/home/home/personal/projects/howl/research/2026-06-13-render-folder-owner-structure-plan.md`
- Implement only Slice 1, `C boundary quarantine`.
- Allowed files:
  - `howl-render/src/libhowl_render.zig`
  - `howl-render/src/text_session.zig`
  - `howl-render/src/handle.zig`
  - `howl-render/src/surface_geometry.zig`
  - `howl-render/src/prepare_request.zig`
  - `howl-render/src/prepared_surface.zig`
  - `howl-render/src/submission.zig`
  - `howl-render/src/work_state.zig`
  - `howl-render/src/test_support.zig`
  - `howl-render/src/text_session_test.zig`
  - `howl-render/src/surface_geometry_test.zig`
  - `howl-render/src/prepare_request_test.zig`
  - `howl-render/src/prepared_surface_test.zig`
  - `howl-render/src/submission_test.zig`
  - `howl-render/src/render_session.zig`
  - `howl-render/src/surface/handle_test.zig`
  - `howl-render/src/surface/emitter_test.zig`
  - `howl-render/src/c/text_session.zig`
  - `howl-render/src/c/text_session_handle.zig`
  - `howl-render/src/c/surface_geometry.zig`
  - `howl-render/src/c/prepare_request.zig`
  - `howl-render/src/c/prepared_surface.zig`
  - `howl-render/src/c/submission.zig`
  - `howl-render/src/c/work_state.zig`
  - `howl-render/src/c/test_support.zig`
  - `howl-render/src/c/text_session_test.zig`
  - `howl-render/src/c/surface_geometry_test.zig`
  - `howl-render/src/c/prepare_request_test.zig`
  - `howl-render/src/c/prepared_surface_test.zig`
  - `howl-render/src/c/submission_test.zig`
  - `howl-render/src/test_abi.zig`
  - `howl-render/src/test/unit/root.zig`
- Exact required shape:
  - `libhowl_render.zig` must import C shims only from `src/c/`.
  - no top-level C shim files may remain.
  - the old generic top-level `handle.zig` must be gone.
  - `test_support.zig` must move to `src/c/test_support.zig`.
  - the five top-level wrapper-test files must move to sibling files under `src/c/`.
  - `render_session.zig`, `surface/handle_test.zig`, and `surface/emitter_test.zig` must rewire their `test_support.zig` imports to `src/c/test_support.zig`.
  - `test_abi.zig` must import the wrapper-test files from `src/c/` paths only.
  - `test/unit/root.zig` must import `text_session_test.zig` and `submission_test.zig` from `src/c/` paths only.
  - function names and ABI exports must stay byte-for-byte identical.
- Exact tests:
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:abi`
  - `cd /home/home/personal/projects/howl/howl-render && zig build test:unit`
- Exact non-goals:
  - no movement of `render_session.zig`.
  - no movement of `surface/`, `text/`, `geometry/`, or `vt_publication/` owners yet.
  - no ABI symbol additions or removals.
- Exact stop conditions:
  - stop if any build root outside `libhowl_render.zig` requires a new public entrypoint name.
  - stop if any caller outside `howl-render/src` depends on the deleted top-level shim paths directly.
  - stop if reviewer judges the `c/` folder insufficiently source-backed.
- Required handoff:
  - files changed
  - concise implementation summary
  - exact verification run and results
  - blockers or deviations
  - coder session id `coder-2026-06-13-render-folder-structure-01`
  - commit-hash handoff status for orchestrator receipt closure

4. Role: orchestrator
- Session id: `orch-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: close the accepted planning receipt and seed Slice 1 for execution.
- How one task went: the accepted planning package was committed as root `ca6f1c4`, the active sprint/research/current index now record that receipt, and Slice 1 has an exact condensed execution contract with allowed files, required shape, tests, non-goals, stop conditions, and receipt demand.
- Next handoff or blocker: coder may execute only `coder-2026-06-13-render-folder-structure-01`; no broader slice or product-code work is authorized.

2. Role: researcher
- Session id: `research-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: map the current `howl-render/src` owner tree against Ghostty terminal and Alacritty renderer pressure, then write the active folder/file planning artifact with exact slices, tests, stop conditions, and receipt demands.
- How one task went: proved that the top-level tree is mixing real owners with C shims, that `session/` is empty, and that `damage/`, `prepare/`, `storage/`, and `renderable_content/` are the weak folder boundaries; wrote the active plan requiring a Ghostty-style `c/` folder, keeping only `geometry/`, `surface/`, `text/`, and `vt_publication/` as render subdomains, and deleting the dead/weak folders.
- Next handoff or blocker: reviewer should gate `research/2026-06-13-render-folder-owner-structure-plan.md`; no implementation is authorized until that review accepts the full slice plan.

3. Role: researcher
- Session id: `research-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: correct the reviewer rejection by re-reading the proof roots and top-level helper/wrapper-test files, then tighten Slice 1 and close final top-level tree truth with no coder invention.
- How one task went: confirmed that `test_abi.zig`, `test/unit/root.zig`, and `test_support.zig` all still depend on the top-level C-wrapper files; updated the active plan so Slice 1 explicitly covers moving `test_support.zig` plus all five wrapper-test owners into `src/c/`, and the final tree now names their exact end locations.
- Next handoff or blocker: reviewer should re-gate `research/2026-06-13-render-folder-owner-structure-plan.md`; implementation is still not authorized.

4. Role: researcher
- Session id: `research-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: correct the second reviewer rejection by replacing the remaining Slice 2 and Slice 3 wildcard/conditional allowlists with exact file lists only.
- How one task went: re-proved the Slice 2 import consumers of the moved VT-ingress owners (`render_session.zig`, `benchmark_main.zig`, `text/direct_normal.zig`, `text/surface_preparer.zig`, and `text/shape/cluster.zig`) and replaced the prior wildcard language; also replaced the Slice 3 conditional closure language with an explicit final-closure file list.
- Next handoff or blocker: reviewer should re-gate `research/2026-06-13-render-folder-owner-structure-plan.md`; implementation remains unauthorized.

5. Role: researcher
- Session id: `research-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: correct the execution-exposed Slice 1 planning defect by re-proving every current `test_support.zig` import user and expanding the Slice 1 allowlist only where relocation forces rewires.
- How one task went: confirmed the previously planned wrapper tests were not the whole set because `render_session.zig`, `surface/handle_test.zig`, and `surface/emitter_test.zig` also import `test_support.zig`; updated Slice 1 so those files are now explicitly allowed and their required import rewires are stated directly.
- Next handoff or blocker: reviewer should re-gate `research/2026-06-13-render-folder-owner-structure-plan.md`; implementation remains unauthorized.

3. Role: reviewer
- Session id: `review-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: review the active render folder/file owner-structure plan against the workflow contract, current source, and the seeded sprint direction.
- How one task went: rejected the plan because it does not fully close the current top-level test-owner and tree-closure consequences, so a coder would still have to invent file moves and final `src/` contents.
- Verdict: reject
- Findings:
  1. `research/2026-06-13-render-folder-owner-structure-plan.md:268-285` under-specifies Slice 1 allowed files. The plan moves every top-level C shim into `src/c/`, but current ABI and unit proof roots still import the wrapper-test owners `text_session_test.zig`, `surface_geometry_test.zig`, `prepare_request_test.zig`, `prepared_surface_test.zig`, and `submission_test.zig` from the top level (`howl-render/src/test_abi.zig:4-11`, `howl-render/src/test/unit/root.zig:1-12`). Those test owners are not listed for Slice 1 even though the move necessarily forces them to move or be rewritten. This violates `loop/flow.md:25-30` because the slice does not give exact allowed files or exact required shape for the consequences it triggers.
  2. `research/2026-06-13-render-folder-owner-structure-plan.md:366-370` claims the final top-level `src/` set is only curated roots plus `c/`, `geometry/`, `surface/`, `text/`, `vt_publication/`, and `test/`, but current top-level also contains `test_support.zig` and the five wrapper-test owners (`howl-render/src` directory listing; `howl-render/src/test_abi.zig:4-11`; `howl-render/src/test/unit/root.zig:1-12`). The slices never specify whether those files remain top-level, move under `c/`, move under `test/`, or are deleted after import rewiring. That leaves final tree closure to coder invention and breaks the planning completion gate in `loop/flow.md:24-41`.
- Next handoff or blocker: researcher must correct the plan so every existing top-level wrapper-test and support owner has an exact slice, allowed-file list, final location, and proof-root rewiring path before review can accept planning.

4. Role: reviewer
- Session id: `review-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: re-review the corrected active render folder/file owner-structure plan against the prior findings and the full planning gate.
- How one task went: confirmed the prior two findings are corrected, but the plan still leaves later-slice allowed-file sets open-ended enough that a coder would have to reconstruct scope during execution.
- Verdict: reject
- Findings:
  1. `research/2026-06-13-render-folder-owner-structure-plan.md:336-354` still leaves Slice 2 without an exact allowed-file list. The line `any tests whose imports must be rewired because of those moves only` is not an exact file contract. `loop/flow.md:25-30` requires exact allowed files, and this wording leaves the coder to discover and choose the touched proof roots during execution.
  2. `research/2026-06-13-render-folder-owner-structure-plan.md:382-389` still leaves Slice 3 without an exact allowed-file list. The line `any files from Slices 1-2 only if import-root closure still requires them` is open-ended scope reconstruction, not a bounded execution contract. That fails the planning completion gate in `loop/flow.md:24-41` and the reviewer standard in `loop/reviewer.md:37-43`.
  3. The earlier rejection points are now closed: Slice 1 explicitly covers `test_support.zig`, the five wrapper-test owners, `test_abi.zig`, and `test/unit/root.zig` (`research/2026-06-13-render-folder-owner-structure-plan.md:276-314`), and the final top-level tree truth now explicitly says those helper/test owners leave the top level and live under `src/c/` (`research/2026-06-13-render-folder-owner-structure-plan.md:390-403`). Those fixes are good, but the plan is still not acceptable until Slices 2 and 3 stop using open-ended file allowances.
- Next handoff or blocker: researcher must replace the open-ended Slice 2 and Slice 3 file allowances with exact file lists, including every proof root or moved owner that may still be touched for import closure, before planning can be accepted and seeded for coding.

5. Role: reviewer
- Session id: `review-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: re-review the corrected active render folder/file owner-structure plan for the remaining explicit-allowlist blockers and the full planning gate.
- How one task went: confirmed the remaining Slice 2 and Slice 3 blockers are corrected and the active planning package is now explicit enough to seed execution without coder invention.
- Verdict: accept
- Findings:
  1. Slice 2 now has a fully explicit allowlist and import-closure contract. The moved owner files plus each proven import consumer are named directly, with no wildcard or conditional wording left (`research/2026-06-13-render-folder-owner-structure-plan.md:336-361`).
  2. Slice 3 now has a fully explicit allowlist and final top-level closure contract. The plan names the exact surviving top-level files, exact surviving top-level folders, the exact `src/c/` destinations for `test_support.zig` and all five wrapper-test owners, and the exact files allowed for final closure (`research/2026-06-13-render-folder-owner-structure-plan.md:384-451`).
  3. The full planning gate now passes: the active artifacts are current-only (`loops/`, `research/`, and `sprints/` each contain one active artifact plus archive folders), the plan remains planning-only with no coding authorization before acceptance (`sprints/current.txt:31-40`, `loops/render-folder-owner-structure-live-loop.txt:32-35`), the slices stay focused on owner/folder boundary work rather than rename theater (`research/2026-06-13-render-folder-owner-structure-plan.md:356-361`, `research/2026-06-13-render-folder-owner-structure-plan.md:426-439`), and each slice carries exact allowed files, required shape, tests, non-goals, stop conditions, session ids, and commit-hash receipt demands (`research/2026-06-13-render-folder-owner-structure-plan.md:267-451`).
- Next handoff or blocker: orchestrator may seed Slice 1 only. First executable slice expectations: use the Slice 1 contract exactly as written in `research/2026-06-13-render-folder-owner-structure-plan.md:267-325`, keep work inside that allowlist, require `zig build test:abi` and `zig build test:unit`, preserve ABI exports byte-for-byte, and require a commit-hash receipt before Slice 2 starts.

6. Role: coder
- Session id: `coder-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: execute Slice 1 exactly as seeded by moving the top-level C shim and wrapper-test owners under `src/c/`, rewiring the curated roots, and proving the slice with `zig build test:abi` plus `zig build test:unit`.
- How one task went: before editing, I re-proved the explicit stop surface and found the Slice 1 allowlist is incomplete for the required move of `test_support.zig`. Current unit-proof consumers outside the allowlist still import the top-level helper directly: `howl-render/src/render_session.zig:892` imports `@import("test_support.zig")`, `howl-render/src/surface/handle_test.zig:11` imports `@import("../test_support.zig")`, and `howl-render/src/surface/emitter_test.zig:12` imports `@import("../test_support.zig")`. Deleting or moving `howl-render/src/test_support.zig` without updating those files would break `zig build test:unit`, but those files are not authorized in the Slice 1 allowlist.
- Verification: not run. No code edit was made because the seeded Slice 1 contract cannot satisfy both the required shape and the required test targets without touching non-allowed files.
- Next handoff or blocker: orchestrator must reseed or escalate. The minimal accountable fix is to amend Slice 1 to include every remaining `test_support.zig` import consumer that must be rewired for `test:unit` closure before coding resumes. Commit-hash handoff status: none; no commit requested and no compliant implementation diff exists.

6. Role: reviewer
- Session id: `review-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: review the Slice 1 planning correction exposed by execution, focusing on exact `test_support.zig` import-user coverage and guarding against new planning drift.
- How one task went: confirmed the corrected Slice 1 allowlist now names every current `test_support.zig` import user that must change for relocation and keeps the correction bounded to reseeding Slice 1 only.
- Verdict: accept
- Findings:
  1. Slice 1 now exactly covers every current `test_support.zig` import user that relocation forces to change. Current users are `text_session_test.zig`, `surface_geometry_test.zig`, `prepare_request_test.zig`, `prepared_surface_test.zig`, `submission_test.zig`, `render_session.zig`, `surface/handle_test.zig`, and `surface/emitter_test.zig` (`howl-render/src/text_session_test.zig:2`, `howl-render/src/surface_geometry_test.zig:2`, `howl-render/src/prepare_request_test.zig:2`, `howl-render/src/prepared_surface_test.zig:2`, `howl-render/src/submission_test.zig:2`, `howl-render/src/render_session.zig:892`, `howl-render/src/surface/handle_test.zig:11`, `howl-render/src/surface/emitter_test.zig:12`). The corrected allowlist now includes the previously missing import users `render_session.zig`, `surface/handle_test.zig`, and `surface/emitter_test.zig`, alongside the already-listed wrapper-test owners (`research/2026-06-13-render-folder-owner-structure-plan.md:276-318`).
  2. No new planning vagueness or scope drift was introduced. The correction stays inside Slice 1, adds exact files plus exact rewiring requirements for those files, and does not broaden authorization into unrelated owner moves or later slices (`research/2026-06-13-render-folder-owner-structure-plan.md:276-330`).
  3. This remains a planning correction only. The artifact still frames the change as Slice 1 repair for reseeding, not as broader implementation authorization; later slices and broader product work remain unchanged and unauthorized until orchestrator reseeds the corrected Slice 1 contract.
- Next handoff or blocker: orchestrator may reseed Slice 1 and may reuse the current no-product-change worktree state because the coder reported no code edits, no verification run, and no commit handoff.

7. Role: orchestrator
- Session id: `orch-2026-06-13-render-folder-structure-01`
- Hello: hi
- Intended task: reseed Slice 1 after the accepted planning correction for missing `test_support.zig` import users.
- How one task went: the live Slice 1 contract now matches the accepted research allowlist and required rewires for `render_session.zig`, `surface/handle_test.zig`, and `surface/emitter_test.zig`, so execution can resume without forcing out-of-scope edits.
- Next handoff or blocker: commit the accepted planning correction, then rerun Slice 1 under the refreshed contract.
