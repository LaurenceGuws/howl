# No Active Slice

Loop:

- Host pacing lane

Sprint:

- Align host wake / pacing ownership with Alacritty where source-backed and keep retained render/present ownership intact.

Scope source:

- `research/cache-2026-06-07-pacing-worker-ready-scratchpad.md`

Execution state:

- This file is the current-contract file for the pacing lane.
- Research and hostile review are complete for the initial pacing slices.
- `Slice B` accepted.
- `Slice A` accepted.
- No worker slice is active.

Loop rules:

- Follow the worker/coder loop from `loop.txt` exactly: accepted research, then worker, then hostile review, then orchestrator acceptance.
- Workers do not design.
- Reviewers assume the researcher and worker both tried to be lazy.
- Do not broaden beyond the accepted pacing scratchpad without a new researcher-backed correction.
- Do not touch retained render/present ownership in this lane.
- Do not redefine `pty_pump.Outcome.keep` in this lane.
- Every accepted slice needs a receipt with orchestrator, reviewer, and coder/worker session ids.
- If pre-implementation review rejects a slice, record the correcting researcher session id before worker seeding resumes.

Accepted slice receipt:

- Slice:
  - `Slice B`: atomic cross-thread SDL wake coalescing
- Files changed:
  - `howl-linux-host/src/event_loop.zig`
- Verification:
  - `zig build test:unit --summary all`
- Researcher correction session id:
  - `ses_15d706406ffe3DzK7XbbPdCUyk`
- Reviewer session id:
  - `ses_15d4c6c81ffe3EtWfrgTglOY83`
- Coder/worker session id:
  - `ses_15d4e1711ffeSW9UL5Ba9H23Jm`

Accepted slice receipt:

- Slice:
  - `Slice A`: per-terminal continuation admission plus active-tab input admission fix
- Files changed:
  - `howl-linux-host/src/terminal/context.zig`
  - `howl-linux-host/src/app/processor.zig`
  - `howl-linux-host/src/terminal/context_test.zig`
- Verification:
  - `zig build test:unit --summary all`
- Reviewer session id:
  - `ses_15c73d29cffeYAedDUNN6EtjWD`
- Coder/worker session id:
  - `ses_15c773036ffeI1i9IE651EQKuL`

Lane state:

- Initial pacing slices complete.
- Further pacing work requires a new promoted slice from the accepted scratchpad or new researcher-backed correction.

Research cache:

- `research/cache-2026-06-07-pacing-worker-ready-scratchpad.md`

Reviewer enforcement highlights:

- Reject if `computeLoopAdmission` still clears terminal-input admission.
- Reject if continuation after `Outcome.keep=true` still depends on sticky input admission or a fresh wait-thread wake.
- Reject if there is more than one live `Context.driveProgress` API shape.
- Reject if wake coalescing state lives outside `EventLoop`.
