Howl Render Cleanup Loop

Orchestrator session id: `orch-2026-06-18-render-cleanup-accountability-01`.

Problem statement:

- The last sprint was called done while `howl-render/src/vt_surface` still existed. That was not done. That was a closure failure.
- Howl is agent-made, not vibe-coded. Slop can exist as a byproduct of hard motion, but preserved slop is failure. We clean it with pride, eagerness, and receipts.
- The team exists for Howl: eat, sleep, shit, dream, code, compile, theorize, strategize, and review for Howl only. The product is still young, but the standard is not young.
- TigerBeetle is our discipline source today. The ambition is that Howl becomes strong enough that future agents clone it into context for biasing with the same seriousness.

Loop phase:

- Active planning and cleanup accountability.
- Next researcher/reviewer work must target already-landed render slop, not new features.
- Coding is allowed only after the active plan gives exact files, forbidden tokens, deletion targets, tests, search gates, stop conditions, and receipt fields.

Current cleanup seed:

- Renderer `vt_surface` residue cleanup is implemented but not yet committed at this loop seed.
- The cleanup moves cursor presentation data to `howl-render/src/text/cursor_presentation.zig`, repoints text contract/scene imports, deletes tracked `howl-render/src/vt_surface/*`, and removes the empty directory.

Next planning target:

- Inventory `howl-render` for dead code, bucket nouns, fake lifecycle nouns, stale reference names, and breached folder ownership.
- Start with cleanup of already-landed work, especially names such as `contract`, `session`, `owner`, `pipeline`, `context`, `state`, `options`, `info`, `data`, and `result` where not ABI-forced or domain-true.
- Preserve real concepts. `text` is real, but scope creep must move sideways into shallow exact owners such as `color/`, `effects/`, `metrics/`, `scene/`, or `cursor/` only when source proves those nouns.

Non-negotiables:

- No generic shared bucket files or folders.
- No symbols named `owner` as a substitute for real nouns.
- No direct terminal-reference product names or comments such as source-project ownership labels unless they are in research citations.
- No compatibility aliases, bridges, fallback names, or aggregate re-export roots to make deletion feel easier.
- Test-only helpers in product files require loud banners and owner-local tests; they must not become public substitutes.

Verification seed:

- Whole-package negative searches must cover package roots, not hand-picked files.
- Passing tests are not enough when stale directories, stale names, or stale comments remain.

Loop notes:

- Orchestrator `orch-2026-06-18-render-cleanup-accountability-01`: hello. I am seeding this loop because the user was right to be angry that `vt_surface` survived after I claimed the sprint was done. Intent: make cleanup and deletion a first-class Howl discipline, not a reluctant afterthought. Outcome: closed VT maturity artifacts are archived, active sprint guidance points here, and the next team has a direct emotional and technical mandate. Handoff: commit the completed render `vt_surface` residue cleanup, then researcher/reviewer must plan the broader render cleanup inventory before more feature work.
- Researcher `research-2026-06-18-render-cleanup-inventory-01`: hello. Intent: inventory `howl-render/src` and `howl-render/include` for already-landed cleanup debt without implementing or touching git. Outcome: read the mandatory live surface and TigerBeetle references, inspected render package files/search gates, and updated `research/howl-render-cleanup-plan.md` with current-code findings, dead-code candidates, duplicate evidence, ordered cleanup plan, and the first executable ABI mirror deletion slice. Handoff: reviewer should pressure the research package, especially cursor folder approval, `TextSessionOwner` naming, and whether fallback-font limit duplication is ABI-forced.
- Reviewer `review-2026-06-18-render-cleanup-metric-gate-01`: hello. Intent: gate the render cleanup research package against the user's deterministic `style.py` pressure, project law, and the active workflow without implementing product code. Outcome: rejected the package because `research/howl-render-cleanup-plan.md` does not integrate the `style.py` baseline/top-offender evidence or make metric-backed closure gates explicit; the proposed ABI cursor cadence mirror deletion is a real cleanup slice but cannot be accepted as the first executable slice until the plan proves why that boundary deletion legitimately precedes the metric-dominant files and pins the next metric-driven slices. Handoff: researcher must add deterministic metric evidence, exact reproduction commands/output shape, metric stop conditions, and a corrected ordered plan before coder execution.
- Researcher `research-2026-06-18-render-cleanup-correction-02`: hello. Intent: correct the rejected render cleanup plan using the user's requirement that `style.py` is pressure, not complete proof. Outcome: reread the active accountability surface, role docs, project law, TigerBeetle references, current render sources, `style.py`, and package-root scans; rewrote `research/howl-render-cleanup-plan.md` around separate style, forbidden-token, reachability, and duplicate-shape lanes; kept ABI cursor cadence mirror deletion as a first safe closure slice only with exact ABI size/offset proof and forced next-slice gates for the metric/token-dominant wounds. Handoff: reviewer should re-gate the corrected plan against the deterministic lanes and especially the decision to allow the small ABI closure slice before the dead-code and metric-dominant cleanup slices.
- Reviewer `review-2026-06-18-render-cleanup-re-gate-02`: hello. Intent: re-gate the corrected render cleanup plan without implementing product code or committing. Outcome: rejected the plan because the first slice's required package-root negative search for `HostCursorCadence` cannot pass while `cursor_presentation.zig` and `render_session.zig` intentionally retain those live names, and because the first slice does not require before/after forbidden-token lane evidence despite the plan's own four-lane rule. Handoff: researcher must narrow the mirror-deletion negative search to the local C mirror or explicitly classify retained internal cursor names, and must add forbidden-token before/after receipts for the first slice before worker execution.
- Researcher `research-2026-06-18-render-cleanup-gate-repair-03`: hello. Intent: repair only the rejected first-slice gates and receipts without implementing product code. Outcome: narrowed the impossible `HostCursorCadence` search to prove deletion of the hand-written `src/c/text_session.zig` ABI mirror and stale `text_session.*` references while explicitly allowing still-live cursor/render-session owner names; added required before/after forbidden-token evidence for allowed files, package-root content totals, and package-root path-scan totals. Handoff: reviewer should re-gate the corrected first executable slice for worker readiness.
- Reviewer `review-2026-06-18-render-cleanup-re-gate-03`: hello. Intent: re-gate the repaired render cleanup plan for first-slice worker readiness without implementing product code or committing. Outcome: accepted the corrected planning package; the stale mirror negative searches are now possible, retained live `HostCursorCadence` names are explicitly classified, before/after forbidden-token evidence is required for allowed files and package-root totals, ABI size/offset proof and verification gates are exact, and next-slice forcing gates prevent the sprint from dodging metric/token wounds after the safe closure slice. Handoff: orchestrator may seed the ABI cursor cadence mirror deletion closure slice for execution, preserving the required receipt fields and commit-hash handoff.
- Coder `coder-2026-06-18-render-cleanup-abi-cadence-01`: hello. Intent: implement only reviewer-accepted first executable slice `review-2026-06-18-render-cleanup-re-gate-03` for ABI cursor cadence mirror deletion closure. Outcome: changed `howl-render/src/c/text_session.zig` and `howl-render/src/test_abi.zig`; did not change `howl-render/src/c/text_session_test.zig`; deleted the hand-written `HostCursorCadenceRect` and `HostCursorCadence` extern mirrors from the C text-session boundary, changed `setCursorCadence` and its local cadence test to use `c.HowlRenderHostCursorCadence` directly, preserved all existing cadence validation and render-session copy behavior, and added exact ABI size/offset proof for `c.HowlRenderHostCursorTrailRect` and `c.HowlRenderHostCursorCadence`. Evidence: pre-change stale mirror searches found only `src/c/text_session.zig` declarations/local uses and no `text_session.HostCursorCadence*` external uses; post-change stale mirror searches returned no mirror declaration matches, no `text_session.HostCursorCadence*` matches, no local deleted-type parameter/test matches, and canonical ABI-name matches only in `include/howl_render.h`, direct C use in `src/c/text_session.zig`, and layout proof in `src/test_abi.zig`. Metric receipt: allowed files before `(sum)` lines 226 prod 132 proof 76 usizes 6 long_funcs 0 structs_top_level 2 bucket_named_structs 0 bucket_struct_lines 0; after `(sum)` lines 226 prod 105 proof 103 usizes 31 long_funcs 0 structs_top_level 0 bucket_named_structs 0 bucket_struct_lines 0; `src/c/text_session.zig` before lines 161 prod 130 structs_top_level 2, after lines 132 prod 103 structs_top_level 0; `src/test_abi.zig` before lines 65 proof 59 usizes 4, after lines 94 proof 86 usizes 29. Forbidden-token receipt: allowed-file line counts unchanged by token (`session` 37, `owner` 16, `state` 2, `config` 5, `result` 1, `support` 4); package-root content totals unchanged (`contract` 51 files/1177 lines, `session` 33/707, `owner` 22/269, `pipeline` 0/0, `context` 3/71, `state` 21/396, `options` 8/138, `config` 20/202, `info` 9/48, `data` 3/26, `result` 19/139, `manager` 0/0, `controller` 0/0, `types` 0/0, `utils` 0/0, `helper` 1/1, `support` 20/270); package-root path scan unchanged with 12 paths: `howl-render/src/c/test_support.zig`, `howl-render/src/c/text_session.zig`, `howl-render/src/c/text_session_handle.zig`, `howl-render/src/c/text_session_test.zig`, `howl-render/src/c/work_state.zig`, `howl-render/src/geometry_contract.zig`, `howl-render/src/render_session.zig`, `howl-render/src/text/contract.zig`, `howl-render/src/text/ft_hb/support.zig`, `howl-render/src/text/ft_hb/support_test.zig`, `howl-render/src/text/scene_contract.zig`, `howl-render/src/text/session.zig`. Verification passed: `zig fmt howl-render/src/c/text_session.zig howl-render/src/test_abi.zig`; `zig build test:abi` in `howl-render`; `zig build test:unit` in `howl-render`; `zig build check` in `howl-render`; root `zig build check`. Blockers/deviations: none; command shapes matched the plan; git was not touched; untracked root `temp.md` and `temp.txt` were not touched. Handoff: commit hash remains open for orchestrator receipt closure because coder was instructed not to touch git.
- Reviewer `review-2026-06-18-render-cleanup-abi-cadence-01`: hello. Intent: review the implemented ABI cursor cadence mirror deletion slice against the accepted plan, actual root/render diffs, stale-mirror searches, metrics/token receipts, and verification gates without implementing product code or committing. Outcome: accepted the slice; the implementation diff is confined to `howl-render/src/c/text_session.zig` and `howl-render/src/test_abi.zig`, the root diff adds only coder/reviewer loop notes, the hand-written C-boundary mirror structs are deleted, `setCursorCadence` uses `c.HowlRenderHostCursorCadence` directly, broader live `HostCursorCadence` names are not renamed, exact cursor cadence ABI size/offset proof exists in `src/test_abi.zig`, stale-mirror searches return the planned results, after-metrics match the coder receipt, path/token receipts are recorded, and `zig build test:abi`, `zig build test:unit`, render `zig build check`, and root `zig build check` pass. Handoff: orchestrator may close acceptance and commit-hash receipt; root untracked `temp.md` and `temp.txt` remain outside this slice and were not reviewed as implementation changes.
- Orchestrator `orch-2026-06-18-render-cleanup-accountability-01`: hello. Intent: close the accepted ABI cursor cadence mirror deletion slice with independent verification and commit receipt. Outcome: reran `howl-render` `zig build test:abi`, `zig build test:unit`, `zig build check`, and root `zig build check`; all passed. Committed accepted product slice as `howl-render` `864ea8b Use C cursor cadence ABI directly`. Handoff: root pointer/receipt commit closes this slice; next planning must follow the metric/token forcing gates instead of adding feature work.
- Researcher `research-2026-06-18-render-cleanup-next-slice-04`: hello. Intent: promote the next exact executable cleanup slice after accepted ABI cursor cadence closure without implementing product code. Outcome: reread the active accountability surface and TigerBeetle references, re-proved current style/token/reachability evidence, and updated `research/howl-render-cleanup-plan.md` plus `sprints/current.txt` to target dead/self-test-only deletions in `tokens.zig`, `submitted_surface.zig`, `text/effects.zig`, and the unused `BackendCaps` re-export in `text/contract.zig`. Handoff: reviewer can gate this next-slice package against the deletion searches, metrics, forbidden-token gates, allowed files, non-goals, and receipt fields now recorded.
- Reviewer `review-2026-06-18-render-cleanup-next-slice-01`: hello. Intent: gate the promoted dead/self-test-only render shape deletion plan for worker readiness without implementing product code or committing. Outcome: accepted the next-slice package; current source reachability confines `PreparedSurfaceTokenWith`, `RenderResult`, `FullPrepareReason`, `SurfaceLostReason`, `LatestMailbox`, `SubmitDecision`, `SubmittedWorkState`, and `BackendCaps` to the claimed declarations, dependent dead declarations, local self-tests, and the single unused re-export; the allowed files, deletion searches, metric/token gates, required tests, non-goals, stop conditions, and receipt fields are exact enough for execution. Handoff: orchestrator may seed the worker with `sprints/current.txt` and `research/howl-render-cleanup-plan.md`; root untracked `temp.md` and `temp.txt` remain outside scope.
- Coder `coder-2026-06-18-render-cleanup-dead-shape-01`: hello. Intent: implement only reviewer-accepted next executable slice `review-2026-06-18-render-cleanup-next-slice-01` for dead/self-test-only render shape deletion. Outcome: changed `howl-render/src/tokens.zig`, `howl-render/src/submitted_surface.zig`, `howl-render/src/text/effects.zig`, `howl-render/src/text/contract.zig`, and this loop note only; deleted `ThreadMutex`, `lockMutex`, `PreparedSurfaceTokenWith`, `RenderResult`, `FullPrepareReason`, `SurfaceLostReason`, `LatestMailbox`, `SubmitDecision`, `SubmittedWorkState`, `SubmittedSurface.workState`, `SubmittedSurface.fullPrepareReason`, `BackendCaps`, the unused `BackendCaps` re-export, and only the local self-tests for those deleted shapes; preserved live retained-base validation and submitted-token behavior. Evidence: before deletion searches found only the planned declarations, dependent dead methods, re-export, and self-tests; after `rg -n '\b(PreparedSurfaceTokenWith|RenderResult|FullPrepareReason|SurfaceLostReason|LatestMailbox)\b' howl-render/src howl-render/include`, `rg -n '\b(SubmitDecision|SubmittedWorkState|BackendCaps)\b' howl-render/src howl-render/include`, `rg -n '\bfullPrepareReason\b|\.workState\(' howl-render/src/submitted_surface.zig`, `rg -n '\b(ThreadMutex|lockMutex)\b' howl-render/src/tokens.zig`, and `rg -n 'pub const BackendCaps = effects\.BackendCaps' howl-render/src/text/contract.zig` all returned no matches. Metric receipt: whole-package `(sum)` before lines 24159 prod 13489 proof 7168 funcs 1120 structs_top_level 207, after lines 23995 prod 13388 proof 7133 funcs 1114 structs_top_level 204; allowed-files `(sum)` before lines 517 prod 305 proof 134 funcs 19 structs_top_level 9, after lines 353 prod 204 proof 99 funcs 13 structs_top_level 6; `tokens.zig` after lines 110 prod 56 proof 35 funcs 5 structs_top_level 4; `submitted_surface.zig` after lines 126 prod 58 proof 49 funcs 8 structs_top_level 2; `text/effects.zig` after lines 40 prod 26 proof 9 funcs 0 structs_top_level 0; `text/contract.zig` after lines 77 prod 64 proof 6 funcs 0 structs_top_level 0. Forbidden-token receipt: allowed-file matches dropped with no increase (`tokens.zig` now only `state:1`, `result:0`; `submitted_surface.zig` now `owner:9`, `state:4`; `text/effects.zig` now `config:0`; `text/contract.zig` lost the `BackendCaps` re-export line while retaining deferred contract pressure); package-root path scan stayed at 12 files; after package-root changed-token totals included `owner 22 files/265 lines`, `state 21/391`, `config 19/200`, and `result 18/137`. Verification: `zig build test:unit`, `zig build test:abi`, and `zig build check` in `howl-render` passed with no output; root `zig build check` passed with no output. Blockers/deviations: none; no ABI/header changes, no moves, no new folders, no compatibility aliases, no git operations, and root untracked `temp.md`/`temp.txt` were not touched. Handoff: commit hash remains open for orchestrator because coder was instructed not to commit.
- Reviewer `review-2026-06-18-render-cleanup-dead-shape-01`: hello. Intent: review the implemented dead/self-test-only render shape deletion slice against the accepted contract, actual root/render diffs, deletion searches, metric/token receipts, and verification gates without implementing product code or committing. Outcome: accepted the slice; the render diff is confined to `src/tokens.zig`, `src/submitted_surface.zig`, `src/text/effects.zig`, and `src/text/contract.zig`, the root diff adds only coder/reviewer loop notes, all targeted dead/self-test-only symbols and the unused `BackendCaps` re-export are gone, live retained-base validation and submitted-token behavior remain, no ABI/header/build/README files changed, metrics decreased as required, forbidden-token/path receipts have no blocking increase, and `zig build test:unit`, `zig build test:abi`, render `zig build check`, and root `zig build check` pass. Handoff: orchestrator may close acceptance and commit-hash receipt; root untracked `temp.md` and `temp.txt` remain outside this slice and were not reviewed as implementation changes.
- Orchestrator `orch-2026-06-18-render-cleanup-accountability-01`: hello. Intent: close the accepted dead/self-test-only render shape deletion slice with independent verification and commit receipt. Outcome: reran `howl-render` `zig build test:unit`, `zig build test:abi`, `zig build check`, and root `zig build check`; all passed. Committed accepted product slice as `howl-render` `7af878b Delete dead render shapes`. Handoff: root pointer/receipt commit closes this slice; next planning remains metric/token/dead-code forced.
