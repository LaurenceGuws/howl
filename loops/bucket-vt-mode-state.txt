# Active Slice

Loop:

- Bucket-struct cleanup lane

Slice:

- vt mode state 1

Status:

- Accepted after hostile review; pending receipt/commit

Reviewer acceptance:

- reviewer session `ses_15e2d5158ffeXevj3asCer4QJ9`

Research authority:

- orchestrator lane plan in `project-memory.md` under `2026-06-07 Zig Hygiene Refocus`
- VT lane audit classified this as the first recommended narrow VT fix with exact Ghostty pressure

Allowed files:

- `howl-vt/src/control/mode.zig`
- `howl-vt/src/terminal.zig`

Required shape:

- In `howl-vt/src/control/mode.zig`, rename `pub const State = struct` to `pub const ModeState = struct`.
- Keep every field, field default, declaration order, helper function, test, and behavior in `howl-vt/src/control/mode.zig` unchanged.
- Do not introduce methods, aliases, wrappers, or compatibility re-exports.
- In `howl-vt/src/terminal.zig`, update the `modes` field type from `TerminalModeNs.State` to `TerminalModeNs.ModeState`.
- Keep `const TerminalModeNs = mode;` unchanged unless the compiler forces a strictly mechanical rename.
- Do not touch `howl-vt/src/action/route.zig`.
- This remains an internal owner-name correction only; no C ABI or public FFI surface changes.

Non-goals:

- no terminal mode behavior changes
- no broader VT control flow redesign
- no unrelated VT cleanup
- no changes to DEC/ANSI mode semantics, save/restore behavior, or tests beyond mechanical rename fallout
- no refactor of `DecView`, `AnsiView`, `SavedDecMode`, or any report/apply flow

Stop conditions:

- stop if any file beyond `howl-vt/src/control/mode.zig` and `howl-vt/src/terminal.zig` needs editing
- stop if the rename requires a compatibility alias or any behavior change
- stop if current source reveals an external/public consumer of `mode.State` outside `terminal.zig`
- stop if the worker cannot keep the change strictly mechanical and rename-sized

Verification:

- `python utils/hygene/style_scan.py "howl-vt/src/control/mode.zig" "howl-vt/src/terminal.zig"`
- `zig build test && zig build check` in `howl-vt`
- grep gate: no `pub const State = struct` in `howl-vt/src/control/mode.zig`
- grep gate: no `TerminalModeNs.State` in `howl-vt/src/terminal.zig`

Receipt requirement:

- record orchestrator session id
- record reviewer session id
- record coder/worker session id
