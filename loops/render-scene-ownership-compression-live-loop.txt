Render scene ownership compression live loop scratchpad

Date: 2026-06-13.

Role owner: orchestrator.

Status: Slice 3 accepted in nested render repo; root receipt pending.

Orchestrator session id: `orch-2026-06-13-render-scene-01`.

Researcher session id: `research-2026-06-13-render-scene-01`.

Reviewer session id: `review-2026-06-13-render-scene-01`.

Loop commit-hash receipt: root `2fde3dd`.

Shared scratchpad rule:

- This file is the live team scratchpad for the current step.
- Every non-trivial orchestrator, researcher, reviewer, or coder pass must append a short note with role/session id, hello, intent, outcome, and handoff or blocker.

Controlling sprint:

- `/home/home/personal/projects/howl/sprints/2026-06-13-render-scene-ownership-compression-sprint.md`

Active research target:

- `/home/home/personal/projects/howl/research/2026-06-13-render-scene-ownership-compression-plan.md`

Current step:

- The planning package is accepted at root `2fde3dd`.
- Slice 1, `Extract scene damage/span ownership and move callers to it`, is accepted in `howl-render` `959576d`; root `5bbe0ec`.
- Slice 2, `Extract scene rect/fill ownership with explicit undercurl carve-out`, is accepted in `howl-render` `acb929e`; root `007a0ee`.
- Slice 3, `Finish scene.zig as the sprite/raster scene root only`, is accepted in `howl-render` `27317db`.
- Root receipt for Slice 3 is pending orchestrator commit.
- No next execution slice is seeded yet.

Exact user direction:

- Use the next sprint on the render complexity bill.
- Start with `howl-render/src/text/scene.zig` unless references and current source prove otherwise.
- Researcher must pressure the target with reference guidance before execution planning is accepted.

Exact non-goals:

- No emitter cleanup.
- No render-session orchestration redesign.
- No host/runtime changes.
- No ABI/header edits.

Exact stop conditions:

- Stop if `scene.zig` still owns rect synthesis or duplicated damage truth after the slice.
- Stop if curly underline ownership leaves the sprite/raster owner.
- Stop if lane-report proof no longer lines up with the sprite-producing owner.

Latest executed slice contract:

- Allowed files only:
  - `/home/home/personal/projects/howl/howl-render/src/text/scene.zig`
  - `/home/home/personal/projects/howl/howl-render/src/text/scene_damage.zig`
  - `/home/home/personal/projects/howl/howl-render/src/text/scene_rects.zig`
  - `/home/home/personal/projects/howl/howl-render/src/text/surface_preparer.zig`
- Required shape:
  - `scene.zig` must finish as the scene root plus sprite/raster assembly owner only.
  - Keep `BuildOptions`, `OwnedTextScene`, `BorrowedTextScene`, `RetainedScratch`, build entrypoints, `SceneAssembly`, group sprite append, icon span extension, atlas reservation, raster request enqueueing, sprite color policy, and curly underline or undercurl ownership in `scene.zig`.
  - Remove any remaining damage normalization or rect synthesis ownership from `scene.zig`; it must consume `scene_damage.zig` and `scene_rects.zig`.
  - `surface_preparer.buildComplexScene` must still build the complex borrowed scene through the scene root and must still record lane proof from produced sprite draws.
- Required assertions:
  - preserve width and height non-zero assertions before rasterized sprite append
  - preserve sorted-by-`first_cell` assertions at merged scene boundaries
  - preserve lane-report proof alignment with the sprite-producing owner
- Required tests by exact full name filters:
  - `zig build test:unit -- "scene builds ordered sprite draws from groups"`
  - `zig build test:unit -- "scene emits undercurl sprite for curly underline"`
  - `zig build test:unit -- "scene carries group placement offsets into sprite draw"`
  - `zig build test:unit -- "scene extends wide icon groups into available blank cells"`
  - `zig build test:unit -- "scene positions sprite draws by grid columns"`
  - `zig build test:unit -- "scene reuses atlas slots for repeated sprite keys"`
  - `zig build test:unit -- "scene does not request raster for cache hit"`
  - `zig build test:unit -- "borrowed scene reuses retained draw list storage"`
  - `zig build test:unit -- "text scene applies kitty dim opacity at render-time for sprite draws"`
  - `zig build test:unit -- "text preparation scene is grid positioned"`
  - `zig build test:unit -- "text preparation rerasterizes pending atlas entries across prepares"`
  - `zig build test:unit -- "text preparation marks curly underline cells complex before shaping"`
  - `zig build test:unit -- "text preparation prepares publication cells through shared full pipeline surface"`
  - final acceptance sweep: `zig build test:unit`
- Verification handoff requirements:
  - coder must report files changed, concise implementation summary, verification run/results, blockers/deviations, coder session id, and commit-hash handoff status
  - reviewer verdict plus exact findings is required before acceptance

Team scratchpad

1. Role: orchestrator
- Session id: `orch-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: seed the next planning sprint around the render complexity bill with `scene.zig` as the first pressure target.
- How one task went: converted the user-provided ranking into live sprint, research, and loop artifacts; kept execution unauthorized and required reference-first pressure on `howl-render/src/text/scene.zig` before any slice plan can be accepted.
- Next handoff or blocker: researcher must write the full planning package and either prove `scene.zig` is the first cut or prove one ranked fallback seam must preempt it.

2. Role: researcher
- Session id: `research-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: re-prove whether `howl-render/src/text/scene.zig` is the first owner-compression cut and write the full source-backed sprint package.
- How one task went: current source and Alacritty/TigerBeetle pressure both point to `scene.zig` first; the file currently mixes damage/span truth, rect/fill synthesis, and sprite/raster assembly, with real duplication already visible in `cluster.zig` and `direct_scene.zig`.
- Next handoff or blocker: reviewer should gate the attached three-slice plan; no implementation is authorized until the reviewer accepts and the orchestrator seeds an execution slice with coder session id and commit-hash receipt fields.

3. Role: reviewer
- Session id: `review-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: review the active render scene ownership compression planning package for executability, source pressure, and workflow completeness.
- How one task went: rejected the plan because Slice 1 leaves the outward damage-type boundary to coder invention, Slice 2 does not explicitly carve curly underline and undercurl ownership, and the listed test commands are substring filters rather than exact proof obligations.
- Next handoff or blocker: researcher must tighten the slice contracts and replace the broad test filters with exact named proofs before reviewer acceptance.

4. Role: researcher
- Session id: `research-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: repair the rejected render scene ownership plan so the slices are executable without coder invention.
- How one task went: tightened Slice 1 to require caller edits instead of scene-owned damage aliases, proved the exact curly-vs-rect underline split from current source, and replaced broad test filters with exact full proof names including the cluster partial-damage checks.
- Next handoff or blocker: reviewer should re-gate the repaired three-slice package; no implementation is authorized until reviewer acceptance and orchestrator seeding of an execution slice.

5. Role: reviewer
- Session id: `review-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: re-gate the repaired render scene ownership compression planning package against the prior rejection points and workflow contract.
- How one task went: accepted the repaired package; Slice 1 now forbids scene-owned outward damage aliases and requires caller edits, Slice 2 now preserves the exact curly/undercurl sprite carve-out, and the proof obligations are now exact named tests tied to existing owners.
- Next handoff or blocker: orchestrator may mark the planning package accepted, close or carry the open planning commit-hash receipt, and seed the first execution slice with coder session id and commit-hash receipt fields.

6. Role: orchestrator
- Session id: `orch-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: close the accepted planning receipt and seed Slice 1 for execution.
- How one task went: committed the accepted planning package as root `2fde3dd` and condensed Slice 1 into this live-loop execution contract with coder session id `coder-2026-06-13-render-scene-01`.
- Next handoff or blocker: coder should implement Slice 1 only and return verification plus commit-hash handoff status for reviewer gating.

7. Role: coder
- Session id: `coder-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: implement Slice 1 only inside the allowed render files.
- How one task went: added `scene_damage.zig` as the sole damage owner, rebound all allowed callers directly to it, removed duplicated damage filtering logic from `cluster.zig`, and passed all six required exact-name unit proofs.
- Next handoff or blocker: reviewer should gate the Slice 1 diff; no commit hash exists yet because git was intentionally untouched.

8. Role: reviewer
- Session id: `review-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: gate the executed Slice 1 diff against the live loop contract, active planning inputs, and proof obligations.
- How one task went: the seven render files match the Slice 1 ownership move and all six required unit proofs passed on rerun, but the active sprint/current/research artifacts still contradicted the real execution state.
- Next handoff or blocker: orchestrator must repair the active accountability artifacts so they truthfully show Slice 1 seeded, executed, and under review before this slice can be accepted.

9. Role: orchestrator
- Session id: `orch-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: repair active artifact truth after the reviewer found execution-state drift.
- How one task went: updated the active sprint, research, and current index statuses so they now match the real Slice 1 seeded-and-under-review state.
- Next handoff or blocker: reviewer should re-gate the unchanged Slice 1 code now that the accountability surface is truthful again.

10. Role: reviewer
- Session id: `review-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: re-gate the already-implemented Slice 1 after the active artifact truth repair.
- How one task went: accepted Slice 1; active sprint/current/research/loop artifacts now truthfully match the seeded-and-under-review execution state, the damage owner remains fully moved into `scene_damage.zig` with direct caller rebinding, and all six required exact-name unit proofs passed again on rerun.
- Next handoff or blocker: orchestrator may commit Slice 1 and record the commit-hash receipt.

11. Role: orchestrator
- Session id: `orch-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: commit the accepted Slice 1 render work and close the root receipt.
- How one task went: committed the nested `howl-render` Slice 1 diff as `959576d` `Extract scene damage owner` after reviewer acceptance.
- Next handoff or blocker: commit the root receipt for Slice 1, then seed Slice 2 if continuing.

12. Role: orchestrator
- Session id: `orch-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: close the Slice 1 root receipt and seed Slice 2 for execution.
- How one task went: recorded Slice 1 root receipt `5bbe0ec` and condensed Slice 2 into this live-loop execution contract with coder session id `coder-2026-06-13-render-scene-02`.
- Next handoff or blocker: coder should implement Slice 2 only and return verification plus commit-hash handoff status for reviewer gating.

13. Role: coder
- Session id: `coder-2026-06-13-render-scene-02`
- Hello: hi
- Intended task: implement Slice 2 only inside the allowed render files.
- How one task went: added `scene_rects.zig` as the rect owner, kept curly underline and undercurl in `scene.zig`, rebound `direct_scene.zig` and `surface_preparer.zig` to the rect owner, and passed all fifteen required exact-name proofs.
- Next handoff or blocker: reviewer should gate the Slice 2 diff; no commit hash exists yet because git was intentionally untouched.

14. Role: reviewer
- Session id: `review-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: gate the executed Slice 2 diff against the live loop contract, accepted plan, and exact proof obligations.
- How one task went: accepted Slice 2; `scene_rects.zig` now owns rect/fill/cursor/non-curly decoration synthesis, curly underline stayed in `scene.zig`, direct callers were rebound to the rect owner, and all fifteen required exact-name unit proofs passed on rerun.
- Next handoff or blocker: orchestrator may commit Slice 2 and record the commit-hash receipt.

15. Role: orchestrator
- Session id: `orch-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: commit the accepted Slice 2 render work and close the root receipt.
- How one task went: committed the nested `howl-render` Slice 2 diff as `acb929e` `Extract scene rect owner` after reviewer acceptance.
- Next handoff or blocker: commit the root receipt for Slice 2, then seed Slice 3 if continuing.

16. Role: orchestrator
- Session id: `orch-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: close the Slice 2 root receipt and seed Slice 3 for execution.
- How one task went: recorded Slice 2 root receipt `007a0ee` and condensed Slice 3 into this live-loop execution contract with coder session id `coder-2026-06-13-render-scene-03`.
- Next handoff or blocker: coder should implement Slice 3 only and return verification plus commit-hash handoff status for reviewer gating.

17. Role: coder
- Session id: `coder-2026-06-13-render-scene-03`
- Hello: hi
- Intended task: implement Slice 3 only inside the allowed render files.
- How one task went: removed the remaining rect-geometry helper ownership from `scene.zig`, kept sprite/raster and undercurl ownership there, moved shared rect truth consumed by curly assembly into `scene_rects.zig`, and passed all required exact-name proofs plus full `zig build test:unit`.
- Next handoff or blocker: reviewer should gate the Slice 3 diff; no commit hash exists yet because git was intentionally untouched.

18. Role: reviewer
- Session id: `review-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: gate the executed Slice 3 diff against the live loop contract, accepted plan, and proof obligations.
- How one task went: rejected Slice 3 because `scene.zig` still exports rect-owner delegation APIs instead of finishing as the sprite/raster scene root only; full `zig build test:unit` still passed on rerun.
- Next handoff or blocker: coder must remove the remaining rect-delegation exports from `scene.zig`, update any local test call sites to use `scene_rects` directly, and return the corrected diff for re-review.

19. Role: coder
- Session id: `coder-2026-06-13-render-scene-03`
- Hello: hi
- Intended task: repair Slice 3 by deleting the remaining rect-delegation exports from `scene.zig` and rebinding the local test call site directly to `scene_rects`.
- How one task went: removed the five remaining rect-delegation exports from `scene.zig`, updated the local cursor test to call `scene_rects.cursorDraws` directly, and passed the full Slice 3 proof set plus full `zig build test:unit` again.
- Next handoff or blocker: reviewer should re-gate the repaired Slice 3 diff; no commit hash exists yet because git was intentionally untouched.

20. Role: reviewer
- Session id: `review-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: re-gate the repaired Slice 3 diff against the prior rejection point, accepted plan, and live-loop contract.
- How one task went: accepted Slice 3; `scene.zig` no longer exports rect-owner delegation APIs, the local cursor test now calls `scene_rects` directly, sprite/raster and undercurl ownership remain in `scene.zig`, lane-report proof still records produced sprite draws, and rerun proof checks plus full `zig build test:unit` passed.
- Next handoff or blocker: orchestrator may commit Slice 3 and record the commit-hash receipt.

21. Role: orchestrator
- Session id: `orch-2026-06-13-render-scene-01`
- Hello: hi
- Intended task: commit the accepted Slice 3 render work and close the root receipt.
- How one task went: committed the nested `howl-render` Slice 3 diff as `27317db` `Finish scene sprite owner` after reviewer acceptance.
- Next handoff or blocker: commit the root receipt for Slice 3, then close the sprint if no further slice is needed.
