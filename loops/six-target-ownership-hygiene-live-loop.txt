# Six-Target Ownership Hygiene Live Loop

Authority:

- Live planning scratchpad for the six-target ownership and hygiene sprint.
- Orchestrator session id: `orch-2026-06-14-six-target-ownership-01`.
- User handoff supplied 2026-06-14.
- No implementation is authorized until research planning is accepted by reviewer and exact slices are seeded.

Problem statement:

- Evaluate six current post-debug-cleanup pressure targets and distinguish true large owners from false concentration.
- Do not treat line count as guilt.
- A file is only a cut target if current source plus references prove mixed ownership, duplicate truth, fake seams, weak naming, excess `anytype`, or runtime/control flow that belongs elsewhere.
- Preserve reference-backed large-owner shapes where they are real.

Target set:

1. `projects/howl/howl-render/src/text/shape/cluster.zig`
2. `projects/howl/howl-render/src/text/ft_hb/support.zig`
3. `projects/howl/howl-linux-host/src/terminal/surface.zig`
4. `projects/howl/howl-render/src/surface/emitter.zig`
5. `projects/howl/howl-render/src/surface/realizer.zig`
6. `projects/howl/howl-vt/src/parser.zig`

Research contract:

- Researcher session id: `research-2026-06-14-six-target-ownership-01`.
- Reviewer session id: `review-2026-06-14-six-target-ownership-01`.
- Active research output path: `research/2026-06-14-six-target-ownership-hygiene-plan.md`.
- For each target, research must produce:
  - current-code facts with exact line references
  - reference comparison
  - owner judgment: keep large or cut
  - exact reason
  - if cut, exact proposed child-owner path and exact symbol movement
  - required assertions
  - required tests
  - stop conditions
  - sprint ranking
- Research must produce an exact ranked sprint plan and seed the next execution slice with zero coder guesswork.

Reference order and pressure:

- Use Alacritty first for host/runtime and render organization.
- Use Ghostty first for VT/parser shape.
- Use TigerBeetle for simplicity, naming, bounds, assertions, and anti-bucket pressure.
- Existing Howl shape is not authority by itself.
- No coder invention. Every cut must be source-backed.

Likely ranking bias only, not authorization:

1. `howl-render/src/text/ft_hb/support.zig`
2. `howl-render/src/text/shape/cluster.zig`
3. `howl-linux-host/src/terminal/surface.zig`

Hard non-goals:

- No ABI/header churn unless explicitly re-approved.
- No benchmark theater.
- No debug-noise revisit.
- No generic `helpers.zig`, `types.zig`, `manager`, `engine`, or `controller`.
- No split-because-big reasoning.
- No runtime redesign outside the proven target seam.

Hard stop conditions:

- Stop if a target is large but reference-shaped and owner-true.
- Stop if references conflict and require user review.
- Stop if a proposed cut needs invention rather than evidence.
- Stop if the sprint cannot produce exact allowed files, tests, non-goals, and stop conditions for each candidate slice.

Orchestrator note:

- Hello from `orch-2026-06-14-six-target-ownership-01`.
- Intent: seed a planning-only sprint around the six current ownership/hygiene pressure points.
- Outcome: live loop now captures the target set, research outputs, reference order, likely ranking bias, and hard non-goals/stops.
- Handoff: researcher must read required workflow docs, `sprints/current.txt`, this loop, `reference-index.md`, TigerBeetle readings, the six target files, and the governing reference seams before writing the active research package.

- Researcher `research-2026-06-14-six-target-ownership-01`: hello.
- Intent: evaluate the six targets against Ghostty, Alacritty, and TigerBeetle pressure and finish the active research plan with exact slice contracts.
- Outcome: completed the research artifact; judged `howl-render/src/text/ft_hb/support.zig` and `howl-render/src/text/shape/cluster.zig` as real cut targets, and judged `howl-linux-host/src/terminal/surface.zig`, `howl-render/src/surface/emitter.zig`, `howl-render/src/surface/realizer.zig`, and `howl-vt/src/parser.zig` as keep-large owner-true files for this sprint.
- Handoff: reviewer should pressure the proposed Slice 1 `ft_hb/loaded_faces.zig` cut and the Slice 2 `shape/provisional_run.zig` cut against the cited references and stop the sprint if either child owner name or boundary is not source-backed enough.

- Researcher `research-2026-06-14-six-target-ownership-01`: hello again.
- Intent: repair the rejected planning package by tightening Slice 2 into a full contract, narrowing the `loaded_faces` seam to true lifecycle ownership, and closing the parser proof gap without inventing a fake cut.
- Outcome: repaired the plan so Slice 2 now lands in existing `howl-render/src/text/shape/run.zig` with run-local scratch, Slice 1 now limits `loaded_faces.zig` to face lifecycle and face lookup only, and `howl-vt/src/parser.zig` stays keep-large but now has an explicit proof-only Slice 3 for parser-owner-local tests.
- Handoff: reviewer should re-check whether `loaded_faces.zig` is now narrow enough, whether the `run.zig` landing zone removes all coder guesswork about run scratch ownership, and whether the new parser proof slice is sufficient to close the keep-large judgment.

- Reviewer `review-2026-06-14-six-target-ownership-01`: hello.
- Intent: re-review the repaired six-target planning package and gate acceptance only if the repaired seams and full slice contracts survive exact scrutiny.
- Outcome: the prior substantive planning defects are repaired; the only blocker from this pass was the missing reviewer loop note itself.
- Handoff: re-run reviewer gate after this accountability repair. No additional research-plan content changes are required from the previous rejection.

- Reviewer `review-2026-06-14-six-target-ownership-01`: hello again.
- Intent: confirm final planning acceptance after the accountability-only repair.
- Outcome: accepted with no findings. The planning package now has a full execution queue, narrow owner-true cut seams, and a proof-only keep-large parser slice instead of an invented structural cut.
- Handoff: orchestrator may record accepted planning receipt and promote the package into the active sprint contract.

- Orchestrator `orch-2026-06-14-six-target-ownership-01`: hello again.
- Intent: promote accepted planning into the active execution sprint contract without starting coder work yet.
- Outcome: accepted planning receipt `16591b9` is recorded; `research/2026-06-14-six-target-ownership-hygiene-plan.md` moved to `research/done/2026-06-14-six-target-ownership-hygiene-plan.md`; active sprint contract is `sprints/2026-06-14-six-target-ownership-hygiene-sprint.md`.
- Handoff: sprint seed receipt is `bf0da6e` `Seed six-target ownership sprint`. Slice 1 is next to seed for coder `coder-2026-06-14-six-target-ownership-slice-01` when execution is authorized.

- Orchestrator `orch-2026-06-14-six-target-ownership-01`: hello again.
- Intent: seed Slice 1 exactly from the accepted sprint contract.
- Outcome: Slice 1 is authorized for coder `coder-2026-06-14-six-target-ownership-slice-01` only.
- Allowed files: `howl-render/src/text/ft_hb/support.zig`, `howl-render/src/text/ft_hb/loaded_faces.zig`, `howl-render/src/text/ft_hb/support_test.zig`.
- Required shape: add a true child owner in `loaded_faces.zig` for FT/HB primary/fallback face lifecycle and face lookup only; move only the lifecycle and lookup symbols proved by the accepted planning package; keep cached metrics policy, resolve-stage policy, provider cache policy, and shaping-input scratch in `support.zig`; replace mixed ownership with delegation from `support.zig` to the new loaded-face owner; preserve fallback loading, deterministic test fallback, and metrics derivation; no C ABI change, no package-root export change, no host-facing API churn.
- Required tests from `howl-render`: `zig build test:unit`.
- Stop conditions: stop if the cut requires touching files outside the allowed set; stop if the new owner cannot stay limited to face lifecycle and face lookup without pulling policy with it; stop if preserving behavior would require public render-session or ABI churn.
- Required handoff fields: changed files, concise implementation summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.

- Coder `coder-2026-06-14-six-target-ownership-slice-01`: hello.
- Intent: cut FT/HB loaded-face lifecycle and lookup into `loaded_faces.zig` while leaving metrics/cache/resolve policy inside `FtHbSupport`.
- Outcome: extracted primary and fallback face state plus lifecycle/lookup methods into a dedicated child owner and rewired `support.zig` to delegate without changing fallback behavior or metrics derivation inputs.
- Handoff: run `zig build test:unit` in `howl-render`, then hand the diff and verification result to reviewer `review-2026-06-14-six-target-ownership-01`.

- Reviewer `review-2026-06-14-six-target-ownership-01`: hello.
- Intent: review Slice 1 loaded-face owner cut against the accepted sprint contract.
- Outcome: accepted with no findings. `support.zig` kept resolve state, caches, shaping-input scratch, and cached metrics policy, while `loaded_faces.zig` stayed narrow to FT library state, primary/fallback face lifecycle, and face lookup only.
- Handoff: orchestrator should verify `zig build test:unit` in `howl-render`, then record nested and root Slice 1 acceptance receipts.

- Orchestrator `orch-2026-06-14-six-target-ownership-01`: hello again.
- Intent: verify and accept Slice 1 after reviewer acceptance.
- Outcome: `zig build test:unit` passed from `howl-render`. Nested render commit is `e952bf1` `Extract loaded face owner`.
- Handoff: root acceptance receipt is `eab184d` `Accept six-target ownership Slice 1`; Slice 2 is next.

- Orchestrator `orch-2026-06-14-six-target-ownership-01`: hello again.
- Intent: seed Slice 2 exactly from the accepted sprint contract.
- Outcome: Slice 2 is authorized for coder `coder-2026-06-14-six-target-ownership-slice-02` only.
- Allowed files: `howl-render/src/text/shape/cluster.zig`, `howl-render/src/text/shape/run.zig`.
- Required shape: do not create a new owner file; move provisional run planning from `cluster.zig` into `run.zig`; rename `OwnedRuns` to `OwnedProvisionalRuns`; move `buildProvisionalRuns`, `buildProvisionalRunsScratch`, and `resolvedRun` into `run.zig`; add run-local scratch ownership in `run.zig` as `RetainedProvisionalRunScratch` and remove run scratch storage from `cluster.RetainedScratch`; leave `cluster.zig` owning text-cache assembly, renderable assembly, cluster extraction, and complex selection only; no C ABI change, no package-root export change, no render-session policy change.
- Required tests from `howl-render`: `zig build test:unit`.
- Additional proof obligations: preserve or relocate the current run proofs from `cluster.zig:867-892` and `cluster.zig:913-946` into owner-true inline tests after the move; preserve or add run-scratch overflow proof under the new run-local scratch owner.
- Stop conditions: stop if the slice cannot remove `ResolvedRun` scratch ownership from `cluster.RetainedScratch` cleanly; stop if the move requires touching files outside the allowed set; stop if the only viable outcome is inventing a new file instead of using `run.zig`; stop if moved tests cannot remain reachable through the existing render unit root without adding a new test root.
- Required handoff fields: changed files, concise implementation summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.

- Coder `coder-2026-06-14-six-target-ownership-slice-02`: hello.
- Intent: move provisional run planning and run scratch ownership out of `cluster.zig` and into the existing run owner `run.zig` without changing shaping behavior.
- Outcome: provisional run planning now lives in `run.zig` under `OwnedProvisionalRuns` and `RetainedProvisionalRunScratch`; `cluster.RetainedScratch` no longer stores resolved-run scratch; the run behavior proofs moved to run-owner-local inline tests.
- Handoff: run `zig build test:unit` in `howl-render`, then hand the diff and verification result to reviewer `review-2026-06-14-six-target-ownership-01`.

- Reviewer `review-2026-06-14-six-target-ownership-01`: hello again.
- Intent: review Slice 2 cluster run-planning move into the existing run owner.
- Outcome: accepted with no findings. The seam lands in `run.zig`, `cluster.RetainedScratch` no longer owns run scratch truth, and the moved run proofs remain owner-true inline tests under `run.zig`.
- Handoff: orchestrator should verify `zig build test:unit` in `howl-render`, then record nested and root Slice 2 acceptance receipts.

- Orchestrator `orch-2026-06-14-six-target-ownership-01`: hello again.
- Intent: verify and accept Slice 2 after reviewer acceptance.
- Outcome: `zig build test:unit` passed from `howl-render`. Nested render commit is `927f032` `Move provisional runs into run owner`.
- Handoff: root acceptance receipt is `4c59cdd` `Accept six-target ownership Slice 2`; Slice 3 is next.

- Orchestrator `orch-2026-06-14-six-target-ownership-01`: hello again.
- Intent: seed Slice 3 exactly from the accepted sprint contract.
- Outcome: Slice 3 is authorized for coder `coder-2026-06-14-six-target-ownership-slice-03` only.
- Allowed files: `howl-vt/src/parser.zig`.
- Required shape: keep `parser.zig` structurally large and owner-true; add parser-owner-local inline tests in `parser.zig` proving the control spine directly; do not cut new parser child files; do not move UTF-8 or string-control logic back into `parser.zig`; use inline tests only so the VT package keeps its current curated test root shape.
- Required tests from `howl-vt`: `zig build test:unit`.
- Minimum proof coverage: exit/transition/entry phase ordering; active-control exclusivity; CSI parameter and separator assembly; DCS hook boundary behavior.
- Stop conditions: stop if direct parser-owner-local proof cannot be added within `parser.zig`; stop if the slice requires touching files outside the allowed set; stop if the work drifts from proof into parser architecture redesign.
- Required handoff fields: changed files, concise implementation summary, verification commands/results, blockers/deviations, coder session id, commit-hash handoff status for orchestrator receipt closure.
