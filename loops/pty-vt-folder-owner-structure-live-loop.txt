PTY VT folder owner structure live loop scratchpad

Date: 2026-06-13.

Role owner: orchestrator.

Status: active planning loop.

Orchestrator session id: `orch-2026-06-13-pty-vt-folder-structure-01`.

Researcher session id: `research-2026-06-13-pty-vt-folder-structure-01`.

Reviewer session id: `review-2026-06-13-pty-vt-folder-structure-01`.

Loop commit-hash receipt: root `efbd9ad`.

Shared scratchpad rule:

- This file is the live team scratchpad for the current step.
- Every non-trivial orchestrator, researcher, reviewer, or coder pass must append a short note with role/session id, hello, intent, one outcome, and next handoff or blocker.

Controlling sprint:

- `/home/home/personal/projects/howl/sprints/2026-06-13-pty-vt-folder-owner-structure-sprint.md`

Active research target:

- `/home/home/personal/projects/howl/research/2026-06-13-pty-vt-folder-owner-structure-plan.md`

Current step:

- Slice 3, `VT terminal core root establishment`, is accepted and receipted.
- The remaining Slice 4 and Slice 5 plan is researcher-repaired and reviewer-accepted after the explicit final no-`terminal/` override.
- Slice 4 may be seeded after the plan repair commit receipt is recorded.
- User explicitly overrode the final VT folder shape: Slice 5 must delete `src/terminal/` and move all contents back under `src/` because the `terminal/` wrapper adds one folder depth for no reason.

Exact user direction:

- PTY and VT are the next cleanup target, together.
- PTY is small enough to combine with VT in one sprint.
- Clean up file and folder locations and naming with discipline and intent.
- Keep only curated owner units under `src`.
- Use shallow child folders only for true subdomains or per-owner definitions.
- Explicit override: final VT shape must not retain `src/terminal/`; move its contents back under `src/` in Slice 5.

Exact non-goals:

- No implementation during planning.
- No sprint narrowing, v1 language, or partial cleanup unless the user explicitly narrows scope.
- No Howl-only folder inventions while Ghostty, Alacritty, TigerBeetle, Kitty, or official docs provide source-backed pressure.
- No fake compatibility wrappers or preserving dead directories for comfort.

Exact stop conditions:

- Stop if the active research cannot name exact allowed files, required shapes, tests, non-goals, stop conditions, and receipt fields for each slice.
- Stop if current source and references conflict in a way that needs explicit user override.
- Stop if active `loops/`, `research/`, or `sprints/` contain stale non-archive artifacts.
- Stop if the plan collapses into rename theater instead of accountable owner/folder boundary work.

Team scratchpad

1. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: seed the new combined PTY+VT folder/file owner structure sprint with reviewer-gated planning and no implementation authority.
- How one task went: confirmed the host folder sprint is closed; current `howl-pty/src` and `howl-vt/src` still show likely folder/file placement debt compared to Ghostty's deliberate `terminal/` and `termio/` structure; PTY is small enough that planning PTY and VT together is accountable and still source-backed.
- Next handoff or blocker: researcher must produce the full source-backed PTY+VT folder/file structure plan; reviewer gates it before any coding.

2. Role: researcher
- Session id: `research-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: write the active PTY+VT folder-owner planning artifact from current source plus Ghostty, Alacritty, and TigerBeetle pressure.
- How one task went: completed the active research artifact with a compact anchor map, target PTY and VT shapes, and five sequential slices covering PTY test-root relocation, VT proof-root relocation, VT `src/terminal/` consolidation, and final dead-path cleanup.
- Next handoff or blocker: reviewer should gate `research/2026-06-13-pty-vt-folder-owner-structure-plan.md`; no coding is authorized until the reviewer accepts the slice plan.

3. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate the active PTY+VT folder/file owner structure plan before any implementation authorization.
- How one task went: rejected the first draft because VT unit proof files under `src/**/*_test.zig` would have survived the curated-product-owner rule and later slices did not explicitly allow proof import updates after terminal-core and protocol moves; repaired the plan to move all VT unit proof roots to `test/unit/` in Slice 2 and require later test import updates directly against moved owner paths.
- Next handoff or blocker: repaired plan is ready for acceptance review; if accepted, orchestrator may commit the planning package and seed Slice 1 only.

4. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: re-gate the repaired PTY+VT folder/file owner structure plan.
- How one task went: accepted the repaired plan; Slice 1 is bounded to PTY proof-root relocation, and later VT slices now explicitly remove unit proofs from `src` and require direct proof import updates after owner moves.
- Next handoff or blocker: orchestrator must commit the accepted planning package, record the commit-hash receipt, and only then seed Slice 1.

5. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: record the accepted PTY+VT planning package commit-hash receipt.
- How one task went: committed the accepted planning package at root `efbd9ad`.
- Next handoff or blocker: commit this receipt, then seed Slice 1 for PTY proof-root relocation only.

## Slice 1 Execution Contract

Name: PTY proof-root relocation out of `src`.

Status: active.

Coder session id: `coder-2026-06-13-pty-vt-folder-structure-slice-1`.

Reviewer session id: `review-2026-06-13-pty-vt-folder-structure-01`.

Commit-hash receipt demand: required on acceptance.

Allowed files:

- `howl-pty/build.zig`
- `howl-pty/src/test_unit.zig`
- `howl-pty/src/test_integration.zig`
- `howl-pty/src/test/abi.zig`
- `howl-pty/src/test/ffi.zig`
- `howl-pty/src/session_test.zig`
- `howl-pty/src/pty_test.zig`
- `howl-pty/src/session_integration_test.zig`
- `howl-pty/src/pty_integration_test.zig`
- `howl-pty/src/pty/posix_test.zig`
- `howl-pty/src/pty/pty_test.zig`
- `howl-pty/test_unit.zig`
- `howl-pty/test_integration.zig`
- `howl-pty/test_abi.zig`
- `howl-pty/test/**`

Required shape:

- `howl-pty/src` keeps only PTY product owners and the true PTY platform subdomain.
- All PTY proof roots move to `howl-pty/test/` outside `src`.
- PTY build roots point to package-root `test_unit.zig`, `test_integration.zig`, and `test_abi.zig`, because Zig 0.16 rejects direct `../src` imports when the module root is `test/`.
- Package-root proof entrypoints load the actual test bodies and ABI proof files under `test/`.
- ABI support helpers also move out of `src`.

Exact tests:

- `zig build test:unit` in `/home/home/personal/projects/howl/howl-pty`
- `zig build test:integration` in `/home/home/personal/projects/howl/howl-pty`
- `zig build test:abi` in `/home/home/personal/projects/howl/howl-pty`

Non-goals:

- No PTY transport logic changes.
- No ABI enum or symbol changes.
- No PTY owner renames beyond proof-root relocation.
- No VT changes.

Stop conditions:

- Stop if any PTY test file remains under `src`.
- Stop if build roots still point at `src` proof files.
- Stop if product logic changes beyond import-path churn are required.

7. Role: coder
- Session id: `coder-2026-06-13-pty-vt-folder-structure-slice-1`
- Hello: hi
- Intended task: implement Slice 1 PTY proof-root relocation.
- How one task went: moving proof files under `test/` exposed a Zig 0.16 module-boundary fact: `test/unit.zig` as a module root cannot import `../src/...`, and overlapping named modules collide when source owners import each other relatively.
- Next handoff or blocker: active contract is corrected to use package-root proof entrypoints that import test bodies under `test/`; continue implementation without moving proofs back into `src`.

8. Role: coder
- Session id: `coder-2026-06-13-pty-vt-folder-structure-slice-1`
- Hello: hi
- Intended task: finish and verify Slice 1 PTY proof-root relocation.
- How one task went: moved PTY proof files out of `src`, repointed build roots to package-root proof entrypoints, removed empty `src/test`, confirmed no `howl-pty/src/**/*_test.zig` or `src/test*.zig` files remain, and passed `zig build test:unit`, `zig build test:integration`, and `zig build test:abi` in `howl-pty`.
- Next handoff or blocker: reviewer should gate the Slice 1 diff; if accepted, commit `howl-pty` and then record nested/root receipts.

9. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate Slice 1 PTY proof-root relocation after verification.
- How one task went: accepted Slice 1; the staged diff was proof-root relocation plus `build.zig` root repointing only, `howl-pty/src` contains product owners only, and required PTY gates passed.
- Next handoff or blocker: nested `howl-pty` commit is `13b7fbd`; orchestrator must commit the root receipt.

10. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: record Slice 1 nested commit receipt.
- How one task went: committed `howl-pty` Slice 1 as `13b7fbd`.
- Next handoff or blocker: commit the root receipt for Slice 1 before seeding Slice 2.

11. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: record Slice 1 root commit receipt.
- How one task went: committed the root Slice 1 receipt as `2718455`.
- Next handoff or blocker: commit this receipt update, then seed Slice 2 if continuing.

## Slice 2 Execution Contract

Name: VT proof and non-product roots out of `src`.

Status: active.

Coder session id: `coder-2026-06-13-pty-vt-folder-structure-slice-2`.

Reviewer session id: `review-2026-06-13-pty-vt-folder-structure-01`.

Commit-hash receipt demand: required on acceptance.

Allowed files:

- `howl-vt/build.zig`
- `howl-vt/src/ffi.zig`
- `howl-vt/src/libhowl_vt.zig`
- `howl-vt/src/howl_vt.zig`
- `howl-vt/src/terminal.zig`
- `howl-vt/src/test/abi.zig`
- `howl-vt/src/test/screen_capture.zig`
- `howl-vt/src/test/stream_harness.zig`
- `howl-vt/src/test/pty_feed_record.zig`
- `howl-vt/src/test/terminal_benchmark.zig`
- `howl-vt/src/terminal_benchmark_main.zig`
- `howl-vt/src/simulation/**`
- `howl-vt/src/ffi/**`
- `howl-vt/src/**/*_test.zig`
- `howl-vt/test_unit.zig`
- `howl-vt/test_abi.zig`
- `howl-vt/test_ffi.zig`
- `howl-vt/benchmark_m7_baseline.zig`
- `howl-vt/test/**`
- `howl-vt/benchmark/**`
- `howl-vt/simulation/**`

Required shape:

- `src/ffi.zig` becomes `src/ffi/main.zig`; `ffi` exists in one place only.
- VT unit test root moves from `src/howl_vt.zig` to a proof root outside `src`; `src/howl_vt.zig` stops importing unit proof files.
- Inline terminal tests move from `src/terminal.zig` to `test/unit/terminal_test.zig`, because source files cannot import proof helpers outside the source module path.
- All VT unit proof files move from `src/**/*_test.zig` to `test/unit/` with owner-shaped subfolders preserved.
- VT ABI test root moves to `test/abi.zig`.
- Benchmark entrypoint and helpers move to `benchmark/`.
- Simulation entrypoint and assets move to `simulation/`.
- Package-root `test_unit.zig`, `test_abi.zig`, `test_ffi.zig`, and `benchmark_m7_baseline.zig` are build entrypoints required by Zig 0.16 module-boundary enforcement; the actual proof, benchmark, and simulation bodies stay under their owner folders.
- `src` no longer contains proof, benchmark, or simulation-only roots.

Exact tests:

- `zig build test:abi` in `/home/home/personal/projects/howl/howl-vt`
- `zig build test:unit:build` in `/home/home/personal/projects/howl/howl-vt`
- `zig build simulate:build` in `/home/home/personal/projects/howl/howl-vt`
- `zig build benchmark:m7_baseline:build` in `/home/home/personal/projects/howl/howl-vt`

Non-goals:

- No VT semantic changes.
- No terminal-core folder moves yet beyond `ffi` root cleanup.
- No new benchmark features or simulation features.
- No PTY changes.

Stop conditions:

- Stop if any VT `*_test.zig` file remains under `src`.
- Stop if any benchmark or simulation file remains under `src`.
- Stop if both `src/ffi.zig` and `src/ffi/main.zig` survive together.
- Stop if build step names or shipped library outputs change.

12. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: seed Slice 2 for VT proof and non-product roots out of `src`.
- How one task went: wrote the active execution contract with exact files, required shape, tests, non-goals, stop conditions, session ids, and receipt demand.
- Next handoff or blocker: coder may implement Slice 2 only.

13. Role: coder
- Session id: `coder-2026-06-13-pty-vt-folder-structure-slice-2`
- Hello: hi
- Intended task: implement and verify Slice 2 VT proof and non-product relocation.
- How one task went: moved VT unit proofs, ABI proof, support helpers, benchmark, simulation, and `ffi` root out of weak `src` locations; collapsed `src/ffi.zig` to `src/ffi/main.zig`; moved inline terminal tests to `test/unit/terminal_test.zig`; added package-root build entrypoints required by Zig module boundaries; passed `zig build test:abi`, `zig build test:unit:build`, `zig build simulate:build`, and `zig build benchmark:m7_baseline:build` in `howl-vt`.
- Next handoff or blocker: reviewer should gate the Slice 2 diff; if accepted, commit `howl-vt` and record nested/root receipts.

14. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate Slice 2 VT proof and non-product relocation after verification.
- How one task went: accepted Slice 2; the diff is proof, benchmark, simulation, and FFI-root relocation plus required package-root build entrypoints, stale `src` proof paths are gone, and all required VT gates passed.
- Next handoff or blocker: nested `howl-vt` commit is `556ab69`; orchestrator must commit the root receipt.

15. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: record Slice 2 nested commit receipt.
- How one task went: committed `howl-vt` Slice 2 as `556ab69`.
- Next handoff or blocker: commit the root receipt for Slice 2 before seeding Slice 3.

16. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: record Slice 2 root commit receipt.
- How one task went: committed the root Slice 2 receipt as `921f630`.
- Next handoff or blocker: commit this receipt update, then seed Slice 3 if continuing.

## Slice 3 Execution Contract

Name: VT terminal core root establishment.

Status: reviewer-accepted after researcher correction; coding may resume from this corrected contract.

Coder session id: `coder-2026-06-13-pty-vt-folder-structure-slice-3`.

Reviewer session id: `review-2026-06-13-pty-vt-folder-structure-01`.

Commit-hash receipt demand: required on acceptance.

Allowed files:

- `howl-vt/src/howl_vt.zig`
- `howl-vt/src/terminal.zig`
- `howl-vt/src/screen.zig`
- `howl-vt/src/screen_set.zig`
- `howl-vt/src/stream_terminal.zig`
- `howl-vt/src/parser/main.zig`
- `howl-vt/src/parser/**`
- `howl-vt/src/screen/**`
- `howl-vt/src/selection/**`
- `howl-vt/src/surface/publication.zig`
- `howl-vt/src/ffi/**`
- `howl-vt/src/action/route.zig`
- `howl-vt/src/action/vocabulary.zig`
- `howl-vt/src/host/apply.zig`
- `howl-vt/src/host/state.zig`
- `howl-vt/src/control/report.zig`
- `howl-vt/src/control/osc_color.zig`
- `howl-vt/src/xterm/osc.zig`
- `howl-vt/src/xterm/dcs.zig`
- `howl-vt/src/xterm/csi/params.zig`
- `howl-vt/src/terminal/**`
- `howl-vt/test/unit.zig`
- `howl-vt/test/unit/**`
- `howl-vt/test/support/**`
- `howl-vt/benchmark/terminal_benchmark.zig`
- `howl-vt/benchmark/pty_feed_record.zig`

Required shape:

- Create `src/terminal/main.zig` as the curated VT-core root.
- Move terminal aggregate owner, screen owners, parser owner, stream owner, selection owner, point owner, and publication owner into `src/terminal/`.
- Preserve snake_case file names.
- Keep `screen/` and `parser/` as shallow per-owner definition folders under `src/terminal/`.
- Update `src/howl_vt.zig` to import VT core through `src/terminal/`, not package-top owner files.
- Update every current direct import of moved terminal-core paths in the same slice, including `src/ffi/**`, `src/action/{route,vocabulary}.zig`, `src/host/{apply,state}.zig`, `src/control/{report,osc_color}.zig`, `src/xterm/{osc,dcs}.zig`, `src/xterm/csi/params.zig`, `test/unit/**`, `test/support/**`, and the two benchmark files named above.
- Dependent product, proof, support, and benchmark files may receive import-path edits only; no protocol-owner absorption, ABI behavior change, benchmark behavior change, or test semantic change is authorized in Slice 3.
- Tests, proof helpers, and benchmark files must follow moved owner paths directly and must not rely on package-top compatibility shims.

Exact tests:

- `zig build test:unit` in `/home/home/personal/projects/howl/howl-vt`
- `zig build test:abi:build` in `/home/home/personal/projects/howl/howl-vt`
- `zig build benchmark:m7_baseline:build` in `/home/home/personal/projects/howl/howl-vt`

Non-goals:

- No protocol-owner consolidation yet for `action`, `control`, `xterm`, `kitty`, or `host`.
- No behavior rewrites.
- No PTY changes.
- No C ABI symbol, enum, struct, or exported-name changes.
- No benchmark or proof behavior changes beyond import-path updates.

Stop conditions:

- Stop if terminal aggregate ownership is still split between top-level and `src/terminal/`.
- Stop if old top-level owner files remain as compatibility shims after acceptance.
- Stop if the coder needs to invent a second curated VT-core root.
- Stop if any product, ABI/FFI, benchmark, support, or unit proof file still imports old terminal-core source paths after acceptance.
- Stop if any allowed `src/ffi/**`, `src/action/**`, `src/host/**`, `src/control/**`, or `src/xterm/**` edit changes behavior instead of only correcting imports required by the terminal-core move.
- Stop if `zig build test:abi:build` fails from stale ABI/FFI imports after terminal-core paths move.
- Stop if current-source proof finds a direct moved-path import in a file not named here; return to research instead of guessing or adding a shim.

17. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: seed Slice 3 for VT terminal-core root establishment.
- How one task went: wrote the active execution contract with exact files, required shape, tests, non-goals, stop conditions, session ids, and receipt demand.
- Next handoff or blocker: coder may implement Slice 3 only.

6. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: seed Slice 1 for PTY proof-root relocation.
- How one task went: wrote the active execution contract with exact files, required shape, tests, non-goals, stop conditions, session ids, and receipt demand.
- Next handoff or blocker: coder may implement Slice 1 only.

18. Role: coder
- Session id: `coder-2026-06-13-pty-vt-folder-structure-slice-3`
- Hello: hi
- Intended task: implement Slice 3 VT terminal-core root establishment only within the allowed file list.
- How one task went: stopped before product edits because moving `terminal.zig`, `screen.zig`, `screen_set.zig`, `stream_terminal.zig`, `parser/`, `screen/`, `selection/`, and `surface/publication.zig` under `src/terminal/` would leave disallowed source files importing deleted package-top paths, including `src/ffi/**`, `src/host/**`, `src/action/**`, `src/control/**`, and `src/xterm/**`; `zig build test:abi:build` is part of the Slice 3 proof and cannot be expected to build with those stale imports.
- Next handoff or blocker: orchestrator/reviewer must repair the Slice 3 allowed file list or split the contract so all required import updates are authorized; no product files were edited.

19. Role: researcher
- Session id: `research-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: correct the blocked Slice 3 execution plan from current-source import proof without implementing product code.
- How one task went: re-proved that `src/ffi/**`, `src/action/{route,vocabulary}.zig`, `src/host/{apply,state}.zig`, `src/control/{report,osc_color}.zig`, `src/xterm/{osc,dcs}.zig`, `src/xterm/csi/params.zig`, `test/unit/**`, `test/support/**`, and the two benchmark files directly import terminal-core paths that move under `src/terminal/`; corrected the research artifact and this loop contract to authorize import-path updates only in those files.
- Next handoff or blocker: reviewer should gate the corrected Slice 3 contract; coding must not resume until reviewer acceptance because this researcher pass does not authorize execution.

20. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate the researcher-corrected Slice 3 execution contract after the coder import-owner blocker.
- How one task went: accepted the corrected Slice 3 contract; current-source import proof matches the expanded allowed import-update files, dependent `src/ffi/**`, selected `src/action/**`, `src/host/**`, `src/control/**`, `src/xterm/**`, `test/unit/**`, `test/support/**`, and benchmark edits are constrained to import-path churn only, and the contract forbids protocol-owner absorption, compatibility shims, ABI behavior changes, benchmark behavior changes, and proof semantic changes.
- Next handoff or blocker: orchestrator may reseed/resume Slice 3 from this corrected live-loop contract; coder must stop and return to research if a direct moved terminal-core import is found in any file not named by the contract.

21. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: resume Slice 3 from the reviewer-accepted corrected execution contract.
- How one task went: marked the corrected Slice 3 contract reviewer-accepted and ready for coder execution after the import-owner blocker repair.
- Next handoff or blocker: coder may implement Slice 3 from the corrected live-loop contract only.

22. Role: coder
- Session id: `coder-2026-06-13-pty-vt-folder-structure-slice-3`
- Hello: hi
- Intended task: implement corrected Slice 3 VT terminal-core root establishment only within the reviewer-accepted allowed file list.
- How one task went: moved terminal aggregate, screen, screen set, stream, parser, selection, and publication owners under `howl-vt/src/terminal/`, added `src/terminal/main.zig` as the curated VT-core root, repointed allowed dependent imports directly to moved paths, confirmed no stale moved-path imports outside `src/terminal/**`, and passed the required unit, ABI build, and benchmark build proofs.
- Next handoff or blocker: reviewer should gate the Slice 3 diff; no commit was made, so commit-hash receipt remains pending for orchestrator acceptance closure.

23. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate corrected Slice 3 implementation after coder verification.
- How one task went: accepted Slice 3; `src/terminal/main.zig` is the curated VT-core root, old moved top-level owner paths are absent, stale moved-path imports outside `src/terminal/**` are clean, dependent allowed files show import-path churn only, and required proofs passed.
- Next handoff or blocker: orchestrator may commit nested `howl-vt` and record receipts.

24. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: verify and commit accepted Slice 3.
- How one task went: reran `zig build test:unit`, `zig build test:abi:build`, and `zig build benchmark:m7_baseline:build` in `howl-vt`; committed nested Slice 3 as `20fb714`.
- Next handoff or blocker: commit the root receipt for Slice 3 before seeding Slice 4.

25. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: record Slice 3 root commit receipt.
- How one task went: committed the root Slice 3 receipt as `4a4fec4`.
- Next handoff or blocker: commit this receipt update, then seed Slice 4 if continuing.

23. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate the corrected Slice 3 VT terminal-core root establishment diff after coder verification.
- How one task went: accepted the Slice 3 diff; `src/terminal/main.zig` is present, moved VT-core owners are under `src/terminal/`, old package-top moved owner paths are gone, dependent product/proof/support/benchmark changes are import-path churn only, stale moved-path imports outside `src/terminal/**` are clean, and `zig build test:unit`, `zig build test:abi:build`, and `zig build benchmark:m7_baseline:build` passed locally.
- Next handoff or blocker: orchestrator may commit nested `howl-vt` for Slice 3 and then record the nested/root commit receipts; no product blocker remains from review.

26. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: record explicit user override for the final VT folder shape and stop coding until the remaining plan is repaired.
- How one task went: recorded the user decision that Slice 5 must delete `src/terminal/` and move all contents back under `src/`, overriding Ghostty's `terminal/` subdomain pressure because the extra folder depth is rejected for Howl.
- Next handoff or blocker: researcher must repair remaining Slice 4 and Slice 5 from current source and the explicit override; reviewer must gate the repair before coding resumes.

27. Role: researcher
- Session id: `research-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: repair remaining Slice 4 and Slice 5 after the explicit user override.
- How one task went: re-proved current post-Slice-3 `howl-vt` shape, preserved the explicit override receipt, repaired Slice 4 to move protocol buckets directly toward final `src/` paths, repaired Slice 5 to delete `src/terminal/` and move contents back under `src/`, then repaired reviewer findings by removing stale final proof buckets and broad dependency globs.
- Next handoff or blocker: reviewer should gate the repaired remaining plan; no coding is authorized until acceptance.

28. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate repaired remaining Slice 4 and Slice 5 plan after explicit override.
- How one task went: rejected the first repair because final proof folders still preserved deleted `action/`, `control/`, and `xterm/` buckets and allowed dependency globs were too broad; accepted the second repair after proof paths became owner-true and broad dependency globs were removed.
- Next handoff or blocker: orchestrator must commit the accepted remaining-plan repair and record the receipt before seeding Slice 4.

## Slice 4 Execution Contract

Name: VT protocol bucket removal toward final no-wrapper layout.

Status: active.

Coder session id: `coder-2026-06-13-pty-vt-folder-structure-slice-4`.

Reviewer session id: `review-2026-06-13-pty-vt-folder-structure-01`.

Commit-hash receipt demand: required on acceptance.

Allowed files:

- `howl-vt/src/howl_vt.zig`
- `howl-vt/src/action/**`
- `howl-vt/src/control/**`
- `howl-vt/src/xterm/**`
- `howl-vt/src/route.zig`
- `howl-vt/src/vocabulary.zig`
- `howl-vt/src/mode.zig`
- `howl-vt/src/report.zig`
- `howl-vt/src/locator.zig`
- `howl-vt/src/osc_color.zig`
- `howl-vt/src/c0.zig`
- `howl-vt/src/esc.zig`
- `howl-vt/src/csi.zig`
- `howl-vt/src/osc.zig`
- `howl-vt/src/dcs.zig`
- `howl-vt/src/csi/**`
- `howl-vt/src/host/apply.zig`
- `howl-vt/src/host/state.zig`
- `howl-vt/src/kitty/apply.zig`
- `howl-vt/src/kitty/color.zig`
- `howl-vt/src/kitty/protocol.zig`
- `howl-vt/src/input/encode.zig`
- `howl-vt/src/terminal/terminal.zig`
- `howl-vt/src/terminal/stream_terminal.zig`
- `howl-vt/src/terminal/screen.zig`
- `howl-vt/src/terminal/screen/apply.zig`
- `howl-vt/src/terminal/screen/cursor.zig`
- `howl-vt/src/terminal/screen/erase.zig`
- `howl-vt/src/terminal/screen/rect.zig`
- `howl-vt/test/unit.zig`
- `howl-vt/test/unit/action/route_test.zig`
- `howl-vt/test/unit/route_test.zig`
- `howl-vt/test/unit/control/report_test.zig`
- `howl-vt/test/unit/report_test.zig`
- `howl-vt/test/unit/xterm/csi_mapping_test.zig`
- `howl-vt/test/unit/csi_mapping_test.zig`
- `howl-vt/test/unit/terminal_modes_test.zig`
- `howl-vt/test/unit/screen_test.zig`
- `howl-vt/test/unit/screen/cursor_test.zig`
- `howl-vt/test/unit/screen/history_test.zig`
- `howl-vt/test/unit/screen/resize_test.zig`
- `howl-vt/test/unit/screen/tabs_test.zig`
- `howl-vt/test/unit/screen/write_test.zig`

Required shape:

- Do not move protocol or consequence folders under `src/terminal/` temporarily; the override makes that throwaway work.
- Remove package-top `action/`, `control/`, and `xterm/` buckets.
- Move `src/action/route.zig` to `src/route.zig` and `src/action/vocabulary.zig` to `src/vocabulary.zig`.
- Move `src/control/mode.zig`, `src/control/report.zig`, `src/control/locator.zig`, and `src/control/osc_color.zig` to direct `src/{mode,report,locator,osc_color}.zig` owners.
- Move `src/xterm/c0.zig`, `src/xterm/esc.zig`, `src/xterm/csi.zig`, `src/xterm/osc.zig`, and `src/xterm/dcs.zig` to direct `src/{c0,esc,csi,osc,dcs}.zig` owners.
- Move `src/xterm/csi/**` to `src/csi/**`; retain `src/csi/` only as the CSI helper subdomain.
- Keep `src/host/` and `src/kitty/` as final shallow true subdomains; do not move them under `src/terminal/`.
- Keep `src/terminal/**` terminal-core files in place for Slice 4 except for import-path updates required by moved protocol owners.
- Update product and proof imports in the allowed files directly to the new paths.
- Rename proof files to remove stale proof buckets: `test/unit/action/route_test.zig` to `test/unit/route_test.zig`, `test/unit/control/report_test.zig` to `test/unit/report_test.zig`, and `test/unit/xterm/csi_mapping_test.zig` to `test/unit/csi_mapping_test.zig`.
- No package-top compatibility mirrors, re-export shims, or duplicate old/new owner paths may survive acceptance.

Exact tests:

- `zig build test:unit` in `/home/home/personal/projects/howl/howl-vt`
- `zig build test:abi` in `/home/home/personal/projects/howl/howl-vt`
- `zig build benchmark:m7_baseline:build` in `/home/home/personal/projects/howl/howl-vt`

Non-goals:

- No deletion of `src/terminal/` in Slice 4.
- No movement of terminal-core files out of `src/terminal/` in Slice 4.
- No host runtime architecture change.
- No new public Zig API.
- No convenience wrapper layer.
- No C ABI symbol, enum, struct, or exported-name changes.
- No proof or benchmark behavior changes beyond import-path updates.

Stop conditions:

- Stop if any `howl-vt/src/action/`, `howl-vt/src/control/`, or `howl-vt/src/xterm/` path remains after acceptance.
- Stop if any `howl-vt/test/unit/action/`, `howl-vt/test/unit/control/`, or `howl-vt/test/unit/xterm/` proof bucket remains after acceptance.
- Stop if `src/host/` or `src/kitty/` is moved under `src/terminal/`.
- Stop if `src/terminal/**` is deleted or moved in Slice 4 instead of saved for Slice 5.
- Stop if imported paths depend on compatibility mirrors.
- Stop if any unit proof still imports old `action/`, `control/`, or `xterm/` source paths after acceptance.
- Stop if current-source proof finds a direct old protocol-bucket import in a file not named here; return to research instead of guessing or adding a shim.

27. Role: researcher
- Session id: `research-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: repair the remaining Slice 4 and Slice 5 plan after the explicit user override and current `howl-vt` source after nested commit `20fb714`.
- How one task went: re-proved that post-Slice-3 `howl-vt/src` has terminal-core files under temporary `src/terminal/` while `action/`, `control/`, `host/`, `kitty/`, and `xterm/` remain top-level; repaired the plan so Slice 4 moves `action/`, `control/`, and `xterm/` directly to final shallow `src` paths while keeping `host/` and `kitty/` as true shallow subdomains, and Slice 5 deletes `src/terminal/` by moving all retained contents back under `src/`.
- Next handoff or blocker: reviewer should gate `research/2026-06-13-pty-vt-folder-owner-structure-plan.md`; no coding is authorized until reviewer acceptance and orchestrator seeding.

28. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate the researcher-repaired remaining Slice 4 and Slice 5 plan after the explicit user override.
- How one task went: rejected the repair because final proof folders still preserve deleted source buckets as `test/unit/action/`, `test/unit/control/`, and `test/unit/xterm/`, and the Slice 4/5 allowed-file lists use broad dependency globs such as `src/ffi/**`, `src/kitty/**`, and `test/unit/**` instead of exact current-source-proved files.
- Next handoff or blocker: researcher must repair the Slice 4 and Slice 5 plan again before orchestrator may seed Slice 4; no implementation is authorized.

29. Role: researcher
- Session id: `research-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: repair the reviewer-rejected remaining Slice 4 and Slice 5 plan without implementation by re-proving exact current-source files and removing stale final proof buckets.
- How one task went: re-proved old `action/`, `control/`, and `xterm/` import users and temporary `src/terminal/` import users from current `howl-vt`; replaced broad dependency globs with exact files except owner folders being moved as units, and changed final proof paths to `test/unit/route_test.zig`, `test/unit/report_test.zig`, and `test/unit/csi_mapping_test.zig`.
- Next handoff or blocker: reviewer should gate the repaired research artifact; no implementation is authorized until reviewer acceptance and orchestrator seeding.

30. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate the repaired remaining Slice 4 and Slice 5 plan after prior rejection of stale proof buckets and broad dependency globs.
- How one task went: accepted the repair; final proof shape no longer preserves `test/unit/action/`, `test/unit/control/`, or `test/unit/xterm/`, Slice 4 exact dependency files are current-source-proved, and the only remaining folder globs are owner folders being moved as units.
- Next handoff or blocker: orchestrator may seed Slice 4 from the repaired research artifact; coder must stop and return to research if current-source proof finds an old protocol-bucket import or temporary terminal-wrapper import in a file not named by the accepted slice contract.

31. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: record accepted remaining-plan repair receipt and activate Slice 4.
- How one task went: committed the accepted remaining-plan repair at root `27bcb75` and marked Slice 4 active.
- Next handoff or blocker: coder may implement Slice 4 from this live-loop contract only.

32. Role: coder
- Session id: `coder-2026-06-13-pty-vt-folder-structure-slice-4`
- Hello: hi
- Intended task: implement Slice 4 protocol bucket removal toward final no-wrapper layout.
- How one task went: moved `action/`, `control/`, and `xterm/` owners to direct `src/` paths and `src/csi/`, removed stale proof buckets, kept `src/terminal/**`, `src/host/`, and `src/kitty/` in place, and passed `zig build test:unit`, `zig build test:abi`, and `zig build benchmark:m7_baseline:build` in `howl-vt`.
- Next handoff or blocker: reviewer should gate the Slice 4 diff; no commit was made by coder.

33. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate Slice 4 implementation after coder verification.
- How one task went: accepted Slice 4; old source/proof buckets are removed, new direct owners and `src/csi/` helper subdomain are present, `src/host/`, `src/kitty/`, and `src/terminal/**` remain in place, changes are relocation plus import-path updates only, and required proofs passed.
- Next handoff or blocker: orchestrator may commit nested `howl-vt` and record receipts.

34. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: verify and commit accepted Slice 4.
- How one task went: reran `zig build test:unit`, `zig build test:abi`, and `zig build benchmark:m7_baseline:build` in `howl-vt`; committed nested Slice 4 as `538a96b`.
- Next handoff or blocker: commit the root receipt for Slice 4 before seeding Slice 5.

35. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: record Slice 4 root commit receipt.
- How one task went: committed the root Slice 4 receipt as `3a3ea9d`.
- Next handoff or blocker: commit this receipt update, then seed Slice 5.

## Slice 5 Execution Contract

Name: Delete temporary VT terminal wrapper and finalize shallow `src` layout.

Status: active.

Coder session id: `coder-2026-06-13-pty-vt-folder-structure-slice-5`.

Reviewer session id: `review-2026-06-13-pty-vt-folder-structure-01`.

Commit-hash receipt demand: required on acceptance.

Explicit override receipt:

- User decision: "ok slice 5 will be deleting terminal anmd moving all its content into src/ terminal makes the fodlers all 1 deeper for no reason. you can delegate until all that is done"
- Reference overridden: Ghostty `src/terminal/` final VT-core subdomain pressure.
- Reason: user rejects the extra folder depth as unjustified in Howl.
- Orchestrator session id: `orch-2026-06-13-pty-vt-folder-structure-01`.

Allowed files:

- `howl-vt/src/howl_vt.zig`
- `howl-vt/src/terminal/**`
- `howl-vt/src/terminal.zig`
- `howl-vt/src/screen.zig`
- `howl-vt/src/screen_set.zig`
- `howl-vt/src/stream_terminal.zig`
- `howl-vt/src/publication.zig`
- `howl-vt/src/parser/**`
- `howl-vt/src/screen/**`
- `howl-vt/src/selection/**`
- `howl-vt/src/ffi/handle.zig`
- `howl-vt/src/ffi/lifecycle.zig`
- `howl-vt/src/ffi/runtime.zig`
- `howl-vt/src/ffi/surface.zig`
- `howl-vt/src/ffi/selection.zig`
- `howl-vt/src/route.zig`
- `howl-vt/src/vocabulary.zig`
- `howl-vt/src/report.zig`
- `howl-vt/src/osc_color.zig`
- `howl-vt/src/osc.zig`
- `howl-vt/src/dcs.zig`
- `howl-vt/src/csi/params.zig`
- `howl-vt/src/host/apply.zig`
- `howl-vt/src/host/state.zig`
- `howl-vt/test/unit.zig`
- `howl-vt/test/unit/terminal_end_to_end_test.zig`
- `howl-vt/test/unit/terminal_snapshot_test.zig`
- `howl-vt/test/unit/terminal_osc_test.zig`
- `howl-vt/test/unit/terminal_surface_test.zig`
- `howl-vt/test/unit/terminal_modes_test.zig`
- `howl-vt/test/unit/terminal_test.zig`
- `howl-vt/test/unit/screen_test.zig`
- `howl-vt/test/unit/route_test.zig`
- `howl-vt/test/unit/report_test.zig`
- `howl-vt/test/unit/csi_mapping_test.zig`
- `howl-vt/test/unit/parser/csi_test.zig`
- `howl-vt/test/unit/parser/events_test.zig`
- `howl-vt/test/unit/parser/main_test.zig`
- `howl-vt/test/unit/parser/string_control_test.zig`
- `howl-vt/test/unit/screen/cursor_test.zig`
- `howl-vt/test/unit/screen/history_test.zig`
- `howl-vt/test/unit/screen/resize_test.zig`
- `howl-vt/test/unit/screen/tabs_test.zig`
- `howl-vt/test/unit/screen/write_test.zig`
- `howl-vt/test/support/screen_capture.zig`
- `howl-vt/test/support/stream_harness.zig`
- `howl-vt/benchmark/terminal_benchmark.zig`
- `howl-vt/benchmark/pty_feed_record.zig`

Required shape:

- Delete `howl-vt/src/terminal/` completely.
- Delete `howl-vt/src/terminal/main.zig` instead of recreating an equivalent wrapper elsewhere.
- Move `src/terminal/terminal.zig` to `src/terminal.zig`.
- Move `src/terminal/screen.zig` to `src/screen.zig` and `src/terminal/screen_set.zig` to `src/screen_set.zig`.
- Move `src/terminal/stream_terminal.zig` to `src/stream_terminal.zig` and `src/terminal/publication.zig` to `src/publication.zig`.
- Move `src/terminal/parser/**` to `src/parser/**`, `src/terminal/screen/**` to `src/screen/**`, and `src/terminal/selection/**` to `src/selection/**`.
- Update `src/howl_vt.zig` to import final direct owners and stop importing `terminal/main.zig`.
- Update all allowed product, proof, support, and benchmark imports directly to final paths.
- Final `src` may contain direct terminal-core owner files plus shallow true subdomains `ffi/`, `input/`, `host/`, `kitty/`, `screen/`, `parser/`, `selection/`, and `csi/` only.
- No `src/terminal/` wrapper, compatibility shim, or duplicate old/new terminal path may survive acceptance.

Exact tests:

- `zig build test` in `/home/home/personal/projects/howl/howl-pty`
- `zig build test` in `/home/home/personal/projects/howl/howl-vt`
- `zig build simulate:build` in `/home/home/personal/projects/howl/howl-vt`
- `zig build benchmark:m7_baseline:build` in `/home/home/personal/projects/howl/howl-vt`

Non-goals:

- No new cleanup outside PTY or VT.
- No execution-phase redesign.
- No protocol bucket redesign beyond imports required after Slice 4.
- No host or kitty movement.
- No public ABI or behavior changes.
- No proof, benchmark, or simulation behavior changes beyond import-path updates.

Stop conditions:

- Stop if `howl-vt/src/terminal/` exists after acceptance.
- Stop if any source, test, support, benchmark, simulation, or build file imports `src/terminal/` or `terminal/main.zig` after acceptance.
- Stop if any stale path survives only because imports still reference it.
- Stop if final build roots still mention old locations.
- Stop if reviewer cannot diff the final tree without reconstructing hidden moves.
- Stop if current-source proof finds a direct temporary terminal-wrapper import in a file not named here; return to research instead of guessing or adding a shim.

36. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: seed Slice 5 to remove the temporary VT terminal wrapper.
- How one task went: wrote the active execution contract with exact override receipt, allowed files, required final no-wrapper shape, tests, non-goals, stop conditions, session ids, and receipt demand.
- Next handoff or blocker: coder may implement Slice 5 from this contract only.

37. Role: coder
- Session id: `coder-2026-06-13-pty-vt-folder-structure-slice-5`
- Hello: hi
- Intended task: implement final no-wrapper Slice 5.
- How one task went: deleted `howl-vt/src/terminal/`, moved terminal-core owners to final shallow `src` paths, updated allowed imports, and passed `zig build test` in `howl-pty`, `zig build test` in `howl-vt`, `zig build simulate:build`, and `zig build benchmark:m7_baseline:build`.
- Next handoff or blocker: reviewer should gate the Slice 5 diff; no commit was made by coder.

38. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate Slice 5 final no-wrapper implementation.
- How one task went: accepted Slice 5; `howl-vt/src/terminal/` is absent, no `src/terminal/` or `terminal/main.zig` imports remain, terminal-core owners are at final shallow `src` paths, changes are required import churn only, and required proofs passed.
- Next handoff or blocker: orchestrator may commit nested `howl-vt` and record receipts.

39. Role: orchestrator
- Session id: `orch-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: verify and commit accepted Slice 5.
- How one task went: reran `zig build test` in `howl-pty`, `zig build test` in `howl-vt`, `zig build simulate:build`, and `zig build benchmark:m7_baseline:build`; committed nested Slice 5 as `4e76640`.
- Next handoff or blocker: commit the root receipt for Slice 5, then close the sprint.

32. Role: coder
- Session id: `coder-2026-06-13-pty-vt-folder-structure-slice-4`
- Hello: hi
- Intended task: implement Slice 4 VT protocol bucket removal toward the final no-wrapper layout without moving `src/terminal/**`.
- How one task went: moved `action/`, `control/`, and `xterm/` owners to direct `src/` and `src/csi/` paths, renamed stale proof bucket files to direct unit proof files, removed the empty old source and proof bucket directories, confirmed no old protocol-bucket imports remain, and passed the required unit, ABI, and benchmark build checks.
- Next handoff or blocker: reviewer should gate the Slice 4 diff; no commit was made, so commit-hash receipt remains pending for orchestrator acceptance closure.

33. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate Slice 4 VT protocol bucket removal implementation against the accepted contract and unstaged nested `howl-vt` diff.
- How one task went: accepted Slice 4; `src/action/`, `src/control/`, `src/xterm/`, `test/unit/action/`, `test/unit/control/`, and `test/unit/xterm/` are absent; moved protocol owners and proof files compare as relocation plus import-path updates only; `src/host/`, `src/kitty/`, and `src/terminal/**` remain in place; no old protocol-bucket imports or compatibility shims were found; `zig build test:unit`, `zig build test:abi`, `zig build benchmark:m7_baseline:build`, and `git diff --check` passed locally.
- Next handoff or blocker: orchestrator may commit nested `howl-vt` for Slice 4 and then record nested/root receipts; no product blocker remains from review.

37. Role: coder
- Session id: `coder-2026-06-13-pty-vt-folder-structure-slice-5`
- Hello: hi
- Intended task: implement Slice 5 by deleting the temporary VT `src/terminal/` wrapper and finalizing the shallow `src` layout.
- How one task went: moved terminal-core owners, parser, screen, and selection subdomains back under `howl-vt/src/`, deleted `src/terminal/main.zig`, removed the empty `src/terminal/` directory tree, repointed allowed product/proof/support/benchmark imports to final paths, and proved no Zig imports of `src/terminal/` or `terminal/main.zig` remain.
- Next handoff or blocker: reviewer should gate the Slice 5 diff; no commit was made, so commit-hash receipt remains pending for orchestrator acceptance closure.

38. Role: reviewer
- Session id: `review-2026-06-13-pty-vt-folder-structure-01`
- Hello: hi
- Intended task: gate Slice 5 implementation against the explicit final no-`terminal/` override and unstaged nested `howl-vt` diff.
- How one task went: accepted Slice 5; `howl-vt/src/terminal/` is absent, `terminal/main.zig` is deleted with no equivalent wrapper, terminal-core owners and `parser/`, `screen/`, and `selection/` are at final shallow `src/` paths, stale `src/terminal/` and `terminal/main.zig` imports are absent, moved owner content only changed required relative imports, dependent allowed files are import-path churn only, and `zig build test` passed in both `howl-pty` and `howl-vt` plus `zig build simulate:build`, `zig build benchmark:m7_baseline:build`, and `git diff --check` passed in `howl-vt`.
- Next handoff or blocker: orchestrator may commit nested `howl-vt` for Slice 5 and then record nested/root receipts; no product blocker remains from review.
