version:
  current: 0.1.3-dev
  status: reconstructed single-terminal foundation

objective: >-
  Preserve independently useful terminal capabilities and one direct first-party single-terminal executable while
  keeping deferred presentation and multiplexing work outside the accepted foundation.

settled_capabilities:
  - name: vt
    acceptance: owns terminal semantics, state, dirty facts, input encoding, replies, and consequences
  - name: pty
    acceptance: owns exact Linux PTY transport and child-process lifecycle
  - name: control
    acceptance: optional ID-addressable PTY and VT composition with bounded operations and no rendering relation
  - name: render
    acceptance: backend-neutral terminal visual knowledge and plain values without runtime policy
  - name: executable
    acceptance: howl-host owns one concrete Wayland, EGL/GLES, input, retained-visual, and lifecycle composition

accepted_implementation:
  - VT protocol behavior and deterministic evidence remain the terminal-semantics baseline.
  - PTY Linux transport, transfer, process-lifecycle, cancellation, and cleanup behavior remain the transport baseline.
  - >-
    Render independently selects native text and generated terminal glyphs; either or both may be absent, and only
    native text selection links libc, FreeType, and HarfBuzz.
  - Render terminal projection writes backend-neutral visual patches into caller buffers and owns no runtime policy.
  - >-
    Render terminal text preparation discovers one homogeneous run at a time, preserves unresolved visual facts, and
    returns exact native, generated, or no-glyph output ownership.
  - >-
    Howl-host directly composes one Control terminal, one Wayland window, and one EGL/GLES render thread with bounded
    keyboard and pointer admission, title, bell, and ordered notification presentation, exact terminal completion,
    and cleanup.
  - >-
    Howl-host owns primary-history viewport intent and exact scrollbar presentation while VT retains history,
    projection, eviction, reflow, and alternate-screen truth.
  - >-
    VT owns projected selection and copied-text semantics; Control exposes bounded local operations; Howl-host owns
    pointer gestures, exact clipboard chords, nonblocking Wayland UTF-8 clipboard transfer, and ordered OSC 52
    system-clipboard set/query policy.
  - >-
    Howl-host draws retained DEC single/double-width and double-height row geometry plus normal, raised, and lowered
    cell baselines from backend-neutral render facts without changing shaping or texture identity.
  - Howl-host presents retained cursor blink intent with one bounded monotonic timer and no VT/render timing policy.
  - Howl-host transactionally answers ordered color-preference queries from its fixed dark presentation policy.
  - >-
    Howl-host validates ordered OSC 22 operations, retains independent bounded primary and alternate pointer stacks,
    answers exact queries, and applies supported shapes through optional Wayland cursor-shape-v1.
  - >-
    Howl-host consumes ordered window operations, requests Wayland minimization, and transactionally reports only
    normal state, unavailable zero position/screen facts, and its retained title.

invariants:
  - every module allocation uses an explicit caller-provided allocator
  - bounds, errors, rollback, cleanup, and ownership are exact at each capability boundary
  - control contains no rendering relation
  - render contains no thread, publication lifecycle, GPU device, window, layout, or presentation policy
  - howl-host is one first-party executable and does not constrain independent embedders
  - private development carries no downstream source or ABI compatibility obligation

deferred:
  - tabs, panes, sessions, and multiplexing
  - remaining ordered host-consequence presentation beyond notification, attention bell, OSC 52 clipboard, and
    window-control policy

gates:
  - permanent authorities and active planning YAML parse
  - every accepted allocation boundary exposes its caller-provided allocator
  - selected render capability graphs prove native and generated dependency exclusion
  - retained VT, PTY, control, and render behavior has exact focused proof
  - source audit and protocol catalogue validation pass for accepted source
  - rejected source and compatibility boundaries remain deleted
