# Cleanup Scratchpad 3

Archived 2026-05-30.

Durable render ABI vocabulary facts and ordered ABI-breaking slices were
consolidated into `project-memory.md` under
`2026-05-30 Render And VT ABI Canonical Memory`.

Historical role of this file:

- Accepted ABI breakage over preserving wrong render vocabulary.
- Identified suspicious nouns: feedback, frame, `SurfaceText`, `PreparedFrame`,
  `PendingState`, and broad `surface/*` buckets.
- Proposed owner-true ABI nouns and a first worker-ready submit-result vocabulary
  slice.

Use `project-memory.md` as the canonical source for new work.
