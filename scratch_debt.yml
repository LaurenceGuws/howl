schema: 1
sprint:
  id: ownership_names
  branch: feature/debt_payoff/ownership_names
  status: active
  active_slice: rename_contract

objective: >-
  Define the finite atomic contract for the two accepted dishonest ownership
  groups without changing product source.

questions:
  - id: shared_owner
    status: answered
    answer: one cohesive bidirectional Window/Render boundary owning copied facts, descriptors, wakes, failure, and retirement
  - id: candidate_census
    status: answered
    answer: chrome_state.zig is the only other dishonest maintained owner group found by the explicit discovery rule
  - id: migration_surface
    status: answered
    answer: ownership_inventory.yml records two complete atomic path sets and every old private alias

accepted_slice:
  id: ownership_inventory
  state: accepted
  evidence: .zig/work/debt-payoff/ownership-names/ownership_inventory.yml
  records: 26
  atomic_groups: [window_render_boundary, session_chrome_adapter]

active_scope:
  evidence: .zig/work/debt-payoff/ownership-names/rename_contract.yml
  product_change: forbidden
  excluded:
    - source renames
    - file splitting
    - package extraction
    - behavior or API changes

progress:
  state: not_started
