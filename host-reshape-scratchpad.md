# Host Reshape Scratchpad

Archived 2026-05-30.

Durable host reshape facts were consolidated into `project-memory.md` under
`2026-05-30 Host Canonical Memory`.

Historical role of this file:

- Rejected the old host `terminal/runtime`, `terminal/host`, and
  `terminal_panel.zig` shapes.
- Recorded the accepted move toward `terminal/context.zig`, `terminal/pty/*`,
  `terminal/vt/*`, `terminal/render/*`, and `window/*` ownership.

Use `project-memory.md` as the canonical source for new work.
