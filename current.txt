# Current Slice: Move Render Prepared Surface Translator

Owner: workspace root.

Purpose:

- Continue the accepted `howl-render` C translator lane.
- Move only `prepared_surface.zig` from the render root into `src/ffi/`.
- Preserve prepared-surface handle behavior, diagnostics/info/buffer conversion, and prepare-handle token use exactly.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/render-c-lane.md`
- `howl-render/design.md`
- `howl-render/include/howl_render.h`
- `howl-render/src/libhowl_render.zig`
- `howl-render/src/prepared_surface.zig`
- `howl-render/src/ffi/prepare_request.zig`
- `howl-render/src/submit_result.zig`
- `howl-render/src/test/ffi.zig`
- `howl-render/src/surface/prepared_owner.zig`

## Source Changes

Edit only:

- `howl-render/src/libhowl_render.zig`
- `howl-render/src/prepared_surface.zig`
- `howl-render/src/ffi/prepared_surface.zig`
- `howl-render/src/test/ffi.zig`

Do not edit any other file unless the build exposes a direct `prepared_surface.zig` import missed by this slice.
If that happens, stop and report the exact import before broadening scope.

## Required Shape

- Move `howl-render/src/prepared_surface.zig` to `howl-render/src/ffi/prepared_surface.zig`.
- Update `libhowl_render.zig` to import the moved translator from `ffi/prepared_surface.zig`.
- Update `test/ffi.zig` to import the moved translator.
- Remove the old root `howl-render/src/prepared_surface.zig`; do not leave a compatibility wrapper.
- Keep `libhowl_render.zig` as an export table only.

## Non-Goals

- Do not move `submission.zig`.
- Do not move already-moved translators again.
- Do not move `submit_result.zig` in this slice.
- Do not rename any `HowlRender*` C type.
- Do not rename any `howl_render_*` symbol.
- Do not change `howl-render/include/howl_render.h`.
- Do not change prepared-surface lifecycle, liveness, token validation, info, buffer, diagnostics, or metric conversion semantics.
- Do not add compatibility aliases.

## Invariants

- The public root curates exports only.
- Prepared-surface exported symbols keep the same names.
- `prepareHandle` still nulls the output handle when provided before owner/token access.
- Missing handle, missing output, non-live handle, invalid argument, and prepare failed/ready statuses remain identical.
- `prepare_request.prepareTokenIn` remains the token validator used by `prepareHandle`.
- `submit_result.metricsOut` remains the metrics converter used by info and diagnostics.
- `byteSpan`, failure constructors, and prepared output converters move unchanged except import path fixes.
- The translator calls prepared/session owners; it does not own prepared-surface state.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg '@import\("prepared_surface\.zig"\)' howl-render/src`
- `rg 'howl_render_prepared_surface_prepare|howl_render_prepared_surface_release|howl_render_prepared_surface_describe|howl_render_prepared_surface_buffer|howl_render_prepared_surface_diagnostics|preparedInfoOut|preparedBufferOut|preparedDiagnosticsOut' howl-render/src`
- `rg 'submission.zig' howl-render/src/libhowl_render.zig`

Expected results:

- The first gate prints no stale root import.
- The second gate supports manual review that prepared-surface C translation moved only.
- The third gate proves this slice did not move the remaining root `submission.zig` translator.

## Stop Conditions

- Stop if moving `prepared_surface.zig` requires changing behavior.
- Stop if the new lane needs a compatibility wrapper at the old root path.
- Stop if an import cycle appears between `ffi/prepared_surface.zig`, `ffi/prepare_request.zig`, and `submit_result.zig`.
- Stop if prepared output conversion or failure construction cannot move unchanged with the translator.
