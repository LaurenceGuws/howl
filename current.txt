# Current Batch: Image Protocol Purge

Owner: workspace root.

Sprint scope: remove all terminal image/graphics protocol traces from Howl.

Status: active.

## Requirement

- Delete remaining Kitty graphics/image protocol parsing, state, ABI, render bridge, host
  acquisition, tests, and deleted-module references.
- Preserve unrelated terminal behavior, text surface publishing, selection, clipboard, input,
  modes, color, hyperlink, and non-graphics Kitty protocols.
- Do not restore user-deleted graphics files.
- Do not add replacement image/graphics architecture, compatibility aliases, or host shortcuts.

## Dirty State To Preserve

- `howl-vt/src/kitty/graphics.zig` is deleted.
- `howl-render/src/ffi_types.zig`, `src/frame/graphics_prepare.zig`,
  `src/frame/graphics_viewport.zig`, `src/frame/surface_text_ffi.zig`, `src/stb_image.c`,
  `src/stb_image.h`, and `src/stb_image.zig` are deleted.
- `howl-linux-host/src/terminal/terminal_panel.zig` has the selection mouse-release
  auto-copy deletion and must not be reverted.

## Slice

1. Remove VT graphics/image C ABI exports and implementation.
2. Remove VT Kitty graphics action/parser/state paths and tests.
3. Remove render decoded-graphics publish ABI and surface preparation state while keeping the
   non-graphics text publish path.
4. Remove host graphics acquisition/publish bridge.
5. Verify with focused submodule builds/tests and diff checks.

## Non-Goals

- No new image/graphics protocol support.
- No Lua copy-selection mapping in this slice.
- No unrelated payload/clipboard/paste deletion unless tied to graphics/image protocol.
