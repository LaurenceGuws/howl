# Current Slice: Valid PNG Query Proof

Owner: workspace root.

Sprint scope: graphics.

Promoted from `graphics-machinery-scratchpad.md` after valid PNG query research.

## Goal

Prove valid `a=q,f=100` PNG queries validate PNG data, reply `OK`, and do not store graphics state.

## Target Files

- `howl-vt/src/test/terminal_graphics.zig`
- `howl-vt/src/kitty/graphics.zig` only if the test exposes a real query/PNG validation bug

## Required Behavior

- Valid `a=q,f=100` with Kitty's 1x1 PNG fixture replies `OK`.
- The query validates/decode-checks the PNG but stores no image, placement, frame, or partial upload state.
- Existing simple PNG publication and invalid PNG `EBADPNG` behavior remains green.

## Hard Constraints

- Do not change ABI, render, host, parser, or terminal files.
- Prefer tests only.
- Do not replace stb_image or add libpng.
- Do not broaden into full PNG mode/color-management/gamma/ICC parity.
- Do not add PNG zlib support.
- Do not change decoded PNG root storage/model truth.
- Do not change indirect media lifecycle, animation, quiet, delete/reset, quota, composition, generated placeholder behavior, or EBADPNG handling.
- Do not special-case yazi/panes.
- Keep the smallest correct diff.

## Required Tests

- `a=q,f=100` with Kitty's 1x1 PNG fixture replies `\x1b_Gi=31;OK\x1b\\`.
- Image count, placement count, frame count, and partial upload state remain empty after the query.

## Verification Required

- `zig build test` in `howl-vt`
- `zig build test:regression:build` in `howl-vt`
- `git diff --check` at workspace root

Stop condition:

- If a valid PNG query requires production changes outside VT graphics validation/query handling, stop and report the exact dependency instead of widening the slice.
