# Current Slice: Re-index Graphics Scratchpad

Owner: workspace root.

Sprint scope: graphics.

## Goal

Replace stale graphics backlog entries with a current live-gap index so future workers do
not start from removed functions, completed placeholder machinery, or completed logging
cleanup.

## Source Truth

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `reference-index.md`
- `graphics-machinery-scratchpad.md`

## Target Files

- `graphics-machinery-scratchpad.md`

## Required Behavior

- Docs-only slice.
- Mark completed/stale items as completed or stale instead of leaving them in the active
  worker backlog.
- Replace stale worker backlog and deletion gate with live unresolved graphics gaps.
- Record current risks for base64 retained payload truth, animation visibility gating,
  persistent generated cell refs, and decoded/cache quota parity.
- Do not edit `howl-vt`, `howl-render`, `howl-linux-host`, ABI, render behavior, host
  behavior, or tests.

## Required Checks

- `git diff --check` at root.
- Root/submodule status remains clean except for the intended scratchpad/current docs.

Stop condition:

- If choosing the next code slice requires new Kitty/Ghostty/Alacritty reference research,
  stop after the re-index and do not promote behavior code.
