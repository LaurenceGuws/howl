# Current Slice: Graphics Decoded Quota Research

Owner: workspace research; likely future owner is howl-vt first, with render cache policy
as a later separate owner.

Sprint scope: graphics.

## Goal

Design the next source-backed step after VT decoded-truth cleanup: decoded graphics
storage quota and eviction semantics that match Kitty/Ghostty ownership without widening
into render cache policy too early.

## Completed Dependency State

- VT direct chunks accumulate decoded transport bytes.
- VT exposes decoded graphics query/copy ABI.
- Render accepts decoded graphics commit-slot ABI.
- Linux host publishes decoded graphics end-to-end.
- VT coalesces frames from decoded bytes instead of legacy base64 compatibility bytes.
- Old base64/protocol payload ABIs remain compatibility paths.

## Source Truth

- `AGENTS.md`
- `loop.txt`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `howl-vt/src/kitty/graphics.zig`
- `howl-vt/src/test/terminal_graphics.zig`
- `howl-vt/include/howl_vt.h`
- `howl-render/src/frame/graphics_prepare.zig`
- `utils/dev_references/terminals/kitty/kitty/graphics.c`
- `utils/dev_references/terminals/kitty/kitty/graphics.h`
- `utils/dev_references/terminals/ghostty/src/terminal/kitty/graphics_storage.zig`
- `utils/dev_references/terminals/ghostty/src/terminal/kitty/graphics_image.zig`

## Research Questions

- What exact storage/quota bytes does Kitty account for images, frames, loading data,
  cache entries, and rendered data?
- What exact eviction priority does Kitty use for incomplete, unreferenced, placed,
  animated, and recently used images?
- What exact storage/quota model does Ghostty implement for decoded images and placements?
- Which Howl VT decoded/legacy/upload fields should count toward VT-owned protocol
  storage quota now that decoded bytes are model truth?
- What can remain a conservative bound for compatibility bytes until old ABI retirement?
- What is the smallest VT-only quota/eviction slice with tests?

## Required Output

- Research only. Do not edit product code.
- Return source-backed findings with exact file/line references.
- Separate VT protocol/storage quota from render raster cache quota.
- Draft one worker-ready VT implementation slice if safe.

## Out Of Scope

- No code implementation in this slice.
- No render cache redesign unless research proves it must precede VT quota.
- No public ABI changes.
- No host changes.

## Verification

- Scratchpad update must pass root `git diff --check`.

Stop conditions:

- Stop if quota/eviction cannot be sliced without broad image identity/ref redesign.
- Stop if source truth requires a render cache policy decision first.
