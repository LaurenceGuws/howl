# Current Slice: Render Graphics Raster Cache Deletion Accepted

Owner: workspace root.

Sprint scope: graphics deletion.

Status: accepted by reviewer, verified locally, and committed.

Accepted commit:

- `howl-render` `e940674 render: delete graphics raster cache`

## Accepted Deletion

- Deleted render decoded graphics raster cache keys and cross-publication reuse policy:
  `DecodedGraphicsKey`, `GraphicsPublicationImageKey`, cache lookup, sweep, and key tests.
- `GraphicsPreparer` now decodes fresh rasters for the current publication only.
- Kept `GraphicsPreparer.raster()` as a view into current publication rasters.
- Deleted tests that asserted cache reuse/replacement by payload key.

## Verification Passed

- `zig build test --summary all` in `howl-render` (`704/707` tests passed, `3` skipped).
- `howl-render` `git diff --check`.

## Known Broken State

- Render no longer reuses decoded graphics rasters across publications.
- Any performance benefit from the old Howl-only cache is gone by design.

## Next Gate

No replacement slice is active. Continue deleting Howl-only graphics ideas that are not
Kitty-identical/source-backed before replacement work.
