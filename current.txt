# Current Slice: Move PTY FFI Tests Out Of Production

Owner: `howl-pty` ABI/FFI test accountability.

Purpose:

- Remove production `howl-pty/src/ffi.zig` dependency on test-only PTY doubles.
- Move FFI behavior tests beside ABI tests, where test helpers are allowed.
- Preserve product behavior and exported ABI exactly.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-test-war/offender-inventory.md`
- `howl-pty/src/ffi.zig`
- `howl-pty/src/test/abi.zig`
- `howl-pty/src/pty/pty_test.zig`
- `howl-pty/build.zig`

## Source Changes

Exact files:

- `howl-pty/src/ffi.zig`
- Add `howl-pty/src/test/ffi.zig` or equivalent ABI-owned test module.
- Update `howl-pty/src/test/abi.zig` only to import the new FFI test module.
- Update `howl-pty/build.zig` only if module imports are required.

## Required Shape

- `howl-pty/src/ffi.zig` imports production owners only.
- FFI tests still exercise the shipped translator functions through ABI test wiring.
- No production file imports `pty/pty_test.zig`.

## Non-Goals

- Do not split PTY integration/runtime tests in this slice.
- Do not redesign PTY test helpers in this slice.
- Do not change C headers, exported symbols, or product behavior.

## Invariants

- FFI remains a translator only.
- Test doubles remain reachable only from test modules.
- Existing PTY ABI tests continue to run.

## Verification

From `/home/home/personal/projects/howl/howl-pty`:

- `zig build check`
- `zig build test`
- `zig build test:abi`
- `zig build test:unit`

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'pty_test|test_pty' howl-pty/src/ffi.zig` prints nothing.
- `rg '@import\("pty/pty_test\.zig"\)' howl-pty/src` prints nothing.
- `rg 'pty_test' howl-pty/src/test` shows test-only ownership only if the helper is imported there.

## Stop Conditions

- Stop if moving the tests requires product API changes.
- Stop if ABI tests cannot import the required test doubles without production imports.
