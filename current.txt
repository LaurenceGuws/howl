# Current Stage: Research Render Protocol Language Cleanup

Owner: workspace root research workflow.

Source scratchpad:

- `research/2026-06-01-render-api-language-deletion-sprint.md`

Accepted research caches:

- `research/cache-2026-06-01-render-api-language-deletion.md`
- `research/cache-2026-06-01-render-v0-source-owner-paths.md`

Completed slices:

- `howl-render` `aa1749d` moved V0 oracle/equivalence tests under unit.
- `howl-render` `6bf6388` deleted the separate protocol-proof build/test bucket.
- `howl-render` `12aafa5` deleted the internal `src/protocol_v0/` source bucket.

## Purpose

Produce focused evidence for the remaining render `protocol` language cleanup.
The next implementation must distinguish stale internal/test/doc category
language from public ABI vocabulary that remains intentionally preserved.

No product code is authorized by this stage.

## Research Questions

- Which remaining `protocol`/`protocol_v0` occurrences under `howl-render` are
  stale internal test names or docs that teach protocol as a separate product?
- Which remaining occurrences are public ABI vocabulary and must remain in this
  sprint?
- Should `docs/render-protocol-v0.md` be renamed, rewritten, or deleted in the
  next slice?
- Which test names can be renamed without changing public ABI assertions?
- Which host occurrences are out of scope and need a separate host research
  cache?
- What exact files, symbols, grep gates, and verification commands make one
  worker-ready slice?

## Required Output

- A focused research cache with exact source paths, line-backed evidence,
  public-ABI versus stale-language classification, exact proposed slice, risks,
  stop conditions, grep gates, and readiness judgment.
- Reviewer acceptance before the scratchpad is updated.
- No worker seeding until `current.txt` is replaced with one exact
  implementation slice.

## Hard Stops

- Stop if public ABI names are renamed as part of language cleanup.
- Stop if host behavior changes enter the slice.
- Stop if documentation changes imply an ABI break without an ABI-product slice.
