# Current Slice: Render Protocol V0 Retire Order Contract

Owner: `howl-render` protocol V0 lifetime contract semantics.

Sprint scratchpad and contract:

- `research/2026-05-31-howl-render-protocol-sprint.md`
- `docs/render-protocol-v0.md`

Purpose:

- Define the exact ordering domain for `HowlRenderV0Retire.retire_seq` so later
  realizer code can validate create/upload/use/retire without inventing behavior.
- Keep the slice document-only because implementation review proved the current
  contract is ambiguous.

## Source Order (Mandatory)

Read in order before editing:

1. `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
2. `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
3. `docs/render-protocol-v0.md`
4. `research/2026-05-31-howl-render-protocol-sprint.md`
5. `howl-render/include/howl_render.h`
6. `howl-render/src/ffi/protocol_v0.zig`
7. `howl-render/src/protocol_v0/realize.zig`

## Exact Change Scope

Allowed files:

- `docs/render-protocol-v0.md`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No product code.
No header changes.
No Zig changes.
No host code.
No protocol emission.
No normal-path replacement.

## Required Contract Closure

- Define whether `retire_seq` is command-index based, upload-index based,
  frame-event-index based, or another explicit domain.
- Define how uploads compare to retire order.
- Define how commands compare to retire order despite commands having no sequence
  field.
- Define valid create/upload/use/retire same-frame ordering.
- Define invalid upload after retire, command use after retire, retire before final
  command use, duplicate retire, missing resource, and wrong generation behavior.
- Define exact negative tests required before implementation retries.
- Avoid changing ABI structs unless the document proves current fields cannot
  express the required ordering.

## Required Invariants

- No hidden sequence domains.
- No implicit backend/host ordering.
- Resource lifetime remains create, update, use, retire, ack, reuse.
- A later worker must be able to implement validation without guessing.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if the current ABI cannot express ordered retire validation without a new
  field; report the exact header/ABI follow-up instead of inventing semantics.
- Stop if command order or upload order remains ambiguous.
- Stop if implementation is attempted.
