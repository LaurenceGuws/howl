# Current Slice: Move Render Token Contract Owner

Owner: `howl-render`.

Purpose:

- Move render snapshot/prepared/submitted token contracts out of the stale `surface/*` bucket.
- Preserve token validation, latest-mailbox, source/prepared/session/FFI behavior exactly.
- Do not split token semantics in this slice.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/render-surface-taxonomy.md`
- `howl-render/src/surface/tokens.zig`
- all current importers of `surface/tokens.zig`
- `libs.yaml`

## Source Changes

Exact files:

- Move `howl-render/src/surface/tokens.zig` to `howl-render/src/render/tokens.zig`.
- Update importers under `howl-render/src`.
- Update `libs.yaml` owner metadata.

## Non-Goals

- Do not split `LatestMailbox` out of token contracts in this slice.
- Do not change token validation behavior.
- Do not move source publication, input adaptation, clip, or rgba files.
- Do not alter C ABI or FFI behavior.

## Invariants

- `render/tokens.zig` owns render work identity contracts shared across source, prepared, session, and FFI.
- `surface/tokens.zig` no longer exists.
- No imports refer to `surface/tokens.zig`.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'surface/tokens\.zig' howl-render/src libs.yaml` prints nothing.
- `rg 'manager|controller|runtime|types\.zig|api\.zig|abi\.zig' howl-render/src/render/tokens.zig` prints nothing relevant to this slice.

## Stop Conditions

- Stop if moving tokens requires changing source, prepared, session, or FFI behavior.
