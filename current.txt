Current Task

Source:

- `feature-gap-scratchpad.md`
- `issue-6-scratchpad.md`

Current comparison and target are pinned in `issue-6-scratchpad.md` under:

- `Clear Comparison`
- `Exact Goal`
- `Exact Howl V1 Graphics Target`

Issue 6: Kitty graphics state is now exportable from VT, but higher consumers only partially ingest it.

First rule:

- Treat this as a staged VT-truth repair first, not “add an ABI now”.

Required outcome:

- Do not ship a fake kitty graphics ABI over false VT truth.
- First repair VT owner truth for the supported Kitty graphics subset.
- Only after VT truth is honest, define the smallest ABI-visible graphics contract.
- Keep ownership aligned: `howl-vt` owns graphics truth, later consumers above VT use only the C ABI seam.

Primary evidence:

- `howl-vt/src/kitty/graphics.zig`
- `howl-vt/src/kitty/state.zig`
- `howl-vt/src/test/terminal_graphics.zig`
- `howl-vt/src/kitty/apply.zig`
- `howl-vt/src/kitty/protocol.zig`
- `howl-vt/src/terminal.zig`
- `howl-vt/src/control/mode.zig`
- `howl-vt/include/howl_vt.h`
- `howl-vt/src/ffi.zig`

Reference against it:

- `AGENTS.md`
- `utils/dev_references/terminals/ghostty/src/lib_vt.zig:244-259`
- `utils/dev_references/terminals/ghostty/src/terminal/c/terminal.zig:577-606`
- `utils/dev_references/terminals/ghostty/src/terminal/c/kitty_graphics.zig`
- `utils/dev_references/terminals/ghostty/include/ghostty/vt/kitty_graphics.h`
- `utils/dev_references/terminals/kitty/kitty/vt-parser.c`
- `utils/dev_references/terminals/kitty/kitty/screen.c`
- `utils/dev_references/terminals/kitty/kitty/screen.h`
- `utils/dev_references/terminals/kitty/tools/tui/loop/terminal-state.go`
- `utils/official_docs/kitty/graphics-protocol.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`

No guessing allowed:

- Read `AGENTS.md`
- Read `reference-index.md`
- Read `feature-gap-scratchpad.md`
- Read the live owner files for VT kitty graphics truth, reset behavior, alt-screen behavior, and publication behavior.
- Read the Ghostty kitty graphics C seam before proposing any ABI shape.
- Read Kitty source/docs before proposing lifecycle semantics.
- Run a TigerBeetle-style skepticism pass against any proposed shape.
- Use `AGENTS.md` boundary rules with zero guessing.
- If the correct owner seam cannot be named exactly, stop and report `work-not-clear`.

Current status:

- VT truth for the current supported physical subset is substantially repaired.
- The public C graphics ABI now exists for graphics meta, indexed image query, indexed placement query, payload copy, and cell pixel size.
- `howl-render` now consumes graphics publication metadata and copied image/placement metadata above VT.
- The real host caller now:
  - pairs copied VT surface truth with queried graphics meta
  - retries whole acquisition on stale graphics item publication
- The next blocker is not "start ABI work" anymore.
- The next blocker is not copied metadata ingestion anymore.
- The next blocker is what retained/render-owned meaning to assign to those copied items without inventing drawing, geometry, or paint order above VT.

Hard constraints for the next implementation pass:

- Do not export `kitty.global.graphics` directly.
- Do not copy Ghostty's borrowed-handle seam literally unless Howl's owner truth matches it.
- Do not claim render-ready image bytes while VT only owns retained base64 payloads.
- Do not silently accept unsupported Kitty graphics actions; reject them explicitly once scope is defined.
- Do not make `howl-render` reach into `howl-vt` through a Zig-module shortcut; above-VT consumers still use only copied C ABI truth.
- Do not assume graphics publication is graphics-local until the contract says so explicitly.
- Do not assume graphics item array order is paint order.

Required research/decomposition before code:

1. Define the supported Kitty graphics subset for Howl right now.
2. State which lifecycle semantics from Kitty are in scope for that subset.
3. Identify the smallest owner-truth repairs required inside `howl-vt` before any ABI export.
4. Decide whether the eventual ABI exports retained payload bytes as stored, or whether VT must first own a normalized representation.
5. Decide whether graphics publication will be tied to visible surface publication `snapshot_seq`, or a separate graphics publication sequence.

If implementation is attempted, the first code changes must be VT-truth repair only:

1. per-buffer/main-vs-alt graphics ownership
2. reset semantics
3. alt-screen semantics
4. supported scroll/text-mutation coupling
5. placement replacement semantics
6. explicit rejection for unsupported graphics actions

Current next step:

1. keep the current supported physical subset fixed
2. keep the current above-VT slice copied-metadata-only
3. keep the exact coherence rule between VT surface publication and graphics item publication explicit
4. define the next retained render-side meaning for copied graphics items before any drawing work

Current blocker before that pass:

- `current.txt` and `issue-6-scratchpad.md` must reflect the now-landed copied item metadata ingestion slice
- the next slice still must not treat query index as stable identity or paint order
- the next slice still must not invent viewport mapping, geometry resolution, or draw order from copied metadata alone
- any next slice must still omit render-derived geometry, decoded-image claims, and payload ingestion

Current coherence rule:

- `howl_vt_terminal_copy_surface()` and graphics item queries are separate publications.
- Surface publication is keyed by `snapshot_seq` from the visible surface copy path.
- Graphics item queries are keyed by `publication_seq` returned by `howl_vt_terminal_query_graphics_meta()`.
- Graphics publication is conservative today: any terminal dirty-generation change can advance it, not only graphics-local mutation.
- A caller that wants one coherent pair must:
  1. copy the VT surface snapshot
  2. query graphics meta
  3. use that returned graphics `publication_seq` for image/placement/payload item queries
  4. if any graphics item query fails with invalid publication, restart acquisition rather than mixing generations
- Above-VT consumers still consume only copied C-ABI truth. They do not reach into `howl-vt` directly through Zig-module structure.

Execution plan for the next real pass:

1. Re-state the supported Kitty graphics subset explicitly before editing.
2. Keep the next slice above VT metadata-only: no payload bytes and no drawing.
3. Decide the smallest truthful retained render-side meaning for copied images and placements.
4. Prove that any retained replacement/invalidation logic follows publication truth, not index stability or guessed paint order.
5. Only after that should drawing/raster questions begin.

Internal driver:

- Use `issue-6-scratchpad.md` as the detailed checkpoint queue.
- `current.txt` stays as the narrow active-loop summary only.

Acceptance target:

- The docs name the real current state, not a stale pre-ABI state.
- The next implementation pass stays above VT but still does not start drawing.
- No false seam is added for graphics-local publication, payload decoding, or render-derived geometry.
