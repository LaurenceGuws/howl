# Current Slice: Delete Render `surface/flow.zig`

Owner: workspace root.

Purpose:

- Delete `howl-render/src/surface/flow.zig`.
- Replace the false `Flow` owner with explicit owners for source input,
  prepare-request lifecycle, submitted output, and geometry.
- Keep `howl-render/include/howl_render.h` byte-for-byte unchanged.

## Source Files

- `howl-render/src/surface/flow.zig`
- `howl-render/src/surface/text.zig`
- `howl-render/src/surface/input.zig`
- `howl-render/src/surface/prepared_owner.zig`
- `howl-render/src/ffi.zig`

## Files To Add

- `howl-render/src/source/vt.zig`
- `howl-render/src/source/damage.zig`
- `howl-render/src/source/slot.zig`
- `howl-render/src/source/prepare_request.zig`
- `howl-render/src/render/geometry.zig`
- `howl-render/src/session/submitted.zig`

## File To Delete

- `howl-render/src/surface/flow.zig`

Do not leave a shim or compatibility import.

## Symbol Movement

Move from `surface/flow.zig` to `source/vt.zig`:

- `VtSnapshot`
- `PublicationSource`
- `ReservedSourceMeta`
- `VtPublishResult`
- `validatePublicationSourceBoundary`

Move from `surface/flow.zig` to `source/damage.zig`:

- `validateDirtySource`
- `canonicalizeDirtyMetadata`
- `cursorPresentationChanged`
- `colorPresentationChanged`
- `setSourceCursorBlinkVisible`
- `samePublicationSource`
- `classifyDirty`
- `sameSnapshotToken` if shared
- `slotCellCountChecked` if needed

Move from `surface/flow.zig` to `source/slot.zig`:

- `PublicationSlot`
- `PublicationState.RetainedSlot` as `RetainedSlot`
- `slotCellCount`
- retained slot methods
- reserved source slot methods

Exported owner name: `SourceSlot`.

Move from `surface/flow.zig` to `source/prepare_request.zig`:

- `PrepareConsume`
- `Publication`
- `ActivePrepare`
- pending/active/blink lifecycle fields and methods
- source classification bridge methods

Exported owner name: `PrepareRequests`.

Move from `surface/flow.zig` to `session/submitted.zig`:

- `ThreadMutex`
- `lockMutex`
- `SubmitDecision`
- `TerminalSurface` renamed to `Submitted`
- `SubmitMailbox`
- `submitted_frame`
- submitted/prepared validation methods
- `prepareTokenForRetainedState`
- `forceFull`
- `fullPrepareReason`

Move from `surface/flow.zig` to `render/geometry.zig`:

- geometry fields and `syncGeometry` as a `GeometryOwner.sync()` method
- `prepareLayout`

Keep `source_dirty_epoch` in `SurfaceTextOwner`, not geometry.

## `SurfaceTextOwner` Replacement

Replace `flow: flow.Flow` with explicit fields:

```zig
geometry: render_geometry.GeometryOwner,
source_slot: source_slot.SourceSlot,
prepare_requests: source_prepare.PrepareRequests,
submitted: session_submitted.Submitted,
source_dirty_epoch: u64 = 0,
cursor_blink_visible: bool = true,
```

Add composition methods on `SurfaceTextOwner`:

- `nextSourceDirtyEpoch()`
- `submittedToken()`
- `syncGeometry()`
- `setCursorBlinkVisible()`
- `reservePublishSlot()`
- `commitPublishSlot()`
- `cancelPublishSlot()`
- `rejectPublishSlot()`
- `prepare()`
- `publishPrepared()`
- `submit()`
- `acceptSubmitted()`
- `pendingState()`

These methods replace `owner.flow.*` in `ffi.zig`. Do not introduce a new
umbrella owner.

## Required Import Changes

In `ffi.zig`:

- Remove `const flow = @import("surface/flow.zig");`.
- Replace `owner.flow.*` with `owner.*` composition methods.
- Replace direct `.publication_state` access with an explicit `SourceSlot`
  accessor.

In `surface/text.zig`:

- Remove `const flow = @import("flow.zig");`.
- Import new owners:
  - `../render/geometry.zig`
  - `../source/vt.zig`
  - `../source/slot.zig`
  - `../source/prepare_request.zig`
  - `../session/submitted.zig`
- `PrepareInput.state` becomes `source_vt.PublicationSource`.

In `surface/input.zig`:

- Remove `const flow = @import("flow.zig");`.
- Use `source_vt.PublicationSource` for source input parameters.

## Tests

- Move tests with their owners as recorded in
  `render-ownership-restart-scratchpad.md`.
- Rename moved tests away from `flow` vocabulary.
- Add tests:
  - `surface text owner keeps source and submitted owners separate`
  - `submitted owner has no source publication state`
  - `prepare requests do not own submitted mailbox`
  - `source slot commit returns source without prepare or submit state`

## Invariants

- No public C ABI changes.
- No compatibility shims.
- No `Flow` owner or replacement umbrella owner.
- Source input and submitted/prepared output owners are separate files and
  separate `SurfaceTextOwner` fields.
- Render still does not own backend presentation or host cadence.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`

From `/home/home/personal/projects/howl/howl-render` if needed:

- `zig build check`
- `zig build test`

## Grep Gates

No matches under `howl-render/src`:

- `flow.zig`
- `@import("flow.zig")`
- `surface/flow.zig`
- `\bFlow\b`
- `flow:`
- `owner.flow`
- `.publication_state`

No matches under `howl-render/src/source` or `howl-render/src/render`:

- `SubmitMailbox`
- `submitted_frame`
- `SubmitDecision`

No matches under `howl-render/src/source`:

- `RenderSurfaceFeedback`

No new owner names under `howl-render/src/source`, `howl-render/src/render`,
`howl-render/src/session`, or `howl-render/src/prepared`:

- `manager`
- `controller`
- `runtime`
- `screen`
- `pipeline`
- `queue`
