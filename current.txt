# Current Slice: V0 Normal Frame Present Proof

Owner: `howl-linux-host` submit diagnostics and terminal texture V0 frame classification.

Purpose:

- Prove which prepared frames present through V0 and which fall back to full RGBA.
- Record bounded fallback reasons before any RGBA deletion work is considered.
- Keep full RGBA present path as fallback/oracle.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/window/term_texture.zig`
- `howl-linux-host/src/terminal/context.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No full-RGBA deletion. No host ack/removal contract.

## Required Shape

- Count prepared submissions with no V0 sidecar.
- Count V0 frames rejected before upload because their command shape is unsupported.
- Count V0 upload/materialization fallback separately for fill-only, sprite, and glyph paths.
- Keep existing present counters for fill-only, sprite, and glyph paths.
- Log the counters through existing debug diagnostics.
- Do not turn diagnostic proof into host policy.

## Required Tests

- Add host tests for V0 frame classification when useful.
- Existing host tests pass.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if proof requires render ABI changes.
- Stop if fallback reasons require hidden policy in host rendering helpers.
