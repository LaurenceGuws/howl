# Current Slice: Render Virtual Placement ABI Deletion Accepted

Owner: workspace root.

Sprint scope: graphics deletion.

Status: accepted by reviewer, verified locally, and committed.

Accepted commits:

- `howl-render` `bab273c render: delete virtual placement ABI`
- `howl-linux-host` `f70c1cb host: drop render virtual placement fields`

## Accepted Deletion

- Deleted render-side virtual placement commit ABI fields and spans.
- Deleted render queue storage, clone/deinit, snapshot, validation, and tests for virtual
  placement input.
- Deleted host initialization/tests for render virtual placement commit fields.
- Left VT virtual placement protocol state and public VT ABI untouched.

## Verification Passed

- `zig build test --summary all` in `howl-render` (`710/713` tests passed, `3` skipped).
- `zig build --summary all` in `howl-linux-host`.
- Root `zig build --summary all`.
- Root, `howl-render`, and `howl-linux-host` `git diff --check`.

## Known Broken State

- External render ABI consumers using the deleted virtual placement commit fields break by
  design.
- Virtual Kitty refs remain VT protocol state only and are not render input.
- Unicode placeholder generated renderable placement behavior remains broken until a
  separately promoted source-backed generated-ref replacement slice.

## Next Gate

No replacement slice is active. Continue deleting Howl-only graphics ideas that are not
Kitty-identical/source-backed before replacement work.
