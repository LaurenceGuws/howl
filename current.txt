# Current Slice: Generated Placeholder Flag Deletion

Owner: howl-vt with ABI mirror follow-up as needed.

Sprint scope: graphics deletion.

## Goal

Rip out stale generated-placeholder placement flag semantics after generated placeholder
publication has been deleted.

## Delegation Requirement

- Main agent must not implement this slice directly.
- Seed a worker with this file and exact source paths.
- Seed a reviewer after worker output.

## Required Work

- Delete `HOWL_VT_GRAPHICS_PLACEMENT_GENERATED_PLACEHOLDER` and Zig mirror constants.
- Delete generated-placeholder-specific destination geometry branches from VT placement
  resolution if they now exist only for deleted generated placements.
- Update or delete tests that still require generated-placeholder placement flags.
- Touch render/host ABI mirrors only as needed to compile after VT ABI deletion.
- Do not add replacement generated refs/materialization.

## Candidate Paths

- `howl-vt/include/howl_vt.h`
- `howl-vt/src/ffi.zig`
- `howl-vt/src/kitty/graphics.zig`
- `howl-vt/src/test/terminal_graphics.zig`
- `howl-render/src/ffi_types.zig`
- `howl-linux-host/src/test/kitty_graphics_replay.zig`

## Expected Broken State

- Any consumer still checking generated-placeholder flags breaks by design.

## Tests Required

- Run impacted VT/render/host tests if feasible.
- Run root and touched submodule `git diff --check`.
- Failing builds/tests are acceptable only if reported precisely.

## Stop Conditions

- Stop if replacement generated-ref architecture is being designed.
- Stop if public ABI layout removal beyond the stale flag is required.
