# Current Slice: Omitted-r Animation Frame Upload Proof

Owner: workspace root.

Sprint scope: graphics.

## Goal

Prove real Kitty Go animation traffic where `a=f` extra-frame uploads omit `r=` and rely
on Kitty append semantics.

## Source Truth

- `utils/dev_references/terminals/kitty/kitty/graphics.h`
- `utils/dev_references/terminals/kitty/kitty/graphics.c`
- `utils/dev_references/terminals/kitty/kitty_tests/graphics.py`
- `utils/dev_references/terminals/kitty/kittens/icat/transmit.go`
- `utils/dev_references/terminals/kitty/kittens/icat/process_images.go`
- `utils/dev_references/terminals/kitty/kittens/choose_files/graphics.go`

## Target Files

- `howl-vt/src/kitty/graphics.zig`
- `howl-vt/src/test/terminal_graphics.zig`

## Required Behavior

- Tests-only unless the replay exposes a real mismatch.
- `a=f` with omitted `r=` appends the next extra frame.
- Repeated omitted-`r` `a=f` uploads append frames in order.
- Chunked omitted-`r` `a=f,m=1` captures the append target from the first chunk and
  stores exactly one appended frame at completion.
- Go-style omitted-`r` frame uploads followed by retained combined `a=a` controls run
  without `EINVAL` and advance runtime.
- Do not special-case `icat`, `choose_files`, or any other client.
- Do not change parser, render, host, ABI, decoded-cache storage, frame graph identity,
  compose/delete, media transports, or runtime timing unless a test exposes a direct bug.

## Required Tests

- Direct omitted-`r` `a=f` after root upload stores frame `2`.
- A second direct omitted-`r` `a=f` stores frame `3`.
- Chunked omitted-`r` `a=f,m=1` followed by final chunk stores exactly one appended
  frame with the expected payload.
- `icat`-style replay uses omitted-`r` frame upload plus retained `a=a,r/z`, `s=2`,
  and `s=3`, and then advances runtime.
- `choose_files`-style replay uses omitted-`r` frame upload plus retained `v=1` and
  preserves the expected loop state.
- Existing explicit-`r` animation tests continue to pass.

## Verification Required

- `zig build test --summary all` in `howl-vt`
- `zig build test:regression:build --summary all` in `howl-vt`
- root `zig build`
- root `git diff --check`

Stop condition:

- If omitted-`r` append semantics conflict with current frame-number allocation
  invariants, stop and report the smaller source-backed redesign needed.
- If a replay requires file media, shared memory, compression, render, or host behavior,
  stop and split that into a separate source-backed slice.
