# Current Slice: V0 Fill Coverage Present Path

Owner: `howl-linux-host` terminal texture V0 fill materialization.

Purpose:

- Present fill-only V0 frames that omit a first clear only when fills are proven to cover the full render surface.
- Keep partial or unproven fill-only frames on RGBA fallback.
- Keep full RGBA present path as fallback/oracle.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/window/term_texture.zig`
- `howl-linux-host/src/terminal/context.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No full-RGBA deletion. No host ack/removal contract.

## Required Shape

- Accept clear/fill-only V0 frames with a first full clear exactly as before.
- Additionally accept fill-only V0 frames without a first clear only when bounded coverage proves every pixel is filled.
- Reject/fallback for overlapping ambiguity, gaps, out-of-bounds rectangles, resource-bearing commands, or non-fill commands.
- Keep coverage proof bounded by render dimensions and existing upload row limits.
- Log coverage-present/fallback counts through existing diagnostics if useful.

## Required Tests

- Add host tests for full fill coverage acceptance and gap rejection.
- Existing host tests pass.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if coverage proof needs allocation or unbounded work.
- Stop if coverage cannot be proved without changing render ABI.
