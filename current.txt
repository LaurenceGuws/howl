# Current Slice: Graphics Placeholder Access Refresh Deletion

Owner: howl-vt.

Sprint scope: graphics.

## Goal

Delete stale accountability for run-based Unicode placeholder image access refresh.

Run-based placeholder access refresh was Howl-only behavior. Product code already removed
the refresh from `Terminal.graphicsMeta()` and `kitty.graphics.State`; this slice removes
the remaining stale test/planning records that would preserve or reintroduce it.

## Delegation Requirement

- Main agent must not implement this slice directly.
- Seed a worker with this file, the scratchpad entry, the existing `howl-vt` product-code
  deletion diff, and the required edit list below.
- Seed a reviewer after worker output and reject any diff that preserves run-based access
  refresh, broadens into materialization, or touches render/host.

## Required Work

- Delete or replace the stale test
  `kitty graphics decoded quota treats placeholder publication as image access` in
  `howl-vt/src/test/terminal_graphics.zig` so tests no longer assert run-based access
  refresh.
- Do not add a replacement unless it is source-backed and narrow.
- Replace the wrong preservation/consolidation record in `current.txt` and
  `graphics-machinery-scratchpad.md` with this deletion record.
- Record that any future replacement must be Kitty-style successful generated
  cell-ref/materialization only.
- Do not broaden to generated placement materialization.
- Do not touch render or host.

## Invariants

- `graphicsMeta()` must not refresh image access merely because placeholder runs resolve.
- Tests must not encode run-based placeholder access refresh as product behavior.
- Future access refresh, if reintroduced, must be tied to Kitty-style successful generated
  cell-ref/materialization, not a public metadata/run-count walk.
- Placeholder-run count/query and generated placement publication remain current behavior.
- Render, host, FFI, and public ABI remain unchanged.

## Tests Required

- `zig build test --summary all` in `howl-vt`.
- `zig build test:regression:build --summary all` in `howl-vt` if unit tests pass.
- Root `git diff --check`.
- `howl-vt` `git diff --check`.

## Stop Conditions

- Stop if the cleanup requires generated placement materialization.
- Stop if render, host, FFI, or public ABI changes become necessary.
- Stop if placeholder-run geometry, count, or ordering semantics need to change.

## Verification

- `zig build test --summary all` in `howl-vt`.
- `zig build test:regression:build --summary all` in `howl-vt`.
- Root and `howl-vt` `git diff --check`.
