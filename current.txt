# Current Slice: None

Owner: workspace root.

Sprint scope: graphics deletion.

Status: `Host Graphics Proof API Deletion` completed and accepted.

Accepted commits:

- `howl-linux-host` `1c1e940 host: delete graphics proof APIs`

Verification passed:

- `zig build test:integration:kitty-graphics-replay:build --summary all` in
  `howl-linux-host`.
- `zig build test:integration:kitty-graphics-replay:app:build --summary all` in
  `howl-linux-host`.
- Root `zig build --summary all`.
- Root `git diff --check`.
- `howl-linux-host git diff --check`.

Next work must continue deleting/quarantining Howl-only graphics machinery before any
replacement planning.
