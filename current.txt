# Current Slice: Move Render Source Text Input Adapter

Owner: `howl-render`.

Purpose:

- Move the final render `surface/*` file to its true owner: source publication to text-scene input adaptation.
- Preserve source cell/style/color/cursor mapping and text frame input behavior exactly.
- Delete the stale render `surface/` folder from source metadata.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/render-surface-taxonomy.md`
- full `howl-render/src/surface/input.zig`
- `howl-render/src/session/text.zig`
- `libs.yaml`

## Source Changes

Exact files:

- Move `howl-render/src/surface/input.zig` to `howl-render/src/source/text_input.zig`.
- Update `howl-render/src/session/text.zig` import.
- Update internal imports in moved file.
- Update `libs.yaml` owner metadata.

## Non-Goals

- Do not split mapping/theme/style functions in this slice.
- Do not change source publication, text scene, frame preparation, or session behavior.
- Do not change C ABI or FFI behavior.
- Do not add a replacement `surface` bucket or compatibility alias.

## Invariants

- `source/text_input.zig` owns source publication to text-scene/frame input adaptation.
- `surface/input.zig` no longer exists.
- No imports or metadata refer to `howl-render/src/surface/`.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'surface/' howl-render/src libs.yaml` prints nothing for `howl-render`.
- `rg 'manager|controller|runtime|types\.zig|api\.zig|abi\.zig' howl-render/src/source/text_input.zig` prints nothing relevant to this slice.

## Stop Conditions

- Stop if moving the adapter requires splitting theme, style, or source publication behavior.
- Stop if behavior changes are needed to empty the stale folder.
