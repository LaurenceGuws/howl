# Current Slice: Flatten Terminal Policy Config Wrappers

Owner: workspace root.

Purpose:

- Kill terminal config fake grouping wrappers that own no lifetime.
- Keep true lifetime owners for loaded strings, font stacks, and bindings.
- Preserve Lua config syntax, defaults, parse behavior, heap ownership, and deinit.

## Source Changes

Edit only:

- `howl-linux-host/src/config/terminal.zig`
- `howl-linux-host/src/main.zig`
- `howl-linux-host/src/terminal/context.zig`

In `terminal.zig`, remove these wrapper structs:

- `Clipboard`
- `Links`
- `MousePolicy`
- `Cursor`

Keep:

- `Config`
- `FontStack`
- `Input.Bindings`
- policy enums

In `terminal.Config`, replace:

- `cursor: Cursor`
- `clipboard: Clipboard`
- `links: Links`
- `mouse: MousePolicy`

with direct fields:

- `cursor_style: CursorStyle`
- `cursor_blink: bool`
- `clipboard_osc_52: ClipboardOsc52Policy`
- `link_open: LinkOpenPolicy`
- `link_hover: LinkHoverPolicy`
- `link_underline: LinkUnderlineStyle`
- `mouse_bypass_mod: Input.Mod`

Update all call sites in `main.zig` and `terminal/context.zig` to use direct field names.

## Non-Goals

- Do not rename `Config.State`.
- Do not remove or change `FontStack`.
- Do not remove window mouse policy.
- Do not remove window bindings.
- Do not change config file syntax.

## Invariants

- `Config.deinit()` still frees only `shell`, `start_path`, `command`, `fonts`, and
  `bindings`.
- Cursor default remains `block` and blink `true`.
- Clipboard OSC 52 default remains `deny`.
- Link defaults remain `disabled`, `underline_and_cursor`, and `straight`.
- Mouse bypass default remains empty `Input.Mod{}`.
- No new heap allocations or deinit paths.
- No aliases or shim wrapper structs.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

No matches under `howl-linux-host/src/config/terminal.zig`:

- `pub const Clipboard = struct`
- `pub const Links = struct`
- `pub const MousePolicy = struct`
- `pub const Cursor = struct`

No matches under `howl-linux-host/src`:

- `.conf.links`
- `.conf.clipboard`
- `.conf.term.mouse`
- `.cursor.style`
- `.cursor.blink`

Expected matches:

- `cursor_style`
- `cursor_blink`
- `clipboard_osc_52`
- `link_open`
- `link_hover`
- `link_underline`
- `mouse_bypass_mod`
