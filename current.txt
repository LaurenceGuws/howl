Current Task

Source:

- host graphics crash repro from deterministic Kitty image send path

Promoted Item:

- Add a host regression surface for the Kitty graphics crash repro and use it to fix the crash.

Required Output:

- A host-owned regression harness exists for the deterministic Kitty graphics image repro.
- The regression fails on the current crash path until fixed.
- The crash is fixed and the regression passes.

Hard Constraints:

- Use a real Zig-owned repro/harness, not the Python helper as proof.
- Keep the harness owner-true to the host integration boundary.
- Use direct assertions and deterministic inputs.
- No ABI-boundary bypass.
