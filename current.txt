# Current Slice: Graphics Placement ABI Mirror Safety

Owner: workspace root.

Sprint scope: graphics.

Promoted from `graphics-generated-placement-transition-scratchpad.md` atomic commit sequence item 1.

## Goal

Make the smallest coordinated placement ABI mirror change needed to support future generated placeholder placement publication, with no behavior change.

## Target Repos And Files

VT:

- `howl-vt/include/howl_vt.h`
- `howl-vt/src/ffi.zig`

Render:

- `howl-render/src/ffi_types.zig`

Host:

- Inspect `howl-linux-host/src/terminal/vt/surface.zig` and generated C import consumers. Edit only if compile drift requires it.

## Required Shape

- If render needs to distinguish future generated placeholder placements to enforce mixed-stream rejection, add only a tail `flags` field to `HowlVtGraphicsPlacement` and mirror it in render.
- Define a public generated-placeholder flag constant only if the field is added.
- All existing placements must publish `flags == 0`.
- Do not add provenance/debug fields such as virtual placement index, placeholder run order, image row, or image col in this slice.
- Do not emit generated placements in this slice.
- Do not alter `placeholder_run_count` behavior in this slice.

## Hard Constraints

- No behavior change.
- No render placeholder path changes.
- No generated placement publication.
- Keep C header and Zig extern structs synchronized exactly.
- ABI is the product. Do not change one repo without the mirror.

## Verification Required

- `zig build test` in `howl-vt`
- `zig build test` in `howl-render`
- `zig build test:unit` in `howl-linux-host`
- `git diff --check` at workspace root

Stop condition:

- If a `flags` tail field is not actually needed for the next render guard, stop and report why no ABI change should be made. Do not add speculative fields.
