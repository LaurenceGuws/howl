# Current Slice: Split Terminal Selection And Link Owners

Owner: workspace root.

Purpose:

- Move host selection gesture behavior out of broad `terminal/context.zig` into a bounded terminal
  selection owner.
- Move visible-link hover/activation behavior out of broad `terminal/context.zig` into a bounded
  terminal links owner.
- Preserve event routing and behavior exactly.

## Source Research

Use `host-owner-next-scratchpad.md` Slice D as the sprint scratchpad.

Reference pressure:

- Ghostty `Surface` owns terminal widget input behavior while terminal core owns selection truth.
- Alacritty input processor uses an `ActionContext` trait to mutate terminal/display/window through
  explicit methods.
- TigerBeetle rejects broad buckets and fake owner nouns.

## Current Source Facts

- `howl-linux-host/src/terminal/context.zig` currently owns too much:
  - terminal event routing/orchestration
  - host selection gesture helper behavior
  - visible link hover/activation helper behavior
- Current terminal context fields remain context-owned in this slice.
- Pointer events remain routed by `Context`; this slice moves helper behavior only.

## Source Changes

Edit only:

- `howl-linux-host/src/terminal/context.zig`
- new `howl-linux-host/src/terminal/selection.zig`
- new `howl-linux-host/src/terminal/links.zig`

Required shape:

- Add `terminal/selection.zig`.
- Add `terminal/links.zig`.
- Move host selection gesture helper functions from `terminal/context.zig` to `terminal/selection.zig`
  as bounded free functions over `context: anytype` where existing storage remains on `Context`.
- Move visible-link hover/activation helper functions from `terminal/context.zig` to
  `terminal/links.zig` as bounded free functions over `context: anytype` where existing storage
  remains on `Context`.
- `terminal/context.zig` imports these as `terminal_selection` and `terminal_links`.
- `Context` continues to own event routing and calls into these owners at the existing decision
  points.
- Keep existing field layout unless a tiny visibility adjustment is required for moved functions.

## Non-Goals

- Do not create `terminal/pointer.zig`.
- Do not move mouse protocol encoding.
- Do not change event routing.
- Do not change selection behavior.
- Do not change link hover/activation behavior.
- Do not introduce new state wrappers just to move code.
- Do not introduce `manager`, `controller`, `runtime`, `flow`, `queue`, `utils`, `types`, `api`, or
  `abi` owner files.
- Do not edit SDL/OpenGL/build files.

## Invariants

- Selection state mutation remains exact.
- Pending selection copy behavior remains exact.
- Link hover detection and cursor changes remain exact.
- Link activation remains exact.
- Host input and VT mouse encoding paths remain exact.
- Context remains the event-routing owner.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build check`
- `zig build test`
- `git diff --check`

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- No `howl-linux-host/src/terminal/pointer.zig`.
- No new owner files named `manager`, `controller`, `runtime`, `flow`, `queue`, `utils`, `types`,
  `api`, or `abi`.
- `terminal/context.zig` imports `terminal_selection` and `terminal_links`.
- `terminal/selection.zig` owns moved selection helper functions.
- `terminal/links.zig` owns moved visible-link helper functions.
