# Current Slice: Normalize Render Test Categories

Owner: `howl-render`.

Purpose:

- Make render package test steps match the root build/test contract.
- Split render deterministic test execution into `test:unit` and `test:abi` categories without
  changing product code or test assertions.
- Preserve root aggregation for `zig build test`, `zig build test:unit`, and `zig build test:abi`.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/build-test-architecture-plan.md`
- `build.zig`
- `howl-render/build.zig`
- `howl-render/src/test.zig`
- `howl-render/src/test/unit.zig`
- `howl-render/src/test/ffi.zig`

## Source Changes

Exact files:

- `howl-render/build.zig`
- Add render test entry roots only if direct roots cannot preserve current coverage cleanly.

## Required Shape

- Add `test:unit` and `test:unit:build` for render owner-local tests.
- Add `test:abi` and `test:abi:build` for render C ABI/FFI tests.
- Keep `test` as the aggregate of `test:unit` and `test:abi`.
- Keep `check` compiling the shipped render C ABI library and fast compile-only proof targets.

## Non-Goals

- Do not change render product code.
- Do not change test assertions or behavior.
- Do not change C headers or exported symbols.
- Do not make private helpers public for tests.
- Do not redesign benchmark ownership in this slice unless the existing build shape blocks category
  normalization.

## Invariants

- Root `zig build test`, `zig build test:unit`, and `zig build test:abi` work.
- Package `zig build test`, `zig build test:unit`, and `zig build test:abi` work from `howl-render`.
- Render ABI tests still cover `libhowl_render.zig` exported declarations and `test/ffi.zig`.
- Render unit tests still cover `test/unit.zig`.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `zig build test:unit`
- `zig build test:abi`
- `git diff --check`

## Grep Gates

- `rg 'test:unit|test:abi|test:unit:build|test:abi:build' howl-render/build.zig`
- `rg 'test:unit|test:abi' build.zig howl-render/build.zig`
- `rg 'pub fn|pub const' howl-render/src/test` must not show broad public helper aliases added for tests.

## Stop Conditions

- Stop if splitting render tests requires product-code visibility changes.
- Stop if `test/ffi.zig` depends on unit-only imports in a way that requires moving product code.
- Stop if benchmark compilation requires a separate owner decision.
