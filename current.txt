# Current Slice: PNG Palette Fixture VT Proof

Owner: workspace root.

Sprint scope: graphics.

## Goal

Prove valid Kitty-compatible palette PNG (`P`) and palette transparency (`tRNS`)
normalization through Howl's VT graphics publication path before considering any decoder
replacement.

## Source Truth

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `utils/dev_references/terminals/kitty/kitty/png-reader.c`
- `utils/dev_references/terminals/kitty/kitty_tests/graphics.py`
- `utils/official_docs/kitty/graphics-protocol.md`
- `graphics-machinery-scratchpad.md` section `Palette PNG blocker re-research`

## Target Files

- `howl-vt/src/test/terminal_graphics.zig`
- `howl-vt/src/kitty/graphics.zig` only if a valid fixture exposes a direct VT-path bug

## Required Behavior

- Tests-only unless a valid palette fixture fails through Howl's VT path.
- Valid palette PNG input must publish normalized RGBA bytes at the VT graphics model
  boundary.
- Valid palette PNG with `tRNS` transparency must publish exact alpha bytes.
- Preserve existing RGBA/RGB/L PNG normalization behavior.
- Preserve invalid PNG `EBADPNG` behavior.
- Do not replace the PNG decoder, add custom palette expansion, change render, ABI,
  host, PNG zlib, color management, storage model, or dependency policy.

## Required Tests

- Fixed palette PNG fixture publishes expected palette-expanded RGBA base64.
- Fixed palette PNG with `tRNS` publishes expected RGBA base64 with transparent alpha.
- Existing PNG normalization tests remain green.
- Existing invalid PNG `EBADPNG` tests remain green.

## Verification Required

- `zig build test --summary all` in `howl-vt`
- `zig build test:regression:build --summary all` in `howl-vt`
- root `zig build`
- root `git diff --check`
- `howl-vt` `git diff --check`

Stop condition:

- If a valid palette fixture is rejected by direct vendored stb but accepted by
  Kitty/libpng, stop and report the decoder policy slice needed.
- If proving parity requires adding/replacing a PNG decoder, ABI/render/host changes,
  color-management policy, or storage-model work, stop and split the work.
