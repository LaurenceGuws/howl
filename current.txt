# Current Slice: Graphics Decoded Render Bridge Research

Owner: workspace research; likely future owners are howl-render and howl-linux-host,
with howl-vt ABI as source input.

Sprint scope: graphics.

## Goal

Design the next source-backed slice after VT decoded graphics publication: make in-tree
host/render paths consume VT-decoded graphics bytes without breaking existing render or
VT C ABI contracts.

## Source Truth

- `AGENTS.md`
- `loop.txt`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `utils/dev_references/terminals/kitty/kitty/graphics.c`
- `utils/dev_references/terminals/kitty/kitty/graphics.h`
- `utils/dev_references/terminals/ghostty/src/terminal/kitty/graphics_image.zig`
- `utils/dev_references/terminals/ghostty/src/terminal/kitty/graphics_storage.zig`
- `howl-vt/include/howl_vt.h`
- `howl-vt/src/ffi.zig`
- `howl-render/include/howl_render.h`
- `howl-render/src/frame/graphics_prepare.zig`
- `howl-render/src/frame/queue.zig`
- `howl-render/src/frame/surface_text.zig`
- `howl-render/src/frame/surface_buffer.zig`
- `howl-linux-host/src/terminal/vt/surface.zig`
- `howl-linux-host/src/terminal/render/retained.zig`
- `howl-linux-host/src/terminal/terminal_panel.zig`

## Research Questions

- Which render C ABI structs/functions currently imply base64/protocol payload bytes?
- Can render add a parallel decoded graphics publication path without breaking existing
  base64 render ABI?
- Should the Linux host switch to VT decoded copy APIs first while render still accepts
  base64, or must render gain decoded ABI first?
- What is the smallest cross-submodule sequence: render ABI addition, host switchover,
  then old base64 path deprecation later?
- How should render raster cache keys change when bytes are already decoded RGBA/RGB?
- What tests prove old render ABI compatibility, decoded render ingestion, and host
  end-to-end behavior without broad cache/quota redesign?

## Required Output

- Research only. Do not edit product code.
- Return source-backed findings with exact file/line references.
- Identify the ABI-safe owner split among VT decoded publication, host copied bytes, and
  render raster/cache realization.
- Draft one worker-ready implementation slice if safe; otherwise draft a narrower child
  research slice.

## Out Of Scope

- No code implementation in this slice.
- No removal of old VT or render base64 payload ABI.
- No host protocol decode.
- No quota/eviction redesign.
- No renderer-side Kitty protocol interpretation.

## Verification

- Scratchpad update must pass root `git diff --check`.

Stop conditions:

- Stop if render ABI compatibility needs a product-breaking decision.
- Stop if the source-backed first implementation slice cannot be made smaller than a
  broad render/cache rewrite.
