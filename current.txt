# Current Slice: Document VT Consequence Capacity Contracts

Owner: `howl-vt`.

Purpose:

- Make current bounded-but-heap-backed VT consequence storage contracts explicit in owner code.
- Preserve behavior while documenting exact capacity ownership and proof gaps.
- Avoid fake static-storage conversion before capacity decisions are source-backed.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/vt-host-consequence-capacity.md`
- `howl-vt/src/host/state.zig`
- `howl-vt/src/parser/events.zig`

## Source Changes

Exact files:

- `howl-vt/src/host/state.zig`
- `howl-vt/src/parser/events.zig`

Exact changes:

- Add compile-time assertions for host consequence capacity constants.
- Add compile-time assertions for parser event/string-control capacity constants.
- Add a short owner comment above `host.State` stating that heap storage is currently bounded by
  constants in `host/state.zig` and owned by host consequence retention.
- Add a short owner comment above `ParsedEvents` stating that parser-event materialization is bounded
  for queued events and string-control payloads, with aggregate byte/int/aux storage still a proof gap.

## Non-Goals

- Do not convert `ArrayList` storage.
- Do not alter allocation behavior.
- Do not change OSC 52, hyperlink, title, DCS, parser event, or report semantics.
- Do not add static buffers, capacity structs, managers, controllers, runtimes, `types.zig`, `api.zig`, or `abi.zig`.

## Invariants

- `pending_output_max_bytes == parser.max_large_osc_control_bytes`.
- `retained_payload_max_bytes == parser.max_large_osc_control_bytes`.
- `retained_metadata_max_bytes == parser.max_metadata_control_bytes`.
- `title_max_bytes <= retained_metadata_max_bytes`.
- `hyperlink_target_max_count > 0`.
- `ParsedEvents.max_queued_events > 0`.
- `parser.max_metadata_control_bytes > 0`.
- `parser.max_apc_control_bytes > parser.max_metadata_control_bytes`.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'pending_output_max_bytes|retained_payload_max_bytes|retained_metadata_max_bytes|title_max_bytes|hyperlink_target_max_count|max_queued_events' howl-vt/src/host/state.zig howl-vt/src/parser/events.zig`
- `rg 'manager|controller|runtime|types\.zig|api\.zig|abi\.zig' howl-vt/src/host/state.zig howl-vt/src/parser/events.zig` prints nothing relevant to this slice.

## Stop Conditions

- Stop if a capacity assertion fails and requires a product capacity decision.
- Stop if documenting capacity reveals behavior changes are needed.
- Stop if static storage conversion looks tempting; that belongs to a later capacity-backed slice.
