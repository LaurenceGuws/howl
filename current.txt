# Current Slice: V0 Resource Plan Status Diagnostics

Owner: `howl-linux-host` submit diagnostics.

Purpose:

- Replace generic `no_sidecar_invalid` with the exact prepared V0 resource-plan status.
- Make `rain` and interactive shell fallback explain whether host rejected upload, resource, command,
  span, version, or byte-limit invariants.
- Keep diagnostics-only behavior; do not alter fallback or presentation policy.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/terminal/context.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No rendering behavior changes. No full-RGBA deletion.

## Required Shape

- Keep existing cumulative and interval counters.
- Add the latest prepared resource-plan status tag to V0 diagnostic logs.
- The status must come from `PreparedProtocolV0ResourcePlanStatus`, not a new unbounded string source.
- Do not use resource-plan diagnostics to change fallback or present policy.

## Required Tests

- Existing host tests pass.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`
