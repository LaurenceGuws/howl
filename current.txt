# Current Slice: Record Render FFI Lane Ownership

Owner: workspace root.

Purpose:

- Update persistent project owner docs after moving render C translators into `src/ffi/`.
- Record that `src/ffi.zig` is only the C import root, while `src/ffi/*` files translate ABI nouns.
- Remove stale render owner-map entries that still point at root translator files.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/render-c-lane.md`
- `howl-render/design.md`
- `libs.yaml`
- current `howl-render/src/libhowl_render.zig`
- current `howl-render/src/ffi.zig`
- current `howl-render/src/ffi/*.zig`

## Source Changes

Edit only:

- `howl-render/design.md`
- `libs.yaml`

## Required Shape

- `howl-render/design.md` must say `src/ffi.zig` imports `include/howl_render.h` only.
- `howl-render/design.md` must say `src/ffi/` contains C ABI translators grouped by ABI noun.
- `howl-render/design.md` must not claim root `src/text_session.zig` composes the C handle.
- `libs.yaml` must map moved render translator owners under `howl-render/src/ffi/`.
- Keep owner names specific; no generic `api`, `types`, `manager`, `controller`, `engine`, or `runtime` bucket.
- Do not rewrite old research evidence paths in `research/2026-05-30-hygiene-audit/render-c-lane.md`; it is historical evidence.

## Non-Goals

- Do not edit code.
- Do not rename `src/ffi.zig`.
- Do not move `submit_result.zig`.
- Do not change public headers or ABI symbols.
- Do not change README unless verification shows it is stale for this exact ownership fact.

## Verification

From `/home/home/personal/projects/howl`:

- `git diff --check`
- `rg 'src/text_session\.zig|src/work_state\.zig|src/vt_surface\.zig|src/prepared_surface\.zig|src/submission\.zig|src/surface_geometry\.zig|src/prepare_request\.zig' howl-render/design.md libs.yaml`
- `rg 'types\.zig|api\.zig|manager|controller|engine|runtime' howl-render/design.md libs.yaml`

Expected results:

- The stale root translator path grep prints nothing.
- The banned-vocabulary grep prints nothing except existing unrelated historical words if already present outside the changed render section; if it prints, review context and do not introduce new banned owners.

## Stop Conditions

- Stop if documenting the lane requires deciding a code rename for `src/ffi.zig`.
- Stop if `libs.yaml` cannot represent `src/ffi.zig` plus `src/ffi/*` without a vague bucket.
