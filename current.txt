# Current Slice: V0 Retained Sprite Patch Present Path

Owner: `howl-linux-host` terminal texture V0 command materialization.

Purpose:

- Present partial no-clear clear/fill/sprite V0 frames as retained texture patches when prior matching texture content exists.
- Keep first-frame, resized, uninitialized, or unsupported patch frames on RGBA fallback.
- Keep full RGBA present path as fallback/oracle.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/window/term_texture.zig`
- `howl-linux-host/src/terminal/context.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No full-RGBA deletion. No host ack/removal contract.

## Required Shape

- Accept partial no-clear clear/fill/sprite V0 frames only if the terminal texture existed before upload and matched the frame size.
- Reject/fallback if the texture was newly created/resized in the current turn.
- Reject/fallback for glyph commands, unsupported command shapes, missing sprite resources, or out-of-bounds rects.
- Preserve existing full-clear sprite path.
- Log retained sprite patch present/fallback counts.

## Required Tests

- Add host tests for retained sprite patch detection and glyph rejection.
- Existing host tests pass.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if retained sprite patch safety needs render ABI changes.
- Stop if command ordering cannot be preserved with host-owned backend state.
