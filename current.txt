# Current Slice: Normalize Render Test Build Coverage

Owner: `howl-render`.

Purpose:

- Make render `test:build` compile the split test categories rather than the old combined root.
- Keep `check` depending on package-local compile-only test coverage.
- Preserve test run behavior and product code exactly.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/build-test-architecture-plan.md`
- `research/2026-05-30-hygiene-audit/render-test-build-coverage.md`
- `howl-render/build.zig`
- `howl-pty/build.zig`
- `howl-vt/build.zig`

## Source Changes

Exact files:

- `howl-render/build.zig`

## Required Shape

- Keep render `test:build` as a package-local aggregate.
- Make `test:build` depend on `test:unit:build` and `test:abi:build`.
- Keep `check` depending on `test:build`.

## Non-Goals

- Do not change render product code.
- Do not change test bodies or assertions.
- Do not change C headers or exported symbols.
- Do not remove the old combined `test-render` root in this slice.

## Invariants

- `howl-render` `zig build test:build` builds unit and ABI category roots.
- `howl-render` `zig build check` still builds the shipped C ABI library and compile-only proof targets.
- Root `zig build check` still passes.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- from `howl-render`: `zig build test:build`
- from `howl-render`: `zig build test:unit:build`
- from `howl-render`: `zig build test:abi:build`
- `git diff --check`

## Grep Gates

- `rg 'test_build_step\.dependOn|test_unit_build_step|test_abi_build_step|check_step\.dependOn' howl-render/build.zig`
- `rg 'test_build_step\.dependOn\(&tests\.step\)' howl-render/build.zig` prints nothing.

## Stop Conditions

- Stop if `test:build` cannot build both category roots without changing product code.
- Stop if the old combined root is required for coverage not represented by `test:unit` or `test:abi`.
