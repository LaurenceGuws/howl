# Current Stage: Research Render V0 Source Owner Paths

Owner: workspace root research workflow.

Source scratchpad:

- `research/2026-06-01-render-api-language-deletion-sprint.md`

Accepted research cache:

- `research/cache-2026-06-01-render-api-language-deletion.md`

Completed slices:

- `howl-render` `aa1749d` moved V0 oracle/equivalence tests under unit.
- `howl-render` `6bf6388` deleted the separate protocol-proof build/test bucket.

## Purpose

Produce the missing evidence needed to promote Slice 3: deleting
`howl-render/src/protocol_v0/` as an internal source bucket without guessing
replacement owner paths.

No product code is authorized by this stage.

## Research Questions

- What exact owner path should replace `howl-render/src/protocol_v0/emit.zig`?
- What exact owner path should replace `howl-render/src/protocol_v0/realize.zig`?
- Should `howl-render/src/ffi/protocol_v0.zig` move in the same slice or remain
  as ABI-layout owner while public C ABI names are preserved?
- Which imports/tests must change for the source-bucket deletion slice?
- What grep gates prove `src/protocol_v0/` is gone while public ABI vocabulary is
  preserved?

## Required Output

- A focused research cache with exact source paths, line-backed owner evidence,
  proposed replacement paths, risks, stop conditions, and readiness judgment.
- Reviewer acceptance before the scratchpad is updated.
- No worker seeding until `current.txt` is replaced with one exact
  implementation slice.

## Hard Stops

- Stop if owner paths are invented from preference rather than source-backed
  responsibility.
- Stop if public ABI names are renamed as part of internal source cleanup.
- Stop if host behavior changes enter the slice.
