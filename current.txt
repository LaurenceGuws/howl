# Current Slice: Move Render Prepared Handle Owner

Owner: `howl-render`.

Purpose:

- Move prepared handle lifecycle owner out of the stale `surface/*` bucket.
- Preserve prepared handle ABI, lifecycle, submit, diagnostics, and buffer behavior exactly.
- Continue reducing render `surface/*` taxonomy debt one exact owner at a time.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/render-surface-taxonomy.md`
- `howl-render/src/surface/prepared_owner.zig`
- current importers of `surface/prepared_owner.zig`
- `libs.yaml`

## Source Changes

Exact files:

- Move `howl-render/src/surface/prepared_owner.zig` to `howl-render/src/prepared/owner.zig`.
- Update importers:
  - `howl-render/src/session/text.zig`
  - `howl-render/src/ffi/submission.zig`
  - `howl-render/src/ffi/prepared_surface.zig`
  - `howl-render/src/ffi/handle.zig`
- Update internal imports in moved file.
- Update `libs.yaml` owner metadata.

## Non-Goals

- Do not move token contracts.
- Do not move source publication, source/text adaptation, geometry, clip, or rgba files.
- Do not change prepared handle state machine or ABI behavior.
- Do not add compatibility aliases or umbrella replacement buckets.

## Invariants

- `prepared/owner.zig` owns prepared handle lifecycle and copied prepared RGBA payload.
- `surface/prepared_owner.zig` no longer exists.
- No imports refer to `surface/prepared_owner.zig`.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'surface/prepared_owner\.zig|prepared_owner\.zig' howl-render/src libs.yaml` prints nothing.
- `rg 'manager|controller|runtime|types\.zig|api\.zig|abi\.zig' howl-render/src/prepared/owner.zig` prints nothing relevant to this slice.

## Stop Conditions

- Stop if moving prepared owner requires token, session, FFI, or submit behavior changes.
- Stop if import rewiring exposes a broader prepared/session taxonomy decision.
