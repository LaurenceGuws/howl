# Current Slice: Render Prepared FFI Status Boundary

Active research:

- `research/cache-2026-06-02-render-prepared-ffi-status.md`

Reviewer acceptance:

- This slice is ready and narrow: move prepared render-surface retrieval status mapping out of `prepared/owner.zig` and into `ffi/prepared_surface.zig`.
- Preserve `HowlRenderPreparedSurface` behavior and avoid public ABI changes.

## Problem

`prepared/owner.zig` still imports C ABI and stores render-surface retrieval status directly. Replace that with owner-local failure truth and keep FFI as the only status translator.

## Allowed Files

- Edit: `howl-render/src/prepared/owner.zig`
- Edit: `howl-render/src/ffi/prepared_surface.zig`
- Edit: `howl-render/src/test/ffi.zig`
- Edit: owner-local tests in `howl-render/src/prepared/owner.zig` only if needed by compile/test fallout

## Required Shape

- Remove `const c = @import("../ffi.zig").c;` from `prepared/owner.zig`.
- Replace render-surface status storage with owner-local failure truth, not C ABI status.
- Keep `Owner.renderSurface()` returning `?*const render_surface_emitter.Surface`.
- Add or keep a narrow owner method exposing owner-local failure truth.
- Move C status mapping to `ffi/prepared_surface.zig`.
- Preserve `howl_render_prepared_surface_render_surface` behavior.

## Required Tests

- Keep existing FFI status stability and retrieval failure tests green.
- Convert owner-local assertions to owner-local failure truth, not C constants.
- Keep FFI retrieval mapping assertions at the FFI seam.

## Verification

- `zig build test --summary all`
- `zig build check`
- `git diff --check`

## Grep Gates

- `prepared/owner.zig` has zero `../ffi.zig` imports.
- `prepared/owner.zig` has zero `HOWL_RENDER_PREPARED_SURFACE_RENDER_SURFACE_` matches.
- `render_surface_status` has zero matches.
- C retrieval status mapping exists only in `ffi/prepared_surface.zig`.

## Stop Conditions

- Stop if public ABI/header changes appear necessary.
- Stop if owner-local failure truth cannot preserve current C status mapping.
- Stop if tests require a new test root.
