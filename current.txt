# Current Slice: Delete Stale Render Surface Remainders

Owner: `howl-render`.

Purpose:

- Delete unreferenced stale `surface/*` files after exact owners already exist elsewhere.
- Preserve render behavior and C ABI exactly.
- Continue reducing stale render `surface/*` taxonomy without touching source/text adaptation.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/render-surface-taxonomy.md`
- `howl-render/src/surface/publication_source.zig`
- `howl-render/src/source/vt.zig`
- `howl-render/src/surface/clip_rect.zig`
- grep results for `publication_source`, `SourceRgb`, `ClipRect`, and `clipRect`
- `libs.yaml`

## Source Changes

Exact files:

- Delete `howl-render/src/surface/publication_source.zig`.
- Delete `howl-render/src/surface/clip_rect.zig`.
- Remove both owner entries from `libs.yaml`.

## Non-Goals

- Do not delete or move `surface/input.zig`.
- Do not delete or move `surface/rgba.zig` in this slice.
- Do not edit `source/vt.zig`.
- Do not change C ABI, FFI layout assertions, source publication behavior, or render geometry behavior.

## Invariants

- `source/vt.zig` remains the source publication contract owner.
- No source imports `publication_source.zig` or `clip_rect.zig`.
- `libs.yaml` must not reference deleted files.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'publication_source|clip_rect|surface/publication_source\.zig|surface/clip_rect\.zig' howl-render/src libs.yaml` prints nothing.

## Stop Conditions

- Stop if either file is imported by build/test paths not captured by grep.
- Stop if deletion requires replacing symbols or adding compatibility aliases.
