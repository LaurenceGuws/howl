# Current Slice: Delete Render `surface/types.zig`

Owner: workspace root.

Purpose:

- Delete `howl-render/src/surface/types.zig`.
- Split every symbol into its smallest true owner. Do not create another
  ownerless bucket or compatibility shim.
- Keep `howl-render/include/howl_render.h` byte-for-byte unchanged.

## Source Files

- `howl-render/src/surface/types.zig`
- `howl-render/src/surface/text.zig`
- `howl-render/src/surface/input.zig`
- `howl-render/src/surface/prepared_owner.zig`
- `howl-render/src/surface/buffer.zig`
- `howl-render/src/surface/clip_rect.zig`
- `howl-render/src/surface/geometry.zig`
- `howl-render/src/ffi.zig`
- `howl-render/src/source/vt.zig`
- `howl-render/src/source/slot.zig`
- `howl-render/src/source/prepare_request.zig`
- `howl-render/src/render/geometry.zig`
- `howl-render/src/test/benchmark.zig`
- `howl-render/src/test/unit.zig`

## Files To Add

- `howl-render/src/render/geometry_contract.zig`
- `howl-render/src/source/cell.zig`
- `howl-render/src/prepared/surface.zig`
- `howl-render/src/prepared/feedback.zig`

## File To Delete

- `howl-render/src/surface/types.zig`

Do not leave a shim or compatibility import.

## Symbol Movement

Move from `surface/types.zig` to `render/geometry_contract.zig`:

- `CellSize`
- `PixelSize`
- `GridSize`
- `FramePixels`
- `Geometry`
- `GeometryResponse`
- `PrepareLayout`
- `SurfaceLayout`

Move from `surface/types.zig` to `source/cell.zig`:

- `Color`
- `CellFlags`
- `CellAttrs`
- `UnderlineStyle`
- `Cell`
- `GridModel`
- `DamageInfo`
- `ViewportInfo`
- `CursorShape`
- `CursorInfo`
- `FrameData`

Move from `surface/types.zig` to `prepared/surface.zig`:

- `PreparedSurface`
- methods on `PreparedSurface`

Move from `surface/types.zig` to `prepared/feedback.zig`:

- `RenderSurfaceHandle`
- `RenderMetrics`
- `RenderSurfaceFeedback`

Delete if unused rather than moving:

- `SurfaceState`
- `FillRect`
- `GlyphQuad`
- `AtlasUpload`
- `RenderStats`
- `PrepareMetrics` only if all users can use the true text/render metrics owner

## Required Import Changes

Rename local aliases to true owners. Do not use `surface_types`, `types`, or
`*_types` aliases.

In `ffi.zig`:

- Import `render/geometry_contract.zig`, `source/cell.zig`,
  `prepared/surface.zig`, and `prepared/feedback.zig` only for the symbols that
  cross the C boundary.

In `surface/text.zig`:

- Import geometry contracts, prepared surface, prepared feedback, and source
  cell contracts explicitly.

In `surface/input.zig`:

- Import `source/cell.zig` for legacy cell tests and source-to-scene mapping.

In `surface/prepared_owner.zig` and `surface/buffer.zig`:

- Import prepared surface/feedback and geometry contracts explicitly.

In `source/*`:

- Import `source/cell.zig` for source cursor/cell structs only.
- Import `render/geometry_contract.zig` only when a prepare request must carry
  render layout.

## Tests

- Move tests with the owners when practical.
- Keep behavior unchanged.
- Add at least one compile/usage test proving `FramePixels` and `PreparedSurface`
  are no longer in the same owner module.

## Invariants

- No public C ABI changes.
- No compatibility shims.
- No `types.zig` file remains under `howl-render/src`.
- No `surface_types`, `types`, or `*_types` aliases remain under
  `howl-render/src`.
- Source input and prepared/submitted output owners stay separate.
- Render still does not own backend presentation or host cadence.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`

From `/home/home/personal/projects/howl/howl-render` if needed:

- `zig build check`
- `zig build test`

## Grep Gates

No matches under `howl-render/src`:

- `types.zig`
- `@import\(.*types.zig`
- `surface_types`
- `\btypes\b`

No new owner names under `howl-render/src/source`, `howl-render/src/render`,
`howl-render/src/session`, or `howl-render/src/prepared`:

- `manager`
- `controller`
- `runtime`
- `screen`
- `pipeline`
- `queue`
