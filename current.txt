# Current Slice: Decide VT Regression Gate Policy

Owner: `howl-vt`.

Purpose:

- Decide whether deterministic VT regression proofs belong in canonical `zig build test`.
- Preserve the decision and review evidence in project memory.
- Do not change build code unless regression fits ordinary deterministic test expectations.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/build-test-architecture-plan.md`
- `research/2026-05-30-hygiene-audit/vt-regression-gate-policy.md`
- `howl-vt/build.zig`
- `howl-vt/src/test/scrollback_regression.zig`
- `howl-vt/src/test_regression_snapshot.zig`
- `howl-vt/src/fuzz/scrollback.zig`

## Source Changes

Exact files:

- `research/2026-05-30-hygiene-audit/vt-regression-gate-policy.md`
- `project-memory.md`

## Required Shape

- Keep `test:regression` as the named VT regression category.
- Keep `test:regression` outside canonical `test` because it exceeded the ordinary 120s review budget.
- Do not pull `fuzz`, `benchmark:m7_baseline`, or any stress-like surface into `test`.

## Non-Goals

- Do not change VT product code.
- Do not change regression test bodies or assertions.
- Do not add runtime knobs.
- Do not change C headers or exported symbols.
- Do not change `howl-vt/build.zig` in the accepted outcome.

## Invariants

- `howl-vt` `test:regression` remains explicit.
- `howl-vt` `test` remains unchanged and bounded for ordinary validation.
- Root `test` remains unchanged.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'test:regression|120s|ordinary' research/2026-05-30-hygiene-audit/vt-regression-gate-policy.md`
- `rg 'test_regression_step|test_step\.dependOn' howl-vt/build.zig` supports review that build code is unchanged.

## Stop Conditions

- Stop if regression tests are flaky or unbounded in ordinary deterministic test expectations.
- Stop if adding regression to `test` requires changing tests or product code.
