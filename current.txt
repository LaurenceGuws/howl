# Current Slice: Final Host Context Cutover

Owner: workspace root.

Purpose:

- Delete banned host `terminal_panel.zig` path and `TerminalPanel` vocabulary.
- Move the per-terminal aggregate to `terminal/context.zig`.
- Rename the exported aggregate type from `TerminalPanel` to `Context` with no
  aliases or shims.

## Source Files

- `howl-linux-host/src/terminal/terminal_panel.zig`
- `howl-linux-host/src/main.zig`
- `howl-linux-host/src/test/host.zig`
- `howl-linux-host/src/test_root.zig`
- `howl-linux-host/src/test/integration_entry.zig`

## Required Move

Move:

- From: `howl-linux-host/src/terminal/terminal_panel.zig`
- To: `howl-linux-host/src/terminal/context.zig`

Type rename:

- `TerminalPanel` -> `Context`

No aliases. Do not leave `terminal_panel.zig` as a compatibility shim.

## Main Import And Symbol Changes

In `howl-linux-host/src/main.zig`, replace:

- `const TerminalPanel = @import("terminal/terminal_panel.zig").TerminalPanel;`

With:

- `const TerminalContext = @import("terminal/context.zig").Context;`

Then update:

- `TabSlots.panels` -> `contexts` or `terms`.
- `[max_tabs]TerminalPanel` -> `[max_tabs]TerminalContext`.
- `active_tabs: [max_tabs]*TerminalPanel` -> `[max_tabs]*TerminalContext`.
- `fn items(self: *TabSlots) []*TerminalPanel` -> `[]*TerminalContext`.
- `activePanel()` -> `activeContext()`.
- `activeTab()` may remain because it refers to tab index, but its returned type
  must be `*TerminalContext`.
- `TerminalPanel.DrainInputOutcome` -> `TerminalContext.DrainInputOutcome`.
- `TerminalPanel.TurnResult` -> `TerminalContext.TurnResult`.
- `TerminalPanel.TurnStep` -> `TerminalContext.TurnStep`.

Do not leave an `activePanel` helper name. It contains banned panel vocabulary.

## Test Export Changes

In `howl-linux-host/src/test/host.zig`, replace:

- `pub const TerminalPanel = @import("../terminal/terminal_panel.zig");`

With:

- `pub const TerminalContext = @import("../terminal/context.zig");`

In `howl-linux-host/src/test_root.zig`, replace:

- `pub const TerminalPanel = Host.TerminalPanel;`

With:

- `pub const TerminalContext = Host.TerminalContext;`

In `howl-linux-host/src/test/integration_entry.zig`, replace:

- `_ = @import("host").TerminalPanel;`

With:

- `_ = @import("host").TerminalContext;`

No `TerminalPanel` alias.

## Internal `context.zig` Symbol Changes

Within moved file:

- `pub const TerminalPanel = struct` -> `pub const Context = struct`.
- `TerminalPanelOps` -> `ContextOps`.
- `TerminalPanel.DrainInputOutcome` -> `Context.DrainInputOutcome`.
- `TerminalPanel.ScrollMouseOutcome` -> `Context.ScrollMouseOutcome`.
- Test fake names containing `FakePanel` -> `FakeContext`.
- Function/local variable names using `panel` -> `context` where they refer to
  the per-terminal aggregate.
- Field names that are not exported but contain panel vocabulary should be
  renamed.

Keep user-facing tab vocabulary where it truly means tab.

## Invariants

- `terminal/context.zig` is the per-terminal aggregate only.
- It composes true owners:
  - `terminal/pty/*`
  - `terminal/vt/*`
  - `terminal/render/*`
  - `window/*`
- It must not become a new runtime, host, panel, manager, or controller bucket.
- Host still uses only shipped C ABI through `terminal/c.zig`.
- No compatibility aliases.
- Do not recreate `terminal/runtime/`, `terminal/host/`, or
  `terminal/terminal_panel.zig`.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test:unit`
- `zig build test:integration`
- `zig build test`

If root `test:unit` is blocked by a known package build-taxonomy issue, record
the exact failure and run host package gates from
`/home/home/personal/projects/howl/howl-linux-host`:

- `zig build check`
- `zig build test:unit`
- `zig build test:integration`

## Final Grep Gates

No matches under `howl-linux-host/src`:

- `terminal_panel`
- `TerminalPanel`
- `activePanel`
- `FakePanel`
- `terminal/runtime`
- `terminal/host`
- `/runtime/`
- `/host/`

No replacement banned owners under `howl-linux-host/src`:

- `manager`
- `controller`
- `runtime2`
- `host2`
- `panel`
