# Current Slice: Delete Temporary Graphics Logging

Owner: workspace root.

Sprint scope: graphics.

## Goal

Remove the temporary duplicated `HOWL_GRAPHICS_LOG` cross-layer probe plumbing now that
generated placeholder publication, render ordering, and graphics replay/proof paths have
durable tests.

## Source Truth

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `reference-index.md`
- `graphics-machinery-scratchpad.md` section `Temporary graphics logging cleanup research`

## Target Files

- `howl-vt/src/graphics_log.zig`
- `howl-vt/src/kitty/apply.zig`
- `howl-vt/src/terminal.zig`
- `howl-render/src/graphics_log.zig`
- `howl-render/src/frame/surface_text.zig`
- `howl-render/src/frame/queue.zig`
- `howl-linux-host/src/graphics_log.zig`
- `howl-linux-host/src/main.zig`
- `howl-linux-host/src/terminal/vt/surface.zig`
- `howl-linux-host/src/terminal/terminal_panel.zig`

## Required Behavior

- Deletion-only slice.
- Delete duplicated `graphics_log.zig` helpers and all `graphics_log.event()` calls.
- Remove all `HOWL_GRAPHICS_LOG` source plumbing.
- Preserve host `graphicsProofSnapshot`, present proof capture, Kitty replay tests, ABI/test
  proof APIs, generated placement behavior, render ordering, and host presentation policy.
- Do not change placeholder/generated placement behavior, render data contracts, host wake
  or presentation policy, replay fixtures, ABI, or product diagnostics beyond this temporary
  env-log probe.

## Required Checks

- No source matches remain for `graphics_log`, `HOWL_GRAPHICS_LOG`, or `vt-mutate`.
- `zig build test --summary all` in `howl-vt`.
- `zig build test:regression:build --summary all` in `howl-vt`.
- `zig build test --summary all` in `howl-render`.
- `zig build test:integration:kitty-graphics-replay:build --summary all` in `howl-linux-host`.
- `zig build test:integration:kitty-graphics-replay:app:build --summary all` in `howl-linux-host`.
- root `zig build --summary all`.
- root `git diff --check`.
- `howl-vt`, `howl-render`, and `howl-linux-host` `git diff --check`.

Stop condition:

- If deleting a hook forces changes to proof snapshot APIs, present proof capture, ABI,
  generated placement behavior, render ordering, host presentation policy, or replay tests,
  stop and report the smaller cleanup needed.
