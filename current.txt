Current Task

Source:

- `build-test-architecture-migration-scratchpad.md`

Promoted Item:

- Add VT ABI verification.

Targets:

- `/home/home/personal/projects/howl/howl-vt/build.zig`
- exact VT ABI test root chosen during implementation

Required Output:

- `test:abi` exists in `howl-vt`.
- Its proof statement explicitly names the shipped VT C ABI contract being verified.

Depends on:

- `build-test-architecture-spec.md`
- `build-test-verification-ledger.md`

Out of scope:

- benchmark reclassification
- broader package taxonomy renames outside the VT ABI surface

Hard Constraints:

- Keep VT as an ABI product package.
- Treat the shipped C ABI as the product under test.
- Bias VT owner split and surface shape to Ghostty first where applicable, while keeping host/runtime posture compatible with Alacritty-style orchestration and TigerBeetle pressure for bounds/assertions/directness.
- Direct cutover only. No compatibility aliases.
- No ABI-boundary bypass.
