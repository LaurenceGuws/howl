# Current Slice: Normalize ABI Export Root Order

Owner: workspace root.

Purpose:

- Make `libhowl_*.zig` public export roots read as mechanical C ABI export tables.
- Align export order with the accepted header grammar and current public headers.
- Preserve all exported symbols and behavior exactly.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/c-abi-header-grammar.md`
- `research/2026-05-30-hygiene-audit/roadmap.md` Slice 1.3
- current public headers:
  - `howl-pty/include/howl_pty.h`
  - `howl-vt/include/howl_vt.h`
  - `howl-render/include/howl_render.h`

## Source Changes

Edit only:

- `howl-pty/src/libhowl_pty.zig`
- `howl-vt/src/libhowl_vt.zig`
- `howl-render/src/libhowl_render.zig`

## Required Shape

- Public roots curate exports only.
- Export order follows the corresponding public header function order.
- Imports are ordered to support the export table.
- PTY and VT remain single-`ffi` export tables.
- Render may still import current translator files in this slice, but imports/exports must be explicit
  and mechanically ordered.

## Non-Goals

- Do not move render translator files.
- Do not edit headers.
- Do not edit `ffi.zig` or translator implementations.
- Do not rename exported C symbols.
- Do not change ABI behavior.
- Do not add compatibility aliases.

## Invariants

- All previously exported C symbols remain exported exactly once.
- Root files contain imports, `comptime` export blocks, and tests only if already present.
- No owner behavior, state, helpers, or policy appears in public roots.

## Verification

From each changed subrepo:

- `zig build check`
- `zig build test`
- `git diff --check`

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg '^const .* = @import|@export\(' howl-pty/src/libhowl_pty.zig howl-vt/src/libhowl_vt.zig howl-render/src/libhowl_render.zig`
- `rg 'pub fn|var |const [A-Z].*= struct' howl-pty/src/libhowl_pty.zig howl-vt/src/libhowl_vt.zig howl-render/src/libhowl_render.zig` prints nothing.

## Stop Conditions

- Stop if render root order cannot be made intelligible without moving translator files.
- Stop if any implementation file must change.
