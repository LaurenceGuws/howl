# Current Slice: Drawn Graphics Feedback ABI Deletion Accepted

Owner: workspace root.

Sprint scope: graphics deletion.

Status: accepted by reviewer, verified locally, and committed in submodules.

Accepted commits:

- `howl-vt` `a599769 vt: delete drawn graphics feedback`
- `howl-render` `cb526f5 render: delete drawn graphics feedback ABI`
- `howl-linux-host` `fddd43e host: stop reporting drawn graphics`

## Accepted Deletion

- Deleted VT public drawn-feedback ABI/export/FFI/Terminal path:
  `howl_vt_terminal_note_drawn_graphics`, `terminalNoteDrawnGraphics`, and
  `Terminal.noteDrawnGraphics`.
- Deleted `Graphics.State.noteDrawnImageRefs` and feedback-only tests.
- Deleted render prepared-surface graphics-ref query ABI that existed to feed the host
  feedback callback.
- Deleted host collection/submission of prepared drawn graphics refs.
- Added no replacement visibility scheduler, cache model, generated refs, or ABI.

## Verification Passed

- `zig build test --summary all` in `howl-vt` (`532/532` tests passed).
- `zig build test --summary all` in `howl-render` (`704/707` tests passed, `3` skipped).
- `zig build --summary all` in `howl-linux-host`.
- Root `zig build --summary all`.
- Root, `howl-vt`, `howl-render`, and `howl-linux-host` `git diff --check`.

## Known Broken State

- Graphics animation drawn/visibility gating no longer receives render feedback.
- Animation progress may remain inert or wrong until a source-backed Kitty-like layer/update
  model exists.

## Next Gate

No replacement slice is active. Continue deleting or proving remaining Howl-only graphics
ideas that are not Kitty-identical/source-backed before replacement work.
