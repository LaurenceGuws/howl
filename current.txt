# Current Slice: Kitty Root-Frame Edit Parity

Owner: workspace root.

Sprint scope: graphics.

## Goal

Match Kitty `a=f,r=1` root-frame edit behavior without changing render, host, ABI,
decoded-cache storage, media transport, generated placeholders, or runtime timing.

## Source Truth

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `utils/dev_references/terminals/kitty/kitty/graphics.h`
- `utils/dev_references/terminals/kitty/kitty/graphics.c`
- `utils/dev_references/terminals/kitty/kitty_tests/graphics.py`
- `graphics-machinery-scratchpad.md` section `### 25. Root-frame edits with `a=f,r=1` diverge from Kitty`

## Target Files

- `howl-vt/src/kitty/graphics.zig`
- `howl-vt/src/test/terminal_graphics.zig`

## Required Behavior

- Keep omitted `r` and `r=0` on `a=f` as append-new-extra-frame behavior.
- Normalize too-large explicit `r` to append at the next frame number.
- Implement `a=f,r=1` as an existing root-frame edit.
- Partial `a=f,r=1,x/y,s/v` edits must compose the transmitted rectangle onto the
  existing root at `x/y`.
- Full root image dimensions must be preserved after a root-frame edit.
- Omitted or zero `z` must preserve the existing root gap.
- Nonzero `z` must update the root gap and clamp negative values to `0`.
- Preserve current successful behavior for explicit extra-frame edits.

## Required Tests

- `a=f,r=1,x/y,s/v` composes a smaller payload into the existing root and preserves
  root dimensions.
- `a=f,r=1` full-size edit preserves root dimensions and replaces the expected pixels.
- `a=f,r=1` without `z` preserves root gap.
- `a=f,r=1,z=0` preserves root gap.
- `a=f,r=1,z=-1` changes root gap to `0`.
- Too-large explicit `r` appends at the next frame number.
- Existing omitted-`r` and `r=0` append tests continue to pass.

## Verification Required

- `zig build test --summary all` in `howl-vt`
- `zig build test:regression:build --summary all` in `howl-vt`
- root `zig build`
- root `git diff --check`
- `howl-vt` `git diff --check`

Stop condition:

- If root-frame editing requires decoded/cache storage, file media/shared memory,
  render behavior, host behavior, ABI changes, or a frame graph redesign, stop and
  report the smaller source-backed slice needed.
