# Current Slice: Move Render Resolve Observability

Owner: workspace root.

Purpose:

- Start splitting `howl-render/src/text/pipeline.zig` by moving the full resolve group.
- Move resolve stage/result/callback/observability symbols to a true resolve owner.
- Preserve enum tags, counter fields/defaults, and telemetry behavior exactly.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/render-text-pipeline-taxonomy.md`
- `howl-render/src/text/pipeline.zig`
- `howl-render/src/session/text.zig`
- `howl-render/src/text/font/ft_hb/support.zig`
- `howl-render/src/text/font/resolver.zig`
- `howl-render/src/prepared/surface.zig`
- `howl-render/src/prepared/submit_result.zig`

## Source Changes

Edit only:

- `howl-render/src/text/pipeline.zig`
- `howl-render/src/text/font/resolve_observability.zig`
- `howl-render/src/session/text.zig`
- `howl-render/src/text/font/ft_hb/support.zig`
- `howl-render/src/text/font/resolver.zig`
- `howl-render/src/prepared/surface.zig`
- `howl-render/src/prepared/submit_result.zig`

Do not edit any other file unless the build exposes a direct resolve-observability import missed by this slice.
If that happens, stop and report the exact import before broadening scope.

## Required Shape

- Add `howl-render/src/text/font/resolve_observability.zig`.
- Move `ResolveStage`, `ResolveRequest`, `ResolveHit`, `ResolveMiss`, `ResolveResult`,
  `ResolveCounters`, `ResolveObservability`, `ResolveFallbackFaceFn`, and `ResolveFallbackFaceOp`
  out of `pipeline.zig`.
- Update the five listed importers to use the new file for moved symbols.
- Keep `pipeline.zig` for all non-moved symbols; do not add aliases from `pipeline.zig` to the moved symbols.

## Non-Goals

- Do not rename or delete `pipeline.zig`.
- Do not move raster operation symbols.
- Do not move prepare counters.
- Do not move shape, build, group, or raster operation symbols.
- Do not change headers or exported C ABI symbols.
- Do not change prepared metrics, submit metrics, shaping, rasterization, scene preparation, or submit behavior.
- Do not add compatibility aliases.

## Invariants

- `ResolveStage` remains `enum(u5)` with identical tags and order.
- `ResolveRequest`, `ResolveHit`, `ResolveMiss`, and `ResolveResult` keep identical fields and tags.
- `ResolveCounters` fields and zero defaults remain identical.
- `ResolveObservability` fields and defaults remain identical.
- `ResolveFallbackFaceFn` and `ResolveFallbackFaceOp` keep identical dispatch behavior.
- `ResolveFallbackFaceOp.resolve` remains covered by a focused test in the new owner file.
- `ResolveObservability` remains the prepared/submit resolve telemetry shape.
- Import repairs are symbol-directed only; no behavior moves with these types.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'ResolveStage|ResolveRequest|ResolveHit|ResolveMiss|ResolveResult|ResolveCounters|ResolveObservability|ResolveFallbackFaceFn|ResolveFallbackFaceOp' howl-render/src/text/pipeline.zig`
- `rg 'ResolveStage|ResolveRequest|ResolveHit|ResolveMiss|ResolveResult|ResolveCounters|ResolveObservability|ResolveFallbackFaceFn|ResolveFallbackFaceOp' howl-render/src/text/font/resolve.zig`
- `rg 'text_pipeline\.Resolve|pipeline\.Resolve' howl-render/src`
- `rg 'types\.zig|api\.zig|manager|controller|engine|runtime' howl-render/src/text howl-render/src/prepared howl-render/src/session`

Expected results:

- The first gate prints nothing.
- The second gate proves the moved symbols live in the new owner.
- The third gate prints nothing.
- The fourth gate prints nothing except unrelated pre-existing terms if explicitly reviewed.

## Stop Conditions

- Stop if moving the resolve symbols requires changing field names, enum tags, backing type, defaults, or dispatch behavior.
- Stop if a direct importer requires both old and new names through an alias.
- Stop if the move exposes a cycle between `font/resolver.zig`, `font/ft_hb/support.zig`, `prepared/*`, and `session/text.zig`.
- Stop if implementation begins moving raster, shape, grouping, or prepare counter symbols.
