# Current Slice: Placeholder Run ABI Deletion

Owner: howl-vt.

Sprint scope: graphics deletion.

## Goal

Rip out Howl-only placeholder-run publication/query ABI and resolver surface before
replacement planning.

## Delegation Requirement

- Main agent must not implement this slice directly.
- Seed a worker with this file, exact source paths, and the destructive scope below.
- Seed a reviewer after worker output. Reviewer must judge whether placeholder-run ABI and
  query machinery were actually removed, not whether previous placeholder tests survive.

## Required Work

- Delete public placeholder-run count/query from VT C ABI if required:
  `placeholder_run_count`, `HowlVtGraphicsPlaceholderRun`,
  `HowlVtGraphicsPlaceholderRunResult`, and
  `howl_vt_terminal_query_graphics_placeholder_run`.
- Delete Zig `Terminal.graphicsPlaceholderRun` and graphics-state placeholder-run indexed
  query/count APIs as needed.
- Delete placeholder-run tests that only prove the removed ABI.
- Keep parser placeholder cell storage only if still required by other current behavior;
  otherwise delete dead resolver pieces too.
- Do not add replacement generated refs/materialization.
- Do not touch render or host unless compile break requires minimal ABI follow-up.

## Candidate Paths

- `howl-vt/include/howl_vt.h`
- `howl-vt/src/ffi.zig`
- `howl-vt/src/libhowl_vt.zig`
- `howl-vt/src/terminal.zig`
- `howl-vt/src/kitty/graphics.zig`
- `howl-vt/src/test/terminal_graphics.zig`

## Source-Order Reason

- Kitty does not expose placeholder runs as public ABI.
- Ghostty does not expose a duplicate public placeholder-run query ABI.
- Howl placeholder-run publication exists only because the deleted generated-placement
  bridge needed a query/proof seam.

## Expected Broken State

- Public VT ABI breaks for consumers of placeholder-run count/query.
- Placeholder-run tests should disappear or fail until reference-backed replacement work
  defines a real ABI consequence.

## Tests Required

- Run `zig build test --summary all` in `howl-vt` if feasible.
- Run `zig build test:regression:build --summary all` in `howl-vt` if feasible.
- Run root and `howl-vt` `git diff --check`.
- Failing builds/tests are acceptable only if reported precisely.

## Stop Conditions

- Stop if render/host replacement architecture becomes necessary.
- Stop if the worker starts designing replacement generated refs/materialization.
