# Current Slice: Graphics Placeholder Run Assembly Proof

Owner: workspace root.

Sprint scope: graphics.

Promoted from `graphics-machinery-scratchpad.md`.

## Goal

Prove and, if needed, correct Howl's Unicode placeholder run assembly against Kitty source.

Kitty source truth:

- `utils/dev_references/terminals/kitty/kitty/screen.c`: `screen_render_line_graphics`
- `utils/dev_references/terminals/kitty/docs/graphics-protocol.rst`: `U+0305` maps to number `0`

Howl target files:

- `howl-vt/src/kitty/graphics.zig`
- `howl-vt/src/test/terminal_graphics.zig`

## Required Acceptance Tests

- `U+0305` maps to image row/col `0`.
- Same-row missing column inference matches Kitty.
- Same-row missing high-byte inference matches Kitty.
- Mismatched row, column, image id, placement id, or high byte breaks runs as Kitty does.

## Hard Constraints

- Do not alter render geometry.
- Do not reinterpret protocol diacritics as 1-based.
- Do not reintroduce cross-row inference.
- Do not special-case yazi or pane positions.
- Keep changes in `howl-vt` only unless a reviewed blocker proves otherwise.

## Verification Required

- `zig build test` in `howl-vt`
- `zig build test:regression:build` in `howl-vt`
- `git diff --check` at workspace root

Stop condition:

- If Kitty source behavior cannot be proven from `screen_render_line_graphics`, stop and report the exact uncertainty. Do not guess.
