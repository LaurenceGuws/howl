Current Task

Source:

- `graphics-owner-split-strict-scratchpad.md`
- `loop.txt`
- `reference-index.md`
- Kitty protocol docs
- Ghostty image/render/runtime references
- TigerBeetle style and architecture references

Promoted Item:

- Proof/fix live size slice: determine why the live app-icon still presents as a tiny visible spec and fix the first owner-true size/placement seam that is still wrong.

Required Output:

- We identify the first live seam where the presented app-icon size collapses from the intended placement size to the tiny visible spec.
- If the seam is wrong, we fix only that seam.
- Existing replay proofs stay green.
- The fix remains narrow and owner-true.

Hard Constraints:

- Prefer touching only the smallest live size/placement seam and directly related proof seams.
- Do not redesign Kitty semantics, animation/runtime state, or broader graphics architecture.
- Do not guess from the screenshot; prove the wrong size numerically in the live path first.
- Rerun the relevant host replay proofs after the fix.
