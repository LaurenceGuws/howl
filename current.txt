# No Active Slice

Sprint:

- Useless LOC sprint

Scope source:

- `loc-debt-sprint-scope.md`

Completed slices:

- Slice 1: deleted confirmed dead host wrappers and dead re-export layers.
  - deleted `howl-linux-host/src/terminal/texture.zig`
  - deleted `howl-linux-host/src/window_chrome.zig`
  - deleted `howl-linux-host/src/display.zig`
  - deleted `howl-linux-host/src/display/renderer.zig`
- Slice 2: removed dead host test build-support indirection.
  - deleted `howl-linux-host/build_support/host_tests.zig`
  - kept `howl-linux-host/src/host_test_root.zig` because rooting `terminal/context.zig` directly breaks `../...` imports outside module path
  - kept `howl-linux-host/src/integration_test_root.zig` because rooting `main.zig` directly changes imported-test compilation behavior and broke current host proofs
  - folded host test-root module creation directly into `howl-linux-host/build.zig`
- Slice 3: removed render benchmark `src/test` ownership.
  - moved `howl-render/src/test/benchmark.zig` to `howl-render/src/benchmark_main.zig`
  - deleted the old trampoline-only `howl-render/src/benchmark_main.zig`
- Slice 4: removed duplicated VT unit-test aggregation from the top VT unit root.
  - deleted overlapping `_test.zig` imports from `howl-vt/src/howl_vt.zig`
  - kept the curated terminal-owner test imports in `howl-vt/src/terminal.zig`
- Slice 5a: removed inline POSIX PTY transport tests from the production owner.
  - deleted inline tests from `howl-pty/src/pty/posix.zig`
  - added `howl-pty/src/pty/posix_test.zig`
  - added only one narrow production test hook: `posix.testing.waitReadablePollResult`
  - wired the sibling test through `howl-pty/src/test_unit.zig`
- Slice 5b: removed inline render text-session tests from the production owner.
  - deleted inline tests from `howl-render/src/session/text.zig`
  - added `howl-render/src/session/text_test.zig`
  - added only two narrow production test hooks in `session/text.zig`:
    - `testing.ftHbCapacity`
    - `testing.ensureCellInputScratchCapacity`
  - wired the sibling test through `howl-render/src/test/unit/root.zig`
- Slice 5c: removed inline prepared-owner tests and local test-only builders from the production owner.
  - deleted inline tests from `howl-render/src/prepared/owner.zig`
  - moved local test-only prepared-surface builders out of production owner
  - added `howl-render/src/prepared/owner_test.zig`
  - added only two narrow production test hooks in `prepared/owner.zig`:
    - `testing.executionMatchesPrepared`
    - `testing.renderSurfaceEmissionFailureFromError`
  - wired the sibling test through `howl-render/src/test/unit/root.zig`
- Slice 5d: removed inline emitter tests and test-only fixture scaffolding from the production owner.
  - deleted inline tests from `howl-render/src/prepared/render_surface_emitter.zig`
  - moved test-only fixture/build helper code to `howl-render/src/prepared/render_surface_emitter_test.zig`
  - added a minimal production `testing` hook surface in `render_surface_emitter.zig` so sibling tests can drive private owner behavior without re-exposing the old top-level fixture path
  - wired the sibling test through `howl-render/src/test/unit/root.zig`
- Slice 5e: removed inline host render-surface tests and local test helper scaffolding from the production owner.
  - deleted inline tests from `howl-linux-host/src/display/renderer/render_surface.zig`
  - moved local test helpers and proofs to `howl-linux-host/src/display/renderer/render_surface_test.zig`
  - added a narrow production `testing` namespace in `render_surface.zig` for sibling access to private owner behavior and nested test-relevant types
  - wired the sibling test through `howl-linux-host/src/host_test_root.zig`
- Slice 5f: removed inline terminal-context tests and local test helper scaffolding from the production owner.
  - deleted inline tests from `howl-linux-host/src/terminal/context.zig`
  - moved local test helpers and proofs to `howl-linux-host/src/terminal/context_test.zig`
  - added a narrow production `testing` namespace in `context.zig` for sibling access to private owner behavior and result types needed by the moved proofs
  - wired the sibling test through `howl-linux-host/src/host_test_root.zig`
- Slice 5g: extracted a concrete top-level processor owner from `main.zig`.
  - added `howl-linux-host/src/app/processor.zig`
  - moved loop/present/input/tab coordination out of `howl-linux-host/src/main.zig`
  - reduced `main.zig` to bootstrap/entry plus one bootstrap-owned `TERM` policy test
- Slice 5h: removed the remaining top-level host `anytype` seams from the concrete processor owner.
  - deleted `anytype` seams from `howl-linux-host/src/app/processor.zig`
  - replaced them with concrete owner methods and explicit data helpers
  - deleted fake-seam processor tests instead of preserving a non-Alacritty test seam
- Slice 6: deleted the render text umbrella layer with no owner truth.
  - deleted `howl-render/src/text/text.zig`
  - rewrote all `howl-render` call sites to direct owner imports
  - no replacement umbrella was introduced
- Slice 7a: collapsed stale PTY retained state into the `term` owner.
  - moved PTY launch/lifecycle/feed-record state from `howl-linux-host/src/terminal/pty/retained.zig` into `howl-linux-host/src/terminal/term.zig`
  - rewrote direct users to depend on `term` instead of the stale retained bucket
  - deleted `howl-linux-host/src/terminal/pty/retained.zig`
- Slice 7b: collapsed stale VT retained state into the `term` owner.
  - moved VT title/scratch/scrollback/cursor retained state from `howl-linux-host/src/terminal/vt/retained.zig` into `howl-linux-host/src/terminal/term.zig`
  - rewrote remaining VT helper code to use `term` as the state owner
  - deleted the stale `vt_retained.State` bucket
- Slice 7c: moved VT title ownership into the `term` owner.
  - moved launch-title fallback, cached title copy, title generation, and VT title refresh from `howl-linux-host/src/terminal/vt/retained.zig` into `howl-linux-host/src/terminal/term.zig`
  - rewired host title consumers and PTY pump title refresh to use `term` directly
  - kept `vt/retained.zig` focused on remaining VT runtime, selection, scrollback, and output helper behavior
- Slice 7d: moved VT selection and hyperlink lookup into host owners.
  - moved VT selection mutation into `howl-linux-host/src/terminal/selection.zig`
  - moved VT visible hyperlink lookup into `howl-linux-host/src/terminal/links.zig`
  - removed those owner-false helpers from `howl-linux-host/src/terminal/vt/retained.zig`
- Slice 7e: moved VT layout mutation into the layout owner.
  - moved VT resize and cell-pixel-size mutation from `howl-linux-host/src/terminal/vt/retained.zig` into `howl-linux-host/src/terminal/render/surface_layout.zig`
  - rewired `context.zig` and layout sync paths to use the layout owner directly
  - follow-up fix: removed a lock re-entry in `surface_layout.resizeTermVtLocked()` by using VT visible-meta directly instead of calling a locking scroll-state helper
- Slice 7f: moved the remaining public host scrollback subgroup into the scrollbar owner.
  - moved `ScrollState`, `scrollState`, `scrollStateLocked`, `setScrollbackOffset`, `setScrollbackOffsetLocked`, and `followLiveBottom` from `howl-linux-host/src/terminal/vt/retained.zig` into `howl-linux-host/src/terminal/scrollbar.zig`
  - rewired `howl-linux-host/src/terminal/context.zig` to use `terminal_scrollbar` directly for follow-live-bottom and scrollback offset observations
  - kept `vt_retained.followLiveBottomLocked` in place for `vt/input.zig` locked input paths
- Slice 7g: moved the final pure VT-state field helpers into `term.zig`.
  - moved `inputScratch`, `followLiveBottomLocked`, and `setFocused` from `howl-linux-host/src/terminal/vt/retained.zig` into `howl-linux-host/src/terminal/term.zig`
  - rewired `howl-linux-host/src/terminal/vt/input.zig` and `howl-linux-host/src/terminal/scrollbar.zig` to use `terminal_term` directly
  - left `vt/retained.zig` as the keeper helper owner for VT runtime/feed/output/clipboard/selection behavior
- Slice 7h: moved the remaining concrete terminal input-adapter leaf surface into `terminal/input.zig`.
  - moved `publishTerminalBytes`, `publishTerminalKey`, `publishTerminalMouse`, `ScrollVisualState`, `ContextOps`, `pixelToCol`, and `pixelToRow` from `howl-linux-host/src/terminal/context.zig` into `howl-linux-host/src/terminal/input.zig`
  - kept `context.zig` thin wrappers for input drain, mouse ownership, and terminal pixel conversion
  - left `vt/retained.zig` unchanged as a keeper helper owner
- Item 8: deduplicated repeated constructor families locally in `howl-vt`.
  - added one file-local helper in `howl-vt/src/screen.zig` for repeated base `Screen` field assembly
  - added one file-local helper in `howl-vt/src/terminal.zig` for repeated `Terminal` assembly after screen creation
  - preserved all constructor asymmetries, public APIs, and explicit `errdefer` cleanup
- Item 9a: deduplicated repeated test-artifact plumbing locally inside `howl-pty/build.zig`.
  - added one file-local helper for repeated `b.addTest(...)` setup while preserving `filters = b.args orelse &.{}` and `use_llvm = true`
  - added one file-local helper for repeated `b.addRunArtifact(...)` setup while preserving conditional `has_side_effects`
  - kept ABI-specific include/import setup explicit and unchanged
- Item 9b: deduplicated unit/ABI test-artifact plumbing locally in `howl-vt/build.zig`.
  - added one file-local helper for repeated `b.addTest(...)` setup while preserving `filters = b.args orelse &.{}` and `use_llvm = true`
  - added one file-local helper for repeated `b.addRunArtifact(...)` setup while preserving conditional `has_side_effects`
  - kept VT-specific module setup explicit and unchanged, including internal/ABI option wiring, simulation wiring, and benchmark wiring
- Item 9c: deduplicated unit/ABI test-artifact plumbing locally in `howl-render/build.zig`.
  - added one file-local helper for repeated `b.addTest(...)` setup while preserving `filters = b.args orelse &.{}` and `use_llvm = true`
  - added one file-local helper for repeated `b.addRunArtifact(...)` setup while preserving conditional `has_side_effects`
  - kept render-specific module setup explicit and unchanged, including unit/ABI roots, benchmark wiring, and FFI/install wiring
- Item 9d: deduplicated repeated test run-artifact plumbing locally in `howl-linux-host/build.zig`.
  - added one file-local helper for repeated `b.addRunArtifact(...)` plus conditional `has_side_effects`
  - kept all `b.addTest(...)`, module-root creation, linking/configuration, and step-graph wiring explicit
- Item 10a: deduplicated the repeated prepared fill-pass helpers in `howl-render/src/prepared/render_surface_emitter.zig`.
  - added one file-local helper for the repeated prepared fill-pass loops
  - kept `emitPrepared()` ordering unchanged
  - kept `appendPreparedFullRedrawClear()`, `appendPreparedFillCommand()`, and `tryMergePreparedFillCommand()` behavior unchanged
- Item 10b: deduplicated the repeated scene-assembly population block in `howl-render/src/text/scene.zig`.
  - added one file-local helper for the shared scene population sequence
  - kept owned-vs-borrowed setup/teardown separate
  - kept public APIs and append order unchanged
- Item 10c: pruned the redundant classic generated-special fallback arms from `howl-render/src/text/font/ft_hb/special_sprite.zig`.
  - removed only the classic box/block switch arms already covered by `howl-render/src/text/raster/special.zig`
  - kept all residual fallback families intact
- Item 10d: reduced `howl-vt/src/parser/events.zig` to the live event vocabulary only.
  - moved `ParsedEvents`, queue/materializer machinery, compaction helpers, and parser-feed collection helpers out of production ownership
  - moved the stale materializer surface and its proofs to `howl-vt/src/parser/events_test.zig`
  - kept `parser/events.zig` as the production owner for `StyleChange`, `DcsEvent`, and `Event`
- Item 10e: deduplicated repeated benchmark workload assembly in `howl-render/src/benchmark_main.zig`.
  - added file-local helpers for repeated `Workload` assembly and repeated damage-literal construction
  - kept workload contents, full-vs-sparse distinctions, and benchmark output/build behavior unchanged
- Item 10f: moved the inline proof lane out of `howl-render/src/render/render_surface_realizer.zig` into a sibling test file.
  - removed inline tests and test-only scaffolding from the production owner
  - added `howl-render/src/render/render_surface_realizer_test.zig`
  - rewired `howl-render/src/test/unit/root.zig` to import the sibling test file instead of the production owner directly
  - kept production logic unchanged and added no production hooks
- Item 10g: moved the inline proof lane out of `howl-render/src/text/raster/special.zig` into a sibling test file.
  - removed inline tests and test-only helpers from the production owner
  - added `howl-render/src/text/raster/special_test.zig`
  - rewired `howl-render/src/test/unit/root.zig` to import the sibling test file
  - kept raster production logic unchanged and added no production hooks
- Item 10h: moved the remaining inline parser proof lanes into existing sibling VT test files.
  - removed inline tests from `howl-vt/src/parser/string_control.zig` and `howl-vt/src/parser/main.zig`
  - rehomed those proofs into `howl-vt/src/parser/string_control_test.zig` and `howl-vt/src/parser/main_test.zig`
  - kept parser production logic unchanged and added no production hooks
- Item 10i: moved the remaining FT/HB support proofs into a sibling test file.
  - removed the test-only import, test-only font-path helper, and inline proofs from `howl-render/src/text/font/ft_hb/support.zig`
  - added `howl-render/src/text/font/ft_hb/support_test.zig`
  - rewired `howl-render/src/test/unit/root.zig` to import the sibling test file
  - kept production behavior unchanged and used only one narrow testing hook for the bounded-buffer proof
- Item 10j: purged dead private wrappers from `howl-linux-host/src/terminal/context.zig`.
  - deleted dead private wrappers `driveRender`, `prepare`, `takePreparedUpload`, and `submit`
  - deleted the dead private alias `ScrollMouseOutcome`
  - kept the live render/submit/testing owner surface unchanged
- Item 10k: purged dead private helpers from `howl-linux-host/src/display/renderer/render_surface.zig`.
  - deleted dead private helpers `findCreate` and `retireForResource`
  - kept live render-surface classification, upload, and draw behavior unchanged
- Item 10l: purged a dead untextured draw seam from `howl-linux-host/src/display/renderer/render_surface.zig`.
  - deleted the dead optional texture-rect parameter from `drawQuad`
  - removed the dead textured-branch locals inside `drawQuad`
  - kept fill and textured draw behavior unchanged
- Item 10m: removed the dead GL state sample carrier from `howl-linux-host/src/display/renderer/render_surface.zig`.
  - deleted `GlStateSample` and `sampleGlState()`
  - replaced the three phase-end sample checks with a direct GL error helper
  - kept exact failure messages and render behavior unchanged
- Item 10n: purged dead and unreachable local raster helpers from `howl-render/src/text/font/ft_hb/special_sprite.zig`.
  - deleted dead private declarations `AlphaSegment`, `drawAlphaH()`, `drawAlphaV()`, `fillAlphaChecker()`, and `drawAlphaRoundedCorner()`
  - deleted unreachable `0x1fb9a` / `0x1fb9b` arms only from `drawAlphaHalfTriangleCodepoint()`
  - removed the identical no-op branch in `drawAlphaBranchNode()`
  - removed dead local `steps` from `drawAlphaSpinner()`
- Item 10o: deduplicated the repeated 8-way range partition algorithm in `howl-render/src/text/raster/special.zig`.
  - added one file-local helper for the shared 8-way partition logic
  - rewired `eightRange()` and `eighthRange()` to use it
  - kept `fourthRange()` separate and behavior unchanged
- Gate fix 1: replaced the flaky PTY interrupt proof with a deterministic child-group proof and fixed control signal routing.
  - rewrote `howl-pty/src/pty_integration_test.zig` to prove interrupt delivery with a foreground child shell trap instead of a timing-based exit wait
  - fixed `howl-pty/src/pty/posix.zig` so `control()` signals the child process group instead of only the shell pid

Verification receipts:

- wrapper import-path grep returned no matches after deletion
- `howl-linux-host`: `zig build test && zig build check`
- workspace root: `zig build test && zig build check`
- host test indirection grep for `build_support/host_tests` returned no matches after deletion
- render benchmark path grep for `src/test/benchmark.zig` and `test/benchmark.zig` returned no matches after the move
- VT overlap grep confirmed the duplicate root imports were removed from `howl-vt/src/howl_vt.zig`
- `howl-render`: `zig build test && zig build check`
- `howl-vt`: `zig build test && zig build check`
- `howl-pty`: `zig build test && zig build check`
- workspace root re-verified: `zig build test && zig build check`
- note: one transient `howl-pty` integration test failure appeared during a workspace `zig build test` run, then passed on immediate rerun; subsequent workspace gates were clean
- `howl-render` re-verified after `session/text.zig` and `prepared/owner.zig` separation: `zig build test && zig build check`
- `howl-render` re-verified after `prepared/render_surface_emitter.zig` separation: `zig build test && zig build check`
- `howl-linux-host` re-verified after `display/renderer/render_surface.zig` separation: `zig build test && zig build check`
- `howl-linux-host` re-verified after `terminal/context.zig` separation: `zig build test && zig build check`
- `howl-linux-host` re-verified after processor extraction and `anytype` removal: `zig build test && zig build check`
- `howl-render` re-verified after deleting `src/text/text.zig`: `zig build test && zig build check`
- `howl-linux-host` re-verified after collapsing `pty/retained.zig`: `zig build test && zig build check && zig build run -Doptimize=ReleaseFast`
- `howl-linux-host` re-verified after collapsing `vt_retained.State`: `zig build test && zig build check && zig build run -Doptimize=ReleaseFast`
- workspace root re-verified after slice 7b: `zig build test && zig build check`
- `howl-linux-host` re-verified after moving VT title ownership into `term`: `zig build test && zig build check && zig build run -Doptimize=ReleaseFast`
- workspace root re-verified after slice 7c: `zig build test && zig build check`
- `howl-linux-host` re-verified after moving VT selection and hyperlink lookup into host owners: `zig build test && zig build check && zig build run -Doptimize=ReleaseFast`
- `howl-linux-host` re-verified after moving VT layout mutation into the layout owner: `zig build test && zig build check && zig build run -Doptimize=ReleaseFast`
- workspace root re-verified after slice 7e: `zig build test && zig build check`
- `howl-linux-host` re-verified after moving the public host scrollback subgroup into `terminal/scrollbar.zig`: `zig build test && zig build check && zig build run -Doptimize=ReleaseFast`
- `howl-pty` deterministic interrupt proof re-verified twice: `zig build test:integration -- "owned unix pty interrupt reaches the child group"`
- `howl-pty` re-verified after gate fix 1: `zig build test && zig build check`
- workspace root re-verified after gate fix 1: `zig build test && zig build check`
- workspace root re-verified after slice 7f: `zig build test && zig build check`
- grep receipt after slice 7g: no `retained.inputScratch`, `retained.followLiveBottomLocked`, or `retained.setFocused` references remain under `howl-linux-host/src/terminal`
- `howl-linux-host` re-verified after moving the final pure VT-state field helpers into `term.zig`: `zig build test && zig build check`
- workspace root re-verified after slice 7g: `zig build test && zig build check`
- grep receipt after slice 7h: `context.zig` no longer defines `ContextOps`, `ScrollVisualState`, `publishTerminalBytes`, `publishTerminalKey`, `publishTerminalMouse`, `pixelToCol`, or `pixelToRow`
- grep receipt after slice 7h: `context.zig` still defines `drainTextInputFastPath`, `drainPointerAndUiInput`, `terminalOwnsMouse`, `pixelToTerminalCol`, and `pixelToTerminalRow`
- `howl-linux-host` re-verified after moving the concrete terminal input-adapter leaf surface into `terminal/input.zig`: `zig build test && zig build check && zig build run -Doptimize=ReleaseFast`
- workspace root re-verified after slice 7h: `zig build test && zig build check`
- `howl-vt` re-verified after local constructor-family dedup: `zig build test && zig build check`
- workspace root re-verified after item 8: `zig build test && zig build check`
- `howl-pty` re-verified after local build-plumbing dedup: `zig build test && zig build check`
- workspace root re-verified after item 9a: `zig build test && zig build check`
- `howl-vt` re-verified after local build-plumbing dedup: `zig build test && zig build check`
- workspace root re-verified after item 9b: `zig build test && zig build check`
- `howl-render` re-verified after local build-plumbing dedup: `zig build test && zig build check`
- workspace root re-verified after item 9c: `zig build test && zig build check`
- `howl-linux-host` re-verified after local build-plumbing dedup: `zig build test && zig build check`
- workspace root re-verified after item 9d: `zig build test && zig build check`
- `howl-render` re-verified after local prepared fill-pass dedup: `zig build test && zig build check`
- workspace root re-verified after item 10a: `zig build test && zig build check`
- `howl-render` re-verified after local scene population dedup: `zig build test && zig build check`
- workspace root re-verified after item 10b: `zig build test && zig build check`
- `howl-render` re-verified after pruning redundant classic generated-special fallback arms: `zig build test && zig build check`
- workspace root re-verified after item 10c: `zig build test && zig build check`
- grep receipt after item 10d: `ParsedEvents` references remain only in `howl-vt/src/parser/events_test.zig`
- `howl-vt` re-verified after reducing `parser/events.zig` to the live vocabulary: `zig build test && zig build check`
- workspace root re-verified after item 10d: `zig build test && zig build check`
- `howl-render` benchmark build re-verified after workload assembly dedup: `zig build benchmark:render:build`
- `howl-render` re-verified after local benchmark workload dedup: `zig build test && zig build check`
- grep receipt after item 10f: `^test ` no longer matches `howl-render/src/render/render_surface_realizer.zig`
- grep receipt after item 10f: `src/test/unit/root.zig` imports `../../render/render_surface_realizer_test.zig`
- `howl-render` re-verified after proof-lane extraction from `render_surface_realizer.zig`: `zig build test && zig build check`
- workspace root re-verified after item 10f: `zig build test && zig build check`
- grep receipt after item 10g: `^test ` no longer matches `howl-render/src/text/raster/special.zig`
- grep receipt after item 10g: `src/test/unit/root.zig` imports `../../text/raster/special_test.zig`
- `howl-render` re-verified after proof-lane extraction from `text/raster/special.zig`: `zig build test && zig build check`
- workspace root re-verified after item 10g: `zig build test && zig build check`
- grep receipt after item 10h: `^test ` no longer matches `howl-vt/src/parser/string_control.zig`
- grep receipt after item 10h: `^test ` no longer matches `howl-vt/src/parser/main.zig`
- `howl-vt` re-verified after parser proof-lane extraction: `zig build test && zig build check`
- workspace root re-verified after item 10h: `zig build test && zig build check`
- grep receipt after item 10i: `support.zig` has no `^test ` matches
- grep receipt after item 10i: `support.zig` no longer references `test_font_options`, `InjectedTestFontPaths`, or `injectedTestFontPaths`
- grep receipt after item 10i: `src/test/unit/root.zig` imports `../../text/font/ft_hb/support_test.zig`
- `howl-render` re-verified after FT/HB support proof extraction: `zig build test && zig build check`
- workspace root re-verified after item 10i: `zig build test && zig build check`
- `howl-linux-host` re-verified after dead-wrapper purge in `context.zig`: `zig build test && zig build check`
- workspace root re-verified after item 10j: `zig build test && zig build check`
- grep receipt after item 10k: no `findCreate` or `retireForResource` references remain under `howl-linux-host/src/display/renderer`
- `howl-linux-host` re-verified after dead-helper purge in `render_surface.zig`: `zig build test && zig build check`
- workspace root re-verified after item 10k: `zig build test && zig build check`
- grep receipt after item 10l: `drawQuad(` has one call site and one definition, both in `render_surface.zig`
- `howl-linux-host` re-verified after dead untextured draw-seam purge in `render_surface.zig`: `zig build test && zig build check`
- workspace root re-verified after item 10l: `zig build test && zig build check`
- grep receipt after item 10m: no `GlStateSample` or `sampleGlState` references remain in `howl-linux-host/src/display/renderer/render_surface.zig`
- `howl-linux-host` re-verified after dead GL state sample removal in `render_surface.zig`: `zig build test && zig build check`
- workspace root re-verified after item 10m: `zig build test && zig build check`
- grep receipt after item 10n: no `AlphaSegment`, `drawAlphaH`, `drawAlphaV`, `fillAlphaChecker`, or `drawAlphaRoundedCorner` references remain in `special_sprite.zig`
- `howl-render` re-verified after local dead-helper cleanup in `special_sprite.zig`: `zig build test && zig build check`
- workspace root re-verified after item 10n: `zig build test && zig build check`
- `howl-render` re-verified after local 8-way partition dedup in `special.zig`: `zig build test && zig build check`
- workspace root re-verified after item 10o: `zig build test && zig build check`

Slice 7 close verdict:

- Item 7 is complete enough on the current boundary path.
- Reviewer rejected one further tiny cut: deleting `Context.terminalOwnsMouse`, `pixelToTerminalCol`, and `pixelToTerminalRow` would create an import cycle between `terminal/input.zig` and `selection.zig` / `links.zig`, so that remaining seam is not isolated debt.
- Any further host input ownership work is a new broader researched slice, not a final tiny item-7 cleanup.

Dirty-state note:

- `howl-linux-host/src/display/frame_timer.zig` still carries the earlier local pacing cleanup and was not changed by this slice

Next slice to promote:

- Continue item 10 keeper-vs-debt audit with the next remaining source-backed candidate after the `special.zig` 8-way partition dedup
