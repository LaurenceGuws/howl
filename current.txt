# Current Slice: V0 Persistent Resource Realization Gate

Owner: `howl-linux-host` submit V0 realization gate.

Purpose:

- Allow V0 frames that reuse persistent sprite resources to reach the host retained resource validator.
- Stop treating the frame-only prepared resource plan as a presentation gate.
- Keep the prepared resource plan as diagnostics only.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/terminal/context.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No rendering behavior changes. No full-RGBA deletion.

## Required Shape

- Attempt `ProtocolV0Textures.realizeFrame()` whenever a prepared V0 frame exists.
- Do not require `prepared_upload.protocol_v0_resource_plan.valid` before realization.
- Record resource-plan status for diagnostics only.
- If host realization or V0 presentation rejects the frame, preserve existing full RGBA fallback.

## Required Tests

- Existing host tests pass.
- Bounded `rain` smoke should reduce/remove `no_sidecar_invalid` for persistent-resource frames and
  show V0 patch presentation where retained-surface gates allow it.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`
