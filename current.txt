Current Task

Source: `feature-gap-scratchpad.md`

Issue 5: Dynamic terminal color state never becomes render truth.

Required outcome:

- Carry VT-owned dynamic color state through the product boundary so render/host use VT truth.
- Replace render-local fixed default fg/bg/cursor color assumptions where VT owns the live state.
- Keep ownership aligned: VT owns dynamic color truth, render consumes it, host presents it.

Primary evidence:

- `howl-vt/src/control/osc_color.zig`
- `howl-vt/src/host/state.zig`
- `howl-vt/include/howl_vt.h`
- `howl-render/src/frame/input.zig`
- `howl-render/src/frame/queue.zig`
- `howl-render/include/howl_render.h`

Reference against it:

- `AGENTS.md`
- `utils/dev_references/terminals/ghostty/src/terminal/c/render.zig:312-360`
- `utils/dev_references/terminals/ghostty/src/renderer/generic.zig:2046-2085`
- `utils/dev_references/terminals/alacritty/alacritty/src/display/content.rs:113-150`

No guessing allowed:

- Read `AGENTS.md`
- Read `reference-index.md`
- Read `feature-gap-scratchpad.md`
- Read the live owner files for VT dynamic color truth, ABI export, and render ingestion.
- Use `AGENTS.md` boundary rules with zero guessing.
- If the correct owner seam cannot be named exactly, stop and report `work-not-clear`.

Acceptance target:

- Dynamic VT-owned fg/bg/cursor-related colors are no longer visual no-ops above VT.
- Render consumes VT color truth instead of fixed local defaults where VT owns state.
- Control flow remains centralized and ownership stays explicit.
