# Current Stage: Render Protocol Symbol Deletion Scope Rejected

Status: no active implementation slice. Prior sprint records are wrong-scoped.

Completed partial render cleanup commits:

- `aa1749d render: move v0 oracle tests under unit`
- `6bf6388 render: delete protocol proof test bucket`
- `12aafa5 render: move v0 source owners`
- `66bf363 render: rename v0 language in tests`

Completed partial root records:

- workflow/research/current records through this sprint
- docs moved from `docs/render-protocol-v0.md` to `docs/render-api-v0.md`

## Result

- Render API V0 proof runs through unfiltered `zig build test:unit`.
- The separate `test:protocol-proof` build/test bucket is gone.
- The internal `howl-render/src/protocol_v0/` source bucket is gone.
- Root docs describe render API V0 instead of a separate render protocol product.
- The sprint did not complete render `protocol` language deletion because public
  render ABI symbols, FFI names, docs, tests, and host consumers still contain
  banned `protocol` language.
- The commits above are useful completed partial cleanup, not complete language
  deletion.

## Next Decision

Do not promote another implementation slice from the superseded sprint/cache
premise. A fresh research cache is required for full `protocol` symbol deletion
across render ABI, FFI, docs, tests, and host consumers.

Known possible follow-ups:

- Fresh render ABI/FFI/docs/tests/host-consumer research for complete banned
  `protocol` symbol deletion.
- Runtime text failure investigation after the render language cleanup sprint.
