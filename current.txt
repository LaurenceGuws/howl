# Current Slice: None

Owner: workspace root.

Sprint scope: graphics deletion.

Status: `Render Legacy Graphics Payload Deletion` completed and accepted.

Accepted commits:

- `howl-render` `187acd9 render: delete legacy graphics decoding`

Verification passed:

- `zig build test --summary all` in `howl-render` (`730/733` tests passed, `3` skipped).
- Root `zig build --summary all`.
- Root `git diff --check`.
- `howl-render git diff --check`.

Next work must decide the remaining graphics deletion candidates before replacement
planning: synthetic generated-placeholder order keys and query-derived generated placement
publication/materialization gap.
