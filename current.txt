# Current Slice: None

Owner: workspace root.

Sprint scope: graphics deletion.

Status: `Graphics App Icon Fixture Deletion` completed and accepted.

Accepted commits:

- `howl-vt` `a1ac412 vt: delete app icon graphics fixture`
- `howl-render` `e7b4edb render: delete app icon graphics fixture`
- `howl-linux-host` `d8bbee7 host: delete app icon graphics fixture`

Verification passed/reported:

- `zig build test --summary all` in `howl-vt` (`638/638` tests).
- `zig build test:regression:build --summary all` in `howl-vt`.
- `zig build test --summary all` in `howl-render` (`733/736` tests passed, `3` skipped).
- `zig build test:integration:kitty-graphics-replay:build --summary all` in
  `howl-linux-host`.
- Root `zig build --summary all`.
- Root, `howl-vt`, `howl-render`, and `howl-linux-host` `git diff --check`.

Known non-blocking timeouts:

- `zig build test --summary all` in `howl-linux-host` timed out after 120s.
- `zig build test:integration:kitty-graphics-replay --summary all` in `howl-linux-host`
  timed out after 180s.

Next work must continue deleting/quarantining Howl-only graphics machinery before any
replacement planning.
