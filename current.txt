# Current Slice: Move Render VT Surface Translator

Owner: workspace root.

Purpose:

- Continue the accepted `howl-render` C translator lane.
- Move only `vt_surface.zig` from the render root into `src/ffi/`.
- Preserve exported symbols, VT source conversions, publish-result mapping, cursor/color/selection validation, and layout assertions exactly.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/render-c-lane.md`
- `howl-render/design.md`
- `howl-render/include/howl_render.h`
- `howl-render/src/libhowl_render.zig`
- `howl-render/src/vt_surface.zig`
- `howl-render/src/test/ffi.zig`
- `howl-render/src/source/cell.zig`
- `howl-render/src/source/slot.zig`
- `howl-render/src/source/vt.zig`
- `howl-render/src/surface/tokens.zig`

## Source Changes

Edit only:

- `howl-render/src/libhowl_render.zig`
- `howl-render/src/vt_surface.zig`
- `howl-render/src/ffi/vt_surface.zig`
- `howl-render/src/test/ffi.zig`

Do not edit any other file unless the build exposes a direct `vt_surface.zig` import missed by this slice.
If that happens, stop and report the exact import before broadening scope.

## Required Shape

- Move `howl-render/src/vt_surface.zig` to `howl-render/src/ffi/vt_surface.zig`.
- Update `libhowl_render.zig` to import the moved translator from `ffi/vt_surface.zig`.
- Update `test/ffi.zig` to import the moved translator.
- Remove the old root `howl-render/src/vt_surface.zig`; do not leave a compatibility wrapper.
- Keep `libhowl_render.zig` as an export table only.

## Non-Goals

- Do not move `submission.zig`.
- Do not move `prepared_surface.zig`.
- Do not move already-moved translators again.
- Do not rename any `HowlRender*` C type.
- Do not rename any `howl_render_*` symbol.
- Do not change `howl-render/include/howl_render.h`.
- Do not change source-slot, publish, cursor, color, selection, or layout assertion semantics.
- Do not add compatibility aliases.

## Invariants

- The public root curates exports only.
- `howl_render_text_session_reserve_vt_surface_slot` keeps the same exported symbol.
- `howl_render_text_session_commit_vt_surface` keeps the same exported symbol.
- `howl_render_text_session_reject_vt_surface` keeps the same exported symbol.
- `howl_render_text_session_cancel_vt_surface` keeps the same exported symbol.
- Output zeroing and invalid-argument/missing-handle/failed status behavior remain identical.
- `cursorIn`, `colorStateIn`, `selectionIn`, `vtSurfaceSlotOut`, `sourceCellsOut`, and publish-result helpers move unchanged except import path fixes.
- All `assertVtCellLayout`, `assertLayout`, and `assertOffset` checks stay beside this translator and move unchanged except import path fixes.
- The translator calls source/session owners; it does not own source-slot state.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg '@import\("vt_surface\.zig"\)' howl-render/src`
- `rg 'howl_render_text_session_reserve_vt_surface_slot|howl_render_text_session_commit_vt_surface|howl_render_text_session_reject_vt_surface|howl_render_text_session_cancel_vt_surface|assertVtCellLayout|cursorIn|colorStateIn|selectionIn' howl-render/src`
- `rg 'submission.zig|prepared_surface.zig' howl-render/src/libhowl_render.zig`

Expected results:

- The first gate prints no stale root import.
- The second gate supports manual review that VT source C translation and layout assertions moved only.
- The third gate proves this slice did not move the remaining root translators.

## Stop Conditions

- Stop if moving `vt_surface.zig` requires changing behavior.
- Stop if the new lane needs a compatibility wrapper at the old root path.
- Stop if an import cycle appears between `ffi/vt_surface.zig` and another translator.
- Stop if layout assertions cannot remain beside the moved VT translator.
