# Current Slice: Move Render Surface Geometry Translator

Owner: workspace root.

Purpose:

- Continue the accepted `howl-render` C translator lane.
- Move only `surface_geometry.zig` from the render root into `src/ffi/`.
- Preserve exported symbols, geometry conversion, and layout/sync behavior exactly.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/render-c-lane.md`
- `howl-render/design.md`
- `howl-render/include/howl_render.h`
- `howl-render/src/libhowl_render.zig`
- `howl-render/src/surface_geometry.zig`
- `howl-render/src/text_session.zig`
- `howl-render/src/test/ffi.zig`
- `howl-render/src/render/geometry_contract.zig`

## Source Changes

Edit only:

- `howl-render/src/libhowl_render.zig`
- `howl-render/src/surface_geometry.zig`
- `howl-render/src/ffi/surface_geometry.zig`
- `howl-render/src/text_session.zig`
- `howl-render/src/test/ffi.zig`

Do not edit any other file unless the build exposes a direct `surface_geometry.zig` import missed by this slice.
If that happens, stop and report the exact import before broadening scope.

## Required Shape

- Move `howl-render/src/surface_geometry.zig` to `howl-render/src/ffi/surface_geometry.zig`.
- Update `libhowl_render.zig` to import the moved translator from `ffi/surface_geometry.zig`.
- Update `text_session.zig` to import the moved translator for `pixelIn` only.
- Update `test/ffi.zig` to import the moved translator.
- Remove the old root `howl-render/src/surface_geometry.zig`; do not leave a compatibility wrapper.
- Keep `libhowl_render.zig` as an export table only.

## Non-Goals

- Do not move `vt_surface.zig`.
- Do not move `submission.zig`.
- Do not move `prepared_surface.zig`.
- Do not move `text_session.zig`.
- Do not move `prepare_request.zig` or `work_state.zig` again.
- Do not rename any `HowlRender*` C type.
- Do not rename any `howl_render_*` symbol.
- Do not change `howl-render/include/howl_render.h`.
- Do not change geometry policy or validation semantics.
- Do not add compatibility aliases.

## Invariants

- The public root curates exports only.
- `howl_render_text_session_derive_layout` keeps the same exported symbol.
- `howl_render_text_session_sync_geometry` keeps the same exported symbol.
- `HowlRenderPixelSize`, `HowlRenderLayoutResult`, and `HowlRenderGeometryResponse` field meanings do not change.
- Missing handle and invalid argument responses remain identical.
- `pixelIn` and `geometryOut` conversions move unchanged except import path fixes.
- `text_session.zig` may keep using `pixelIn`; it must not gain geometry policy.
- The translator calls session/render owners; it does not own render geometry policy.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg '@import\("surface_geometry\.zig"\)' howl-render/src`
- `rg 'howl_render_text_session_derive_layout|howl_render_text_session_sync_geometry|HowlRenderLayoutResult|HowlRenderGeometryResponse|pixelIn|geometryOut' howl-render/src`
- `rg 'vt_surface.zig|submission.zig|prepared_surface.zig|text_session.zig' howl-render/src/libhowl_render.zig`

Expected results:

- The first gate prints no stale root import.
- The second gate supports manual review that symbols and C geometry translation moved only.
- The third gate proves this slice did not move other remaining root translators.

## Stop Conditions

- Stop if moving `surface_geometry.zig` requires changing behavior.
- Stop if the new lane needs a compatibility wrapper at the old root path.
- Stop if an import cycle appears between `ffi/surface_geometry.zig` and `text_session.zig`.
- Stop if `pixelIn` or `geometryOut` cannot move unchanged with the translator.
