# Current Slice: Render Protocol V0 Resource Lifetime Decision

Owner: `howl-render` protocol contract and sprint design memory.

Sprint scratchpad and contract:

- `research/2026-05-31-howl-render-protocol-sprint.md`
- `docs/render-protocol-v0.md`

Purpose:

- Decide the V0 resource lifetime model before more implementation.
- Resolve whether prepared sprite resources become persistent render-owned resources
  first, whether host ack/removal is required first, or whether both must be
  specified together.
- Preserve the reference truth that disposable per-draw backend texture churn is not
  the product path.
- Preserve the ABI truth that `glDeleteTextures()` is not protocol ack.

## Source Order (Mandatory)

Read in order before editing:

1. `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
2. `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
3. `research/2026-05-31-howl-render-protocol-sprint.md`
4. `docs/render-protocol-v0.md`
5. `utils/dev_references/terminals/alacritty`
6. `utils/dev_references/terminals/ghostty`
7. `howl-render/include/howl_render.h`
8. `howl-render/src/protocol_v0/emit.zig`
9. `howl-render/src/protocol_v0/realize.zig`
10. `howl-render/src/prepared/buffer.zig`
11. `howl-linux-host/src/window/term_texture.zig`

## Exact Change Scope

Allowed files:

- `current.txt`
- `research/2026-05-31-howl-render-protocol-sprint.md`
- `docs/render-protocol-v0.md` only if contract text must be sharpened

No product code. No ABI/header changes. No render emission changes. No host changes.

## Required Decision Output

- One accepted lifetime model.
- Exact ownership of resource IDs, generations, retire, ack, and reuse.
- Whether prepared sprite resources can persist across prepared frames.
- Whether software oracle/realizer must become retained before persistent resources
  can ship.
- The first implementation slice after this decision.
- Tests that will prove the decision.

## Stop Conditions

- Stop if the decision would require treating backend deletion as protocol ack.
- Stop if the decision would bless disposable per-draw backend texture churn as the
  product path.
- Stop if the decision requires V0 command drawing or replacing full RGBA.
- Stop if reference-backed ownership remains split after research; record the split
  and ask the user rather than coding.
