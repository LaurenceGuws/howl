# Current Slice: V0 FBO Texture Row Orientation

Owner: `howl-linux-host` V0 command materialization into terminal texture.

Purpose:

- Fix upside-down V0 sprite/fill patch presentation after retained V0 paths become active.
- Preserve the host convention that terminal texture row zero is the logical top row.
- Keep full RGBA fallback/oracle unchanged.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/window/term_texture.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No rendering behavior changes. No full-RGBA deletion.

## Required Shape

- FBO command replay must write logical row zero into terminal texture row zero.
- Do not change final window texture presentation coordinates.
- Do not change protocol command coordinates or upload byte order.
- Preserve GL state restoration and fallback behavior.

## Required Tests

- Existing host tests pass.
- Add a host unit test for FBO Y coordinate mapping.
- Bounded `rain` smoke should keep V0 sprite patch presentation active without fallback regression.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`
