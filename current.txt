# Current Slice: Rename Input Wake Owner

Accepted scratchpad:

- `research/2026-06-01-host-sprint.md`

Accepted research cache:

- `research/cache-2026-06-01-host-owner-boundary-map.md`

## Purpose

Rename the misnamed SDL wake/time bridge from `input/window.zig` to `input/wake.zig` without behavior changes.

## Orchestration Rule

- If ownership, naming, scope, test gates, or ABI consequences are under-specified, stop and report orchestration needed.
- Do not silently fill design gaps, broaden scope, choose new owner names, or turn an ambiguous path into implementation.

## Allowed Files

- `howl-linux-host/src/input/window.zig`
- `howl-linux-host/src/input/wake.zig`
- `howl-linux-host/src/main.zig`
- `howl-linux-host/src/terminal/context.zig`
- `howl-linux-host/src/input/input.zig`
- `howl-linux-host/src/terminal/scrollbar.zig`
- `howl-linux-host/src/terminal/render/surface_layout.zig`
- `howl-linux-host/src/terminal/pty/wait_thread.zig`

No other files are in scope.

## Required Shape

- Rename `howl-linux-host/src/input/window.zig` to `howl-linux-host/src/input/wake.zig`.
- Preserve all functions, types, tests, and behavior in the renamed file.
- Update imports in all current readers:
  - `howl-linux-host/src/main.zig`
  - `howl-linux-host/src/terminal/context.zig`
  - `howl-linux-host/src/input/input.zig`
  - `howl-linux-host/src/terminal/scrollbar.zig`
  - `howl-linux-host/src/terminal/render/surface_layout.zig`
  - `howl-linux-host/src/terminal/pty/wait_thread.zig`
- Update same-directory alias in `input/input.zig` from `window` to `wake`.
- Update local aliases from `InputWindow` to `InputWake` in:
  - `main.zig`
  - `terminal/context.zig`
  - `terminal/scrollbar.zig`
  - `terminal/render/surface_layout.zig`
  - `terminal/pty/wait_thread.zig`
- Preserve centralized app control spine, bounded input turns, and PTY wait-thread wake-only discipline.
- No behavior changes, no ABI changes, no test wiring changes.

## Required Tests

- Preserve and run tests attached to impacted files:
  - `main.zig`
  - `input/input.zig`
  - `terminal/context.zig`
  - `terminal/pty/wait_thread.zig`
- `terminal/scrollbar.zig` and `terminal/render/surface_layout.zig` have no local test blocks in the accepted research, but must compile through the full host test build.

## Verification

From `howl-linux-host`:

- `zig build check`
- `zig build test --summary all`
- `git diff --check`

From workspace root:

- tracked `.zig` line scan must print `TOTAL 0` for lines over 190 chars.

## Stop Conditions

- Stop if any additional file must be touched.
- Stop if any behavior change is required to make the rename compile.
- Stop if test wiring changes are needed.
- Stop if the rename affects PTY, VT, or render ABI semantics.
