# Current Slice: Research VT Selection Ghostty Shape

Owner: workspace root.

Purpose:

- Stop before moving VT selection behavior out of `ffi.zig` until the owner shape is anchored in Ghostty.
- Record whether Howl's next selection cleanup is a mechanical FFI extraction or a deeper screen/selection redesign.
- Preserve C ABI and product behavior; this slice edits research/planning only.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/roadmap.md` Slice 3.1
- `research/2026-05-30-hygiene-audit/roadmap.md` Slice 3.2
- `research/2026-05-30-hygiene-audit/vt-selection-ghostty-shape.md`
- `utils/dev_references/terminals/ghostty/src/terminal/Selection.zig`
- `utils/dev_references/terminals/ghostty/src/terminal/Screen.zig`
- `utils/dev_references/terminals/ghostty/src/terminal/ScreenSet.zig`
- `howl-vt/src/ffi.zig`
- `howl-vt/src/selection.zig`
- `howl-vt/src/selection/state.zig`
- `howl-vt/src/screen_set.zig`

## Source Changes

Edit only research/planning files in this slice:

- `research/2026-05-30-hygiene-audit/vt-selection-ghostty-shape.md`
- `current.txt`

Do not edit product code in this slice.

## Required Shape

- Document Ghostty's selection ownership: `Screen` owns selection, `Selection` owns tracked bounds, `Screen.selectionString` owns text extraction.
- Document current Howl mismatch: coordinate selection state lives under `selection/state.zig`, while projection/copy behavior is still in `ffi.zig`.
- Decide whether the next implementation slice is bounded FFI extraction or a deeper screen/page redesign.
- If bounded extraction is acceptable, define exact files, non-goals, invariants, tests, and grep gates.

## Non-Goals

- Do not change `howl-vt/include/howl_vt.h`.
- Do not edit `howl-vt/src/ffi.zig` yet.
- Do not move selection helper behavior yet.
- Do not introduce Ghostty-style pins yet.
- Do not change C enum values, struct layout, exported names, or behavior.

## Invariants

- Ghostty remains the first source for VT owner shape.
- FFI is a C translator boundary, not the owner of selection projection/copy behavior.
- Howl's immediate cleanup may preserve coordinate semantics, but must state that this is not equivalent to Ghostty's tracked pin model.
- Any future implementation slice must prove history/live row semantics and alternate-screen isolation with tests.

## Verification

From `/home/home/personal/projects/howl`:

- `git diff --check`

## Grep Gates

- `rg 'Screen\.select|selectionString|tracked selection|Selection.init' research/2026-05-30-hygiene-audit/vt-selection-ghostty-shape.md`
- `rg 'HowlVt|howl_vt_' research/2026-05-30-hygiene-audit/vt-selection-ghostty-shape.md` must mention these only as C ABI non-goals/gates, not proposed owner imports.
- `rg 'manager|controller|runtime|flow|pipeline|types\.zig|api\.zig|abi\.zig' research/2026-05-30-hygiene-audit/vt-selection-ghostty-shape.md` prints nothing except banned vocabulary in gates/non-goals.

Expected results:

- The research file cites Ghostty's concrete owner paths and exact Howl seams.
- The next implementation slice is explicitly bounded or sent back to deeper research.

## Stop Conditions

- Stop if Ghostty evidence suggests this cannot be safely handled as FFI extraction.
- Stop if the next slice cannot name exact owners without vague bucket vocabulary.
- Stop if the proposed extraction would hide a screen/page selection redesign.
