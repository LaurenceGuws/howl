# Current Slice: Delete VT `types.zig` Files

Owner: workspace root.

Purpose:

- Delete the remaining `types.zig` files in Howl.
- Split every symbol into its smallest true owner. Do not create another
  ownerless bucket or compatibility shim.
- Preserve public root exports where they are curated API surfaces, but do not
  route them through `types.zig` or a replacement namespace bucket.

## Source Files

- `howl-vt/src/kitty/types.zig`
- `howl-vt/src/kitty/state.zig`
- `howl-vt/src/kitty/apply.zig`
- `howl-vt/src/kitty.zig`
- `howl-vt/src/input/types.zig`
- `howl-vt/src/input.zig`
- `howl-vt/src/input/keyboard.zig`
- `howl-vt/src/input/mouse.zig`
- `howl-vt/src/input/encode.zig`

## Files To Add

- `howl-vt/src/input/event.zig`
- `howl-vt/src/input/encoded.zig`

## Files To Delete

- `howl-vt/src/kitty/types.zig`
- `howl-vt/src/input/types.zig`

Do not leave a shim or compatibility import.

## Symbol Movement

Move from `kitty/types.zig` to existing true owners:

- `ScreenState` -> `kitty/state.zig`
- `GlobalState` -> `kitty/state.zig`
- `ShellMark` -> `kitty/state.zig`
- `NotificationRequest` -> `kitty/state.zig`

Delete namespace aliases instead of moving:

- `Key`
- `Pointer`
- `Color`

Users must import `kitty/key.zig`, `kitty/pointer.zig`, or `kitty/color.zig`
directly when they need those owners. Public `kitty.zig` may still curate root
exports directly from the owner files.

Move from `input/types.zig` to existing true owners:

- keyboard-owned exports stay in `input/keyboard.zig`:
  - `Key`
  - `Modifier`
  - `mod_none`
  - `mod_shift`
  - `mod_alt`
  - `mod_ctrl`
  - all `key_*` aliases
- mouse-owned exports stay in `input/mouse.zig`:
  - `MouseButton`
  - `MouseEventKind`
  - `MouseEvent`
  - `MouseTrackingMode`
  - `MouseProtocol`
  - `mouse_button_*`
  - `mouse_press`
  - `mouse_release`
  - `mouse_move`
  - `mouse_wheel`

Move from `input/types.zig` to new true owners:

- `KeyEvent`, `FocusEvent`, `Event` -> `input/event.zig`
- `Encoded` -> `input/encoded.zig`

Delete namespace aliases instead of moving:

- `Key`
- `Modifier`
- mouse type aliases that duplicate `input/mouse.zig`

## Required Import Changes

Rename local aliases to true owners. Do not use `types`, `KittyNs`,
`kitty_types`, `input_types`, or `*_types` aliases.

In `kitty/state.zig`:

- Own `ScreenState`, `GlobalState`, `ShellMark`, and `NotificationRequest`.
- Import `key.zig`, `pointer.zig`, and `color.zig` directly.
- `State` fields use local `ScreenState` and `GlobalState`.

In `kitty/apply.zig`:

- Import `kitty/state.zig` and `kitty/color.zig` directly.
- `setShellMark` takes `*kitty_state.ShellMark`.
- `appendNotification` takes `*std.ArrayList(kitty_state.NotificationRequest)`.
- Kitty color stack operations call `kitty_color.pushState` / `popState`.

In `kitty.zig`:

- Export public names directly from true owners:
  - `Key` from `kitty/key.zig`
  - `Pointer` from `kitty/pointer.zig`
  - `Color` from `kitty/color.zig`
  - state structs from `kitty/state.zig`

In `input.zig`:

- Export public names directly from true owners:
  - keyboard constants from `input/keyboard.zig`
  - mouse constants from `input/mouse.zig`
  - events from `input/event.zig`
  - `Encoded` from `input/encoded.zig`

In `input/encode.zig`:

- If it currently depends on `input.zig` public root for `Encoded`, prefer
  importing `input/encoded.zig` directly only if needed. Do not introduce cycles.

## Tests

- Move tests with the owners when practical.
- Keep behavior unchanged.
- Add or preserve owner-local tests proving:
  - `kitty/state.zig` owns notification storage/deinit.
  - `input/event.zig` owns event union tags.
  - `input/encoded.zig` owns encoded buffer deinit.

## Invariants

- No compatibility shims.
- No `types.zig` file remains anywhere in Howl.
- No import of `types.zig` remains anywhere in Howl.
- No `types`, `KittyNs`, or `*_types` owner aliases remain under `howl-vt/src`.
- Public root files (`kitty.zig`, `input.zig`) curate exports only; they do not
  own state or mutation.
- No ABI contract changes unless build proves these are Zig-only curated roots.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`

From `/home/home/personal/projects/howl/howl-vt` if needed:

- `zig build check`
- `zig build test`

## Grep Gates

No matches under `/home/home/personal/projects/howl`:

- `types.zig`
- `@import\(.*types.zig`
- `\b[a-zA-Z0-9_]+_types\b`

No matches under `howl-vt/src`:

- `\btypes\b`
- `KittyNs`

No new owner names under `howl-vt/src`:

- `manager`
- `controller`
- `runtime`
- `screen`
- `pipeline`
- `queue`
