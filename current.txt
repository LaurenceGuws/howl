# Current Slice: None

Owner: workspace root.

The VT style attrs slice is complete.

Completed in this slice:

- VT now retains and exports dim, italic, invisible, and strikethrough attrs.
- VT SGR handling covers `2`, `3`, `8`, `9`, `22`, `23`, `28`, and `29` for those attrs.
- VT SGR rectangle reporting includes common dim, italic, invisible, and strikethrough attrs.
- Render maps inverse/reverse video, italic, and strikethrough through existing fields, dims foreground/decorations, and suppresses invisible foreground/decorations while preserving background.
- `howl-vt:test` is routine unit + ABI only; VT regression proofs are explicit under `test:regression`.

Verification run:

- `zig build test` in `howl-vt`
- `zig build test:regression:build` in `howl-vt`
- `zig build test` in `howl-render`
- `zig build test:unit` in `howl-linux-host`
- `zig build test:integration:kitty-graphics-replay` in `howl-linux-host`
- `zig build -Doptimize=ReleaseFast` in `howl-linux-host`

Stop condition:

- Start a new research round or scratchpad before seeding more coding workers.
