# Current Slice: None

Owner: workspace root.

Sprint scope: graphics deletion.

Status: `Generated Placeholder Flag Deletion` completed and accepted.

Accepted commits:

- `howl-vt` `6d5854d vt: delete generated placeholder flag`
- `howl-render` `c44d53c render: drop generated placeholder flag mirror`
- `howl-linux-host` `4db4691 host: delete placeholder replay flag dependency`

Verification passed:

- `zig build test --summary all` in `howl-vt` (`606/606` tests).
- `zig build test:regression:build --summary all` in `howl-vt`.
- `zig build test --summary all` in `howl-render` (`730/733` tests passed, `3` skipped).
- `zig build test:integration:kitty-graphics-replay:build --summary all` in
  `howl-linux-host`.
- `zig build test:integration:kitty-graphics-replay --summary all` in `howl-linux-host`.
- Root `zig build --summary all`.
- Root, `howl-vt`, `howl-render`, and `howl-linux-host` `git diff --check`.

Known broken behavior:

- Any consumer checking `HOWL_VT_GRAPHICS_PLACEMENT_GENERATED_PLACEHOLDER` now breaks by
  design.
- Unicode placeholder generated renderable placement behavior remains broken until a
  separately promoted source-backed replacement slice.

Generated-placeholder deletion queue is complete. Replacement planning can now start from
reference-backed generated cell refs.
