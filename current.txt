# Current Slice: Render Protocol V0 Prepared Oracle Test Target

Owner: `howl-render` test target wiring for protocol proof.

Sprint scratchpad and contract:

- `research/2026-05-31-howl-render-protocol-sprint.md`
- `docs/render-protocol-v0.md`

Purpose:

- Add the smallest test wiring needed for V0 prepared-emission tests to import
  `prepared_buffer.compose()` and text backend dependencies.
- Do not implement prepared emission in this slice.
- Do not weaken the proof with a protocol-local oracle.

## Source Order (Mandatory)

Read in order before editing:

1. `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
2. `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
3. `docs/render-protocol-v0.md`
4. `research/2026-05-31-howl-render-protocol-sprint.md`
5. `howl-render/build.zig`
6. `howl-render/src/test_unit.zig`
7. `howl-render/src/test_abi.zig`
8. `howl-render/src/test.zig`
9. `howl-render/src/test/unit.zig`
10. `howl-render/src/prepared/buffer.zig`
11. `howl-render/src/session/text.zig`
12. `howl-render/src/protocol_v0/emit.zig`

## Exact Change Scope

Allowed files:

- `howl-render/build.zig`
- new test root under `howl-render/src/` if needed
- `howl-render/src/test/unit.zig` only if moving/importing protocol proof tests is needed
- `research/2026-05-31-howl-render-protocol-sprint.md` to record accepted facts

No product code.
No header changes.
No C ABI export functions.
No host code.
No prepared owner/surface storage changes.
No normal render path replacement.
No prepared emission implementation.

## Required Product Shape

- Keep fast unit tests free of unnecessary FreeType/Harfbuzz dependencies unless
  review proves wiring them there is the smallest true owner.
- Prefer a dedicated protocol/prepared-proof test target or root that links the
  existing FreeType/Harfbuzz dependencies already present in `build.zig`.
- The target must be able to import `prepared/buffer.zig`, `session/text.zig`, and
  `protocol_v0/emit.zig` without `ft2build.h` failures.
- The target must run under root `zig build test` and have a focused command for
  protocol proof tests.
- Do not add local or duplicate oracles.

## Required Tests

- Add the smallest compile/import smoke test proving the new target can import
  `prepared/buffer.zig` and `protocol_v0/emit.zig` together.
- If straightforward, add a zero-behavior test named
  `protocol v0 prepared proof target imports owner oracle`.

## Required Invariants

- Test target dependency ownership is explicit in `build.zig`.
- No product module starts depending on test-only wiring.
- No broad umbrella test manager or runtime layer.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

From `/home/home/personal/projects/howl/howl-render`:

- focused protocol proof test command added by this slice
- `zig build test:unit -- "protocol v0"`

## Stop Conditions

- Stop if the test target requires product-code changes.
- Stop if dependency wiring leaks into public ABI or host code.
- Stop if the proof target cannot import `prepared_buffer.compose()` without broad
  unrelated build changes.
