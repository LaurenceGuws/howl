# Current Slice: Delete VT Kitty Root

Owner: `howl-vt`.

Purpose:

- Delete `howl-vt/src/kitty.zig` as stale root glue.
- Preserve exact Kitty owners under `howl-vt/src/kitty/`.
- Preserve Kitty protocol behavior, parser behavior, host-facing ABI, and tests exactly.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/roadmap.md` Slice 3.4
- `howl-vt/src/kitty.zig`
- `howl-vt/src/kitty/state.zig`
- `howl-vt/src/kitty/protocol.zig`
- `howl-vt/src/kitty/apply.zig`
- `howl-vt/src/kitty/key.zig`
- `howl-vt/src/kitty/color.zig`
- `howl-vt/src/kitty/pointer.zig`
- current importers of `kitty.zig` under `howl-vt/src`
- `libs.yaml`

## Source Changes

Exact files:

- Delete `howl-vt/src/kitty.zig`.
- Edit `libs.yaml` to remove stale `kitty.root`.

No import replacements are expected because `kitty.zig` currently has zero direct importers under `howl-vt/src`.

## Non-Goals

- Do not change Kitty behavior.
- Do not change parser behavior.
- Do not change protocol vocabulary.
- Do not change host-facing ABI.
- Do not combine with parser root deletion.
- Do not add compatibility aliases or replacement buckets.

## Invariants

- Exact Kitty owners remain:
  - `howl-vt/src/kitty/state.zig`
  - `howl-vt/src/kitty/protocol.zig`
  - `howl-vt/src/kitty/apply.zig`
  - `howl-vt/src/kitty/key.zig`
  - `howl-vt/src/kitty/color.zig`
  - `howl-vt/src/kitty/pointer.zig`
- `libs.yaml` must not reference deleted files.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg '@import\("kitty\.zig"\)|@import\("\.\./kitty\.zig"\)|@import\("\.\./\.\./kitty\.zig"\)' howl-vt/src` prints nothing.
- `rg 'kitty\.zig' howl-vt/src libs.yaml` prints nothing.

## Stop Conditions

- Stop if any `kitty.zig` importer appears.
- Stop if deleting the root requires touching Kitty behavior.
- Stop if protocol vocabulary, parser behavior, Kitty behavior, or host-facing ABI changes appear.
- Stop if the working tree is dirty before work starts.
