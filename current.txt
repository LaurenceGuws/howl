# Current Slice: V0 Atlas Backed Prepared Sprites

Owner: `howl-render` prepared text atlas truth and V0 glyph/sprite emission.

Purpose:

- Stop emitting one V0 texture resource per prepared sprite draw.
- Move V0 prepared sprite/glyph consequences toward shared atlas resources, matching Ghostty/Alacritty renderer truth.
- Preserve full RGBA fallback/oracle unchanged while atlas-backed V0 coverage is proven.
- Keep full RGBA fallback/oracle unchanged.

## Exact Change Scope

Allowed files:

- `howl-render/src/protocol_v0/emit.zig`
- `howl-render/src/protocol_v0/realize.zig`
- `howl-render/src/prepared/owner.zig`
- `howl-render/src/text/raster/cache.zig`
- `howl-render/src/text/frame_preparer.zig`
- `howl-render/src/text/scene.zig`
- `howl-render/src/test_protocol_proof.zig`
- `howl-render/src/test/ffi.zig`
- `research/2026-05-31-v0-prepared-sprite-resource-bound.md`
- `research/2026-06-01-v0-atlas-backed-prepared-sprites.md`

No host ABI changes unless the current V0 glyph-run ABI is proven insufficient. No full-RGBA deletion.

## Required Shape

- Use Ghostty/Alacritty as the source shape: glyph/sprite raster payloads live in shared atlas textures, draws reference atlas regions.
- Do not add another Howl-only transient churn budget as the product fix.
- Reuse existing V0 glyph-run command/resource model if it can express atlas-backed alpha sprite draws.
- If the current Howl text cache only tracks slots, add the smallest render-owned atlas placement truth required for V0 emission.
- Preserve current transient fallback only as a safety path until atlas-backed V0 is accepted.

## Required Tests

- Existing host tests pass.
- Protocol proof tests show repeated prepared sprite/glyph draws share atlas resources instead of creating per-draw resources.
- Bounded `vttest` smoke should not enter steady-state `v0_emit_status=2` / create-bound fallback from per-draw sprite resources.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`
