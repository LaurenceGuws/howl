# Current Slice: Move Render Surface Geometry Helper

Owner: `howl-render`.

Purpose:

- Move render grid derivation helper out of stale `surface/*` bucket.
- Preserve geometry behavior exactly.
- Do not move source/text adaptation, token contracts, clip helpers, or rgba contracts.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/render-surface-taxonomy.md`
- `howl-render/src/surface/geometry.zig`
- current importers of `surface/geometry.zig`
- `libs.yaml`

## Source Changes

Exact files:

- Move `howl-render/src/surface/geometry.zig` to `howl-render/src/render/grid_geometry.zig`.
- Update importers:
  - `howl-render/src/session/text.zig`
  - `howl-render/src/test/unit.zig`
- Update `libs.yaml` owner metadata.

## Non-Goals

- Do not move `clip_rect.zig` in this slice.
- Do not move token contracts, source publication, input adaptation, or rgba files.
- Do not change geometry derivation behavior.
- Do not change C ABI or render/session behavior.

## Invariants

- `render/grid_geometry.zig` owns render grid derivation from pixel/cell sizes.
- `surface/geometry.zig` no longer exists.
- No imports refer to `surface/geometry.zig`.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'surface/geometry\.zig' howl-render/src libs.yaml` prints nothing.
- `rg 'manager|controller|runtime|types\.zig|api\.zig|abi\.zig' howl-render/src/render/grid_geometry.zig` prints nothing relevant to this slice.

## Stop Conditions

- Stop if moving geometry requires changing session layout, source, prepared, or FFI behavior.
