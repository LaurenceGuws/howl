# Current Slice: Render Generated Placeholder Mixed-Stream Guard

Owner: workspace root.

Sprint scope: graphics.

Promoted from `graphics-generated-placement-transition-scratchpad.md` atomic commit sequence item 2.

## Goal

Add render-side validation that prevents future generated-placeholder placements from being consumed together with legacy placeholder runs.

## Target Files

Render only unless a compile blocker proves otherwise:

- `howl-render/src/frame/surface_text_ffi.zig`
- `howl-render/src/frame/queue.zig`
- render tests in existing test files

## Required Behavior

- Existing old-shape publications with no generated-placeholder placement flags and non-empty placeholder runs still work.
- If any `FfiVtGraphicsPlacement.flags & HOWL_VT_GRAPHICS_PLACEMENT_GENERATED_PLACEHOLDER != 0`, then render must reject or not accept a publish state with non-empty placeholder runs.
- Do not require VT to emit generated placements yet.
- Do not delete old placeholder render path in this slice.

## Hard Constraints

- No VT edits.
- No host edits unless a compile blocker proves otherwise.
- No generated placement publication.
- No render-side Kitty placeholder parsing.
- No yazi/pane special cases.
- Keep this as a guard-only behavior change.

## Verification Required

- `zig build test` in `howl-render`
- `zig build test:unit` in `howl-linux-host` if public ABI use is touched
- `git diff --check` at workspace root

Stop condition:

- If current render publication validation cannot express the guard cleanly, stop and report the exact blocker. Do not add a second placement stream or new protocol interpretation.
