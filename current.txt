# Current Slice: Prepared Render-Surface Retrieval Status ABI

Active scratchpad:

- `research/2026-06-02-render-surface-abi-status-sprint.md`

Accepted research:

- `research/cache-2026-06-02-render-surface-abi-status-readiness.md`

Accepted reviewer decision:

- ABI cleanup is the first hygiene implementation slice.
- `render_surface_emit_status` is a real render-owned consequence but is in the wrong ABI shape.
- `howl_render_prepared_surface_render_surface(...)` must return one C-visible retrieval status directly.
- `HowlRenderPreparedSurfaceInfo.render_surface_emit_status` must be removed.
- No old enum names, old constants, aliases, wrappers, compatibility shims, or Zig-shaped host shortcuts.

Worker must implement exactly the promoted scratchpad and stop on its stop conditions.
