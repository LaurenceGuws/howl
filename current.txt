# Current Slice: Shrink VT Input Bucket Imports

Owner: workspace root.

Purpose:

- Start reducing `howl-vt/src/input.zig` by replacing internal broad-bucket imports with exact input owners.
- Preserve C ABI values and input encoding behavior exactly.
- Leave `input.zig` in place only until remaining test/root compatibility is intentionally resolved.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/roadmap.md` Slice 3.1
- `research/2026-05-30-hygiene-audit/synthesis.md` VT wrapper finding
- `howl-vt/src/input.zig`
- direct importers of `input.zig`:
  - `howl-vt/src/host/apply.zig`
  - `howl-vt/src/kitty/apply.zig`
  - `howl-vt/src/ffi.zig`
  - `howl-vt/src/test/terminal_modes_reports.zig`
  - `howl-vt/src/host/state.zig`
  - `howl-vt/src/control/mode.zig`
  - `howl-vt/src/test/terminal_surface.zig`
  - `howl-vt/src/howl_vt.zig`
  - `howl-vt/src/control/report.zig`
  - `howl-vt/src/control/locator.zig`
- exact owners under `howl-vt/src/input/`

## Source Changes

Edit only direct importers listed above and `howl-vt/src/input.zig` if it can be narrowed after import repairs.

Do not edit any other file unless the build exposes a direct `input.zig` importer missed by this slice.
If that happens, stop and report the exact import before broadening scope.

## Required Shape

- Replace broad `input.zig` imports in internal owner files with exact input owner imports:
  - `input/encode.zig` for `Scratch` and encode functions.
  - `input/encoded.zig` for `Encoded`.
  - `input/event.zig` for `Event`, `KeyEvent`, and `FocusEvent`.
  - `input/keyboard.zig` for key/modifier types and key constants.
  - `input/mouse.zig` for mouse types and constants.
- Keep `input.zig` only if needed as a temporary module index for tests/root; if kept, do not add new aliases.
- Do not delete `input.zig` unless all importers are repaired and tests prove it is unused.

## Non-Goals

- Do not change `howl-vt/include/howl_vt.h`.
- Do not change C enum values or struct layout.
- Do not rewrite input encoding.
- Do not touch `action.zig`, `parser.zig`, or `kitty.zig` buckets beyond exact import repair in `kitty/apply.zig`.
- Do not delete tests or change test intent.

## Invariants

- C input constants preserve exact numeric values.
- FFI exports and status behavior remain unchanged.
- Internal callers import exact owner files instead of `input.zig` wherever possible.
- If `input.zig` remains, it is a temporary module index only, not a behavior owner.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg '@import\("input\.zig"\)|@import\("\.\./input\.zig"\)' howl-vt/src`
- `rg 'pub const key_|pub const mouse_|pub const encode' howl-vt/src/input.zig`
- `rg 'HOWL_VT_|HowlVtInput|HowlVtMouse' howl-vt/include/howl_vt.h howl-vt/src`

Expected results:

- The first gate shrinks to only explicitly justified temporary module-index/test/root uses or prints nothing.
- The second gate supports review that `input.zig` is no longer a broad behavior alias bucket if it remains.
- The third gate supports manual numeric ABI review; values and public names must not change.

## Stop Conditions

- Stop if any numeric ABI value must change.
- Stop if a test/root import requires deciding whether `input.zig` is a public internal root.
- Stop if import repairs require behavior changes in input encoding.
