# Current Slice: Host Lock Graph Normalization (Slice 1)

Owner: `howl-linux-host` terminal synchronization ownership.

Sprint scratchpad (authoritative):

- `research/2026-05-30-alacritty-host-fair-mutex-sprint.md`

Purpose:

- Remove render-turn nested re-lock cycles on `term.mutex`.
- Preserve serialization between VT mutation and VT surface copy.
- Land a deadlock-free first render turn without inventing new host-loop policy.

## Source Order (Mandatory)

Read in order before editing:

1. `utils/dev_references/terminals/alacritty/alacritty_terminal/src/sync.rs`
2. `utils/dev_references/terminals/alacritty/alacritty_terminal/src/event_loop.rs`
3. `utils/dev_references/terminals/alacritty/alacritty/src/window_context.rs`
4. `utils/dev_references/terminals/alacritty/alacritty/src/display/mod.rs`
5. `utils/official_docs/ziglang.org/download/0.16.0/release-notes.html`
6. `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
7. `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
8. `howl-linux-host/src/terminal/term.zig`
9. `howl-linux-host/src/terminal/context.zig`
10. `howl-linux-host/src/terminal/pty/pump.zig`
11. `howl-linux-host/src/terminal/vt/surface.zig`
12. `howl-linux-host/src/terminal/vt/retained.zig`
13. `howl-linux-host/src/main.zig`

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/terminal/context.zig`
- `howl-linux-host/src/terminal/vt/surface.zig`
- `howl-linux-host/src/terminal/pty/pump.zig`
- `howl-linux-host/src/terminal/term.zig` (only if needed for lock API normalization)
- `howl-linux-host/src/main.zig` (diagnostic log removal only)

## Required Shape

- No function may hold `term.mutex` and call another function that can lock `term.mutex`.
- Locked vs unlocked variants are explicit where necessary.
- `renderTurn` path is deadlock-free.
- VT copy path remains serialized against VT mutation.

## Non-Goals

- Do not finish full fair-mutex feature parity in this slice.
- Do not refactor unrelated host modules.
- Do not change ABI contracts.

## Invariants

- First render turn cannot self-deadlock.
- No concurrent VT feed+copy on shared terminal state.
- No temporary trace logs remain in final diff.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build check`
- `zig build test`
- `zig build run` (manual: no startup deadlock at first render turn)

From `/home/home/personal/projects/howl/howl-vt`:

- `zig build check`
- `zig build test`

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if lock ownership remains ambiguous at any render-turn boundary.
- Stop if fix requires unscoped broad locking that regresses boundedness.
- Stop if any ABI boundary change is required.
