# Current Slice: V0 Interval Diagnostics

Owner: `howl-linux-host` submit diagnostics.

Purpose:

- Separate cumulative V0 counters from per-log-interval counters.
- Make mixed runs such as `btop` followed by `rain` diagnosable without resetting the process.
- Record bounded no-sidecar reason buckets from the prepared V0 resource plan.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/terminal/context.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No rendering behavior changes. No full-RGBA deletion.

## Required Shape

- Keep existing cumulative diagnostics.
- Add per-log interval deltas for V0 present/fallback/no-sidecar counters.
- Add bounded no-sidecar reason buckets for resource-plan statuses.
- Include a stable label from current title/fallback command when logging.
- Do not use diagnostics to change rendering policy.

## Required Tests

- Existing host tests pass.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`
