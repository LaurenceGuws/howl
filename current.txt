# Current Slice: V0 Retained Fill Patch Present Path

Owner: `howl-linux-host` submit diagnostics and terminal texture V0 fill materialization.

Purpose:

- Present partial no-clear fill-only V0 frames as retained texture patches when prior matching texture content exists.
- Keep first-frame, resized, or uninitialized no-clear fill patches on RGBA fallback.
- Keep full RGBA present path as fallback/oracle.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/window/term_texture.zig`
- `howl-linux-host/src/terminal/context.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No full-RGBA deletion. No host ack/removal contract.

## Required Shape

- Accept partial no-clear fill-only V0 frames only if the terminal texture existed before upload and matched the frame size.
- Reject/fallback if the texture was newly created/resized in the current turn.
- Reject/fallback for non-fill commands, resource-bearing commands, malformed spans, or out-of-bounds rectangles.
- Preserve existing first-full-clear and full-coverage fill-only paths.
- Log retained-patch present/fallback counts.

## Required Tests

- Add host tests for partial fill patch acceptance and resource/mixed command rejection.
- Existing host tests pass.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if retained patch safety needs render ABI changes.
- Stop if prior texture initialization cannot be proved at the host boundary.
