Current Task

Source: `host-simplification-plan.md`

Task 14: Public PTY facade still exists as a parallel Zig surface.

Required outcome:

- Demote `howl-pty/src/pty.zig` to internal plumbing.
- Keep build-selected PTY construction behind `Session`.
- Remove the parallel Zig PTY surface as a preserved package story.

Primary offenders:

- `howl-pty/src/pty.zig:19-74`

No guessing allowed:

- Read `AGENTS.md`
- Read `reference-index.md`
- Read `host-simplification-plan.md`
- Read `howl-pty/design.md`
- Read the live PTY owner/root files.
- Read Ghostty and Alacritty PTY reference files explicitly.
- Use `AGENTS.md` boundary rules with zero guessing.
- If the correct owner seam cannot be named exactly, stop and report `work-not-clear`.

Acceptance target:

- The public PTY facade is gone or fully demoted from package surface.
- The C ABI and true owners remain the only real PTY story.
- The host gets simpler and dumber.
