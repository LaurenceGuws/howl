# Current Slice: Linux Host V0 Sprite Present Path

Owner: `howl-linux-host` terminal texture V0 command materialization.

Purpose:

- Materialize V0 clear/fill/sprite commands into the terminal texture in protocol order.
- Keep glyph-run frames and unsupported resource shapes on RGBA fallback.
- Keep full RGBA present path as fallback/oracle.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/window/term_texture.zig`
- `howl-linux-host/src/terminal/context.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No full-RGBA deletion. No glyph-run V0
presentation.

## Required Shape

- Accept V0 present frames containing clear/fill and draw-sprite commands only.
- Reject/fallback on glyph-run commands.
- Draw into the terminal texture, not over the already-presented RGBA texture, so command
  ordering is preserved.
- Use host-owned GL resources only; render ABI must not expose backend objects.
- Fall back to full RGBA upload on any unsupported V0 shape or GL failure.

## Required Tests

- Add host tests for sprite-capable V0 detection.
- Existing host tests pass.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if sprite drawing requires render ABI changes.
- Stop if command ordering cannot be preserved with host-owned backend state.
