# Current Slice: Placeholder Semantic Residuals Research

Owner: workspace root.

Source scratchpad:

- `render-surface-hardening-scratchpad.md`
- Promoted item: `Slice F: Placeholder Semantic Residuals`

## Goal

Decide whether any residual Kitty placeholder semantics are still incorrectly owned or inferred by render after VT placeholder-run export landed.

This is a research/design slice first. Do not seed a coding worker until the ownership answer and proof matrix are unambiguous.

## Current Baseline

- VT exports resolved placeholder runs.
- Render consumes VT placeholder runs directly.
- Render no longer reconstructs placeholder runs from raw cells in the primary path.
- Host carries placeholder-run ABI metadata.
- Integration replay proofs cover app-icon and unicode-placeholder paths.

## Questions To Answer

- Are one-axis placement semantics correct against Kitty protocol behavior?
- Is cross-kind ordering fully authoritative from VT truth, or does render still infer ordering locally?
- Does render retain any fallback path that semantically co-owns placeholder truth and should move to VT?
- Are placeholder run bounds, clipping, placement identity, and virtual placement behavior proven at the right owner boundary?
- What smallest code slice, if any, would make render a pure consumer of VT placeholder truth?

## Scope For Research Agents

Read Howl:

- `howl-vt` placeholder export, graphics placement, and surface publication files found by search.
- `howl-render` placeholder run ingestion, composition ordering, shaping/draw math, and tests found by search.
- `howl-render/include/howl_render.h` placeholder-run ABI structs.
- `howl-linux-host` placeholder replay/proof surfaces only as integration evidence.

Read references:

- Kitty graphics protocol docs in repo references, especially unicode placeholders, placement, z/order, virtual placements, and cell coverage rules.
- Ghostty placeholder/graphics handling if present in `utils/dev_references/terminals/ghostty`.
- TigerBeetle style docs for assertion/proof discipline.

## Required Output

The research round must produce a plain-English rewrite brief with:

- current bad shape, if any
- reference-derived semantic owner answer
- exact files that need code changes, if any
- smallest acceptable code slice
- proof matrix by owner boundary
- explicit rejection criteria for coding workers

## Stop Condition

Stop after research if there is no concrete code slice or if ownership remains ambiguous. Do not invent render-side semantics to paper over unclear Kitty/VT behavior.
