# Current Slice: Linux Host V0 Glyph Run Present Path

Owner: `howl-linux-host` terminal texture V0 command materialization.

Purpose:

- Materialize V0 glyph-run commands into the terminal texture in protocol order.
- Reuse render-owned V0 glyph atlas resources realized as host-owned GL textures.
- Keep full RGBA present path as fallback/oracle for unsupported glyph/resource shapes or GL failure.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/window/term_texture.zig`
- `howl-linux-host/src/terminal/context.zig`
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No protocol changes. No full-RGBA deletion. No host ack/removal contract.

## Required Shape

- Accept V0 present frames containing a full first clear plus clear/fill/sprite/glyph-run commands.
- Draw into the terminal texture, not over the final framebuffer, so command ordering is preserved.
- Use host-owned GL resources only; render ABI must not expose backend objects.
- Support `HOWL_RENDER_V0_RESOURCE_GLYPH_ATLAS_ALPHA` glyph references only.
- Reject/fallback for color glyph atlas resources, malformed glyph refs, missing realized atlas resources, or GL failure.
- Keep fill-only and sprite paths working.

## Required Tests

- Add host tests for glyph-capable V0 detection and rejection of unsupported glyph atlas shape.
- Existing host tests pass.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if glyph presentation requires render ABI changes.
- Stop if command ordering cannot be preserved with host-owned backend state.
- Stop if glyph atlas ownership requires protocol ack/removal semantics in this slice.
