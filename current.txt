# Current Slice: Move Render Text Prepare Counters

Owner: workspace root.

Purpose:

- Continue splitting `howl-render/src/text/pipeline.zig` by moving prepare counters to the frame-preparation owner.
- Move only `TextPrepareCounters` to a true prepare counter owner.
- Preserve counter fields and defaults exactly.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/render-text-pipeline-taxonomy.md`
- `howl-render/src/text/pipeline.zig`
- `howl-render/src/text/frame_preparer.zig`
- `howl-render/src/text/direct_normal.zig`

## Source Changes

Edit only:

- `howl-render/src/text/pipeline.zig`
- `howl-render/src/text/prepare_counters.zig`
- `howl-render/src/text/frame_preparer.zig`
- `howl-render/src/text/direct_normal.zig`

Do not edit any other file unless the build exposes a direct `TextPrepareCounters` use missed by this slice.
If that happens, stop and report the exact import before broadening scope.

## Required Shape

- Add `howl-render/src/text/prepare_counters.zig`.
- Move only `TextPrepareCounters` out of `pipeline.zig`.
- Update `frame_preparer.zig` and `direct_normal.zig` to use the new owner.
- Keep `pipeline.zig` for all non-moved symbols; do not add aliases from `pipeline.zig` to moved symbols.

## Non-Goals

- Do not rename or delete `pipeline.zig`.
- Do not move shape operation symbols.
- Do not move build/group request-output symbols.
- Do not change headers or exported C ABI symbols.
- Do not change scene preparation, shaping, rasterization, prepared metrics, submit metrics, or submit behavior.
- Do not add compatibility aliases.

## Invariants

- `TextPrepareCounters` fields and zero defaults remain identical.
- `frame_preparer.TextFramePreparer.counters` keeps the same semantics.
- `direct_normal.counters` keeps the same return values.
- `applyCounters` still adds the same fields.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'TextPrepareCounters' howl-render/src/text/pipeline.zig`
- `rg 'TextPrepareCounters' howl-render/src/text/prepare_counters.zig howl-render/src/text/frame_preparer.zig howl-render/src/text/direct_normal.zig`
- `rg 'pipeline\.TextPrepareCounters|text_pipeline\.TextPrepareCounters' howl-render/src`
- `rg 'types\.zig|api\.zig|manager|controller|engine|runtime' howl-render/src/text howl-render/src/prepared howl-render/src/session`

Expected results:

- The first gate prints nothing.
- The second gate proves the moved counter type and direct uses.
- The third gate prints nothing.
- The fourth gate prints nothing except unrelated pre-existing terms if explicitly reviewed.

## Stop Conditions

- Stop if moving prepare counters requires changing fields, defaults, or accumulation behavior.
- Stop if a direct importer requires both old and new names through an alias.
- Stop if implementation begins moving shape, build, or group symbols.
