# Current Slice: Present/Retire Final Contract Research

Owner: workspace root.

Source scratchpad:

- `render-surface-hardening-scratchpad.md`
- Promoted item: `Slice E: Present/Retire Final Contract`

## Goal

Decide the final render/host contract for presented-frame identity before any more code is written in this area.

This is a research/design slice, not a coding slice yet. The scratchpad still has a real open question: keep the explicit one-in-flight render-owned presented frame, or move present/retire identity fully to host ownership with a cleaner acknowledgement model.

## Current State

- Render currently has explicit one-in-flight `presented_frame: ?pipeline.SubmittedFrame` in `queue.zig`.
- Host currently gates submit until presented-frame retire/ack clears.
- `retirePresented()` returns the presented snapshot sequence and clears render’s presented state.
- This was an improvement over implicit/lossy state, but it may still be architecturally muddy.

## What We Need To Know

Research must answer plainly:

- What does Ghostty’s render/runtime seam imply about who owns present/ack identity?
- What does Alacritty’s display/window presentation path imply about host-owned presentation state?
- What does TigerBeetle require for state-machine proof if render keeps one-in-flight identity?
- Is Howl’s current explicit render-owned `presented_frame` stronger than the references, or just a local patch over a host ownership gap?
- What exact next code slice should follow, if any?

## Scope For Research Agents

Read Howl:

- `howl-render/src/frame/queue.zig`
- `howl-render/src/frame/surface_text_ffi.zig`
- `howl-render/src/frame/pipeline.zig`
- `howl-linux-host/src/terminal/terminal_panel.zig`
- `howl-linux-host/src/terminal/vt/surface.zig`
- `howl-render/include/howl_render.h`

Read references:

- `utils/dev_references/terminals/ghostty/src/renderer.zig`
- `utils/dev_references/terminals/ghostty/src/Surface.zig`
- `utils/dev_references/terminals/alacritty/alacritty/src/display/window.rs`
- `utils/dev_references/terminals/alacritty/alacritty/src/display/mod.rs`
- `utils/dev_references/terminals/alacritty/alacritty/src/window_context.rs`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`

## Required Output

The research round must produce a plain-English rewrite brief with:

- current bad shape, if any
- reference-derived ownership answer
- whether to keep render-owned one-in-flight presented state
- whether to replace `retirePresented()` with a host-owned acknowledgement shape
- smallest acceptable code slice
- proof matrix for the chosen design
- explicit rejection criteria for coding workers

## Stop Condition

Do not seed a coding worker for this slice until the research brief makes the ownership answer unambiguous.
