# Current Slice: Fold Render Rgba Contract

Owner: `howl-render`.

Purpose:

- Delete stale `surface/rgba.zig` by moving `Rgba8` into the text contract owner that already exports it.
- Preserve all color contract behavior exactly.
- Leave large `surface/input.zig` for a separate source-to-text adaptation decision.

## Source Research

Read before editing:

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `research/2026-05-30-hygiene-audit/render-surface-taxonomy.md`
- `howl-render/src/surface/rgba.zig`
- `howl-render/src/text/contract.zig`
- grep results for `Rgba8`
- `libs.yaml`

## Source Changes

Exact files:

- Edit `howl-render/src/text/contract.zig` to define `Rgba8` directly.
- Delete `howl-render/src/surface/rgba.zig`.
- Remove the `rgba` owner entry from `libs.yaml`.

## Non-Goals

- Do not move `surface/input.zig`.
- Do not change any color fields or defaults.
- Do not change C ABI or FFI layout behavior.

## Invariants

- `contract.Rgba8` remains the public Zig type used by text/prepared code.
- `surface/rgba.zig` no longer exists.
- No imports refer to `surface/rgba.zig`.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'surface/rgba\.zig' howl-render/src libs.yaml` prints nothing.

## Stop Conditions

- Stop if folding `Rgba8` requires changing source, text, prepared, or ABI behavior.
