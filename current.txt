# Current Slice: Render C Translator Lane Decision

Owner: workspace root.

Purpose:

- Decide the exact C translator lane shape for `howl-render` before moving any root translator files.
- Classify every current render root ABI file as translator, owner, or stale.
- Select one bounded first implementation move for the next slice.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/roadmap.md` Slice 2.1
- `research/2026-05-30-hygiene-audit/synthesis.md`
- `research/2026-05-30-hygiene-audit/c-abi-header-grammar.md`
- `howl-render/design.md`
- `howl-render/include/howl_render.h`
- `howl-render/src/libhowl_render.zig`

## Source Changes

Edit only:

- new `research/2026-05-30-hygiene-audit/render-c-lane.md`

## Required Content

- Decide whether render uses one `ffi.zig` translator or an `ffi/` translator lane grouped by ABI nouns.
- Classify these current root files as translator, owner, or stale:
  - `howl-render/src/vt_surface.zig`
  - `howl-render/src/prepare_request.zig`
  - `howl-render/src/submission.zig`
  - `howl-render/src/prepared_surface.zig`
  - `howl-render/src/text_session.zig`
  - `howl-render/src/surface_geometry.zig`
  - `howl-render/src/work_state.zig`
- Preserve render ownership:
  - `source/*` owns VT-derived input snapshots.
  - `prepared/*` owns prepared output.
  - `session/*` composes the C handle.
  - `render/*` and `text/*` own rendering and shaping.
- Select the first implementation move and explain why it is bounded.
- Include exact files, non-goals, verification, grep gates, and stop conditions for that next slice.

## Non-Goals

- Do not edit render code.
- Do not move files.
- Do not rename ABI symbols.
- Do not change headers.
- Do not introduce `manager`, `controller`, `engine`, `runtime`, `types.zig`, `api.zig`, or broad
  owner buckets.

## Verification

From `/home/home/personal/projects/howl`:

- `git diff --check`

## Grep Gates

- `rg 'vt_surface.zig|prepare_request.zig|submission.zig|prepared_surface.zig|text_session.zig|surface_geometry.zig|work_state.zig' research/2026-05-30-hygiene-audit/render-c-lane.md`
- `rg 'manager|controller|engine|runtime|types.zig|api.zig' research/2026-05-30-hygiene-audit/render-c-lane.md` must not propose banned owners.

## Stop Conditions

- Stop if the document cannot distinguish translator files from true render owners.
- Stop if the only proposed shape is a generic `types.zig` or vague C bucket.
