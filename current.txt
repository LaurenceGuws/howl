# Current Slice: Delete Render Boundary `frame.zig`

Owner: workspace root.

Purpose:

- Delete `howl-render/src/frame.zig`.
- Render does not own “frames” as a product noun at the C boundary.
- Render knows VT surface/events and render surface/events.
- Keep `howl-render/include/howl_render.h` unchanged even though the C ABI still
  has shipped `HowlRenderPreparedFrame` names.

## Source Files

- `howl-render/src/frame.zig`
- `howl-render/src/prepare_request.zig`
- `howl-render/src/prepared_surface.zig`
- `howl-render/src/submission.zig`
- `howl-render/src/surface/tokens.zig`
- `howl-render/src/libhowl_render.zig`

## File To Delete

- `howl-render/src/frame.zig`

Do not leave a shim or compatibility import.

## Symbol Movement

Move from `frame.zig` to `prepare_request.zig`:

- `prepareRequestOut()`
- `prepareTokenIn()`

Move from `frame.zig` to `submission.zig`:

- `preparedFrameOut()`
- `preparedFrameIn()`
- `samePreparedFrame()`

Move or duplicate only where needed, with the smallest true owner:

- `damageKindIn()` may move to `prepare_request.zig` if only prepare request
  parsing needs it, or to `submission.zig` if submission parsing needs it too.
- Prefer a private helper in each file over a new shared generic module for this
  slice, unless duplication becomes unsafe.

## Required Import Changes

In `prepare_request.zig`:

- Remove `@import("frame.zig")`.
- Own C `HowlRenderPrepareRequest` translation locally.

In `prepared_surface.zig`:

- Remove `@import("frame.zig")`.
- Import `prepare_request.zig` for `prepareTokenIn()` only.

In `submission.zig`:

- Remove `@import("frame.zig")`.
- Own C `HowlRenderPreparedFrame` translation locally.

## Invariants

- No C header change.
- No behavior change.
- No `frame.zig` file remains under `howl-render/src`.
- No `@import("frame.zig")` remains.
- No compatibility shim.
- No `types.zig`.
- No `manager`, `controller`, `runtime`, `flow`, `pipeline`, or `queue` owner.
- Do not rename public C ABI symbols in this slice.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`

From `/home/home/personal/projects/howl/howl-render` if needed:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

No matches under `howl-render/src`:

- `@import("frame.zig")`

No file:

- `howl-render/src/frame.zig`

No new paths/names under `howl-render/src` matching:

- `types.zig`
- `manager`
- `controller`
- `runtime`
- `flow`
- `pipeline`
- `queue`
