# Current Slice: Delete Render Protocol-Proof Build Bucket

Owner: `howl-render` test build ownership only.

Source scratchpad:

- `research/2026-06-01-render-api-language-deletion-sprint.md`

Accepted research cache:

- `research/cache-2026-06-01-render-api-language-deletion.md`

Previous slice:

- `howl-render` `aa1749d` moved render API V0 oracle/equivalence tests under
  unfiltered `zig build test:unit`.

## Purpose

Delete the separate `test:protocol-proof` build/test bucket now that its proof
tests are owned and discovered by the unit gate.

## Allowed Files

- `howl-render/build.zig`
- `howl-render/src/test_protocol_proof.zig`

No other files are in scope.

## Required Shape

- Delete `howl-render/src/test_protocol_proof.zig`.
- Remove `protocol_proof_mod`.
- Remove `protocol_proof_tests` and `run_protocol_proof_tests`.
- Remove `test:protocol-proof` and `test:protocol-proof:build` steps.
- Remove dependencies from `test:build` and `test` on the deleted proof bucket.
- Keep `zig build test` aggregating `test:unit` and `test:abi`.
- Keep `zig build test:build` aggregating `test:unit:build` and
  `test:abi:build`.
- Do not remove font/options/FreeType/HarfBuzz wiring from `unit_mod`; the moved
  oracle tests require it.

## Non-Goals

- Do not move source owner files.
- Do not rename public ABI symbols or exported functions.
- Do not edit host code.
- Do not edit docs.
- Do not rename internal `protocol_v0` source files in this slice.

## Stop Conditions

- Stop if any proof/oracle test becomes undiscoverable from unfiltered
  `zig build test:unit`.
- Stop if deleting the build bucket requires ABI or host changes.
- Stop if `test:protocol-proof` or `protocol_proof` remains in active render
  build/test source after this slice.

## Verification

From `/home/home/personal/projects/howl/howl-render`:

- `zig build test:unit`
- `zig build test:abi`
- `zig build test`
- `zig build test:build`
- `zig build check`
- `git diff --check`

Grep gate from `/home/home/personal/projects/howl/howl-render`:

- `rg 'test:protocol-proof|protocol_proof|test_protocol_proof' build.zig src`
  prints nothing.
