# Current Slice: Graphics Base64 Compatibility Retirement Research

Owner: workspace research; likely future owners are howl-vt, howl-render, and public ABI
compatibility policy.

Sprint scope: graphics.

## Goal

Decide the next source-backed step after the end-to-end decoded graphics publication path:
which base64/protocol payload paths are compatibility ABI that must remain, and which
base64-retained internals can be retired or narrowed.

## Completed Dependency State

- VT direct chunks accumulate decoded transport bytes.
- VT exposes parallel decoded graphics query/copy ABI.
- Render exposes parallel decoded graphics commit-slot ABI.
- Linux host copies VT decoded graphics bytes and publishes through render decoded ABI.
- Existing VT/render base64/protocol payload ABIs remain compatibility paths.

## Source Truth

- `AGENTS.md`
- `loop.txt`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `howl-vt/include/howl_vt.h`
- `howl-render/include/howl_render.h`
- `howl-vt/src/kitty/graphics.zig`
- `howl-vt/src/ffi.zig`
- `howl-render/src/frame/graphics_prepare.zig`
- `howl-render/src/frame/queue.zig`
- `howl-linux-host/src/terminal/vt/surface.zig`
- `utils/dev_references/terminals/kitty/kitty/graphics.c`
- `utils/dev_references/terminals/kitty/kitty/graphics.h`
- `utils/dev_references/terminals/ghostty/src/terminal/kitty/graphics_image.zig`
- `utils/dev_references/terminals/ghostty/src/terminal/kitty/graphics_storage.zig`

## Research Questions

- Which old base64/protocol payload APIs are public ABI compatibility and must remain for
  external embedders?
- Which old base64-retained fields are now internal-only compatibility publication and no
  longer product truth?
- Can VT stop storing base64 as primary image/frame truth while still generating old ABI
  compatibility bytes on demand or at publication time?
- Can render keep legacy base64 ingestion as an ABI compatibility path while decoded path
  becomes the in-tree default?
- What is the smallest next implementation slice: VT internal decoded-truth cleanup,
  render legacy-path isolation, ABI documentation, or quota/eviction redesign?

## Required Output

- Research only. Do not edit product code.
- Return source-backed findings with exact file/line references.
- Classify each base64 path as public ABI compatibility, internal debt, or still-needed
  implementation bridge.
- Draft the next worker-ready slice if one is safe.

## Out Of Scope

- No code implementation in this slice.
- No breaking ABI changes without an explicit product decision.
- No quota/eviction redesign unless source proves it is the first required cleanup.
- No removal of external compatibility paths by assumption.

## Verification

- Scratchpad update must pass root `git diff --check`.

Stop conditions:

- Stop if removing or changing any public ABI requires a product compatibility decision.
- Stop if internal base64 cleanup cannot be sliced without broad quota/frame/render work.
