# Current Slice: Move Render Handle Translator

Owner: workspace root.

Purpose:

- Move the remaining root render C handle translator into the accepted `src/ffi/` lane.
- Move only `handle.zig` from the render root into `src/ffi/`.
- Preserve C handle-to-owner pointer translation exactly.

## Source Research

Use:

- `howl-render/design.md`
- `libs.yaml`
- `howl-render/src/handle.zig`
- `howl-render/src/ffi/*.zig`
- `howl-render/src/surface/prepared_owner.zig`
- `howl-render/src/session/text.zig`

## Source Changes

Edit only:

- `howl-render/src/handle.zig`
- `howl-render/src/ffi/handle.zig`
- `howl-render/src/ffi/prepare_request.zig`
- `howl-render/src/ffi/work_state.zig`
- `howl-render/src/ffi/surface_geometry.zig`
- `howl-render/src/ffi/text_session.zig`
- `howl-render/src/ffi/vt_surface.zig`
- `howl-render/src/ffi/prepared_surface.zig`
- `howl-render/src/ffi/submission.zig`
- `howl-render/design.md`
- `libs.yaml`

Do not edit any other file unless the build exposes a direct `handle.zig` import missed by this slice.
If that happens, stop and report the exact import before broadening scope.

## Required Shape

- Move `howl-render/src/handle.zig` to `howl-render/src/ffi/handle.zig`.
- Update `src/ffi/*.zig` translators to import `handle.zig` from the same `ffi/` lane.
- Remove the old root `howl-render/src/handle.zig`; do not leave a compatibility wrapper.
- Update `howl-render/design.md` and `libs.yaml` to include handle translation in the `ffi/` lane.

## Non-Goals

- Do not rename `src/ffi.zig`.
- Do not move `src/libhowl_render.zig` or `src/test.zig`.
- Do not move prepared/session owner files.
- Do not rename any `HowlRender*` C type.
- Do not rename any `howl_render_*` symbol.
- Do not change `howl-render/include/howl_render.h`.
- Do not change pointer cast, null-handle, or prepared-handle semantics.
- Do not add compatibility aliases.

## Invariants

- `textSessionOwner`, `opaquePreparedHandle`, and `abiPreparedHandle` move unchanged except import path fixes.
- All FFI translators still use the same handle translator functions.
- The handle translator casts C handles only; session and prepared owners keep behavior.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg '@import\("(\.\./)?handle\.zig"\)' howl-render/src`
- `rg 'textSessionOwner|opaquePreparedHandle|abiPreparedHandle' howl-render/src/ffi`
- `rg 'howl-render/src/handle\.zig' howl-render/design.md libs.yaml`

Expected results:

- The first gate prints no stale root import; same-directory `@import("handle.zig")` inside `src/ffi/` is expected.
- The second gate supports manual review that handle translation moved only.
- The third gate prints nothing.

## Stop Conditions

- Stop if moving `handle.zig` requires changing behavior.
- Stop if the new lane needs a compatibility wrapper at the old root path.
- Stop if an import cycle appears inside `src/ffi/`.
