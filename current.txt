# Current Slice: Move Render Submission Translator

Owner: workspace root.

Purpose:

- Finish the accepted `howl-render` C translator lane for current root ABI translators.
- Move only `submission.zig` from the render root into `src/ffi/`.
- Preserve prepared publication, submit-decision, submit-handle, token conversion, and submit-result behavior exactly.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/render-c-lane.md`
- `howl-render/design.md`
- `howl-render/include/howl_render.h`
- `howl-render/src/libhowl_render.zig`
- `howl-render/src/submission.zig`
- `howl-render/src/submit_result.zig`
- `howl-render/src/test/ffi.zig`
- `howl-render/src/surface/prepared_owner.zig`
- `howl-render/src/surface/tokens.zig`

## Source Changes

Edit only:

- `howl-render/src/libhowl_render.zig`
- `howl-render/src/submission.zig`
- `howl-render/src/ffi/submission.zig`
- `howl-render/src/test/ffi.zig`

Do not edit any other file unless the build exposes a direct `submission.zig` import missed by this slice.
If that happens, stop and report the exact import before broadening scope.

## Required Shape

- Move `howl-render/src/submission.zig` to `howl-render/src/ffi/submission.zig`.
- Update `libhowl_render.zig` to import the moved translator from `ffi/submission.zig`.
- Update `test/ffi.zig` to import the moved translator.
- Remove the old root `howl-render/src/submission.zig`; do not leave a compatibility wrapper.
- Keep `libhowl_render.zig` as an export table only.

## Non-Goals

- Do not move `submit_result.zig` in this slice.
- Do not move already-moved translators again.
- Do not rename any `HowlRender*` C type.
- Do not rename any `howl_render_*` symbol.
- Do not change `howl-render/include/howl_render.h`.
- Do not change prepared publish, submit decision, submit handle, token validation, same-token comparison, or submit-result semantics.
- Do not add compatibility aliases.

## Invariants

- The public root curates exports only.
- Submission exported symbols keep the same names.
- `takeSubmitDecision` still zeroes token output before owner access.
- `takeSubmitHandle` still nulls handle output before owner access.
- Submit result output still starts as `failedSubmitResult` when output is present.
- `preparedSurfaceTokenOut`, `preparedSurfaceTokenIn`, `samePreparedSurfaceToken`, and `damageKindIn` move unchanged except import path fixes.
- `submit_result.submitExecutionIn`, `submit_result.submitResultOut`, and `submit_result.failedSubmitResult` remain the submit-result boundary.
- The translator calls session/prepared owners; it does not own prepared or submitted state.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg '@import\("submission\.zig"\)' howl-render/src`
- `rg 'howl_render_text_session_publish_prepared|howl_render_text_session_publish_prepared_handle|howl_render_text_session_take_submit_decision|howl_render_text_session_take_submit_handle|howl_render_text_session_accept_submitted|howl_render_prepared_surface_submit|howl_render_prepared_surface_submit_handle|preparedSurfaceTokenOut|preparedSurfaceTokenIn|samePreparedSurfaceToken' howl-render/src`
- `rg 'const .* = @import\("(vt_surface|prepared_surface|prepare_request|text_session|surface_geometry|work_state|submission)\.zig"\)' howl-render/src/libhowl_render.zig`

Expected results:

- The first gate prints no stale root import.
- The second gate supports manual review that submission C translation moved only.
- The third gate prints nothing; all render ABI translators should now import from `ffi/`.

## Stop Conditions

- Stop if moving `submission.zig` requires changing behavior.
- Stop if the new lane needs a compatibility wrapper at the old root path.
- Stop if an import cycle appears between `ffi/submission.zig`, `ffi/prepared_surface.zig`, and `submit_result.zig`.
- Stop if token conversion or submit-result failure/defaulting cannot move unchanged with the translator.
