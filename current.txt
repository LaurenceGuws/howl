# Current Slice: Delete Host Terminal ABI Wrapper Buckets

Owner: workspace root.

Purpose:

- Delete vague host wrapper buckets:
  - `howl-linux-host/src/terminal/render/abi.zig`
  - `howl-linux-host/src/terminal/vt/abi.zig`
- Use the imported C ABI directly from `terminal/c.zig` at the true owner files.
- Move hidden helper behavior to the smallest existing true owner instead of replacing
  wrappers with new buckets.
- No compatibility aliases, no shim files, no new `abi`, `api`, `wrapper`, `manager`, or
  `types` owners.

## Source Changes

Delete:

- `howl-linux-host/src/terminal/render/abi.zig`
- `howl-linux-host/src/terminal/vt/abi.zig`

Render wrapper behavior:

- Move render text-session initialization/config helpers from `render/abi.zig` into
  `terminal/context.zig`, where terminal initialization already owns resolved fonts,
  launch config, text session creation, PTY init, and VT init.
- Move surface-layout derivation/commit logic from `render/abi.zig` into
  `terminal/render/surface_layout.zig`, where host render surface geometry is owned.
- Move `pixelToCol` and `pixelToRow` logic into `terminal/context.zig` or another exact
  call-site owner, not a replacement wrapper.
- Move font-size render call from `render/abi.zig` into `terminal/render/font_size.zig`.
- Use `c.HowlRenderHostSurface`, `retained.SurfaceLayout`, and
  `retained.SurfaceLayoutSync` directly instead of aliases from `render/abi.zig`.

VT wrapper behavior:

- Move VT init/deinit with default history capacity into `terminal/context.zig`, where VT
  lifecycle is owned.
- Move VT call status helpers needed by source publication into `terminal/vt/surface.zig`.
- Move VT input type aliases/status handling into `terminal/vt/input.zig` or replace with
  direct C ABI types.
- Move VT feed status check in `terminal/pty/pump.zig` to direct comparison with
  `c.HOWL_VT_CALL_OK`.
- Keep existing runtime helpers in `terminal/vt/retained.zig`; do not route through a
  wrapper.

## Required Call-Site Updates

Update imports and references in:

- `howl-linux-host/src/terminal/context.zig`
- `howl-linux-host/src/terminal/render/surface_layout.zig`
- `howl-linux-host/src/terminal/render/font_size.zig`
- `howl-linux-host/src/terminal/vt/surface.zig`
- `howl-linux-host/src/terminal/vt/input.zig`
- `howl-linux-host/src/terminal/pty/pump.zig`

## Invariants

- Host still uses the beautiful C ABI from `terminal/c.zig` directly.
- Terminal context owns terminal lifecycle: render text session, VT handle, PTY handle,
  startup and deinit ordering.
- `render/surface_layout.zig` owns host render surface layout derivation and resize
  side effects.
- `render/font_size.zig` owns font-size mutation calls.
- `vt/input.zig` owns host-input to VT-input translation and encoding.
- `vt/surface.zig` owns VT visible source publication and its status assertions.
- Behavior is unchanged.
- No old wrapper imports remain.
- No shim files remain at deleted paths.
- No `types.zig`.
- No new `api`, `abi`, `wrapper`, `manager`, `controller`, `runtime`, `flow`, or `queue`
  owner.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

No files:

- `howl-linux-host/src/terminal/render/abi.zig`
- `howl-linux-host/src/terminal/vt/abi.zig`

No matches under `howl-linux-host/src`:

- `render/abi.zig`
- `vt/abi.zig`
- `render_api`
- `vt_api`
- `vt_abi`
- `const api = @import("abi.zig")`

No new files matching:

- `**/types.zig`
- `**/api.zig`
- `**/abi.zig`

Expected direct C ABI usage remains through:

- `const c = @import("../c.zig").c`
- `const terminal_c = @import("c.zig").c`
