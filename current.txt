# Current Slice: Classify Render Text Pipeline Taxonomy

Owner: workspace root.

Purpose:

- Classify `howl-render/src/text/pipeline.zig` before any rename or split.
- Decide whether the file should be renamed whole or split into smaller true owners.
- Select exactly one bounded implementation move for the next slice.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/roadmap.md` Phase 2 render taxonomy guidance
- `research/2026-05-30-hygiene-audit/synthesis.md` render taxonomy finding
- `howl-render/design.md`
- `libs.yaml`
- `howl-render/src/text/pipeline.zig`
- all direct importers of `text/pipeline.zig`

## Source Changes

Edit only:

- new `research/2026-05-30-hygiene-audit/render-text-pipeline-taxonomy.md`

## Required Content

- Inventory every public symbol in `howl-render/src/text/pipeline.zig`.
- For each symbol, classify the true owner category, such as resolve, prepare counters, shape operation, raster operation, build/group request-output, or other.
- List every importer and exactly which symbols it uses.
- Decide whether the next implementation should be a whole-file rename or a split.
- If split, select one first file move with exact files, symbols, import repairs, non-goals, invariants, verification, grep gates, and stop conditions.
- Explain why the selected first move is bounded.

## Non-Goals

- Do not edit render code.
- Do not move or rename `pipeline.zig` in this slice.
- Do not change public ABI headers or symbols.
- Do not invent `types.zig`, `api.zig`, `manager`, `controller`, `engine`, `runtime`, or another vague bucket.
- Do not claim a whole-file rename is safe unless every public symbol has the same true owner.

## Verification

From `/home/home/personal/projects/howl`:

- `git diff --check`

## Grep Gates

- `rg 'ResolveStage|ResolveRequest|ResolveHit|ResolveMiss|ResolveResult|ResolveCounters|ResolveObservability|TextPrepareCounters|BuildRunsRequest|BuildRunsOutput|GroupGlyphsRequest|GroupGlyphsOutput|ShapeRequest|ShapeOutput|RasterizeRequest|RasterizeOutput|ShapeClustersFn|RasterizeGlyphFn|ResolveFallbackFaceFn|ShapeClustersOp|RasterizeGlyphOp|ResolveFallbackFaceOp' research/2026-05-30-hygiene-audit/render-text-pipeline-taxonomy.md`
- `rg 'session/text\.zig|frame_preparer\.zig|direct_normal\.zig|font/provider\.zig|font/resolver\.zig|font/ft_hb/support\.zig|font/ft_hb/provider\.zig|prepared/surface\.zig|prepared/submit_result\.zig' research/2026-05-30-hygiene-audit/render-text-pipeline-taxonomy.md`
- `rg 'types\.zig|api\.zig|manager|controller|engine|runtime' research/2026-05-30-hygiene-audit/render-text-pipeline-taxonomy.md`

Expected results:

- The first two gates prove the document covers all known symbols and importers.
- The banned-vocabulary gate must show rejected context only, if anything.

## Stop Conditions

- Stop if symbol ownership cannot be classified from current source.
- Stop if importers use symbols in a way that makes the first implementation move ambiguous.
