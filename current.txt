# Current Slice: Linux Host V0 Fill-Only Present Path

Owner: `howl-linux-host` terminal submit/present boundary.

Purpose:

- Add the first visible V0 present path without deleting full RGBA.
- Use V0 directly only when a prepared frame contains clear/fill commands only.
- Keep mixed text/sprite frames on the existing RGBA path until sprite/glyph drawing is
  promoted.

## Exact Change Scope

Allowed files:

- `howl-linux-host/src/terminal/context.zig`
- `howl-linux-host/src/window/term_texture.zig`
- `howl-linux-host/src/window/present.zig` only if direct framebuffer drawing is needed
- `howl-linux-host/src/window/layout.zig` only if present frame metadata is needed
- `howl-linux-host/src/app/present.zig` only if present frame metadata is needed
- `research/2026-05-31-howl-render-protocol-sprint.md`

No render ABI changes. No V0 protocol changes. No full-RGBA deletion. No mixed
sprite/glyph V0 presentation.

## Required Shape

- Detect fill-only V0 frames: commands may be clear/fill only, with no glyph spans and
  no sprite resource use.
- Present fill-only frames through V0 consequences instead of full RGBA upload.
- Preserve RGBA fallback for every non-fill-only frame or V0-unavailable frame.
- Keep backend work outside `term.mutex`.
- Diagnostics must report fill-only present counts and fallback counts.

## Required Tests

- Add host tests for fill-only detection if helper is introduced.
- Existing host submit-lock tests must still pass.

## Verification

From `/home/home/personal/projects/howl/howl-linux-host`:

- `zig build test --summary all`
- `zig build -Doptimize=ReleaseFast`
- `git diff --check`

From workspace root:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if visible fill-only drawing requires reordering mixed text/sprite commands.
- Stop if implementing this requires render ABI changes.
