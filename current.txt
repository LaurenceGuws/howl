# Current Slice: Current Frame Publication Override Deletion Accepted

Owner: workspace root.

Sprint scope: graphics deletion.

Status: accepted by reviewer, verified locally, and committed.

Accepted commit:

- `howl-vt` `b3f199a vt: delete frame publication override`

## Accepted Deletion

- Deleted Howl-only current-frame publication override state:
  `current_override_format`, `current_override_width`, `current_override_height`, and
  `current_override_decoded_payload`.
- Deleted `refreshCurrentFramePublication` and every call that existed only to populate that
  duplicate publication payload.
- `imageAt()` and `decodedImageAt()` now publish only the root image decoded payload state.
- Decoded quota/free accounting no longer counts a second current-frame publication copy.
- Tests now assert the expected broken state: selecting frame `2` updates current-frame
  metadata but leaves decoded image publication on root bytes.

## Verification Passed

- `zig build test --summary all` in `howl-vt` (`538/538` tests).
- `zig build test:regression:build --summary all` in `howl-vt`.
- `howl-vt` `git diff --check`.

## Known Broken State

- Selecting or advancing animation frames no longer changes decoded image publication bytes.
- Current-frame animation display remains broken until a source-backed frame/render model is
  designed later.
- Unicode placeholder generated renderable placement behavior remains broken until a
  separately promoted source-backed generated-ref replacement slice.

## Next Gate

No replacement slice is active. Continue deleting or proving remaining Howl-only graphics
ideas before replacement work.
