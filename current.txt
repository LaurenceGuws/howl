# Current Slice: Delete Render Text Pipeline Remainder

Owner: workspace root.

Purpose:

- Delete the remaining unused `howl-render/src/text/pipeline.zig` file after all live symbols moved to true owners.
- Remove the stale `text_flow` owner-map entry.
- Preserve behavior by deleting only unimported Zig-only contracts and their self-test.

## Source Research

Use:

- `research/2026-05-30-hygiene-audit/render-text-pipeline-taxonomy.md`
- current grep proving no direct importers of `pipeline.zig`
- `howl-render/src/text/pipeline.zig`
- `libs.yaml`

## Source Changes

Edit only:

- `howl-render/src/text/pipeline.zig`
- `libs.yaml`

## Required Shape

- Delete `howl-render/src/text/pipeline.zig`.
- Remove `text_flow: howl-render/src/text/pipeline.zig` from `libs.yaml`.
- Do not create a replacement bucket file.

## Non-Goals

- Do not move any more render symbols.
- Do not edit render code besides deleting the unimported file.
- Do not change headers or exported C ABI symbols.
- Do not add compatibility aliases.
- Do not add `types.zig`, `api.zig`, manager, controller, engine, runtime, flow, or pipeline owner files.

## Invariants

- No source file imports `pipeline.zig` before deletion.
- Deleted symbols are not used outside the deleted file.
- Build and tests continue to pass.
- Live split owners remain:
  - `text/font/resolve.zig`
  - `text/raster/operation.zig`
  - `text/prepare_counters.zig`

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Grep Gates

- `rg 'pipeline\.zig|text_flow|text_pipeline|pipeline\.' howl-render/src libs.yaml`
- `rg 'BuildRunsRequest|BuildRunsOutput|GroupGlyphsRequest|GroupGlyphsOutput|ShapeRequest|ShapeOutput|ShapeClustersFn|ShapeClustersOp' howl-render/src`
- `rg 'types\.zig|api\.zig|manager|controller|engine|runtime|text_flow|pipeline' howl-render/src/text howl-render/src/prepared howl-render/src/session libs.yaml`

Expected results:

- The first two gates print nothing.
- The third gate prints nothing except unrelated pre-existing terms if explicitly reviewed; it must not show `text_flow` or `pipeline` owner usage.

## Stop Conditions

- Stop if any file still imports `pipeline.zig`.
- Stop if any deleted symbol is referenced outside `pipeline.zig`.
- Stop if deleting the file requires adding a replacement bucket.
