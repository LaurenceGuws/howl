# Current Slice: Remaining Unicode Placeholder Publication Proof

Owner: workspace root.

Sprint scope: graphics.

## Goal

Prove the remaining Kitty Unicode placeholder publication behaviors at Howl's VT
generated-placement boundary, without adding persistent generated refs or touching render,
host, or ABI code.

## Source Truth

- `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
- `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
- `utils/dev_references/terminals/kitty/kitty/screen.c`
- `utils/dev_references/terminals/kitty/kitty/graphics.c`
- `utils/dev_references/terminals/kitty/kitty/graphics.h`
- `utils/dev_references/terminals/kitty/kitty_tests/graphics.py`
- `graphics-machinery-scratchpad.md` section `Remaining Unicode placeholder publication research`

## Target Files

- `howl-vt/src/test/terminal_graphics.zig`
- `howl-vt/src/kitty/graphics.zig` only if tests expose a direct VT bug
- `howl-vt/src/terminal.zig` only if tests expose a direct VT publication/dirty bug

## Required Behavior

- Tests-first/proof-only unless a real mismatch is exposed.
- Third combining char must select image-id high byte and publish only matching generated
  placements.
- Multiple virtual placements for one image must resolve by underline placement id and
  publish distinct generated placements with expected source geometry/order.
- Scroll-region `index`/`reverse_index` over placeholder rows must publish surviving or
  moved generated placements from VT cell truth.
- Public `placeholder_run_count` remains `0`; proof-only APIs may be used only for
  internal assembly assertions.
- Existing generated placement dirty/publication tests remain green.
- Do not change render, host, ABI, persistent generated refs, virtual-placement ABI
  cleanup, storage model, PNG/media, animation, or unrelated parser behavior.

## Required Tests

- Third-combining-char high-byte image id publication proof.
- Multiple virtual placements for one image and underline placement id publication proof.
- Scroll-region `index` over placeholder rows publication proof.
- Scroll-region `reverse_index` over placeholder rows publication proof.
- Public graphics meta still reports `placeholder_run_count == 0`.

## Verification Required

- `zig build test --summary all` in `howl-vt`
- `zig build test:regression:build --summary all` in `howl-vt`
- root `zig build`
- root `git diff --check`
- `howl-vt` `git diff --check`

Stop condition:

- If correctness requires render-side placeholder parsing, ABI changes, persistent
  generated refs, host viewport/render behavior, or broad terminal-screen redesign, stop
  and report the smaller source-backed slice needed.
