# Current Slice: Graphics Placeholder Access Refresh

Owner: howl-vt.

Sprint scope: graphics.

## Goal

Make resolved Unicode placeholder publication count as image access for VT quota ordering,
matching Kitty's `grman_put_cell_image` access-time consequence.

## Required Work

- Edit only `howl-vt/src/kitty/graphics.zig`, `howl-vt/src/terminal.zig`, and
  `howl-vt/src/test/terminal_graphics.zig` unless a stop condition is hit.
- Add a VT-owned method that scans resolved placeholder runs and marks existing referenced
  images as accessed.
- Call that method from `Terminal.graphicsMeta()` before returning graphics counts.
- Missing image ids in placeholder cells must not fabricate images or fail publication.
- Keep public ABI layout unchanged.
- Keep render and host unchanged.
- Do not add retained generated-placement storage in this slice.

## Tests Required

- A placeholder-resolved image becomes newer for decoded quota eviction.
- Missing-image placeholder runs report zero generated placements/runs and do not crash.
- Existing placeholder-run publication tests remain green.

## Stop Conditions

- Stop if this requires render or host changes.
- Stop if this requires public ABI changes.
- Stop if retained generated-placement storage becomes necessary.
- Stop if generated placement geometry changes become necessary.

## Verification

- `zig build test --summary all` in `howl-vt`.
- `zig build test:regression:build --summary all` in `howl-vt`.
- Root `zig build --summary all`.
- Root and `howl-vt` `git diff --check`.
