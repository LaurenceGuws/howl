# Current Slice: Render Protocol V0 ABI Skeleton

Owner: `howl-render` C ABI contract.

Sprint scratchpad and contract:

- `research/2026-05-31-howl-render-protocol-sprint.md`
- `docs/render-protocol-v0.md`

Purpose:

- Add V0 protocol constants and C ABI struct declarations only.
- Add compile-time Zig layout/offset tests proving the ABI skeleton matches
  internal mirror structs.
- Do not emit protocol frames, do not change render behavior, and do not change
  host behavior.

## Source Order (Mandatory)

Read in order before editing:

1. `utils/dev_references/zig_maturity/tigerbeetle/docs/TIGER_STYLE.md`
2. `utils/dev_references/zig_maturity/tigerbeetle/docs/ARCHITECTURE.md`
3. `docs/render-protocol-v0.md`
4. `research/2026-05-31-howl-render-protocol-sprint.md`
5. `howl-render/include/howl_render.h`
6. `howl-render/src/ffi.zig`
7. `howl-render/src/ffi/vt_surface.zig`

## Exact Change Scope

Allowed files:

- `howl-render/include/howl_render.h`
- new `howl-render/src/ffi/protocol_v0.zig`
- `howl-render/src/libhowl_render.zig` only if needed to force compile-time
  layout checks through existing imports
- `research/2026-05-31-howl-render-protocol-sprint.md` to record accepted facts

No host code.
No behavior changes.
No functions exported for protocol production yet.
No prepared-surface deletion.
No software realizer.
No protocol emission.
No performance claims.

## Required Header Shape

Add public constants from `docs/render-protocol-v0.md` exactly:

- `HOWL_RENDER_PROTOCOL_V0_VERSION = 0`
- `HOWL_RENDER_V0_FRAMES_IN_FLIGHT_MAX = 2`
- `HOWL_RENDER_V0_SNAPSHOTS_IN_FLIGHT_MAX = 2`
- `HOWL_RENDER_V0_DAMAGE_ITEMS_MAX = 1024`
- `HOWL_RENDER_V0_UPLOADS_MAX = 256`
- `HOWL_RENDER_V0_COMMANDS_MAX = 8192`
- `HOWL_RENDER_V0_GLYPHS_PER_RUN_MAX = 256`
- `HOWL_RENDER_V0_UPLOAD_BYTES_MAX = 8388608`
- `HOWL_RENDER_V0_ATLAS_PAGES_MAX = 64`
- `HOWL_RENDER_V0_RESOURCES_MAX = 4096`
- `HOWL_RENDER_V0_CREATES_MAX = 256`
- `HOWL_RENDER_V0_RETIRES_MAX = 256`
- `HOWL_RENDER_V0_HOST_ACKS_MAX = 256`

Add C structs exactly matching the draft names and fields:

- `HowlRenderV0Token`
- `HowlRenderV0Rect`
- `HowlRenderV0DamageItem`
- `HowlRenderV0DamageSpan`
- `HowlRenderV0ResourceId`
- `HowlRenderV0Upload`
- `HowlRenderV0UploadSpan`
- `HowlRenderV0Create`
- `HowlRenderV0CreateSpan`
- `HowlRenderV0GlyphRef`
- `HowlRenderV0GlyphRunSpan`
- `HowlRenderV0Command`
- `HowlRenderV0CommandSpan`
- `HowlRenderV0Retire`
- `HowlRenderV0RetireSpan`
- `HowlRenderV0HostAck`
- `HowlRenderV0HostAckSpan`
- `HowlRenderV0Frame`

Do not add `HowlRenderV0HostAckSpan` to `HowlRenderV0Frame`.

## Required Zig Layout Proof

- Create internal extern mirror structs in `howl-render/src/ffi/protocol_v0.zig`.
- Add a `comptime` block that asserts:
  - every constant equals the contract value;
  - every mirror struct has the same size and alignment as the C imported struct;
  - every field offset matches.
- Reuse the direct assertion style from `ffi/vt_surface.zig`; do not add generic
  testing infrastructure unless required.

## Verification

From `/home/home/personal/projects/howl`:

- `zig build check`
- `zig build test`
- `git diff --check`

## Stop Conditions

- Stop if the C header cannot represent the contract without ambiguous padding.
- Stop if any constant value differs from `docs/render-protocol-v0.md`.
- Stop if host ack appears inside `HowlRenderV0Frame`.
- Stop if product behavior must change to make the skeleton compile.
- Stop if a protocol export function seems necessary; that belongs to a later
  slice.
