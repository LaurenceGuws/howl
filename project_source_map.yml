sources:
  build.zig: development-only child-build aliases plus root audit and protocol validation
  build.zig.zon: howl_workspace identity and the source boundary required by those operations
  consumer-vt: isolated external package proof that imports only the standalone howl-vt module
  howl-*/build.zig: child-owned module, proof, native dependency, executable, and command construction
  howl-*/build.zig.zon: child-owned package identity, direct sibling dependencies, and source boundary
  howl-vt/src: current terminal model implementation preserved through source-shape simplification
  protocol_coverage.yml: >-
    root-owned pinned libvterm, iTerm2, and Kitty evidence with normalized support, disposition, owner, and proof facts
  protocol_coverage.nu: >-
    root-owned catalogue normalization, validation, structured queries, and compact human views
  howl-text/src: current text loading, shaping, and rasterization implementation
  howl-frame/src/howl_frame.zig: >-
    geometry-sized immutable terminal-local frames, transactional resize storage, cumulative cell damage,
    bounded publication slots, and exact renderer acknowledgement
  howl-render/src/howl_render.zig: >-
    shared font ownership, bounded cross-terminal glyph-mask cache, pane/frame validation, and damage-aware draw preparation
  howl-window/src: >-
    sole Wayland/xkb host loop, workspace-backed terminal lifecycle and tab/pane commands, keyboard repeat,
    stable pane wake, label/tab and focused-pointer routing, transactional PTY geometry, one EGL/GLES render thread,
    shared label/pane text and texture representation, and live host route
  howl-window/src/labels.zig: >-
    platform-independent bounded tab-label cells, terminal-availability presentation facts, deterministic truncation,
    and exact cell/pixel hit geometry without workspace mutation policy
  howl-window/src/viewport.zig: >-
    pane-local follow and retained-row anchor reconciliation plus exact bounded scrollbar paint/hit geometry
  howl-window/src/workspace.zig: >-
    platform-independent bounded session, tab identity/order/names, split-pane topology and cell extents, direct and
    directional focus, exact pane retirement, transactional outer resize, and hostile-state invariant validation
  howl-pty/src/howl_pty.zig: >-
    Linux PTY creation, copied launch values, process-group lifecycle, bounded transfer outcomes, resize,
    concurrent cancellation, exact native errors, and cleanup
  howl-control/src/howl_control.zig: >-
    curated PTY and terminal-model lifecycle, immutable frame publication and recovery, optional local endpoint server,
    shared mutation admission, primitive operations, and exact shutdown ownership
  howl-control/src/client.zig: >-
    terminal identity, endpoint facts, private protocol codec, Linux socket mechanics, and one-shot client
  howl-control/src/test.zig: live PTY, immutable frame publication, and semantic control proofs
  howl-window/vendor/xdg-shell: generated Wayland shell protocol source consumed by the native host
  tools/audit_source.sh: maintained non-vendor Zig source policy and reviewed construct uses
  howl-window/build/probe_enabled.zig: temporary bounded development event worker and Linux resource sampling
  howl-window/build/probe_disabled.zig: compile-time disabled normal-window probe boundary
  howl-window/build/probe_scenario.zig: temporary deterministic PTY-to-render measurement owned by howl-window
