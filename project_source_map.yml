accepted:
  .zigversion: exact Zig compiler pin resolved locally through ignored .zig/zig
  protocol_coverage.yml: pinned protocol evidence and normalized ownership facts
  protocol_coverage.nu: catalogue normalization, validation, and query commands
  tools/audit_source.sh: maintained-source audit
  tools/source_audit.allow: reviewed sensitive-source sites
  build.zig: development-only aliases that invoke surviving child build graphs
  build.zig.zon: workspace identity and surviving child source paths
  howl-vt/build.zig: VT package build and proof commands
  howl-vt/build.zig.zon: VT package identity and source boundary
  howl-vt/src: terminal parser, semantics, state, input, replies, consequences, and borrowed semantic views
  howl-vt/src/graphics.zig: bounded terminal image plane, animation frames, placements, deletion, and lifetime
  howl-vt/src/sixel.zig: bounded Sixel measurement and RGBA decoding into the common terminal image plane
  howl-vt/test: external VT behavior proofs
  howl-vt/test_unit.zig: package-owned VT unit proofs
  howl-vt/simulation: deterministic VT simulations
  howl-vt/simulation_main.zig: simulation entry point
  howl-vt/benchmark: VT benchmark evidence
  howl-vt/benchmark_m7_baseline.zig: benchmark comparison entry point
  howl-pty/build.zig: PTY package build, coherent translated Linux headers, and proofs
  howl-pty/build.zig.zon: PTY package identity and source boundary
  howl-pty/src/howl_pty.zig: Linux PTY transport, foreground termios signals, and child-process lifecycle
  howl-render/build.zig: independently selected capability graphs and native-text-only translated headers
  howl-render/build.zig.zon: render package identity, owned assets, and licenses
  howl-render/src/root*.zig: curated roots for selected render capabilities
  howl-render/src/native_text.zig: native font loading, shaping, metrics, and alpha rasterization
  howl-render/src/generated*.zig: generated terminal-glyph classification, geometry, and rasterization
  howl-render/src/terminal_projection.zig: stateless VT cell semantic projection into caller buffers
  howl-render/src/image_projection.zig: stateless VT image-plane projection into caller buffers
  howl-render/src/terminal_test.zig: terminal projection bounds, sparse damage, cursor, and rollback proofs
  howl-render/src/image_projection_test.zig: image projection identity, placement, capacity, and alias proofs
  howl-render/src/chrome.zig: caller-neutral tab, pane-frame, label, and scrollbar projection
  howl-render/src/chrome_test.zig: deterministic chrome geometry, ordering, capacity, and alias proofs
  howl-render/src/terminal_text*.zig: one-run text preparation, raster identity, and native decoration metrics
  howl-render/src/terminal_text_test.zig: selected native and generated run preparation proofs
  howl-render/src/*_test.zig: selected render capability proofs
  howl-render/testdata: deterministic render test fonts
  howl-render/LICENSES: render-owned asset and native-library provenance
  howl-host/build.zig: first-party executable build, install, run, proof, and distinct translated native-header ownership
  howl-host/build.zig.zon: host package identity and direct standalone dependencies
  howl-host/manual-fixtures: bounded compositor-visible color-ring receipt
  howl-host/src/main.zig: process roots, Window and Render thread construction, joining, and final failure
  howl-host/src/shared.zig: bounded copied cross-thread facts and directional eventfd ownership
  howl-host/src/window.zig: Wayland discovery, dispatch, DMA-BUF wrappers, frame pacing, and presentation
  howl-host/src/renderer.zig: exclusive Vulkan mutation, DRM synchronization, generation-tagged rings, and release observation
  howl-host/src/chrome_state.zig: Renderer-owned Topology with bounded stable tab/pane identities, transactional geometry, focus, and chrome projection composition
  howl-host/src/chrome_draw.zig: bounded native-label shaping, shared alpha-atlas residency, and Vulkan-ready chrome vertices, indices, and draw facts
  howl-host/src/gpu_chrome.zig: Host-owned solid and alpha-text Vulkan pipelines, shared GPU atlas, mapped bounded staging, and per-slot framebuffer ownership
  howl-host/shaders: maintained GLSL sources and compilation provenance for the two chrome pipelines
  howl-host/src/shaders: checked SPIR-V consumed directly by the Vulkan owner
  howl-host/vendor/vulkan/vulkan.zig: maintained minimal Vulkan 1.4.357 Zig ABI used by the host
  howl-host/vendor/xdg-shell: Wayland-protocols 1.49 generated xdg-shell client ABI
  howl-host/vendor/linux-dmabuf: Wayland-protocols 1.49 generated linux-dmabuf-v1 client ABI
  howl-host/vendor/linux-drm-syncobj: Wayland-protocols 1.49 generated linux-drm-syncobj-v1 client ABI
  howl-host/LICENSES: exact Vulkan and Wayland generated-source provenance and license facts
