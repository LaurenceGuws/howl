accepted:
  .zigversion: exact Zig compiler pin
  build.zig: development-only curator for local core checks and tests
  build.zig.zon: local core workspace source boundary including tracked howl-text
  howl-vt: terminal semantics, state, protocol input/output, history, graphics, and conformance proofs
  howl-session: opaque canonical PTY/VT session owner and observer-independent lifecycle proofs
  howl-pty: Linux PTY and child-process lifecycle
  howl-text: tracked module owning native font metrics, fallback, shaping, source-cluster identity, glyph lookup, and bounded natural alpha rasterization
  protocol_coverage.yml: pinned terminal-protocol census
  protocol_coverage.nu: protocol query and validation surface
  tools/audit_source.sh: maintained-source structural audit
  tools/source_audit.allow: reviewed sensitive Zig sites
  tools/json_logger.zig: reusable bounded JSONL evidence writer

experimental:
  howl-client: reusable UI-agnostic native session client engine: portable local connection/framing, interaction state, canonical semantic actions including mouse, one lossless rich text_v1 decoder, compact semantic snapshots, and an explicit-lifetime one-allocation coarse semantic view
  howl-cli: native human/agent session client under active proof; compact semantic observation plus canonical input/control, not shell execution or remote transport
  howl-render: backend-neutral rendering experiments consuming howl-text and howl-client.view; terminal Content emits complete Canvas ProducerUpdate state through private caller-bounded shape/atlas caches, and Composer-derived Canvas frames are the Android/iOS-pressure-proven semantic presentation seam; no stable mobile ABI is accepted
  howl-flutter: Flutter platform host for the native Howl client: platform input/viewport/history UX plus an app-private version-locked native observer/control binding and batched final-Canvas backend; Android live path accepted, Linux bundled host proven, iOS native-host semantics pressure-proven and explicit numeric IPv4 client reachability under active proof; iOS owns no PTY/userland; not part of the core gate
  howl-vk: Vulkan experiments; not part of the core gate
  howl-wayland: Wayland experiments; not part of the core gate
  howl-web: maintained experimental WebAssembly canary alongside Flutter; bounded asynchronous byte assembly, shared rich.decodeFrames, exact zero-import wire and Canvas gates, and a disposable live PTY echo test; full text/glyph rendering and authenticated browser delivery are not yet accepted; outside the root core gate
