# Cursor Kitty Amended Slices

## Receipt Header

- Artifact owner: `research/2026-06-15-cursor-kitty-amended-slices.md`
- Role: reviewer amendment from hostile audit findings
- Session id: no internal session identifier exposed beyond this task session
- Basis:
  - `research/2026-06-15-cursor-kitty-line-map.md`
  - reviewer judgment that the original cursor sprint missed both stated goals:
    1. remove Howl's bolted-on cursor code
    2. port Kitty's cursor code to its entirety

## Sprint Verdict Basis

- The completed sprint achieved partial Kitty-shaped behavior.
- The completed sprint did not remove all bolted-on Howl cursor code.
- The completed sprint did not port Kitty's cursor code in its entirety.
- The amended sprint below is the execution shape the cursor work should have had after those findings were known.

## Remaining NO KITTY SOURCE Code That Must Be Deleted Or Replaced

- `howl-vt/src/route.zig:138-168,202-217`
- `howl-vt/src/report.zig:228-260`
- `howl-render/src/vt_publication/prepare_queue.zig:22-198,288-305`
- `howl-render/src/vt_publication/source_slot.zig:152-306,461-494,495-525,545-559`
- `howl-render/src/render_session.zig:381-415,499-504,586-666,854-879`
- `howl-linux-host/src/terminal/render_retained.zig:63-93,191-194`
- `howl-linux-host/src/event.zig:52,72,89,194-202,240,255,263,273,631,653,668,684,692-764`
- `howl-vt/simulation/protocol.zig:155-156,387-390,415-416`
- `howl-vt/simulation/scrollback.zig:46-47,257-267,370-371,380-406`

## Ambiguous Zones Needing User Decision

- Multiple cursors are still missing as a full port item, but the earlier sprint marked them non-goal. The user must decide whether the amended sprint now expands to full Kitty multiple-cursor support or records an explicit override against "port Kitty's cursor code to its entirety".
- Kitty's full cursor replay/export shape is still not present as one coherent Howl owner. The user must decide whether the product requires full Kitty replay/export parity or whether the C ABI/render surface boundary intentionally replaces part of that shape with a different exported consequence model.
- Some Howl-owned render/host orchestration may be unavoidable because Howl is embeddable and C-ABI-first. The user must decide whether "port Kitty's cursor code to its entirety" means behavior-only parity at those seams or a stricter structural deletion of as much Howl-owned cursor orchestration as possible.

## Ordered Slice List

### Slice 1: Delete VT Cursor Plumbing That Has No Kitty Owner

- Slice type: deletion-driven
- Goal:
  - remove Howl-only VT cursor routing/plumbing that survived the original sprint
  - delete cursor report assembly with no Kitty source match
- Exact files:
  - `howl-vt/src/route.zig`
  - `howl-vt/src/report.zig`
  - `howl-vt/test/unit/csi_mapping_test.zig`
  - `howl-vt/test/unit/terminal_modes_test.zig`
  - `howl-vt/test/unit/terminal_surface_test.zig`
  - `howl-vt/test/unit/terminal_cursor_test.zig`
  - `howl-vt/test/unit/terminal_snapshot_test.zig`
- Required delete/replace focus:
  - delete or replace `howl-vt/src/route.zig:138-168,202-217`
  - delete or replace `howl-vt/src/report.zig:228-260`
- Exact proof roots:
  - `howl-vt/test/unit/csi_mapping_test.zig`
  - `howl-vt/test/unit/terminal_modes_test.zig`
  - `howl-vt/test/unit/terminal_surface_test.zig`
  - `howl-vt/test/unit/terminal_cursor_test.zig`
  - `howl-vt/test/unit/terminal_snapshot_test.zig`
  - `howl-vt/src/report.zig` inline tests
- Stop conditions:
  - stop if cursor routing remains in an unbacked Howl dispatch spine rather than a Kitty-pressured owner split
  - stop if `DECCIR` cursor report behavior is preserved without an explicit user override against Kitty
  - stop if VT proof roots still mention deleted cursor report behavior without replacement proof

### Slice 2: Replace The Full Retained Render Cursor Spine

- Slice type: deletion-driven owner-boundary slice
- Slice shape decision:
  - slice 2 stays one slice
  - it must not be split into smaller cursor-only edits because the targeted `NO KITTY SOURCE` cursor owners are embedded in one coupled retained render source/prepare/submit spine and one coupled host adapter seam
- Revised goal:
  - replace the retained render source/prepare/submit spine that still carries Howl-invented cursor ownership with one explicit retained-session owner
  - reduce the cursor path to the smallest retained-render and C-ABI seams actually forced by the product boundary
  - delete or replace unbacked cursor owners only in a shape that leaves the retained render contracts executable end to end
- Why the old slice-2 boundary was not executable:
  - the old file list stopped at cursor-adjacent leaf files, but the real owner boundary runs through `render_session` into the retained C entrypoints, prepared-handle/submitted-surface owners, exported render ABI surface, and the host `term.render` contract
  - the coder could not honestly delete or replace `howl-render/src/vt_publication/prepare_queue.zig`, `source_slot.zig`, `render_session.zig`, and `howl-linux-host/src/terminal/render_retained.zig` without also editing the retained prepare/submit consumers that instantiate, expose, and verify that spine
  - keeping the old narrow boundary would have forced fake progress: wrapper preservation around the same retained cursor machinery instead of a true owner-boundary replacement
- Exact replacement owner map that must survive after this slice:
  - `howl-render/src/render_session.zig` survives as the only retained-session owner.
  - `howl-render/src/vt_publication/publication.zig` survives as copy/validation owner only.
  - `howl-render/src/submitted_surface.zig` survives as submitted-base validator only.
  - `howl-render/src/surface/handle.zig` survives as the single prepared-handle object owner only.
  - `howl-render/src/surface/prepared_surface.zig` survives as immutable prepared-surface payload owner only.
  - `howl-render/src/vt_publication/prepare_queue.zig` is delete-only in this slice and must not survive.
  - `howl-render/src/vt_publication/source_slot.zig` is delete-only in this slice and must not survive.
  - `howl-render/src/c/prepare_request.zig`, `howl-render/src/c/prepared_surface.zig`, `howl-render/src/c/submission.zig`, `howl-render/src/c/work_state.zig`, and `howl-render/src/c/text_session.zig` survive as FFI translators only.
- Exact surviving owner fields in `howl-render/src/render_session.zig`:
  - `latest_source: ?source_publication.PublicationSource`
  - `latest_source_dirty_epoch: u64`
  - `prepare_request: ?tokens.RenderRequest`
  - `prepared_candidate: ?*prepared_handle.PreparedHandle`
  - `submitted: submitted_surface.SubmittedSurface`
  - `cursor_focused: bool`
  - `cursor_opacity: u8`
  - `text_blink_opacity: u8`
  - `cursor_effective_shape: source_abi.SourceCursorShape`
  - `cursor_color: source_abi.SourceColor`
  - `cursor_text_color: source_abi.SourceColor`
  - `cursor_trail_color: source_abi.SourceColor`
  - `cursor_beam_thickness: f32`
  - `cursor_underline_thickness: f32`
  - `cursor_trail_count: u16`
  - `cursor_trail_rects: [source_abi.max_cursor_trail_rects]source_abi.SourceCursorTrailRect`
- Exact surviving method surface in `howl-render/src/render_session.zig`:
  - `nextSourceDirtyEpoch() u64`
  - `setHostCursorCadence(cadence: HostCursorCadence) bool`
  - `syncGeometry(layout: geometry_contract.Geometry) !geometry_contract.GeometryResponse`
  - `ingestPublishedSource(result: c.HowlVtSurfaceResult) !?tokens.RenderRequest`
  - `prepareHandle(token: tokens.SnapshotToken) !*prepared_handle.PreparedHandle`
  - `takeSubmitHandle() SubmitHandleDecision`
  - `submitPreparedHandle(prepared: *prepared_handle.PreparedHandle, execution: TextSession.SubmitExecution) SubmitPreparedResult`
  - `workState() SessionWorkState`
- Exact surviving owner fields and methods for every other retained owner in this slice:
  - `howl-render/src/vt_publication/publication.zig`
    - surviving role: copy/validation owner only
    - surviving methods:
      - `validatePublicationSourceBoundary(source: PublicationSource) !void`
      - `ownedSourceFromSurfaceResult(allocator: std.mem.Allocator, result: anytype, cursor_phase_visible: bool) !PublicationSource`
      - `testSourceFromSnapshot(...) !PublicationSource`
      - `ownedTestSource(...) !PublicationSource`
    - surviving owner fields on `PublicationSource` used by slice 2:
      - `cols`
      - `rows`
      - `history_count`
      - `scroll_row`
      - `snapshot_seq`
      - `dirty_epoch`
      - `is_alternate_screen`
      - `cells`
      - `cursor`
      - `extra_cursor_count`
      - `extra_cursors`
      - `cursor_trail_count`
      - `cursor_trail_rects`
      - `colors`
      - `selection`
      - `cursor_phase_visible`
      - `dirty_rows`
      - `dirty_cols_start`
      - `dirty_cols_end`
  - `howl-render/src/submitted_surface.zig`
    - surviving role: submitted-base validator only
    - surviving fields:
      - `submitted_token: ?tokens.SubmittedSurfaceToken`
    - surviving methods:
      - `acceptSubmitted(submitted: tokens.SubmittedSurfaceToken) void`
      - `submittedToken() ?tokens.SnapshotToken`
      - `isStalePrepared(latest_prepare: ?tokens.SnapshotToken, prepared: tokens.SnapshotToken) bool`
      - `validatePrepared(prepared: tokens.PreparedSurfaceToken) Validation`
      - `workState() SubmittedWorkState`
  - `howl-render/src/surface/handle.zig`
    - surviving role: single prepared-handle object owner
    - surviving fields on `PreparedHandle`:
      - `owner: *render_session.TextSessionOwner`
      - `prepared: surface_prepared.PreparedSurface`
      - `state`
    - surviving methods:
      - `create(owner: *render_session.TextSessionOwner, prepared: *surface_prepared.PreparedSurface) !*PreparedHandle`
      - `fromHandle(handle: anytype) ?*PreparedHandle`
      - `belongsToSession(owner: *render_session.TextSessionOwner) bool`
      - `isLive() bool`
      - `preparedSurfaceToken() tokens.PreparedSurfaceToken`
      - `info() surface_prepared.PreparedInfo`
      - `renderSurface() ?*const c.HowlRenderSurface`
      - `release() void`
      - `consume() void`
      - `destroy() void`
  - `howl-render/src/surface/prepared_surface.zig`
    - surviving role: immutable prepared-surface payload owner
    - surviving fields on prepared payload/info:
      - `snapshot_seq`
      - `dirty_epoch`
      - `geometry_epoch`
      - `required_base_seq`
      - `render_px`
      - `cell_px`
      - `grid`
      - `damage_kind`
    - surviving methods:
      - `preparedSurfaceToken() tokens.PreparedSurfaceToken`
      - `info() PreparedInfo`
      - `renderSurface() ?*const c.HowlRenderSurface`
      - `deinit() void`
  - `howl-render/src/c/prepare_request.zig`
    - surviving role: FFI prepare-request translator only
    - surviving methods:
      - `takePrepareRequest(value: c.HowlRenderTextSessionHandle, vt_surface: ?*const c.HowlVtSurfaceResult, out: ?*c.HowlRenderPrepareRequest) callconv(.c) c_int`
      - `prepareRequestOut(value: tokens.RenderRequest) c.HowlRenderPrepareRequest`
      - `prepareTokenIn(value: c.HowlRenderPrepareRequest) ?tokens.SnapshotToken`
    - exact seam: translator calls only `owner.ingestPublishedSource(...)` and serializes/deserializes `tokens.RenderRequest`
  - `howl-render/src/c/submission.zig`
    - surviving role: FFI submit translator only
    - surviving methods:
      - `takeSubmitHandle(value: c.HowlRenderTextSessionHandle, out: ?*c.HowlRenderRdrSfcHandle) callconv(.c) c_int`
      - `submitHandle(text_session_handle: c.HowlRenderTextSessionHandle, rdr_sfc_handle: c.HowlRenderRdrSfcHandle, execution_in: ?*const c.HowlRenderSubmitExecution, result_out: ?*c.HowlRenderSubmitResult) callconv(.c) c_int`
      - `submitResultOut(value: render_session.SubmitResult) c.HowlRenderSubmitResult`
      - `submitExecutionIn(value: c.HowlRenderSubmitExecution) render_session.TextSession.SubmitExecution`
    - exact seam: translator calls only `owner.takeSubmitHandle()` and `owner.submitPreparedHandle(...)`
  - `howl-render/src/c/work_state.zig`
    - surviving role: FFI work-state translator only
    - surviving methods:
      - `workState(value: c.HowlRenderTextSessionHandle, out: ?*c.HowlRenderSessionWorkState) callconv(.c) c_int`
      - `sessionWorkStateOut(value: render_session.SessionWorkState) c.HowlRenderSessionWorkState`
      - `sessionWorkStateFailure(status: c_int) c.HowlRenderSessionWorkState`
    - exact seam: translator calls only `owner.workState()`
  - `howl-linux-host/src/terminal/render_retained.zig`
    - surviving role: host retained adapter only
    - surviving fields on `State`:
      - `surface_layout`
      - `geometry_epoch`
      - `text_session`
      - `rdr_sfc_handle`
      - `present_in_flight`
      - `retained_state`
    - surviving methods:
      - `syncSurfaceLayout(layout: SurfaceLayout) void`
      - `workState(bootstrap_surface: bool) WorkState`
      - `setHostCursorCadence(cadence: *const HostCursorCadence) bool`
      - `prepare(vt_surface: *const c.HowlVtSurfaceResult) PrepareResult`
      - `preparedUpload(upload: *PreparedUpload) bool`
      - `submit(execution: *const c.HowlRenderSubmitExecution, result: *c.HowlRenderSubmitResult) SubmitResult`
      - `notePresentSubmitted(snapshot_seq: u64, token: u64) void`
      - `completePresent(token: u64) ?u64`
      - `presentPending() bool`
    - exact seam: no source retention, no prepare queue, no submitted-base cursor policy
  - `howl-linux-host/src/terminal/surface.zig`
    - surviving role: host cursor-policy owner and render-call orchestrator
    - surviving cursor-policy fields:
      - `cursor_blink`
      - `cursor_position_changed_by_client_at_ms`
      - `cursor_source_row`
      - `cursor_source_col`
      - `cursor_source_rows`
      - `cursor_source_cols`
      - `cursor_source_visible`
      - `cursor_source_blink`
      - `cursor_source_has_shape`
      - `cursor_source_shape`
      - `cursor_text_blinking`
      - `cursor_render`
      - `cursor_trail_started_ns`
    - surviving methods:
      - `cursorFacts(now_ns: u64) CursorFacts`
      - `consumeCursorFacts(facts: CursorFacts) bool`
      - `computeCursorRender(now_ns: u64, focused: bool) render_retained.HostCursorCadence`
      - `notePublishedCursorSource(cursor: vt_c.HowlVtCursor, cells: []const vt_c.HowlVtSurfaceCell, now_ns: u64) void`
      - `renderTurn() TurnResult`
      - `completePresent(token: u64) void`
    - exact seam: this file computes the host cadence payload and passes it to `render_retained.State.setHostCursorCadence`, then drives prepare/submit/present through `render_retained.State`
- Exact latest-source / prepare / prepared / submitted data-flow handoff:
  - `c/prepare_request.zig:takePrepareRequest` calls `render_session.TextSessionOwner.ingestPublishedSource(visible.*)`.
  - `ingestPublishedSource` copies the incoming `HowlVtSurfaceResult` through `publication.ownedSourceFromSurfaceResult`, applies the current host cursor cadence fields onto that retained source, stores it into `latest_source`, derives `prepare_request`, and returns either that request or `null`.
  - `c/prepared_surface.zig:prepareHandle` calls `render_session.TextSessionOwner.prepareHandle(token)`.
  - `prepareHandle` consumes `prepare_request`, prepares the retained latest source, creates exactly one `prepared_candidate`, and exposes it through `surface/handle.zig`.
  - `c/submission.zig:takeSubmitHandle` calls `render_session.TextSessionOwner.takeSubmitHandle()`.
  - `takeSubmitHandle` returns only the current `prepared_candidate` after validating it against `submitted_surface`.
  - `c/submission.zig:submitHandle` calls `render_session.TextSessionOwner.submitPreparedHandle(prepared, execution)`.
  - `submitPreparedHandle` submits only `prepared_candidate`, updates `submitted_surface` on success, clears `prepared_candidate`, clears `prepare_request`, and leaves `latest_source` as the retained latest published source.
- Exact stale-prepared invalidation semantics:
  - `render_session.TextSessionOwner` is the only owner that may invalidate a stale prepared candidate.
  - If `ingestPublishedSource` receives a snapshot whose `snapshot_seq` differs from `prepared_candidate.preparedSurfaceToken().token.snapshot_seq`, it must:
    - clear `rdr_sfc_handle`
    - release the old prepared handle through `surface/handle.zig`
    - set `prepared_candidate = null`
    - preserve `latest_source`
    - recompute `prepare_request` from `latest_source`
  - `submitted_surface` remains the only owner that may reject a prepared candidate for submitted-base mismatch.
  - No other file may carry stale-prepared policy.
- Exact single live prepared-candidate rule:
  - At most one `prepared_candidate` may exist at once.
  - `prepareHandle` may replace a null candidate only.
  - A newer source invalidates the old candidate before any new candidate is prepared.
  - `submitPreparedHandle` clears the candidate on every successful submit.
- Exact surviving `howl_render` host/render API after replacement:
  - Surviving calls:
    - `howl_render_text_session_init`
    - `howl_render_text_session_deinit`
    - `howl_render_text_session_set_font_size_px`
    - `howl_render_text_session_set_font_path`
    - `howl_render_text_session_set_fallback_font_paths`
    - `howl_render_text_session_set_cursor_cadence`
    - `howl_render_text_session_is_valid_font`
    - `howl_render_text_session_derive_layout`
    - `howl_render_text_session_sync_geometry`
    - `howl_render_text_session_take_prepare_request`
    - `howl_render_text_session_prepare_handle`
    - `howl_render_text_session_take_submit_handle`
    - `howl_render_text_session_submit_handle`
    - `howl_render_text_session_work_state`
    - `howl_render_rdr_sfc_release`
    - `howl_render_rdr_sfc_describe`
    - `howl_render_rdr_sfc_render_surface`
  - Removed calls:
    - `howl_render_text_session_set_cursor_blink_visible`
    - `howl_render_text_session_accept_submitted`
    - `howl_render_text_session_submit`
  - Removed public ABI type:
    - `HowlRenderPreparedSurfaceToken`
  - Exact host usage after rewrite:
    - `howl-linux-host/src/terminal/surface.zig` may call only:
      - `howl_render_text_session_set_cursor_cadence`
      - `howl_render_text_session_take_prepare_request`
      - `howl_render_text_session_prepare_handle`
      - `howl_render_rdr_sfc_describe`
      - `howl_render_rdr_sfc_render_surface`
      - `howl_render_text_session_take_submit_handle`
      - `howl_render_text_session_submit_handle`
      - `howl_render_text_session_work_state`
    - `howl-linux-host/src/terminal/surface.zig` must not call any removed render ABI.
- Exact surviving host-facing consequence contract across `render_retained.zig`, `surface.zig`, and `vt_surface.zig`:
  - `vt_surface.zig` exports `HowlVtSurfaceResult` and copies cursor facts into host-owned `term.vt_state` only.
  - `surface.zig` owns host cursor policy and produces one `render_retained.HostCursorCadence` payload containing:
    - `focused`
    - `cursor_opacity`
    - `text_blink_opacity`
    - `effective_shape`
    - `cursor_color`
    - `cursor_text_color`
    - `cursor_trail_color`
    - `cursor_beam_thickness`
    - `cursor_underline_thickness`
    - `cursor_trail_count`
    - `cursor_trail_rects`
  - `render_retained.zig` is translation-only and may forward only that payload through `setHostCursorCadence`, plus retained host prepare/submit/present bookkeeping.
  - No retained latest source, prepare queue, or submitted-base cursor policy may survive in host files.
- Exact work-state semantics after replacement:
  - exact owner computing work-state booleans: `howl-render/src/render_session.zig:TextSessionOwner.workState()`
  - `source_pending` means `latest_source != null`, `prepare_request == null`, `prepared_candidate == null`, and `latest_source.snapshot_seq` is not already the submitted base.
  - `prepare_pending` means `prepare_request != null`.
  - `submit_pending` means `prepared_candidate != null`.
- Slice-2 change authority:
  - coder may edit any file required to land the exact slice-2 replacement shape and make the required proof surface pass
  - coder must fill structurally coupled gaps discovered during implementation, including ABI tests, C-boundary tests, imports, build wiring, and host/render call sites
  - coder must not broaden past the retained render source/prepare/submit spine, the host submission seam, and proof files directly coupled to that spine
- Required delete/replace focus:
  - delete or replace `howl-render/src/vt_publication/prepare_queue.zig:22-198,288-305`
  - delete or replace `howl-render/src/vt_publication/source_slot.zig:152-306,461-494,495-525,545-559`
  - delete or replace `howl-render/src/render_session.zig:381-415,499-504,586-666,854-879`
  - delete or replace `howl-linux-host/src/terminal/render_retained.zig:63-93,191-194`
  - delete `howl-render/src/vt_publication/prepare_queue.zig` outright
  - delete `howl-render/src/vt_publication/source_slot.zig` outright
  - rewrite `render_session.zig` so the surviving retained-session owner directly replaces both deleted owners
  - carry every structurally coupled retained-render contract through prepare, prepared-handle, submit, work-state, exported ABI, and host-owned submission consumers so no wrapper preserves the old cursor spine by inertia
- Exact proof roots:
  - `howl-render/src/vt_publication/publication.zig` inline tests
  - `howl-render/src/vt_publication/damage.zig` inline tests
  - `howl-render/src/vt_publication/cursor.zig` inline tests
  - `howl-render/src/vt_publication/text_input.zig` inline tests
  - `howl-render/src/c/text_session.zig` inline tests
  - `howl-render/src/c/prepare_request.zig` inline tests
  - `howl-render/src/c/prepared_surface.zig` inline tests
  - `howl-render/src/c/submission.zig` inline tests
  - `howl-render/src/c/work_state.zig` inline tests
  - `howl-render/src/render_session.zig` inline tests
  - `howl-render/src/submitted_surface.zig` inline tests
  - `howl-render/src/surface/handle.zig` inline tests
  - `howl-render/src/surface/prepared_surface.zig` inline tests
  - `howl-render/src/test_abi.zig`
  - `howl-linux-host/src/terminal/render_retained.zig` inline tests
  - `howl-linux-host/src/terminal/term.zig` inline tests
  - `howl-linux-host/src/terminal/vt_surface.zig` inline tests
  - `howl-linux-host/src/terminal/surface_test.zig`
- Stop conditions:
  - stop if the replacement owner map above is not what the diff actually lands on
  - stop if `prepare_queue.zig` or `source_slot.zig` still survive after the replacement
  - stop if `render_session.zig` does not land the exact surviving fields and methods named above
  - stop if more than one prepared candidate can remain live at once
  - stop if stale-prepared invalidation is owned anywhere except the retained-session owner plus `submitted_surface` base validation
  - stop if the host->render call chain differs from the exact surviving call chain named above
  - stop if any of the disappearing ABI calls or types above are kept alive for convenience rather than because the user explicitly wants that public surface
  - stop if the retained cursor pipeline survives mainly because wrapper contracts were preserved around it instead of being replaced at the real owner boundary
  - stop if any remaining cursor-retention owner in the retained render spine is still `NO KITTY SOURCE` without either deletion, replacement, or explicit user-approved product-boundary justification
  - stop if host `render_retained` or render-session state continues to own cursor policy broader than the minimal ABI-forced consequence surface described above
  - stop if the slice broadens past the retained render source/prepare/submit and host submission spine into unrelated renderer architecture work

### Slice 3: Delete Howl-Owned Host Cursor Event/Cadence Policy

- Slice type: deletion-driven
- Goal:
  - remove host cursor event/cadence orchestration that is still Howl-owned cursor policy rather than a minimal host consequence model pressured by Kitty
  - collapse the event-loop cursor policy surface to the smallest necessary host seam
- Exact files:
  - `howl-linux-host/src/event.zig`
  - `howl-linux-host/src/terminal/surface.zig`
  - `howl-linux-host/src/terminal/cursor_blink.zig`
  - `howl-linux-host/src/terminal/render_retained.zig`
  - `howl-linux-host/src/terminal/surface_test.zig`
- Required delete/replace focus:
  - delete or replace `howl-linux-host/src/event.zig:52,72,89,194-202,240,255,263,273,631,653,668,684,692-764`
  - delete or replace any remaining host-side cursor policy in `howl-linux-host/src/terminal/surface.zig:155-167,398-425,779-906` that cannot be traced to Kitty or to a forced host-only cadence obligation
- Exact proof roots:
  - `howl-linux-host/src/terminal/surface_test.zig`
  - `howl-linux-host/src/terminal/vt_surface.zig` inline tests
  - `howl-linux-host/src/event.zig` tests
  - `howl-linux-host/src/terminal/cursor_blink.zig` inline tests
- Stop conditions:
  - stop if the host event loop still owns cursor policy beyond wake/wait/present obligations that the host boundary truly forces
  - stop if cursor wait/redraw choreography remains Howl-invented without explicit Kitty pressure or explicit user override
  - stop if host-side cursor blink/trail policy survives as broad local machinery instead of the smallest accountable host owner surface

### Slice 4: Delete Or Quarantine Unbacked Cursor Diagnostics And Simulation Code

- Slice type: deletion-driven
- Goal:
  - remove or explicitly quarantine remaining VT cursor diagnostics/simulation code that has no Kitty source and only preserves Howl-owned cursor accounting
- Exact files:
  - `howl-vt/simulation/protocol.zig`
  - `howl-vt/simulation/scrollback.zig`
  - any curated test roots that still rely on those cursor diagnostics
- Required delete/replace focus:
  - delete or replace `howl-vt/simulation/protocol.zig:155-156,387-390,415-416`
  - delete or replace `howl-vt/simulation/scrollback.zig:46-47,257-267,370-371,380-406`
- Exact proof roots:
  - simulation or diagnostic roots that still consume cursor row/col/shape snapshots
  - any package root that would fail if these cursor diagnostics are removed
- Stop conditions:
  - stop if these cursor-only diagnostics remain merely because they are convenient local tooling
  - stop if deleting them exposes hidden runtime cursor dependencies that should instead live in accountable proof roots

### Slice 5: Port Kitty Multiple Cursors End To End Or Escalate For User Override

- Slice type: missing-port-driven
- Goal:
  - finish the still-missing Kitty multiple-cursor port from VT protocol noun through shipped VT surface and render consumption
  - if the product intentionally refuses multiple cursors, force an explicit user override receipt against the original sprint goal
- Exact files:
  - `howl-vt/src/vocabulary.zig`
  - `howl-vt/src/kitty/apply.zig`
  - `howl-vt/src/ffi/surface.zig`
  - `howl-vt/include/howl_vt.h`
  - `howl-vt/test/abi.zig`
  - `howl-render/src/vt_publication/abi.zig`
  - `howl-render/src/vt_publication/publication.zig`
  - `howl-render/src/vt_publication/source_slot.zig`
  - `howl-render/src/vt_publication/cursor.zig`
  - `howl-render/src/text/scene_rects.zig`
  - `howl-linux-host/src/terminal/vt_surface.zig`
  - `howl-linux-host/src/terminal/surface.zig`
- Missing-port basis:
  - protocol noun still exists in `howl-vt/src/vocabulary.zig:193-195`
  - VT export still hard-codes no extra cursors in `howl-vt/src/ffi/surface.zig:252-253`
  - render can consume extras in `howl-render/src/vt_publication/cursor.zig:173-202`
- Exact proof roots:
  - `howl-vt/test/abi.zig`
  - `howl-render/src/vt_publication/abi.zig` inline tests
  - `howl-render/src/vt_publication/publication.zig` inline tests
  - `howl-render/src/vt_publication/source_slot.zig` inline tests
  - `howl-render/src/vt_publication/cursor.zig` inline tests
  - `howl-render/src/text/scene_rects.zig` inline tests
  - `howl-linux-host/src/terminal/vt_surface.zig` inline tests
  - `howl-linux-host/src/terminal/surface_test.zig`
- Stop conditions:
  - stop if multiple cursors remain intentionally absent without an explicit user override receipt
  - stop if VT still exports `extra_cursor_count = 0` by construction after this slice
  - stop if render keeps extra-cursor consumption code without a matching VT producer or a removal decision

### Slice 6: Port Full Kitty Cursor Replay/Export Shape As One Coherent Owner Or Escalate

- Slice type: missing-port-driven
- Goal:
  - finish the still-partial Kitty cursor replay/export behavior as one coherent Howl owner
  - stop treating partial reports as completion of the full Kitty cursor export surface
- Exact files:
  - `howl-vt/src/report.zig`
  - `howl-vt/src/terminal.zig`
  - `howl-vt/src/ffi/surface.zig`
  - `howl-vt/include/howl_vt.h`
  - `howl-vt/test/unit/terminal_modes_test.zig`
  - `howl-vt/test/unit/terminal_cursor_test.zig`
  - `howl-vt/test/abi.zig`
- Missing-port basis:
  - only partial pieces currently exist in `howl-vt/src/report.zig:130-170,203-210`
  - the original sprint never established one coherent owner equivalent to Kitty cursor replay/export behavior
- Exact proof roots:
  - `howl-vt/test/unit/terminal_modes_test.zig`
  - `howl-vt/test/unit/terminal_cursor_test.zig`
  - `howl-vt/test/abi.zig`
  - `howl-vt/src/report.zig` inline tests
- Stop conditions:
  - stop if the product boundary intentionally replaces part of Kitty replay/export behavior and no explicit user decision documents that replacement
  - stop if cursor export remains fragmented across unrelated report helpers without one accountable owner

### Slice 7: Final Structural Review Of Remaining Cursor Owners Against Kitty Entirety Goal

- Slice type: deletion-driven plus missing-port-driven closeout
- Goal:
  - prove no remaining cursor-touching Howl code survives without either exact Kitty backing or an explicit user-approved override
  - close the amended sprint only after every remaining cursor owner passes the line-map standard
- Exact files:
  - `research/2026-06-15-cursor-kitty-line-map.md`
  - `research/2026-06-15-cursor-kitty-amended-slices.md`
  - `sprints/current.txt`
  - `loops/cursor-kitty-full-sprint.txt`
- Exact proof roots:
  - full line-map rereview against current tree
  - `zig build test:unit` in `howl-vt`
  - `zig build test:abi` in `howl-vt`
  - `zig build test` in `howl-render`
  - `timeout 300s zig build test:unit` in `howl-linux-host`
- Stop conditions:
  - stop if any remaining cursor-touching line is not mapped to exact Kitty lines or explicit `NO KITTY SOURCE`
  - stop if any `NO KITTY SOURCE` cursor code remains without being either deleted, replaced, or explicitly user-approved as a Howl invention
  - stop if the sprint still only achieves partial Kitty-shaped behavior instead of the full stated goal

## Reviewer Bottom Line

- The original sprint should have split into a deletion campaign and a missing-port campaign instead of stopping at behavior-level parity.
- The amended execution shape above does not shrink scope.
- Without these amended slices, the cursor work remains incomplete against the original user goal.
