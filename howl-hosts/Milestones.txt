Pre-howl-term
    1) copy IDE metadata + ops scripts from zide for iteration tooling parity
    2) bring up empty window + iteration run/deploy/log checks
    3) bring up GPU lifecycle + full-window blank GL/GLES frame
    4) android specific platform weakness work
    4.1) bring over zide side_bar + assist_bar
    4.2) bring over zide userland -> ps howl-pm was set up for howl-android last night before I accidentally nuked the hosts
