name: howl_flutter
description: Disposable Flutter pressure client for the Howl shared-session wire.
publish_to: none
version: 0.1.6-dev+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: false
